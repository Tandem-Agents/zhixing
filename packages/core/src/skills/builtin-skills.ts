/**
 * 内置能力的内容 —— 与登记处机制(builtin.ts)分离:机制恒定,能力是内容。
 * 新增内化能力 = 在此加一份 def(+ 随能力登记关联工具),机制零改动。
 *
 * 内容纪律(创建技能这份方法的写作要求,改动时守住):
 *   - 教"判据"而非"流程"——判断交给模型,这里给它无从自知的产品约定
 *   - 提议有边界、保存须明确同意、交代要诚实 —— 三条产品护栏全在文字里,
 *     与 save_skill 工具的系统护栏(脱敏 / 落位 / 格式 / 索引)双层互补
 */

import type { BuiltinSkillDef } from "./builtin.js";

/** 「提炼技能」—— 能力内化机制的首个承载:把对话里的做法沉淀为可复用技能。 */
const DISTILL_SKILL: BuiltinSkillDef = {
  name: "提炼技能",
  description:
    "用户想把一套做法存成技能(说『存成技能』『记下来以后这么做』),或刚一起验证走通一套值得复用的做法时,加载本方法来起草、打磨并保存技能",
  modes: ["main", "work"],
  tools: ["save_skill"],
  body: `# 提炼技能的方法

把用户的一套做法收成一个以后能复用的技能。你来起草和落笔,用户只做确认和微调——全程在对话里完成,没有表单、没有切换。

## 何时提议

只在两种时机主动提议"要不要把这套存成技能":

1. 用户显式要求(说"存下来"、"记住这个做法"等);
2. 你们刚把一件事做对、且这套做法明显会再用(有用户特定的约定或踩过的坑,不是一次性操作)。

只提议、绝不自动保存。用户拒绝后,同一件事不再重提。

## 起草前:说清"收自哪件事"

动笔前先用一句话说明你要收的是哪段做法(比如"收自:刚才配置生产部署的流程")——用户一眼能看出你理解对没有,猜错了他一句话就能纠正。

## 怎么写好一个技能

- **只留特定性,丢掉通用步骤**:有价值的是用户的特定约定、这次踩过的坑、验证过的最优路径。通用的"怎么用 git"这类内容你本来就会,写进去只是噪音。
- **description 决定它以后能不能被想起来**:以"什么时候该用"为导向写(如"要把服务部署到生产环境时"),不要写成内容摘要。
- **正文短而准**:几条到十几条,每条是一个可执行的判据或步骤。
- **mode 按当前场景**:在工作场景里收的归 work,日常对话收的归 main;用户可以改。

## 打磨

把草稿直接贴在对话里(markdown),请用户用大白话指哪改哪。改完按幅度贴回:小改只贴变化的部分,大改重贴全文。

## 保存

- **必须拿到用户明确同意才调 save_skill**("存吧"、"就这样"、"保存");意图不明确就问一句。
- 用户要改已有技能("把那个技能补一条")时,同名保存即更新——改之前确认是哪个技能,改完同样要明确同意。

## 存好之后:如实交代

按 save_skill 的返回如实告诉用户:

- 存好了,打 \`/<技能名>\` 随时唤起;以后聊到相关的事(说出具体场景)也会被检索到。不要说"我会自动想起它"——做不到的不承诺。
- 如果返回里脱敏计数大于零,告诉用户:"对话里有 N 处密钥已自动抹掉,不会写进技能。"没抹就不提。`,
};

/** 全部内置能力 def —— 登记处(builtin.ts)从此组装注册集。 */
export const BUILTIN_SKILL_DEFS: readonly BuiltinSkillDef[] = [DISTILL_SKILL];
