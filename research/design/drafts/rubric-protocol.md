# Rubric（推进准则）协议规格

<!-- ══════════════════════════ 文档写作规约 · 请勿删除 ══════════════════════════ -->
> **本文是执行规格(execution spec),不是修订日志。**
> **只写**:当前生效的协议结构与语义、决策的"为什么"、与真实代码的对接点(精确到文件:符号)。
> **不写**(协作者修订时一并清理):版本号 / 修订日期 / "最后更新";"vX vs vY" 对比;"改了什么 / 废案"叙事。
> **演化方式**:设计变化时**原地修改**,不追加修订段。历史留给 `git log`。
<!-- ═════════════════════════════════════════════════════════════════════════ -->

> **需求依据**:[task-advancement-rubric-architecture.md](./task-advancement-rubric-architecture.md) §需求区、[requirement-backlog.md](./requirement-backlog.md)「任务推进闭环（Rubric 推进准则）」条目。
> **父规格**:[task-advancement-rubric-architecture.md](./task-advancement-rubric-architecture.md)（任务推进闭环使用 Rubric；本文只定义 Rubric 协议本身）。
> **事实依据**:Rubric 机制尚未落地代码；落地点按本文「归属声明」执行。对接到真实代码后，补充精确 `文件:符号`。

## 〇、定位与范围

Rubric 是推进侧使用的一等协议资产，与 Skill、Rule 同级但职责不同：

- **Skill**：教执行侧怎么做事。
- **Rule**：约束 Agent 不能 / 必须怎样。
- **Rubric**：交给推进侧判断任务是否完成、未完成如何续推、何时退出。

Rubric 不是执行方案，也不是运行中每轮协商的草稿。它是在任务第一次 run 前经用户确认的任务契约；一旦进入推进流程，推进侧按该 Rubric 自动验收和续推。

本文只定义 Rubric 作为协议资产的格式、字段语义、运行约束和校验要求。

- **本文范围**:Rubric 协议本身（身份字段、触发描述、content 结构、运行契约、校验清单、扩展原则）。
- **不在范围**:推进侧怎么创建任务闭环、怎么触发主 Agent、怎么持久化推进侧上下文、Rubric 库的具体 Store / 索引实现、用户确认交互细节。这些归属任务推进闭环架构或后续 Rubric 系统实现规格。
- **当前强制最小闭环**:只强制“通过标准”和“未通过时的处理”两块内容；“证据要求”是一等可选内容，有客观信号的任务应尽量写入；退出边界、优先级、变量定义、版本/来源等先作为扩展点保留。

## 一、协议目标

Rubric 当前先解决两件核心事：

1. **什么叫完成**：推进侧依据什么标准判断任务可以结束。
2. **没完成怎么推**：推进侧依据什么内容代替用户回复执行侧，触发下一轮 run。

协议必须开放、可扩展、可沉淀复用；但当前不追求一次性覆盖所有任务形态，先把最小闭环做稳。

## 二、基础结构

每个保存态 Rubric 至少由稳定身份和三部分内容组成：

```text
id
title
description
content
```

落盘形态后置，但协议层先固定语义：

- `id` 是 Rubric 的稳定资产身份。
- `title` 是展示和引用名。
- `description` 是场景命中描述，决定推进侧什么时候应该用它。
- `content` 是推进侧实际执行验收和续推的契约正文。

### id

Rubric 的稳定身份字段，用于 Store、索引、加载、归档和后续编辑。

要求：

- 首次保存时由系统生成并写入。
- 一旦写入，不随 `title` 后续编辑而变化。
- 用户一般不需要手写；旧 Rubric 缺少 `id` 时允许按 `title` 兼容推导，但新保存的 Rubric 必须固化 `id`。

### title

Rubric 的人类可读名称，用于用户确认、列表展示和引用。

要求：

- 简短、稳定、可复用。
- 表达场景，不表达某一次具体任务。

示例：

- `代码开发完成验收`
- `需求收敛推进`
- `文档审查完成验收`

### description

Rubric 的适用场景描述，用于推进侧按任务语义检索和命中。

要求：

- 写清楚“什么时候使用这份 Rubric”。
- 不写具体执行步骤。
- 可以包含反例，避免误命中。

### content

Rubric 的核心内容。当前至少必含两块，并可包含一等可选的证据要求：

1. **通过标准**
2. **未通过时的处理**
3. **证据要求**（可选；有客观信号的任务应尽量写入）

## 三、content: 通过标准

通过标准定义“什么情况下任务可以结束”。

要求：

- 面向验收，不面向执行。
- 尽量可观察、可核对、可引用证据。
- 可以是多个条件；推进侧必须按条件逐项判断。

结构建议：

```text
通过标准：
- <条件 1>
- <条件 2>
- <条件 3>
```

示例：

```text
通过标准：
- 用户提出的核心需求已经被明确回应或落地。
- 必要的文件修改已经完成，并能指出修改位置。
- 需要验证的场景已经验证，或明确说明无法验证的原因。
- 没有遗留与本任务目标直接冲突的问题。
```

## 四、content: 证据要求

证据要求定义“推进侧验收时应该独立核对哪些事实”。它不是执行步骤，也不是让执行侧自述，而是给推进侧裁判使用的取证清单。

证据要求是可选字段；写作、讨论、决策类任务可能没有稳定客观证据。但一旦任务存在文件、差异、测试、构建、日志、产物状态等可观察信号，Rubric 草案应尽量写入证据要求，供用户确认。

要求：

- 面向验收取证，不面向执行操作。
- 优先写推进侧可只读核验的客观产物。
- 可以声明“需要执行侧补充的证据”，但不能把执行侧自述等同于客观证据。
- 无客观证据时可以省略，但推进侧验收应更保守。

结构建议：

```text
证据要求：
- <需要核对的证据 1>
- <需要核对的证据 2>
```

示例：

```text
证据要求：
- 查看相关文件 diff，确认修改覆盖需求点。
- 查看测试或构建结果，确认没有直接回归。
- 若无法运行验证，执行侧必须说明阻塞原因。
```

## 五、content: 未通过时的处理

未通过时的处理定义“如果验收没过，推进侧应该怎么代替用户回复执行侧”。

这是用户参与的核心承载点：推进侧不能自由发挥，必须使用 Rubric 中用户确认过的推进内容。允许填入事实变量，但不能擅自改写推进意图。

要求：

- 写清楚未通过时给执行侧的回复内容。
- 回复内容是固定 / 非随机的任务契约内容。
- 可以包含占位变量，用于填充本轮事实。

结构建议：

```text
未通过时的处理：
- 场景：<未通过场景>
  回复：<给执行侧的固定推进内容，可带变量>
```

示例：

```text
未通过时的处理：
- 场景：仍有验收项未满足
  回复：当前任务还未达到验收标准。请继续处理以下未满足项：{missing_items}。完成后请给出对应证据。

- 场景：缺少验证证据
  回复：当前结果缺少可核对的验证证据。请补充验证过程、结果，或说明无法验证的具体原因。
```

## 六、运行契约

推进侧使用 Rubric 时必须遵守以下契约：

1. **先验收**:每轮 run 后先按“通过标准”逐项判断。
2. **先核证据**:存在证据要求时，推进侧必须优先独立核验证据，再做判断。
3. **通过即结束**:验收通过时结束推进流程，不再向执行侧发送代理回复。
4. **未通过才续推**:验收未通过时，匹配“未通过时的处理”中的场景。
5. **回复受限**:代理回复必须使用 Rubric 中用户确认过的固定 / 非随机推进内容。
6. **变量只填事实**:允许填入本轮事实变量，但不得借变量改写推进意图。
7. **缺口不自由发挥**:当前情况无法被 Rubric 覆盖时，不得擅自发明新推进策略；由任务推进闭环的退出 / 风险机制处理。

## 七、归属声明

Rubric 是可沉淀、可复用、可被用户长期维护的一等资产。具体 Store / 索引 / 检索实现不在本文展开，但归属必须满足：

- **资产管线**:Rubric 的落盘、读取、索引和用户管理应进入独立 Rubric 资产管线，不能混入普通对话历史。
- **渐进披露**:推进侧常驻上下文应只放 Rubric 索引或候选摘要；命中后再加载确认所需的 Rubric 正文，避免无界撑大上下文。
- **场景检索**:`description` 是命中依据的核心字段，应像 Skill 一样以“什么时候使用”为导向编写，而不是写成内容摘要。
- **安全与脱敏**:Rubric 是用户可长期复用、未来可能分享的资产；保存前应复用或对齐 Skill 资产管线的凭证脱敏、边界校验和来源标记纪律。
- **执行分工**:Rubric Store / 索引归 Rubric 系统实现；推进侧运行器只消费已确认 Rubric，不在运行中修改 Rubric。

## 八、扩展点

当前保存态强制 `id / title / description / content`，其中草案阶段可不带 `id`，由 Store 首次保存时生成并固化；content 中强制两块必含核心内容，证据要求是一等可选内容。后续可以在不破坏旧 Rubric 的前提下扩展：

- **退出边界**：死胡同、重复失败、风险、成本、需要用户裁决等停止条件。
- **优先级**：多个未通过场景同时命中时的处理顺序。
- **变量定义**：`{missing_items}`、`{evidence}` 等占位变量的来源和格式。
- **版本信息**：Rubric 的版本、来源、最后确认时间、适用范围。

扩展原则：新增字段只能增强表达力，不能改变已确认 Rubric 的原有语义；旧 Rubric 在新协议下必须仍可解释。

## 九、校验清单

每份 Rubric 发布或保存前至少检查：

- 有 `title`，且标题表达场景，不表达某一次具体任务。
- 有 `description`，且描述“什么时候使用”，不是内容摘要。
- 有 `content`。
- `content` 中有“通过标准”。
- `content` 中有“未通过时的处理”。
- 每条通过标准面向验收，不写执行步骤。
- 如有证据要求，证据要求面向推进侧只读取证，不写执行步骤。
- 每条未通过处理包含给执行侧的代理回复内容。
- 代理回复是固定 / 非随机内容；如有变量，变量只承载事实。
- 未把安全权限约束写成 Rubric 职责。
- 未把执行侧 Skill 内容混入 Rubric。

## 十、非目标

- Rubric 不教执行侧如何完成任务，那是 Skill 或执行侧策略的职责。
- Rubric 不承载安全和权限硬约束，那是 Rule 的职责。
- Rubric 不在每轮 run 后重新向用户确认；用户确认发生在第一次 run 前。
- Rubric 不允许推进侧在未授权情况下把固定推进内容改写成另一套推进策略。
