# 待做需求模块灵感池

> **性质**：活文档，只记录尚未落地的需求模块、灵感、思路和待收敛信息。
> **范围约束**：这里只放“还没做、还值得保留”的东西；已经完成、废弃、迁入正式 draft / spec / roadmap 的内容应从本文清理。
> **维护原则**：宁可短、准、可继续追问；不要把本文写成长期规格、实现计划或历史归档。

## 维护规则

1. **只记录待做**：做完、放弃或迁入正式文档后，删除对应条目。
2. **先保留灵感原貌**：早期想法可以粗糙，但要保留触发点、用户价值和未决问题。
3. **不急着架构化**：没有收敛前不强行拆包、定接口、排期或写实现方案。
4. **及时迁出**：某个模块进入认真设计时，新建独立 draft，并在本文移除原条目。
5. **避免历史堆积**：本文不是决策记录；需要保留演进过程时，迁到对应需求确认或架构草稿里。
6. **分隔独立需求**：每个独立需求模块之间用 `---` 分隔，便于后续追加、迁出和整体删除。
7. **新增追加到末尾**：新增需求条目默认追加到「待做需求模块」列表末尾，不插到已有条目前；只有用户明确要求置顶、排序或重排时才调整顺序。

## 记录格式

每个条目建议只写到足够恢复上下文，不追求完整：

```text
### <需求模块名>

- **触发**：
- **用户价值**：
- **灵感 / 思路**：
- **未决问题**：
- **下一步**：
```

## 临时草稿工作台

> 用于暂存一个尚未归纳为明确需求的粗糙想法。这里只放**一个需求**的原始大白话；想法说完后，清空这里，并把内容归纳到下方「待做需求模块」列表。
>
> **阅读原则**：本区是按时间顺序推进的口语思考流——先通读、以最新表述为准；后文已澄清的，别拿前文的早期纠结当矛盾来挑。

```text
以文件化可编排基础设施为基础
主要是为了更新下面的那个需求列表的已有的那个需求点
之前的思路其实也比较清晰了，有一个@符号，如果在行首，@后面跟空格，表示唤醒并发工作；如果在行中，@前面必须得有一个空格，后面必须得有一个空格，表示唤醒并发工作；
如果@后面跟的是名称，那先匹配单个agent，没命中再匹配文件；这是之前的设计想法。
现在决定先把这个需求简化一下，因为这其实是不同的东西，我们先不做那个单个智能体了。或者说这个需求目前不关注单个智能体了，那个东西以后再说，现在和它没有任何关系。
也就是说这个需求点只考虑并发的事情，嗯，唤醒并发的规则和上面一样不变。触发唤醒后，先到达一个分配节点，该节点负责进行不同视角的分配。具体分配什么视角，需依据用户需求（即唤醒并发的消息内容）来决定。
目前的想法是推荐 3 个，最多 5 个，即上限为 5 个。这个限制的处理方式如下：

1. 在分配节点的提示词中明确说明该限制。
2. 在系统层面，如果用户要求使用 7 个，由用户自行决定，但系统最多只处理 5 个。
我们系统有不同档位的模型，你可以先去了解一下。对于这个需求来说，相关的只有两个档位，就是main和 power，Power档位用户不一定配置，它是可选的。
所以如果只有 Main 档位的话，就是就是一个列表，然后列表里面就是 5 个 Main 单位的模型，如果用户配置了 Power，那就以 Main 模型为开始，与 Power 交替。最多 5 个，这样可以从不同视角（由分配节点提供）使用不同档位的模型，让不同模型参与不同视角，尽可能带来不一样的结果，以提高结果质量。
对于常见的场景，可以在分配节点的提示词中提前举几个例子，让它在分配时有所参考：

1. 思考需求的场景，有哪些视角
2. 审查代码的场景，有哪些视角

以文件化可编排基础设施为基础开发，请先判断目前的基础设施是否能够支撑我们的需求：
1. 方向上能否支撑
2. 细节上能否支撑

如果整体方向上能够支撑的话，那就没有大问题。然后在细节上如果不满足这个支撑条件的话，
我是这样想的：不能够为了我们这个需求去定制化改造那个基础设施，而是从基础设施的视角，
往更兼容的方向去支持，但不是定制化的支持，你理解我意思吗？
大概意思就是说，如果机制基础设施不够的话，那么应该把它做得更强大、更兼容。
因为基础设施不是专门为了我们这个需求做的，所以它一定不能专门为了我们这个需求去耦合或者绑死，否则基础设施就变得糟糕了。
而是站在基础设施的视角，往我们这个方向思考，如何能做得更好，去支持我们的场景，但不是专门为了我们这个场景去做的支持。我希望我能表达清楚。
你去看一下基础设施相关的文档，会发现执行计划里后面两个单元都还没做。它的倒数第二个单元好像就是和我们本次的这个有关系，你去确认一下。
我再重复一遍，基础设施是公用的，绝对不能为了我们的需求去耦合或绑死。如果能力不够，应该去完善基础设施，而不是与我们绑死。

这个说明白的话，那我就继续说一下那个并发的流程：
就唤醒的方式上面已经说的很清楚了，接下来就是第一个分配节点。分配节点我其实也说的很明白了啊，就是他负责这个视角的分配啊，最多 5 个啊，建议 3 个，就如果用户没要求的话默认 3 个啊，但是有个上限就是最多 5 个。
分配节点完成后，根据分配的视角，按照模型列表（即前面提到的）查看具体的档位配置情况，我们也有默认情况，main是必有的。
按照这个模型的列表，去装配这个并发的流程。由多个视角呢同时去执行，把这个分配出来的任务，这是第一个并发；
在第一轮并发结束后，每个并发节点拿到其余并发节点的结果。提示词应告知这是其他人的结果，让其判断谁的更优，并结合自身版本与其他人的版本，给出一个更优的最终版本，这是第二轮并发。
第二轮并发会带来很多 Token 的使用，但这是明确的需求，你不用再怀疑这个点。
第二轮并化结束之后，进入下一个单个收敛节点。收敛节点拿到第二轮并发各自的最优版本，然后从各自最优版本中收敛出一个唯一的最终版本。
最后最终版本回到主线，用户就能看到了。

到现在为止，我感觉并发需求就说完了，从唤醒的方式到这个并发的流程，以及并发使用哪些模型，我感觉我说的都比较清晰了，或者说很清晰。
基础是以文件编排基础设施为基础，我再补充说明一下基础设施的情况。基础设施本身确实不是专门为了当前这个需求点做的。
基础设施未来还支持模型临时动态的生成编排文档，对吧？所以这个功能是很强大的。这个功能绝对不能写死，不能写回去，就是功能不能破坏，基础设施的功能不能破坏。
而我们这个需求，其实它的编排流程是死的、固定的。但系统本身是活的、动态的，能支持动态生成编排文档。所以理论上，它必然能够支撑我们本次需求中固定的编排流程，对吧？
所以我们这个流程就按照之前基础设施的规则走，不用写进代码，而是搞一个编排文档。这个文档内置到系统中，作为一个固定的流程文档。未来可能还会有更多，反正这个需求就是输出一个编排文档。
用户触发时，其实就是把这个编码文档拿出来，然后去做任务就行了。未来临时生成动态的那都是其他需求，本次不管。但我理解的是，基础设施本身是动态的、是活的，它能支持我们这个死的、固定的流程。

我再三强调一遍：如果基础设施对我们这个需求支持不好，要站在基础设施的视角把它做得更好，而不是和我们的需求耦合或绑死，一定不能这样做。
```

## 待做需求模块

### 多视角发散收敛

- **本质需求**：复杂、关键、易误判问题需要一种内功能力：临时拉起多个隔离子 agent，从不同视角独立产出、交叉吸收，再收敛出唯一最终版本回主线。中间过程瞬时、不持久化、不污染主线。
- **地基**：以文件化可编排基础设施为基础——本需求的流程是固定的，产物是一份内置编排文档，不写死进代码。基础设施是公用的，绝不为本需求定制耦合；能力不足时（如节点级模型档位）从基础设施视角做通用增强。
- **唤醒**：`@` 行首后跟空格，或行中前后皆空格，后接问题正文即触发并发；`@` 后无正文不触发，避免误伤 `email@x.com`。并发数量写在自然语言问题里。手动 `@` 是第一阶段发起者，未来 AI 自动触发等多种发起方式复用同一机制，注意架构设计。
- **分配节点**：唤醒后先经分配节点，依据问题内容决定视角集合；用户未要求时默认 3 个，上限 5 个（提示词声明 + 系统层强制，超出按 5 处理）。提示词内置常见场景示例（思考需求、审查代码各有哪些视角）作分配参考。
- **模型档位**：main 必有、power 可选。仅 main 时列表全为 main；配置了 power 则以 main 起始与 power 交替。不同档位模型参与不同视角，用视角 × 档位双重多样性提高结果质量。
- **流程**：第一轮并发——各视角基于同一主注意力窗口只读快照独立产出；第二轮并发——每个节点拿到其余节点的结果（提示词告知这是他人结果），判断优劣并融合出自己的最优版本；收敛节点——从各最优版本收敛出唯一最终版本回主线。第二轮的 token 成本是明确接受的需求。
