# 待做需求模块灵感池

> **性质**：活文档，只记录尚未落地的需求模块、灵感、思路和待收敛信息。
> **范围约束**：这里只放“还没做、还值得保留”的东西；已经完成、废弃、迁入正式 draft / spec / roadmap 的内容应从本文清理。
> **维护原则**：宁可短、准、可继续追问；不要把本文写成长期规格、实现计划或历史归档。

## 使用规则

本文固定由「临时草稿工作台」「待做需求模块」「最近完成的模块」「用户使用的提示词」四块组成；迁出或完成需求时，只清理对应块里的条目内容，不删除块标题、说明、文本块格式或提示词。

## 维护规则

1. **只记录待做**：做完、放弃或迁入正式文档后，删除对应条目。
2. **先保留灵感原貌**：早期想法可以粗糙，但要保留触发点、用户价值和未决问题。
3. **不急着架构化**：没有收敛前不强行拆包、定接口、排期或写实现方案。
4. **及时迁出**：某个模块进入认真设计时，新建独立 draft，并在本文移除原条目。
5. **避免历史堆积**：本文不是决策记录；需要保留演进过程时，迁到对应需求确认或架构草稿里。
6. **分隔独立需求**：每个独立需求模块之间用 `---` 分隔，便于后续追加、迁出和整体删除。
7. **新增追加到末尾**：新增需求条目默认追加到「待做需求模块」列表末尾，不插到已有条目前；只有用户明确要求置顶、排序或重排时才调整顺序。

## 记录格式

每个条目建议只写到足够恢复上下文，不追求完整：

```text
### <需求模块名>

- **触发**：
- **用户价值**：
- **灵感 / 思路**：
- **未决问题**：
- **下一步**：
```

## 临时草稿工作台

> 用于暂存一个尚未归纳为明确需求的粗糙想法。这里只放**一个需求**的原始大白话；在需求清晰后用于把内容归纳到下方「待做需求模块」列表。
>
> **阅读原则**：本区是按时间顺序推进的口语思考流——先通读、以最新表述为准；后文已澄清的，别拿前文的早期纠结当矛盾来挑。

```text
暂无。
```

## 待做需求模块

_暂无。_

## 最近完成的模块

- [工作场景管理能力统一、目录管理与智能创建](./workscene-management-architecture.md)

## 用户使用的提示词

```
审核审核标准如下：

  1. 原意表达清晰：必须完全包含并清楚表达临时工作台的设计原意，不能遗漏或导致后续架构设计与原意相反。凝练只是抛弃废话、说清核心，但原意必须准确保留。
  2. 无曲解歪义：不能歪解或曲解我的意思。
  3. 具备可执行性：凝练后的内容需足以开启完整的架构设计，能基于此语言直接架构到可执行阶段。

  如果满足要求，简要回复我；如果不满足，简要告知不满足的内容、原因及修改建议。
```
