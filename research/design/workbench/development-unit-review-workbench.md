# 开发单元审查与修复工作台

> **静态区**：本区记录固定问题、收敛规则和提示词，未经用户明确要求不得修改或清理。

## 一、问题描述及分析

第 8 单元主体开发约用 40 分钟。随后以“修复到本单元没有问题为止，并连续两轮审查无新增问题”为目标进入审查修复，约 3 小时后仍未完成：第一轮无新增问题审查尚未通过，最新修改仍待验证。

实际过程中，先后围绕秘密存储、迁移、并发与崩溃恢复、安全边界、凭据最小投影、启动流程及其消费者发现问题；每发现一处便进行修复和局部验证，再继续审查，后续又发现其他问题并重复该过程。实际工作循环是“审查发现问题 → 立即修复 → 局部验证 → 继续审查”，没有先形成一份完整问题清单再集中处理。

## 二、目标模式协作协议

### 1. 唯一目标与完成条件

在不降低审查深度、测试覆盖或最终质量的前提下，高效完成当前单元的审查与修复：必要影响面达到最优架构、不留已知债务；同一份未修改交付物的全部验收与必要验证通过，并连续两轮完整审查无新增真实问题后立即结束。

后续单元、无关重构和架构未要求的无限防御不在范围内；确需改变边界时先重新确认，禁止静默扩面。

### 2. 执行闭环

| 阶段            | 完成门禁                                                                         | 下一步                                                                                               |
| --------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 边界锁定        | 来源、范围、不变量、验收、必要上下游、排除项、核查对象、交付物指纹和验证计划齐全 | 终审一                                                                                               |
| 终审一 / 终审二 | 整轮只读扫完，问题一次收齐，交付物未变化                                         | 有问题进入问题裁决；零问题且证据缺失进入集成验证；零问题且证据齐全时终审一进入终审二、终审二进入完成 |
| 问题裁决        | 按根因去重；每项写清事实、完整影响面、最优方案、共同验证范围和验收条件           | 集中修复                                                                                             |
| 集中修复        | 全部问题一次修完，共同范围只做一次最小回归；更新交付物、覆盖表与证据状态         | 集成验证                                                                                             |
| 集成验证        | 交付物冻结；全部必要证据有效                                                     | 验证失败回问题裁决；内容改过回终审一；同一指纹的终审一已通过则进终审二                               |
| 完成            | 同一份交付物全部验收与必要验证通过，连续两轮完整审查零新增问题                   | 立即结束                                                                                             |

### 3. 五条核心规则

1. **边界完整**：终审前把架构要求、验收、全部交付文件、跨边界符号、生产端、消费者和测试映射到九类核查面；每面必须有对象或不适用依据，禁止扩面或漏项。
2. **先审完再修**：终审整轮只读，只登记问题；全部扫完后统一裁决。问题按根因合并，一次覆盖生产端、类型组合、消费者、异常终态和测试，再集中修复。
3. **命令必须有证据缺口**：运行测试、构建或门禁前，先在账本登记证明目标与当前缺口、最小命令、具体输入闭包、阶段和成本；执行后记录结果、实耗与输入指纹。输入指纹为命令及其工具/配置、源码、测试和依赖闭包的路径与内容哈希。同一目标已有相同输入的有效证据时禁止重跑。
4. **昂贵验证只跑一次**：修复阶段只做按共同范围合并的最小回归；全部问题修完并冻结交付物后，才按依赖顺序运行静态/类型、直接测试、受影响包、反向依赖、必要构建和 golden/安全门禁，每层同一输入最多一次。失败先归因，无变化不得重跑；仅并行已证明输出隔离且互不争用的命令。
5. **变化精确失效**：交付物变化使两轮审查归零；机械证据仅在本行输入指纹变化时失效。终审二必须重新读取交付物独立判断，不重复有效测试；达到完成条件立即结束。

### 4. 动态区维护

- 动态区是唯一状态源。每次开始、目标续跑或历史压缩后先完整读取本文；阶段转换前写完当前结果，转换后立即更新状态。
- 本文和构建产物不属于交付物；暂存状态不影响指纹。动态区格式固定，只维护字段值和表格内容；已解决问题保留到完成。
- 用户确认完成或开始下一单元时，清空单元字段值、问题行和证据行；九类核查面与两轮终审属于固定骨架，只把待填列复位。第一部分及其他静态内容未经用户要求不得修改。

### 5. Codex 目标模式提示词

```text
/goal 完成当前开发单元的审查与修复：不降低审查深度、测试覆盖或最终质量，在锁定边界内达到最优架构、不留已知债务。

首个动作，以及每次续跑或历史压缩后的首个动作，都是完整读取 `research/design/workbench/development-unit-review-workbench.md`。严格执行其执行闭环和五条核心规则，把动态区作为唯一状态源；门禁未满足不得转换阶段。

按动态区状态持续推进；每次阶段转换前先更新动态区。只有需要用户改变范围、授权高风险动作或作出无法由项目事实确定的产品决策时才暂停，其余情况自主推进。

唯一完成条件：同一份未修改交付物的全部验收与必要验证通过，并连续两轮完整审查无新增真实问题；达到后立即结束，未达到不得宣称完成。
```

---

## 动态区

> 以下标题、说明、字段、表头、九类核查面行和两轮终审行是固定骨架；只维护字段值、单元相关表格行及固定行的待填列。

### 当前状态

- **单元**：第 9 单元（S3）AuthorityCommitLog 与 ArtifactStore
- **架构来源**：`research/design/modules/distributed-runtime/distributed-runtime-charter.md`、`research/design/modules/distributed-runtime/specification.md` 的耐久存储模型、测试验收与执行单元 9
- **当前状态**：完成
- **连续无新增问题轮数**：2 / 2
- **交付物是否冻结**：是
- **交付物文件集**：`packages/core/src/{authority,persistence,protocol,contracts}`、`packages/core/{package.json,tsup.config.ts}`、`packages/mesh/src/{canonical,checkpoint,pairing}.ts` 及摘要测试、`packages/secrets/src/file-lock.ts`、运行时结构测试与金标、包导出及基线脚本
- **当前交付物指纹**：`sha256:1b67d436229ee8d43aff463ca1079ce6c2f71f0ac593249a05ddba4e51a28578`

### 固定边界

- **功能范围**：建立单物理日志/逻辑流、CommitEnvelope 单次 fsync 原子追加、坏尾隔离、投影重放、内容寻址 ArtifactStore、先资产后引用与并发安全的保留/GC 基座。
- **架构不变量**：AuthorityCommitLog 是唯一耐久事实源；跨流事实同一 envelope 原子落盘；投影可按 LSN 重建；内容摘要使用统一规范；日志引用生效前 artifact 必须在场；GC 不得删除已被并发提交引用的资产。
- **验收条件**：WAL 崩溃边界、跨流原子性、投影重建、坏尾隔离、引用在场、孤儿回收与并发提交/GC 竞态测试通过；受影响合同、包测试、结构基线与构建通过。
- **必要上下游**：core 合同与统一摘要原语；mesh 复用统一摘要；secrets 复用共享文件锁；后续 owner/executor 只消费本基座。
- **明确不属于本单元**：不接管现有 run/control/job/delivery 生产路径；不实现后续状态机、派发、CAS、mesh 业务 adapter、语义保留策略或用户资产传输。

### 覆盖与核查

> 覆盖来源包括架构要求、不变量、验收项、交付文件与跨边界符号、生产端、消费者和测试；核查面固定为状态、入口与生产端、消费端与继承面、生命周期、并发与崩溃点、异常路径与终态、安全边界、模块边界、测试与验收。每轮填写“通过：证据”“不适用：依据”或“有问题：编号”。

| 覆盖来源     | 来源项                             | 核查面         | 对象或路径                                                                       | 第一轮结论与证据                                                         | 第二轮结论与证据                                                                         |
| ------------ | ---------------------------------- | -------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 单元 9 验收  | envelope/LSN/逻辑流/投影           | 状态           | CommitEnvelope、日志扫描、流过滤与投影 reducer                                   | 通过：精确 v1、摘要、连续 LSN、多流 envelope 及全量/增量投影同值         | 通过：独立复核原子 envelope、连续全局 LSN、同 envelope 记录顺序与游标边界闭合            |
| 单元 9 范围  | put/append/replay/GC               | 入口与生产端   | FileArtifactStore、FileAuthorityCommitLog                                        | 通过：入口规范化、耐久 put/append、有界 replay 与保守 GC 闭合            | 通过：输入快照、逐帧有界扫描、显式物化查询及保守 GC 职责无混用                           |
| 后续单元复用 | owner/executor 共享耐久基座        | 消费端与继承面 | `@zhixing/core/authority` 稳定子路径；当前零生产消费者                         | 通过：稳定子路径与类型导出完整；零外部生产消费者，旧路径唯一             | 通过：公开合同与文件实现边界清晰；当前零生产接管，后续仅经稳定子路径复用                 |
| 耐久存储模型 | 创建、引用、重放、保留、回收       | 生命周期       | artifact put→commit ref→projection/retention→GC                               | 通过：先资产后引用、按 LSN 重放、引用快照后回收；语义保留明确后置        | 通过：写入、引用、重放、保留快照与回收顺序闭合；语义保留仍正确留给后续单元               |
| 单元 9 验收  | append 与 GC 串行、崩溃恢复        | 并发与崩溃点   | 文件锁、frame 写入/fsync、截断、GC 保留快照                                      | 通过：FIFO、原子 claim 发布、artifact→log 单锁序、单 fsync frame 与尾恢复闭合 | 通过：claim 完整落盘后由硬链接原子发布，主锁与 reclaim 锁均只有唯一胜者                  |
| 单元 9 验收  | 坏尾、内段损坏、缺件、内容损坏     | 异常路径与终态 | 隔离目录、fail-closed 错误、日志零半提交                                         | 通过：未完成尾隔离；完整坏帧、LSN 断裂、缺件、损坏与游标超前均拒绝       | 通过：仅物理未完成末帧可隔离；完整损坏、短读、缺件、摘要错与游标超前全部 fail-closed     |
| 耐久存储模型 | 路径、摘要、原型污染、锁归属       | 安全边界       | 严格反序列化、JCS/sha256、内容复核、私有权限、共享锁                             | 通过：精确字段/JCS、B(bytes) 单源、摘要路径、内容复核与 token 锁归属闭合 | 通过：规范化拒绝访问器/稀疏值/原型污染，摘要复核、路径派生和锁代际均无自报信任           |
| S3 执行边界  | core 共享能力、旧路径不接入        | 模块边界       | core authority/protocol/persistence；mesh/secrets 仅复用基础原语；生产消费者扫描 | 通过：core 内聚；mesh/secrets 只复用基础原语；结构门禁零越界             | 通过：core 内聚且依赖单向；mesh/secrets 仅共享摘要与锁，未提前实现后续状态机或接管旧路径 |
| 单元 9 验收  | 最小测试→受影响包→结构门禁→构建 | 测试与验收     | authority 直接测试、contracts、core/mesh/secrets、runtime baseline、全量 build   | 通过：V8 保留子集及 V11–V13 覆盖直接、全包、构建、导出和结构            | 通过：V11–V13 与未失效的 mesh/secrets 子集覆盖崩溃、竞态、全包、构建、导出和结构门禁    |

### 问题清单

> 每个根因只保留一行；“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试；状态只允许“待裁决、待修复、修复中、待验证、已验证”。

| 编号  | 事实与证据                                                                                                                                                                                                          | 根本原因                                                                                           | 完整影响面                                                                                                                                                                                                                                                  | 最优解决方案与验收条件                                                                                                                                                                                                                                                                                                                 | 状态   |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| U9-01 | WAL 仅以 4 字节长度分帧；非法长度一律按坏尾截断，内段长度损坏可静默丢弃其后的已提交 envelope；每次 append 又全量读取并重放日志，累计写入为 O(n²)                                                                   | frame 缺少版本、魔数、头尾一致性校验及已验证尾游标，恢复边界与常态追加共用全量扫描                 | 生产端：append/recover；类型：frame/LSN；消费者：全部投影；终态：内段损坏误降级为可截断尾、长期日志延迟持续增长；测试：头部损坏、逐点截断、跨实例追加与大日志追加                                                                                           | 建立版本化自校验 frame（魔数、版本、长度及补码/尾标记），只允许“校验有效但物理未完成的最后一帧”隔离；其他损坏 fail-closed。锁内维护并核对文件身份、长度和 lastLsn 的已验证尾游标，首次/外部变化才恢复扫描，常态 append O(1)。验收覆盖每个 frame 截断点、内段头/体/尾篡改、跨实例续写、连续追加不全量重扫                             | 已验证 |
| U9-02 | 规格规定 LogicalRecord 内联对象不超过 32 KiB；实现只限制整 envelope 为 16 MiB，超限正文仍可进入 WAL                                                                                                                 | 持久层未把 artifact 分流阈值固化为入口不变量                                                       | 生产端：append；类型：全部 LogicalRecord body；消费者：后续 control/run/job；终态：WAL 膨胀、内存与恢复放大；测试：阈值边界及 artifact 替代路径                                                                                                             | 按规范化 UTF-8 字节对每条 record body 强制 32 KiB 上限，超限 fail-fast 并要求调用方先写 ArtifactStore 后改用 ArtifactRef；保留 envelope 总上限。验收覆盖 32 KiB 边界、多条小记录可同 envelope、超限零落盘                                                                                                                              | 已验证 |
| U9-03 | 新建 ArtifactStore/CommitLog 根目录后只 fsync 根目录内部，没有 fsync 其父目录；首次成功返回后遇系统掉电，整个新根目录项仍可能丢失                                                                                   | 目录创建与目录同步由各存储类零散处理，缺少“耐久创建目录”共享原语和顺序合同                       | 生产端：首次 put/append/quarantine；类型：目录与文件元数据；消费者：重启恢复；终态：已确认首条 artifact/log 事实消失；测试：首次创建各持久化步骤                                                                                                            | 在 core persistence 建唯一耐久目录/目录同步原语：逐层创建缺失目录并同步其父目录，文件/rename 后同步直接父目录；log、artifact 与隔离目录统一使用。以可观测 I/O 顺序测试覆盖首次创建、既有目录与异常中止，平台不支持目录 fsync 时只允许显式受控退化                                                                                      | 已验证 |
| U9-04 | core 已建立`byteDigest` 作为 B(bytes) 单源，但 mesh 的 Checkpoint chunk/artifact 与 PairingOffer keyFingerprint 仍各自直接 createHash；当前值相同，演进时会静默分叉                                               | 只迁移了 D/JCS 消费者，没有按摘要注册表反查并同步全部 B(bytes) 消费者                              | 生产端：checkpoint、pairing；类型：ArtifactRef/chunk digest/keyFingerprint；消费者：恢复与配对验证；终态：算法/编码演进后跨端拒绝；测试：固定向量与跨包同值                                                                                                 | 由`@zhixing/core/protocol` 唯一实现 B(bytes)，mesh canonical 统一转出；只替换注册表明确属于 B 的 checkpoint 与 pairing 调用，TLS/PAKE 内部 SHA 保持独立。固定向量断言 core、mesh 及现有协议字段完全同值，并以 grep/结构门禁禁止协议 B 再次私建                                                                                       | 已验证 |
| U9-05 | core 全量测试中同一 FileAuthorityCommitLog 的 12 个并发 append 有调用在 2 秒内报`lock is busy`；当前每个同进程调用都直接创建/轮询同一文件锁，没有资源内排队                                                       | 文件锁同时承担进程内串行与跨进程互斥；并发调用产生轮询风暴，并把本进程正常队列等待误算为外部锁争用 | 生产端：log append/read/GC、artifact put/reference check/GC；类型组合：同实例并发与 artifact→log 固定锁序；消费者：后续 owner/executor 与 secrets 共享锁；异常终态：正常突发被误拒、失败 Promise 留下未汇合 I/O；测试：FIFO、失败后续行、并发 LSN、GC 竞态 | 为 AuthorityCommitLog 与 ArtifactStore 各建立资源内 FIFO 串行执行器，先在内存队列排队，只有队首获取跨进程文件锁；保持 artifact→log 锁序、外部锁超时和 fail-closed 语义。验收覆盖 FIFO、任务失败不堵后继、并发 append 无丢失/跳号、GC 竞态                                                                                             | 已验证 |
| U9-06 | 同一全量回归中完整 frame 损坏与 GC 竞态测试均触发 Vitest 默认 5 秒超时；两者执行多次真实 file/directory fsync，定向低负载通过、全包并发 I/O 下超时                                                                  | 耐久存储集成测试沿用 CPU 单元测试的默认时间预算，验证目标与运行成本不匹配                          | 生产端：无；类型组合：真实磁盘 fsync + 多步骤故障/竞态场景；消费者：CI 与本地全包验收；异常终态：真实正确性被环境负载误判、失败后临时目录清理竞态；测试：仅高 I/O 场景                                                                                      | 保留真实 fsync，不改全局测试预算；只给本文件的耐久 I/O 场景设置明确上限，并让资源自身更短的锁等待/错误先行暴露。验收要求全包负载下稳定通过，真正死锁仍由资源超时或场景上限失败                                                                                                                                                         | 已验证 |
| U9-07 | 最终结构门禁仅报 golden mismatch；现有金标中`core/authority → core/persistence` import 引用数为 2，本轮两个资源类各新增 SerialTaskQueue 依赖后应为 4，其余构建、导出与行为金标均通过                             | 集中修复改变了已登记的内部拓扑边计数，结构基线尚未同步                                             | 生产端：无行为变化；类型组合：authority 到 persistence 的同包分区依赖；消费者：结构门禁；异常终态：正确交付被旧金标阻断；测试：结构快照精确差异                                                                                                             | 只在显式更新模式生成新结构金标，审查 diff 必须恰为该边`references: 2 → 4`，其他字段零变化；随后普通只读结构测试通过                                                                                                                                                                                                                 | 已验证 |
| U9-08 | `#readAndRecover` 以 `readFile` 一次载入完整 WAL；`rebuildProjection` 与 GC 再经 `readAll` 物化全部 envelope，接口也无法从已持久化 LSN 增量重放。日志是长期 append-only，内存与启动恢复成本会随历史无界增长 | 恢复校验、物化查询和投影重放共用“整文件→整数组”路径，缺少有界 frame 扫描原语与 LSN 游标         | 生产端：首次/外部变化后的 append、read/rebuild/GC；类型组合：frame、LSN、stream 过滤；消费者：后续全部权威投影；异常终态：大日志启动或恢复 OOM、投影每次重复全历史；测试：跨 chunk frame、增量 LSN、坏尾/坏帧及有界读取                                     | 建立单一有界 frame 扫描器，逐帧校验并只保留当前 frame；恢复、lastLsn、投影和 GC 直接消费扫描器，`readAll` 仅作为显式物化便利接口。投影合同增加 `afterLsn`/stream 选项以重放未投影段，并拒绝游标超前。验收覆盖跨读取边界 frame、从 LSN 增量重建与全量同值、坏尾/坏帧不回退，并以可注入随机访问 reader 证明单次读取受 frame 上限约束 | 已验证 |
| U9-09 | `open(lockPath, "wx")` 先公开空文件，随后才写入并同步 owner 记录；若创建者暂停超过 stale 阈值，竞争者可把无效记录当作陈旧锁删除并重新获锁，而原创建者恢复后仍会返回成功                                                                 | 固定锁路径的占位与完整 owner 记录分两步发布，缺少唯一、原子的线性化点                             | 生产端：AuthorityCommitLog、ArtifactStore、SecretStore；类型组合：主锁与 reclaim 锁；消费者：全部日志、GC 与秘密写入；异常终态：双持锁、重复 LSN、已提交 artifact 引用悬空及并发秘密写；测试：发布前暂停、唯一胜者、遗留 claim 清理                 | 先在同目录独占 claim 文件中写完 owner 记录并 fsync/close，再以不覆盖的硬链接原子发布到固定锁路径；`EEXIST` 即竞赛失败，其他错误 fail-closed，禁止退回空占位路径。主锁与 reclaim 锁统一使用该原语，并按 owner 活性与 stale 规则清理崩溃遗留 claim。验收以确定性交错证明发布前固定路径不可见、并发只产生一个胜者、目标记录始终完整，且遗留 claim 不阻塞后续获锁 | 已验证 |

### 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口，输入闭包必须具体到可重复计算指纹；执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口                                      | 最小命令或检查                                                                                                                                 | 输入闭包                                                                           | 阶段 / 成本 / 实耗        | 结果                                                                                                                | 证据输入指纹       | 状态 |
| ---- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------ | ---- |
| V1   | 合同、直接持久化行为与改动文件静态正确性                | contracts typecheck、schema lint、core typecheck、authority 直接测试、改动文件 Biome                                                           | core contracts/protocol/authority/persistence 源码、配置与 authority 测试          | 开发最小 / 低 / 约 30 秒  | 原交付物全部通过；修复已改变输入                                                                                    | `46da778e…e3f7` | 失效 |
| V2   | 受影响包及摘要共享回归                                  | core 全量测试、secrets 全量测试、mesh canonical 定向测试                                                                                       | core/mesh/secrets 源码、测试、包配置及其依赖闭包                                   | 单元收尾 / 中 / 约 1 分钟 | 原交付物全部通过；修复已改变输入                                                                                    | `46da778e…e3f7` | 失效 |
| V2D  | 判断 mesh 全量测试长时运行是否代表失败                  | `pnpm --filter @zhixing/mesh test`                                                                                                           | mesh 全量测试及依赖闭包                                                            | 诊断 / 高 / 304 秒        | 超时前进程持续占用 CPU 且无失败；改用固定向量与直接消费者测试补证，不重复执行                                       | `46da778e…e3f7` | 诊断 |
| V3   | 新模块拓扑、导出与旧生产路径不变                        | 结构金标比较、`pnpm runtime:package-exports`、生产消费者静态扫描                                                                             | 结构测试/金标、导出检查、core authority 导出与 packages 生产源码                   | 单元收尾 / 中 / 约 30 秒  | 原交付物全部通过；修复已改变输入                                                                                    | `46da778e…e3f7` | 失效 |
| V4   | 上游 core 变更后的最终构建                              | `pnpm build`                                                                                                                                 | 全 workspace 生产源码、构建配置与依赖闭包                                          | 最终交付 / 高 / 138 秒    | 原交付物全部通过；修复已改变输入                                                                                    | `46da778e…e3f7` | 失效 |
| V5D  | 判断 workspace lint 失败是否属于本单元                  | `pnpm lint`                                                                                                                                  | workspace lint、安全门禁与全部源码                                                 | 诊断 / 中 / 已执行        | 安全门禁通过；仅未改动的 CLI 测试存在既有临时目录导入违规，改动文件 Biome 已通过                                    | `46da778e…e3f7` | 诊断 |
| V6   | 四项修复的最小直接证据缺失                              | contracts lint/typecheck、core typecheck、authority/WAL/durable-directory 直接测试、mesh canonical/pairing/checkpoint 直接测试、改动文件 Biome | 本轮 core/mesh 源码、直接测试、contract lint 与包配置                              | 修复最小 / 中 / 约 66 秒  | 原修复全部通过；后续修复改变 core authority/persistence 输入                                                        | `65e2fcfd…e953` | 失效 |
| V7   | 新模块/共享原语改变结构金标，需机械刷新后确认仅预期差异 | `pnpm runtime:baseline:update` 后审查结构金标 diff                                                                                           | runtime baseline、结构测试/金标、core/mesh/secrets 导入图                          | 修复收尾 / 中 / 172 秒    | 原交付物通过；后续新增 persistence 内部依赖改变输入                                                                 | `65e2fcfd…e953` | 失效 |
| V8   | 冻结交付物的受影响包全量回归缺失                        | core、secrets、mesh 各全量测试一次                                                                                                             | 冻结后的 core/mesh/secrets 源码、测试、配置与依赖闭包                              | 集成验证 / 高 / 85 秒     | U9-09 改变 secrets 直接消费的共享锁；core/secrets 证据失效并转 V15，mesh 摘要输入未变                              | `3ebe0596…86e1` | 诊断 |
| V9   | 冻结交付物的导出、结构与最终构建证据缺失                | `pnpm build` 后运行 package exports、三项 runtime baseline 测试与生产消费者扫描                                                              | 冻结后的全 workspace 生产源码、构建配置、结构金标及导出脚本                        | 最终交付 / 高 / 178 秒    | 原冻结交付物全部通过；U9-08 改变构建输入                                                                            | `c100ec68…a46e` | 失效 |
| V10  | 同进程 FIFO 与耐久测试预算修复缺少最小直接证据          | 改动文件 Biome、core typecheck、SerialTaskQueue 与 authority 直接测试                                                                          | core authority/persistence 源码、直接测试与包配置                                  | 修复最小 / 中 / 20 秒     | 原冻结交付物全部通过；U9-08 改变 authority 输入                                                                     | `3ebe0596…86e1` | 失效 |
| V11  | U9-08 的有界扫描、增量投影与全部直接消费者尚无机械证据  | 改动文件 Biome、core typecheck、authority-storage 与 wal-frame 直接测试                                                                        | core authority 接口/实现/WAL 扫描器、两份直接测试与 core 配置                      | 修复最小 / 中 / 约 24 秒  | Biome 与 core typecheck 通过；2 文件 13/13，通过有界逐帧读取、截断尾、全量/增量投影同值及游标超前拒绝               | `428bb339…3753` | 有效 |
| V12  | U9-08 改变 core 后缺少受影响包全量回归                  | `pnpm --filter @zhixing/core test`                                                                                                           | 当前冻结 core 源码、全部 core 测试、配置及 workspace 测试依赖                      | 集成验证 / 高 / 52 秒     | 131 文件 2178/2178 通过；U9-09 改变 core 输入                                                                       | `428bb339…3753` | 失效 |
| V13  | 当前冻结交付物缺少最终构建、导出与结构证据              | `pnpm build`，随后 package exports、结构金标只读比较与生产消费者扫描                                                                         | 当前冻结 workspace 生产源码/构建配置、core 导出、结构测试/金标及 packages 生产源码 | 最终交付 / 高 / 约 148 秒 | 原交付物通过；U9-09 新增 persistence 源码并改变构建与结构输入                                                       | `428bb339…3753` | 失效 |
| V14  | U9-09 的原子发布、唯一胜者与全部直接消费者尚无机械证据  | 改动文件 Biome、core typecheck、文件锁/authority 直接测试、secrets 文件锁直接测试                                                             | core 文件锁实现与测试、authority 直接消费者、secrets 文件锁消费者及包配置          | 修复最小 / 中 / 约 35 秒  | Biome、core typecheck 通过；core 2 文件 12/12、secrets 1 文件 5/5 通过                                              | `1b67d436…8578` | 有效 |
| V15  | 共享文件锁改变后 core 与 secrets 全量回归缺失           | core 全量测试一次，随后 secrets 全量测试一次                                                                                                  | 当前冻结 core/secrets 源码、全部测试、配置及依赖闭包                               | 集成验证 / 高 / 约 71 秒  | core 132 文件 2180/2180、secrets 2 文件 18/18 通过                                                                  | `1b67d436…8578` | 有效 |
| V16  | 当前冻结交付物缺少最终构建、导出与结构证据              | `pnpm build`，随后 package exports、结构金标只读比较与生产消费者扫描                                                                         | 当前冻结 workspace 生产源码/构建配置、core 导出、结构测试/金标及 packages 生产源码 | 最终交付 / 高 / 约 149 秒 | 全量构建、package exports、结构金标 1/1 与生产消费者扫描通过                                                        | `1b67d436…8578` | 有效 |

### 终审记录

| 轮次   | 审查侧重                             | 矩阵是否完整 | 新增问题 | 交付物指纹         | 结论                                         |
| ------ | ------------------------------------ | ------------ | -------- | ------------------ | -------------------------------------------- |
| 第一轮 | 需求、架构、功能闭环、状态、回归     | 是           | 0        | `1b67d436…8578` | 通过，进入终审二                             |
| 第二轮 | 并发、崩溃、安全、异常终态、测试盲区 | 是           | 0        | `1b67d436…8578` | 通过；同一冻结交付物连续两轮零新增，单元完成 |
