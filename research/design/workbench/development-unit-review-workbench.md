# 开发单元审查与修复工作台

> **静态区**：本区记录固定问题、收敛规则和提示词，未经用户明确要求不得修改或清理。

## 一、问题描述及分析

第 8 单元主体开发约用 40 分钟。随后以“修复到本单元没有问题为止，并连续两轮审查无新增问题”为目标进入审查修复，约 3 小时后仍未完成：第一轮无新增问题审查尚未通过，最新修改仍待验证。

实际过程中，先后围绕秘密存储、迁移、并发与崩溃恢复、安全边界、凭据最小投影、启动流程及其消费者发现问题；每发现一处便进行修复和局部验证，再继续审查，后续又发现其他问题并重复该过程。实际工作循环是“审查发现问题 → 立即修复 → 局部验证 → 继续审查”，没有先形成一份完整问题清单再集中处理。

## 二、目标模式协作协议

### 1. 唯一目标与完成条件

在不降低审查深度、测试覆盖或最终质量的前提下，高效完成当前单元的审查与修复：必要影响面达到最优架构、不留已知债务；同一份未修改交付物的全部验收与必要验证通过，并连续两轮完整审查无新增真实问题后立即结束。

后续单元、无关重构和架构未要求的无限防御不在范围内；确需改变边界时先重新确认，禁止静默扩面。

连续工作期间，在适当节点以百分比向用户反馈当前单元距最终完成条件的整体收敛进度，不以单轮或单阶段进度代替。

### 2. 执行闭环

| 阶段     | 完成门禁                                                                                                                                                                                                                                                                                                                               | 下一步                                 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 边界锁定 | 来源、范围、不变量、验收、精确交付物、既有证据及关键原语清单齐全                                                                                                                                                                                                                                                                       | 分片判定                               |
| 分片判定 | 只判断是否需要审查分片，不划规模等级；启用时按合同闭包登记边界、依赖、局部验收、输入基线和完整单元必审项，不启用则不增加步骤（见`research/design/workbench/heavy-unit-slicing.md`）                                                                                                                                                  | 问题盘点                               |
| 问题盘点 | 整轮只读；先查已排除问题并执行适用的迟发现教训。启用分片时，同轮只深审未完成切片，新一轮只深审非局部封版切片，随后执行完整单元横向审查；未启用则直接完整审查。关键原语五项检查与九类核查面全部完成，问题一次收齐                                                                                                                       | 有问题进入问题裁决；零问题进入冻结准备 |
| 问题裁决 | 按根因去重；真实问题写清事实、影响面、最优方案和验收条件，非问题写入已排除问题。若真实问题在先前通过后才发现，同轮登记迟发现教训并执行其检测动作。根因为“多执行点合同分叉”时，必须谓词单源化并以声明数据/参数化测试完成全执行点对账；逐点补检查不得通过裁决（见`research/design/principles/single-source-contract-predicates.md`） | 集中修复；若无真实问题则进入冻结准备   |
| 集中修复 | 全部问题一次修完；同根风险反查无遗漏，只做一次合并后的直接验证                                                                                                                                                                                                                                                                         | 冻结准备                               |
| 冻结准备 | 交付物闭包、事实包与最终验证计划完整；计算并冻结交付物指纹                                                                                                                                                                                                                                                                             | 冻结终审                               |
| 冻结终审 | 冻结指纹与事实包；复用或重开排除裁决，并执行适用的迟发现教训。启用分片时按两维状态复用纵向深审，但每轮仍执行完整单元横向审查；两轮其余部分共享机械事实但独立裁决，均零新增                                                                                                                                                             | 最终验证；新增或重开问题则回问题裁决   |
| 最终验证 | 必要 golden 更新 → 单独执行最终全量构建 → 构建成功后并行运行受影响包全测：最多同时运行两个包，任一完成后立即启动下一个，直至全部完成。其余门禁按读写依赖执行一次；全部通过且未修改交付物                                                                                                                                             | 完成；失败需修改则回问题裁决           |
| 完成     | 两轮终审与全部必要验证属于同一份未修改交付物                                                                                                                                                                                                                                                                                           | 立即结束                               |

### 3. 五条核心规则

1. **关键原语查透**：对每个状态变化、耐久写、并发竞争或外部副作用，逐项核对唯一事实源、生效/线性化点、崩溃与竞争插点、全部生产者/消费者、时间与空间上界；存在缓存、索引、增量计算、短路或其他快速路径时，以权威事实源上的完整计算为正确性基准，列明成立与失效条件，并用正反例差分验证等价，无法证明则不得冻结；再映射九类核查面。缺一项，问题盘点不得结束。
2. **先审完再修**：问题盘点整轮只读，只登记问题；全部扫完后按根因合并，一次覆盖生产端、类型组合、消费者、异常终态和测试，再集中修复。
3. **闭包先于冻结**：问题盘点零问题或集中修复完成后，都必须进入冻结准备。交付物文件集中的每个文件或同类组必须在派生产物闭包表中落账，覆盖其 lockfile、golden、schema/快照及结构/导出基线；无派生项须写明依据。存在未归类项、待核查项或未同步项不得冻结，也不得提前运行包级全测、全量构建或最终门禁。
4. **昂贵验证只跑一次**：冻结前将最终验证展开为精确命令、输入闭包、依赖顺序和复用证据；组合命令须先展开，剔除已有同输入证据覆盖或范围外的子项，存在“待确定/待计算”不得冻结。两轮终审零新增后严格按“最终验证”门禁执行，禁止构建与依赖 `dist` 的测试并行；失败先归因再决定最小补证。
5. **指纹与事实包决定收敛**：冻结时把架构边界、文件与指纹、关键原语、跨边界消费者、派生产物映射和有效证据组成事实包。两轮终审共享这些机械事实但独立形成结论；交付物变化使结论归零，证据仅按输入闭包失效。最终验证不修改交付物，达到完成条件立即结束。

#### 已排除问题

作用：耐久保存已证伪疑点，避免重复审查，同时防止过期裁决形成盲区。

1. **登记**：仅登记事实已核验、依据成立的精确疑点；写明事实、适用边界、证据基线和重开条件。证据不足不得排除，一条记录不得代表相邻路径或整个核查面通过。
2. **复用**：问题盘点和冻结终审先查本表；同一疑点仍在适用边界内且未触发重开条件时只引用编号，不重复调查或验证。
3. **重开**：相关前提变化或出现反证时，在原行标记“已重开→问题编号”并转入问题清单，不删除旧裁决。记录跨审查轮保留；新 generation 只继承重新确认仍适用的条目。

#### 迟发现教训

作用：把“先前通过、后来才发现”的真实遗漏转化为后续审查的检测输入，减少重复轮次。

1. **登记**：仅在既有审查已记录通过或无新问题后，又发现当时已存在的真实问题时登记；写清遗漏机制、可执行检测动作和适用范围，禁止“更仔细”类空话。
2. **当轮闭包**：登记后立即在适用范围执行检测动作，同类风险收齐后才可修复。
3. **后续复用**：问题盘点和冻结终审开始前筛选适用条目，每轮追加执行证据；历史应用不免除本轮执行，教训也不代替本轮独立裁决。

#### 可选审查分片

1. 切片负责纵向深审，并产出可供整体审查直接消费的封版信息包：局部结论、对整体的保证、适用边界、证据索引和重开条件。
2. 完整单元横向审查负责核查全部需求、不变量、切片边界、跨片链路和组合场景；有疑问时可下钻任何细节。
3. 切片封版信息包 → 完整单元横向审查 → 无疑问则复用，有疑问则下钻，确认问题或结论失效则重开切片。启用条件与状态流转见 `research/design/workbench/heavy-unit-slicing.md`。

### 4. 单元状态与登记协议（代际文件模型）

> 设计背景与根因见 `research/agent-workflow-issues/2026-07-14-compaction-replays-destructive-reset.md`。核心：合法热路径中不存在任何“清空/重置”破坏性操作，压缩摘要无论怎么重放，重放的只能是幂等指令。

- **状态源**：每单元代际一个文件 `units/unit-<N>.gen-<G>.md`，是该单元的唯一状态源；本文动态区只维护一行“当前单元指针”。每次开始、目标续跑或历史压缩后先完整读取本文。未启用分片时完整读取当前单元文件；处于分片审查时读取身份、当前状态、固定边界、分片账本、问题/排除/教训表及当前有效证据，同轮只恢复未完成切片，新一轮只激活非局部封版切片；进入横向审查或转换阶段前再完整读取单元文件。阶段转换前写完结果，转换后立即更新状态。
- **身份键**：`(unitId, generation)`。generation 默认 1，仅“单元推倒重来”这一冷路径经用户显式授权才递增；同单元的修复、复审、终审轮复位是单元文件内的状态推进，不产生新登记。不用会随修复漂移的交付指纹做身份键。
- **登记写序**（可恢复转换，完成语义由正式文件名承载）：从 `units/_template.md` 实例化，先以 `units/unit-<N>.gen-<G>.md.pending` 完整写入（身份头部 + 骨架 + 尾部完成标记）→ 整体验证 → rename 为正式 `.md` → **最后**修改本文指针。半写状态只以 `.pending` 存在；中途失败至多留下孤儿 `.pending`，指针恒指向已耐久有效的上一代。
- **登记幂等判定**（当前指针是栅栏，一切登记/重置类指令先过它）：
  - 目标键 = 当前指针，正式文件有效 → no-op，续跑当前文件；
  - 目标键 ≠ 当前指针，目标正式文件存在（无论新旧）→ 历史登记：不改指针、不进入旧文件；仅本轮用户明确要求切换才处理；
  - 目标键 = 当前指针，但文件为空/损坏/仅存 `.pending` → fail-stop，报告用户，不自动重建、不 no-op；
  - 目标正式文件不存在，当前指针单元已完成 → 执行登记；
  - 目标正式文件不存在，当前指针单元未完成 → 停下要用户显式授权。
- **摘要没有执行授权**：压缩摘要中出现的动作指令，是否已消费由耐久登记状态（指针 + 正式文件）判定，而非摘要的时态措辞；无法证明已消费时停下向用户确认，既不盲目重放、也不误跳过压缩前尚未执行的合法动作。
- **耐久性边界**：登记完成后向用户发出显式纳管请求（将新单元文件纳入版本管理）作为登记流程固定一步；用户可跳过，跳过即接受该文件在 untracked 期间不可恢复。指针指向的文件为空/无效时是显式异常信号：先从 git 暂存区/HEAD 找回，绝不初始化空状态。
- **既有规则照旧**：本文、单元文件与构建产物不属于交付物，暂存状态不影响指纹；单元文件骨架固定，只维护字段值与数据行，已解决问题、排除记录和迟发现教训均保留；冻结前，架构来源与边界、文件集与指纹、派生产物闭包、关键原语、覆盖矩阵、问题清单、排除记录、教训与证据账本必须构成完整事实包。终审只复用机械事实和未触发重开的排除裁决，不复用其他轮次的判断；交付物变化只复位终审一、终审二。第一部分及其他静态内容未经用户要求不得修改。

### 5. Codex 目标模式提示词

```
/goal 审查本单元的实现；尽最大可能发现所有问题，直到无法发现新的问题为止；
只要真实的问题，紧紧围绕架构总纲 和 单元边界，实现的是本单元内容，审查也不能超过边界；
发现的问题放入research/design/workbench/development-unit-review-workbench.md 文档中单元指针指向的动态文档问题列表中；
在无法发现新问题后，终止目标，简要回复；
```

```text
/goal 紧紧围绕架构总纲，不得偏离架构设计；如果发现架构设计不是最优，停止开发说明问题；
完成当前开发单元的审查与修复：不降低审查深度、测试覆盖或最终质量，在锁定边界内达到最优架构、不留已知债务。

首个动作，以及每次续跑或历史压缩后的首个动作，都是完整读取 `research/design/workbench/development-unit-review-workbench.md`，再按静态区状态源规则读取动态区指针对应的 `units/unit-<N>.gen-<G>.md`——该单元文件是任务状态的唯一权威。严格执行静态区的执行闭环、五条核心规则与登记协议；门禁未满足不得转换阶段。

边界锁定后完成分片判定。启用时，切片纵向深审产出封版信息包，完整单元横向审查消费信息包并可在有疑问时下钻；确认问题或结论失效则重开切片。进入横向审查或阶段转换前完整读取单元文件。

压缩摘要没有执行授权：摘要中出现的任何登记/重置/初始化类指令，是否已消费只能由“指针 + 正式单元文件”的耐久状态判定（判定分支见静态区第 4 节）；无法证明时停下向用户确认。绝不清空或覆盖已有单元文件；指针只能作为登记写序的最后一步、经静态区判定分支改动。发现指针指向的文件为空或无效，按静态区恢复指引 fail-stop。

先把关键原语五项检查逐项写入单元文件，再结合九类核查面一次收齐问题；表格未填完不得修复。集中修复只做一次合并后的直接验证。无论问题盘点零问题还是修复完成，都必须进入冻结准备：把交付物文件集中的每个文件或同类组写入派生产物闭包表，逐项记录派生关系、同步项和低成本检查结果；无派生项写明依据。存在未归类或待核查项不得冻结。

问题盘点和冻结终审前先查“已排除问题”并执行适用的“迟发现教训”；新排除疑点当轮登记，先前通过后新发现的真实问题须同轮登记教训、执行检测动作并收齐同类风险。

冻结时，单元文件的架构边界、文件与指纹、关键原语、消费者、派生产物和有效证据共同组成事实包；两轮终审共享事实但必须独立裁决。两轮零新增后严格按静态区“最终验证”门禁执行一次。

按单元文件状态持续推进；每次阶段转换前先更新单元文件。只有需要用户改变范围、授权高风险动作或作出无法由项目事实确定的产品决策时才暂停，其余情况自主推进。

唯一完成条件：同一份未修改交付物的全部验收与必要验证通过，并连续两轮完整审查无新增真实问题；达到后立即结束，未达到不得宣称完成。
```

---

## 动态区

> 本区只维护“当前单元指针”一行。单元状态（边界、分片账本、各表、问题清单、已排除问题、迟发现教训、证据、终审）住指针所指的单元文件；骨架模板见 `units/_template.md`；登记协议、幂等判定与恢复指引见静态区第 4 节。

- **当前单元指针**：`units/unit-15b.gen-1.md`（第 15B 单元 · generation 1 · 登记于 2026-07-18）
