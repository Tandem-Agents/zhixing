# 单元登记:第 14 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:14
- **generation**:1(仅经用户显式授权递增;同单元的修复、复审、终审轮复位是本文件内的状态推进,不产生新登记)
- **登记时间**:2026-07-15
- **登记来源**:第 13 单元完成后,由执行者按用户“现在做第14单元;动手”的明确授权登记

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、九类核查面行、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:**已完成，可提交**
- **连续无新增问题轮数**:2 / 2
- **交付物是否冻结**:是（U14-50 修复后冻结新指纹 `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d`；旧指纹仅保留为历史问题证据基线。）
- **交付物文件集**:`packages/core/src/contracts/ports.ts`、`packages/core/src/contracts/protocol.ts`、`packages/core/src/contracts/records.ts`、`packages/core/src/contracts/state.ts`、`packages/core/src/protocol/assignment.ts`、`packages/core/src/protocol/commit.ts`、`packages/core/src/protocol/index.ts`、`packages/core/src/protocol/job.ts`、`packages/core/src/protocol/resource-lease.ts`、`packages/executor/src/__tests__/assignment-ledger.test.ts`、`packages/executor/src/__tests__/job-assignment.test.ts`、`packages/executor/src/assignment-ledger.ts`、`packages/owner-kernel/package.json`、`packages/owner-kernel/src/__tests__/control-admission.test.ts`、`packages/owner-kernel/src/__tests__/conversation-run-contracts.test.ts`、`packages/owner-kernel/src/__tests__/job-run-contracts.test.ts`、`packages/owner-kernel/src/control-admission.ts`、`packages/owner-kernel/src/conversation-assignment.ts`、`packages/owner-kernel/src/conversation-run-contracts.ts`、`packages/owner-kernel/src/index.ts`、`packages/owner-kernel/src/job-assignment.ts`、`packages/owner-kernel/src/job-run-contracts.ts`、`packages/owner-kernel/tsup.config.ts`、`research/design/modules/distributed-runtime/specification.md`（共 24 个文件;工作台与构建产物不计入业务交付物）
- **当前交付物指纹**:`sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d`（算法:按上行文件顺序对每文件取 SHA-256 小写 hex,以 `相对路径=hash` 每行 LF 拼接为 UTF-8 manifest,再取 SHA-256）
- **架构来源**:`research/design/modules/distributed-runtime/distributed-runtime-charter.md`;`research/design/modules/distributed-runtime/specification.md` 的摘要/身份合同、job journal、控制与派发协议、user job 38 行状态机、system job 6 行状态机、验证矩阵及执行计划第 14 项;`research/design/drafts/scheduler-architecture.md` 的既有 scheduler 产品语义

## 固定边界

- **功能范围**:TaskDefinition 与 JobOccurrence 分离;JobJournal、JobCommitFence、user job 耐久派发/取消/重派/uncertain 同构协议;system job 锚点本地幂等执行与重驱;旧 scheduler 仅作兼容投影
- **架构不变量**:job 权威唯一属于锚点;definition 更新仅影响未来 occurrence;occurrence 冻结 taskRevision 与 delivery plan;旧 assignment 永不恢复写权;user job 复用统一 assignment 账本且无 conversation 伪语义;system job 不进派发/数据面/uncertain 且 surface 永不可构造;所有状态、租约锚与终态事实可耐久重放
- **验收条件**:user job 38 行与 system job 6 行逐边独立覆盖;任务更新/删除/错过/暂停、三方竞态、system handler 崩溃重驱与 surface 越权测试通过;受影响包类型检查、全测及必要构建通过
- **必要上下游**:第 9 单元 AuthorityCommitLog/ArtifactStore;第 10 单元 ControlRequestJournal;第 11~13 单元 assignment 账本、ActivationProof、SealedBundle、提交/取消/冲突/uncertain 合同;现有 scheduler 只读兼容消费面
- **明确不属于本单元**:delivery 权威流与 S3 生产切换(第 15 单元);正式能力/权限与资源 governor 实现(第 17~18 单元);mesh adapter 与跨机启用(第 19~20 单元);scheduler CRUD、定时触发与产品入口切换(第 26 单元)

## 审查分片

- **是否启用**:是（当前单元追补登记）
- **决定依据**:当前交付包含四个可独立裁决的合同闭包;既有长时审查已发生重复事实链重建,分片具备实际复用收益
- **完整单元与跨切片必审项**:definition→occurrence→assignment/fence→bundle/proof/result/ACK 身份链;user 正向提交与取消/uncertain 竞态及旧 attempt;user/system、surface/host 与 ingress 权威边界;在线生产、full reducer、compact guard、恢复器和 executor 的合同一致性;conversation 回归、后续 delivery/governor/scheduler 接缝及 spec/导出/测试同步

> 本轮进度用于跨窗口续跑,收敛状态用于跨审查轮复用;状态与流转规则见工作台静态区。

| 切片 | 审查闭包（边界、依赖、局部验收） | 输入基线 | 当前轮次 | 本轮进度 | 收敛状态 | 封版信息包（结论、保证、证据、重开条件） |
| ---- | ---------------------------------- | -------- | -------- | -------- | ---------- | ---------------------------------------- |
| U14-S1 · 任务定义、触发与准入 | TaskDefinition revision、JobOccurrence 创建/换代/错过、trigger 与 ControlAdmission ingress、user/system 分类及 scheduler 兼容投影;依赖 owner 权威与 definition digest;验收=调用方不能自报 ingress、definition 只影响未来 occurrence、禁用/删除/旧 revision 与 system surface 越权均拒绝 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 修复后冻结终审二 | 已完成 | 局部封版 | 重开=U14-42~43;删除与 uncertain 的停止义务、user queued 状态合同需重新闭合 |
| U14-S2 · user 正向执行与提交 | occurrence→assigned→dispatched→running→bundle sealed→committed→ACK 回执及恢复 outbox;覆盖 JobCommitFence、submission guard、旧 assignment/迟到 bundle 与 executor 账本;依赖 S1 冻结 occurrence 身份;验收=身份/fence/闭包/ACK 全绑定、旧 attempt 永无写权、崩溃与响应丢失收敛 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | U14-50 后冻结终审一 | 已完成 | 局部封版 | 结论=两域 active recovery 与 committed ACK outbox 已结构分离；保证=状态关闭仅停止前者，ACK 回执是后者唯一清除事实；证据=E37+第十九轮；重开=恢复索引、commit/ACK reducer 或 dispatcher 变化 |
| U14-S3 · user 取消与 uncertain 收束 | 双取消源、proof/conflict/containment/supersede、uncertain 三分裁决、interaction mirror 与 capability revocation;依赖 S1 occurrence 身份和 S2 assignment/fence;验收=竞态原子闭合、无证明禁重派、历史 attempt 隔离、请求幂等与重启恢复成立 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | U14-50 后冻结终审一 | 已完成 | 局部封版 | 结论=uncertain 关闭/containment 不再终结已 committed ACK 义务；保证=迟到提交响应丢失经重启唯一收敛，active 停止语义保持；证据=E37+第十九轮；重开=uncertain 收束、停止事实或 ACK 恢复变化 |
| U14-S4 · system 本地执行 | system occurrence→started/reclaim→result、资源协调、miss/coalescing、handler 崩溃重驱及 compact guard;依赖 S1 system definition/occurrence 身份;验收=不进入 assignment/data plane/uncertain、资源伴随事实全绑定、miss 合并唯一且 full/guard 同判定 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | U14-49 后冻结终审二 | 已完成 | 局部封版 | 结论=system 本地执行、租约、miss alias 与崩溃重驱闭合；保证=host-only、资源伴随事实及未知身份 fail-closed；证据=E32/E33/E35 与两轮终审；重开=system 资源合同、执行点或恢复门禁变化 |

### 完整单元横向审查记录

> 每轮必填;默认消费封版信息包,有疑问可下钻细节,确认问题或结论失效时重开切片。

| 轮次 | 完整输入基线 | 全范围与跨片核查 | 重开触发 / 切片 / 问题 | 结论 |
| ---- | ------------ | ---------------- | ---------------------- | ---- |
| 第三轮独立终审 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 入口来源权威、full/guard 合同单源、fail-closed 与切换兼容边界;沿生产端、full reducer、compact guard、恢复消费者和测试覆盖完成同族核查 | S1/U14-37;S4/U14-38~39;EX14-01 已排除 | 不通过,进入集中修复 |
| 修复后终审一 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 完整消费四片事实包并下钻修复增量;核对 definition→occurrence→assignment/fence→bundle/proof/result/ACK 全身份链、user/system 与 surface/host 边界、在线/full/guard/recovery 合同及跨片同类根因 | 无;EX14-01 适用边界与重开条件未变化 | 通过,连续无新增 1/2 |
| 修复后终审二 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 独立从竞态排列、各耐久边界崩溃、恶意 raw 日志、未知身份、缓存/索引失效、资源与队列上界及测试弱化反向核查完整单元;有疑问处下钻 S2/S3 | 无;四片均局部封版,EX14-01 未触发重开 | 通过,连续无新增 2/2 |
| 当前冻结终审一 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 完整消费四片信息包并复核 definition→occurrence→assignment/fence→bundle/proof/result/ACK 身份链、23 类记录的在线/full/guard/recovery/test 执行点、跨片边界与后续消费者;EX14-01 未触发,LD14-01 已执行 | 无 | 通过,连续无新增 1/2 |
| 当前冻结终审二 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 独立从事务竞争、耐久边界崩溃、响应丢失、恶意 raw 日志、历史身份、资源/分页上界、恢复索引及 conversation 共享改动反向核查完整单元;有疑问处下钻四片细节 | 无;EX14-01 未触发,四片维持局部封版 | 通过,连续无新增 2/2 |
| 第九轮独立终审 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 从冻结合同重新核对任务删除×全部在途状态，并以 definition kind×前态×次态×assignment 形态枚举共享 state 谓词；沿在线/full/guard/recovery、迟到 bundle 与测试覆盖收齐同类风险 | S1/S3/U14-42；S1/S2/S3/U14-43 | 不通过，进入集中修复 |
| 新冻结终审一 | `sha256:2b0cc21bf88f6e0804abb26c860796ca897825d59e45764d8a5559bfc798f5f3` | 重新核对需求与边界、ResourceLease 类型/签名/公共基约束及 assignment/system 专型绑定；逐记录核对两域在线生产、full/guard、真实恢复消费者/事实化 N/A、对抗测试和未来 governor/mesh 接缝 | 第二轮重开：U14-48；本轮“矩阵 full 接受已严格证明”的判断失效 | 不通过，回问题裁决 |
| 重新冻结终审一 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 从需求/架构重新核对 definition→occurrence→assignment/fence→bundle/proof/result/ACK 全链；逐一确认 ResourceLease 公共/专型谓词、24 文件闭包、两域 producer/full/guard/recovery/adversarial 门禁及后续接缝 | 无；EX14-01 未触发，LD14-01~06 均完成适用检测 | 通过，连续无新增 1/2 |
| 重新冻结终审二 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 独立从恶意 wire 值、缺失/未知字段、签名覆盖、历史 attempt、并发/崩溃、恢复消费者遗漏、N/A 真实性与门禁 catch 分支反向核查；有疑问处下钻两域矩阵与公共租约谓词 | 无；指纹复算一致，公共租约谓词唯一，23/20 记录格闭合，债务/skip/only 扫描为零 | 通过，连续无新增 2/2 |
| U14-49 后冻结终审一 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 重新核对 U14-49 唯一测试增量及完整业务事实包；确认实际 attempt、ResourceLease 自摘要、签名与 admission 分支的身份链，生产代码与既有协议零变化 | 无；E34 的 core/owner 证据输入闭包未变，E35 直接证据有效 | 通过，连续无新增 1/2 |
| U14-49 后冻结终审二 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 独立扫描全部 ResourceLease 与受签名 DTO 负例，区分签名层拒绝与合法重签后的业务层拒绝；复核 helper 删除旧摘要/签名、按实际 attempt 重算且无其他直接展开污染 | 无；唯一旧夹具已收敛，diff-check 与指纹一致 | 通过，连续无新增 2/2 |
| 第十八轮独立终审 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 从两域 ACK outbox 的权威事实、提交响应丢失、uncertain 关闭/containment 停止与真实恢复候选生成器重新核查；对照正常提交与迟到 bundle 测试的正交组合 | S2/S3/U14-50；LD14-08 已执行同族检测 | 不通过，进入集中修复 |
| U14-50 后冻结终审一 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 从需求、边界与完整身份链核对 24 文件；逐项确认两域 committed/ACK 生产、full 投影、O(pending) 索引、dispatcher、executor sealed→acked、uncertain 关闭与后续 mesh 消费；LD14-01~08 适用检测均执行 | 无；S2/S3 重新局部封版，EX14-01 未触发 | 通过，连续无新增 1/2 |
| U14-50 后冻结终审二 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 独立从竞态/崩溃排列、矛盾 executor 快照、历史 attempt、索引新增/清除、O(pending) 上界、测试 helper 与跨片合同反向核查；有疑问处下钻 S2/S3，复算指纹一致 | 无；四片维持局部封版，EX14-01 未触发 | 通过，连续无新增 2/2 |

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| -------------------------- | ------------------------ | ---------------- | ---- |
| core job/assignment/commit/resource-lease 合同与导出 | TaskDefinition/Occurrence/Fence、job dispatch/activation、job bundle、uncertain 结果类型、ResourceLease 公共基约束与公开协议导出必须同步 | 大值由 Stored sidecar 闭合;acked snapshot 的 bundle ref+commit revision 同在性由 wire validator 强制;uncertain-resolve 目标联合封闭为 queued/cancelled/failed;两类 ResourceLease adapter 共用内部纯谓词且不新增公开 API;core 类型与构建通过 | 通过 |
| executor 共享 assignment ledger 与既有 conversation 测试 | job 必须复用统一账本,同时不得改变 conversation 语义 | 共享账本按 execution 判别;acked snapshot 实际回放 commit revision;E37 证明两域 uncertain late commit 响应丢失经真实重启 sealed→acked、二次重启零查询,既有恢复矩阵 60 格保持通过 | 通过 |
| owner-kernel JobJournal、conversation journal、控制面、合同执行矩阵、包入口与构建入口 | 两域 journal/dispatcher/control creator 必须可从受支持子路径消费,内部 reducer 不得成为公开根 API;record shape 变化必须同步机械执行矩阵 | 两域 full projection 以独立 `bundleAcknowledgementOutbox` 承载 committed ACK 义务,仅回执删除；active recovery 停止事实保持原边界；权威记录类型与五执行点矩阵未变且 E37 全绿 | 通过 |
| specification.md | 任务启停权威、not-started 关闭分支、job-run ingress、system-result、两域 ACK outbox、uncertain 结果与资源绑定语义必须与实现同构 | 逐段核对 §3.6/§3.7/§4.3/§6.2/§6.2b:字段、回执、三选目标、同 proof 原子关闭与资源断言一致;U14-42/45 修复随附两处声明面同步(validateDispatchBinding 段 domain 判别封闭+job anchor 同 epoch+conversation 双变体;任务定义规则句删除×uncertain 唯一栅栏语义),终审一逐字段核同构;最终合同 lint 列入 E31 | 通过 |
| owner-kernel 公开导出面(新增再导出) | job-assignment 再导出 JOB_JOURNAL_RECORD_SHAPES/JobJournalRecordType、conversation-assignment 再导出 CONVERSATION_RUN_RECORD_SHAPES/CONVERSATION_RUN_INTERNAL_RECORD_TYPES/ConversationRunRecordType——两域行为矩阵的类型闭合依赖;DTS 随 owner build 更新 | 唯一消费者=executor 两测试套件(E30 全绿);注册表为只读声明数据、非 reducer 泄漏;package.json/tsup 无变化(既有子路径);E31 全量 build 验证 17 包消费 | 通过 |
| package manifest / lockfile / 生成物 | 新子路径与 tsup entry 同步;依赖集合未变化 | package.json 与 tsup.config.ts 同步;lockfile 无派生变化;dist 由最终构建成功生成且不计入交付物 | 通过 |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| TaskDefinition 与 JobOccurrence | 锚点 job 流的 task-revision/occurrence/admitted 记录 | 单 CommitEnvelope 追加 | 更新/禁用/删除与触发并发、received→applied 崩溃重试、超限 definition artifact 先写后引用 | job 内核生产;完整/轻量 reducer、兼容投影、票据签发与未来 scheduler 消费 | revision 单调;occurrence 冻结 revision/delivery;稳定请求身份与首次耐久 ingress 分离;任一合法 definition 可按 O(n) 规范校验并耐久表示;task-revision 紧凑历史与原子伴随由在线/full/guard 单源谓词同判定,full/producer 叠加来源深验 | 通过 |
| user job assignment 生命周期 | job 流 + executor assignment 流 | assigned/state/terminal/ACK 回执各原子 envelope | 派发、取消、封包、证明、响应丢失、跨 attempt 再次 uncertain 与重启 | anchor job 内核、executor 通用账本、提交 redriver | 当前 attempt O(1) fencing;历史 exact replay;两域 sealed 未 ACK 必须重提;ACK 回执后从 O(pending) outbox 移除;conflict not-started 同 proof 原子关闭 | 通过:E37 |
| JobCommitFence、bundle CAS 与 staged mutation | occurrence 冻结事实 + assigned 快照 + ArtifactStore | committed envelope | 历史 attempt、乱序 started、迟到 bundle、恶意 job mutation batch | executor 产 bundle;anchor 在线/guard/reducer与未来 delivery 消费 | exact replay 幂等;旧 attempt 零写权;job 类型联合在 executor、owner closure 与重放边界封闭 | 通过 |
| task uncertain 暂停与三分续跑 | job 流按 assignment 换代的 open/closed fact | uncertain/resolution 原子 envelope | 到点触发、证明/裁决/迟到 bundle、A1 关闭后 A2 再 uncertain | anchor job 内核、control admission、full/guard reducer | 每 task 至多一个当前未裁决 fact;历史 closed fact 保留但不阻断后继 attempt;关闭绑定当前 openFactDigest | 通过 |
| system job 本地执行与租约锚 | job 流 SystemJobFence + governor 记录 + trigger 幂等别名 | reserve+running+fence;terminal+settle/release | handler 输出边界、终态事务失败、miss 合并响应丢失后重试、超限结果 artifact 先写后引用 | anchor host-only 入口、封闭 handler registry、资源协调器与恢复器 | 同 jobRunId/同 trigger 幂等与重放语义正确;在线 producer 先构造实际记录集合并机械投影原子 state,full/guard 共用紧凑谓词,full/producer 叠加 definition 深验 | 通过 |
| 旧 scheduler 兼容投影 | JobJournal 权威前缀的只读投影 | 每个已提交状态变化后刷新 | 投影响应丢失与重建 | job 内核生产;旧 scheduler 消费 | 可从日志重建;失败可重试;不得反向成为事实源 | 通过 |
| job 提交面 submission guard | job 流内联事实的紧凑投影(零 ArtifactStore) | 每次提交入口按 cursor 追平后 preflight | guard 缓存失效重建、冷实例全量重放、跨 attempt resolution 换代 | JobJournal 四提交入口消费;与 full reducer 共享合同谓词与投影适配 | ACK 与 task-revision 紧凑合同均同源;guard 用历史 revision/kind/state 与同 envelope 原子事实 fail-closed,不依赖 ArtifactStore | 通过 |

## 覆盖与核查

> 覆盖来源包括架构要求、不变量、验收项、交付文件与跨边界符号、生产端、消费者和测试;核查面固定为状态、入口与生产端、消费端与继承面、生命周期、并发与崩溃点、异常路径与终态、安全边界、模块边界、测试与验收。每轮填写“通过:证据”“不适用:依据”或“有问题:编号”。

| 覆盖来源 | 来源项 | 核查面 | 对象或路径 | 问题盘点结论与证据 | 终审一结论与证据 | 终审二结论与证据 |
| -------- | ------ | ------ | ---------- | ------------------ | ---------------- | ---------------- |
| 状态机 | user/system job 全部逐边 | 状态 | user 38 行 + system 6 行及跨 attempt 正交维度 | 通过:既有 38+6 行、跨 attempt/竞态矩阵及新 occurrence/containment/resource 谓词对账闭合 | 通过:完整状态边、跨 attempt 与 committed ACK 独立义务无分叉 | 通过:从非法前态、终态回退和正交恢复义务反向核对无新增 |
| 控制与 host 入口 | trigger/cancel/resolve/system trigger | 入口与生产端 | surface、anchor clock、host principal | 通过:入口保持 owner/anchor 时钟与 principal 守卫;occurrence 与 system resource 生产提交前调用共享合同 | 通过:入口、时钟、surface/host 与 manual ingress 边界未受恢复投影修改 | 通过:未知来源、旧 epoch 与 surface/system 交叉仍 fail-closed |
| 端口与投影 | executor、兼容 scheduler、后续 delivery/mesh/governor | 消费端与继承面 | 共享账本、两域 journal、兼容投影、submission redriver | 通过:E37；两域 active recovery 与 committed ACK outbox 已分离，迟到提交响应丢失经真实重启收敛 | 通过:两域 producer/full/outbox/dispatcher/executor 与未来 mesh 合同闭合 | 通过:索引仅由耐久 commit/ACK 派生，停止事实不再跨义务越权 |
| definition/occurrence/attempt | 更新、删除、错过、暂停、重派 | 生命周期 | taskRevision/jobRunId/assignmentId/openFactDigest | 通过:task-revision 紧凑历史/原子伴随已三点同源;当前 definition admission、历史 attempt fencing、closed→新 uncertain 与 ACK 回执均有单调事实;E28 六类 raw full/guard 同拒绝 | 通过:definition→occurrence→attempt→commit/ACK 身份链单调且历史 attempt 零写权 | 通过:历史/current、关闭/重开与 ACK 身份分层无循环写权 |
| 故障矩阵 | 三方竞态与各耐久边界 | 并发与崩溃点 | control received/applied、dispatch/cancel/seal/proof/system handler、artifact 先写后引用 | 通过:E37；两域 uncertain late commit×响应丢失、首次重启补 ACK 与二次重启零查询均通过 | 通过:全部既有崩溃窗加 U14-50 正交组合均有耐久出口 | 通过:ACK/提交并发、重复恢复和二次重启均幂等且有界 |
| 终态合同 | committed/cancelled/failed/expired/missed/uncertain | 异常路径与终态 | bundle ACK、证明、用户三选、system result 与资源终结 | 通过:uncertain-resolve 类型与 runtime replay 仅接受 queued/cancelled/failed;missed/committed 原始日志对抗重放拒绝 | 通过:终态不可逆，状态关闭不再越权清除 committed ACK 义务 | 通过:矛盾快照不能建立新事实，合法 sealed/acked 各自唯一收敛 |
| 身份与越权 | anchor epoch、attempt、surface/system 隔离 | 安全边界 | request/envelope/首次 ingress/proof/fence/capability/resource record | 通过:共享 job state 谓词按 definitionKind 封闭双向隔离,system 无 assignment 转移仅允许规范三类,full/guard 同源 | 通过:ACK outbox 仍绑定 assigned+committed+ref+revision，未知/旧身份 fail-closed | 通过:错 assignment/ref/revision/epoch 不可借恢复恢复写权 |
| 包结构 | core/owner-kernel/executor/scheduler | 模块边界 | 合同单源、投影依赖方向、公开导出 | 通过:E28 证明 task-revision 谓词单源,23类权威 record-shape 注册表与五执行点矩阵机械闭合,新增类型缺行自动失败 | 通过:新投影为两域内部派生索引，不新增公开 API 或反向依赖 | 通过:两域投影同构、公共 ACK 谓词保持单源且零框架债务 |
| 执行计划验收 | 逐边、专项、类型与构建 | 测试与验收 | 直接相关套件→包全测→必要构建 | 通过:E37 的直接相关恢复与两域行为矩阵 62/62、双包类型、Biome、diff-check、指纹复算通过；最终包测/构建待两轮零新增后唯一执行 | 通过:E37 直接证据与新指纹一致，最终高成本验证门禁保持待执行 | 通过:两轮零新增已满足，按 E38 执行唯一最终验证 |

## 问题清单

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U14-01 | system 恢复曾不能区分有效租约复用、过期替换与另一实例活跃执行 | 恢复动作未绑定完整 durable fence 与当前状态 | system 生产端、reducer、资源协调器、并发 runner 与测试 | recovery 显式返回 reuse/replace;同 attempt 复用、attempt+1 替换、比较当前 fence 后原子追加;活跃 rival fail-closed | 已验证 |
| U14-02 | task 禁用/删除与已派发、uncertain not-started 的组合语义不完整 | definition 状态与 occurrence/attempt 生命周期未做正交闭包 | 定义生产、trigger/assign/resolve、reducer、兼容投影与状态机行 | 外层 state 唯一权威;禁用仅取消 queued、删除取消在途;证明到达时未启用则 `proven-not-started-cancelled` | 已验证 |
| U14-03 | 历史 assignment 与终态吊销后的重放授权可能破坏后继 attempt 或拒绝合法 exact replay | 当前性、授权和重放模式未统一建模 | started/proof/bundle/mirror 全入口、reducer、A1→A2、capability guard | 统一校验当前 job authority 与 activated capability;active/settlement/durable-replay 分模;revocation 键绑定 assignment+capId | 已验证 |
| U14-04 | cancellation audit settlement 与兼容投影在响应丢失后可能不再推进 | 恢复驱动未覆盖状态变化后的全部消费边 | cancel dispatcher、interaction mirror、projection 与重启 | 审计闭合后重入同一 cancel 并重提 proof;所有状态变化后刷新可重建投影 | 已验证 |
| U14-05 | committed job bundle 的冷重放若只信 journal 摘要会漏验 artifact closure 与原子伴随事实 | 在线 CAS 与 reducer 合同分叉 | bundle/sidecar ArtifactStore、在线提交、恢复 reducer、delivery/mutation 后续消费者 | 在线与 reducer 共用 occurrence/fence 绑定谓词;重放复验 bundle、artifact ref、closure、mutation batch 及必要 foreign records | 已验证 |
| U14-06 | user/system queued 与终态方法可交叉调用,重复 system trigger 可能错误过期 | 两族状态机共享状态名但缺少 kind 判别 | fail/expire/disable/repeated trigger、reducer 与兼容投影 | user-only 操作显式守卫;system 活跃/queued 重复触发合并 missed;非法终态重放拒绝 | 已验证 |
| U14-07 | job dispatch prompt 只验非空,弱于 TaskDefinition 的冻结资源边界 | 协议投影未继承来源值的上界合同 | TaskDefinition、签名 JobEnvelope、executor wire 边界与对抗测试 | 两端统一 65,536 字符上界,新增超界签名前拒绝用例 | 已验证 |
| U14-08 | spec §4.3 job `assigned` 行写 `baseRevision`、缺 `taskId/taskRevision/deliveryPlanDigest`,与实现及 JobCommitFence 冻结域矛盾;另 dispatched/running 中到点只记 missed 为实现补充语义、spec 未声明 | 单元 13 纸面同构推演残留 + 本单元 spec 同步漏行,声明面滞后于生效面 | 仅 spec 声明面;代码、测试、运行时零影响 | 按实现更新 §4.3 assigned 字段集并注明 job 无 baseRevision 链;任务定义规则补"至多一个非终态 occurrence,queued 过期让位、已派发只记 missed";验收=spec 与实现逐字段一致 | 已验证 |
| U14-09 | job 侧投影 `cancelOrigins` 只写不读(接口声明、reducer 写入、emptyProjection 三处),全包无消费者 | 自 conversation reducer 复刻结构时带入;job 侧等价语义已由 `durableStarted` 单调集合覆盖(running 入边与 started-observation 两处加入) | 仅 owner 内部投影内存与认知噪音;无正确性影响 | 删除 job 侧三处(conversation 侧有消费者、保留);验收=grep 零残留 + 类型与直接测试通过 | 已验证 |
| U14-10 | U14-08 修复只落规则段:6.2 行 1 守卫仍写"无同 task uncertain"、行 2 仍只枚举 uncertain,与规则段"已派发只记 missed"冲突;实现三分支(uncertain/queued/其余非终态)而终审二误记"同一守卫条件";行 2 测试只覆盖 uncertain,queued 到点换代(trigger 内部 expired+新 queued)与 dispatched/running/cancel-requested 到点 missed 无用例(执行者交叉审查发现) | 修复未沿"表格-规则段-实现-测试"全链闭合;终审断言凭上轮阅读印象未重核分支结构 | spec 6.2 行 1/行 2、任务定义规则段;trigger() 三分支;行 2/行 8 测试矩阵;终审记录 | 行 2 守卫扩为"存在未裁决 uncertain 或已派发在途 occurrence"(同一转移,38 行编号与验收口径不动);行 1 守卫同步"无同 task 在途 occurrence";占用矩阵参数化四态、补 queued 到点换代 trigger 路径用例;终审重做不复用旧断言 | 已验证 |
| U14-11 | job 提交面缺零 ArtifactStore submission-guard 结构:conversation 四提交入口先过 `#loadSubmissionGuard`(单元 13 建),job 四入口直接 full transact,且 submitBundle 全闭包深验(ArtifactStore IO)先于 exact-replay/稳定拒绝判断;§3.7 标题"同一接口同一 guard"、780 段 preflight 合同、782 行 active 分类明文 assignmentByRun/jobRun 双域(架构者审查发现) | 同构协议只复刻了 full 投影,提交面守卫结构未随单元 13 先例同构;job full reducer 守卫为内联布尔、与 conversation 谓词族分叉 | job 四提交入口、submitBundle 顺序、full reducer 全部授权 case、mesh(19/20)前置结构、测试 | 谓词单源化:通用谓词(assignment/ack/conflict/supersede/cancel-accepted/resolution/committed/state-atomic/capability)状态参数放宽为 SharedRunState 跨两域复用,job 差异谓词(转移表/state/cancel-fence/resolution-binding)入 job-run-contracts;full reducer 全部授权 case 切至谓词;建 JobSubmissionGuardProjection + guard reducer(同谓词、数据闭包适配、activation 经 buildJobActivationPayloadFromBinding 零解引用重建)+ 四入口接线;submitBundle 改为"guard 稳定拒绝/exact identity→结构验证→闭包深验→事务终决";顺带闭合 conflict acked 后迟到拒绝(生产端+reducer 同步);验收=guard 直接用例(historical 先于闭包、stale epoch、冷实例 exact replay)+ 全部既有用例通过 | 已验证 |
| U14-12 | JobCommitFence 逐字段污染无测试:§十二行 3"两域栅栏字段逐一污染→任一不符即拒"验收形态,conversation 侧单元 12 已做;owner 侧 `jobBundleBindsAssignedOccurrence` 拒绝分支、executor 侧 `sealJobBundle` fence 失配分支均无直接用例;覆盖矩阵"身份与越权"行的声称与事实有出入(架构者审查发现) | 逐边测试沿状态机行生成,栅栏字段污染矩阵不在行清单内、被遗漏 | submitBundle/sealJobBundle 拒绝分支、§十二行 3 验收、覆盖矩阵声称 | 参数化 8 字段污染用例:自洽 fence(重算 digest)逐字段偏移→owner submitBundle 恒 fence-rejected 且状态不变;sealJobBundle 对非耐久 fence 拒绝;验收=每字段一断言全拒 | 已验证 |
| U14-13 | job mirror 合同不完整:生产端对 cancel-requested/uncertain 不校验耐久 cancel-fence(uncertain 无 fence 经 ledger-unknown 可达、现状错误接受),cancel-requested 错走 active 而非 settlement,requestId 无跨批次唯一性;full reducer 缺同一状态/fence 守卫、requestId 去重与 `batch.assignmentId === record.assignmentId` 绑定——违反 spec §3.7 783/787(conversation 侧 1784-1813 三重守卫俱全);架构者前轮终审"settlement 判定留 full 与 conversation 同构"为不实断言(只对了位置、未对内容)(执行者交叉审查发现、架构者逐条核实) | U14-11 谓词单源化覆盖了 submitBundle,mirror 入口只接了 guard 加载、未逐守卫对照 conversation 合同;生产端与 reducer 各自内联再度分叉 | mirror 生产端、full reducer mirror case、投影 requestIds、审计消费者、测试 | 守卫抽 `assertJobMirrorReplayContract` 入 job-run-contracts、生产端与 reducer 共用(裁决门禁);投影 interactionMirrors 增 requestIds 集合;fence/settlement/requestId/绑定四守卫齐备;验收=uncertain 无 fence 拒、重复 requestId 拒、既有 mirror 用例通过 | 已验证 |
| U14-14 | job CancelProof 耐久重放合同未闭合:exact 分支比较含签名对象(executor 合法重签被误拒,787"可重签、对象身份不变");contained 与 contradictory-not-started 重放只查"已有记录"不核对原 proof 身份(异 proof 被静默当成功),且位于 active/settlement 授权之后——epoch 转移/capability 过期后按 §3.7 784 应 durable-replay 却被 settlement 当前 epoch 检查拒掉;conversation 侧有统一 durableProofs 四类重放结构(1885-1908)、job 缺失(执行者交叉审查发现、架构者逐条核实) | 同 U14-13:cancelProof 入口未逐守卫对照 conversation 重放合同;重放身份判定与授权模式选择分叉 | submitCancelProof、#rejectContradictoryNotStarted 两处 owner 侧调用点、投影 containments 索引、响应丢失/epoch 转移恢复路径、测试 | 对齐 durableProofs 结构:decide 开头 binds 后统一收集 accepted/superseded/containment/rejectedNotStarted 四类耐久 proof,任一存在→全部去签名全等否则拒→durable-replay 零追加;投影增 containments(assignmentId→contained 记录);#rejectContradictoryNotStarted 幂等分支比对 proof 身份;新路径 mode 恒 settlement(conversation 1927 同构);contained 内联幂等分支改为不可达防腐 corrupt;验收=重签 exact 幂等、contained/rejected 同 proof 重放成功、异 proof 拒 | 已验证 |
| U14-15 | job abort-ticket 证明缺 owner 在线重新鉴权:`JobJournalOptions` 无 conversation 已有的 abortTickets authorizer 接缝,`submitCancelProof` 只验 executor 签名+assignment/epoch 绑定,而共享谓词对 abort-ticket 分支直通(其合法性本应来自票据鉴权)——持 assignment capability 的 executor 可自造任意 ticketDigest/surfacePrincipal 使 owner 接受 cancelled/uncertain;违反 spec §3.6"abort-ticket proof 在在线接收时重新鉴权 ticket"与 6.2 行 19/20/30/31/33"票据归属"守卫;既有测试全用固定 digest 零覆盖(执行者交叉审查发现、架构者核实) | 同 U14-13/14 根因:cancelProof 入口未逐守卫对照 conversation 合同,票据分层验权接缝漏移植 | JobJournalOptions、submitCancelProof 在线接收点、harness 装配、对抗测试;重放路径不涉(spec:落日志后机械重放) | 复用 conversation 同一 authorizer 接口加 `abortTickets` 接缝;durableProofs 重放之后、settlement 授权之前对 cause==="abort-ticket" 强制鉴权,未配置 fail-closed 抛错(conversation 1916-1925 同构);验收=错误票据拒、未配置拒、既有 abort-ticket 用例经鉴权全过 | 已验证 |
| U14-16 | job owner 侧 mirror 缺记录字节上界提前拒绝层:conversation 在验签前检查 mirror 记录 ≤ MAX_INLINE_LOGICAL_RECORD_BYTES(1719-1726),job 入口无此层;产批端(共享 ledger 934)与 log 层 assertInlineBody(commit-log 706-713)已 fail-closed,无正确性漏洞;可达差异=超大批次付一次日志重放+验签成本后被 log 层以内部错误文案拒,而非入口合同错误(架构者自审发现) | 同构复刻遗漏防御深度层 | mirrorInteractions 入口、资源上界、错误分类 | guard 加载后、批次验签前加同款字节检查(conversation 1719 同构);验收=超限批次入口拒且文案为合同错误 | 已验证 |
| U14-17 | 交付物源码注释 4 处引用易变 spec 章节号(job-assignment 两处 §3.6/§3.7、job-run-contracts 一处 §3.7、assignment-ledger 一处 §十):distributed-runtime 模块生产代码既有惯例为零裸 § 引用(conversation 侧与 job 主体全部如此),编号是位置坐标、spec 重排即腐(执行者发现 3 处,架构者按同类一次审完补第 4 处) | 架构者修复轮与主体开发期引入,未对齐模块注释惯例 | 4 处注释;零行为面 | 改为稳定表述(端口名/文字直述/仅保留 S 段标记),语义保留;验收=交付物源码 § 残留为 0 + 双包类型与格式通过 | 已验证 |
| U14-18 | ControlRequest received 后崩溃再以同 requestId/载荷但新 `at` 重试时,applyAuthority 只比较 payloadDigest+surfacePrincipal,job-run/resolution 决策仍读取重试 envelope;手动 job-run 同时拒绝 spec 要求的 admitted.ingress、生成 `sha256:` jobRunId 且遇现役 queued 直接拒绝 | authority apply 没有把首次耐久 envelope/source 提升为完成阶段唯一输入,手动入口又复制 trigger 规则而未复用规范 occurrence 生产合同 | control received/applied、job-run/uncertain-resolve、manual ingress/ticket 后继消费者、queued 换代、id 生成、conversation 共享 applyAuthority 与崩溃测试 | RequestProjection 保留首次耐久完整 envelope,AtomicControlApplicationContext 只暴露该规范 envelope,job/conversation 决策禁读重试闭包;job-run body 耐久携全量 ingress且 creator 从 trusted source 派生;按 receivedAt+requestId 生成 `jobrun-<ULID>`;queued 在同 authority envelope 先 expired 后建新 occurrence;参数化验证首次时间/ingress、queued 换代和 id 形态 | 已验证 |
| U14-19 | user-risk-retry 关闭 A1 fact 并以 A2 重派后,`markUncertain(A2)` 会产新 fact,但 full/guard reducer 见 jobRun 已有 closed fact即报 corrupt | resolution 投影以 jobRun 单槽保存事实却把“存在已关闭历史”误当“当前序列非法”,未以 assignment/openFactDigest 表达换代当前性 | markUncertain/所有打开 fact 生产者、full reducer、submission guard、currentResolution、A1→A2→uncertain 与恢复测试 | 在共享合同层单源 `isOpenResolutionFact`;生产端、full reducer、轻量 guard 只拒当前 open,允许 closed→新 assignment open,关闭仍严格绑定当前 openFactDigest;增加 A1 关闭→A2→再次 uncertain→重放测试 | 已验证 |
| U14-20 | 三个耐久幂等链断裂:sealed/未 ACK 恢复只补 started 不重提 bundle;system miss 合并不记录来访 jobRunId→批次别名;supersede 完成后原 requestId 重试先被 current-assignment 守卫拒绝 | 将“当前待办查询”误当“耐久操作身份回放”,缺少 sealed submission outbox/别名事实/终态前置 exact replay | executor submission adapter、job dispatcher 恢复、system trigger/reducer、supersede request/reducer、响应丢失和重启测试 | in-process recovery submission 显式重提 sealed bundle并按结果收敛;新增 `system-miss-coalesced` 耐久别名及 reducer 绑定,别名永久回放原 missed 批次;requestSupersede 先按 assignment+requestId 回放耐久 fence再检查当前性;三条分别做响应丢失后终态重试测试 | 已验证 |
| U14-21 | executor stageMutation 拒绝 job `delivery-enqueue.target=turn-origin`,但 owner closure 只验 generic MutationBatch + global domain,可接受外部构造的同类 bundle | JobGlobalStagedMutation 的封闭联合只存在于 TypeScript 类型与 executor 局部守卫,远端 owner 信任边界未复用同一纯数据谓词 | core 结构 validator、executor producer/reducer/sealed reload、owner closure/full transaction、JobCommitParticipant、future mesh/delivery 消费与对抗测试 | core 提供纯数据 `validateJobStagedMutationRecord/validateJobMutationBatch`;executor stage、ledger replay、sealed reload与 owner closure 全部调用同一谓词,各点只做投影适配;参数化对账合法 explicit 与非法 turn-origin 在四执行点结论一致 | 已验证 |
| U14-22 | SystemJobHandler 可返回非字符串 summary;handler 已执行后才在 reducer 拒绝,任务留 running 并在恢复中反复执行 | 不受信 handler 输出未在副作用返回点归一化为“合法成功或有界失败”,把类型错误推迟到耐久事务内部 | handler 端口、#runSystem 成功/失败归一化、system-result validator、资源 terminal、崩溃恢复与边界测试 | 在 job-run-contracts 单源 summary 类型谓词;handler 返回后立即验证,非法类型在同一 try/catch 归一为 failed,随后照常 terminal+state 原子提交;非字符串证明只执行一次且不留 running | 已验证 |
| U14-23 | 协议允许 65,536 字符 TaskDefinition/system 结果且 system params/数组可扩张,AuthorityCommitLog 单记录硬限 32 KiB;实测通过 `validateTaskDefinition` 的 40,000 字符 prompt 形成 40,286-byte task-revision,必然无法落日志;system summary/error 同样可能在 handler 已执行后失败 | 业务值边界与物理日志表示边界分叉,未执行 spec 已冻结的“超限值入 ArtifactStore、日志只写引用”;局部字符限制不能证明整条记录可表示 | task-revision 生产/完整 reducer/零 ArtifactStore guard/兼容投影;system-result 生产/reducer/恢复;artifact retention;assigned 等全部 JobJournal 记录的机械尺寸对账与测试 | 对可合法超限的 TaskDefinition 与 system result 采用 `Stored<T>` artifact 侧车,先 artifact 后引用并在 full reducer 规范回读;record 内保留 guard 所需紧凑绑定并逐字段复核;所有其余 JobJournal 生产记录在单一入口按完整 canonical body 预检 32 KiB,防止协议合法但日志才报内部错误;参数化证明 inline/sidecar 两形态重放同值、缺件/非规范/错绑定拒绝、40k prompt 与 summary/error 均可终态耐久、全部生产记录尺寸门禁一致 | 已验证 |
| U14-24 | 当前实现把 owner 派生的完整 `IngressContext` 放进 `job-run` body 与 payloadDigest;其中 `receivedAt`、channel replyTarget 等是接收事实,同 requestId 在 received→applied 崩溃后重新接收可产生新值,合法重试会在读取首次耐久 envelope 前被判 payload 冲突;spec 的 job-run 正文原本只含 taskId+anchorEpoch,完整 ingress 属 admitted/接收事实 | 为让完成阶段取得首次 ingress,错误地把易变接收事实并入稳定请求身份;实现偏离“控制请求身份与 owner 接收事实分层”的冻结合同 | core ControlRequest/ControlRecord、三类 creator、received reducer/projection、applyAuthority context、job admitted 生产、conversation 共享控制面、崩溃重试与旧日志兼容测试 | 保持 job-run body 与 payloadDigest 只绑定稳定业务字段;ControlRecord.received 可选耐久 owner-derived ingress,投影保存首次规范 ingress,AtomicControlApplicationContext 暴露该首次事实;job 决策只读 context.ingress,重试 source 仅做当次认证不得覆盖耐久值;input/job-run 要求 ingress,其余控制禁止;旧无 ingress 记录仅对无需 ingress 的类型兼容;验收=同 requestId/同载荷但不同 retry at/receivedAt/replyTarget 仍使用首次时间与 ingress,载荷变化仍冲突 | 已验证 |
| U14-25 | `assignmentsAwaitingRecovery` 把所有 retained `committed` assignment 永久列入候选;executor ACK 后 owner 无耐久停止事实,每次重启仍扫描、query 并重提同一 bundle;`LedgerSnapshot(acked)` 又不携实际 commit revision,无法安全确认 ACK 绑定本次提交 | sealed submission 的“owner committed→executor acked”跨账本义务只有两端终态、没有 owner 侧耐久回执与待办索引,把历史表扫描误当有界 outbox | core LedgerSnapshot/wire validator、executor snapshot、JobJournal record/full reducer、恢复候选索引、dispatcher sealed/acked 两分支、响应丢失/二次重启测试、spec | `acked` snapshot 必携 `acknowledgedCommitRevision`;job 流新增 `bundle-ack-observed` 绑定 assignment+bundleRef+jobRevision,只在 committed 全等且 executor 已 ACK 后写;投影维护 O(pending) recoveryAssignments,提交保留、回执/关闭/contained 删除;sealed 重提成功后写回执,acked 冷恢复直接核对并写回执;错 ref/revision 在不触碰不可信 artifact 前拒绝;验收=两类崩溃均收敛且第二次重启零 query/零提交 | 已验证 |
| U14-26 | 在线 `trigger()` 拒绝 disabled/deleted definition,且 user 现役 queued 必须先 expired 再建新 queued;完整 reducer 的 occurrence case 未验 definition enabled,并允许 user queued 现役下直接写 missed;轻量 guard 更未绑定最新 revision/state/kind | occurrence 准入规则分别内联在生产端、完整 reducer 与轻量 guard,同一合同多执行点分叉 | trigger 决策、JobJournalRecord occurrence、full/guard reducer、definition/active 投影、兼容投影、禁用与 user queued 对抗重放测试 | 在 job-run-contracts 建纯 occurrence admission 谓词;生产端以有效 active 投影、full/guard 以各自可得字段调用;guard 补 latestDefinitionRevision;完整 reducer另保留 deliveryPlan 深验;参数化覆盖 disabled/stale/复用 id/缺 admitted/user queued→missed 与两种合法形态 | 已验证 |
| U14-27 | full reducer 可接受单独的 not-started `dispatch-conflict-contained`;即使同 envelope 存在 assignment-superseded,现有检查也只比 assignment/openFactDigest,不要求两条记录携同一规范 proof;可停止 cancel outbox却不关闭 uncertain/旧 assignment | conflict containment 的 halted 单记录与 not-started 原子关闭共用 case,伴随事实及 proof 身份未形成单一谓词 | submitCancelProof/acceptSupersedeProof、#acceptNotStarted、containment/assignment-superseded full reducer、submission guard、pendingCancellations、对抗重放测试 | 单源 containment 谓词按 proof 决策分流;not-started 必须 resolution close+同 proof superseded+目标 state+全 revocations;full containment、full/guard superseded 与在线 producer共用;运行时 validator 收紧为 CancelProof 或 not-started-fenced;缺伴随/异 proof/错目标/已 started proof 参数化拒绝 | 已验证 |
| U14-28 | `SystemJobResourceCoordinator` 只返回任意 `LogicalRecord[]`;在线仅验非空/非 job stream,full reducer 也只验“存在任一 foreign record”,因此无关记录即可冒充 reserve、settle/release/reclaim 的 fence/outcome 原子伴随,与 6.2b 明确逐字段绑定合同不符 | 本单元提前留了资源参与者生产接缝,但遗漏同一参与者在重放端的合同投影;把记录存在性误当身份绑定 | coordinator prepare/recover/terminal、system-started/system-result full reducer、running/terminal state、未来 governor 实现与伪造 foreign record 测试 | coordinator 提供 activation/terminal 纯绑定断言;prepare/replace/terminal 生产端与 full/guard reducer 共用;封闭测试记录逐字段绑定 reserve/reclaim/settle/release、attempt/reservation/outcome;错绑定与缺 coordinator 重放均 fail-closed | 已验证 |
| U14-29 | 共享 `LedgerSnapshot(acked)` 已增加 commit revision,且 spec 明确 run/job 均须 owner 耐久 ACK 回执;job 已有 `bundle-ack-observed`+O(pending) outbox,conversation 仍只枚举当前 active assignment并在 sealed 时直接跳过。owner commit 后响应丢失会留下 executor sealed;executor 已 ACK 后 owner 又无停止回执,均不能由 conversation 恢复器收敛 | U14-25 只修 job 消费端,未沿本次共享协议变化反查既有 conversation 生产/消费全链 | conversation record/validator、full+guard reducer、O(pending) 索引、dispatcher bundle submission、两种崩溃恢复、spec run 联合与既有提交测试;job 同族谓词 | 两域共用 ACK 绑定谓词;conversation 新增绑定 assignment+bundleRef+commitRevision 的耐久回执及 recoveryAssignments 索引,enabled dispatcher 显式要求 bundle submission;sealed 重提后核对 acked,已 acked 冷恢复只补回执,回执后二次重启零 query/零提交;full/guard 同谓词拒绝重复/错绑定;spec 两域联合同步 | 已验证 |
| U14-30 | `uncertain-resolve` 的三种用户决定只可能返回 failed/cancelled/queued,但 ControlResult 类型与 replay validator 被放宽到全部 conversation/job 状态,conversation 日志中的 job-only `missed` 及其他非裁决目标可通过 control result 重放校验 | 为支持 job execution 直接把两个完整状态联合并入结果,没有建模控制操作自身更窄的结果集合 | core ControlResultBody、spec 控制结果、在线 apply、artifact result 重放、conversation/job control projection 与对抗测试 | 定义并复用精确的 `UncertainResolutionTargetState = queued|cancelled|failed`;类型与 runtime validator 同源封闭,两域生产端保持现状;原始 control received+伪造 applied(`missed`/`committed`)重放必须拒绝 | 已验证 |
| U14-31 | system task 虽禁止进入 assignment/data plane,但 shared `assertJobStateReplayContract` 允许 definitionKind=system 且 assignmentId 在 queued→dispatched;完整 reducer稍后靠 dispatch artifact 深验拒绝,零 ArtifactStore guard 会接受同一 assigned+dispatched envelope,从而让冷 exact replay建立在比完整 reducer更宽的权限投影上 | system/user 隔离谓词只存在完整 artifact closure,未落入 full/guard 都可得的状态合同 | assigned+state 在线生产、共享 state 谓词、full reducer、submission guard、冷 exact replay、system 越权测试 | 在共享 job state 谓词中规定任何 assignmentId 仅可绑定 user definition,full/guard 同时收紧且不增加依赖;参数化测试证明 system assignment 拒绝、user assignment与 system 无 assignment activation 保持合法 | 已验证 |
| U14-32 | `SharedRunState` 注释称 `expired` 为 job-only,但 ConversationRunState 也包含 expired | 状态联合泛化时注释未与真实类型集合核对 | 仅源码注释与维护认知,零运行行为 | 改为不枚举错误类型的稳定表述,并复查本轮改动注释不引用易变章节或错误域事实 | 已验证 |
| U14-33 | full reducer 对 `system-started` 校验 system definition、fence 全绑定、attempt/前态、foreign records 与初次 running 原子伴随,对 `system-result` 另验当前 fence、唯一结果和原子终态;轻量 guard 原仅验 occurrence/current fence 与资源协调器。第一轮修复已统一运行谓词,但在线 producer 把 `hasAtomicRunningState/hasAtomicTerminalState` 写成常量 true,不是从实际待追加记录推导,definition 深层绑定也只有正路径 | system 记录与 state 的同一身份/原子合同原在在线生产、full reducer、guard 三处内联且投影宽度不一;U14-31 只封了 system→assignment 单向;首轮修复的调用形态仍未做到 by-construction | system 初次 activation/恢复 replacement/terminal 生产端;system-started/result 类型组合;full reducer、零 ArtifactStore guard、冷 exact replay、恢复器与未来 mesh;非法 user/system 混入、重复 result、错 attempt/fence、缺原子 state 终态;对抗测试 | 两层纯谓词保持不变;在线 producer 先构造实际 job records,再从该集合计算原子 state 伴随并把同一集合落盘,禁止常量声明;为 definition handler/paramsDigest 加负例。验收=生产/full/guard 六维对账,参数化拒绝 user 伪 activation、definition 错绑定/错 attempt、缺伴随、重复 result,合法初次/替换/终态保持通过 | 已验证 |
| U14-34 | job full reducer 已拒绝无 committed、重复、错 bundleRef/jobRevision,首轮修复也给 guard 增加 acknowledgement 集合;但 full 仍通过构造 expected record + canonical 比较独立表达绑定,guard/在线 ledger 侧才直接调用两域共享 `bundleAcknowledgementBindsCommitted` | U14-25/29 建 ACK 耐久合同后遗漏 job guard;首轮补 guard 时没有反向删除 full 的第二份绑定表达,同一 ACK 谓词仍未完成全执行点结构单源 | job ACK 在线观察生产、record validator、full reducer、submission guard、O(pending) 恢复与 exact replay;无 committed/重复/错 ref/错 revision 组合;job/conversation 共享谓词与测试 | full 与 guard 均直接调用共享 ACK 绑定谓词,record validator继续负责字段封闭;保留参数化原始日志 ref/revision/duplicate 双端拒绝与合法恢复用例 | 已验证 |
| U14-35 | executor 默认 usageFinal 注释仍写 `Placeholder until ... (S4)`,把已裁决的显式零 usage fallback 表述为占位物并引用易变阶段编号 | 注释沿用前期接缝措辞,此前只清除了裸章节号,未对全部未来阶段/占位语义做同类扫描 | 仅 `assignment-ledger.ts` 注释与维护认知;运行代码、类型、协议摘要和测试零变化 | 改为稳定说明:未配置 reporter 时使用域分离的零 usage 证据,配置后由签名 UsageReport 摘要替代;全交付物扫描不得再出现 placeholder/阶段编号式债务 | 已验证 |
| U14-36 | 最终 owner 全测 90/91:既有 conversation 合同执行矩阵按 `CONVERSATION_RUN_RECORD_SHAPES` 自动发现新增 `bundle-ack-observed`,但 `CONTRACT_EXECUTION_MATRIX` 缺该行;生产 record shape、在线观察、full reducer、compact guard、恢复 outbox 与两域错绑定集成测试均已在场 | U14-29 增加共享 ACK 耐久记录时遗漏同步既有机械覆盖注册表,派生产物闭包又未纳入该测试文件,使直接定向验证未触发全记录类型门禁 | conversation ACK 生产端、结构 validator、full reducer、submission guard、恢复消费者、共享 job 谓词与参数化负例;`conversation-run-contracts.test.ts` 必须加入交付闭包 | 新增独立 `bundle.acknowledgement-binding` 合同身份与 `bundle-ack-observed` 五执行点矩阵行,证据明确指向两域共享谓词及现有 ref/revision/duplicate 参数化负例;不复制生产校验;验收=矩阵测试、owner 全测、两域 ACK 定向测试全绿且新 23 文件指纹冻结 | 已验证 |
| U14-37 | `JobJournal.trigger()` 对 user/system definition 均接受可选 `IngressContext`,只做结构校验且 authorizer 不接收 ingress,随后可把调用方提供的 surface ingress 写入 `admitted`;只读最小复现已证明 system/timed 触发可耐久写入 ingress | timed/host trigger 与 manual control 共用一条含 ingress 的入口形态,形成两个 ingress 生产源;这违反 ingress 必须由 owner 从已验证控制入口派生、manual job-run 才携 ingress、system job 不进入 surface/data plane 的冻结合同 | `trigger()` 输入与 authorizer、manual `applyControl(job-run)`、admitted record 合同、full/guard reducer、scheduler/ticket 消费面及 system/timed/manual 正反测试 | 从直接 clock/host trigger 移除 ingress（或用判别入口使其不可表达）;manual job-run 仅由 ControlAdmission 生产 owner-derived ingress;full/guard 重放对非 user manual-control admission 携 ingress 一律 fail-closed;补 system/timed ingress 负例与合法 manual 生产/重放正例 | 已验证 |
| U14-38 | `system-miss-coalesced` 的 full reducer 校验 system definition、pending 绑定、coalesced=`missed`、active 存在且非终态;compact guard 仅验 requested id 未占用与 coalesced occurrence 存在,两处为独立手写合同 | 同一记录的 full/guard 判定未复用合同谓词;当前 full reducer 是权威而未形成运行时分叉,但未来规则变化必须同步两处,违反“多执行点合同必须谓词单源”门禁 | system miss 在线生产、record validator、full reducer、compact guard、pending/current 投影、恢复消费者及 coalescing 对抗测试 | 抽取两端可共享的纯合同谓词,由 full/guard 各自投影适配;guard 投影补齐所需 pending/current 事实;参数化覆盖 pending 不匹配、coalesced 非 missed、active 缺失/终态、requested/coalesced 缺失或重复及合法重放 | 已验证 |
| U14-39 | compact guard 的 state case 以 `definitionKinds.get(taskRevision) ?? "user"` 将缺失权威 definition kind 默认成 user;full reducer及 guard 的 system-started/system-result 对同类缺失均 fail-closed | 该默认值掩盖缺失的权威身份元数据,把 user/system 隔离寄托在“definition 必先写且永不删除”的顺序不变量上;当前合法历史中不可达,但入口改动后可能静默放行,且无缺 kind 重放测试 | shared state replay predicate、full/guard state reducer、definitionKinds 投影、user/system 合法重放与缺 kind 原始日志测试 | 删除 user 默认值并向共享谓词传递缺失态,统一 fail-closed;新增 compact guard 缺 taskRevision kind 的原始重放负例,保持合法 user/system 路径通过 | 已验证 |
| U14-40 | `task-revision` 在线入口与 full reducer 校验 taskId 归属、revision 连续、kind/来源不变、删除后禁复活及停用原子伴随;compact guard 仅写入 kind/state/revision，会接受 full 判为腐坏的紧凑历史 | 多执行点合同未按 record type 机械对账，概括性“已单源”结论掩盖了无共享谓词的分支 | task-revision 在线生产、结构 validator、full reducer、compact guard、durable replay/rejection 短路与对抗测试 | 提取不依赖 ArtifactStore 的紧凑历史/原子伴随谓词，在线/full/guard 按数据闭包共用;full 叠加 definition 侧车与来源深验;参数化覆盖错 taskId、跳 revision、kind 变更、删除复活和缺伴随事实的 full/guard 同拒绝 | 已验证 |
| U14-41 | job 矩阵已改为真实 API 生产、full/guard 接受与对抗注入，但 `JobBehaviorHarness`/测试体没有 recovery 格；删除恢复候选过滤或停止消费仍不会使矩阵失败，因而尚未达到原验收条件 | 修复只把元数据门禁行为化了三个执行维度，遗漏矩阵声称覆盖的恢复消费者维度 | job 权威 record-shape 注册表、全部日志生产者、full/guard reducer、恢复候选与 outbox、对抗测试、U14-40~44 及未来记录扩展 | 在同一权威记录注册表下为每类记录登记可执行 recovery probe 或事实化 N/A；probe 必须调用真实恢复查询/重驱入口并断言候选新增、保留或停止，删除真实恢复消费时测试必失败；与现有生产/full/guard/对抗格共同保持 23 类零缺行 | 已验证 |
| U14-42 | user occurrence 因 `ledger-unknown` 进入 uncertain 后删除任务，`define()` 只写新 task revision：不新增 cancel fence、停止 outbox 或 resolution；兼容投影随即移除任务，但原 assignment 仍可运行并以迟到合法 bundle 提交，违反“删除取消全部在途 occurrence” | task-revision 停止合同只枚举 queued、dispatched、running，遗漏 uncertain 中“事实待裁决”与“执行停止义务”是两条正交状态；38 行测试也未覆盖删除与 uncertain 的组合 | task-revision 在线/full/guard 合同、uncertain 原因与既有 fence、pendingCancellations/recovery、CancelProof/迟到 bundle 胜负、兼容投影、spec 任务定义规则及删除×uncertain 对抗测试 | 明确并冻结删除 uncertain 的可执行语义：删除不得伪装成已证明取消；对尚无停止 fence 的当前 assignment，同 task-revision envelope 耐久建立唯一 cancel fence/outbox并保留 open fact，已有 fence 幂等复用；后续 bundle/proof仍按账本顺序裁决，not-started 在 deleted 定义下原子关闭为 cancelled；在线/full/guard/recovery 共用伴随谓词，覆盖各 uncertain cause、已有/无 fence、响应丢失与迟到 bundle | 已验证 |
| U14-43 | `assertJobStateReplayContract()` 的 user 分支会接受携当前 assignmentId 的 `queued→running/cancelled/expired`；直接调用已实证三条均放行。当前 full/guard 依赖“assigned 必与 dispatched 原子落定、queued 会清除 assignmentByJob”间接使其不可达，但共享权威谓词本身比 user 38 行状态机更宽 | user/system 共用 transition 超集后只收紧了 system 分支，user 分支只校验 assignment 当前性，未按 definition kind、前态、次态和 assignment 形态封闭合法边；现有测试遗漏“user + queued + 合法 assignmentId”正交组合 | 共享 state 谓词、在线 producer、full reducer、compact guard、恢复消费者、合同执行矩阵和 user/system 状态边参数化测试 | 将状态边按 execution kind 与 assignment 形态精确建模：user queued 仅允许无 assignment 的 cancelled/failed/expired，或携同 envelope assignment 的 dispatched；system 仅允许其封闭三类边；其余 assigned/user 与 system companion 混入一律 fail-closed。以状态笛卡尔参数化测试驱动在线/full/guard 同判定，并保留原子伴随谓词分层 | 已验证 |
| U14-44 | `not-started-rejected` 已按 `(assignmentId, proofKind)` 耐久记录矛盾证明，但恢复生产端均不读取对应停止锚：`pendingCancellations()` 不排除 `cancel-owner-fence`，`pendingSupersedes()` 不排除 `supersede`，`assignmentsAwaitingRecovery()` 不排除 `dispatch-rejection`，也不排除会由账本 `cancelProof` 再次触发提交的 `cancel-owner-fence` / `cancel-abort-ticket`。因此 owner 已裁定“不得重派并保持 uncertain”后，每次恢复仍会重复 cancel、supersede、dispatch-recovery 或 cancel-proof submission；响应会被 durable replay 吸收，但恢复义务永不收敛 | 停止锚只完成了生产与幂等重放，未把 proofKind 投影接入各自 outbox 与账本恢复消费谓词；测试只断言 contradictory proof 后状态仍为 uncertain，未验证对应恢复源停止且其他 proofKind 不受影响 | full 投影的 rejectedNotStarted 索引、三类 pending/recovery 查询、InProcessJobDispatcher 的 cancel/supersede/dispatch/ledger-proof 重驱链、compact guard 授权投影边界、四类 proofKind 及恢复测试 | 建立单源的“该恢复动作是否被对应 proofKind 停止”谓词，并接入 cancel、supersede、dispatch-recovery 与 ledger cancel-proof 候选生成；严格只停止对应动作，abort-ticket 矛盾不得误杀 owner-fence outbox，其他来源继续收敛。参数化覆盖四类 proofKind、重启与响应丢失，断言对应动作永久消失、非对应候选保留、状态与 open fact 不变 | 已验证 |
| U14-45 | domain、admissionClass、budget 数值与 delegation 形状已补，但 ResourceLease 冻结基约束仍未闭合：assignment validator 接受空 budget；system validator 接受空 audience 与空 budget，现有 system 夹具就是空 audience。两套 validator 还独立实现同一 audience/budget/时间合同并已分叉（assignment 不验 expiry 晚于 issuedAt） | 只按字段类型和值域补洞，没有把两域复用的 ResourceLease 公共谓词抽成单源，也未验证可选键对象的语义基数 | core ResourceLease 两类 validator、owner 派发/system 资源锚、executor wire 准入、未来 governor/mesh guard、合法/空/污染/时间负例 | 抽取纯数据公共 ResourceLease 基谓词，assignment/system adapter 只保留各自 workload/scope/domain/activation 投影；公共层强制 audience、budget 至少一项、budget 非负整数、有效时间窗，delegation.maxBudget 同样非空且有界。补两域空对象、时间倒置、delegation 空预算负例并修正合法 system 夹具；验收=两 validator 同判公共字段且域专属语义不受影响 | 已验证 |
| U14-46 | conversation 矩阵已改为真实生产、full/guard 接受与对抗注入，但与 job 一样没有 recovery 格；恢复 projector/outbox 的真实消费者仍只散落在专项测试中，矩阵删除消费者不会失败 | 与 U14-41 同根：行为化修复遗漏了矩阵声明的恢复消费者维度 | conversation 权威 record-shape 注册表、全部日志生产者、full/guard reducer、恢复 projector/outbox、对抗测试及未来记录扩展 | 与 U14-41 共用同一 recovery 格模型：每类记录登记可执行 probe 或事实化 N/A，probe 调真实恢复入口并断言候选/回执/投影的新增或停止；保留现有生产/full/guard/对抗行为；验收=删除任一真实恢复消费时矩阵失败、全部记录零缺行 | 已验证 |
| U14-47 | 单元文件首行被状态文本拼接破坏，顶部写“完成/2轮/已冻结”，问题表 U14-41~46 却仍为待修复；E31 因漏验项不能支撑完成态 | 阶段转换更新不是原子一致写，权威状态、问题表和证据账本未机械对账 | 单元唯一状态源、续跑恢复、冻结/提交判断；不影响业务运行 | 修复标题与顶部状态；按真实证据把 U14-42~44 标为已验证、U14-41/45/46 重开，E30/E31 降级并将终审轮数清零；验收=文件结构有效且状态/问题/证据/终审四处一致 | 已验证 |
| U14-48 | 两域行为矩阵的 acceptance helper 捕获异常后只在 `AuthorityStorageError` 时失败，其余 `TypeError/Error` 被静默吞掉；同一 helper 同时用于 full reader 与 guard 业务入口，导致 full probe 抛普通异常仍被记为“full 接受” | 把“guard 已加载合法日志但业务调用可稳定拒绝”与“full reader 必须零异常”合并成同一宽松判定，门禁语义分叉 | job/conversation 行为矩阵的每类记录 full acceptance、guard replay acceptance、测试文案与未来记录扩展；运行时零影响 | full probe 直接 await，任何异常都失败；宽松 helper 仅供 guard 且改名表达“只拒 corruption”，保留 corrupt 向量必须由 `AuthorityStorageError/TypeError` 拒绝；两域完整行为矩阵全绿，故意让 fullProbe 抛普通 Error 时测试必须失败（结构审查确认直接 await） | 已验证 |
| U14-49 | system resource admission 负例通过对象展开把已签 `ResourceLease.admissionClass` 改为 interactive，却保留旧 digest/signature；公共谓词收敛后会先报 digest invalid，无法验证用例声明的 scheduler-admission 分支，executor 全测 345/346 | 负例篡改了受签名覆盖字段但未使用同一重签夹具，测试身份模型弱于真实对手“持合法签发能力但签出错误域值”的场景 | system resource coordinator 测试夹具、admission 分支、公共摘要/签名校验；运行时实现零问题 | 统一使用 `mutateSystemLease` 按实际 attempt 重算 ResourceLease digest/signature，再把 admissionClass 改为 interactive；验收=定向用例命中 `must use scheduler admission`、executor 全测通过，其他负例仍各自命中目标合同 | 已验证 |
| U14-50 | conversation 从 uncertain 提交合法迟到 bundle 后，resolution 将 assignment 标为 closed，`assignmentsAwaitingRecovery()` 在检查 committed ACK 义务前即过滤。故 owner 已 durable commit、响应在 executor ACK 前丢失时，executor 永久停在 sealed，重启无候选可重提/补 ACK 回执；job 当前因 resolution 不关闭 assignment 而未触发同一行为，但仍以同一 recoveryAssignments 同时承载两种不同恢复义务 | 同一 `recoveryAssignments` 同时承载“活跃 assignment 的派发/取消恢复”和“committed bundle 的 ACK 恢复”，conversation 的 closed/contained/rejected 停止条件错误终结后者；job 依赖不同的关闭实现才暂时不触发，继续保留会形成两域结构分叉。测试分别覆盖正常提交响应丢失与迟到 bundle，未覆盖二者正交组合 | conversation 任意 non-conflict uncertain 迟到提交；两域 full projection、恢复索引/dispatcher、executor sealed→acked、响应丢失/重启、第二次重启零 query，以及 spec sealed outbox 合同；active cancellation/dispatch recovery 停止语义 | 两域均将 ACK 义务建为独立 O(pending) 投影：committed 原子加入、仅 `bundle-ack-observed` 移除；active recovery 仍由 closed/contained/proofKind 停止。候选生成先按 ACK outbox 产 committed 候选，不受 assignment 停止事实影响；两域覆盖 uncertain late commit×响应丢失，断言首次重启重提并补回执、再次重启零 query/零提交；正常取消停止语义保持不变 | 已验证 |

## 已排除问题

> 保存已证伪疑点供复审复用;使用与重开规则见工作台静态区。裁决只允许“已排除”或“已重开→问题编号”,重开时保留原行。

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | ---------------- | -------- | -------- |
| EX14-01 | 疑点:`ControlRecord.received.ingress` 会使旧版缺顶层 ingress 的 `input` received 日志无法重放。只读最小复现确认:无论 pending,还是 `received+applied` completed 形态,当前 reducer 都会以缺 owner-derived ingress 拒绝 | 行为存在,但在“第 10 单元仅做进程内 shadow validation、分布式运行时尚未上线且没有需保留的真实旧 ControlRecord”边界内,不构成当前回归或架构债务;U14-24 已冻结“旧无 ingress 记录仅对无需 ingress 的类型兼容”,为虚构旧数据放宽 input 校验会破坏可信 ingress 安全不变量 | `control-admission.ts` 的 received ingress 重放守卫与只读 pending/completed 合成日志复现;执行计划的第 10 单元 shadow、第 15 单元接管边界;审查输入 `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 发现真实部署或持久保留的切换前 input ControlRecord 必须跨版本恢复;生产接管时点提前;或新增明确的兼容产品需求。届时必须设计显式迁移/隔离流程,不得通过伪造 ingress 或放宽验证处理 | 已排除 |

## 迟发现教训

> 仅登记“先前通过后才发现”的真实遗漏。检测动作必须可执行并写明适用范围;每个适用轮次追加执行证据。

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | -------------------------- | -------- | ------------------------------------ | ---- |
| LD14-01 | U14-40 及 job 全记录机械矩阵缺失;修复后终审一、二均记录无新问题 | 复用“full/guard 已单源”的概括结论，只抽查近期修复谓词;job 没有由权威记录类型闭合的执行点矩阵，人工审查也未逐类型核对 | 凡声称多执行点合同一致，必须先确认代码注册表或参数化测试与权威记录类型机械闭合，再逐项核对在线端、full reducer、compact guard、恢复消费者和对抗测试;新增记录或缺格必须自动失败，Markdown/人工清单不算闭合证据;适用于本单元全部多投影合同 | 第六轮:按全 record type 对账发现 U14-40;第七轮:确认 job 机械矩阵缺失;集中修复:E28 以23类权威注册表、五执行点矩阵、逐类未知字段门禁和六类 task-revision raw full/guard 负例执行闭包;当前冻结终审一:逐项复核23类 producer/full/guard/recovery/test,零缺格、无新增;当前冻结终审二:从故障与越权反向复核同一23类闭包,无新增 |
| LD14-02 | U14-42/U14-44;第七、八轮冻结终审均记录无新问题后由第九轮发现 | 冻结合同中的量词句与停止语义只核对了“存在实现”:量词域未逐元素枚举(删除停止合同漏 uncertain 态)、停止锚只核生产端未核消费端(rejectedNotStarted 无恢复消费者) | 对本单元 spec 冻结区每句带量词/停止语义的合同(全部/各自/任一/至多/仅/恒/只停止),先枚举量词域(状态×kind×形态)再逐元素指认代码与测试落点;凡“停止/拒绝/tombstone”类耐久记录,枚举其全部恢复/outbox/重驱候选生成器并核实消费过滤存在;适用于本单元全部冻结合同语句 | 本轮(问题裁决后):执行检测动作——停止锚全枚举(containedFacts/superseded/supersedeStarted/acceptedCancellations/bundle-ack/miss-alias 均有消费者,唯 rejectedNotStarted 缺=U14-44);量词句全枚举(“至多一个非终态 occurrence”“sealed outbox 只留未 ACK”“租约终结同 envelope”均已闭合;“删除取消全部在途”缺 uncertain=U14-42;禁用/删除停止谓词语义与 spec 一致);无新增同族问题 |
| LD14-03 | U14-41 改写/U14-43/U14-45;同上轮次 | 把“类型层专型/门禁测试存在”当成“生效面已闭合”:wire validator 未强制类型判别约束(lease.domain)、共享谓词接受集宽于域状态机(user 分支)、矩阵测试只断言元数据存在 | ①对每个跨 wire DTO 的类型层字面量/判别约束,打开对应运行时 validator 核实逐字段强制;②对每个按域参数化的共享谓词,按域×前态×次态×形态枚举接受集并与冻结状态机边集比对;③对每个门禁类测试(矩阵/注册表/conformance),打开断言体核实其执行所声称的行为而非元数据;适用于本单元共享合同谓词、wire validator 与全部门禁测试 | 本轮(问题裁决后):执行检测动作——①assertResourceLease 逐字段清点:domain 零校验(=U14-45),admissionClass 成员值/budget 数值/delegation 内容同缺(并入 U14-45 同根);parentId/parentDigest 经 exact-keys 拒绝、绑定字段齐;②conversation 状态值集拒绝 job-only 状态(RUN_STATES 成员校验);③conversation CONTRACT_EXECUTION_MATRIX 与 job 矩阵同款元数据断言=新增 U14-46;无其余同族 |
| LD14-04 | U14-45；第十、十一轮均记录 validator 字段清单闭合 | 字段清点只覆盖键名、成员值与数值范围，没有验证“可选键对象至少一项”的语义基数，也未比较同一 DTO 的多个 validator 是否共用基谓词 | 对所有 exact-keys 的可选键对象执行空对象、单一合法键、未知键、非法值四类测试；同一 DTO 存在第二 validator 时逐公共字段对账并抽取单源谓词。适用于本单元 ResourceLease audience/budget/delegation.maxBudget | 本轮：空 assignment budget、空 system audience/budget 均可通过；两 validator 的时间窗规则亦分叉，全部并入 U14-45 |
| LD14-05 | U14-41/U14-46；第十、十一轮把行为矩阵记为五执行点闭合 | 只确认门禁从元数据断言改成可执行测试，没有逐项核对测试体是否实际执行其声称的每个维度；生产/full/guard/对抗已执行，但 recovery 列完全不存在 | 对每个门禁声称的执行维度，必须在参数化引擎中找到可执行格或事实化 N/A；以删除该执行点能否使门禁失败作验收。适用于两域记录行为矩阵及后续 conformance 门禁 | 本轮：逐测试体对账确认两域均缺 recovery 格，重开 U14-41/U14-46；其余四维已有真实调用 |
| LD14-06 | U14-48；第十三轮把两域矩阵的 full/guard acceptance 记录为通过 | 只确认 probe 被调用，没有逐分支检查“通过 helper”如何处理异常；full 与 guard 复用宽松 helper，使门禁在普通异常下假绿 | 对所有“应接受/应成功”门禁，打开 helper 的 catch 分支并验证非预期异常必然向外抛出；同一 helper 服务不同完成语义时必须拆分。适用于本单元两域行为矩阵及后续 conformance 门禁 | 本轮：两域 acceptance helper 同根命中；rejection helper 会在未拒绝或异常类型不符时失败，其他矩阵断言未发现同类吞错 |
| LD14-07 | U14-49；两轮重新冻结终审通过后由最终 executor 全测发现 | 定向验证覆盖了新公共谓词，但未检查既有“合法签名下的错误业务值”负例是否仍能越过摘要层命中目标分支；旧夹具直接展开篡改受签字段 | 对所有声称验证签名后业务分支的负例，确认篡改后重算摘要并合法重签；仅验证摘要/签名拒绝的用例可保留旧签名。适用于本单元 ResourceLease、Capability、Proof 与 envelope 负例 | 本轮：扫描 ResourceLease 新增/既有负例，只有 invalidAdmission 直接展开受签对象；其余 mutateSystemLease/jobWithLease 均重算摘要与签名，proof/envelope 污染用例按声明分别验证签名层或重签业务层 |
| LD14-08 | U14-50；U14-49 后两轮终审均记录无新问题 | 分别验证“正常提交响应丢失”和“uncertain 迟到提交”，却未对跨状态转换仍存续的 ACK 义务做正交组合；又把 recoveryAssignments 存在误当为不同恢复义务均已独立建模 | 对每个跨状态仍存续的 outbox/恢复义务，枚举所有可到达该状态的来源与会清除候选的停止事实，逐一验证“义务建立→状态关闭/收编→响应丢失→重启→唯一回执”；多个义务共用索引时必须证明清除条件等价，否则拆分。适用于两域 dispatch/cancel/supersede/bundle-ACK 恢复索引 | 第十八轮：枚举两域 recoveryAssignments 全部 add/delete/filter；确认 normal commit 可恢复、conversation 任意 uncertain late commit 被 resolution close 过滤；job 当前不关闭 assignment，但索引仍承载两种清除语义。contained/rejected-not-started 只能停止对应 active recovery，不得停止已 committed ACK；同族收齐为 U14-50 |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| E1 | 最终源码类型、格式与冻结合同结构闭合 | core/owner/executor 各包 `tsc -p tsconfig.json --noEmit`;`pnpm exec biome check` 14 代码/测试文件;`pnpm contracts:lint` | 冻结 16 文件中的代码/测试/合同与三包源码 | 最终 / 低 / 分段执行 | 三包类型零错;Biome 14 文件零问题;合同 schema lint 通过 | `sha256:c894ed07e324a190cc49f6c836474564f000eb7e39a2bf6b035ad45e28fff2cf` | 失效(owner 子项由 E6 重建;core/executor/合同子项输入未变经 E6 声明继承) |
| E2 | Unit 14 全部直接行为、44 行状态机、6 排列竞态与新增资源边界 | `pnpm --filter @zhixing/executor test -- src/__tests__/job-assignment.test.ts` | 最终 core dist + owner/executor 源与 Unit 14 测试 | 最终 / 中 / 45.42s | 86/86 通过 | `sha256:c894ed07e324a190cc49f6c836474564f000eb7e39a2bf6b035ad45e28fff2cf` | 失效(由 E6 在新指纹上完整重跑) |
| E3 | 共享账本与 owner 既有功能完整回归 | owner-kernel 与 executor 包级完整测试（开发收敛阶段各执行一次） | prompt 上界最终增量前的同族完整源码;最终增量仅触及 job prompt validator 与其直接测试,由 E1/E2 覆盖 | 收尾 / 高 / 已执行 | owner 59/59、executor 207/207;core 既有 2189/2189;最终增量未触及其余路径 | 输入闭包继承 + E1/E2 最终增量闭合 | 失效(当前 core/executor/owner 输入均变化,由 E19 重建) |
| E4 | 最终交付可被全部 workspace 包消费 | `pnpm build` | 冻结源码 + workspace package manifests/lockfile | 最终 / 高 / 165.9s | 17 个 workspace 包 ESM/DTS 全部构建成功 | `sha256:c894ed07e324a190cc49f6c836474564f000eb7e39a2bf6b035ad45e28fff2cf` | 失效(当前 21 文件指纹已变化,由 E19 重建) |
| E5 | 补丁结构、文件闭包与冻结后无漂移 | `git diff --check HEAD -- <16 files>`;复算 16 文件 manifest 指纹 | 冻结 16 文件 | 冻结 / 低 / <10s | diff-check 零错;指纹复算一致 | `sha256:c894ed07e324a190cc49f6c836474564f000eb7e39a2bf6b035ad45e28fff2cf` | 失效(由 E7 在新指纹上重建) |
| E6 | U14-08/09 修复后类型、格式与直接行为闭合(E1 的 owner tsc 与 E2 全量由此重建;core/executor tsc、biome 其余 13 文件、contracts:lint 输入未变继承 E1;executor 既有 conversation 套件输入闭包未变继承 E3) | owner `tsc --noEmit`;`pnpm --filter @zhixing/executor test -- src/__tests__/job-assignment.test.ts`;`pnpm --filter @zhixing/owner-kernel test`;`pnpm --filter @zhixing/owner-kernel build`;`biome check job-assignment.ts` | 修复后 owner/executor 源 + core dist | 修复轮 / 中 / 67.68s+6.15s | tsc 零错;86/86;59/59;DTS 构建成功(cancelOrigins 为内部投影字段,导出面零变化,E4 全量构建结论继承);biome 零问题 | `sha256:c7a3e219253efcf7fe654389542a1e9d6de1fd61c2d7bf2680977cb117133e20` | 失效(owner 源再变,由 E8 在新指纹上重建) |
| E7 | 修复后补丁结构与冻结无漂移 | `git diff --check HEAD -- <16 files>`;复算 16 文件 manifest 指纹;grep cancelOrigins 零残留 | 冻结 16 文件 | 冻结 / 低 / <10s | diff-check 零错;指纹复算一致;job-assignment.ts 零残留 | `sha256:c7a3e219253efcf7fe654389542a1e9d6de1fd61c2d7bf2680977cb117133e20` | 失效(由 E8/E9 在新指纹上重建) |
| E8 | U14-10/11/12 修复后全部直接行为与回归闭合:job 直接套件(44 行逐边、6 排列竞态、占用矩阵、fence 8 字段污染、guard 三用例)、owner 全测、executor conversation 套件(共享谓词库 SharedRunState 放宽的消费面)、双包类型、4 改动文件格式;**executor 测试经 package exports 消费 owner-kernel dist(E2/E6 曾误记为源)——owner 源改动必须先单包 build 再跑,已按此串行执行** | `pnpm --filter @zhixing/owner-kernel build`;owner/executor `tsc --noEmit`;`pnpm --filter @zhixing/executor test -- src/__tests__/job-assignment.test.ts`;`pnpm --filter @zhixing/owner-kernel test`;`pnpm --filter @zhixing/executor test -- src/__tests__/assignment-ledger.test.ts`;`biome check` 4 文件 | 修复后 owner 源→dist + executor 源/测试 + core dist | 修复轮 / 高 / 42.18s+4.15s+78.71s | DTS 构建成功(guard 全内部、无新导出,dts 消费面等价→E4 其余 15 包继承);双包 tsc 零错;103/103;59/59;117/117;biome 零问题 | `sha256:141275b170fc27d6bb0c9dff07edc8a7b6f18298a7638ebc694965804fcf4e5e` | 有效 |
| E9 | 修复后补丁结构与冻结无漂移 | `git diff --check HEAD -- <16 files>`;复算 16 文件 manifest 指纹 | 冻结 16 文件 | 冻结 / 低 / <10s | diff-check 零错;指纹复算一致 | `sha256:141275b170fc27d6bb0c9dff07edc8a7b6f18298a7638ebc694965804fcf4e5e` | 失效(由 E10/E11 在新指纹上重建) |
| E10 | U14-13/14 修复后全部直接行为与回归闭合(E8 的 owner 侧由此重建;executor 源与 conversation 套件、core 输入闭包未变继承 E8) | `pnpm --filter @zhixing/owner-kernel build` 先行;owner/executor `tsc --noEmit`;job 直接套件;owner 全测;`biome check` 3 改动文件 | 修复后 owner 源→dist + executor 测试 + core dist | 修复轮 / 高 / 48.54s+3.73s | DTS 构建成功;双包 tsc 零错;109/109(含 mirror 合同 3 用例与 cancelProof 重放 3 用例);59/59;biome 零问题 | `sha256:c3244eaf94c98c5c69dcd89c16c29b97fcb251d6ff7c1b0aaf62e548794ab25a` | 有效 |
| E11 | 修复后补丁结构与冻结无漂移 | `git diff --check HEAD -- <16 files>`;复算 16 文件 manifest 指纹 | 冻结 16 文件 | 冻结 / 低 / <10s | diff-check 零错;指纹复算一致 | `sha256:c3244eaf94c98c5c69dcd89c16c29b97fcb251d6ff7c1b0aaf62e548794ab25a` | 失效(由 E12/E13 在新指纹上重建) |
| E12 | U14-15/16 修复后全部直接行为与回归闭合(E10 的 owner 侧由此重建;conversation 套件与 core 输入闭包未变继承) | `pnpm --filter @zhixing/owner-kernel build` 先行;owner `tsc --noEmit`;job 直接套件;owner 全测;`biome check` 2 改动文件 | 修复后 owner 源→dist + executor 测试 + core dist | 修复轮 / 高 / 42.75s+4.26s | DTS 构建成功(JobJournalOptions 仅追加可选字段,向后兼容);owner tsc 零错;112/112(含伪造票据拒、未配置 fail-closed、超限批次拒 3 用例);59/59;biome 零问题 | `sha256:28c687f9294eba1e338805360e3d191c7e81373fca4d2cbd57cdb384a071fd48` | 有效 |
| E13 | 修复后补丁结构与冻结无漂移 | `git diff --check HEAD -- <16 files>`;复算 16 文件 manifest 指纹 | 冻结 16 文件 | 冻结 / 低 / <10s | diff-check 零错;指纹复算一致 | `sha256:28c687f9294eba1e338805360e3d191c7e81373fca4d2cbd57cdb384a071fd48` | 失效(由 E14 在新指纹上重建) |
| E14 | U14-17 注释级修复闭合:零行为面(仅注释文本),行为证据(E12 的 112/112、59/59 与 conversation 继承链)按输入闭包继承——注释不参与任何断言、digest 域或运行时路径 | 双包 `tsc --noEmit`;`biome check` 3 改动文件;`pnpm --filter @zhixing/owner-kernel build`;交付物源码 § 残留 grep;`git diff --check`;复算 16 文件指纹 | 修复后三源文件 + 全交付物 | 修复轮 / 低 / <60s | 双包 tsc 零错;biome 零问题;DTS 构建成功;§ 残留 0;diff-check 零错;指纹一致 | `sha256:ec5f903fbc0f2dfd0dbc258b4cf1431ece85e0ef94c81fb779b0a5b27c4552f2` | 有效 |
| E15 | U14-18~23 集中修复前诊断:确认当前机械断点与日志尺寸反例,不作为通过证据 | core build→owner `tsc --noEmit`;40k prompt 通过 core validator 后计算 task-revision canonical 字节 | 当前未完成修复源 + core dist | 诊断 / 低 / 75s | core 构建成功;owner tsc 因 systemMissAliases 错/漏初始化 2 错;40k prompt 40,286 bytes > 32,768 limit | 未冻结 | 诊断 |
| E16 | U14-18~24 裁决后集中修复前诊断:确认半成品机械断点、恢复装配与请求身份分叉,不作为通过证据 | core/owner/executor 源码交叉检查;core tsc;owner/executor tsc;既有 job-run 与 dispatcher 装配静态扫描 | 当前 19 文件未完成修复源 + 现有 dist | 诊断 / 低 | core 源类型通过;owner 因 sidecar helper/投影初始化/类型闭包未完成而失败;executor 因 core dist 未同步失败;启用 dispatcher 缺 bundleSubmission,既有 job-run 测试缺 ingress;完整 ingress 进入 payloadDigest 与 spec 分叉 | 未冻结 | 诊断 |
| E17 | U14-18~24 集中修复直接闭合;随后完整跨链静态复核发现 U14-25~28,故只作为已解决七项的直接证据、不作为冻结通过证据 | core/owner/executor 类型检查;core/owner build;`pnpm --filter @zhixing/executor test -- src/__tests__/job-assignment.test.ts` | U14-18~24 修复后的 19 文件与同步 dist | 修复轮 / 中 / job 约74s | 三包类型零错;core/owner build 成功;job 123/123;静态复核新增四项已登记 | 未冻结 | 诊断 |
| E18 | U14-25~28 集中修复直接闭合:ACK 回执/outbox、occurrence 三点准入、conflict 原子关闭、system resource 同断言重放与同根 validator/错 ref 前置拒绝 | 三包 `tsc --noEmit`;core/owner build;job 直接套件首轮与失败根因隔离;owner job predicate 直接测试;ACK/containment/system replay 定向用例;Biome 8 文件;`git diff --check` | 当时 21 交付文件及由 core/owner build 同步的 dist | 修复轮 / 中 / 分段执行 | 三包类型零错;core/owner 构建成功;job 首轮 127/129,唯一共同候选引用根因修复后原失败 2/2;最终增量 owner predicate 12/12、ACK 两路径 2/2、standalone containment 与缺 coordinator 2/2、ledger ACK 1/1;Biome/diff-check 零错 | `sha256:d4fabca05635e754789e04995da90a3d2664a2ede1770ece2642d4e41aef12a6` | 失效(冻结终审发现 U14-29~32 后交付物变化,由 E20 重建直接证据) |
| E19 | 冻结指纹的最终回归与交付门禁;当前执行在 owner 全测发现确定性覆盖矩阵缺口,已停止后续昂贵命令并回问题裁决 | 依赖顺序:`pnpm --filter @zhixing/core test`→`pnpm --filter @zhixing/owner-kernel test`→`pnpm --filter @zhixing/executor test`→`pnpm build`→`pnpm contracts:lint`→三包 `tsc --noEmit`→`biome check` 交付代码/测试→`git diff --check`+复算指纹 | 当时冻结 22 文件 + workspace manifests/lockfile + 同步 dist | 最终 / 高 / core 67.4s+owner 5.81s | core 132 文件/2189 测试通过;owner 90/91,唯一失败为 conversation `bundle-ack-observed` 执行矩阵缺行;失败已归因 U14-36,未继续 executor/build | `sha256:2e15bd11fbb384893c7524e9066b8d7d4f647024beb64be61bf01c0d4eeff171` | 诊断 |
| E20 | U14-29~32 集中修复直接闭合:两域 ACK 谓词与 conversation O(pending) outbox、控制结果窄联合、system/user 数据面隔离、稳定注释及 full/guard 对抗矩阵 | 三包 `tsc --noEmit`;core/owner build;owner control+job 合同测试;conversation ACK 恢复/伪造定向测试;job ACK 回归;Biome 改动代码/测试;`git diff --check`;复算 22 文件指纹 | 当时 22 交付文件及同步 core/owner dist | 修复轮 / 中 / 分段执行 | 三包类型零错;core/owner 构建成功;control+job 合同 25/25;conversation ACK 两类恢复+三类伪造 5/5;job ACK 2/2;Biome/diff-check 零错 | `sha256:c6133cc758031fa74ab1b8f78a8fb2352c8aed5dfa39320fdd7241a7a257e308` | 失效(U14-33/U14-34 修复后输入变化,由 E21 重建直接证据) |
| E21 | U14-33/U14-34 集中修复直接闭合:system 紧凑/definition 两层谓词、双向 user/system state 联合、guard systemResult/ACK 投影与 full/guard 对抗矩阵 | owner/executor `tsc --noEmit`;owner `job-run-contracts.test.ts`;owner build 后 executor `job-assignment.test.ts -t system job local protocol|job submission guard preflight`;失败归因为错误 ACK 测试引用不存在 artifact,改用已存在异身份 artifact 后仅重跑 ACK 三项;Biome 4 文件;`git diff --check`;复算指纹 | 修复后 22 交付文件及同步 owner dist | 修复轮 / 中 / 分段执行 | 双包类型零错;owner 26/26;DTS 构建成功;相关集成首轮 24/25(唯一夹具在日志引用层提前拒绝),修正夹具后 ACK 3/3;合法 system 执行/恢复、user 伪 activation 与 ACK ref/revision/duplicate 的 full+guard 对账全通过;Biome/diff-check 零错;指纹一致 | `sha256:b2a9d6985da9b8a137f010c09e916db8eb54945842cb69a8464b96f126f80f65` | 失效(同根结构验收继续收口,由 E22 重建) |
| E22 | U14-33/U14-34 最终结构收口:在线初次/替换/终态均从实际待追加记录机械投影原子伴随;job full/guard ACK 直接共用同一绑定谓词;definition handler/paramsDigest 负例闭合 | owner/executor `tsc --noEmit`;owner `job-run-contracts.test.ts`;owner build 后 executor `job-assignment.test.ts -t system job local protocol|job submission guard preflight`;Biome 3 文件;合同调用点/常量与独立 canonical 表达 grep;`git diff --check`;复算 22 文件指纹 | 最终 22 交付文件及同步 owner dist | 修复轮 / 中 / 约55s | 双包类型零错;owner 29/29;DTS 构建成功;相关集成 25/25;Biome/diff-check 零错;在线生产无原子伴随常量,full/guard 直接共用谓词;指纹一致 | `sha256:c763e440523698b56e6a324e44a6a162aeeb12a189aae8fa34a442cd6644e738` | 失效(仅注释输入由 E23 显式继承行为证据并重建结构门禁) |
| E23 | U14-35 注释级修复闭合:只改 executor 默认 usageFinal 的说明文本,不改变 AST、摘要域、类型或运行路径;E22 的类型/直接行为/构建证据按零行为输入差异继承 | `biome check assignment-ledger.ts`;生产源码注释债务扫描;`git diff --check` 22 文件;复算 22 文件指纹 | 最终 22 交付文件 | 修复轮 / 低 / <10s | Biome/diff-check 零错;生产源码无 placeholder、易变阶段或裸章节号注释;指纹一致 | `sha256:2e15bd11fbb384893c7524e9066b8d7d4f647024beb64be61bf01c0d4eeff171` | 有效 |
| E24 | U14-36 合同执行矩阵闭合:ACK 合同身份、`bundle-ack-observed` 五执行点行与共享谓词正反例在场;生产代码零变化,既有两域 ACK 集成负例证据继承 E20/E22 | owner conversation 合同矩阵定向测试;owner `tsc --noEmit`;Biome 新测试文件;`git diff --check` 23 文件;复算指纹 | 修复后 23 交付文件 | 修复轮 / 低 / 8.2s+11.3s | 矩阵 51/51;owner 类型零错;Biome/diff-check 零错;自动 record-type 闭包门禁通过;指纹一致 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 有效 |
| E25 | 新冻结指纹的最终回归与交付门禁;证明两轮独立终审后的同一交付物可完整测试、构建并通过结构门禁 | `pnpm --filter @zhixing/core test`→`pnpm --filter @zhixing/owner-kernel test`→`pnpm --filter @zhixing/executor test`→`pnpm build`→`pnpm contracts:lint`→三包 `tsc --noEmit`→`biome check` 23 文件中的代码/测试→`git diff --check`+复算指纹 | 冻结 23 文件 + workspace manifests/lockfile + 同步 dist | 最终 / 高 / core 67.4s+owner 11.5s+executor 106.2s+build 145.4s+低成本门禁 | core 132 文件/2189 测试、owner 3 文件/92 测试、executor 3 文件/261 测试全部通过;17 个 workspace 包 ESM/DTS 构建成功;合同 lint 与三包类型零错;Biome 21 文件零问题;diff-check 零错;23 文件指纹复算一致 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 失效（U14-37~39 修复改变四个业务文件,由 E27 重建最终证据） |
| E26 | U14-37~39 根治闭合:trigger ingress 来源收敛、admission 与 system miss 合同谓词单源、state 未知 kind fail-closed | owner/executor `tsc --noEmit`;owner `job-run-contracts.test.ts`;owner build 后 job 相关 6 项定向与完整 `job-assignment.test.ts`;Biome 4 改动文件;合同调用点扫描;`git diff --check`;复算 23 文件指纹 | 修复后四个业务文件 + 既有 19 文件合同闭包 + 同步 owner dist | 修复轮 / 中 / 分段执行 | 双包类型零错;owner 合同 41/41;DTS 构建成功;定向 6/6、job 完整 138/138;Biome/diff-check 零错;在线/full/guard 共用谓词且无 trigger ingress 调用残留;指纹一致 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 失效（U14-40/U14-41 改变同一输入,由E28重建直接证据） |
| E27 | 两轮零新增后的唯一最终回归与交付门禁;证明冻结交付物的包级行为、workspace 消费、合同、类型、格式和指纹全部闭合 | 串行执行:`pnpm --filter @zhixing/core test`→owner test→executor test→`pnpm build`→`pnpm contracts:lint`→core/owner/executor `tsc --noEmit`→`biome check` 21 个代码/测试文件→`git diff --check` 23 文件→复算指纹 | 冻结 23 文件 + workspace manifests/lockfile + 由 build 同步的全部 dist | 最终 / 高 / core 63.6s+owner 11.6s+executor 106.3s+build 164.4s+低成本门禁 | core 132 文件/2189 测试、owner 3 文件/104 测试、executor 3 文件/265 测试全部通过;17 个 workspace 包 ESM/DTS 构建成功;合同 lint 与三包类型零错;Biome 21 文件零问题;diff-check、skip/only 与注释债务扫描零问题;23 文件指纹复算一致 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 失效（U14-40/U14-41 改变四个业务文件,由E28重建增量证据、E29重建最终证据） |
| E28 | U14-40/U14-41 集中修复闭合:task-revision 紧凑合同三点单源,23类 job 日志权威字段注册表与五执行点矩阵机械闭合 | owner/executor `tsc --noEmit`;owner `job-run-contracts.test.ts`;owner build;六类 raw task-revision full/guard 定向负例后完整 `job-assignment.test.ts`;Biome 4 改动文件;`git diff --check`;复算23文件指纹 | 四个改动业务文件 + 与E27字节相同的其余19文件合同闭包 + 同步 owner dist | 修复轮 / 中 / 分段执行 | 双包类型零错;owner 合同74/74;DTS构建成功;task-revision定向6/6、job完整144/144;23类×5执行点零缺格、23类未知字段逐类拒绝;Biome/diff-check零错;指纹一致 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 有效 |
| E29 | 两轮零新增后的唯一最终回归与交付门禁;证明冻结交付物的包级行为、workspace消费、合同、类型、格式和指纹全部闭合 | 串行执行:`pnpm --filter @zhixing/core test`→owner test→executor test→`pnpm build`→`pnpm contracts:lint`→core/owner/executor `tsc --noEmit`→`pnpm exec biome check` 交付物文件集中21个TypeScript文件→`git diff --check` 23文件→`rg` 扫描 `.only`/`.skip` 与注释债务→按登记算法复算指纹 | 冻结23文件 + workspace manifests/lockfile + 由build同步的全部dist | 最终 / 高 / core 63.13s+owner 6.89s+executor 104.57s+build 180.7s+低成本门禁 | core 132文件/2189测试、owner 3文件/137测试、executor 3文件/271测试全部通过;17个workspace包ESM/DTS构建成功;合同lint与三包类型零错;Biome 21文件零问题;diff-check、skip/only与注释债务扫描零问题;23文件指纹复算一致 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 失效(U14-41~46 集中修复改变交付物,由 E30 重建直接证据、E31 重建最终证据) |
| E30 | U14-41~46 集中修复直接证据:U14-42~44 已闭合；validator 域和值域、两域生产/full/guard/对抗矩阵已通过，但后续终审确认 ResourceLease 基约束与矩阵 recovery 维度仍缺失 | core build;owner build;core/owner/executor `tsc --noEmit`;owner/executor 包测;Biome 8 文件 | 当时 23 交付文件 + 同步 core/owner dist | 修复轮 / 高 | U14-42~44 及矩阵既有四维证据仍可复用；U14-41/45/46 的“完整闭合”结论无效 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 诊断 |
| E31 | 旧指纹包级回归与交付门禁；机械测试结果真实，但无法覆盖后续发现的合同与门禁缺口 | core→owner→executor 包测→build→contracts lint→三包 tsc→Biome→diff/skip/only→指纹 | 旧冻结 23 文件 + workspace manifests/lockfile + dist | 最终 / 高 | 全部命令通过；因 U14-41/45/46 仍存在，不能支撑完成态，修复后需按新输入重建最终证据 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 失效 |
| E32 | U14-41/45/46 集中修复直接闭合：ResourceLease 公共谓词单源，两域可选键对象基数与时间合同封闭；两域权威记录矩阵补齐真实 recovery consumer / 事实化 N/A 格 | core/executor `tsc --noEmit`;core build;Biome 5 文件;job `-t \"job resource lease domain closure|recovery contract|binds every\"`;conversation `-t \"recovery contract|binds every\"`;双 diff-check | 修复后 24 个业务文件 + 同步 core dist | 修复轮 / 中 / 分段执行 | 双包类型零错;core build 成功;Biome/diff-check 零问题;job 15/15、conversation 8/8 通过；恢复探针直接调用 pending/recover/resume 等真实消费者，权威类型与 probe 注册表零缺格 | `sha256:2b0cc21bf88f6e0804abb26c860796ca897825d59e45764d8a5559bfc798f5f3` | 有效 |
| E33 | U14-48 门禁异常传播闭合：full reader 直接执行且任意异常失败，guard helper 仅把已加载日志后的稳定业务拒绝视为接受 | executor `tsc --noEmit`;Biome 两测试文件;job/conversation 两域完整 execution-point behavior matrix;双 diff-check | 修复后 24 个业务文件 | 修复轮 / 中 / 47.2s+41.5s | 类型、Biome、diff-check 零问题；job 32/32、conversation 28/28 全绿，全部记录的 full probe 均零异常，guard 与 corrupt 向量维持原合同 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 有效 |
| E34 | 冻结后最终验证；executor 全测暴露 U14-49 后按纪律停止，不继续 build/门禁 | core test→owner test→executor test | 冻结 24 文件 + workspace 输入 | 最终 / 高 / 93.6s+40.9s+202.1s | core 132文件/2189、owner 3文件/139 通过；executor 345/346，唯一失败为 invalidAdmission 夹具旧摘要；失败已归因 U14-49，后续命令未执行 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 诊断 |
| E35 | U14-49 夹具身份闭合：错误 admission 值仍由有效 ResourceLease 摘要/签名覆盖，并命中目标业务分支 | executor `tsc --noEmit`;Biome job 测试；invalidAdmission 定向用例；双 diff-check | 修复后 24 个业务文件 | 修复轮 / 低 / 18.9s+低成本门禁 | 类型、Biome、diff-check 零问题；定向 1/1 命中 `must use scheduler admission` | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 有效 |
| E36 | 原冻结指纹的最终回归与交付门禁；机械结果真实，但未覆盖 U14-50 的 uncertain late-commit×ACK 响应丢失组合 | 复用 E34 中输入未变的 core/owner 包测；重跑 executor 全测；随后串行 `pnpm build`、`pnpm contracts:lint`、三包 `tsc --noEmit`、Biome 22 文件、双 diff-check、skip/only 与注释债务扫描、指纹复算 | 原冻结 24 文件 + workspace manifests/lockfile + 最新 dist | 最终 / 高 / executor 196.6s+build 212.5s+低成本门禁 | 全部既有命令通过；因 U14-50 当时已存在，不能支撑完成态，修复后需在新输入上重建最终证据 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 诊断 |
| E37 | U14-50 直接闭合：两域 active recovery 与 committed ACK outbox 分离，真实重启后迟到提交响应丢失唯一收敛，二次重启零查询 | owner/executor `tsc --noEmit`;owner build 后两域 late-commit×response-loss 定向测试；两域完整 record execution-point behavior matrix；ACK/late/response-loss 同族 17 项；Biome 4 文件；24 文件 diff-check 与指纹复算 | 四个改动业务文件 + 与 E36 字节相同的其余 20 文件 + 同步 owner dist | 修复轮 / 中 / 分段执行 | 双包类型零错；真实重启定向 2/2、同族 17/17、行为矩阵连同定向 62/62；Biome/diff-check 零问题；两域 commit 加入独立 O(pending) outbox、仅 ACK 回执移除；新指纹一致 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 有效 |
| E38 | 两轮零新增后的唯一最终验证：受影响 owner/executor 包回归、workspace 构建与交付门禁；core 包测及合同 lint 输入未变复用 E34/E36 | 串行 `pnpm build`→owner test→executor test→owner/executor `tsc --noEmit`→Biome 4 改动文件→24 文件 diff-check→skip/only 与注释债务扫描→复算指纹；复用 core 2189/2189 与 contracts lint | 冻结 24 文件 + workspace manifests/lockfile + 由 build 同步的全部 dist | 最终 / 高 / build 209.7s+owner 7.85s+executor 180.47s+低成本门禁 | 17 个 workspace 包 ESM/DTS 构建成功；owner 3 文件/139、executor 3 文件/347 全绿；双包类型与 Biome 零问题；diff-check、skip/only、注释债务扫描通过；24 文件指纹复算一致 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 有效 |

## 终审记录

| 轮次 | 审查侧重 | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论 |
| ---- | -------- | ------------ | -------- | ------------ | ---- |
| 第一轮 | 需求、架构、功能闭环、状态、回归 | 九面完整;以 23 文件新指纹从架构边界、状态机、生产/full/guard/recovery 与新增矩阵闭包重新裁决,仅共享机械事实 | 无 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 通过 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 九面完整;独立按 raw ACK 注入、并发 observe、双崩溃恢复、canonical 身份和测试弱化风险反向裁决 | 无 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 通过 |
| 第三轮（独立终审） | 入口来源权威、full/guard 合同单源、fail-closed 与切换兼容边界 | 沿生产端、full reducer、compact guard、恢复消费者和测试覆盖完成同族核查;合成旧日志只证明行为,另行核对生产接管事实后裁决兼容疑点 | U14-37~39;EX14-01 已排除 | `sha256:47323ce04fa5a916ae1d3bfd0ce168e816688eee4e4af8fb6578de960044d6fa` | 不通过,待集中修复 |
| 第四轮（修复后终审一） | 需求、架构、功能闭包、状态与回归;完整消费四片信息包并对修复增量下钻 | 九面完整;核对全身份链、全部入口/生产者、full/guard/recovery、跨切片合同分叉与后续消费者;EX14-01 未触发重开 | 无 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 通过 |
| 第五轮（修复后终审二） | 并发、崩溃、安全、资源上界、异常终态与测试盲区;独立反向裁决 | 九面完整;从恶意 raw 日志、乱序/重复/响应丢失、缓存与索引失效、未知身份及后续消费者反查,按疑问下钻 S2/S3;EX14-01 未触发重开 | 无 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 通过 |
| 第六轮（独立终审） | 从头复核完整交付物，重点对账 full/guard 合同等价性与 system 恢复边界 | 按全 record type 枚举发现 task-revision 三执行点分叉;其余闭包无新问题 | U14-40;S1 重开;LD14-01 待应用 | `sha256:80e1c9f05d4a7d87a680e4121cc8f73a5e560aff7a9be101ee7e66169167f617` | 不通过,待修复 |
| 第七轮（当前冻结终审一） | 需求、架构、功能闭包、状态与回归;从冻结事实包独立裁决 | 九面完整;复核完整身份链、23 类执行点、切片边界、恢复消费者和后续接缝;EX14-01 未触发,LD14-01 已执行 | 无 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 通过,连续无新增1/2 |
| 第八轮（当前冻结终审二） | 并发、崩溃、安全、资源上界、异常终态与测试盲区;独立反向裁决 | 九面完整;复核事务竞争、响应丢失、恶意 raw 日志、历史身份、分页与恢复索引及 conversation 共享改动;EX14-01 未触发 | 无 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 通过,连续无新增2/2 |
| 第九轮（独立终审） | 任务删除与全部在途状态组合；user/system 状态边及 assignment 形态笛卡尔复核 | 沿任务定义、uncertain 停止义务、proof/bundle 胜负、在线/full/guard/recovery 与测试矩阵完成同族核查 | U14-42~43；S1/S2/S3 重开 | `sha256:162e6d849b0e3ba7147bbb9e841419c3f7e0dee328aa8fa15c7ca3ece3ce7d83` | 不通过，待集中修复 |
| 第十轮（修复后冻结终审一） | 需求、架构、功能闭包、状态与回归;LD14-01/02/03 检测动作逐项重执行 | 九面完整:量词句全枚举(删除×{queued,dispatched/running,uncertain 无栅栏/有栅栏,cancel-requested 既有栅栏}逐格有代码臂+测试;停止/拒绝类耐久记录全枚举消费者,rejectedNotStarted 四链单源键 6 调用点零内联残留);validator 字段清单复核(domain/admissionClass/budget/delegation/activation/workload 全闭合);两域行为矩阵=代码注册表 satisfies 闭合+43 格逐格执行(生产/接受/对抗);38+6 行与两域全部集成 329/329 回归;spec 两处同步句与实现逐字段同构;交付物源码零注释债务;EX14-01 边界未触发。core 全测与全量 build 按纪律归 E31 | 无 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 通过,连续无新增 1/2 |
| 第十一轮（修复后冻结终审二） | 并发、崩溃、安全、资源上界、异常终态与测试盲区;独立反向裁决 | 九面完整:删除栅栏为单 envelope 原子追加(零半写窗口);停止锚消费为纯读投影、与候选生成同一 #select 快照(零并发窗口);uncertain 无 assignment 的 raw deleted 组合 fail-closed(合法日志不可达,拒绝正确);running→cancelled 新边与 spec 行 18/33 对照成立;四个 20s 时限均为磁盘压力下的既有慢测试、非掩蔽回归;system 场景 guard N/A 有事实依据(鉴权先于 guard 加载)且 system guard 行为仍由既有 raw 注入专项承载;矩阵对抗向量单向量/类为下限、既有专项负例(fence 8 字段/ACK 绑定/mirror 合同/task-revision 六类)保留为深度层;指纹计算时点在最后一次交付物编辑之后(spec 编辑先于计算,其后仅动单元文件) | 无;S1~S4 四片经本轮横向确认转局部封版 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 通过,连续无新增 2/2 |
| 第十二轮（独立终审） | 从头核对公共 wire DTO 语义基数、同合同多 validator 单源性及门禁测试实际执行维度 | 沿 ResourceLease 两类生产/validator/未来 guard 与两域行为矩阵测试体逐项核对；U14-42~44 直接证据仍成立 | U14-41/U14-45/U14-46；LD14-04/05 已应用；U14-47 已修复 | `sha256:60d070bd3036b503d3a04df9d4880a78ab0c26f70f1386115f9556f0575bc397` | 不通过，进入集中修复 |
| 第十三轮（新冻结终审一） | 需求、架构、功能闭包、状态与回归；从新指纹独立核对本轮修复及完整单元 | ResourceLease 与 recovery 格本体闭合；第二轮检查门禁异常传播时发现 acceptance helper 语义不严，故本轮关于“full 接受已严格证明”的结论失效 | U14-48；LD14-06 已登记并完成同根扫描 | `sha256:2b0cc21bf88f6e0804abb26c860796ca897825d59e45764d8a5559bfc798f5f3` | 不通过，待集中修复 |
| 第十四轮（重新冻结终审一） | 需求、架构、功能闭包、状态与回归；以 U14-48 后新指纹从头裁决 | 九面完整：38+6 状态机及跨 attempt 身份链未变；公共租约谓词与两域 adapter 边界封闭；23 类 job/20 类 conversation 记录绑定真实生产、full 零异常接受、guard corruption 接受边界、真实 recovery probe/N/A 与对抗拒绝；派生闭包和未来接缝一致 | 无 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 通过，连续无新增 1/2 |
| 第十五轮（重新冻结终审二） | 并发、崩溃、安全、资源上界、异常终态与测试盲区；独立反向裁决 | 九面完整：恶意 ResourceLease 空/污染/过期/签名与两域专型错绑定均 fail-closed；恢复候选新增/保留/停止、system 重驱/miss alias 与 commit projection 均有真实 probe；full acceptance 任意异常失败，guard 仅放行业务稳定拒绝，corrupt 向量须明确拒绝；跨片身份链、恢复与后续消费者无新分叉 | 无 | `sha256:26e617cf2bd3abe72586443f6409a2fbcc2ce99202ac63722f05e8975d207249` | 通过，连续无新增 2/2 |
| 第十六轮（U14-49 后冻结终审一） | 需求、架构、功能闭包、状态与回归；以新指纹重新裁决 | U14-49 仅改变测试夹具：按实际 attempt 构造、去旧 digest/signature、修改业务值、重算 ResourceLease 摘要并重签；目标 admission 分支真实可达。完整单元的生产/full/guard/recovery 合同和包边界未变 | 无 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 通过，连续无新增 1/2 |
| 第十七轮（U14-49 后冻结终审二） | 安全、异常路径、测试盲区与证据复用；独立反向裁决 | 全量扫描 ResourceLease 测试生产器与负例：jobWithLease/mutateSystemLease 均重算摘要与签名，invalidAdmission 使用实际 attempt；签名污染测试与业务值负例边界清楚。生产代码未变，E34 core/owner 证据可按输入闭包复用 | 无 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 通过，连续无新增 2/2 |
| 第十八轮（独立终审） | 两域恢复义务、状态关闭与响应丢失的正交组合 | 九面完整；沿 committed producer、resolution/containment reducer、O(pending) 索引、dispatcher、executor sealed/acked 与现有测试逐点核对，并按 LD14-08 枚举所有停止事实 | U14-50；S2/S3 重开 | `sha256:f1021c93c8f2d9bdc30f0fbcc76e790bfb5d53e245572be27ae65b0cf626af3e` | 不通过，待集中修复 |
| 第十九轮（U14-50 后冻结终审一） | 需求、架构、功能闭包、状态与回归；从新指纹重新裁决完整单元 | 九面完整；消费四片事实包并下钻 S2/S3，核对 24 文件、完整身份链、两域 producer/full/guard/recovery/executor、全部停止事实、后续 mesh/delivery 接缝与 LD14-01~08；EX14-01 未触发 | 无；S2/S3 重新局部封版 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 通过，连续无新增 1/2 |
| 第二十轮（U14-50 后冻结终审二） | 并发、崩溃、安全、资源上界、异常终态与测试盲区；独立反向裁决 | 九面完整；从矛盾 executor 快照、commit/ACK 并发、历史 assignment、所有索引 add/delete/filter、二次重启、O(pending) 上界、矩阵 helper 与跨片合同反查；指纹复算一致 | 无；四片维持局部封版 | `sha256:7bc1f54bd518ff456e0c1b68921737b367d0c7a7cc528deb4f84a55e07b8337d` | 通过，连续无新增 2/2 |

<!-- registration-complete: unit-14.gen-1 -->
