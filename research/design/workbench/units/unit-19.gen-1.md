# 单元登记:第 19 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:19
- **generation**:1
- **登记时间**:2026-07-21
- **登记来源**:用户明确要求开始 distributed-runtime 模块第 19 单元开发

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、分片账本、九类核查面行、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:U19-17、U19-19 已根因闭合并验证，待独立终审
- **连续无新增问题轮数**:0 / 2
- **交付物是否冻结**:否
- **交付物文件集**:28 个业务、测试、架构与结构 golden 文件（工作台文件不计入交付指纹）
- **当前交付物指纹**:`7cfe94288d324c99cd280d259c2ec31d34a2ea0dfe2a7c80ff5f830f429d1380`（`SHA256(UTF8(sorted(path<TAB>SHA256(file-bytes)<LF>)))`）
- **架构来源**:`research/design/modules/distributed-runtime/distributed-runtime-charter.md` 的 S5 控制面边界与 `research/design/modules/distributed-runtime/specification.md` 第 19 单元、§3.6～3.7、§4.2

## 固定边界

- **功能范围**:实现 assignment 域资产闭包按摘要推拉、断点续传与去重；实现 `RunExecutorPort`、`RunSubmissionPort` mesh adapter，并以同一 conformance 套件验证进程内与 mesh 等价
- **架构不变量**:消息签名或摘要先验；root/dependency 闭包精确且规范有序；资产耐久在场先于引用消息交付；传输按 digest 幂等、断点连续且不越界；每次操作绑定当前 assignment 的授权；adapter 只承载既有端口语义，不重写业务状态机或 guard
- **验收条件**:root/dependency 少列、多列、乱序、跨层重复、缺件、断点、重复上传及错 assignment 授权全部拒绝或幂等；WindowInput/Dispatch/MutationBatch/SealedBundle 全闭包可恢复传输；进程内/mesh contract conformance 等价
- **必要上下游**:S2 认证 mesh session、ArtifactStore 与摘要原语、S3 conversation/job assignment 及提交端口、S4 capability/permission/resource 双侧守卫
- **明确不属于本单元**:第 20 单元跨机业务控制面启用；第 21～24 单元 stream/spool、数据面票据、surface 内容资产与中继；第 23 单元 surface 预上传授权；任何生产拓扑切换

## 审查分片

- **是否启用**:是
- **决定依据**:协议横跨共享闭包算法、可续传资产通道、owner/executor 双向 adapter 与授权/等价测试，四者依赖有序且正确性维度正交
- **完整单元与跨切片必审项**:消息验证→闭包计算→缺件协商→分段传输→耐久落盘→端口调用全链；双向授权与 assignment 绑定；重试/断线/重复传输；进程内与 mesh 结果及副作用等价

| 切片 | 审查闭包（边界、依赖、局部验收） | 输入基线 | 当前轮次 | 本轮进度 | 收敛状态 | 封版信息包（结论、保证、证据、重开条件） |
| ---- | ---------------------------------- | -------- | -------- | -------- | ---------- | ---------------------------------------- |
| S19-A 共享闭包合同 | root/dependency 提取、规范排序、精确闭包与消息绑定 | HEAD `4abc38f` | 完成 | 100% | 局部封版 | roots 差集、ArtifactRef 收集及缺件错误分类均由 core 单源；14 项闭包/耐久接收直接测试及双消费端缺件测试通过。重开条件：闭包集合、ArtifactRef 或错误分类合同变化。 |
| S19-B 资产传输 | digest 协商、range 续传、去重、耐久落盘与断线恢复 | 指纹 `7cfe9428…d1380` | 已完成 | 100% | 局部封版 | 未完成且字节已齐的进度统一进入显式终结，零/非零、并发精确重放及 final-store 重试均幂等；core 14/14、CLI adapter 11/11 通过。重开条件：progress、receiver 终结或 upload 状态合同变化。 |
| S19-C executor adapter | owner→executor 的 `RunExecutorPort` mesh 承载与授权 | 指纹 `7cfe9428…d1380` | 已完成 | 100% | 局部封版 | 通用 request channel 只发布封闭错误码与固定安全文案，本地异常原文不进入 wire；mesh 9/9 与 CLI adapter 11/11 通过。重开条件：错误码、失败帧或服务异常公开合同变化。 |
| S19-D submission adapter | executor→owner 的 `RunSubmissionPort` mesh 承载、资产先行与双形态 conformance | 指纹 `7cfe9428…d1380` | 已完成 | 100% | 局部封版 | 失败响应复用同一公开边界，submission 四方法与真实 journal 拓扑无需复制错误规则；CLI adapter 11/11 通过。重开条件：失败帧、服务注册或 submission 承载合同变化。 |

### 完整单元横向审查记录

> 启用分片时每轮必填;默认消费封版信息包,有疑问可下钻细节,确认问题或结论失效时重开切片。

| 轮次 | 完整输入基线 | 全范围与跨片核查 | 重开触发 / 切片 / 问题 | 结论 |
| ---- | ------------ | ---------------- | ---------------------- | ---- |
| 1 | 当前 16 文件业务闭包 | 消息验证→闭包解析→缺件协商→分段传输→耐久落盘→端口调用；双向授权、重试、断线、响应绑定及全部生产/消费点 | U19-01～04 均在开发审查中闭合；无未解决问题 | 完整链路与跨切片合同通过 |
| 2 | 指纹 `1fc491e3…185d` | 需求、身份链、崩溃窗、资源上界、异常终态、模块边界、同类合同分叉与测试盲区 | U19-05 测试预算债务已验证；无新增业务问题 | 两轮终审及最终验证通过 |
| 3 | 指纹 `1fc491e3…185d` | 共享闭包差集、扩展 ArtifactRef、认证身份与顺序、静默连接终结、大资产存储复杂度及真实 conformance | 重开 S19-A～D；新增 U19-06～11 | 待集中修复，不得提交 |
| 4 | 候选指纹 `c6bf93c4…ba29` | U19-06～11 同根影响面、四切片边界与消息→资产→授权→端口完整链路 | 六项根因均完成并通过合并直接验证；无遗留待修项 | 进入冻结准备 |
| 5 | 冻结指纹 `c6bf93c4…ba29` | 需求、架构、全链功能、生产端/消费端、双拓扑语义与既有兼容 | 未触发切片重开；U19-06～11 结论仍成立 | 终审一零新增 |
| 6 | 冻结指纹 `c6bf93c4…ba29` | 安全、并发、崩溃、资源上界与异常终态 | 重开 S19-B；新增 U19-12 | 耐久前缀目录落盘与瞬时失败保留未闭合 |
| 7 | 冻结指纹 `6fc59890…b282` | U19-12 根因、四切片边界、完整消息→资产→授权→端口链与派生资产闭包 | S19-B 重新封版；U19-12 通过合并直接验证，结构 golden 同步并复核 | 进入重新冻结终审 |
| 8 | 冻结指纹 `6fc59890…b282` | 需求、架构、完整功能链、生产/消费端、双拓扑语义与既有兼容 | 未触发切片重开；U19-01～12 结论及全部边界仍成立 | 重新冻结终审一零新增 |
| 9 | 冻结指纹 `6fc59890…b282` | 安全、并发、全部 `.part` 目录项迁移、终结失败分型与异常终态 | 重开 S19-B；U19-12 的零进展写入仍误标内容损坏，删除目录项未同步 | U19-12 同根尾项集中修复 |
| 10 | 冻结指纹 `c5788c49…6f84` | U19-12 完整根因、四切片边界、完整消息→资产→授权→端口链与派生资产闭包 | S19-B 再次封版；全部 `.part` 创建/删除迁移与存储失败分型闭合 | 进入最终重新冻结终审 |
| 11 | 冻结指纹 `c5788c49…6f84` | 需求、架构、完整功能链、生产/消费端、双拓扑语义与既有兼容 | 未触发切片重开；U19-01～12 及四切片保证均成立 | 最终重新冻结终审一零新增 |
| 12 | 冻结指纹 `c5788c49…6f84` | 安全、并发、崩溃、资源上界、异常终态及全部耐久目录项迁移 | 未触发切片重开；L19-01～03 检测动作均适用并完成 | 最终重新冻结终审二零新增，进入最终验证 |
| 13 | 冻结指纹 `c5788c49…6f84` | 最终验证失败归因：共享闭包错误分类及受影响双域消费者 | 重开 S19-A；新增 U19-13，第三项失败单例隔离通过，排除功能回归 | 返回集中修复，终审结论归零 |
| 14 | 候选指纹 `60f822b5…bdb` | U19-13 稳定错误分类、合法缺件夹具及 Dispatch/SealedBundle 全消费端 | S19-A 重新封版；结构非法、内容损坏、root/dependency 缺件分型闭合 | 进入重新冻结终审 |
| 15 | 冻结指纹 `60f822b5…bdb` | 需求、架构、完整消息→资产→授权→端口链及双域消费者 | 未触发切片重开；U19-01～13 与四切片保证成立 | 最终终审一零新增 |
| 16 | 冻结指纹 `60f822b5…bdb` | 安全、重试语义、异常终态、同类错误分类和既有兼容 | 未触发切片重开；L19-01～04 检测动作均完成 | 最终终审二零新增，进入最终验证 |
| 17 | 冻结指纹 `60f822b5…bdb` | 派生资产、构建、受影响包回归、diff、指纹与临时证据清理 | 全部门禁通过，交付物未变化 | 第 19 单元完成 |
| 18 | 冻结指纹 `60f822b5…bdb` | 独立复核请求—响应绑定、静默连接终结及同类上下游链路 | 重开 S19-B～C；新增 U19-14～15 | 返回集中修复，既有终审结论归零 |
| 19 | 冻结指纹 `60f822b5…bdb` | 请求通道全部双向发送点、容量归属与运行时计时器边界 | 扩展 U19-15 完整影响面；新增 U19-16 | 同族边界已收齐，维持集中修复 |
| 20 | 候选指纹 `ee9f379b…e54` | U19-14～16 合并修复、四切片保证及完整消息→资产→授权→端口链 | S19-B～C 重新封版；请求判别、双向发送所有权、容量与计时器边界闭合 | 进入冻结终审 |
| 21 | 冻结指纹 `ee9f379b…e54` | 需求、架构、完整消息→资产→授权→端口链、生产/消费端及既有兼容 | 未触发切片重开；U19-01～16 与四切片保证成立 | 冻结终审一零新增 |
| 22 | 冻结指纹 `ee9f379b…e54` | 安全、并发、崩溃、资源上界、异常终态及请求通道全部同类执行点 | 未触发切片重开；L19-05～06 检测动作完成，请求判别与双向发送生命周期无遗漏 | 冻结终审二零新增，进入最终验证 |
| 23 | 冻结指纹 `ee9f379b…e54` | 结构派生资产、全量构建、受影响包回归、diff、文件集、指纹与临时证据清理 | 全部门禁通过，交付物未变化；未变输入包复用同层证据 | 第 19 单元完成 |
| 24 | 冻结指纹 `ee9f379b…e54` | 零字节资产状态、真实 guard/port 双拓扑等价、全部端口合法载荷上界及后续单元边界 | 重开 S19-B～D；新增 U19-17、U19-18，并重开 U19-11；临时件生产调度归第 20/23 单元，不误报 | 返回集中修复，既有终审结论归零 |
| 25 | 候选指纹 `54caf931…d498` | U19-11、U19-17～18 根因、四切片边界、真实双拓扑与聚合载荷预算 | 三项完成合并直接验证；S19-B～D 重新封版；结构 golden 仅 executor→core 既有边引用 5→6 | 冻结 28 文件，进入两轮终审 |
| 26 | 冻结指纹 `54caf931…d498` | 需求、架构、消息→资产→授权→真实端口全链、双域分页消费与既有兼容 | 未触发切片重开；U19-01～18 和四切片保证均成立 | 冻结终审一零新增 |
| 27 | 冻结指纹 `54caf931…d498` | 安全、并发、崩溃、聚合资源上界、异常终态、同类执行点与后续单元边界 | 未触发切片重开；零字节状态、512 KiB 页预算、签名开销及全部八方法无遗漏 | 冻结终审二零新增，进入最终验证 |
| 28 | 冻结指纹 `54caf931…d498` | 全量构建、受影响包回归、结构基线、diff、指纹与临时证据清理 | 全部门禁通过；交付物未变化；临时机器可读证据已清理 | 第 19 单元完成 |
| 29 | 冻结指纹 `54caf931…d498` | 独立终审；mesh 失败响应、服务异常公开边界、秘密与本地环境事实 wire 审计 | 重开 S19-B～D；新增 U19-19；同族错误序列化点已收齐 | 返回集中修复，既有终审结论归零 |
| 30 | 冻结指纹 `54caf931…d498` | 独立完整审查；资产上传全部合法进度组合、并发重放、最终存储失败、响应绑定及其上下游 | 重开 U19-17；非零完整前缀待终结时客户端误报上传成功；无新增根因 | U19-17、U19-19 集中修复，不得提交 |
| 31 | 候选指纹 `7cfe9428…d1380` | U19-17、U19-19 根因、全状态终结、双向上传及通用服务失败公开边界 | 两项完成合并直接验证；S19-B～D 重新封版；无同根遗漏 | 修复完成，进入独立终审 |

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| -------------------------- | ------------------------ | ---------------- | ---- |
| core assignment-artifacts、ArtifactStore、测试与 authority 导出 | 新闭包/range/stream 原语必须由公开 authority 入口导出并可生成声明 | core 类型检查；14 项闭包与耐久接收直接测试；公开导出检查 | 通过 |
| mesh request-channel、错误、入口、package 与 tsup | 新子路径必须同时进入 package exports、构建入口和类型声明 | mesh 类型检查；request-channel 9/9；既有 exports/入口未变 | 通过 |
| executor ledger 与 owner conversation/job | 全部在线、重放和提交消费者必须复用共享闭包与耐久 guard | executor/owner 类型检查；两域守卫 10 项定向测试 | 通过 |
| CLI assignment mesh adapter 与测试 | 双端 port、资产服务、认证 peer、授权顺序及响应绑定必须闭合 | adapter 10/10；真实 ledger/journal 双拓扑覆盖八方法、零字节双向传输及超限页拒绝 | 通过 |
| distributed-runtime 结构 golden | 新导入只能增加既有允许边与预期引用计数 | 冻结预检 1/1 通过；当前修复未改变跨包导入边 | 通过 |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| assignment 资产闭包 | `assignment-artifacts.ts` 的 root 描述器、注册 schema 与共享 resolver | resolver 对提取集合减 roots 后精确比对；opaque 仅验在场 | root/dependency 少列、多列、乱序、声明重复、合法提取相交、缺件及嵌套依赖 | executor 生成；owner/executor replay、mesh adapter 消费 | 引用集合去重排序；仅解析注册 schema；opaque 资产止于叶节点 | 通过 |
| 可续传资产接收器 | `.part` 耐久前缀与最终内容寻址 `ArtifactStore` | 分段短写循环并 fsync；创建与删除均同步父目录；流式摘要匹配且 verified put 后完成 | 首次目录项落盘、final store 瞬时失败、响应丢失、短写/零进展、零/非零终结、并发精确重放 | owner/executor 双向 artifact client/service | 单件与分段字节上界；每 digest 串行；读取 O(N)、峰值 O(分段)；可重试存储失败保留前缀 | 通过:U19-17 已验证 |
| mesh request channel | 单连接唯一 receive loop、request id 与 pending/inbound 表 | 有界准入后写帧；响应 id 精确命中后完成请求 | 双向并发、嵌套请求、静默连接、过载、关闭、handler/send 失败 | assignment 三服务及后续 mesh 服务 | 双向发送归属请求容量与 deadline；不可终结时关闭连接；失败帧仅含封闭错误码与固定安全文案 | 通过:U19-19 已验证 |
| assignment port adapter | 既有 `RunExecutorPort`/`RunSubmissionPort` 与共享耐久业务 guard | 认证 peer 与零载荷 guard 通过后传输资产，再由原端口完成业务写线性化 | 上传/下载中断、静默连接、错设备/assignment、错回执/证明 | owner↔executor 双向 adapter | queryLedger 请求判别唯一决定响应形态；page 至多 256 条/512 KiB 并按连续前缀续读 | 通过 |

## 覆盖与核查

> 覆盖来源包括架构要求、不变量、验收项、交付文件与跨边界符号、生产端、消费者和测试;核查面固定为状态、入口与生产端、消费端与继承面、生命周期、并发与崩溃点、异常路径与终态、安全边界、模块边界、测试与验收。每轮填写“通过:证据”“不适用:依据”或“有问题:编号”。

| 覆盖来源 | 来源项 | 核查面 | 对象或路径 | 问题盘点结论与证据 | 终审一结论与证据 | 终审二结论与证据 |
| -------- | ------ | ------ | ---------- | ------------------ | ---------------- | ---------------- |
| 架构 | 第 19 单元 | 状态 | 缺件协商、分段进度、完成 | 通过:规范 progress 与 durable prefix 单源 | 通过:状态与恢复线性化闭合 | 通过:任意 `{full,false}` 均显式终结后才返回 |
| 架构 | 第 19 单元 | 入口与生产端 | 双向 mesh adapter | 通过:两端各四方法完整承载 | 通过:资产先行且原端口唯一业务写入 | 通过:全部请求与响应发送点有界终结 |
| 架构 | 第 19 单元 | 消费端与继承面 | ArtifactStore、既有端口与 guard | 通过:真实 ledger/journal 同时承担 guard/port，远端首写与进程内精确重放等价 | 通过:八方法生产/消费端与双域分页续读闭合 | 通过:无第二套业务状态机或 guard |
| 架构 | 第 19 单元 | 生命周期 | 临时分段、请求通道、完成资产与断线清理 | 通过:零字节 `{0,false}` 经空分段线性化为完成，重复传输幂等 | 通过:零字节与非空状态迁移同构 | 通过:非零精确重放与 final-store 重试终结幂等 |
| 架构 | 第 19 单元 | 并发与崩溃点 | range 重试、重复上传、响应丢失与阻塞发送 | 通过:每 digest 串行；发送容量不可绕过且阻塞时有界关闭 | 通过:重复、断线与发送阻塞均有唯一出口 | 通过:阻塞发送不能释放容量或累积队列 |
| 架构 | 第 19 单元 | 异常路径与终态 | 少列、多列、乱序、缺件、断点与响应错型 | 通过:错误分类、请求响应四组合及异常终态闭合 | 通过:拒绝类别与终态无悬挂 | 通过:错型、超时、取消与发送失败均 fail-closed |
| 架构 | 第 19 单元 | 安全边界 | 认证 session、assignment 授权、摘要绑定与失败响应 | 通过:认证 peer→零载荷 guard→资产读取顺序单源 | 通过:认证授权先于资产深验 | 通过:任意服务异常原文不进入 wire |
| 架构 | 第 19 单元 | 模块边界 | 19/20/21～24 | 通过:只交付 adapter，未切换生产拓扑 | 通过:未越界启用后续拓扑与数据面 | 通过:未提前启用第 20 单元生产拓扑 |
| 架构 | 第 19 单元 | 测试与验收 | 双 adapter conformance 与对抗矩阵 | 通过:executor 分页 1/1、CLI adapter 10/10、结构门禁 1/1；CLI 类型差异仅 X19-01 | 通过:真实生命周期与连续分页证据有效 | 通过:异常、预算和同类执行点覆盖完整 |

## 问题清单

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U19-01 | 完整 `.part` 前缀可能在 fsync 后、最终 store put 前崩溃，旧 progress 只报告已收满而不完成。 | 耐久前缀与最终内容寻址写入之间缺恢复线性化。 | owner/executor 双向接收、重启、响应丢失与空/非空资产。 | progress 发现完整前缀时复算摘要并幂等完成 put；崩溃遗留前缀测试通过。 | 已验证 |
| U19-02 | 完整字节摘要不符时若先 `ArtifactStore.put`，会以实际摘要留下不属于声明 ref 的孤立资产。 | 终验顺序晚于最终 store 副作用。 | 错摘要上传、资产污染、临时件 GC 与恶意输入。 | put 前复算声明摘要；不符拒绝且最终 store 零新增，直接测试通过。 | 已验证 |
| U19-03 | ledger evidence/snapshot 初版只拉直接 ref，且部分证明、回执未反绑原请求。 | transport 成功被误当作协议语义完成，未沿响应引用继续闭包和身份链。 | evidence 分页、received dispatch、bundle_sealed/snapshot、supersede、mirror 与提交响应。 | 物化并深验记录，递归拉取共享闭包；所有响应反绑 assignment/executor/fence/range/ref/revision/digest；CLI 对抗测试通过。 | 已验证 |
| U19-04 | request handler 在响应写出时连接关闭会留下未观察的异步拒绝。 | 入站任务生命周期未归属唯一 channel 关闭路径。 | 双向并发、连接断开、pending 请求与进程级 unhandled rejection。 | handler/send 失败统一关闭 channel 并终结 pending/inbound；定向测试通过。 | 已验证 |
| U19-05 | CLI 包级负载下两条真实耐久适配器测试超过 Vitest 默认 5 秒，超时后清理又与后台写入竞态。 | 同类真实 IO 用例未显式拥有有界测试预算。 | 本文件 6 条 FileArtifactStore/receiver 集成用例、CLI 包测可靠性与清理。 | 六条用例统一复用 120 秒项目耐久 IO 预算，不改变断言；直接 6/6、CLI 2379/2379 通过。 | 已验证 |
| U19-06 | `resolveClosure` 把提取闭包与 roots 相交直接判错，并用未减 roots 的集合对比已声明 dependencies；而架构规定 dependencies 是传递闭包减去 roots，跨层重复限制只约束声明清单。 | 提取事实与声明分层约束混用，差集运算落在错误对象上。 | Dispatch/SealedBundle 生产与双端验证；root 内容再次引用 body 直接资产的合法组合；封包、重放和 mesh 提交；现有测试仅覆盖声明层重复。 | 先从提取闭包减去 root digests，再与声明 dependencies 精确比对；只保留声明层跨层重复拒绝；合法提取相交与非法声明重复对抗测试通过。 | 已验证 |
| U19-07 | CLI `collectRefs` 对 `{digest, bytes, ...扩展字段}` 要求精确键并拒绝，而 core `collectArtifactRefs` 接受既有 `ContentAssetRef` 等 ArtifactRef 扩展；该逻辑用于 ledger evidence 两轮预拉取。 | 同一 ArtifactRef 提取合同在 CLI 重写，形成第二套更窄的接受谓词。 | queryLedger evidence/snapshot 的嵌套引用、现有及未来 ArtifactRef 扩展、CLI mesh adapter 与 core 消费者；现有测试未使用扩展 ref。 | 删除 CLI 私有实现，直接复用 core 权威导出；扩展 ArtifactRef 与嵌套引用测试证明合法数据可预拉取，非法 ref 仍由单源 validator 拒绝。 | 已验证 |
| U19-08 | artifact handler 在授权前解码分段载荷；executor/submission handler 在既有 guard 前读取并深验 Dispatch/Bundle，且丢弃 registry 传入的认证连接。`OwnerControlGrant` 要求 caller、认证 peer 与耐久 owner 四者全等，但当前远端调用既无法绑定 peer，既有进程内 authorizer 又把本机 deviceId 同时当 caller 与认证身份。 | adapter 没有承载共享的“认证连接身份→零载荷预检→授权后解引用”边界，把资产先行误置于认证授权先行之前。 | artifact 与双业务服务、全部 executor/submission 方法；错设备/capability/assignment 请求、跨机合法 owner 调用、大闭包资源消耗、错误顺序及第 20 单元生产启用。 | 三类服务统一先以 `connection.peer.deviceId` 执行共享 first-level guard，绑定 method/scope/assignment/executor/caller，再解码载荷或访问 ArtifactStore；合法跨机调用仍由原端口完成最终业务裁决；错设备/授权对抗测试证明零资产访问，进程内与 mesh 共用同一谓词。 | 已验证 |
| U19-09 | `MeshRequestChannel` 的 outbound pending 无默认截止时间或并发上界，adapter 也未传播 deadline/capability 约束；入站 handler 无 deadline/取消传播，挂起后永久占槽，过载帧仍可持续创建等待发送失败响应的异步任务。 | 请求生命周期只覆盖响应与连接关闭，遗漏静默连接下双向时间、容量与关闭归属。 | 资产上传/下载、executor/submission 全方法、outbox 重试、静默或恶意 peer、pending/handler/过载任务内存及调用链终结。 | adapter 传播由操作 deadline 与 capability 有效期共同约束的 signal；channel 对 outbound、inbound 与过载响应统一实施有界容量、截止和关闭取消，迟到响应只丢弃且依赖远端幂等安全重试；静默 peer、挂起 handler、过载洪泛与迟到响应测试通过。 | 已验证 |
| U19-10 | wire 虽按 range 分段，但发送端每段调用 `ArtifactStore.get` 读取完整对象，接收端恢复/终验也整件读取；大资产峰值内存为 O(N)，多段下载会重复 O(N) 读取并退化为 O(N×分段数)。 | 分段只存在于协议层，存储抽象缺少 range/stream 能力，传输实现仍物化完整资产。 | owner/executor 双向传输、恢复与摘要终验、大附件和后续内容资产、内存/磁盘 IO 上界及 ArtifactStore 实现。 | 为 ArtifactStore 提供可插拔的 range/stream 读取与流式摘要、原子晋升能力；传输全程按块读取且不重复整件物化；大资产测试验证峰值内存与读取字节为 O(分段) / O(N)。 | 已验证 |
| U19-11 | 当前测试已使用真实认证 TLS 与 `MeshRequestChannel`，但全部业务 guard/port 仍为桩；仅 dispatch、submitBundle 比较桩结果，其余方法只核对参数转发，没有以真实 owner/executor journal 比较等价初态下的返回、错误类别、耐久日志与 ArtifactStore 副作用。 | 把“真实传输 + 桩业务端”的 adapter 测试误封为完整 contract conformance，既定修复只完成了通道层而未完成权威业务层。 | RunExecutorPort/RunSubmissionPort 全方法、真实连接身份、guard 重放、成功/拒绝结果、日志/ArtifactStore 副作用与未授权零读取；此前的 conformance 封版结论失效。 | 保留 adapter 细节测试，并以真实 `ConversationAssignmentLedger` / `ConversationRunJournal` 同时充当 guard 与 port，经过认证 TLS 覆盖完整提交生命周期和取消/替换生命周期；远端首写后用同一真实端口精确重放，比较结果并证明不产生第二份权威事实。 | 已验证 |
| U19-12 | `.part` 首次创建只同步文件内容；终结阶段又把全部失败及零进展存储写误作内容损坏，并且删除 `.part` 后未同步父目录。断电可丢失已确认前缀、复活已删除前缀，瞬时存储异常会强制从零重传。 | 耐久前缀全部目录项迁移与终结失败分类未闭合，把文件内容 fsync 当作完整线性化，并混同内容损坏与可重试存储错误。 | owner/executor 双向接收、首次分段、空资产、重启/断电、短写/零进展、final store 瞬时失败、内容损坏、成功/失败清理及重试。 | 创建与删除前缀均使用项目耐久目录原语同步父目录；只有字节数/摘要不符归 `artifact-corrupt` 并删除坏前缀，零进展及其他存储失败保留前缀，由 `progress` 幂等终结；瞬时失败零重传和清理测试通过。 | 已验证 |
| U19-13 | 共享 resolver 以 `has=false` 后抛 `TypeError` 表示 root/dependency 缺失；owner 的 bundle 错误映射因此把可补传的缺件返回为不可重试 `invalid`，而非既有 `missing-base`。executor 全测同时暴露一条旧缺依赖夹具已不满足新的精确闭包前提。 | 闭包结构非法与资产暂时缺失没有复用 ArtifactStore 的稳定错误分类，且消费端测试未按“合法闭包、缺少实体”构造缺件。 | Dispatch/SealedBundle 的 root 与 dependency 缺件；owner 提交重试语义、executor 派发错误、mesh 补传闭环及双域测试。 | resolver 对实体缺失统一抛 `AuthorityStorageError("artifact-missing")`，结构差异仍为 `TypeError`；缺件测试先构造合法提取闭包再移除实体，验证 owner 稳定返回可重试 `missing-base`，同类 root/dependency 全部对账。 | 已验证 |
| U19-14 | `MeshRunExecutorPort.queryLedger` 在请求携带 `range` 时仍接受 `LedgerSnapshot`，现有测试还将该行为断言为成功；无 `range` 时返回 page 已会拒绝，双向约束不一致。 | 响应判别只按返回对象形状分支，未把请求的 `range` 判别作为响应类型合同。 | conversation/job 证据分页恢复、错误响应下的资产拉取、adapter conformance 与响应绑定测试。 | 将请求与响应形态设为双向封闭合同：有 `range` 只接受严格反绑范围的 `LedgerEvidencePage`，无 `range` 只接受 `LedgerSnapshot`；补齐正反向对抗测试并复核双域调用方。 | 已验证 |
| U19-15 | 入站 handler 的 deadline 只包围 `registry.dispatch`，成功/失败响应发送均在 abort race 之外；出站请求发送又是 fire-and-forget，调用方 abort 会删除 pending 并清除 timeout，却不终止阻塞发送。连接发送串行化后，重复创建再 abort 可绕过 outbound 上限并无限排队；成功响应发送失败还会再次尝试发送失败响应。 | 容量与 deadline 只归属 pending/handler，没有覆盖请求产生的传输写入，双向发送缺少统一的时间、容量与关闭所有权。 | 出站请求、成功/失败响应、调用方取消、静默或恶意 peer、pending/inbound 上限、串行发送队列、assignment 三服务及后续 mesh 服务。 | 让每次请求的发送与响应共同归属一个有界生命周期：未完成发送始终占用容量，abort/deadline 无法终结发送时关闭连接并统一清空；入站只生成一次响应，发送失败直接关闭而不二次发送。以阻塞双向 transport 验证有界关闭、容量不绕过及 pending/inbound/发送队列零悬挂。 | 已验证 |
| U19-16 | `requestTimeoutMs` 与 `handlerTimeoutMs` 仅校验正整数，允许大于 `2_147_483_647`；Node 会把该值的 `setTimeout` 延迟压成约 1ms，而 mesh 包已有 `assertRuntimeTimerDelay` 单源约束。 | 新通道重复实现了不完整的时间配置校验，没有复用运行时可表达的计时器边界。 | 所有自定义 request/handler timeout、请求过早失败、连接被误关及使用该通道的当前与后续 mesh 服务。 | 两项配置统一复用 `assertRuntimeTimerDelay` 并 fail-closed；补最大合法值与上界加一的构造测试，确保配置不会在运行时改变语义。 | 已验证 |
| U19-17 | 零字节首次上传已修复，但非零资产仍可在并发重放与可重试 final-store 失败后返回 `{ receivedBytes: ref.bytes, complete: false }`；`upload()` 仅在 `bytes===0` 时终结，非零路径会退出循环并误报成功。 | 修复只覆盖零字节实例，没有落实“字节已齐不等于最终资产已落定”的完整 progress 合同。 | owner→executor 与 executor→owner 并发/重试上传；完整耐久前缀、final-store 瞬时失败、精确重放及随后引用该资产的端口调用。 | 单源化终结语义：任意未完成且字节已齐的进度都执行显式终结并确认 `complete=true` 后才允许 `upload()` 返回；receiver 对零/非零、并发精确重放和 final-store 重试统一幂等。 | 已验证 |
| U19-18 | `RunExecutorPort.queryLedger` 允许每页 256 条，每条耐久正文可接近 32 KiB，owner 取消恢复也固定按至多 256 条请求；但 `MeshRequestChannel` 默认服务载荷上限仅 1 MiB。合法页约 33 条大记录即可在进程内返回、经 mesh 时被通道拒绝。 | adapter 只对单条记录预算，未把聚合分页合同与承载通道预算对账，也未为大页提供资产化或自适应分页表示。 | conversation/job 取消证据恢复、长生命周期 interaction/side-effect 账本、默认 mesh 装配、断线重试及进程内/mesh 等价；其他端口大对象已资产化或受单记录预算约束。 | 协议冻结每页至多 256 条且未签名规范载荷至多 512 KiB；executor 返回请求范围内可容纳的最大连续前缀，validator 双重强制，owner 继续按 `toSeq + 1` 无缝续读。以大记录组合验证多页连续、每页预算与签名/范围不变。 | 已验证 |
| U19-19 | `MeshRequestChannel.failureFrame()` 将任意 handler 的 `Error.message` 原样写入失败响应，测试还明确断言远端收到该原文；资产服务的文件系统异常可因此暴露本机路径，未来服务异常也可能携带秘密或敏感上下文。 | 通用 mesh 通道没有区分本地诊断与可公开协议错误，把“有界长度”误当成“可安全上 wire”。 | assignment 资产、executor、submission 三服务及后续全部 registry 服务；普通 Error、伪装为 `MeshProtocolError` 的动态消息、文件系统/业务异常、认证 peer 与秘密 wire 审计。 | 通用通道单源映射封闭错误码与固定安全文案；普通异常降为 `service-failed`，协议异常只保留受控错误码，接收端拒绝任何非规范文案。包含路径和秘密标记的两类异常均证明 wire 零原文。 | 已验证 |

## 已排除问题

> 保存已证伪疑点供复审复用;使用与重开规则见工作台静态区。裁决只允许“已排除”或“已重开→问题编号”,重开时保留原行。

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | ---------------- | -------- | -------- |
| X19-01 | CLI `tsc --noEmit` 仍报告 8 个 credential projection 既有错误。 | 错误位于 config-editor/startup 旧路径，与第 19 单元交付文件及新增符号无交集；CLI 直接测试、包测和构建通过。 | 类型检查差异归因及指纹 `1fc491e3…185d`。 | 第 19 单元文件出现新增类型错误或 credential projection 进入当前边界。 | 已排除 |
| X19-02 | 第 19 单元实现 mesh adapter 但没有把生产派发切到远端。 | 执行计划明确第 19 单元只交付传输与 adapter；跨机业务控制面启用属于第 20 单元。 | specification 第 19/20 项边界及当前 16 文件集。 | 第 20 单元完成后生产拓扑仍未使用这些 adapter。 | 已排除 |
| X19-03 | adapter 的 deadline helper 将计时器延迟限制在 Node 可表达上界，疑似会让超长合法调用提前超时。 | 当前所有合法输入均远低于该上界：assignment capability 最长 24 小时，owner-control/control lease 最长 60 秒，调用 deadline 又不得晚于 capability expiry；因此不存在会被提前截断的合法第 19 单元调用。 | `authority.ts` 的 TTL 常量与校验、conversation assignment credential policy、adapter deadline 取最小值；指纹 `54caf931…d498`。 | 任一合法 capability/control deadline 上限提高到运行时计时器上界以上，或新增不受同等 TTL 约束的调用主体。 | 已排除 |
| X19-04 | `MeshServiceDefinition.access` 未在 registry 中再次执行通用业务授权。 | `access` 用于约束兼容模式：write 服务只能是 negotiated-version；具体 peer、assignment 与 capability 授权由各服务 handler 的耐久 guard 执行，职责没有缺失。 | `service-registry.ts` 定义/注册/dispatch 与 assignment 三服务 guard 链；指纹 `54caf931…d498`。 | 出现缺少自身授权 guard 的服务，或 write 服务可在 version skew 下开放。 | 已排除 |
| X19-05 | `LedgerSnapshot` 没有 `executorId` 与签名，疑似缺少远端 executor 响应绑定。 | 该即时快照的冻结合同本就不含签名，mesh 响应由已认证连接承载且 adapter 已反绑 `assignmentId`；选择与 capability.executorId 对应的连接属于第 20 单元生产拓扑，审计用 `LedgerEvidencePage` 仍显式签名并反绑 executor。 | specification §3.6、snapshot/page validator、adapter queryLedger 分支与 X19-02 边界；指纹 `54caf931…d498`。 | 第 20 单元未将 client 绑定 capability.executorId，或 snapshot 被改作跨连接耐久审计证据。 | 已排除 |

## 迟发现教训

> 仅登记“先前通过后才发现”的真实遗漏。检测动作必须可执行并写明适用范围;每个适用轮次追加执行证据。

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | -------------------------- | -------- | ------------------------------------ | ---- |
| L19-01 | U19-05；直接测试通过后的 CLI 包级验证 | 真实耐久 IO 在隔离运行中很快，默认 5 秒预算未在包内并发负载下暴露。 | 新增真实磁盘/加密/进程集成用例时，按同类文件单源声明有界预算并以一次所属包全测验收；禁止失败后逐例追涨。 | 最终验证:六条同类用例统一预算，CLI 164 文件 2379 项通过。 |
| L19-02 | U19-06～11；第三轮独立终审前两轮曾通过；U19-11 后续误封 | 先前按成功路径和 adapter 单体测试裁决；后续虽换成真实 channel，仍以桩 guard/port 代替真实权威拓扑。 | 共享闭包或远程 adapter 终审必须执行：声明/提取差分、认证前零深验、双向静默/过载终结、存储复杂度审计，以及真实 channel+guard+port 双拓扑差分；“真实”必须逐层列出并核对，不得以真实传输替代真实业务端。 | 第三轮收齐 U19-06～11；第十七轮复核发现仅 channel 真实，重开 U19-11。 |
| L19-03 | U19-12；冻结终审一零新增后由终审二发现 | 先前只核对文件内容 fsync 与重启可读，未把新文件目录项持久性和终结错误分类纳入耐久前缀线性化。 | 新增耐久文件/前缀时逐项核对：目录创建、文件内容、目录项、终结迁移和失败分类；可重试错误不得删除唯一恢复事实。 | 终审二:横扫全部新文件写路径定位 receiver；修复复核:创建、成功/损坏/过期删除、零进展及其他存储失败均逐项对账。 |
| L19-04 | U19-13；两轮最终终审零新增后由 executor 包测发现 | 先前验证了闭包集合和值，却未把 resolver 的结构非法、内容损坏和实体缺失错误类别沿消费端映射到最终重试语义。 | 共享校验器终审须以同一合法对象分别注入结构非法、内容损坏和实体缺失，逐层核对稳定错误类别及调用方重试性；缺件夹具不得以过声明代替。 | 最终验证归因：root 缺件被误映射为 `invalid`；同类 root/dependency 与 Dispatch/SealedBundle 消费端已纳入 U19-13。 |
| L19-05 | U19-14；单元完成后独立终审发现 | 只验证了返回对象各自内部有效，未对请求判别与响应判别做笛卡尔积反向测试。 | 判别式远程接口必须对每种请求形态验证唯一允许的响应形态，并对其余合法形状逐一拒绝。 | 第十二轮：`queryLedger` 的 snapshot/page × range/无 range 四组合已完成静态对账；集中修复：四组合正反向验证通过。 |
| L19-06 | U19-15～16；单元完成后独立终审发现 | 只审查 pending 与 handler，遗漏双向传输写入、容量释放和运行时计时器可表达范围。 | 异步请求终结审查须从准入追到发送、响应或关闭，并对账每个 await 的时间与容量所有权；计时配置必须复用运行时边界校验。 | 第十二～十三轮：横扫全部双向发送点、串行队列及计时器入口，收齐 U19-15～16；集中修复：阻塞双向发送、容量和计时器边界 3 项对抗测试通过。 |
| L19-07 | U19-17；两轮冻结终审与最终验证后发现，首次修复又未闭合 | 先遗漏零字节，修复时又只覆盖该实例，未枚举非零完整前缀待终结与并发精确重放。 | 状态协议必须枚举最小值、最大值及全部合法组合；修复一个实例后按状态笛卡尔积复核同根路径。 | 第十六轮发现 `{0,false}`；第二十一轮确认非零 `{full,false}` 仍会被误作上传成功。 |
| L19-08 | U19-18；既有 conformance 封版后发现 | 只对单条记录和单次传输设限，未计算聚合页在默认承载层的最坏合法尺寸。 | 分页、批次或聚合响应接入新 transport 时，必须以“内层单项上界 × 最大项数 + 包装开销”对账默认载荷预算；不适配时资产化或收紧协议上限。 | 第十七轮：账本页 256×32 KiB 与 1 MiB 默认通道上限对账，定位 U19-18。 |
| L19-09 | U19-19；两轮冻结终审与最终验证通过后发现 | 只检查错误码、长度和终结状态，未区分本地异常诊断与允许公开到 wire 的协议信息。 | 通用 RPC/mesh 错误边界必须向 handler 注入包含路径和秘密标记的异常，断言 wire 只含稳定错误码与安全文案；任意异常类型不得自行获得公开资格。 | 第二十轮：横扫 request channel、registry 与 assignment 三服务的错误序列化链，定位 U19-19。 |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| V19-01 | 共享闭包与资产传输合同 | core/mesh 类型检查与直接测试 | S19-A～B 直接源码与测试 | 开发 / 低 / 已执行 | 被 U19-06～11 修复输入取代，保留历史证据 | `1fc491e3…185d` | 失效 |
| V19-02 | 双向 adapter 与既有 guard 等价 | owner/executor 类型检查；闭包目标测试；CLI adapter 直接测试 | S19-C～D 直接源码与测试 | 开发 / 中 / 已执行 | 被 U19-06～11 修复输入取代，保留历史证据 | `1fc491e3…185d` | 失效 |
| V19-03 | 最终交付闭包、构建与受影响包回归 | 结构 golden 预检；逐包全测；全量构建；diff 与指纹复核 | 最终 16 文件、依赖图与构建产物 | 最终 / 高 / 已执行 | 交付物已变更，旧最终证据失效 | `1fc491e3…185d` | 失效 |
| V19-04 | U19-06～11 根因修复与同类面闭合 | 直接对抗测试、四包类型检查、CLI 基线比对、结构 golden 预检 | 修复后的 S19-A～D 源码、测试与派生产物 | 修复 / 中 / 已执行 | 后续 U19-12 修改了共享 ArtifactStore/receiver 输入，保留历史证据 | `c6bf93c4…ba29` | 失效 |
| V19-05 | 最终交付闭包与受影响回归 | 复核结构门禁→`pnpm build`→core、mesh、owner-kernel、executor、CLI 逐包机器可读测试→diff/指纹复核 | 冻结 24 文件及其直接消费者、构建与结构基线 | 最终 / 高 / 已执行至 executor | structure、17 包构建、core 2395/2395、mesh 84/84、owner 180/180 通过；executor 409/412 暴露 U19-13，第三项单例隔离通过 | `c5788c49…6f84` | 诊断 |
| V19-06 | U19-12 耐久前缀根因闭合及受影响消费端等价 | receiver 直接测试、core 类型检查/构建、CLI adapter 直接测试、结构门禁 | assignment-artifacts、ArtifactStore 接口/实现、CLI adapter 及结构 golden | 修复 / 中 / 已执行 | 终审二发现同根尾项，保留历史证据 | `6fc59890…b282` | 失效 |
| V19-07 | U19-12 完整根因与受影响消费端闭合 | receiver 直接测试、core 类型检查/构建、CLI adapter 直接测试、结构门禁与 diff | assignment-artifacts、ArtifactStore 接口/实现、CLI adapter 及结构 golden | 修复 / 中 / 已执行 | core receiver 13/13、类型检查与构建通过；CLI adapter 7/7；结构门禁及 diff check 通过 | `c5788c49…6f84` | 有效 |
| V19-08 | U19-13 错误分类与合法缺件夹具闭合 | core assignment-artifacts 直接测试、core 构建、executor 两项失败单例、executor 类型检查 | 共享 resolver、owner 双域映射及 executor 缺件测试 | 修复 / 低 / 已执行 | core 14/14、executor 2/2、core 构建与 executor 类型检查通过；第三项包测失败单例独立通过 | `60f822b5…bdb` | 有效 |
| V19-09 | 当前最终交付闭包与受影响回归 | 结构门禁复核→`pnpm build`→core、owner-kernel、executor、CLI 逐包机器可读测试→diff/指纹复核；mesh 复用未变输入证据 84/84 | 冻结 24 文件、core 反向依赖消费者及结构基线 | 最终 / 高 / 已执行 | 后续 U19-14～16 修改 mesh/CLI 输入，保留历史证据 | `60f822b5…bdb` | 失效 |
| V19-10 | U19-14～16 根因闭合 | adapter 响应形态对抗测试；request-channel 双向阻塞发送、容量与计时器边界测试；CLI/mesh 直接相关测试 | assignment mesh adapter、request channel 及对应测试 | 修复 / 低 / 已执行 | mesh 9/9 与类型检查通过；CLI 定向 2/2，类型差异精确等于 X19-01；structure 预检 1/1 | `ee9f379b…e54` | 有效 |
| V19-11 | 当前最终交付闭包与受影响回归 | 复核 structure 预检→`pnpm build`→mesh、CLI 逐包机器可读全测→diff/指纹复核；core、owner-kernel、executor 复用未变输入证据 | 冻结 24 文件、mesh/CLI 反向依赖、构建与结构基线 | 最终 / 高 / 已执行 | structure 1/1、17 包构建、mesh 24/24 套件与 87/87 测试、CLI 611/611 套件与 2380/2380 测试通过；core、owner-kernel、executor 复用未变输入包测证据；diff check、24 文件指纹与临时证据清理通过 | `ee9f379b…e54` | 有效 |
| V19-12 | U19-11、U19-17～18 根因闭合 | core/executor 类型检查；大记录分页定向；CLI adapter 全文件；结构 golden 预检 | contracts 分页常量与 validator、executor selector、CLI progress/双拓扑测试、spec 与 golden | 修复 / 中 / 已执行 | U19-11、U19-18 证据有效；U19-17 只覆盖零字节，未覆盖非零完整前缀待终结 | `54caf931…d498` | 诊断 |
| V19-13 | 冻结交付闭包最终验证 | `pnpm build`→core、owner-kernel、executor、CLI 串行机器可读全测→diff/指纹/临时证据复核 | 冻结 28 文件、core 反向依赖消费者、构建与结构基线 | 最终 / 高 / 已执行 | 17 包构建、core 2396/2396、owner-kernel 180/180、executor 413/413、CLI 2383/2383、structure 1/1 通过；diff check、指纹与临时证据清理通过 | `54caf931…d498` | 有效 |
| V19-14 | U19-19 错误公开边界与三服务失败响应闭合 | request-channel 定向对抗测试；mesh 类型检查；assignment adapter 全文件测试 | request-channel、错误类型、registry、assignment 三服务及对应测试 | 修复 / 低 / 已执行 | mesh 9/9、CLI adapter 11/11 与 mesh 类型检查通过；普通/协议异常中的路径和秘密标记均未进入 wire | `7cfe9428…d1380` | 有效 |
| V19-15 | U19-17 全状态终结闭合 | receiver 与 artifact client 定向测试；core/CLI 类型检查差异归因 | receiver 终结原语、upload 状态机及对应测试 | 修复 / 低 / 已执行 | core 14/14、CLI adapter 11/11 与 core 类型检查通过；CLI 类型差异精确等于 X19-01，无新增错误 | `7cfe9428…d1380` | 有效 |

## 终审记录

| 轮次 | 审查侧重 | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论 |
| ---- | -------- | ------------ | -------- | ------------ | ---- |
| 第一轮 | 需求、架构、功能闭环、状态、生产/消费端与回归 | 是 | U19-01～04，均已验证 | `1fc491e3…185d` | 通过 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、模块边界与测试盲区 | 是 | U19-05，已验证；无业务新增 | `1fc491e3…185d` | 通过 |
| 第三轮 | 闭包差集、扩展类型、认证身份与顺序、静默连接、真实存储复杂度及 conformance 有效性 | 是 | U19-06～11，待修复 | `1fc491e3…185d` | 不通过 |
| 第四轮 | 需求、架构、全链功能、生产/消费端与既有兼容 | 是 | 无 | `c6bf93c4…ba29` | 通过 |
| 第五轮 | 安全、并发、崩溃、资源上界、异常终态与测试盲区 | 是 | U19-12 | `c6bf93c4…ba29` | 不通过 |
| 第六轮 | 需求、架构、完整功能链、生产/消费端与兼容 | 是 | 无 | `6fc59890…b282` | 通过 |
| 第七轮 | 安全、并发、崩溃、资源上界、异常终态与测试盲区 | 是 | U19-12 同根尾项 | `6fc59890…b282` | 不通过 |
| 第八轮 | 需求、架构、完整功能链、生产/消费端与兼容 | 是 | 无 | `c5788c49…6f84` | 通过 |
| 第九轮 | 安全、并发、崩溃、资源上界、异常终态与测试盲区 | 是 | 无 | `c5788c49…6f84` | 通过 |
| 第十轮 | 需求、架构、完整功能链、生产/消费端与双域重试语义 | 是 | 无 | `60f822b5…bdb` | 通过 |
| 第十一轮 | 安全、异常分类、同类执行点、既有兼容与测试盲区 | 是 | 无 | `60f822b5…bdb` | 通过 |
| 第十二轮 | 请求—响应绑定、静默连接终结及同类上下游链路 | 是 | U19-14～15 | `60f822b5…bdb` | 不通过 |
| 第十三轮 | 双向发送生命周期、容量归属与运行时计时器边界 | 是 | 扩展 U19-15；新增 U19-16 | `60f822b5…bdb` | 不通过 |
| 第十四轮 | 需求、架构、完整功能链、生产/消费端与既有兼容 | 是 | 无 | `ee9f379b…e54` | 通过 |
| 第十五轮 | 安全、并发、崩溃、资源上界、异常终态与同类执行点 | 是 | 无 | `ee9f379b…e54` | 通过 |
| 第十六轮 | 独立完整终审；资产进度、零字节终结、双向传输及测试盲区 | 是 | U19-17 | `ee9f379b…e54` | 不通过 |
| 第十七轮 | 独立完整终审；真实业务拓扑 conformance、全端口合法载荷预算及后续单元边界 | 是 | 重开 U19-11；新增 U19-18；无其他新增 | `ee9f379b…e54` | 不通过 |
| 第十八轮 | 需求、架构、完整功能链、真实生产/消费端、双域分页与既有兼容 | 是 | 无 | `54caf931…d498` | 通过 |
| 第十九轮 | 安全、并发、崩溃、聚合资源上界、异常终态、同类执行点与模块边界 | 是 | 无 | `54caf931…d498` | 通过 |
| 第二十轮 | 独立完整终审；mesh 服务异常公开边界、wire 秘密与本地环境事实 | 是 | U19-19 | `54caf931…d498` | 不通过 |
| 第二十一轮 | 独立完整审查；资产进度全状态、并发精确重放、响应绑定及同类上下游 | 是 | 重开 U19-17；无新增根因 | `54caf931…d498` | 不通过 |

<!-- registration-complete: unit-19.gen-1 -->
