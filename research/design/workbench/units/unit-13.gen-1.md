# 单元登记:第 13 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:13
- **generation**:1(仅经用户显式授权递增;同单元的修复、复审、终审轮复位是本文件内的状态推进,不产生新登记)
- **登记时间**:2026-07-14
- **登记来源**:压缩重放死循环根治方案(代际文件模型)落地时,由架构者按用户授权登记;承接此前被重置的第 13 单元审查状态与本轮架构者×执行者对抗审查的收敛结论

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、九类核查面行、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:完成——U13-44 修复后两轮终审于冻结指纹 `009752e6…` 零新增;最终验证（E27 全量构建、E15 三包全测、E28 运行时导出门禁）全部通过,验证后复算指纹与冻结值一致
- **连续无新增问题轮数**:2 / 2
- **交付物是否冻结**:是（指纹见下行；两轮终审在此指纹上重新独立裁决）
- **交付物文件集**:`packages/core/src/contracts/ports.ts`、`packages/core/src/contracts/records.ts`、`packages/core/src/contracts/schema.ts`、`packages/core/src/protocol/assignment.ts`、`packages/core/src/protocol/commit.ts`、`packages/core/src/protocol/index.ts`、`packages/core/src/protocol/values.ts`、`packages/executor/src/assignment-ledger.ts`、`packages/executor/src/__tests__/assignment-ledger.test.ts`、`packages/owner-kernel/src/conversation-assignment.ts`、`packages/owner-kernel/src/conversation-run-contracts.ts`、`packages/owner-kernel/src/__tests__/conversation-run-contracts.test.ts`、`packages/owner-kernel/src/control-admission.ts`、`scripts/check-runtime-package-exports.mjs`、`research/design/modules/distributed-runtime/specification.md`(共 15 个文件;工作台、单元文件与 principles 协作规则不计入业务交付物)
- **当前交付物指纹**:`sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce`（算法:按上行 15 文件清单顺序对每文件取 SHA-256 小写 hex,以 `相对路径=hash` 每行 LF 拼接成 manifest 文本,再对该 UTF-8 文本取 SHA-256;失效旧值 `917d83d3…`、`c8b64a37…`、`80747461…`、`5c1e7b40…`）
- **架构来源**:`research/design/modules/distributed-runtime/distributed-runtime-charter.md` 的取消、三分续跑与不变量;`research/design/modules/distributed-runtime/specification.md` 的 §1.2、§3.6、§4.3、§5.1、§5.5、§6.1、§十二及执行计划第 13 项

## 固定边界

- **功能范围**:conversation 的 `cancel-requested`、owner-fence / abort-ticket 双取消源、SupersedeProof、DispatchConflictProof、containment、三分续跑与 UncertainResolution 用户三选
- **架构不变量**:封包先赢则合法提交;取消先赢才阻止新动作;仅可验证的未启动终结证明允许重派;结果不明必须进入 uncertain 并锁定对话;裁决、吊销与状态变化原子耐久且可重驱
- **验收条件**:§6.1 全 36 行逐边覆盖;owner-fence × abort-ticket × sealed 三方竞态、相关 fsync 崩溃点、未闭合副作用、无证明禁重派、迟到合法 bundle 自动提交及用户三选通过
- **必要上下游**:第 9 单元 AuthorityCommitLog / ArtifactStore、第 10 单元 ControlRequestJournal、第 11 单元 assignment 账本与 ActivationProof、第 12 单元 SealedBundle / CAS / FinalOutbox;冻结的 proof、control 与状态合同
- **明确不属于本单元**:job、delivery、资源治理实际结算、数据面票据签发、mesh adapter、生产总切换(uncertain 的超时触发策略可留外层,但账本证据判定与证明重取驱动属本单元恢复驱动闭环——见 U13-06 裁决)

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| -------------------------- | ------------------------ | ---------------- | ---- |
| core 协议层(contracts/ports.ts + records.ts + schema.ts + protocol/values.ts + assignment.ts + commit.ts + index.ts) | `uncertain-resolve` 结果绑定、账本增量状态机、单条 staged mutation 深验、wire schema/端口/耐久嵌套值单源深验、协议函数的包导出与运行时导出门禁清单 | 三包类型零错；core 可消费构建成功；U13-36 直接嵌套污染反例与完整 111 场景通过；源码导出已登记，运行时导出列入 E28 最终门禁 | 通过 |
| spec §1.2/§3.6/§3.7/§4.3/§6.1 | run/job 同构记录、摘要公式、containment、恢复、正常 proof→cancelled 与 submission 授权语义 | U13-34~36 已同步无 ArtifactStore submission guard、epoch 四模式、mirror requestId/乱序回执及 canonical tool input 合同；U13-41 已回填 assigned 权威快照三字段（run=ownerEpoch/job=anchorEpoch 同构）；U13-43 已同步 §3.6 guard 消费清单至与实现逐项一致（E34）；E29 111/111、E32 116/116 通过 | 通过 |
| executor 账本 / owner journal / 测试 | 状态机行为、机械 reducer 守卫、增量热点索引、共享流会话隔离与 §6.1 逐边一致性；owner run 合同谓词模块与六维机械矩阵测试（guard 列引用数据完备谓词） | E32：owner 合同矩阵 46/46；assignment 整文件 116/116（含 U13-41 三类行为反例）；三包类型零错、4 文件 Biome 零错；core/owner build 成功 | 通过 |
| owner control-admission | cancel / uncertain-resolve 控制请求与 run 权威流的同 envelope 原子提交 | control 定向 10/10 + assignment 内原子 cancel/三选/运行时交叉入口用例通过 | 通过 |
| scripts/check-runtime-package-exports.mjs | core/owner 新协议函数、共享 validator 与 control creator 的公开运行时导出清单 | `node --check` 通过；新增符号已同时存在 protocol/index.ts 与门禁清单；无 package manifest、lockfile、golden、schema/快照同步项；最终 dist 实值由 E28 检查 | 通过 |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| run 准入、派发激活与权威域绑定 | owner run 流 admitted/assigned/state + DispatchEnvelope artifact | admitted+queued、assigned+dispatched 各自同一 CommitEnvelope | 两个原子半边之间的语义坏日志、跨 conversation artifact、非队首 assignment、重启重放 | owner admit/assign 产；完整 run reducer、无 ArtifactStore submission guard、pending dispatch 消费 | 每次 O(log n) 队首索引与 O(1) 权威域/同 envelope 检查；full/guard 共用 admission/assignment/state 谓词；assigned 携 ownerEpoch/baseRevision/dispatchDigest 权威快照,full reducer 反绑 artifact；坏日志 fail-closed | 通过:E31 跨 conversation、初始半边、非队首反例;E32 快照反绑与 guard 语义谓词重放 |
| assignment 取消、真实副作用与封包排序 | executor assignment 流(recordSeq 全序) | bundle_sealed 与 abort/halted 的账本先后 | seal 与 abort 同事务域竞争 | executor 产;owner 消费 proof/bundle | 单 assignment 记录受 lease/交互上界约束;证据查询每页上限 256;热投影 LRU 上限 64,peer tail 与 staged/mirror 索引均增量推进 | 通过:生产/reducer 双守卫;abort 后新接收、未闭合 interaction/effect 终态与第二 halted 均机械拒绝(E20) |
| owner 取消栅栏与终态收束 | owner run 流 cancel-fence + state | submitCancelProof 事务 | 恢复重放插点 | owner cancel/恢复器产;executor 消费 fence | fence 每 assignment 至多一条;矛盾 proof 以 `(assignmentId,proofKind)` 的 `not-started-rejected` 只停止对应生产源 | 通过:取消矛盾不误停 supersede,重启保持 uncertain(E17) |
| 未启动证明、supersede 与安全重派 | durable-started 投影 + supersede-requested + AssignmentTerminationProof | assignment-superseded 同 envelope | fence 先到 tombstone;响应丢失重驱;uncertain 后迟到 proof | owner 产 fence/started;executor 产 proof | running/cancel/uncertain 入边与 supersede-started-observed 共同组成不可降格 started 事实;三类 not-started 统一先验 | 通过:Cancel/Supersede/DispatchRejection 正反例与独立停止锚均闭合(E17) |
| dispatch conflict、账本证据与 containment | dispatch-conflict 记录 + received 前缀链 + interaction 审计前缀 | 同 envelope 打开 conflict/吊销;审计批次 owner fsync 后 executor mirrored;contained+resolution+superseded | conflict 响应丢失、owner-fence/abort-ticket first-terminal-wins、pending/finished-unmirrored interaction、mirror ACK 丢失、Supersede 收束、缺半边重放 | executor 产 proof/审计批次;owner 裁决/回执;executor 以回执推进 mirrored | conflict 身份键去重;contained 每 openFactDigest 至多一条;mirror 必须签名、ordinal/摘要连续、精确批次重放;Cancel 两来源严格后继 received | 通过:签名连续批次、吊销后 audit settlement、精确 replay/反例与恢复均通过(E26) |
| wire/耐久闭合值 schema | core protocol/values.ts + assignment mirror batch 单源 validator | 落盘、验签或摘要复算前;submission 前置身份认证先于不受信 payload 深验 | 嵌套未知字段、错误可选值、full/delta/manifest、mirror batch、超大/缺失 artifact 两路 | control/owner/commit/assignment 生产与重放共同消费 | 每层 closed 校验;mirror batch 有非空/至多256/记录字节上界/签名/连续摘要;未认证调用不得触发 ArtifactStore | 通过:共享开放 JSON 子树递归 canonical 守卫及嵌套 Date 污染反例在 E29 通过 |
| uncertain 打开事实、三分解析与用户三选 | resolution 记录(openFactDigest/factDigest) | resolution/state/control applied 同 envelope 原子 | 裁决重放、received 后崩溃、不同 requestId 重投 | owner 产;surface 裁决;control journal 回放 | 每 run 至多一个打开 fact;requestId 单次;不同 requestId 不得把已关闭 fact 当回放 | 通过:摘要机械复算、同 requestId 回放、异 requestId 拒绝及原子提交测试(E4/E5) |
| 迟到合法 bundle 自动提交 | conversation CAS + attempt/open-conflict durable fence + ArtifactStore 闭包 | 前置身份认证→无 ArtifactStore guard 分类→必要时读取闭包→最终重检/committed | cold historical/open-conflict/terminal/旧 ownerEpoch 稳定拒绝、cold committed exact、响应丢失、坏 sidecar、并发终态 | executor 提交;owner auth/CAS;ArtifactStore 消费 | durable-rejection 零 payload/零 store;committed exact 仅验 artifact identity+内联原子 sidecar+capability↔assigned 快照 epoch 绑定；active/settlement 当前 epoch 且闭包前授权；历史 executor/ownerEpoch/baseRevision/open-conflict fence 全执行点共用谓词 | 通过:E31 错 executor/epoch/open-conflict 反例;E32 错 assigned base 与错 epoch capability 的 cold exact 反例 |
| 取消、重派与证明提交的恢复驱动 | InProcessConversationDispatcher 各 recover* + owner proof/batch 耐久回执 | 各 pending 谓词、精确 durable request 与耐久停止事实 | owner 重启、proof/mirror/response 丢失、A1→A2、证据分页中断 | owner 驱动;executor queryLedger/supersede/审计 batch 供证 | evidence 256/页;mirror 有界分批且 ordinal/摘要连续;exact proof/batch 零追加;异 proof/子集/拼批拒绝 | 通过:ACK loss、重启、A1→A2 exact proof/batch 与污染反例通过(E26) |
| publish / final-outbox 恢复热点 | 共享 `publish` / `final-outbox` 逻辑流 | committed 同 envelope 的 conversation 绑定与 per-conversation revision 水位 | 多会话交错提交、发布/终态响应丢失与重启 | owner CAS 产;publish/final 恢复器消 | pending 索引按 conversation 分区;进度 O(1);settled 只留身份墓碑;FinalOutbox 仅保留 pending/published 24h 热项 | 通过:两个会话同 commitRevision=8 且各有 pending publish/final 的交错恢复测试证明零串扰(E11) |
| attempt 代际当前性 | owner run `state.assignmentId` + `assignmentByRun` + proof/batch 耐久身份索引 | 每个 assignment 驱动状态事实与 exact replay 的 journal 事务/reducer | A1 关闭、A2 建立、A1 started/mirror/proof/bundle 迟到、响应丢失及坏日志注入 | owner 各 submission/proof 入口生产;run reducer 与 job 同构合同消费 | 新写 O(1) 比对当前映射;历史仅已耐久 exact proof/batch 零追加;异载荷不得读取/改变 A2 | 通过:A1→A2 全类迟到、精确历史 replay 与异载荷拒绝通过(E26) |
| submission 吊销后收束与重放 | 无 ArtifactStore submission guard + capability-revoked + 前置身份认证 + owner 串行四分分类 + 精确请求回执 | base authenticate→guard 激活/稳定分类；active/settlement 再验当前 epoch 与完整投影；durable replay/rejection 绑定 assigned 历史 epoch | 终态响应丢失、conflict 已吊销后 interaction closure/proof、异载荷、cold restart、旧 ACK、历史/打开 conflict/旧 epoch | executor adapter 产签名批次;owner authenticator/authorizer+耐久 revocation guard 消 | active 立即拒绝 revoked/旧 epoch；audit settlement 只推进当前 fence；合法旧 ACK 被后继水位覆盖时 no-op；replay/rejection 零追加 | 通过:E29 首轮 110/111 暴露 guard active revocation 漏接；统一授权原语后隔离 1/1、最终 111/111 通过 |

## 覆盖与核查

> 覆盖来源包括架构要求、不变量、验收项、交付文件与跨边界符号、生产端、消费者和测试;核查面固定为状态、入口与生产端、消费端与继承面、生命周期、并发与崩溃点、异常路径与终态、安全边界、模块边界、测试与验收。每轮填写“通过:证据”“不适用:依据”或“有问题:编号”。

| 覆盖来源 | 来源项 | 核查面 | 对象或路径 | 问题盘点结论与证据 | 终审一结论与证据 | 终审二结论与证据 |
| -------- | ------ | ------ | ---------- | ------------------ | ---------------- | ---------------- |
| 总纲/状态机 | 取消、三分续跑、uncertain | 状态 | §6.1 全 36 行 + interaction 审计正交维度 | 有问题:U13-04、U13-05、U13-25、U13-27~28、U13-31~32、U13-38~39 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 执行计划 13 | 双取消源、重派与裁决生产端 | 入口与生产端 | owner cancel/supersede/conflict/resolve/submission;executor abort/halt/mirror | 有问题:U13-06、U13-07、U13-25、U13-28、U13-31~33、U13-37~39 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 端口/导出 | proof、submission 与控制消费者 | 消费端与继承面 | protocol、owner/executor adapter、history/status、S5 mesh/S4 guard | 有问题:U13-09、U13-23、U13-25~28、U13-31~33、U13-37~38 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 记录合同 | fence/proof/conflict/resolution/mirror 生命周期 | 生命周期 | request→audit/proof→terminal/contained/resolved→exact replay | 有问题:U13-01~02(已验证)、U13-08、U13-23、U13-25、U13-27~28、U13-31~32、U13-38~39 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 故障矩阵 | 三方竞态与各 fsync 插点 | 并发与崩溃点 | owner-fence×abort-ticket×sealed、conflict×interaction×revocation、proof/mirror 响应丢失与重启 | 有问题:U13-04、U13-05、U13-06、U13-25、U13-27~28、U13-31~32、U13-39 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 状态机/产品终态 | committed/cancelled/failed/queued/uncertain | 异常路径与终态 | 自动解析、用户三选、无证明禁重派、审计/资源终态闭合 | 有问题:U13-05、U13-08、U13-25、U13-27、U13-31~32、U13-38 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 凭证/对抗矩阵 | proof/batch 签名、epoch、fence、账本链、授权时序与闭合 schema | 安全边界 | 污染、重放、错身份、子集/拼批、合法签名非法迁移、未认证 artifact 探测 | 有问题:U13-01(已验证)、U13-09、U13-23、U13-26~28、U13-31~33、U13-37~38 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 包结构/顺序 | core/owner/executor | 模块边界 | 进程内闭环,为 job/mesh/生产 guard 冻结可直接消费合同 | 有问题:U13-31、U13-33、U13-37~38(S5/S4 未来消费者会直接继承重放语义) | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |
| 执行计划验收 | 36 行与专项竞态/崩溃测试 | 测试与验收 | 逐边独立用例 + revocation×interaction、request replay、auth-before-store 正交矩阵 | 有问题:U13-10、U13-11、U13-12、U13-23~39 | 通过:指纹 `009752e6…` 重裁,裁决见终审记录第一轮 | 通过:裁决见终审记录第二轮 |

§6.1 三十六行逐边证据（测试名均位于 `packages/executor/src/__tests__/assignment-ledger.test.ts`;参数化行按独立 case 计数）：

| 行 | 直接证据 | 行 | 直接证据 |
| -- | -------- | -- | -------- |
| 1 | assigns only the earliest queued run | 19 | moves a requested cancellation to uncertain when an abort leaves an open effect |
| 2 | recoverable queued condition: temporary-capacity | 20 | commits when bundle sealing won before the owner cancellation fence + sealed-first 三方排列 |
| 3 | closes unassigned queued run as failed | 21 | keeps late halted proof as evidence after cancel-requested→uncertain |
| 4 | cancels a queued run without assignment governance | 22 | auto-commits a legal late bundle from non-conflict uncertain |
| 5 | closes unassigned queued run as expired | 23 | automatically requeues uncertain only after not-started proof + conflict not-started containment |
| 6 | durable receipt/start recovery + already-started supersede | 24 | uncertain decision user-verified-side-effects exactly once |
| 7 | commits from dispatched when started report is lost | 25 | uncertain decision user-abandoned exactly once |
| 8 | requeues dispatched only from signed rejection + lost-response recovery | 26 | uncertain decision user-retry-acknowledged exactly once |
| 9 | persists supersede fence and safely requeues + response-loss recovery | 27 | recoverable queued condition: offline-explicit-target |
| 10 | never redispatches an uncertain assignment without a termination proof | 28 | accepts abort-ticket not-started proof during recovery |
| 11 | cancel-requested from dispatched（参数化独立 case） | 29 | accepts halted abort proof while owner still considers dispatched |
| 12 | commits sealed conversation exactly once（running） | 30 | marks dispatched state uncertain from verified abort evidence |
| 13 | cancel-requested from running（参数化独立 case） | 31 | accepts abort-ticket halted proof for running assignment |
| 14 | moves running assignment with unknown ledger outcome to uncertain | 32 | contradictory not-started proof moves running to uncertain |
| 15 | durable owner cancellation accepts not-started proof | 33 | marks running owner state uncertain when abort cannot close effect |
| 16 | halts running only after every side effect closes | 34 | idempotently accepts owner-authoritative dispatch conflict |
| 17 | abort-ticket not-started after owner cancellation requested | 35 | opens mismatched dispatch conflict without treating it as termination |
| 18 | abort-ticket halted after running cancellation requested | 36 | opens/contains mismatch with owner-fence 或 abort-ticket halted proof;not-started 两来源 containment 分别原子 requeue |

专项矩阵：`linearizes the three-way cancellation race...` 六个参数化 case 覆盖 owner-fence × abort-ticket × sealed 全排列；abort fence 后 received/dispatch-rejected、pending interaction seal/halt、第二个 freshly-signed halted 五类负向重放均拒绝；`contains a dispatch conflict when an abort-ticket...` 两个 case 覆盖 not-started/halted 先赢、后到 owner-fence 零追加及重启停止；`rejects sealing while a side effect is open in both producer and replay` 覆盖 effect 双守卫；其余污染、错对象/错 fence、非原子/缺半边与错 factDigest 对抗覆盖保持；IO 重用例均显式 `15_000ms`。

## 问题清单

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U13-01 | `createOpenResolutionFact` 曾以 `"UncertainResolutionOpenFact"` 计算 openFactDigest,spec 冻结为 `D("UncertainOpenFact",…)`;`resolution.factDigest` 曾三路径三种摘要来源,违反 §1.2 每 Digest 字段恰命中一类 | 摘要域字符串未从 spec 冻结值取用;factDigest 计算散在调用方 | resolution 记录、`uncertain-resolve` 绑定摘要、S5 跨机复算、重放验证、uncertain 全部测试 | openFactDigest 改回 `"UncertainOpenFact"`;factDigest 收敛单点 `resolutionFactDigest = D("UncertainResolutionDecision",1,{openFactDigest,kind,by,at})`,reduce 增复算与字段封闭守卫;spec 同步冻结公式 | 已验证 |
| U13-02 | owner 日志新增 `supersede-requested` / `cancel-contained` 为机制必需,但不在 spec §4.3 两联合 | spec 缺口:supersede fence 无耐久载体、非 conflict containment 无止损锚 | spec §4.3 run/job 两联合、单元 14 同构、S5 重驱、S8 收编导出 | 按封版流程回填 spec(run/job 双流 + fenceSeq=envelope lsn 语义),代码不动 | 已验证 |
| U13-03 | executor 默认 `usageFinal` 以自造 `"AssignmentUsageFinal"` 摘要占位,spec 注册目标是 `UsageReport.digest`(S4 落地) | 资源治理未落地前的结构占位,曾无边界标注 | 耐久 CancelProof.usageFinal、S4 对账、租约终结表 | 保留占位 + 源码注释显式标注 S4 边界与注入义务;S4 接入真 governor 时替换 | 已验证 |
| U13-04 | `#recordSealedBundle` 决定函数与 reducer `bundle_sealed` case 均只查 phase/aborts,未查未闭合 side effect;spec §4.3 明文“halted 与 bundle_sealed 之前全部 effect 必已闭合” | 同一句纪律只在 halted 半边落实 | executor 封包生产端、reducer 重放守卫、owner CAS 消费的 bundle 可信度、副作用不明的提交、封包测试 | 生产端与 reducer 双点补 open-effect 守卫(存在未闭合 effect 拒绝封包);补“开 effect 时 seal 被拒”用例 | 已验证 |
| U13-05 | 矛盾 not-started CancelProof 首次被 `contradictoryNotStarted` 拒绝转 uncertain(cause: cancel-unproven);恢复器重提交同一 proof 走 uncertain 分支,该分支无矛盾复查,直接 resolution+superseded+queued 自动重派;abort-ticket 证据从 running 直接开 uncertain 时又没有 cancelOrigins 可供复查 | 矛盾守卫只存在于非 uncertain 分支,投影未保留 uncertain 的耐久入边状态 | owner submitCancelProof、恢复驱动 recoverCancellationProofs、“已耐久见 started 不得自动重派”不变量、owner-fence/abort-ticket 两源重放测试 | 投影由 state 记录序列派生 uncertainOrigin;所有 not-started 路径在 containment/解析前统一检查“当前或耐久入边曾 running”;矛盾则保持 uncertain;补 owner-fence 重放与 running→evidence→uncertain→not-started 两源用例 | 已验证 |
| U13-06 | `markAssignmentUncertain` 生产零调用(仅测试);§6.1 行 19/30/33“账本见 abort-requested 无可接受 halted → uncertain”owner 侧无判定路径;uncertain 后 pendingSupersedes 停止重驱;若恢复重驱得到 already-started,又无耐久停止事实 | uncertain 的生产、可核验账本证据消费和 supersede 全决定生命周期只建了一半 | InProcessConversationDispatcher 恢复驱动、LedgerSnapshot/LedgerEvidencePage 签名/链/分页/artifact-ref 消费、§6.1 行 10/14/19/21/30/33、not-started/already-started 两种 supersede 响应及恢复测试 | core 增 LedgerEvidencePage/AssignmentEntry 结构与签名校验;owner 对 snapshot 冻结前缀按 256/页流式验签、解析 ref、复算完整链,见 abort 且无 proof/seal 时生产 cancel-unproven;uncertain 继续同 fence 重取 proof:not-started 原子解析,already-started 写 `supersede-started-observed` 耐久停止锚并保持 uncertain;spec run/job 联合同步;超时/失联触发策略留外层 | 已验证 |
| U13-07 | control-admission 仅支持 `input/session-create`;§6.1 行 24 动作字面含“applied”而 `resolveUncertain` 无 control applied 半边;幂等以“decision 相等”近似替代“requestId 回放/已关闭 fact 拒绝”,与 §5.1/§5.5 合同有实义差 | cancel/uncertain-resolve 的控制面 wire 接线未建,幂等语义降级 | ControlRequestJournal、session.abort/uncertain-resolve 入口、三选裁决原子性、渠道重投场景、控制面测试 | 必须把 `packages/owner-kernel/src/control-admission.ts` 纳入交付边界:扩展受信 envelope 校验与 requestId 回放,提供在同一 AuthorityCommitLog 临界区联合重放 control+run 投影的原子 apply;cancel 写 fence/state/applied,resolve 写 resolution/revocations/state/applied;同 requestId 回放原结果,不同 requestId 对旧/关闭 fact 耐久拒绝;现有 assignment 集成测试承载验证,无需再扩测试文件 | 已验证 |
| U13-08 | 实现对非 conflict not-started 也写 `cancel-contained`;冻结三分语义(§3.6 contained 属 conflict 专属段落、§6.1 行 23 仅 conflict 分支“另写 contained”)为:conflict not-started 写 `dispatch-conflict-contained`+resolution+superseded;非 conflict halted 只写 `cancel-contained`;非 conflict not-started 只写 resolution+superseded | 引用 spec 时未核对段落归属,实现语义超出合同 | owner submitCancelProof 两分支、reducer `cancel-contained` case(需拒收 not-started proof)、spec §4.3 注释、containment 测试 | 改实现对齐冻结三分语义(非 conflict not-started 去掉 containment;reducer 收紧仅接受 halted);spec §4.3 已回填注释与此一致,复核后不动 | 已验证 |
| U13-09 | `dispatchPending` 的 rejected-before-received 分支只消费 `outcome.proof`、丢弃 `outcome.error`,未按 §3.6“响应 error 必须与 proof.error 全等”校验;conflict 路径有对应校验,同构不齐 | 消费端合同校验漏了 rejection 半边 | owner 派发回执消费、DispatchRejectionProof 接受守卫、污染响应测试 | `acceptDispatchRejection` 入口补 response.error 与 proof.error 全等校验;补污染用例 | 已验证 |
| U13-10 | 两个 IO 重用例(interaction backlog、publish/final 恢复)单跑 3.8s+,贴 vitest 默认 5s 超时线;全套或负载下失败集漂移,隔离重跑全过 | 显式测试预算缺失是表层原因;重复全量 WAL 重放是已确认的放大根因(U13-12) | CI 稳定性、每次偶发红的归因成本、第 12 单元遗留 | 先修 U13-12 消除重复全量重放;仍仅为这两个真实 fsync 重用例声明显式 timeout 预算,不砍测试内容 | 已验证 |
| U13-11 | §6.1 全 36 行逐边独立覆盖与相关崩溃点的验收证据未成套;交付物边界此前未含 spec 修订、control-admission 与单条 mutation validator 所在 commit.ts | 单元验收流程未走完即中断(压缩重放死循环事故),后续边界登记又漏掉一个真实改动文件 | 冻结终审、最终验证、交付物指纹 | 集中修复完成后按执行闭环走冻结准备→两轮终审→最终验证;本文件已按最终 10 文件边界完整登记 | 已验证 |
| U13-12 | 热投影已使用 cursor,但 executor/owner `#transact` 仍在每次写后复制完整 projection;executor `queryLedger` 每个证据页又先复制完整账本,故长 assignment/对话的连续写仍累积平方级复制,分页额外内存也不是登记的 O(页) | 把“内部事务返回值”误当成必须暴露的隔离快照,未按调用者所需最小投影选择结果 | executor 全部账本写、owner run 写、取消证据分页、Windows/CI 时延、长会话与大 authority WAL;协议结果不变但仍是确定的性能架构债 | `#transact` 只返回内部事务结果,唯一需要 state 的 assign 仅快照单条 assigned;executor 增串行 selector,证据查询只复制 snapshot/目标页而非全账本;保留公开异步读的隔离快照;类型、分页/恢复与完整 assignment 回归通过 | 已验证 |
| U13-13 | 矛盾 not-started proof 修复后会保持 uncertain,但 `pendingCancellations` 仍把无 contained 的 owner-fence 视为待重驱;该 proof 未落任何耐久记录,重启后会无限重提同一矛盾证明 | 只修了状态安全,遗漏恢复 outbox 的耐久停止事实 | owner-fence 矛盾 proof、pendingCancellations、响应丢失/重启、run/job spec 同构与测试 | 新增 `cancel-not-started-rejected` 耐久观察记录:与首次转 uncertain 原子写,或在已 uncertain 时单独幂等写;全验 proof 与 started 矛盾后生效;pendingCancellations 据此停止,状态保持 uncertain;run/job spec 同步,补重启后 outbox 为空用例 | 已验证 |
| U13-14 | 签名 evidence page 可被持 executor 签名者替换其中 `received.activation/envelope.ref` 后整体重签并重算链;此前 owner 只验页签名/链,未把内层 received 重新绑定本地 assigned | 外层完整性与内层业务身份绑定被误当成同一件事 | LedgerEvidencePage artifact/inline 两路、received ActivationProof、assigned dispatch/commit、污染重签测试 | 解引用后逐条验证 AssignmentEntry;received 再跑 validateConversationActivation,由本地 assigned 重建期望 activation 并全等比较 envelope ref/commit/payload;对抗用例重签整页仍拒绝 | 已验证 |
| U13-15 | cancel fence 耐久后,`finishInteraction`/`stageMutation` 在查已完成 requestId 前先拒绝 abort,导致相同请求的响应丢失重放失败 | 新工作栅栏误伤已完成结果的幂等读回 | executor 交互结果、staged mutation、取消后响应重试、reducer | 先按 requestId 查耐久结果并要求规范载荷全等;仅新结果/新 mutation 受 abort fence 拒绝;补同请求成功回放与新请求拒绝 | 已验证 |
| U13-16 | executor/control LRU 与 cursor 身份已修,但 `ControlProjection` 的 copy-on-commit 每次用完整 Map 合并全部历史,连续 n 个控制请求仍为平方级复制 | 为隔离 global/authority cursor 状态采用全量不可变拷贝,而未利用两套 cursor 可各自推进的事实 | control 全部请求、长进程内存/时延、global 与最多 8 个 authority cache 的游标正确性 | global projection 停在 receipt cursor、authority cache 独立推进 applied tail,两者不共享 Map;成功提交仅把本次 draft delta 合入各自 base,失败仍丢弃 draft/cache;保留交错普通/atomic control 回归 | 已验证 |
| U13-17 | TypeScript 区分 initial/conversation control,但运行时 `apply`/`applyAuthority` 可被 cast 后交叉调用 | 类型入口没有运行时封闭,wire 边界可绕过 | createInitial/createConversation、apply/applyAuthority、控制结果类型与 authority stream | 两类 creator 与两个 apply 入口均做运行时判别;conversation 禁 ingress,initial 拒 cancel/resolve,atomic 拒 input/session-create;补交叉注入测试 | 已验证 |
| U13-18 | supersede-requested 或 executor dispatch-rejected 先耐久、用户 cancel 后,原 cancel outbox 会在 executor 终态 no-op,owner 又停止原终结链,形成 cancel-requested 无出口 | 两条独立停止协议在交错顺序下互相覆盖恢复义务 | executor cancel、owner pendingSupersedes/assignmentsAwaitingRecovery、not-started/already-started、响应丢失与重启 | terminal executor cancel 幂等 no-op;cancel-requested 继续原 supersede fence;not-started 以 superseded+revoked+cancelled 收束,already-started 写停止锚并继续 cancel;dispatch-rejected 只重放同 durable dispatch 取原 proof;三条竞态测试 | 已验证 |
| U13-19 | 正常 CancelProof 驱动 assigned run 转 cancelled 时 owner 曾只写 state+revocation,未耐久被接受 proof;同 assignment 的异 proof 会被“已 cancelled”短路,usageFinal/审计与 reducer 无法机械复算;同族新增 owner 记录亦缺部分状态/同-envelope 守卫 | 把生产端已验证的瞬时决定误当成可由结果状态替代的耐久事实 | cancel proof 精确幂等、owner replay、未来 usage/租约结算、conflict/containment/supersede/fence/resolution 原子性及对抗测试 | 新增 `cancel-proof-accepted`(完整规范 proof),与 revoked+cancelled 原子写并按去 signature 载荷精确回放;spec run/job 同构回填;reducer 对本单元 proof/fence/resolution 新记录补当前态、权威绑定、同-envelope 状态/吊销/解析守卫;非原子注入测试必须拒绝 | 已验证 |
| U13-20 | 两轮终审反向构造发现同 envelope 守卫仍有半边缺口:`dispatch-conflict(acked-original)` 可不带 dispatch-acked,`opened-uncertain` 可不带 cancel-fence/吊销;`resolution(proven-not-started-redispatched)` 可不带 assignment-superseded;用户三选可不带 control applied/吊销;正常或迟到 bundle 提交关闭 assignment 时生产端未写 capability 吊销,且 committed reducer 仍可接受缺 content-index/final-outbox/条件 publish sidecar 的提交 | 生产端“通常写全”被误当成 reducer 可机械证明原子合同;伴随事实只由另一半检查,遗漏反向完整性 | conflict 两分支、not-started 自动重派、用户三选、正常/迟到提交、capability 生命周期、既有提交投影/final/publish 恢复、owner 重放与非原子注入测试 | reducer 对 conflict 两分支要求完整伴随事实;resolution 按 kind 要求 termination/control-applied、完整吊销与目标状态;提交生产端补 remaining capability revocations,committed reducer要求同 envelope state+吊销+content/final/条件 publish 完整闭包;以删半边的对抗用例证明零接受,并复跑 36 行与既有提交回归 | 已验证 |
| U13-21 | user resolution reducer 只检查同 envelope 存在 `{t:"uncertain-resolve",state}` 的成功 control applied,未与本次 resolution.factDigest 绑定;另一打开事实的合法 applied 可被误用为当前事实的原子半边,现有缺半边测试未覆盖错半边 | 只证明伴随记录“同类存在”,未证明它与本次权威决定是同一身份 | core ControlResult 合同、control 结果校验/回放、owner applyControl 生产端、resolution reducer、三选对抗重放、spec §4.3 | `uncertain-resolve` 成功结果增加 `factDigest`,生产端写 resolution 的同一摘要,control validator 精确校验;run reducer要求 applied 的 state+factDigest 与正在关闭的 resolution 全等;补错 factDigest applied 反例并复跑三选 | 已验证 |
| U13-22 | owner run reducer 对新增取消/冲突记录已逐条 `assertExactRecordKeys`,但 `admitted/assigned/dispatch-acked/capability-revoked/interaction-mirror/committed` 六个既有分支仍接受并忽略未知字段 | 验证纪律只随新增记录局部落实,没有在当前完整 reducer 边界反查同族分支 | AuthorityCommitLog 重放、旧记录升版隔离、提交/吊销/派发状态投影、未知字段对抗测试 | 六个分支按冻结联合补精确字段集合;以带额外字段的既有记录重放证明 fail-closed;类型、格式与完整 assignment 回归通过 | 已验证 |
| U13-23 | owner 恢复曾只验 evidence 页签名/链与局部字段,可接受 executor 自身 reducer 会拒绝的合法签名记录序列;`staged-mutation.mutation` 未在 AssignmentEntry 边界命中 closed union;owner `admitted` 与 control source 的 IngressContext 只做浅层或分散校验 | 同一 wire/耐久事实在 executor、owner、control 三处各自维护不完整验证器,没有共享“单条深验 + 增量迁移”合同 | LedgerEvidencePage inline/artifact 两路、executor 本地重放、owner uncertain 生产、staged mutation 全联合、admitted queue/ingress、channel responder/principal 与所有污染反例 | core 建唯一 `validateAssignmentEntry` 深验与 `applyValidatedAssignmentEntry` 增量状态机,executor/owner 共用;commit.ts 暴露单条 staged mutation 深验;IngressContext/ChannelResponderRef 单源验证并接入生产与重放;补重复 received、非法 mutation、非法 admission/ingress 反例,88/88 通过 | 已验证 |
| U13-24 | 长 assignment/会话仍有确定性平方路径:每次 staged 写重验完整 batch、镜像分批反复序列化累计数组、conflict/commit/publish/final/status 从全历史扫描或复制;性能改造若将共享 `publish/final-outbox` 平铺索引,又会把不同 conversation 的同 commitRevision 冲突并跨会话恢复 | 快路径只局部换 Map,未把热投影设计成按事件增量维护、按输出选择最小快照,也未显式建模共享流的 conversation 作用域 | executor mutation/interaction/seal;owner conflict、投影恢复、publish/final/status history;重启/响应丢失;多会话共享 authority log;时间、空间与身份隔离 | 单条 mutation O(1) 深验与 requestId 索引;镜像字节增量精确计数;seal 只选所需投影;owner 为 conflict/pending projection/status/publish/final 建增量索引与终态压缩墓碑;publish 恢复复用不可变 batch/domain plan,由耐久水位经 `grantedIndexBySeq` O(1) 定位下一条且不重读/重筛整批;共享流 pending/revision 按 conversation 分区;补 32 条 mutation 重启、镜像边界、发布中途水位续跑及双会话同 revision/pending 恢复用例 | 已验证 |
| U13-25 | CancelProof 之外的 DispatchRejection/Supersede(not-started) 未统一检查 owner 已耐久 started；`cancel-not-started-rejected` 又以 assignment 为全局键，可能让一种矛盾 proof 误停另一恢复链；dispatch-conflict 只能由 CancelProof containment，且 reducer 未反向要求 containment 与 superseded 携同一 proof 原子出现 | “未启动证明”按调用入口分别实现，没有把 durable-started 建模为 AssignmentTerminationProof 的共同前置不变量，也没有把停止事实按生产源分区 | 三类 termination proof 的生产、reducer、pending cancel/supersede/dispatch recovery、running/cancel-requested/uncertain 历史入边、dispatch conflict 的 Cancel/Supersede/DispatchRejection 三路与崩溃重放 | 建唯一 `validateAssignmentTerminationProof` 与 `hasDurableStartedObservation`；`not-started-rejected` 键固定 `(assignmentId,proofKind)` 且各自只停对应源；started 或 received-conflict 矛盾保持 uncertain；conflict 允许严格后继 received 的 Cancel(owner-fence)/Supersede，并强制 contained+resolution+superseded 同 proof 原子出现，DispatchRejection 禁止解析；补全部正反例及重启证据 | 已验证 |
| U13-26 | wire/耐久闭合 schema 仍可被嵌套未知字段或 falsy 可选值绕过：UserTurnInput 验证分散且浅；full/delta messages、manifest.environment 子数组缺深验；workspace/mutationBatch 以 truthy 判定 | 协议边界没有共享递归 validator，可选对象把“存在性”错误等同为 truthiness，生产与重放各维护半套规则 | control-admission、owner admitted、DispatchEnvelope full/delta、ExecutionManifest、TranscriptRunRecord、ConversationCommitBundle、AssignmentEntry bundle_sealed、staged workscene mutation及运行时导出 | core 新增 `protocol/values.ts` 单源验证 UserTurnInput/Message/EnvironmentRequirement，所有 wire/耐久消费者复用；嵌套判别联合与数组逐元素 closed 校验；可选对象统一 `!== undefined`；导出门登记并补嵌套污染/falsy 反例 | 已验证 |
| U13-27 | 共享 assignment reducer 可接受 abort-requested 后的 received/dispatch-rejected、pending interaction 未闭合时的 halt/seal，以及基于最新前缀重新签名的第二个 halted；executor 生产端不会产生这些序列，但 owner evidence recovery 与 executor replay 会接受 | 把生产端顺序纪律误当成账本状态机不变量；终态只检查 effect/部分 phase，没有统一验证 fence 单调、interaction 闭合与终态唯一性 | core 增量 reducer、executor 本地恢复、owner evidence recovery、S5 mesh 证据、job 同构、GC 终态与全部负向重放 | 共享 reducer 统一要求 received/rejection 只能位于无 abort 的首记录；halted 只能从与 decision 匹配的 unknown/received/started 一次进入且 interaction/effect 全闭合；seal 同样要求 interaction/effect 全闭合；补五类合法结构但非法迁移的重放反例 | 已验证 |
| U13-28 | dispatch-conflict containment 只接受 owner-fence CancelProof；若 abort-ticket 先写唯一 halted，后到 owner fence 只能零追加返回原 abort-ticket proof，owner 永久拒绝且 outbox 无法收束；spec 记录类型又与 §6.1“cancel fence/ticket”状态行矛盾 | 双取消源已经采用 first-terminal-wins，但 conflict 消费端仍按“预期调用来源”而非“可信终止事实”裁决 | executor halt 幂等、owner submit/reducer、pendingCancellations、not-started 自动重派、halted 保持 uncertain、重启恢复、run/job spec 与 S5 mesh | `dispatch-conflict-contained.proof` 接受完整 CancelProofBody；在线 owner-fence 绑定当前耐久 fence、abort-ticket 重新鉴权，重放统一验签与 assignment/executor/epoch；二者均要求严格后继 received；not-started 仍同 envelope contained+resolution+superseded，halted 只 contained；补两 decision 下 abort-ticket 先赢→owner fence 零追加→提交→重启停止的竞态测试 | 已验证 |
| U13-29 | A1 经 uncertain 用户重试关闭并建立 A2 后，多个 owner 入口仍以 A1 找 run、再读取 run 的 A2 当前状态；`state` 耐久记录又不携 assignment 身份，导致迟到 A1 started/proof/bundle 可越权改变 A2，reducer 无法机械区分触发 attempt | run 状态事实只按 runId 建模，遗漏“事件所属 assignment 必须等于状态转换所属当前 attempt”的代际栅栏 | started/reconcile、cancel/supersede/rejection/conflict/evidence、bundle、interaction mirror、全部 reducer 分支、A1→A2 延迟消息矩阵与 job 同构合同 | assignment 驱动的每条 state 记录携 assignmentId，未分配转换明确省略；所有生产端在事务前缀验证 current assignment，精确历史回放仅零追加；reducer 对记录身份、当前映射及同 envelope 关闭顺序双守卫；A1 关闭→A2 建立后各类 A1 迟到消息均拒绝且 A2/日志不变 | 已验证 |
| U13-30 | 终态提交同 envelope 吊销全部 capability，但 submitBundle/submitCancelProof/reportStarted/interaction mirror 的响应丢失重试仍使用原 capability；未来严格 guard 会在 owner 幂等事实检查前拒绝。dispatch-conflict 等 uncertain 又需在已吊销后接收可信收束 proof；未镜像 interaction 还可能跨终态留下新的审计写 | 把“新写授权”“只收窄状态的终结证据”“已耐久结果零写重放”混成同一种 capability 活跃性判断，且终态未要求 finished interaction 已镜像 | owner submission authorizer、bundle/cancel/started/mirror、capability revocation/expiry、executor 终态纪律、进程内/mesh adapter、S4 guard 与响应丢失/重启 | 四分授权：active 由 owner 耐久激活/吊销守卫；settlement 仅当前 attempt 的已验 bundle/cancel proof；durable-replay 仅耐久精确吸收且零追加；durable-rejection 仅耐久栅栏决定的稳定 fence。seal/halt 前强制 pending 已关闭且全部 finished interaction 已镜像，正常 run-end/取消均先镜像再终态；严格 guard、owner 本地 guard、异载荷、收束和 exact replay 全通过 | 已验证 |
| U13-31 | dispatch-conflict(opened-uncertain) 同 envelope 立即吊销全部 capability；若 executor 已 started 且存在 pending/finished-unmirrored interaction，cancel 会先写 abort + interaction-finished，halted 又必须等待 owner mirror；owner 对 fresh mirror 只判 active，严格 guard 必拒 revoked cap，故 proof 永远无法形成、conflict outbox 永不收束。另 executor/core 的 `mirrored(upTo)` 只要求 upTo 命中某个 finished，稀疏提交可越过并清除更早未镜像结果 | interaction 审计仍是“裸 entries + recordSeq 水位”的活跃写调用，没有独立的签名连续事实链、精确批次身份和只收窄审计状态的 settlement 权限；终态守卫与吊销时点被分别设计后形成环 | executor interaction-finished/pending batch/mirrored reducer、owner conflict/revocation/mirror reducer、in-process adapter、S5 mesh、正常取消/封包/用户裁决同族路径、pending 与 finished-unmirrored 两种组合、ACK 丢失/重启/稀疏水位测试 | 建 `ConversationInteractionMirrorBatch`：executor 对按 finished ordinal 连续的非空批次签名，批次携 previous/end mirror digest，owner 以 assignment+ordinal+digest 机械验连续并耐久记录 exact batch 身份；current cancel-fence 下 fresh batch 走仅审计 settlement（允许该 attempt 已 revoked，绝不恢复 started/bundle/session/global 写），其他 fresh 仍 active；owner 回执携 ordinal/digest，executor `mirrored` 必全等绑定并拒绝跨缺口水位。补 conflict×pending/finished、revoked 仅审计、稀疏/伪造批次、ACK 丢失与重启全矩阵 | 已验证 |
| U13-32 | owner 仅以 `containedFacts:Set<openFactDigest>` 记 containment，丢失 proof/request 身份；exact contained CancelProof 在用户裁决/A2 后会先撞 historical guard，异 proof 同 fact 也无法与原 proof 比对；`mirrorInteractions` 又把空数组、任意已耐久子集/跨批拼接均当 durable-replay，未证明原请求真实存在 | durable-replay 以“结果已覆盖”近似“该规范请求已耐久精确吸收”，投影只留结果水位而未留请求级身份 | cancel-contained/dispatch-conflict-contained/not-started-rejected/cancel-proof-accepted 四类 proof 终态、A1→A2、revoked/expired 重放；mirror 单批/多批/混合批、owner 响应丢失、reducer 与测试 | containment 按 assignment/openFact 保留完整规范 proof；cancel exact replay 在 current-attempt/settlement 判定前，以 accepted/superseded/contained/rejected 四类耐久事实逐类全等匹配，异载荷拒绝；mirror 以 U13-31 的 unsigned batch digest 建耐久请求索引，仅完整原批次可零追加 replay，空/子集/拼批/跨批混合不得命中。补关闭后/A2/过期 exact proof 与异 proof、batch 子集/拼批反例 | 已验证 |
| U13-33 | `submitBundle` 在任何 submission 鉴权前先深验整个 SealedBundle 并读取 bundle/mutation/transcript/dependency artifacts，历史/open-conflict 的 durable-rejection 直到其后才判定；未授权或 malformed A1 可触发 IO 并观察 missing-base/invalid 差异。`reportStarted` 先读状态、mirror 先遍历 payload、submitCancelProof 先验 proof 且在 owner-fence/abort-ticket 绑定前就以 settlement 授权，同族入口均缺统一前置身份层 | 单一 `authorize(mode)` 同时承担静态身份认证和基于耐久状态的结果能力分类；为知道 mode 先消费了本应受保护的状态/payload/store，形成循环依赖 | 四个 RunSubmissionPort 入口、AssignmentSubmissionAuthorizer、owner 激活/吊销投影、ArtifactStore、bundle closure/CAS、S4 正式 guard、S5 mesh、资源放大与存在性侧信道测试 | 授权分两层：`authenticate` 先只验 capability 签名/方法/scope/assignment/executor 静态身份，owner 随即以只消费 assigned 内联身份的轻量投影验激活，任何业务状态/payload/store 消费前完成；当前 epoch 独立栅栏，bundle 返回结构化稳定拒绝。随后在同串行前缀判四种 mode；committed exact 只算 artifact identity、不读 closure；active/settlement 完成授权后才读 ArtifactStore并最终事务重检。以计数 ArtifactStore 证明未认证/未激活/旧 epoch/稳定拒绝/exact replay 零读 | 已验证 |
| U13-34 | `submitBundle` 的 preflight 仍调用会解引用 admitted/dispatch/committed artifact 的完整 `RunProjection`；现有零读取测试均复用热 journal，冷重启的 historical/open-conflict/committed exact 路径会先读 ArtifactStore。另当前 ownerEpoch 在 mode 分类前被当成“是否激活”统一拒绝，使旧 epoch 的 exact durable-replay 与 active fence 混同 | 轻量投影只承载 activation，没有承载 bundle 结果分类所需的 current-attempt/state/open-conflict/committed 内联事实；epoch 有效性又被放错到历史身份层 | 四个 submission 入口的 mode/epoch 语义、cold restart、ArtifactStore 侧信道、committed exact/historical/open-conflict/terminal fence、S4 guard 与 S5 mesh | 将轻量投影提升为无 ArtifactStore 的 submission guard 投影，保留 assigned 激活并增 run/current/state/open-conflict/committed 内联索引；bundle 先在该投影判 stable rejection/committed candidate，exact artifact identity 后直接 durable-replay，active/settlement 才加载完整投影与 closure。当前 epoch 只约束 active/settlement；durable replay/rejection 仅验历史 capability 与 durable assigned 全绑定。补全冷 journal 零读取及旧 epoch exact/fresh 对抗 | 已验证 |
| U13-35 | owner `interaction-mirror` reducer 只验 current mapping/closed flag，不验当前 state 与 cancel-fence，因 committed/cancelled assignment 仍保留 mapping 且未进 closedAssignments，可接受终态后的 fresh batch；普通无 cancel-fence uncertain 同样可接受。跨批重复 requestId 也未拒绝。executor 在后继 mirror 水位已耐久后收到合法旧批回执，会因 ordinal<current 报冲突 | 生产端 mode 守卫、账本 interaction 唯一性与至少一次回执吸收语义没有完整下沉到 replay reducer/receipt consumer | owner 生产/reducer、executor receipt、终态/uncertain/cancel settlement、跨批审计唯一性、并发 flush/响应乱序、S5 mesh 与 job 同构 | owner mirror 投影保存已镜像 requestId，生产与 reducer 同时要求 dispatched/running，或 current cancel-requested/uncertain+同 attempt cancel-fence；批内/跨批 requestId 唯一。executor 先按 finished checkpoint 全等验回执，已被当前连续水位覆盖的合法旧前缀幂等 no-op，异回执仍拒绝。补终态/无 fence uncertain 注入、重复 requestId、旧 ACK 乱序矩阵 | 已验证 |
| U13-36 | `validateTranscriptRunRecord` 原实现对 `tool_use.input` 调用 canonical snapshot；抽取共享 `validateMessages` 后只验最外层 plain object，嵌套 Date/BigInt/undefined/accessor/稀疏值可由独立 validator 接受，进程内与 mesh 的协议边界不再等价 | 共享 validator 迁移只复刻字段联合，遗漏开放 JSON 子树仍须递归满足 canonical JSON 的旧守卫 | protocol/values.ts、commit transcript、dispatch full/delta、独立导出消费者、签名/摘要前输入与嵌套污染测试 | 在共享 tool_use 分支复用 canonicalize 对 input 做完整递归协议值校验并给出稳定错误；补直接 validateTranscriptRunRecord/validateMessages 的嵌套非 canonical 反例，保持合法任意 JSON 输入可扩展 | 已验证 |
| U13-37 | `assign()` 在线入口拒绝其他 conversation 的 DispatchEnvelope，但完整 run reducer 的 `assertAssignedMatchesDispatch` 只比较 run/assignment/executor/manifest/ref/lease/cap/reservation/ingress，未验证 `dispatch.work.conversationId === journal conversationId`；因此同签名域下、内部自洽但属于另一 conversation 的 artifact 可随 assigned+dispatched 坏日志进入当前 run 流，并由 pending outbox 重新签发 activation。历史 ownerEpoch 必须允许新 owner重放，故问题只在 conversation 权威域，不把“不同于当前 ownerEpoch”误判为坏历史 | 把 DispatchEnvelope 的内部自洽验证等同于它与当前 AuthorityCommitLog stream 的外部权威绑定；在线 producer 守卫未下沉到权威 reducer | assign producer、dispatch artifact、完整 run reducer、pendingDispatches/materialize activation、冷/热恢复；submission guard 仅消费内联 assigned，fresh 写最终仍须由完整投影兜底；S5 传输消费者与跨 conversation 对抗测试 | `assertAssignedMatchesDispatch` 显式接收并校验 journal conversationId；保留历史 ownerEpoch 重放语义，只要求 bundle/dispatch 与各自 assigned 历史 epoch 全等；补合法签名跨 conversation artifact 的 assigned+dispatched 重放反例，并确认同 journal 历史 epoch 重开仍通过 | 已验证 |
| U13-38 | `submitBundle` 在线路径完整校验 bundle 的 conversation/run/assignment/baseRevision/ownerEpoch/executorId 且拒绝打开的 dispatch-conflict；完整 reducer 的 committed 分支只校验前四类身份/版本，遗漏 `bundle.executorId === assigned.executorId`、`bundle.body.ownerEpoch === assigned.dispatch.ownerEpoch` 与 open conflict；resolution 又允许 `cause=dispatch-conflict + kind=late-bundle-committed`。无 ArtifactStore guard 的 committed exact 分支同样未用调用方原 bundle 复核 executor/历史 capability epoch，坏日志可被冷启动重放为成功 | 完整 CAS 只在在线事务决策实现，权威 reducer、cause×resolution 判别联合和冷 guard 各保留了不同子集，违反同一合同三处等价 | submitBundle preflight/final transaction、committed/resolution 完整 reducer、submission guard reducer 与 cold exact replay、dispatch-conflict 终态、S4 guard/S5 mesh、错 executor/epoch/conflict late bundle 对抗测试 | 建唯一历史 assignment commit fence 比较：完整 reducer以 assigned dispatch epoch/executor 为准；cold exact 以 guard assigned.executorId + 已验签历史 capability.ownerEpoch 复核调用方 bundle，保持零 ArtifactStore 读取；完整 reducer与 guard双向拒绝 open conflict committed及 conflict+late-bundle resolution。补错 executor、错 assigned epoch、open conflict late commit 的完整投影与 cold guard 反例 | 已验证 |
| U13-39 | 在线 `admit` 固定同 envelope 写 admitted+queued，`assign` 固定同 envelope 写 assigned+dispatched 且只选队首；完整 reducer 与 submission guard 却接受 admitted/assigned 单独 envelope，后续 state 也不要求本 envelope 的对应半边，完整 reducer 的 assigned 分支还未复核当前 queued/队首。语义坏日志可重建出“已 admitted 无 state”“已 assigned 仍 queued”或跳过更早 queued run 的投影 | 将 CommitEnvelope 的物理原子性误当成记录组合的语义原子校验；producer 顺序与 reducer 可接受语言不相等 | admit/assign 生产端、run reducer、submission guard reducer、queuedPosition heap/active index、pending outbox、重启与格式迁移、初始半边/非队首对抗测试；未来 governor reserve 仍属单元18，不在本项静默扩面 | admitted 分支反向要求同 envelope 初始 queued(revision=1,无 assignment)；assigned 分支要求当前 queued、队首及同 envelope dispatched(next revision,同 assignment)；state 分支反向要求 queued→dispatched 的本 envelope assigned；submission guard 对 assigned/dispatched 执行可在内联事实完成的同一闭包守卫。补 admitted-only、assigned-only/跨 envelope、非队首 assigned+dispatched 反例，保留合法原子路径 | 已验证 |
| U13-40 | 合同分叉族(U13-20、U13-22、U13-23、U13-37~39)四轮挤牙膏的根因:在线生产端、完整 reducer、轻量 guard 各自实现同一合同,发现实例后未枚举全部执行点,修复一直是补第二份检查而非合并谓词 | 违反 spec 既有纪律(matchManifest / validateDispatchBinding 双端共用同一实现)——同一合同的多个执行点未谓词单源化 | owner journal 全部记录类型的在线入口 / 完整 reducer / 轻量 guard / 恢复消费者四类执行点;executor reducer 与协议 validator 分工;全部对抗测试 | 终局口径 = **合同谓词单源、投影适配分层、全执行点机械对账**:①结构/字段封闭/域绑定谓词全执行点共用;②前态与伴随事实谓词按数据可得性组合,状态机语义保留函数形态,声明化仅用于伴随事实与身份绑定;③各执行点只做数据闭包适配,禁止内联重写合同;④“记录类型 × 在线生产端 × 完整 reducer × 轻量 guard × 恢复消费者 × 对抗测试”六维对账由代码注册表或参数化测试驱动,不以 Markdown 承载;每格必须落“实现与证据”或“有事实依据的不适用”,仅适用格生成正反例。U13-37~39 保留各自独立验收;本行验收 = 六维对账每格闭合(实现/不适用二选一、零待核查)+ 分叉族对抗用例全绿(规则权威定义见 `research/design/principles/single-source-contract-predicates.md`) | 已验证 |
| U13-41 | 冻结终审一逐格复核发现：轻量 guard 虽调用共享谓词，但投影适配缺少决定同一谓词所需事实。`acked-original` conflict 未记“已见 conflict”，可接受第二条；`assignment-superseded` 未验 durable-started、proofKind 对应 fence/dispatch 与原子 closure，却会删除当前映射；同族上，assigned 内联记录缺 ownerEpoch/baseRevision/dispatchDigest，guard 无 ArtifactStore 时无法机械复算 conflict activation、DispatchRejection 与 committed exact 的完整历史栅栏 | U13-40 只统一了谓词函数，未把“投影适配的数据闭包”作为共享合同输入闭包验收；矩阵证明了执行点存在，却未证明每个 implemented 格提供了完整事实 | assigned 生产/spec/run+job 冻结联合；dispatch-acked/conflict/supersede-requested/supersede-started/cancel-fence/cancel-proof/state/resolution/committed 的 full reducer 与 guard；cold bundle stable/exact、A1→A2、started 禁重派、响应丢失恢复、S4/S5 继承与对抗测试 | assigned 耐久快照增加 `ownerEpoch/baseRevision/dispatchDigest`（均由已验 DispatchEnvelope 派生），full reducer 反绑 artifact，guard 以此零 ArtifactStore 重建 activation/dispatch/commit fence；将 dispatch ack/conflict handling、supersede request/started observation、cancel fence、accepted cancel proof、assignment supersede closure 的授权相关谓词单源化，guard 维护 ack/conflict/fence/durable-started/accepted proof 紧凑投影；历史 bundle fence 同时绑定 executor/ownerEpoch/baseRevision/open-conflict。补重复 acked conflict、无 fence/started 后 supersede、错 assigned base 的 cold exact 反例；矩阵对应格必须引用数据完备谓词 | 已验证 |
| U13-42 | 冻结终审一（指纹 `c8b64a37…`）发现:`acceptSupersedeProof` 在线预路由在事务外内联重写 fence↔request 绑定比较,是 `terminationProofBindsDurableSource` 合同的第四处实现;两处判定可各自演进 | U13-40 纪律"各执行点只做数据闭包适配,禁止内联重写合同"未覆盖到入口预检层——预检把路由判断与合同判定混在一起 | acceptSupersedeProof 入口、#acceptNotStartedTermination 与 already-started 事务内谓词、响应丢失重试、绑定失败错误口径、supersede 对抗测试 | 删除预路由的绑定比较（合并谓词而非替换重复）:入口只按已验签 decision 路由,绑定唯一由各事务内共享谓词判定;错误口径统一为谓词稳定消息;复跑 supersede 全场景 | 已验证 |
| U13-43 | 执行者自审（spec 声明面 × 实现生效面逐段对照）发现:§3.6 第 780 段 submission-guard 投影消费清单停留在 U13-34 时代的 4 项事实,未随 U13-41 扩展同步;§4.3 已同步而 §3.6 漏改 | 修复完备性自检第④步只 grep 了代码消费者,未扫 spec 中引用 guard 投影的声明段落——声明面与生效面不一致 | spec §3.6 消费清单一句;S4 正式 guard 与 S5 mesh 消费者按 spec 实现的正确性;§3.6 其余四分类/mirror 段落（核实为措辞兼容,不动）;§4.3 记录注释(已同步,不动) | 按实现更新 §3.6 清单:完整 assigned 快照(权威域三元组+全字段)及提交身份、ack、conflict 已见/打开两态、supersede request/started observation、cancel fence、accepted proof、resolution、durable-started 单调并集、revocation、committed 紧凑绑定,并声明授权记录执行与完整 reducer 同一份共享谓词;功能零改动 | 已验证 |
| U13-44 | 架构者独立终审发现:resolution 的轻量 guard 缺 ownerEpoch 权威绑定——full reducer 以内联检查验 `subject.ownerEpoch === assigned ownerEpoch` 并复算 expectedOpen digest,guard 无同款,共享谓词 `assertConversationResolutionBinding` 只绑 conversation/runId;摘要自洽但 epoch 错误的 resolution 可被 guard 接受并关闭当前映射,随后稳定拒绝合法提交,而 full 判同一日志损坏。当轮枚举同族内联残留:①队首合同 full/guard 双内联(`queuedPositionHeap[0]` 比较);②committed 的 `commitRevision===assignedBase+1` full 内联/guard 缺;③full state case 的 hasAtomicResolutionClose 用 entries.some 与 guard 的 envelopeHasRunRecord 同构不同源;④连带暴露既有矛盾:`validateResolutionFact` job 分支 keys 用 `ownerEpoch` 而 core 类型为 `anchorEpoch`,合法 job fact 会被结构层误拒 | full 的权威绑定检查本身是谓词化时的内联残留,guard 依赖谓词故天然缺失;修复完备性走查只对照了"谓词调用两端",未把谓词调用之前的内联前置块列入对照面 | resolution 的 full/guard/在线打开三执行点;assigned 队首、committed revision 绑定的 full/guard;validateResolutionFact 的 run/job 两分支;S4/S5 继承;矩阵与对抗测试 | ①ownerEpoch 绑定并入 `assertConversationResolutionBinding`(subjectOwnerEpoch/authorityOwnerEpoch,undefined 交由 assignmentExists 拒),full 删内联与 expectedOpen 复算(结构层 digest 自洽+四字段权威绑定数学等价承载),guard 补喂 `record.ownerEpoch`;②队首并入 `assertAssignmentReplayContract`(isEarliestQueuedRun),在线 805 属 producer 决策层保留;③revision 绑定并入 `assertCommittedReplayContract`(commitRevisionMatchesAssignedBase),guard 用 record.baseRevision 喂;④full state case 换 envelopeHasRunRecord;⑤job 分支改 anchorEpoch 对齐类型与 spec 同构。补 wrong-ownerEpoch 谓词反例+行为反例(full+cold guard 双执行点)、非队首/错 base 谓词反例 | 已验证 |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| E1 | U13-01~03 修复后三包类型闭合 | `pnpm --filter <pkg> exec tsc --noEmit`(core / owner-kernel / executor) | 当时 7 交付文件 + 各包源 | 直接 / 低 / ~70s | 三包零错 | 后续交付物已变化 | 失效 |
| E2 | U13-01 修复后行为回归与失败归因 | `pnpm --filter @zhixing/executor exec vitest run src/__tests__/assignment-ledger.test.ts` | 同上 | 直接 / 中 / ~57s | 67 用例:resolution 全路径及其余逻辑用例全过;两个 IO 重用例负载下超时、隔离重跑通过(U13-10 的直接证据) | 修复期证据,冻结时重算 | 诊断 |
| E3 | 集中修复后类型与可消费构建闭合 | core / owner-kernel / executor `tsc --noEmit`;core/owner 构建按依赖顺序 | 旧 9 文件交付 + 三包源 | 直接 / 中 / 分段执行 | 三包类型零错;core/owner build 成功 | 旧指纹 `sha256:2dd111609865e480f437e1853f62932b100955e9c0a5dc1065b7209af654e3e1` | 失效 |
| E4 | §6.1 全路径、竞态、崩溃重放与既有提交回归 | `pnpm --filter @zhixing/executor exec vitest run src/__tests__/assignment-ledger.test.ts` | 旧 9 文件交付 + core/owner/executor 运行时代码 | 直接 / 中 / 66.25s | 83/83 通过 | 旧指纹 `sha256:2dd111609865e480f437e1853f62932b100955e9c0a5dc1065b7209af654e3e1` | 失效 |
| E5 | control-admission 准入、回放与独立 cursor 语义回归 | `pnpm --filter @zhixing/owner-kernel exec vitest run src/__tests__/control-admission.test.ts` | 旧 control-admission + core authority | 直接 / 低 / 7.99s | 10/10 通过 | 旧输入指纹 `sha256:4771ac45142cff951781dd0da497868e285e5c94a252ef12c229fc2e177881b1` | 失效 |
| E6 | 新协议函数与 conversation control creator 的运行时导出 | `pnpm runtime:package-exports` | core/owner dist + 导出检查脚本 | 冻结准备 / 低 / 18s | 先前通过;U13-20 后 owner dist 输入变化,由 E9 最终重验 | 输入已变化 | 失效 |
| E7 | 上游包改动后的全部可消费构建产物 | `pnpm build` | 旧冻结 9 文件 + workspace 全部包源码、package manifests 与 lockfile | 最终 / 高 / 157.9s | workspace 全量构建通过 | 旧指纹 `sha256:2dd111609865e480f437e1853f62932b100955e9c0a5dc1065b7209af654e3e1` | 失效 |
| E8 | 受影响三包完整回归 | 依次执行 core / owner-kernel / executor 包级 test | E7 生成的旧 dist + 三包完整源码/测试 + 旧冻结 9 文件 | 最终 / 高 / 37.13s + 5.19s + 47.85s | core 132 文件/2189 用例、owner-kernel 1 文件/10 用例、executor 2 文件/88 用例全部通过 | 旧指纹 `sha256:2dd111609865e480f437e1853f62932b100955e9c0a5dc1065b7209af654e3e1` | 失效 |
| E9 | 最终构建产物的公开运行时导出与检查清单一致 | `pnpm runtime:package-exports` | E7 生成的旧 dist + 冻结导出脚本 | 最终 / 低 / 7.8s | 运行时公开导出检查通过 | 旧指纹 `sha256:2dd111609865e480f437e1853f62932b100955e9c0a5dc1065b7209af654e3e1` | 失效 |
| E10 | U13-23/U13-24 修复后的三包类型闭合 | `pnpm --filter <pkg> exec tsc --noEmit`(core / owner-kernel / executor) | 当时 10 文件交付 + 三包源 | 直接 / 低 / core 输入未变沿用既有证据;owner/executor 10.1s | 三包类型零错 | `sha256:f0bcc69e9475a316cc7533e361ee8e77a9fc519d1f789c5527cd28ab5db13d67` | 失效 |
| E11 | 共享账本状态机、深验、全部第 13 单元路径与增量索引回归 | `pnpm exec vitest run src/__tests__/assignment-ledger.test.ts`(executor 包目录) | 当时 10 文件交付 + core/owner dist + executor 源 | 直接 / 中 / 57.53s | 88/88 通过 | `sha256:f0bcc69e9475a316cc7533e361ee8e77a9fc519d1f789c5527cd28ab5db13d67` | 失效 |
| E12 | control-admission 共享 ingress 校验与回放回归 | `pnpm exec vitest run src/__tests__/control-admission.test.ts`(owner-kernel 包目录) | 当时 core protocol + owner control-admission | 直接 / 低 / 6.23s | 10/10 通过 | 旧输入闭包 | 失效 |
| E13 | 当时完整交付的可消费构建产物 | `pnpm build` | 当时 10 文件交付 + workspace 全部包源码、package manifests 与 lockfile | 直接 / 高 / 155.6s | workspace 17 个包全量构建通过 | `sha256:f0bcc69e9475a316cc7533e361ee8e77a9fc519d1f789c5527cd28ab5db13d67` | 失效 |
| E14 | 当时最终构建产物的公开运行时导出一致性 | `pnpm runtime:package-exports` | E13 构建输出 + 当时导出脚本 | 直接 / 低 / 7.9s | 运行时公开导出检查通过 | 旧构建输出 | 失效 |
| E15 | 独立终审后受影响三包完整回归 | E27 成功后依次执行 `pnpm --filter @zhixing/core test`、`pnpm --filter @zhixing/owner-kernel test`、`pnpm --filter @zhixing/executor test` | E27 生成的 dist + 三包完整源码/测试 + 冻结 15 文件 | 最终 / 高 / 52.27s + 4.63s + 86.37s | core 2189 用例、owner-kernel 59 用例、executor 122 用例全部通过 | 冻结指纹 `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 有效 |
| E16 | U13-25/U13-26 根治后的类型、格式与协议边界闭合 | core / owner-kernel / executor 各包 TypeScript 5.9 `tsc --noEmit`;`pnpm biome check` 直接相关文件 | 当时 11 文件交付 + 三包源码 | 直接 / 低 / 分段执行 | 三包类型零错;Biome 7 文件零问题;根目录旧 TS 4.3 误调用已归因且未作为证据 | `sha256:3f787d5e087505bc460bf6fa4f6d57d728d074fe5078abd2159656f002b08955` | 失效 |
| E17 | 三类 not-started、独立恢复源、conflict 原子收束、深层 schema 及全部既有第 13 单元回归 | `pnpm --filter @zhixing/executor exec vitest run src/__tests__/assignment-ledger.test.ts` | 当时 11 文件交付 + E18 前按依赖构建的 core/owner dist + executor 源 | 直接 / 中 / 54.60s | 93/93 通过;两个既有 IO 重用例 1.95s/1.61s,无随机超时 | `sha256:3f787d5e087505bc460bf6fa4f6d57d728d074fe5078abd2159656f002b08955` | 失效 |
| E18 | 当时完整交付可消费构建与公开导出 | `pnpm build` 后 `pnpm runtime:package-exports` | 当时 11 文件交付 + workspace 全部包源码/manifests/lockfile | 直接 / 高 / 142.2s + 7.1s | workspace 17 包全量构建成功;运行时导出门通过 | `sha256:3f787d5e087505bc460bf6fa4f6d57d728d074fe5078abd2159656f002b08955` | 失效 |
| E19 | U13-27/U13-28 根治后的类型、格式与跨包合同闭合 | core / owner-kernel / executor 各包 TypeScript 5.9 `tsc --noEmit`;`pnpm biome check` 三个直接代码/测试文件 | 当时 11 文件交付 + 三包源码 | 直接 / 低 / 17.0s/14.4s/12.6s + 6.9s | 三包类型零错;Biome 3 文件零问题 | `sha256:c5e06f2afee7c961dc43eaa0a96dfe7f3bfd855301c4a3754043d8dfae8f867e` | 失效 |
| E20 | 栅栏/终态负向重放、双取消源 conflict first-terminal-wins 与全部既有 assignment 场景 | `pnpm --filter @zhixing/executor exec vitest run src/__tests__/assignment-ledger.test.ts`;失败归因后只定向复验两个夹具与两个新增重启竞态 | 当时 11 文件交付 + 按依赖重建的 core/owner dist + executor 源 | 直接 / 中 / 60.53s + 0.93s + 1.66s | 首轮 100 个场景中 98 通过;两个失败分别为测试 AuthorityError 缺必填 message、旧错误文案断言,非实现失败;修正后 2/2 定向通过;新增 abort-ticket conflict 两 decision 加重启断言 2/2 通过 | `sha256:c5e06f2afee7c961dc43eaa0a96dfe7f3bfd855301c4a3754043d8dfae8f867e` | 失效 |
| E21 | 当时完整交付可消费构建与公开导出 | `pnpm build` 后 `pnpm runtime:package-exports` | 当时 11 文件交付 + workspace 全部包源码/manifests/lockfile | 直接 / 高 / 144.2s + 7.6s | workspace 17 包全量构建成功;运行时导出门通过 | `sha256:c5e06f2afee7c961dc43eaa0a96dfe7f3bfd855301c4a3754043d8dfae8f867e` | 失效 |
| E22 | U13-29/U13-30 修复后的类型、格式与跨包合同闭合 | core / owner-kernel / executor 各包 `tsc --noEmit`;`pnpm biome check` 四个直接代码/测试文件 | 旧 11 文件交付 + 三包源码 | 直接 / 低 / 16.9s/14.1s/11.7s + 3.6s（最终增量复验 owner/executor 12.4s/10.4s） | 三包类型零错;Biome 零问题;U13-31~33 后输入变化 | `sha256:2133065e27faf6a46ed6822a46e7790f76081a1907bf840347b56ed63bc83d66` | 失效 |
| E23 | attempt 代际、吊销后收束/重放、终态 interaction 闭合及全部既有 assignment 场景 | `pnpm --filter @zhixing/executor test -- src/__tests__/assignment-ledger.test.ts` | 旧 11 文件交付 + 当时 core/owner dist + executor 源 | 直接 / 中 / 81.03s 测试、89.13s 总耗时 | 104/104 通过;U13-31~33 后行为输入变化 | `sha256:2133065e27faf6a46ed6822a46e7790f76081a1907bf840347b56ed63bc83d66` | 失效 |
| E24 | 旧完整交付的可消费构建与公开运行时导出 | `pnpm build` 后 `pnpm runtime:package-exports` | 旧 11 文件交付 + workspace 全部包源码/manifests/lockfile | 直接 / 高 / 163.5s + 25.9s | workspace 17 包全量构建成功;U13-31~33 后输入变化 | `sha256:2133065e27faf6a46ed6822a46e7790f76081a1907bf840347b56ed63bc83d66` | 失效 |
| E25 | 第 5 轮只读盘点对 U13-31~33 的机械事实链与同类边界闭合 | 静态读取 owner/executor/core 生产端、reducer、adapter、spec §3.6/§3.7/§4.3/§6.1 及 104 场景名；核对全部 revocation site、submission 入口与 durable request 投影 | 当前 11 文件交付，不执行测试、不修改业务文件 | 问题盘点 / 低 / 只读 | 确认 conflict×interaction 互锁、proof/batch 请求身份丢失、四入口 base-auth 缺失；未发现第四个独立根因，同族风险已并入三项影响面 | `sha256:2133065e27faf6a46ed6822a46e7790f76081a1907bf840347b56ed63bc83d66` | 有效 |
| E26 | U13-31~33 集中修复后的类型、格式、可消费依赖与直接行为闭合 | 依次执行 core / owner-kernel / executor `tsc --noEmit`;Biome 检查直接代码/测试文件;按依赖构建 core→owner;最后只运行 assignment-ledger 定向文件 | 当时 13 文件 + 三包源码 + core/owner dist;不含工作台 | 直接 / 中 / 分段执行;最终行为 77.24s | 三包类型零错、Biome 零问题、core/owner 构建成功;最终同一文件 109/109 通过;U13-34~36 修复使输入变化 | `sha256:dd96046ce0e9a06258d659f0f282a240b5651c877d35c54b35161567eed9d05c` | 失效 |
| E27 | 冻结交付物的 workspace 全部可消费构建产物 | 两轮终审零新增后先执行 `pnpm build` | 冻结 15 文件 + workspace 全部包源码、package manifests 与 lockfile | 最终 / 高 / ~150s | workspace 17 包全量构建通过 | 冻结指纹 `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 有效 |
| E28 | 最终构建产物的公开运行时导出与冻结检查清单一致 | E27、E15 成功后执行 `pnpm runtime:package-exports` | E27 生成的 core/owner 等 dist + 冻结 `scripts/check-runtime-package-exports.mjs` | 最终 / 低 / ~8s | 运行时公开导出检查通过;验证后复算交付物指纹与冻结值一致（`009752e6…`） | 冻结指纹 `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 有效 |
| E29 | U13-34~36 集中修复后的类型、格式、可消费依赖与直接行为闭包 | 依次执行 core / owner-kernel / executor `tsc --noEmit`；Biome 检查 values/ledger/owner/test；按依赖构建 core→owner；最后仅运行 `assignment-ledger.test.ts` | 冻结 13 文件交付 + 三包源码 + 本行生成的 core/owner dist；不含工作台 | 直接 / 中 / core build 70s、owner 最终 build 15s、最终行为 84.39s | 三包类型零错、Biome 零问题、core/owner 构建成功；首轮 110/111 精确暴露 guard active revocation 漏接，统一原语后隔离 1/1、最终整文件 111/111 通过 | `sha256:25eebebfb795eb2b1b7b2b6df934064cbadac26f536f3f94e63d40700bd2be16` | 有效 |
| E30 | 最终独立终审对原冻结交付物重新构造权威身份、完整 CAS 与初始原子闭包；现有测试缺对应正交反例 | 静态对照 spec §4.1/§5.4/§6.1 与 owner 在线 producer、完整 reducer、submission guard、恢复 outbox；复核历史 ownerEpoch 重放测试，排除把合法旧 epoch 当坏日志 | 原冻结 13 文件，不运行测试、不修改交付物 | 问题裁决 / 低 / 只读 | 确认 U13-37~39；在线路径正确，权威重放语言和 cold guard 缺失三类约束；总纲与 spec 合同本身无需修改 | `sha256:25eebebfb795eb2b1b7b2b6df934064cbadac26f536f3f94e63d40700bd2be16` | 有效 |
| E31 | U13-37~40 集中修复后的跨 conversation/队首/原子半边、历史 commit fence 与合同谓词六维闭包 | owner/executor `tsc --noEmit`；4 直接文件 Biome；owner build；owner `conversation-run-contracts.test.ts`；executor `assignment-ledger.test.ts`，失败归因后仅复验 5 条文案断言 | 当前 15 文件交付 + owner/executor 源 + owner dist；不含工作台/principles | 直接 / 中 / owner 矩阵 2.6s，assignment 首轮 84.08s，定向复验 8.09s+10.69s+4.82s | owner 34/34；assignment 首轮 108/113 行为通过，5 条均为共享谓词替换后的旧错误文案失配；更新断言后对应 5/5 定向通过；类型、Biome、owner build 全过 | `sha256:917d83d329c2614d4395f32386f8efbb52b44ed88470fdec05e7a500299b9170` | 失效 |
| E32 | U13-41 集中修复（assigned 权威快照三字段+生产端/reducer 反绑、授权谓词全执行点单源接线、guard 紧凑投影与零 ArtifactStore fence、spec run/job 同构回填）后的类型、格式、依赖与行为闭合 | 按依赖序 core build→owner `tsc --noEmit`→owner build→executor `tsc --noEmit`；`pnpm exec biome check` 4 直接文件；owner `conversation-run-contracts.test.ts`；executor `assignment-ledger.test.ts` 整文件；owner `control-admission.test.ts` | 当前 15 文件交付 + 三包源码 + 本行按依赖构建的 core/owner dist；不含工作台/principles | 直接 / 中 / owner 矩阵 2.45s、assignment 首轮 68.41s、整文件复跑 77.02s、control 3.68s | 三包 tsc 零错、core/owner build 成功、Biome 4 文件零问题；owner 矩阵 46/46（guard 列语义化引用数据完备谓词,含 termination 绑定四分支与重复 conflict/started 后 supersede/错 base fence 谓词反例）；assignment 首轮 110/113,3 条失败均为共享谓词稳定文案/夹具缺新字段,修正并新增 3 个行为反例（错 epoch capability cold exact、已 ack 后第二条 conflict 双执行点拒、started/无 request 后 supersede 注入拒）后整文件 116/116；control 10/10 | `sha256:c8b64a3727db503e99594eb34153cc14cd1f2f959ba7359addde034c2790160f` | 失效 |
| E33 | U13-42（预路由合同分叉删除）修复后的类型、依赖与 supersede 全场景行为闭合 | owner `tsc --noEmit`→owner build；`pnpm exec biome check` 2 再改动文件；executor `assignment-ledger.test.ts` 整文件 | 当前 15 文件交付 + 三包源码 + 本行重建的 owner dist（core dist 沿用 E32） | 直接 / 中 / 整文件 68.70s | owner 类型零错、build 成功、Biome 2 文件零问题；整文件 116/116（含错 fence 重签 proof 改由事务内谓词统一拒绝的口径更新）。E32 中 owner 矩阵 46/46 与 control 10/10 的输入闭包（conversation-run-contracts.ts 及其测试、control-admission.ts 及其测试）未被 U13-42 触及,该两项结论按输入闭包继续有效 | `sha256:80747461012ccd1c83851ebf679bb07ff138a1795d3bd46939a50d3df30a5006` | 有效 |
| E34 | U13-43（spec §3.6 消费清单同步）为纯文档改动;证明代码交付未变、既有最终验证仍有效 | 指纹 manifest 逐行对比:15 行中仅 specification.md 一行哈希变化,14 个代码文件哈希与 `80747461…` 代完全一致;§3.6 新句逐项对照 guard 实现字段 | 冻结 15 文件(仅 spec 变化) | 冻结准备 / 低 / 只读+指纹复算 | 仅 spec 行哈希变化;E15/E27/E28/E33 的真实计算输入(包源码、manifests、lockfile、dist)不含 spec,按输入闭包保持有效;E32 子项继承说明不受影响 | `sha256:5c1e7b404ec06b286307ea16450e2e497e008fd5b17098a3a535c876c6057d39` | 有效 |
| E35 | U13-44 集中修复（resolution ownerEpoch 权威绑定谓词化、队首/commitRevision 合同下沉、job 分支 anchorEpoch 对齐）后的类型、依赖与行为闭合 | owner `tsc --noEmit`→owner build→executor `tsc --noEmit`；`pnpm exec biome check` 4 改动文件；owner `conversation-run-contracts.test.ts`；executor `assignment-ledger.test.ts` 整文件；owner `control-admission.test.ts` | 当前 15 文件交付 + 三包源码 + 本行重建的 owner dist（core dist 沿用 E32） | 直接 / 中 / 矩阵 2.58s、整文件首轮 101s+复跑 82.91s、control 4.26s | owner/executor tsc 零错、owner build 成功、Biome 4 文件零问题;矩阵 49/49（新增 wrong-ownerEpoch/非队首/错 base 三对谓词正反例）;整文件首轮 116/117,唯一失败为队首合同进谓词后的稳定文案迁移,更新断言并新增 wrong-ownerEpoch 行为反例（full+cold guard 双执行点）后 117/117;control 10/10 | `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 有效 |

## 终审记录

| 轮次 | 审查侧重 | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论 |
| ---- | -------- | ------------ | -------- | ------------ | ---- |
| 第一轮 | 需求、架构、功能闭环、状态、回归 | 完整矩阵:U13-44 修复等价链逐项复核（expectedOpen 删除的数学等价、反绑传递、assignedById 键不变量）、同族内联残留复扫（mirror 单执行点内联判不适用）、job anchorEpoch 与 spec §四 ExecutionRef/UncertainResolutionFact 一手对照（历史:`917d83d3…` 出 U13-41、`c8b64a37…` 出 U13-42、`80747461…` 出 U13-43、`5c1e7b40…` 出 U13-44） | 零新增 | `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 通过 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 完整矩阵:错误 epoch 攻击面四执行点 fail-closed、undefined 权威参数无旁路、close 注入入口被 open 反例封死、纯判定改动无事务边界/写路径变化、删除冗余 digest 复算的资源收益、guard/full 新参数两端同源 | 零新增 | `sha256:009752e69d197a6dadfcc7ff98ef10718592b091555fc25a19cb3b5bea8e27ce` | 通过 |

<!-- registration-complete: unit-13.gen-1 -->
