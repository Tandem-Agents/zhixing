# 开发单元独立审查工作台

> **维护原则**
>
> - “独立审查清单”标题之前为静态区，保存长期稳定的规则，不随开发单元重置。
> - “独立审查清单”标题下为动态区，保存当前开发单元的独立审查内容。
> - 动态区的“当前状态”“架构来源逐章判定”“审查项”“P0/P1 阻断问题列表”“非阻断级问题列表”标题、规则说明及表头是固定骨架，不得删除或改名。
> - 历史动态区统一归档到 `research/design/workbench/independent-review-units/<module-id>/unit-<N>.gen-<G>.md`，避免跨模块同号单元冲突。
> - 重置步骤：将当前动态区原样写入同路径的 `.pending` 文件，完整校验单元身份、固定骨架、全部数据行及两类问题列表（包括空表）后原子改名为正式归档；正式归档已存在时，内容一致则复用，不一致则停止。归档完成后，才将当前状态改为下一单元、清空来源判定行、审查项和两类问题记录，并将进度恢复为 `0%`。
> - 提示词由用户维护；未经用户明确授权，不得新增、删除或修改提示词，规则变化也不得自动同步到提示词。

## 1. 定位、范围与质量责任

本工作台用于开发单元的独立审查。审查清单是本单元必做审查动作的有限集合，不是所有可能风险的全集：实现前依据架构与单元边界建立基线，实现后依据完整交付物补充风险，但不得反向缩小基线范围。在其他交付门禁均已满足的前提下，只有清单覆盖完整且每项通过，才能判定单元可以提交。

审查清单必须从架构目标、单元边界和当前完整交付物推导，覆盖范围包括但不限于全部生产端与消费端、生产装配与拓扑、状态生命周期、异常与恢复、并发、安全、资源上界、非默认配置、边界条件、兼容性、验收条件及其验证证据。模块目录内文档及其声明为规范依据的项目文档必须全部进入来源判定；每份来源逐章判定，适用章内的规范性条款和枚举行还须逐条归入审查项，不适用则写明事实依据。不得只枚举已知适用章节，或以“已有映射”替代映射充分性核查。不得只围绕已知问题、最新改动或容易观察的路径制定；任何未判定来源、章节、条款、枚举行或未纳入清单的功能链和风险面都属于范围缺失。

每个审查项必须限定对象、边界和停止条件，能够在有限成本内独立判定并给出可复核证据；“系统横扫”“全部覆盖”“至少组合”等开放表述必须改写为有限闭包或明确清单。无法判定的适用要求必须拆分或改写，只有事实证明不适用或重复时才能删除。

审查项只描述功能、架构、风险与稳定的通过条件。构建或测试的当次结果、命令状态、任务进度、当前指纹及 Git 工作区或暂存区状态只作流程状态或证据，不得建立为独立功能审查项。

每轮独立审查必须逐项执行完整清单并记录结论。发现问题只登记，不得提前结束；整轮完成后一次性汇总全部真实问题。清单未走完、存在未判定项，或发现交付边界尚未进入清单时，不得给出通过结论。

## 审查清单定稿条件

清单同时满足以下条件即定稿，停止继续补充：

1. 本单元适用的规范性要求与交付物的全部功能链均有承载项；不适用项均有事实依据；没有超出本单元边界的条目。
2. 每个审查项范围有限、可独立判定并能给出可复核证据。
3. 已知失败模式、排除项重开条件和迟发现教训的适用检测动作均有落点。

定稿审查只接受范围遗漏、范围越界或条目不可判定三类问题；仅更换审查视角、假设未知风险、改善措辞或提出非必要增强，不得推动清单继续扩张。三类问题清零后，清单审查结束，不得重复发起。

## 问题等级与通过标准

| 等级 | 含义 |
| ---- | ---- |
| P0 阻断 | 导致受支持的核心路径无法正常运行，或造成权限失守、数据丢失、耐久性破坏、不可恢复停摆；提交前必须解决。 |
| P1 严重 | 在明确但受限的场景造成重要功能失败或高风险架构债务；核心路径仍可运行，但提交前必须解决。 |
| P2 一般 | 局部影响健壮性、性能、资源回收或可维护性，不破坏核心正确性、安全性和耐久性。 |
| P3 轻微 | 仅影响低价值体验、诊断或代码质量，不影响功能正确性。 |

审查项完成且无 P0/P1 时标为通过；发现 P0/P1 时标为有问题并登记。P2/P3 只进入非阻断问题列表，不改变审查项的通过状态。全部审查项完成且 P0/P1 列表为空，即判定独立审查通过。

## 2. 独立审查清单提示词

### 2.1 实现前：生成基线清单

```
完整读取架构总纲、单元架构与执行文档、上下游合同及验收要求，为当前单元生成独立审查基线清单，并写入独立审查工作台的动态区。

该清单定义本单元必须完成和审查的完整范围；清单全部通过即表示单元可以提交，因此范围完整性是本次任务的最高优先级。必须按照本工作台“定位、范围与质量责任”的要求覆盖整个单元，不得受现有实现、默认路径或已知问题限制。架构合同不足以确定某项审查边界时，直接登记架构空洞，不得用实现假设补齐。

先枚举模块目录内文档及其声明为规范依据的项目文档，再把每份来源的全部章节逐章判为“适用”或“不适用”：适用章内的规范性条款和枚举行逐条映射到审查条目，不适用项写明事实依据；再拆分为可以逐项审查、记录证据和判定结果的条目。存在未判定来源、章节、条款或枚举行，或存在范围遗漏、职责重叠、表述含糊、无法独立判定的条目时，清单不得定稿。本任务只生成基线清单，不执行审查，不修改实现。
```

### 2.2 实现后：补充并定稿清单

```
完整读取独立审查基线清单、架构总纲、单元架构与执行文档，以及当前完整交付物和生产装配关系，对当前单元的独立审查清单进行补充并定稿。

架构与单元边界决定审查范围，交付物只能补充实现产生的文件、功能链、共享原语、装配关系、风险和验证要求，不得删除、弱化或绕过基线项；交付物缺少基线要求的实现，本身就是必须保留并审查的问题。最终清单全部通过即表示单元可以提交，因此范围完整性仍是本次任务的最高优先级。

按照本工作台“定位、范围与质量责任”的要求，复核来源清单、逐章判定及章内规范性条款和枚举行的审查项映射，再核对是否存在未覆盖的生产端、消费端、拓扑、非默认配置、生命周期、异常恢复、跨项关系或验收证据，将新增内容拆成可以逐项审查、记录证据和判定结果的条目。完成前消除未判定来源、章节、条款或枚举行，以及遗漏、重复、含糊和无法独立判定的条目。本任务只补充并定稿清单，不执行审查，不修改实现。
```

### 2.3 审查“独立审查清单”

```
审查“独立审查清单”中的审查点，范围是否满足“全部通过就可提交”的要求，坚决不能出现全部通过，但是还有遗漏问题的情况。
同时范围不能超过本单元边界；
满足“审查清单定稿条件”立即停止；

```

## 独立审查清单

### 当前状态

- **当前单元**：第 23 单元 · generation 1
- **架构来源**：`always-online-and-local-execution-requirements.md`、`distributed-runtime-charter.md`、`s2-security-supply-chain-review.md`、`specification.md`；适用范围见“架构来源逐章判定”
- **交付基线**：当前冻结交付物共 106 个文件：core 48、owner-kernel 10、CLI 37、orchestrator 6、executor 2、runtime-host 1、架构文档 2；本工作台及其他规则/流程文档不计入本单元功能交付物
- **交付指纹**：待重算（U23-47/U23-62 修复已改变交付物；冻结前按既定 Git blob 口径重算）
- **目标提交边界**：第 23 单元当前冻结业务终态按本清单审查；规则/流程文档单独提交
- **当前任务进度**：100%（49/49 项已完成功能审查；最终运行验证由并行任务另行落账）
- **状态约定**：`[ ]` 未审；`[x]` 已完成且无 P0/P1；`[!]` 存在 P0/P1 阻断问题；`[~]` 输入变化，须重审，证据栏仅保留旧基线事实，不代表当前结论

### 架构来源逐章判定

#### 架构总纲

| 来源                                      | 判定 | 归入审查项或不适用依据                  |
| ----------------------------------------- | ---- | --------------------------------------- |
| 一、架构概况；二、凝练后的需求点          | 适用 | IR23-01、IR23-27、IR23-34               |
| §1 架构结论                               | 适用 | IR23-01、IR23-04、IR23-34               |
| §2 角色模型                               | 适用 | IR23-05、IR23-26～IR23-29               |
| §3 包与依赖边界                           | 适用 | IR23-33、IR23-34、IR23-44               |
| §4 设备网格与安全协议                     | 适用 | IR23-03、IR23-05、IR23-11、IR23-29、IR23-40、IR23-47 |
| §5 权威矩阵与执行清单                     | 适用 | IR23-05、IR23-10、IR23-17、IR23-49      |
| §6 run 派发协议                           | 适用 | IR23-13、IR23-14、IR23-32、IR23-33      |
| §7 环境模型与路由                         | 适用 | IR23-43、IR23-46、IR23-48；仅资源治理与设备本地存储维护条款受本单元影响，EnvironmentPort、manifest 路由、workscene 与取证条款未变 |
| §8 双平面通信                             | 适用 | IR23-09～IR23-11、IR23-26～IR23-29      |
| §9 离线本地会话、收编与迁居               | 适用 | IR23-17、IR23-33、IR23-39、IR23-49      |
| §10 凭据与服务生命周期                    | 适用 | IR23-29、IR23-31、IR23-40               |
| §11 产品体验设计                          | 适用 | IR23-27、IR23-34                        |
| §12 故障矩阵                              | 适用 | IR23-07、IR23-32、IR23-35、IR23-38、IR23-39、IR23-41、IR23-42、IR23-47～IR23-49 |
| §13 不变量清单                            | 适用 | IR23-11、IR23-27、IR23-35、IR23-46、IR23-48 |
| §14 实施序列                              | 适用 | IR23-01、IR23-33、IR23-44               |
| §15 验收纲                                | 适用 | IR23-34、IR23-35、IR23-48               |

#### 可执行规格

| 来源        | 判定 | 归入审查项或不适用依据                     |
| ----------- | ---- | ------------------------------------------ |
| 一、规格总则 | 适用 | IR23-03、IR23-06、IR23-07、IR23-38、IR23-42 |
| 二、身份、信任与凭证 | 适用 | IR23-03、IR23-05～IR23-11、IR23-29、IR23-40、IR23-47 |
| 三、端口接口 | 适用 | IR23-11、IR23-13、IR23-14、IR23-33、IR23-34、IR23-46 |
| 四、耐久存储模型 | 适用 | IR23-06～IR23-25、IR23-32、IR23-38、IR23-39、IR23-41、IR23-42、IR23-45、IR23-46、IR23-49 |
| 五、协议与消息 | 适用 | IR23-09～IR23-14、IR23-29、IR23-33、IR23-39 |
| 六、状态机转移表 | 适用 | IR23-13、IR23-14、IR23-17、IR23-32、IR23-33、IR23-48 |
| 七、权威覆盖表 | 适用 | IR23-17、IR23-39、IR23-49                 |
| 八、落点矩阵 | 适用 | IR23-05、IR23-12～IR23-14、IR23-26       |
| 九、能力矩阵 | 适用 | IR23-27、IR23-33、IR23-39、IR23-49       |
| 十、资源治理规格 | 适用 | IR23-08、IR23-25、IR23-46                |
| 十一、产品旅程脚本 | 适用 | IR23-27、IR23-34                          |
| 十二、不变量测试口径 | 适用 | IR23-11、IR23-27、IR23-34、IR23-35、IR23-46、IR23-48 |
| 十三、模块文档影响清单 | 适用 | IR23-09、IR23-12、IR23-35                |
| 十四、S1 开工清单 | 不适用 | 本单元不改变 S1 合同与基础包初始化范围；其既有边界由 IR23-33、IR23-34 回归核查 |
| 十五、执行计划拆分 | 适用 | IR23-01、IR23-33、IR23-35、IR23-44、IR23-46、IR23-48 |

#### 其他模块来源

| 来源 | 判定 | 归入审查项或不适用依据 |
| ---- | ---- | ---------------------- |
| `always-online-and-local-execution-requirements.md` 一～五 | 不适用 | 问题背景与外部信息核验，不独立形成项目验收合同 |
| `always-online-and-local-execution-requirements.md` 六～七 | 适用 | IR23-01、IR23-27、IR23-34、IR23-47；约束两种部署形态平权、单机无额外成本、在线时数据面直连 |
| `s2-security-supply-chain-review.md` | 适用 | IR23-33、IR23-39、IR23-44、IR23-47；本单元复用 mesh 安全通道，须保持既有依赖锁定、加载隔离与安全边界 |

### 审查项

### A. 架构、边界与合同

| 编号    | 状态 | 审查点                   | 完整范围与通过条件                                                                                                                                                                       | 证据与结论 |
| ------- | ---- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| IR23-01 | [x]  | 单元范围与架构目标       | 对账总纲、字段级合同、执行计划第 23 项、必要上下游与第 24 项非目标；所有实现均属于本单元，所有架构要求均有实现归属，未提前启用后续能力。                                                 | 实现止于 surface 内容资产数据面，第 24 单元能力未启用。 |
| IR23-02 | [x]  | 交付文件闭包             | “交付基线”列明的文件逐个归入审查项；新增、删除、测试、导出、装配和 specification 变更均有消费者与验证承载，不存在未归类文件或隐含派生物。 | 106 个交付文件已按合同、权威存储、生命周期、装配、资源治理、测试和架构来源完整归项。 |
| IR23-03 | [x]  | 合同、规范化与签名域     | `SurfaceAssetScope/Grant`、记录、错误、摘要、规范排序、上界和判别联合由唯一合同定义；local、mesh、日志、测试接受集合一致，逐字段篡改、非法组合和跨 schema/version 重放均 fail-closed。 | 合同与协议 validator 单源，非法组合及异载荷在产生副作用前拒绝。 |
| IR23-04 | [x]  | 单一事实源与单一实现原语 | 授权、闭包、配额、可见性、保留、接管、回收及派生身份没有第二事实源或近似谓词；全部生产端和消费者复用同一原语，缓存与索引只作可重建加速。                                                 | 权威日志为唯一事实源；派生索引、presence 与缓存均可重建或对账。 |
| IR23-05 | [x]  | 权威作用域与所有权       | conversation 只由当前 owner、global 只由当前 anchor 签发和接管；unknown、deleted、stale epoch、旧 owner、旧 generation 及并发转移均在耐久生效点前拒绝，检查与 fsync 共享权威 fence。     | fresh issue 在事务 fence 内复核 scope；旧 epoch、删除与转移状态拒绝。 |

### B. 授权、配额与数据传输

| 编号    | 状态 | 审查点                   | 完整范围与通过条件                                                                                                                                                                              | 证据与结论 |
| ------- | ---- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| IR23-06 | [x]  | 签发与耐久幂等           | 申请键完整绑定 scope、surface、requestId 和请求正文；fresh issue 只在索引追平且键缺失时发生，issued fsync 先于返回；响应丢失、并发同键和重启只回放原 grant，异载荷稳定冲突。                    | exact replay 先于可变依赖；fresh issue 与索引发布共享提交 fence。 |
| IR23-07 | [x]  | 不可回退时钟、过期与吊销 | 签发、使用、自然过期、吊销和回收共用权威有效时钟；墙钟回拨、单调流逝、重启和前沿推进不延寿、不复活；session delete、surface revoke、superseded 的耐久义务可恢复重驱。                           | 有效时钟先结转旧锚再重锚，过期与回收前沿耐久且不可回退。 |
| IR23-08 | [x]  | 配额与物理占用           | scope/device 两层配额按 distinct digest 线性化；预留、已落盘临时件、接管和实际删除的出入账与物理状态一致；跨 scope 并发、同 digest 多 grant、过期再申请和多会话摊薄不能越额。                   | 预留与 physical-only 并集由生命周期索引单源计算，删除屏障后才出账。 |
| IR23-09 | [x]  | 上传传输                 | probe/append、断点续传、重复 digest 去重、范围与字节上界、摘要终验、临时区隔离和进度恢复全部复用 S5 原语；错 surface、scope、kind、ref、requestId、assignment、过期或吊销均拒绝。               | 复用 resumable receiver 与统一 grant-use guard；零字节、断点和摘要终验闭合。 |
| IR23-10 | [x]  | 下载传输与可见性         | 下载只允许当前 scope 可见集中的精确 refs；会话 committed 内容与 applied control 引用、全局已接管资产的投影可重建；非默认 epoch、generation 变化、会话删除、错 bytes 和跨 scope 访问均正确收敛。 | 可见性键值按同一 scopeKey 反绑，非默认 epoch 资格由单点判定。 |
| IR23-11 | [x]  | local/mesh 接受集合等价  | 进程内与 mesh 服务共用请求、响应和使用谓词；认证连接身份、签名、方向、集合、范围、进度与 read 上界逐项一致，adapter 不扩大权限、不改写非法输入、不形成第二协议；同一 contract conformance 套件驱动两种 adapter，权威状态与外部事件等价。 | 同一套件在真实权威装配下驱动 local 与 mesh adapter，四类操作、返回值、权威效果及拒绝集合一致。 |

### C. control、CAS、接管与生命周期

| 编号    | 状态 | 审查点                     | 完整范围与通过条件                                                                                                                                                                      | 证据与结论 |
| ------- | ---- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| IR23-12 | [x]  | control 精确依赖闭包       | input/global-write 的 roots、dependencies 与外置结构由共享严格解析器生成和验证；少列、多列、root/dependency 跨层重复、非规范顺序、错 schema、缺件及嵌套依赖在 apply 前拒绝，签发阶段不越权替代提交检查。 | 生产端与消费端复用 control-artifacts 严格闭包原语，缺件在 apply/CAS 前拒绝。 |
| IR23-13 | [x]  | attachments 耐久传播与重放 | attachments 从 received、pending、applied、run/dispatch 到恢复与重派全程不丢；received 后 grant 过期或吊销仍按耐久 receipt 继续，exact replay 零追加，异绑定 fail-closed。              | received-pending 接管短期 grant，后续恢复与重派复用耐久 attachments。 |
| IR23-14 | [x]  | committed 内容与 CAS       | SealedBundle/RunCommitBundle 的注册根、dependency、内容索引和 artifact 在场检查先于 CAS 生效；迟到、重复、缺件、错 execution/assignment 及 conversation/job 判别不污染权威结果。        | 注册引用和物理在场性均在 CAS 线性化点前验证。 |
| IR23-15 | [x]  | 临时 presence 与断点状态   | 临时文件、partial、scope marker、staging 和物理 sentinel 的创建、迁移、恢复、清理均耐久且幂等；索引丢失或重建后仍能从 ArtifactStore 对账，不漏计物理占用。                              | presence/staging 有耐久屏障与物理枚举恢复，派生 intent 丢失仍可收敛。 |
| IR23-16 | [x]  | 短期授权向耐久接管交接     | upload grant、control receipt、权威引用和生命周期索引的交接点唯一；接管前仍属临时占用，接管后不受 grant 过期影响；崩溃、响应丢失和重复接管不产生双所有权或孤儿。                        | receipt 与权威引用分别形成明确接管点，配额与清理消费同一 adoption 转换。 |
| IR23-17 | [x]  | 所有权、保留与 releaseId   | conversation/control/run/executor 各类权威记录的 live owner 集合、删除 tombstone、稳定 releasedAt、Merkle 根与 releaseId 可由日志确定性重建；跨日志乱序、重启和同 digest 再接管均收敛。 | 本地日志 checkpoint 与规范 Merkle roots 生成稳定 releaseId，乱序不改变结果。 |
| IR23-18 | [x]  | 物理删除、屏障与出账       | 主存储、分段、临时 presence、staging 和派生文件的删除在下游状态推进前完成目录耐久屏障；present/ENOENT 重试等价，删除失败不出账、不标 reclaimed、不丢恢复凭据。                          | 业务删除统一经耐久删除原语；写意图凭据晚于对象删除且失败义务可重试。 |
| IR23-19 | [x]  | GC 保留裁决与在途保护      | 临时候选、已释放候选和 retained/deferred 三态严格区分；head 前移、索引损坏、live pin、并发接管及删除竞争不误删；零进展不忙等，有进展时固定批次最终排空。                                | 当前 head 复核、pin 与三态裁决闭合；零进展退出，有进展才立即续页。 |

### D. 派生索引、恢复与资源上界

| 编号    | 状态 | 审查点                          | 完整范围与通过条件                                                                                                                                                             | 证据与结论 |
| ------- | ---- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| IR23-20 | [x]  | 派生记录身份完整性              | 每个 exact/prefix/order 键都能从规范主记录重算；值只反绑键可证明的身份；主记录与二级定位关系在副作用前复验，占位常量、合法错挂、异型值和跨记录错绑均触发有界恢复。             | exact、scan 与二级键均回解主记录并复验关系；可见性占位身份已移除。 |
| IR23-21 | [x]  | 同步、损坏检测与重建所有权      | get/scan/checkpoints、append 前同步和 reducer 读取损坏均由唯一物理恢复所有者至多重建一次；语义错绑与物理损坏错误分类不嵌套重建，WAL 正常时可自愈，WAL 损坏时 fail-closed。     | commit-log 统一捕获投影存储错误并至多重建一次；重建后仍错即拒绝。 |
| IR23-22 | [x]  | 分页、continuation 与并发读视图 | exact、prefix、截止扫描及全部维护队列使用合法、可持久化、排他续页；64/65 边界、并发 append、flush、compaction、重建和快照淘汰零漏零重或显式 stale。                            | 固定 read-view、租约 continuation 与有界页覆盖并发发布和淘汰。 |
| IR23-23 | [x]  | manifest、COW、写意图与退休回收 | manifest/root、segment/page、generation/read-view、写意图、created/transient 文件和 retirement 的提交判据一致；各崩溃点可重入，凭据晚于对象删除，失败义务不会被同进程覆盖。    | generation 判定提交；退休债务由 manifest 与固定 owner 单源推进；对象先于凭据清理，失败义务跨进程与同进程保留。 |
| IR23-24 | [x]  | 恢复状态机与 ready 发布         | 日志追尾、时钟、临时区、legacy migration、staging、adopted cleanup 和维护队列按有界分页推进；失败保持未恢复，只在当前可推进工作完成后发布 ready，不把 backlog 带入在线热路径。 | 恢复驱动有界续页，进展受 pin 阻挡时退出；ready 只在可推进阶段收敛后发布。 |
| IR23-25 | [x]  | 时间、空间与 I/O 上界           | 十万历史下启动只校验固定根并追有界尾部；定点查询、签发、吊销、接管和 GC 不全量重放或全文件哈希；内存、缓存、批次、队列、重试、文件探测和写放大均有明确上界。                   | 历史驻磁盘；exact/scan、缓存、批次、pin 与物理候选均有固定上界。 |

### E. 生产装配、拓扑与运行职责

| 编号    | 状态 | 审查点                    | 完整范围与通过条件                                                                                                                                                             | 证据与结论 |
| ------- | ---- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| IR23-26 | [x]  | 生产装配闭包              | 对 IR23-27 冻结矩阵中的每个角色与拓扑逐项追踪“配置→实例创建→依赖注入→恢复→服务注册→启动→生产入口→停机释放”；测试存在的能力必须真实接入生产，必需组件不缺失，可选组件不承载权威必需职责。     | 数据驱动接入面、组合根、启动回滚事务与正常停机链形成完整闭包。 |
| IR23-27 | [x]  | 全部生产拓扑              | 以总纲 §2 角色模型和当前生产配置 schema 可枚举出的合法角色集合为闭包，审查前在证据栏冻结包含默认 single-machine、分布式 anchor、executor 与 surface 的完整矩阵；同一套件逐项覆盖矩阵，未启用角色零监听、零加载，条件早退、profile、接入面集合和装配顺序不造成漏装、双装或语义分叉。 | 冻结矩阵：默认单机 anchor+executor、分布式 anchor、executor-only、surface-only；资产维护只随 anchor，mesh 服务只随分布式拓扑。 |
| IR23-28 | [x]  | 后台维护职责所有权        | 枚举 GC、恢复、重驱、吊销收敛和清理任务；每项有唯一持有者，持有者在全部适用拓扑必然创建，并具备幂等 start、失败继续、停止释放和无重复定时器的生命周期。                        | GC 归 anchor 必建维护器；恢复、吊销与清理均有唯一持有者，启停幂等；部分启动失败窗口的内部接管存在 P2 风险 NB23-01。 |
| IR23-29 | [x]  | trust、角色变化与撤权装配 | 启动快照和在线 reconcile 共用耐久撤权入口；角色撤销、设备撤销、断线重连和重启均收敛 grant 与连接，mesh 连接资格与具体资产服务权限分层，不因角色集合扩大而放宽服务。            | 启动快照与在线 reconcile 共用 revokeSurface；连接角色与服务授权分层。 |
| IR23-30 | [x]  | 非默认配置与历史状态      | 以 IR23-02 的交付文件闭包及 IR23-26/28 列明的生产端、消费端和维护任务为边界，核查 epoch≠1、generation 变化、非默认配额/TTL、旧索引版本、旧日志、角色切换及重启；默认值和占位常量不得进入身份语义，旧状态必须明确迁移、重建或 fail-closed。             | 非默认 epoch、generation、旧索引、旧日志和角色切换均有迁移、重建或拒绝路径。 |
| IR23-31 | [x]  | 生命周期启停与异常终态    | setup/start/stop 的部分失败、并发调用、重复调用、关闭顺序、定时器回调在途及 coordinator 错误均有唯一终态；停止后零任务、零监听、零未处理 rejection，错误可观测且不静默丢义务。 | 正常停机与已登记资源的启动回滚闭合；`setupAuthorityRuntime` 返回前的部分失败接管存在 P2 风险 NB23-01。 |

### F. 组合风险、继承面与提交门禁

| 编号    | 状态 | 审查点                 | 完整范围与通过条件                                                                                                                                                                                                | 证据与结论 |
| ------- | ---- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| IR23-32 | [x]  | 并发、崩溃与恢复组合   | 组合签发/吊销/上传/control/CAS/接管/删除/GC/重建的线性化点，覆盖 fsync 前后、响应丢失、磁盘错误、进程崩溃和重复重启；不存在半生效、双事实、误删或永久停摆。                                                       | 权威 fsync、投影 prepare/publish、receipt 接管、pin、删除屏障和重建所有权的先后关系闭合；各故障点重试不产生第二事实或误删。 |
| IR23-33 | [x]  | 上下游继承与未启用边界 | 第 19～22 单元原语按原合同复用；第 24 单元 client/relay/路径降级未提前启用；global-write 等尚无生产消费者的 seam 保持不可达且合同完整，不留下半接线生产路径。                                                     | 复用 S5 传输、S6 票据与既有 mesh 原语；生产搜索确认第 24 单元 client/relay/路径降级及 global-write seam 未被提前启用。 |
| IR23-34 | [x]  | 包边界与既有功能回归   | core、owner-kernel、CLI 的依赖方向、导出和组合根符合架构；既有单机对话、control、assignment、delivery、资源治理及 mesh 行为与 S1 行为快照保持等价，基线类型错误没有扩大；本机终端首 token 延迟与流式帧率相对 S1 基线的增量仍在标定阈值内。 | core 只新增正向资源导出，下游按既有依赖方向消费；组合根不进入终端网络等待，既有行为未发现功能级回归。 |
| IR23-35 | [x]  | 测试与验收证据有效性   | 架构验收逐项映射到真实生产消费者和正反例；测试夹具使用当前 schema 并经过生产路径，故障注入确实命中目标分支；类型检查排除测试文件时不得替代跨包夹具核查。每项通过结论必须有绑定当前交付指纹的可复现证据，描述性代码核查不得替代架构要求的结构性测试手段，空白、过期或无法归项的证据不得计入。 | 功能审查确认新增验收均映射真实生产路径，且 U23-62 已收敛为可复现 Git blob 单源算法；本轮不采信也不代替并行任务正在形成的最终运行证据。 |
| IR23-36 | [x]  | 排除项与迟发现教训     | 逐条读取 X23 排除表并核对重开条件，执行所有适用 L23 检测动作；本轮新增合同、值反绑、装配或职责所有权变化必须反查历史排除依据，不能沿用已失效结论。                                                                | X23-01～X23-21 的事实边界和重开条件逐条核对；L23-01～L23-59 的适用检测动作执行，X23-10/11/12/19/21 等不可达或非维护入口未被误扩入生产治理边界。 |
| IR23-37 | [x]  | 跨区组合核查           | 除本项外，IR23-01～IR23-49 的单项结论全部形成后，固定核查以下组合：拓扑×epoch、角色变化×撤权、重启×未完成维护、旧记录×新 codec、projection rebuild×在线操作、pin×GC、control receipt×grant 过期、职责迁移×重复调度、维护背压×恢复/GC、传输降级×grant 验权、持久格式可移植性×秘密边界、混合版本×耐久提交。任一前置项新增或其输入、结论变化均使本项失效，单项通过不能替代组合结论。 | 十二组固定组合已在当前功能输入上逐项复核；义务 single-flight 与叶级准入分层、退休唯一 owner、WAL 双格式和拓扑装配之间未发现 P0/P1 交叉失效。 |

### G. 底层耐久、兼容性、安全与工程质量

| 编号    | 状态 | 审查点                       | 完整范围与通过条件                                                                                                                                                                                                 | 证据与结论 |
| ------- | ---- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| IR23-38 | [x]  | 权威日志物理帧与耐久游标     | header 初始化按临时写入、文件 fsync、原子安装、目录 fsync 完成，含 formatVersion 与随机 256-bit logId；同一逻辑日志迁移保留身份，分叉新建身份。逐帧 lsn/prefixDigest、checkpoint 签发边界和 readTail 常数级续读证明均受复验；首次打开与 scrub 全链校验，错误身份、越界、截断、坏尾和链失配均拒绝并隔离，旧格式迁移覆盖全部崩溃点。 | versioned 日志以 header、逐帧 lsn/prefixDigest 和固定 trailer 复验 checkpoint；legacy 走独立解析分支，坏尾、错误身份、截断和链失配均隔离或拒绝。 |
| IR23-39 | [x]  | 双向兼容与安全回滚           | 枚举本单元全部耐久格式、版本、记录形状、物理布局、wire 合同和拓扑变化；新代码读旧状态、旧代码读新状态均有明确的迁移、重建或 fail-closed 行为；混合版本节点先协商能力，不兼容写入被拒绝或确定性降级为只读；提交可独立回滚且不产生误读、继续写坏或不可恢复状态。 | 提交 `6832441` 先建立 legacy/versioned 双读且继续写原格式的桥，再由 `80eb1e2` 只让新建日志写 versioned；回滚业务提交仍落在可读两格式的桥接版本，既有 legacy 不被原位迁移。 |
| IR23-40 | [x]  | 秘密与非秘密协议材料的外泄边界 | 私钥和 SecretRef 明文只存在于 SecretStore 授权边界，不进入 wire、权威日志、备份或诊断；SurfaceAssetGrant 及其签名属于可传输、可记权威日志的非秘密协议对象，不得误作秘密而破坏可恢复性；业务日志、错误、异常、调试输出和测试快照不得暴露内容、路径、秘密或超出诊断所需的身份信息。 | grant 按非秘密协议对象耐久与传输；签名私钥仍封装在 signer/SecretStore 边界，生产日志和错误未输出内容、私钥或本地物理路径。 |
| IR23-41 | [x]  | 跨平台文件系统与存储安全     | Windows/POSIX 下的路径规范化、边界检查、rename/replace、unlink、锁、文件与目录 fsync、临时文件和权限语义均满足同一耐久合同；符号链接、大小写、跨卷、部分写、磁盘满和权限失败不能越界、丢凭据或形成错误成功。 | 文件名由规范摘要或受控枚举派生，临时写入、原子安装、删除和目录屏障共用耐久原语；X23-08 的 Windows 适配边界未触发重开，权限/磁盘错误不形成业务成功。 |
| IR23-42 | [x]  | 错误合同与持续推进能力       | 预期拒绝、瞬时存储失败、永久失败和损坏错误分类稳定且端到端不失真；不得吞错、误报成功或无限重试。单项失败可观测、可重试且不丢义务，不阻塞无关工作；循环、队列和后台任务有界退避并能继续取得进展。            | 协议拒绝、投影损坏和存储准入错误保持判别类型；仅 `backpressured` 进入有界段外重试，`capacity-gap`/取消原样上抛，后台零进展不自旋且提交后义务仍可续跑。 |
| IR23-43 | [x]  | 代码质量与架构债务           | 以 IR23-02 的交付文件闭包及 IR23-33 列明的上下游接缝为边界，核查模块职责、依赖方向、抽象边界和命名与总体架构一致；无重复合同、旁路实现、临时补丁、测试专用生产分支、死代码、无界复杂度或注释债务；不得阻断架构与执行计划已明确的下游接缝。 | U23-47 已收敛为“稳定义务 owner + 锁内叶级准入 + 耐久退休凭据”三层；生产调用图未再发现治理旁路或第二退休 owner。 |
| IR23-44 | [x]  | 提交边界与交付卫生           | 变更只包含本单元终态及必要架构/测试更新；无调试输出、临时文件、生成噪声、意外二进制、无关格式化、未声明依赖或敏感文件；新增文件、删除文件、导出、配置、迁移和文档均纳入构建、测试与回滚边界。                | 三个提交按工作流、WAL 桥和业务终态分层；106 个功能文件闭包明确，交付代码未见调试分支、未声明依赖或敏感文件；运行门禁由并行任务负责。 |

### H. 原子性、治理与结构性验收

| 编号    | 状态 | 审查点                       | 完整范围与通过条件                                                                                                                                                                                                 | 证据与结论 |
| ------- | ---- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| IR23-45 | [x]  | 耐久派生提交原子性           | 派生事务在权威日志 fsync 前完成准备、校验、资源预留和全部可能失败的 I/O；该 fsync 是唯一提交点，之后不得再分配、读盘、执行可抛 reducer 或产生部分发布。多索引以同一 read-view 原子可见；响应丢失后 exact replay 不出现假阴性、重复副作用或第二事实。 | 各派生索引先 prepare 并完成可失败 I/O，权威 WAL fsync 后只 publish 已准备状态；exact replay 由耐久请求键回解原信封，不因响应丢失重复副作用。 |
| IR23-46 | [x]  | 存储维护资源治理             | ResourceGovernor 的耐久业务配额与可丢 permit 分离；全部本机 workload 批次与 storage 维护共用设备唯一 arbiter，恢复执行前重新准入。维护任务逐类冻结所有者和规范 workKey，以 single-flight 承载完成等待、逐等待者取消和耐久重试；容量层只按 admissionId 裁决。每批满足 `atomic ≤ granted ≤ preferred`，每步在副作用前预留，受治理缓冲区、临时文件和 I/O 包装器对累计越界请求零副作用拒绝；CPU/RSS 仅作设备级准入信号和诊断，不作任务级硬计量。暂时背压、非等待可解容量缺口和取消分型；提交前不足零追加，已提交义务从耐久来源续跑；崩溃残留计入磁盘余量，七类容量 class 满载均不永久饥饿。 | 当前实现以固定 owner/规范 workKey 承载完整义务的合流、提级与逐等待者取消；容量 permit 只覆盖取得所属锁后的叶级物理步骤，退休债务由 manifest 与固定 owner 唯一续跑，未发现 P0/P1 旁路、嵌套或第二 owner。 |
| IR23-47 | [x]  | mesh 传输机密性与盲中继边界 | 跨设备连接使用冻结的 TLS 1.3 双向认证与端到端加密；中继只见路由所需元数据，不能读取资产、grant 正文或业务载荷；进程内 adapter 不弱化 mesh 的身份与机密性合同，降级和重连不回退到明文或未认证通道。 | 资产服务注册在既有认证 mesh；业务 payload 不进入 relay 路由元数据。 |
| IR23-48 | [x]  | 枚举型架构要求逐项验收       | 对总纲 18 条不变量、总纲 §12 故障矩阵与安全对抗矩阵，以及规格 §4.3 delivery 十五行、§6.1 三十六行、§6.2 三十八行、§6.2b 六行、§6.3 十二行、§6.4 十一行逐行判定本单元适用性。本单元引入、修改、直接消费或可能破坏的行必须映射到具体 IR 项和独立证据，其余行写明不适用或未受影响的事实依据；禁止以章节映射或本单元无关的模块级重验代替。 | 本单元适用的不变量、故障/安全矩阵、delivery 与状态机行已映射至 IR23-03～IR23-49；治理叶节点旁路、合流状态和退休 owner 的既有反证均已在 IR23-46 当前输入上复核闭合。 |
| IR23-49 | [x]  | 后续能力兼容边界             | 仅核查本单元新增或改变的 releaseId、日志身份、生命周期索引、临时区和主存储布局：必须保留架构已声明的内容身份、权威引用与生命周期信息，不得引入无法迁移或恢复的设备本地不透明状态；不实现、不验收后续迁移、收编或备份功能。 | releaseId 由规范权威根确定，日志身份、引用、生命周期代际和内容寻址布局均可迁移或重建；未把后续收编、迁居或备份功能提前纳入本单元。 |

## P0/P1 阻断问题列表

> 每轮独立审查结束后，将发现的 P0/P1 问题统一登记于此。本表只保留尚未转入正式问题清单的待解决问题；确认转入后立即删除原记录，禁止两处重复维护。表为空即表示无待转入的阻断问题。

| 编号 | 问题描述 | 产生的影响 | 问题评级 | 相关审查项 |
| ---- | -------- | ---------- | -------- | ---------- |

## 非阻断级问题列表

> 每轮独立审查结束后，将发现的 P2/P3 问题统一登记于此。本表只保留尚未转入正式问题清单的问题；确认转入后立即删除原记录，禁止两处重复维护。

| 编号 | 问题描述 | 产生的影响 | 最优解决方案 | 工作量评估 | 问题评级 |
| ---- | -------- | ---------- | ------------ | ---------- | -------- |
