# 单元开发工作台

> **维护原则**
>
> - 以独占一行的“开发清单”标题为界：此前为静态区，保存长期规则与用户提示词，不随开发单元重置；该标题及其后为动态区，只保存当前单元的开发清单。分隔线仅用于视觉区隔，不承担区域边界语义。
> - 动态区只固定“当前单元”“开发事项”标题、状态说明及空表，不建立独立文档、不归档。
> - 重置步骤：仅当全部开发事项均为 `[x]` 且用户明确要求结束或切换单元时，才将模块、单元、来源、边界和排除项恢复为 `—`，清空全部开发事项并将进度恢复为 `0%`；存在 `[ ]` 或 `[!]` 时禁止重置，历史摘要不构成重置授权。只重置动态区，不得删除固定骨架或改动静态区。
> - 下一单元的来源、边界、开发事项及应交付的实现与测试必须重新生成，不得继承上一单元内容。
> - 提示词由用户维护；未经用户明确授权，不得新增、删除或修改提示词，规则变化也不得自动同步到提示词。

## 一、背景信息

知行分布式运行时（`distributed-runtime`）模块第 23 开发单元（surface 内容资产授权与生命周期治理）的实现约耗时 2 小时，后续审查与修复却耗时 4 天半。复盘表明，根因是进入审查时交付物仍不完整：审查阶段既在补做开发阶段本应完成的功能，又在处理大量遗留问题。

在审查阶段补开发，必须反复经历“发现问题—修复—构建验证—再次审查”，使原本一次可完成的实现工作额外叠加多轮审查与验证成本。本工作台用于把范围识别和实现完整性控制前移到开发阶段，确保进入审查时实现已经完整，审查只负责发现残余问题，而不是继续完成开发。

## 二、目标与边界

本工作台前移的是开发范围的识别与完整实现，不是把独立审查或单元提交验证搬到开发阶段。开发前必须依据架构、需求和上下游合同，明确本单元应完成的边界、功能链与异常场景；开发时一次性完成这些已知义务，不得把未考虑或未实现的功能留给审查阶段补做。

### 交付优先级与扩张门禁

开发清单只承载当前单元的**最小完整产品范围**：权威来源明确要求的核心功能，以及缺少就会阻断核心使用、破坏既定产品体验或违反冻结合同的直接依赖。范围内必须好用、完整并采用最优架构；“最优”不得被解释为增加功能、预建未来能力或扩大验证设施。

任何新增事项都必须写出权威来源和当前必要性。非核心增强、未来单元能力、通用化预留、纯观测/benchmark、诊断平台、推测性防御和非必要重构不得进入开发事项；可以作为未来建议，但不得阻塞当前单元。无法判断的是产品范围变化时才交由用户确认，不能用架构偏好自行扩面。

清单定稿和开发完成后立即进入下一阶段，不得因“还可以更完整”反复扩充已经闭合的范围。

开发阶段只执行支撑当前实现所必需的直接验证，不进行重复的全量审查与重型验证。审查阶段只负责发现完整实现后的残余缺陷；本单元的已知范围必须在进入审查前全部落实，不能等到审查时才识别或补做。

开发清单只定义“必须交付什么”：每项写清功能范围、边界、场景、应实现行为及直接相关的测试代码，不登记“测试通过”“连续审查无问题”“达到可提交状态”等质量状态。勾选只表示对应实现与测试已经编写，不代表审查或单元提交验证通过。

### 架构与需求空洞裁决

1. 现有架构总纲、规格和边界能够唯一推出清晰、可行且无明显副作用的最优方案：这只是文档细节不足，应自行补齐并继续开发，不应停工。
2. 产品目标、用户体验、功能边界和外部合同已经明确，只剩架构或实现路径需要选择：这不是需求空洞。架构者必须以整体最优、长期可维护、可扩展且不留债务为唯一标准，自主完成架构决策、补齐设计并继续开发，不得把架构选择上抛给用户。
3. 现有权威来源无法确定产品应提供什么行为、体验或边界，且不同选择会形成实质不同的产品结果：这才是需要用户确认的需求空洞。必须停止并说明缺失的需求事实及各选择的产品影响，等待用户裁决；不得用架构偏好替代产品决策。
4. 是否需要用户裁决只看产品需求是否缺失，不看架构方案是否多、实现是否复杂或工程成本是否较高。若最优方案需要改变既定产品范围或外部合同，应按需求边界变化处理并提交用户确认。

## 三、用户提示词

### 3.1 生成开发清单

```
先按本工作台维护原则处理动态区：当前单元为空时直接继续；仍登记上一单元且全部开发事项均为 `[x]` 时，先重置动态区并保留固定骨架，再生成下一单元清单；存在 `[ ]` 或 `[!]` 时立即停止，不得清空旧内容。

完整读取下一开发单元的架构总纲、可执行规格或执行计划、上下游合同与验收要求，确认单元身份和边界。存在架构总纲时以其为最高依据，开发范围与方案不得偏离架构设计；文档不足以确定边界时，明确指出架构空洞并停止定稿，不得用实现假设补齐。

按照本工作台“目标与边界”，把开发阶段必须完成的功能范围、生产端与消费端、状态与异常路径、边界条件、非默认场景及直接相关的测试代码，拆成有限、可执行的开发事项，写入动态区。明确不属于本单元的内容只写入“明确排除”字段，不得生成开发事项。每项只写清必须交付的实现与测试，不得写入审查轮次、验证结果或可提交状态；动态区不得沿用上一单元的分类或内容。

本任务只建立开发清单，完成后立即停止。
```

### 3.2 审查开发清单

```
不得沿用生成清单时的范围判断。重新完整读取当前单元的权威架构、可执行规格或执行计划、上下游合同与验收要求，审查动态区开发清单；目标是在清单定稿前收齐全部已知开发义务，避免后续审查才发现功能范围遗漏。

先从权威来源正向核对本单元全部适用要求是否已有开发事项，再沿每条功能链核对生产端、消费端、装配入口、状态与异常终态、直接相关的测试代码，以及适用的边界条件、非默认场景、并发、安全、资源和兼容性要求，最后反向确认每个事项都有架构依据且没有越出单元边界。

通过标准：本单元的全部已知开发义务均有明确落点；每项只描述必须交付的功能范围、边界、场景、行为和测试代码，内容有限、无重复、无歧义且可直接执行；不存在架构偏离、范围遗漏、范围越界或依赖实现猜测的事项。完成全部核对后，一次性向我报告所有问题和架构空洞，不得修改文档；没有问题时，明确回复开发清单通过。仅报告以下四类实质问题：偏离架构、遗漏开发内容、超出单元边界、开发事项无法执行。单纯润色措辞、设想没有事实依据的风险或增加非必要功能，不得作为问题。满足全部标准后立即停止。
```

```text
目标：通过多轮“独立审查—集中修正—受影响范围复审”，将当前单元开发清单定稿；一次收齐架构总纲、执行规格和单元边界已经确定的全部开发义务，确保后续审查只需寻找实现缺陷，不再补做已知功能。

首个动作及每次续跑或历史压缩后的首个动作：读取本工作台静态规则、当前开发清单及其已列权威来源，只依据文档中的当前状态继续；不得沿用生成清单时的范围判断，也不得重做已有事实未变化且已通过的局部复审。

进度反馈：首次读取后报告一次整体进度；此后每完成一个实质阶段、进入等待或暂停以及用户询问时，以百分比报告距离本提示词完成条件的整体进度，并用一句话说明已完成、当前和剩余。不得以单轮或单项进度冒充整体进度，不得为汇报而中断工作或重复检查。

持续执行：

1. 首轮从权威来源正向枚举全部适用义务，再沿每条完整功能链核对生产端、消费端、装配入口、状态与异常终态、边界与非默认场景及直接相关测试，最后反向确认每项都有架构依据且未越出单元边界。整轮完成后一次性归并同根问题，不得边发现边零散修补。
2. 只修改开发清单，按根因集中修正范围遗漏、架构偏离、范围越界和事项不可执行；不得修改实现或运行构建、测试。修正后只复审变化项及其直接交界，未受影响且事实未变化的结论继续复用；出现反证时扩大受影响范围，不得让历轮问题无依据消失。
3. 每个新增或扩写事项都必须同时反绑权威来源与当前单元必要性。无法证明“不做就会阻断当前核心使用、破坏明确产品体验或违反既定合同”的未来能力、通用化预留、纯观测或 benchmark、诊断设施、推测性防御和非必要增强，一律排除，不得以“更完整”推动清单无限扩张。
4. 持续循环至全部实质问题闭合。准备封版时，对同一份未修改清单执行一次冷启动对抗复审，不得复用前轮结论：分别从“权威要求是否全部有落点”“完整功能链是否存在断点”“事项是否越界或缺乏当前价值”三个对立视角重新推导应有范围，并与清单及明确排除项双向对账。发现真实缺口后必须修正并重新执行本步骤。

只有按本工作台“架构与需求空洞裁决”确属需要用户确认的需求空洞时才暂停；其余清单组织和架构选择自主按锁定范围内的最优方案收敛。

完成条件：同一份未修改清单通过冷启动对抗复审；当前单元全部已知开发义务均有有限、无重复、无歧义且可直接执行的实现与测试落点，明确排除项有事实依据，不存在架构偏离、范围遗漏、范围越界、实现猜测或未处置反证。满足后明确回复“开发清单通过”并立即停止，不得继续增加审查视角或扩充范围。
```

### 3.3 按清单开发

```
动手，完成本单元开发。

开始前：
1、完整读取 `research/design/workbench/unit-development-workbench.md` 的静态规则、当前单元已经定稿的开发清单及其引用的权威架构文档。
2、严格围绕架构总纲、可执行规格和单元边界开发，不得偏离架构设计。发现疑似架构空洞时，按照 “# 单元开发工作台”文档的“架构空洞裁决”规则判断并处理。发现架构设计不是最优，或不同选择会带来明显副作用时，停止开发并说明。

开发要求：
1、先以顶级架构师、顶级智能体专家的身份思考代码组织与设计，注意宏观视角看整体架构、要可维护、可扩展、可插拔，要最佳代码实践方案。
2、按开发清单逐项完成生产实现、消费链路、边界场景及直接相关测试代码；完成一项立即标为 `[x]`。勾选只表示实现与测试代码已经完成，不代表审查或单元提交验证通过。
3、开发中发现清单遗漏了本单元必须实现的内容时，先补入清单再实现；不得擅自扩展单元边界。
4、渐进式实现、分步验证。

原则：

我们的原则不是追求最小变更、修修补补、错上加错、妥协，而是避免架构债务，需要最优架构和方案设计。

验证纪律：
1、开发中只跑最小必要验证（类型检查 + 直接相关测试），修改边界只跑对应测试；源码修改后的必要构建按项目常驻规则执行并登记输入，同一构建输入在单元提交验证中直接复用。开发阶段不得运行受影响包全测、全模块回归或同输入重复构建；失败先归因，只重跑失效闭包。
2、执行构建、包测、全测或 CI 验证前，先按任务与运行条件查 `research/design/workbench/verification-runbook.md`，命中记录时必须采用其中已验证的运行方式；若本轮确认失败源于运行方式而非实现，并因此需要重跑，正确方式验证通过后当轮登记。
3、注意避免陷入“业务代码或测试代码导致无限循环、Bash 长时间无输出”的情况；出现风险时先停止并审查原因，不得持续等待。

注意：
1、不要留下注释债务，不引用 `Phase-N`、`M-N`、`INV-N`、`ADR §N`、`§X.Y` 等会变化的标识符，只保留解释当前代码所必需的稳定注释。
2、全部开发事项均为 `[x]` 时，报告本单元开发完成并立即停止；不得自行重置动态区。
```

### 3.4 专项循环审查补充开发清单

```
目标：把开发清单中的 D25-F08、D25-F09 补充完整，在开发前一次收齐两项功能的全部已知开发义务，避免实现完成后才由独立审查发现功能遗漏。单元边界、架构一致性和可执行性是必须遵守的限制条件，不得以它们代替功能完整性。

只读取 D25-F08、D25-F09，以及判断当前疑点所需的最小事实；不得重审其他开发事项，不得重新横扫整个单元或无关架构来源。只修改这两项清单内容，不实现代码、不运行构建或测试。

持续执行：

1. 审查 D25-F08 是否完整闭合：唯一生产组合根、本机受信入口、排他 bootstrap、唯一 owner/arbiter、稳定操作身份与重放、reset 展示—确认—恢复链，以及首装、并发、响应丢失、重启和路径保密场景。
2. 审查 D25-F09 是否完整闭合：两阶段恢复合同、真实 registry golden、逐分支结构账本，以及能够识别合同漂移、分支缺失和错绑的机械验收。
3. 整轮审查后一次收齐问题，按根因集中修正 D25-F08、D25-F09；收敛过程中只复审发生变化及受其影响的部分，未受影响的结论可临时复用，但不得据此直接判定封版。
4. 持续循环，直到两项的生产端、消费端、装配与所有权、正常与异常终态、重试恢复、边界场景及直接相关测试均有明确落点，同时不存在范围越界、架构偏离、重复、歧义或依赖实现猜测。
5. 准备封版时，对同一份未修改终稿执行一次冷启动对抗复审，不得复用前述结论：D25-F08 从 client prepare、host prepared/committed、权威结果、completed 回放、终态确认到 checkpoint 压缩，逐个核对 owner、线性化点、响应丢失、任意边界崩溃及重启收敛；D25-F09 从规格合同、production registry、runtime descriptor、scenario adapter、validator/reducer 到唯一 recovery owner 逐段核对，确认每个分支均由真实生产事实产生并能识别缺格、错绑和绕过。冷启动复审发现问题后必须修正并重新执行本步骤，任何修改都会使本次复审失效。

只有出现无法由现有产品目标确定的真实需求选择时才暂停；其余架构选择按整体最优且不留债务的标准自主决定。

完成条件：同一份未修改的 D25-F08、D25-F09 通过冷启动对抗复审；执行者仅凭两项内容就能一次性完整交付功能及直接相关测试，不需要自行补充范围或猜测关键方案；后续独立审查只需寻找实现缺陷，不应再补做已知功能。满足后明确回复“D25-F08、D25-F09 开发清单通过”并立即停止。
```

### 持续协调项目推进

```text
你是知行项目的架构师、项目经理和任务分配者，对整个项目的推进效率与交付质量负责。
目标是在不牺牲产品、架构和工程质量的前提下，以最短有效路径和最高效率持续推进当前关注任务直到完成。

当前关注的任务：依据 `docs/tasks/authority-substrate-capability-generation-migration.md` 完成权威基座—能力世代架构迁移，全部结束条件成立后停止并向用户报告。

现在你负责持续协调现有目标对话推进项目。首次执行、收到目标对话通知、恢复或上下文压缩后，先读取目标对话最新历史恢复调度状态；若目标对话正在执行工作包，先核对是否由本协调对话正常派发，正常则不再处理，异常则立即停止调度并报告；否则继续处理。

先读取目标对话的最新状态，再核对仓库当前事实、Git 状态与历史、权威目标和需求、架构、当前关注任务的完成条件、有效证据与最近检查点，独立判断项目整体进度和当前最关键的未满足条件；不得把目标对话自报的局部完成当作项目整体进展。
同时复盘上一工作包的执行过程；发现真实的低效或无价值路径时，将可复用且与本包相关的规避经验写入新工作包，避免同类问题重演。

工作包的职责必须直接推进项目整体目标，并遵守以下原则：

1. 只选择当前价值最高、能够真实推进完成条件的一条问题链。
2. 以至少两小时级的真实工作量起步；不足时只合并同一问题链的必要后续，不得凑时长。
3. 任务不能大到无法在开始前说清。工作包必须明确当前检查点、要做什么、哪些不做、怎样算完成；超出这个边界就拆分。
4. 保留执行者在任务边界内的技术决策权，不重复有效工作，也不运行本包不需要的高成本验证。
5. 不启动或恢复与当前关注任务无关的支线；上一工作包未结束时，不派发新任务。
6. 目标对话无论完成工作、停在安全交接点还是遇到阻塞，本轮结束前都必须在自身对话中留下完整、可复核的结果；不得执行任何 Git 暂存、提交、历史改写或推送操作。

每个新工作包、续做或纠正要求的末尾，需要原样附加以下两条标记之间的正文，让目标对话能在工作结束后给我们反馈。开始和结束标记只用于界定复制范围，禁止写入工作包。

【固定尾注正文·开始（本行禁止发送）】
本轮结束时，通过 Codex 原生任务通信能力向协调对话 thread ID `01a02f45-1a72-7690-90e8-9e8def854d08` 发送且只发送下面一行：

工作包本轮已结束。请重新读取 `E:\Dev\longxia\zhixing\research\design\workbench\unit-development-workbench.md` 中“持续协调项目推进”提示词内容，检查发送本通知的目标对话最新状态并继续调度。

通知发送成功后停止。
【固定尾注正文·结束（本行禁止发送）】

【收到工作反馈后的处理规则·禁止发送给目标对话】
收到该固定通知后，只把它视为开始复核的信号，不把它视为完成证明。必须独立读取目标对话的最新历史，并核对仓库变更、验证证据和项目整体状态，然后按以下规则处理：

1. 工作未完成、结果无效或仍可在原边界内自主解决时，直接向目标对话发送同一工作包的续做或纠正要求。
2. 工作真实完成且变更需要提交时，按提交规范审查本包变更；确认边界完整、验证充分且不混入无关内容后，由你执行本地暂存和提交，再生成并发送下一工作包。
3. 工作真实完成且不需要提交时，直接生成并发送下一工作包。
4. 只有确实需要用户决定的产品问题才向用户报告并停止；普通技术问题、执行异常和可自行修复的问题不得转交用户。
5. 当前关注任务的全部完成条件已经真实满足时，不再派发工作包，向用户报告最终结果并停止。

本提示词，已授权你在本次持续调度期间，仅对经独立复核确认属于目标工作包终态边界的变更执行本地 `git add` 和 `git commit`。不得执行 `git amend`、`git reset`、`git rebase`、`git push` 或提交无关变更；目标对话始终不具有 Git 写操作权限。

首次执行或完成上述复核后，只要不属于需要用户决定或当前关注任务已经完成的情况，就必须通过 Codex 原生任务通信能力把当前所需的工作包发送给现有目标对话。发送成功后简要报告任务目标和完成边界并结束本轮；等待目标对话通过固定通知再次唤醒本调度循环。
【收到工作反馈后的处理规则·结束】

生成工作包前必须复核发送内容：只附加固定尾注正文，不得包含两条标记行，也不得把协调者的复核流程、判断分支、Git 授权或循环调度规则发送给目标对话。
```

---

## 开发清单

### 当前单元

- 模块：`distributed-runtime`
- 单元：无。以下第 38 单元内容只保存历史开发事实，其合同已由[软件分发信任与产品边界](../modules/distributed-runtime/software-distribution-trust-and-target-strategy.md)取代，不是当前规范或待办。
- 架构与规格来源：`research/design/modules/distributed-runtime/distributed-runtime-charter.md` 的“当前版本交付原则”、§4、§10～§15；`research/design/modules/distributed-runtime/always-online-and-local-execution-requirements.md` 的第七～八章；`research/design/modules/distributed-runtime/specification.md` 的 §1.2、§1.5、§2.1、§4、§5.3、§6.4、§11～§15 及第 33～37 单元已冻结上游合同；`research/design/modules/distributed-runtime/s2-security-supply-chain-review.md` 的裁决与强制门禁。
- 单元边界：在第 30～37 单元已封版的 owner、transfer、checkpoint、trust、SecretStore、supervisor、stop/removal/uninstall 合同之上，交付同一 stable 产品的 `win32-x64`、`darwin-x64`、`darwin-arm64`、`linux-x64`、`linux-arm64` 安装制品及稳定启动入口；完成默认可见且不打断当前工作的稳定版自动检查、下载、安全空闲点安装、失败保留旧版与兼容恢复，并保留用户主动立即检查/重试入口；闭合崩溃续做、schema/protocol 兼容门与不兼容只读降级，提供可行动诊断，闭合供应链和发布检查，退役首个公开基线前的能力开关与迁移兼容层，并为 18 条不变量、故障/安全矩阵、双拓扑和产品旅程提供最终可执行验收入口。升级只替换程序层，必须保留当前 `ZHIXING_HOME`、设备身份、信任、权威数据、配置和用户工作；程序移除与永久设备退役在名称、入口、后果和确认上完全分离；其他 platform/arch 在清单校验、下载和安装前明确拒绝，不作隐式兼容。
- 明确排除：自动 failover、quorum/witness、多 active 值班设备、全局或持续同步、恢复应用；不可见的强制切版、独立 updater daemon、多发布通道/灰度/企业策略、远程批量设备管理、应用商店能力；支持包/原始日志导出、通用 updater/lifecycle/diagnostics/telemetry/benchmark 框架及性能 benchmark 门禁；当前兼容窗口不需要的 schema 功能迁移；实际保管或提交生产签名私钥、商店账号及外部发布操作。第 38 单元只实现并验证这些外部操作所消费的签名、制品和发布自动化接口。

### 开发事项

状态：`[ ]` 未完成；`[x]` 已完成；`[!]` 阻塞。进度：`100%`。

| 状态 | 编号   | 开发事项                                                   | 必须交付的实现与直接相关测试代码                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | ------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x]  | D38-01 | 冻结升级、兼容与发布的唯一可执行合同                       | **来源与必要性：**总纲 §4、§10、§14～§15，规格 §1.2、§5.3、§15 第 38 单元；没有唯一版本、签名与 lifecycle 合同会直接造成错包安装、回放降级或数据格式不可回滚。把根`package.json.version` 建为 canonical SemVer release/version 唯一源，workspace 版本、CLI 显示、构建与运行投影只从它生成；冻结上述五个 target 的 exact-set。保持已补写总纲/规格与实现同步，并在既有 contracts 实现严格 `StableReleaseIndex` v1：`channel=stable/releaseVersion/releaseSequence/targets[{target,manifest{url,digest,bytes},artifactUrl}]/keyId/signature`，target 恰为 exact-set、URL 只接受 HTTPS，manifest digest 为下载所得已签名 canonical bytes 的 `B(bytes)`；以及严格 `ReleaseManifest` v1：`releaseVersion/releaseSequence/channel=stable/target/nodeVersion/sourceTreeDigest/packageGraphDigest/artifact{digest,bytes}/protocolRange{readMin,readMax,writeVersion}/durableSchemas[{schemaId,readMin,readMax,writeVersion}]/minimumRollbackVersion/keyId/signature`，artifact digest 为最终平台签名/公证后归档 bytes 的 `B(bytes)`。`releaseSequence` 是规范 uint64 十进制字符串；索引与目标 manifest 必须序列、版本、target、manifest 摘要及 artifact 摘要/字节全等，二者均按 §1.2 JCS 与签名域验证；同 releaseVersion 只允许 exact replay，发布新 bytes 必须同时严格提高 SemVer 与 sequence。稳定启动器内置发布公钥，反降级高水位只从当前已验安装 manifest 与当前 home 的同一 `device-lifecycle` upgrade 记录投影（无 home 时只有前者），同索引/manifest 重放幂等，旧序列网络结果拒绝，程序卸载保留 home 后重装也不得降低水位，手动本机兼容恢复不改写它。实现程序层唯一原子 `ProgramUpdateReceipt` v1：`currentManifestDigest/target/candidateManifestDigest?/phase/operationId?/notice/code?/action?`；`phase=idle                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [x]  | D38-02 | 生成可验证、可安装的目标矩阵制品并闭合供应链门禁           | **来源与必要性：**单一安装包合同、规格第 38 单元、S2 强制门禁；缺少自包含与可信制品会让首装/升级不可用或可被篡改。按 D38-01 target exact-set 构建自包含 Node 22、workspace 生产闭包、CLI 与 checkpoint 原生桥接制品，消解`workspace:*` 并只打包运行资产；先消费并验证平台签名/公证的外部结果，再冻结最终归档 bytes、逐文件摘要与 canonical manifest 待签 payload，禁止开发依赖、安装脚本、源码秘密、source map 或测试资产泄入运行包。扩展现有 supply-chain/package-export 门校验四个受管依赖只由 mesh 精确声明，并同步/核验 `security-dependencies.json`、锁文件完整性、许可、安装脚本、官方漏洞审计结果、完整生产闭包、制品内容、版本全等、目标原生件和签名链。修正当前 PAKE 声明与 S2 清单不一致：`@cipherman/pake-js` 仅留在 mesh `devDependencies`，adapter 零 export/build/生产引用；生产 `zz pair` 只装配二维码与同一高熵邀请文本的复制粘贴，命令帮助和交互零短码入口。短码只有在精确实现及 `@noble/curves`/`@noble/hashes` 闭包取得独立审计不可变证据，或替换为经审计成熟实现并证明 suite 名、wire 字节、标准向量与耐久限次语义全等后，才可单独解除生产门；这不是当前高熵配对或第 38 单元发布阻断。直接测试可复现构建、篡改/错 target/错包图/多余文件/路径穿越/符号链接逃逸拒绝、OS 签名结果消费、PAKE 禁入生产门、高熵邀请生产闭包、标准向量和 production dependency closure。 |
| [x]  | D38-03 | 交付零概念首装、默认自动维护、稳定启动器与无歧义移除入口   | **来源与必要性：**总纲单一产品原则、§10～§11 自动维护体验及规格第 36/38 单元；用户必须安装一个包后直接用`zz`，不记维护命令，也不能因“卸载”一词误触永久设备退役。为五个 target 提供同一安装合同：平台标准用户级程序根、固定 PATH 中的 `zz/zhixing` 启动器、不可变 `versions/<release>` 与原子 current 指针；首次/重复安装先验证平台签名和内嵌 signed manifest/归档 bytes 全等，再只写程序层及安装器持有的单一 stable index HTTPS URL（默认包已给定，非用户配置、非 home 状态），不创建配对/恢复概念、不改 `ZHIXING_HOME`。稳定启动器在真实启动时异步检查，foreground/按需进程在实际运行期、托管宿主在自身生命周期内有界检查同一 feed；current 启动、首个输入与 ready 不等待网络，不新增 updater daemon。所有入口由内置发布公钥经现有安全网络出口严格解码并验签 D38-01 索引，按 current target 取得 manifest 与 artifact 定位，逐层核对签名、高水位、target、摘要、字节与兼容后，再以 digest+bytes 身份在程序层内容寻址 stage；通过 D38-01 `ProgramUpdateReceipt` 让同 candidate 并发检查/重试幂等合并并耐久投影安全错误，固定分块下载、断点/同请求重放与解压复用现有 device capacity/storage maintenance 治理，网络等待不持 permit，空间不足在 lifecycle accept 前失败。stage 完整回读后，已有 home 自动等待安全空闲点再交 D38-04；尚未首次运行、无 home 时复用幂等重复安装路径原子推进 current，崩溃重启只从已验 program stage 继续，不启动 host、不创建 home 或 lifecycle 事实。`zz update` 只作为立即检查/重试同一合同的显式入口。CLI/server/第一方 surface 共用一个无副作用更新投影：健康无更新不展示；下载、等待安全点、安装、成功、失败仍安全使用旧版、已自动恢复或必须处理时，在当前输入区/状态区显示固定产品文案和唯一下一步，安全可继续时不得阻塞输入，零 raw error。安装器及 `zz app remove` 只显示“移除知行应用（保留全部数据）”，命令只交接给平台安装器持有的移除入口；其复用安全 stop、关闭 future launch、注销程序层 definition并在进程退出后删除 launcher/version/stage，始终保留 home、身份、信任、配置与工作。永久退役只能由 `zz device remove --permanent` 显示设备名、未转移工作和不可逆后果并强确认后进入第 37 单元既有流程，首个公开基线前删除歧义的 `zz uninstall`。直接测试五 target 首装/重装、各形态异步自动检查且首个输入零等待、并发/有界检查、下载/等待/安装可见性、失败旧版可用与同 candidate 重试、程序移除/永久退役零混淆及强确认、默认 feed 零配置、无 home 各崩溃窗口、路径空格/Unicode、同版与旧索引/manifest 重放、错绑、range/响应丢失、无网/容量不足/篡改/解压逃逸和已有 home，证明 accept 前失败仍由旧版启动且用户数据零改写。 |
| [x]  | D38-04 | 复用本机生命周期与安全停机完成耐久升级收束                 | **来源与必要性：**总纲 §10、§12，规格第 37 单元 stop 闭包和第 38 单元升级纪律；自动下载后若直接替换会打断 accepted work，独立升级锁又会与 stop/removal/uninstall 分叉。让自动更新、`zz update` 显式重试、稳定启动器恢复和托管宿主只消费 D38-01 的同一 upgrade operation：签名制品已完整 stage/read-back 且到达安全空闲点后才 accept；随后按原 operation 关闭十类 producer gate、冻结 exact accepted-work/source/delivery artifact，以固定 `drain` 逐 owner settle/read-back，flush authority/assignment/intent/final/delivery 日志、outbox、lease/permit 与物理步骤，再以完整 endpoint lock、definition 与 manager projection 停止旧 exact host并写 `old-host-stopped`。当前进程只把 operation 交给稳定启动器；启动器逐 phase 继续 D38-05，禁止嵌套 stop、第二 journal/锁、wall-clock 强制切版或强杀。accept 后取消不回退 gate，只能完成目标升级或兼容恢复；并发自动/显式 update、stop/removal/uninstall、OS signal、响应丢失和连续重启只重放原 identity/phase，异载荷冲突零副作用。直接测试三形态、当前交互结束前零 accept、十 owner 空/非空、迟到 source/delivery、每个 fsync/phase 窗口、双调用者、旧/新 PID、manager 不可判定和后继 work，验证 `old-host-stopped` 前零切版、完成 phase 零二次效果、terminal 前无新准入。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [x]  | D38-05 | 原子切版、真实健康门、自动兼容恢复与有界版本保留           | **来源与必要性：**总纲 §10 自动维护/恢复纪律、§12 故障矩阵、规格第 36～38 单元；“进程启动”不能冒充成功，升级失败也不能要求用户理解 rollback 或留下无界旧程序/双 host。稳定启动器恢复 active upgrade 后，只在`old-host-stopped` 从已验 stage 以“临时指针 fsync→原子替换→目录 fsync”推进 current 与 pointer generation；托管定义、foreground/on-demand 始终只指向该启动器。目标版以同一 home 启动，经既有 inactive server/ready、managed-status snapshot 与真实 RPC health 全等复验 release digest、protocol/schema inventory、home、endpoint 与角色计划后才写 `health-verified/terminal` 并开放 ready。超时、崩溃、定义漂移或健康失配时，只在 manifest rollback floor 与当前耐久 schema 证明旧 reader 兼容时自动原子恢复最近已验证版本并启动，公开报告“更新未完成，已恢复上一版本”；否则 gate 保持关闭并只给“恢复上一个可用版本”或带安全问题码的“联系支持”之一。脚本入口 `zz update --restore-previous` 只选择本机已验兼容版本，用户文案和正常旅程不得暴露 rollback；首个公开基线前删除 `--rollback` 旧别名。程序层只保留 current、最近一个已验兼容 previous 与 active stage；terminal 后通过既有 storage governor 固定批次清理其余版本/stage，绝不遍历或删除 home。直接测试指针/启动/health/terminal/cleanup 各效果与响应丢失、指针损坏、连续失败、三形态、自动恢复/人工引导恢复、旧版缺失/篡改及清理重启，证明唯一前滚或兼容恢复、无双 host、空间有界。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [x]  | D38-06 | 把 schema/protocol 兼容门落实到升级预检、mesh 与全部写入口 | **来源与必要性：**总纲 §4“协议不兼容禁写、只读降级”、§13 版本偏斜，规格 §1.2、§4、§5.3 与不变量 10；只显示版本而不约束 writer 会让混合版本写坏耐久数据。由当前 production codec/record descriptor 机械生成有限`durableSchemas` exact inventory，覆盖 AuthorityCommitLog format 与全部实际耐久记录族；release manifest、运行投影和 reader/writer policy 只消费该清单，不另造通用 schema registry。升级预检全量核对 current→target 读写交集、bridge 与 rollback floor；mesh 连接继续以 `negotiateMeshProtocol` 冻结一代，read-write 才装配 mutation、assignment、delivery、transfer、lifecycle 等现有写服务，不兼容代只开放现有 registry 明示 `version-independent` 的身份/状态/升级诊断 exact-set，普通 trust/admission 不得绕过。bridge release 携旧/新 reader 与 writer、初始仍写旧；后继 release 对每个实际发生新写的 AuthorityCommitLog，在首条新格式记录同一 envelope 追加该 schema 的单调 activation，之后任何兼容恢复到 bridge 都从日志投影继续写已激活版本，禁止降回旧 writer；没有新写的日志无需虚构迁移。format、protocol、schema activation 与 epoch 均不回退。状态/doctor 对需要更新的本机或已命名设备给用户行动；每台运行设备按 D38-03 自动维护，不由当前设备远程代替另一设备切版。直接测试 inventory 缺/多/重复、低/同/高版本、范围交集、连接换代、全部公开写方法、只读 exact-set、在途 manifest 复验、activation 与首写原子/响应丢失/重放、bridge→writer→兼容 recovery、未知 schema 与多设备偏斜，验证不兼容零写且状态诚实。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [x]  | D38-07 | 提供离线也可用的可行动诊断                                 | **来源与必要性：**规格第 38 单元“诊断”、总纲 §11、§15；自动更新故障若只给 raw error、要求记参数或把用户送进无信息的“联系支持”都会让恢复失败。新增本机`zz doctor` 并让 `zz status`、CLI 输入区/状态区与第一方 surface 复用同一 effect-free 更新/运行投影；即使 host 未启动也从 release manifest/current/previous 指针、program stage/check outcome、active upgrade phase、protocol/schema inventory、真实 managed snapshot、checkpoint/SecretStore inspect 和 lifecycle blocker 得出一条状态与唯一下一步（重试同 candidate、继续更新、恢复上一个可用版本或联系支持并复制安全问题码），不得 activation、SecretStore 写入、网络探测或 lifecycle 效果。公开结果只含固定 code、产品文案、release 与 action exact-set，排除 secret/token/恢复包、URL、内部设备/operation/路径、环境变量和 raw error；健康无更新不显示维护噪声，失败可安全继续时不阻塞输入。直接测试所有 update/recovery/兼容终态、host absent、损坏/无权限投影、问题码可复制及恶意错误注入，CLI/server/surface 同源、离线可用、零写且零 raw 外泄。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [x]  | D38-08 | 按有限生产清单退役首个公开基线前的双路径与迁移层           | **来源与必要性：**规格第 38 单元明确要求移除全部能力开关与迁移兼容层，总纲“单机是分布式同进程特例”；模糊地“搜 legacy”既不可执行，也可能误删正式兼容合同。建立并冻结一次 production reachability inventory，只处理当前可达的七组：明文`credentials.json` 迁移/旧公开凭据字段，filesystem workscene registry/cutover，legacy memory/journal Markdown import/projection，scheduler compatibility JSON store，recovery package `legacyCheckpoint` decoder，已废弃 RPC/request/CLI aliases（含歧义 `zz uninstall` 与 `zz update --rollback`）与 source-compatible 注入参数，以及 shadow writer/旧 delivery queue/registry/producer/capability flag 的残留入口；逐组删除 reader、writer、组合根、公开 export、迁移状态与对应未发布数据测试，默认安装不得扫描或改写这些旧位置。保留项只能是 D38-06 inventory 声明、被 `minimumRollbackVersion` 实际消费且没有隐藏开关的正式 reader；协议名称中的 `openai-compatible` 等当前产品值不属于清退对象。更新 runtime-baseline、package-export、registry golden 与结构检查，加入负向突变证明任一旧入口、开关、重复 owner 或未声明 bridge 回归即失败；生产 smoke 证明单机/双拓扑只装配最终状态机。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [x]  | D38-09 | 建立单一发布检查与真实目标矩阵安装/自动更新/恢复证据       | **来源与必要性：**规格第 38 单元验收、总纲 §12～§15、S2 门禁；发布者需要一次命令得到可复核结论，不能手工拼接局部绿灯或用瞬时暂存区状态充当制品身份。新增根级`release:check` 作为固定编排入口而非新测试框架：先从实际构建输入闭包生成规范 source-tree fingerprint，并校验根版本、target 矩阵、依赖/供应链与 PAKE 裁决；不得把 Git staged/unstaged/clean 状态写入 ReleaseManifest 或当作耐久正确性依据。随后调用既有 `pnpm lint`、`pnpm test`、`pnpm build`、runtime baseline/package exports/registry golden，构建五 target，并按固定顺序消费外部平台签名/公证结果→冻结最终归档 bytes→外部签名并验证 canonical manifests→由已签 manifest 摘要生成、外部签名并验证 candidate stable index。各目标再以这组 exact candidate bytes 在对应真实 OS/arch 的隔离 home/程序根执行 clean install→首次运行→同版静默检查→自动发现/下载→安全空闲点安装→故障自动恢复→诊断引导兼容恢复→移除应用保留数据→永久设备移除强确认 smoke，真实系统证据不得由 guest fake-runner 冒充。全部通过、输入与 candidate bytes 未漂移且交叉绑定仍全等后，才原子安装可发布 stable index；本单元不持有私钥、不调用外部发布。机器报告精确记录命令、source/依赖/制品/证据摘要和结果，不采集秘密。直接测试顺序、失败短路、同输入证据复用、矩阵缺格、输入漂移/过期报告、错签名、签名后 bytes 漂移、smoke 后漂移和索引原子安装。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [x]  | D38-10 | 闭合最终不变量、故障/安全、双拓扑和零认知产品旅程交付      | **来源与必要性：**总纲 §11～§15、规格 §11～§12 及第 38 单元最终验收；这是模块发布门，不是额外 benchmark。为 18 条不变量、总纲故障矩阵、安全对抗矩阵、单机/跨机双拓扑与开箱/扩展/日常/异常旅程建立固定逐行账本，每行反绑 production producer→consumer→组合根→终态的既有或本单元直接测试；缺口只补该合同/场景测试，禁止自报通过、无生产调用图 mock 或新增通用验收框架。产品场景覆盖单机 golden、配对与恢复包回读、ready/值班选择、就地执行、离线排队与继续本机工作、uncertain 裁决、撤销/迁居/备份恢复、停机/永久设备移除、安装、自动更新可见性、安全空闲点安装、失败旧版可用、自动/诊断引导恢复、移除应用保留数据和离线诊断；所有文案只用“值班设备/干活的电脑”，默认路径零拓扑/版本内部术语，维护健康时不制造噪声，每个失败只给一个下一步。同步安装、自动更新、恢复、移除应用、永久设备移除、诊断用户文档与发布运行手册，D38-09 机械对账固定矩阵、文档版本与 release；不新增性能、遥测或诊断平台。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
