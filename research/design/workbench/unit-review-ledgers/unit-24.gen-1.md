# 单元登记:第 24 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:24
- **generation**:1(仅经用户显式授权递增;同单元的修复、复审、终审轮复位是本文件内的状态推进,不产生新登记)
- **登记时间**:2026-07-28
- **登记来源**:用户要求将第 24 单元独立审查问题转入审查与修复工作台

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、审查结论复用表、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:完成
- **连续无新增问题轮数**:2 / 2
- **交付物是否冻结**:是
- **交付物文件集**:第 24 单元起点 `010dfd1` 至当前工作区共 129 个交付文件（Feishu 6、CLI 66、core 26、executor 9、orchestrator 1、owner-kernel 10、rpc 1、runtime-host 1、server 7、规格 1、合同 lint 1）；无删除项；路径集 SHA-256 `532dc1d803d4d412a4ce5096394527bf7a7720a0d48340ea6922f492f05b4771`
- **当前交付物指纹**:`git-clean-blob-manifest-v1:770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8`（129 个路径排序后逐行计算 `path<TAB>git hash-object --path=<path> <path>`，UTF-8、LF、无末尾换行；工作台、单元登记文件和暂存状态不计入）
- **架构来源**:`research/design/modules/distributed-runtime/distributed-runtime-charter.md`、`research/design/modules/distributed-runtime/specification.md`、`research/design/modules/distributed-runtime/remote-confirmation-execution.md`、`research/design/modules/distributed-runtime/remote-interruption-execution.md`、第 24 单元开发清单及其上下游合同

## 固定边界

- **功能范围**:第 24 单元 S6 中继、渠道确认与最终性整合的生产实现、直接相关测试与规格补充
- **架构不变量**:直连/中继无损同构；确认与 grant 权威唯一且耐久幂等；状态、最终性及 outbox 可恢复；生产装配、资源治理、安全与兼容合同不弱化
- **验收条件**:独立审查 24 项全部通过；P0/P1 清零；同一冻结交付物完成两轮终审、独立功能审查及必要验证
- **必要上下游**:第 21～23 单元 stream/spool、ticket、surface asset、WAL/索引与容量治理；现役 Feishu、第一方确认、远程确认/中断及 DeliveryPipeline
- **明确不属于本单元**:第 25 单元 Environment/workscene；第 26 单元 scheduler CRUD、定时触发产品闭环及旧投递退役；第 27 单元及以后模块接入、本地域 owner、收编、迁居、备份、服务与发布

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| ------------------------ | ------------------------- | ---------------- | ---- |
| 协议、合同、规格与合同 lint（24） | records/schema/导出与规格必须一致；结构门禁脚本随冻结符号和摘要原语同步；无外置 codegen/golden | `@zhixing/core lint:contract-schemas` 通过；Biome 覆盖该组；路径均归 R24-01 | 通过 |
| owner relay、challenge、grant 与 journal（21） | 记录枚举、full/guard reducer、outbox/恢复行为矩阵均为源码内结构基线；无外置快照 | 行为矩阵和恢复测试均在交付闭包；路径均归 R24-02 | 通过 |
| executor ledger、interaction 收敛与恢复索引（14） | AssignmentRecord、reducerVersion、corruption vector、真实生产/消费矩阵必须同改；索引可由日志重建 | 结构测试均在交付闭包；路径均归 R24-03 | 通过 |
| stream、ticket、spool 与 lossless data plane（14） | spool identity、ticket registry、mesh DTO 与消费水位必须同步；无外置 schema/golden | 路径均归 R24-04；无独立生成文件 | 不适用:派生基线均内嵌于源码与测试 |
| 生产组合根、角色/profile 与配置（22） | 包导出、mandatory surface、启动/回滚顺序和分布式运行时结构 golden 必须同步；无 package/lockfile 变化 | 结构 golden 的 contract/19 条引用计数逐项映射到具体生产源码；只读门禁通过；路径均归 R24-05 | 通过 |
| Feishu、RPC/server 与三域 finality（24） | callback DTO、wire/session、RPC 关闭语义、行为 golden 及 inline/ref 展示必须与共享协议一致 | 行为 golden 仅新增 `deliveryStatusNext`、`jobStatus`、`jobStatusNext` 三个已审响应字段；只读门禁通过；路径均归 R24-06 | 通过 |
| runtime bridge、资源治理与类型消费（8） | runtime-host/orchestrator、共享 commit-log 与 storage maintenance 合同随 core 类型同步；无 lockfile 或生成物变化 | 路径均归 R24-07；工作区全量构建列入最终计划 | 通过 |
| S6 跨链 conformance（2） | 汇总前七组的 local/mesh、conversation/job、first-party/channel 组合证据 | `s6-conformance.test.ts` 与 job owner 恢复/停机 conformance 归 R24-08；无外置 golden | 通过 |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| 渠道 token、grant 与 callback | 规范签名 DTO + owner authority/job journal | owner CommitEnvelope fsync；executor ledger 终态提交 | token/grant 签发、owner fsync、转交请求/响应、重复 callback | Feishu/第一方入口、owner coordinator/outbox、executor worker | TTL 有界；同 challenge/request 单飞；全等重放，异载荷拒绝 | 通过 |
| assignment stream、ticket、spool 与 ACK | executor assignment log + spool identity/checkpoint | ledger append 与 spool durable checkpoint；ACK 后推进 consumer 水位 | append/ACK/续订/切路、票据撤销、spool 回收各边界 | executor data plane、surface/owner relay、ticket registry、path manager | 页长、consumer、保留窗与恢复尾部均有界；sourceId 幂等 | 通过 |
| channel challenge、relay cursor 与 grant outbox | owner journal 的 prepared/delivered/granted/closed/cursor 记录 | 同一 journal 提交；matching mirror 关闭义务 | prepared/send/callback/grant-transfer/closed/cursor 任一步崩溃 | coordinator、渠道 adapter、owner relay、executor answer port | pending/outbox 分页恢复；同 assignment/request 单飞与封顶退避 | 通过 |
| job interaction mirror/stream/settlement 收敛 | executor assignment log；owner mirror 仅作远端权威完成事实 | terminal、mirrored、stream-projected、settlement accepted 各自 ledger/journal 提交 | 两条投影任意先后、取消竞争、响应丢失、索引/checkpoint 三步崩溃 | JobAssignmentWorker、projector、owner relay、proof/settlement ports | assignment 单飞；派生索引可重建；恢复分页、尾部有界、义务无尝试上限 | 通过 |
| 三域状态与最终性投影 | conversation/job/delivery 各自权威 source | consumer 成功后提交 subject revision/游标 | history/live 交界、乱序、拒绝回调、断线重连 | owner/RPC/server/surface/channel finality 消费者 | 按 subject key 有界缓冲；失败 resync，不建第二队列 | 通过 |
| executor-job 生产 owner 与域路由 | 各拓扑唯一 `ExecutorJobOwner`；execution 来自耐久 assignment binding | owner recover 完成后 ready；durable accept 前门禁 | 组合根启动失败、恢复并发、晚注册 journal、关闭中 accept | anchor-host、executor-host、local/mesh operations adapter | 单实例、幂等 start/close；等待可中止；零可选组件独占 owner | 通过 |
| runtime、取消、usage、proof 与设备容量 | assignment ledger + JobJournal pending fence；设备唯一容量 arbiter | cancel prefix fsync 后先中止 runtime；proof owner accept 退休；受治理物理步骤取得 permit 后生效 | runtime/stream 失败、usage 不可用、proof 请求/响应丢失、关闭竞争、隐藏日志迁移与维护 | runtime bridge、worker reconciler、owner dispatcher、usage intake、executor data plane | joined task 唯一完成；封顶退避；本地义务不被远端等待阻塞；维护零旁路 | 通过 |

## 审查结论复用表

> 每行一个可独立失效的完整功能或合同事实链，生产端、事实源、消费者、异常终态和测试不得拆开；无法独立指纹、独立失效或需重读多数其他项时合并。整表须覆盖固定边界、全部交付文件、关键原语和九类核查面。
>
> 常设一项跨项组合推演。其他项均已取得或复用本轮结论后，再审查组合项；组合项按编号汇总各项当前输入指纹与结论。任一其他项新增，或其边界、输入指纹、结论变化时连带失效。
>
> 只有覆盖全部登记输入且该项结论无问题的问题盘点或冻结终审可计次，每轮每项至多一次，证据列须引用审查轮及证据；某项发现问题只清零该项，同一输入达到 2/2 后才可持续复用。状态只允许“待审”“审查中”“通过”“失效”“有问题:编号”，独立深审只允许“0/2”“1/2”“2/2”。

| 编号 | 审查目标与核查面 | 登记输入（关键实现、全部生产点、消费路径、测试） | 最近通过的输入指纹（算法 + 值） | 重审条件 | 当前状态 | 有效独立深审 | 本轮结论与证据 |
| ---- | ---------------- | ------------------------------------------------ | ------------------------------- | -------- | -------- | ------------ | -------------- |
| R24-01 | 协议、schema、签名/摘要与安全边界 | core channels/confirmation/contracts/protocol/types/authority，规格与合同 lint，共 24 文件；生产端、validator/reducer、adapter 消费与协议测试 | `git-clean-blob-subset-v1:8881511dd6971d5fabbbc35ccf8c451a162c9f531f6579ff2562e1d01ca0a5c4` | 任一 DTO、记录、签名域、摘要、validator、导出、规格或 lint 变化 | 通过 | 2/2 | 冻结终审二：独立复核拒绝顺序、记录行为矩阵与兼容边界；本批仅调整派生恢复值，未新增 wire/schema 或放宽 validator |
| R24-02 | owner relay、challenge/grant、journal/outbox 与恢复 | owner-kernel 10 文件；CLI coordinator、conversation/job relay 与 durable interaction 11 文件；真实生产、消费与恢复测试 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | owner 记录、outbox、cursor、mirror、恢复、共享 commit-log 或 adapter 边界变化 | 通过 | 2/2 | 重开后冻结终审二：从 backpressure、投影 prepare、WAL fsync 与 outbox 重启逐点反推，失败在提交前可重试，提交后仍由耐久事实重驱；零新增问题 |
| R24-03 | executor ledger、job interaction、取消与耐久收敛 | executor 9 文件；CLI worker/projection/obligation/retry 及恢复测试 5 文件；终态、索引、proof/settlement 与异常路径 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | ledger/schema/reducer、共享 commit-log、reconciler、恢复索引、取消/终结谓词或测试变化 | 通过 | 2/2 | 重开后冻结终审二：共享日志的即时背压不改变 assignment winner、checkpoint 或恢复目录；半提交、响应丢失与取消组合仍由既有唯一 reconciler 收敛 |
| R24-04 | ticket、stream/spool、direct/relay 与回收 | CLI assignment stream/mesh、ticket、executor/lossless data plane 及共享 commit-log，共 16 文件；生产服务、消费路径、路径切换及测试 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | ticket、checkpoint、spool、ACK、隐藏日志治理、路径选择、资源/回收边界变化 | 通过 | 2/2 | 重开后冻结终审二：逐插目录游标、sidecar、migration、tail、ACK 与回收边界，失败最多延迟至下一有界扫描轮，身份和水位不丢、不重置、不跨域 |
| R24-05 | 生产装配、拓扑、生命周期、配置与域路由 | CLI config/registry、access/command/profile/role、mesh、executor owner/lifecycle/operations、setup 与结构 golden，共 22 文件 | `git-clean-blob-manifest-v1:770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8` | 组合根、角色/profile、mandatory surface、owner ready/close、local/mesh 路由、governor 注入、结构 golden 或配置变化 | 通过 | 2/2 | U24-18 冻结终审二：反向从结构 golden 的 contract 与全部 19 条计数变化回查具体源码；第 23 单元遗留漂移与第 24 单元增量已分离，当前图无环、无未声明依赖、无 server/executor 越层，零未知差异 |
| R24-06 | 渠道、RPC/server、三域状态与最终性消费 | Feishu 6、RPC 1、server 7、CLI RPC/conversation/finality/status/channels 11，共 24 文件 | `git-clean-blob-manifest-v1:770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8` | callback/wire、RPC session、resync、subject cursor、渠道能力、终态消费或行为 golden 变化 | 通过 | 2/2 | U24-18 冻结终审二：反向核对行为 golden 三个新增字段均由 `server.info` 当前三域响应产生，`tryNotify` 失败仍触发 resync；未删除或弱化旧字段、wire、游标和最终性状态机 |
| R24-07 | runtime bridge、资源治理、跨包类型与异常清理 | orchestrator、runtime-host、共享 commit-log/storage maintenance、CLI agent runtime 共 8 文件；运行、abort/dispose、permit 与消费者 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | runtime port、joined task、资源准入、互斥区重驱、取消/释放或跨包类型变化 | 通过 | 2/2 | 重开后冻结终审二：物理 permit 不横跨目录、日志锁或外层队列等待；normal/legacy 义务 owner 唯一，背压、取消与关闭均有确定出口，零治理旁路 |
| R24-08 | 跨项组合推演：S6 local/mesh × conversation/job × first-party/channel × finality | R24-01～07 当前输入指纹与结论 + `s6-conformance.test.ts` | `review-combination-v1:6124845c197de6f8541e17c8901f85f3a1bdfa5b0f35e1912f0c00b7cfd9bcdc` | 任一其他项边界、输入、状态或结论变化；conformance 变化 | 通过 | 2/2 | U24-18 冻结终审二：结构图、RPC 通知背压、三域历史响应与既有 local/mesh conformance 组合后仍保持唯一 CLI 组合根、零跳失 resync 和域隔离；零新增问题 |

## 问题列表

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U24-01 | **P0，工作量：中。** 原实现把 direct/relay 都映射为锚点进程内同一 client，且流中网络错误不能触发路径切换。当前残留：远程 executor 的 direct 被固定判为不可用；本地 direct 与 relay 仍落到同一个 `ownerStreamClient`，owner-forward 只增加包装层，没有形成可独立失败的真实链路。受影响审查项：IR24-01、05、14、17、19。 | 路径选择和连接生命周期被错误放在 owner 进程；第一方 surface 没有成为 direct/relay 的唯一拨号与切换所有者，owner 只能给同一连接贴两个路径标签，无法产生真实独立故障面。 | 第一方 RPC/surface、executor 数据面端点、owner relay 端点、ticket/consumer/checkpoint/ACK、路径切换与断线恢复；渠道和 job 固定 owner 路径不参与双路径。 | 将 `AssignmentStreamPathManager` 下沉到已认证第一方 surface adapter：owner 经现有认证控制连接只下发 ticket、稳定 executor 身份和 relay 能力；surface 从既有受信 `MeshEndpointDescriptor`/设备目录解析 executor 端点，禁止另建地址事实源。direct 为 `surface→executor`，relay 为当前认证控制连接上的 `surface→owner→executor`；二者只共享 assignment、consumer、checkpoint、seq/digest 与 ACK。无 direct 能力返回类型化 unavailable，不得在 owner 内伪造。以两条真实连接的建连/流中故障、切换续订、授权不变及协议拒绝不降级验收。**修复落地**：已认证 surface session 独占路径管理器；本地 direct 使用 surface-principal 端点，relay 使用 owner-forward 端点，建连及流中传输失败统一映射为可降级错误并复用同一消费身份与水位；当前拓扑不具备远程 surface→executor 直连时明确返回 unavailable，禁止伪造。principal 与 ticket 在接收票据前反绑。定向路径用例 7/7，S6 双 adapter 路径与故障矩阵 4/4。**稳定功能闭包已确认（2026-07-29）**：后续直接复用，不再重复功能审查；仅 surface 路径所有权、direct/relay 端点、ticket/consumer 身份、切换水位或失败分类变化时重开。 | 已验证 |
| U24-02 | **P0，工作量：大。** 原实现没有 owner 作用域的 relay/challenge 长期所有者；job relay 无生产登记入口，callback 依赖活跃内存会话且以 fire-and-forget 处理。当前残留：`ChannelInteractionCoordinator.recover()` 只枚举 job opening；conversation 会话只在新请求执行时打开，重启后 pending challenge 不会重建路由；opening 还携带 executor 运行实例的进程内 `confirmationBroker`，该对象无法由 owner 耐久日志恢复。受影响审查项：IR24-01、03、07～10、14、16～19、21、24。 | conversation 的耐久义务、owner 调度投影与 executor 运行时所有权仍混在同一 session：owner 既缺可重建 opening，又错误持有 executor 私有 broker，导致 local/mesh callback 执行边界不一致且恢复结构上不可成立。 | conversation/job relay、challenge outbox、callback/grant、cursor/ACK、executor interaction broker、local/mesh adapter、重复回调、发送失败、owner/channel/executor 重启、状态 source 和跨域凭证。 | 冻结 executor-owned interaction answer port，由 local/mesh adapter 共同实现，`ConversationAssignmentWorker` 只是生产实现而非 owner 依赖；owner opening 仅保存可由 journal 重建的 assignment、ticket、ref 与 pending challenge 身份，callback 持 ticket 调该端口，由 executor 查找 broker。协议恢复先登记 opening，渠道就绪后协调器幂等重开路由/outbox；executor runtime 暂未恢复返回类型化可重试 unavailable，不得关闭耐久 challenge；仅 durable closed 或 assignment 终态注销。以 local/mesh 全等、prepared/send/callback/closed 各崩溃点、渠道晚就绪、runtime 重建与重复恢复验收。**修复落地**：已冻结 executor-owned answer port，并由本地 observer/远程 mesh client-handler 共用；owner opening 只含 journal 可重建身份，conversation 从 pending challenge 与活跃 ticket facts 幂等恢复，job 由稳定义务目录登记并恢复。answer 与无应答面解决在 runtime 未恢复时均返回类型化可重试 unavailable，pending challenge 不关闭；仅耐久终态释放。协调器、mesh 与恢复定向用例 11/11，S6 conversation/job 双 adapter 与负组合 6/6。**稳定功能闭包已确认（2026-07-29）**：后续直接复用，不再重复功能审查；仅 answer port、opening 耐久身份、challenge 恢复/关闭条件、local/mesh adapter 或义务目录变化时重开。 | 已验证 |
| U24-03 | **P0，工作量：小。** Feishu adapter 新增 `verificationToken`、`encryptKey` 启动必填项，但唯一配置 registry、编辑器校验和既有凭据迁移仍只声明 `appId`、`appSecret`。受影响审查项：IR24-09、14、16、20。 | 渠道配置 schema、UI 字段与 adapter 凭据要求存在多个来源，且缺少“基础消息可用/互动确认可用”的能力就绪状态。 | CLI 配置 schema/editor/check、凭据存储、adapter setup、能力声明、既有 Feishu 用户升级及回归测试；当前正常入口无法补齐必需凭据并导致整个 adapter 失效。 | 建立 Feishu 配置与能力声明单源，由 registry、编辑器、校验和 adapter 共用；callback 凭据只进 SecretStore。缺少无法自动迁移的凭据时保留基础消息能力，明确标记互动确认 degraded，禁止发送 challenge 并给出补录指引；凭据齐全后原子启用 callback。以新配置闭环、旧配置消息不回归、degraded fail-closed、秘密零回显验收。**修复落地**：`SUPPORTED_CHANNELS` 加 verificationToken/encryptKey（sensitive+`capabilityGroup:"interactive-confirmation"`），checkMessaging 按能力组裁决（全空=合法 degraded、半填=报缺失字段），编辑器行显示"选填·能力未启"；feishu `resolveConfig` 改成对可选 `interactiveConfirmation`（半配置显式抛错），adapter 的 `sendChallenge` 改按凭据挂载的实例能力——degraded 时 `isChallengeChannel` 如实为 false、不注册 callback 路由、warn 补录指引，宿主侧经既有 NonInteractiveChannelError fail-closed。测试：feishu 72/72（含 degraded 保消息、半配置拒）、CLI checks 18/18（含能力组半填/全填）。**功能闭包已确认（2026-07-28）**：后续直接复用，不再重复功能审查；仅配置字段、能力组裁决、SecretStore 边界或 adapter 能力挂载变化时重开。 | 已验证 |
| U24-04 | **P1，工作量：中。** 原实现的 `ExecutionFinalityProjection` 没有生产消费者，`JobStatusDirectory` 没有真实 source 且返回虚假空游标。当前残留：live notice 以 fire-and-forget 并发进入投影，projection 在异步 `onStatus` 成功前已删除 pending 并推进 revision，回调失败会留下不可补读的假水位；`ExecutionStatusHub` 按域重排 `next`，session 却按数组位置判断推进。受影响审查项：IR24-01、12、14、17、21、24。 | 三域权威 source 已接入，但 live/history/消费者确认没有统一的按 subject 串行交付事务；游标身份仍部分依赖位置而非稳定 subject key，零跳失合同尚未机械成立。 | conversation/job/delivery 权威 source，RPC/surface 历史补读与实时通知，异步/失败消费者、乱序并发、断线游标，conversation final、job result 及渠道投递隔离。 | 在 `FirstPartyFinalitySession` 建立覆盖 history/live 的单一串行入口；projection 仅在消费者成功后提交 revision 并删除 pending。失败时保持旧游标并向所属认证 RPC/surface 连接传播类型化 `resync-required`，终止该订阅或连接，强制携客户端 last-seen 重连；不得只静默关闭内部 session，也不得建立第二重试队列。服务端 session cursor 只代表成功入队，客户端 last-seen 才是恢复水位。cursor/next 全部按 `subjectKey` 对账并校验集合全等。以并发、拒绝回调、跨域乱序、补读/live 交界及失败重连验收。**修复落地**：history/live/status/final 全部进入一个 session 串行入口；projection 先等待消费者成功再推进 revision、删除 pending 或确认 final。失败保留旧水位并触发类型化 resync，RPC 所属连接以 1012 关闭；cursor 按稳定 subject key 对账并要求集合全等。移除架构外的 job-result 第二终态路径，job committed result 仍唯一进入冻结 DeliveryOutbox。core 投影 4/4；跨包会话用例已在最终构建后随 CLI 全包验证通过。**稳定功能闭包已确认（2026-07-29）**：后续直接复用，不再重复功能审查；仅 history/live 串行入口、消费者确认线性化点、subject-key 游标、resync 传播或终态所有权变化时重开。 | 已验证 |
| U24-05 | **P1，工作量：小。** job grant 使用 `D("ChannelInteractionDecision",1,{interactionRequestId,decision})`，冻结合同唯一口径为 `D("ConfirmationDecision",1,{requestId,decision})`。受影响审查项：IR24-02、16。 | 同一确认决定被引入第二套摘要域和字段命名。 | grant/token builders、validators、签名与审计消费者、wire codec、手写夹具和兼容测试；协议规范字节发生分叉。 | 删除第二套摘要语义，所有生产者和消费者复用冻结的 confirmation decision digest；需要字段适配时只在边界显式映射，不改变摘要对象。以规范向量、跨 adapter 全等及错域拒绝验收。**修复落地**：删除 `channelInteractionDecisionDigest`，新建唯一边界映射 `channelInteractionConfirmationDecision`（{allowed}→{kind:"allow-once"}、{allowed:false,reason?}→{kind:"deny",reason?}），executor ledger 与 core validator 统一改走 `confirmationDecisionDigest`；grep 全仓零残留。测试：channel-interaction 8/8（含摘要域全等向量）、executor job-assignment channel grant 定向 1/1。**功能闭包已确认（2026-07-28）**：后续直接复用，不再重复功能审查；仅确认决定结构、摘要域、边界映射或其生产/校验入口变化时重开。 | 已验证 |
| U24-06 | **P1，工作量：小。** 原 `ExecutorDataPlaneRuntime.maintain()` 在治理外全量枚举 spool 并执行退休、回收和物理删除；分页、稳定 workKey 和叶级准入已落地。独立功能审查发现同根残留：第 24 单元已在 `ExecutorDataPlaneRuntime` 建立首个生产 `AssignmentStreamSpool`，触发 X23-11 重开条件，但该构造未把设备唯一 `StorageMaintenanceGovernorPort` 传入 spool；spool 在正常 handle 与遗留 identity 回填路径各自构造 `FileAuthorityCommitLog`，均只传 `clock`。受影响审查项：IR24-01、03、04、07、08、11、14、17～22、24。 | 顶层维护入口完成治理后，没有把“每个嵌套存储设施也必须由同一组合根注入 governor”纳入生产构造合同；正向核对已知 runner 取代了从隐藏日志 migration/maintenance 物理 I/O 叶节点反查唯一 arbiter，导致先前仅因无生产构造而排除的旁路在启用 spool 时重新出现。 | `ExecutorDataPlaneRuntime` 的生产 spool 构造、`AssignmentStreamSpoolOptions`、每 assignment 正常日志与遗留 identity 日志、log migration 的前台/恢复触发、assignmentId 分页发现的外层 permit、共享 commit-log 互斥区重驱、设备唯一 arbiter、关闭与直接测试。既有 ticket/spool 分页、回收、workKey 和删除叶级准入保持不变。 | 给 `AssignmentStreamSpoolOptions` 增加进程内 `storageMaintenance` 注入并由 `ExecutorDataPlaneRuntime` 传入；spool 创建正常与遗留两类 `FileAuthorityCommitLog` 时统一透传。将 `assignmentIdPage` 的准入拆成有界目录游标、sidecar 读写和日志自己的 migration/tail 叶级步骤；共享 commit-log 在检测到外层互斥区时不得锁外等待重试，而应把背压交还最外层 owner 重驱。不得以外层 permit 代替内部日志治理，也不得持 permit 等日志锁。以生产构造间谍、正常/遗留日志迁移、分页背压、外层串行队列背压和关闭验证完成。**修复落地**：唯一生产组合根已透传同一 governor；两类内部日志均受治理，目录 discovery 与日志 migration/tail 零 permit 嵌套；共享日志在外层互斥区背压时单次返回。直接测试 `AssignmentStreamSpool` 28/28、data-plane runtime 3/3、外层互斥重驱 1/1，合同 lint 通过。 | 已验证 |
| U24-07 | **P1，工作量：大。** 原问题为 S6 缺少从 job dispatch 开始的 executor-owned 生产执行链，测试绕过生产入口直操 ledger、stream 与 submission；主体接缝虽已落地，2026-07-29 独立功能核查仍确认七条确定失败时序：① `interaction-finished` 已耐久后，owner mirror 或 stream 投影失败会使 broker 回到等待态；grant 的 `replayed` 分支和无应答的既有 outcome 分支随后直接返回，不再推进 broker、mirror 与投影，job 必然卡死；若 broker 先到 expiry，还会用另一终态冲击已耐久结果并拒绝运行。② 存在 pending interaction 时，首次 owner cancel 只能写 abort 与 interaction-finished，因未 mirror 不能生成 cancel proof；若 owner 的两次即时尝试早于 mirror 完成，worker 完成清理后只轮询不存在的 proof，没有任何所有者继续重驱原 fence。③ job 无合法互动面时，owner 先取得 executor 的耐久终态再以“已有 mirror”接管 requested 帧，但随后接管 finished 帧却无条件要求存在 prepared challenge；合法的“无 challenge 终态”因此无法推进 relay cursor。④ executor 的数据面 ticket 服务固定把所有 execution 域交给 `ConversationAssignmentWorker`；手动 job 的 ticket 答复及 abort-ticket 因此会走 conversation envelope/interaction 路径并确定失败。⑤ `JobAssignmentWorker` 只由可选的 `MeshRuntimeAssembly.options.executor.job` 创建；默认 access-surface 与 executor-only 生产装配均未提供 `JobRuntimePort`，job worker、恢复与答复端口因此恒不创建。⑥ owner 已耐久写入 `channel-challenge-granted` 后若进程崩溃或转交 executor 失败，没有任何恢复枚举或 dispatcher 重驱该 grant；现有代码只依赖平台重投 callback，合法授权可能永久停在 owner 日志。⑦ owner-fence 的 `executor.cancel` 在通知 live worker 中止前便可能等待 usage intake 与 proof 生成；worker 正常收尾也先无限等待 usage final，再检查取消，而取消任务反向等待正常任务，导致停止信号、本地 mirror/stream 清理和 proof 前置义务被串在远端可用性之后。另有结构性反证：mirror 摘要链、mirror 水位与 stream 次序均为 assignment 级，按 `(assignmentId,requestId)` 分别串行仍会让并发交互提交相互冲突；started assignment 又被业务恢复枚举明确排除，崩溃后未完成的交互派生义务没有恢复入口。最新独立审查进一步确认当前 `job-interaction-obligation` 索引只比较 finished 与 mirror 水位，未记录或核对 stream 投影完成；`recover()` 又允许 cancellation 认领压过 interaction recovery，而取消收束只 drain stream、不补 mirror，故“mirror 已齐但 stream 独缺”和“finished 已写但取消发生在 mirror 前”仍确定无人收敛。受影响审查项：IR24-01、14、15、17、19、21、22、24。 | 原始根因为缺少 executor-owned job 执行接缝；更深同根是**没有按 execution 域和耐久线性化点定义唯一收敛 owner，而把收敛义务实现成一次调用栈的后续步骤**：`interaction-finished` 只是业务裁决，不等于 assignment 级 mirror、stream、可选 broker 与 owner relay 投影均已完成；`channel-challenge-granted` 只是授权事实，不等于 executor 已消费；cancel-fence 只是取消义务事实，也不等于 runtime 已停止、usage 已收束、proof 已生成并被 owner 接受。当前既缺从耐久事实派生未完成步骤的唯一 reconciler，又把 request 级幂等身份误作 assignment 级有序投影的串行边界，并将 job ticket 操作误投 conversation worker、将 fence 重驱拆给 owner 与无 fence 的 worker，还把权威必需的 job 执行与恢复 owner 挂在可选 mesh 组件和可选配置上。当前索引把 mirror 当作全部派生义务完成，恢复调度又把 cancellation 与 interaction 当作互斥 owner，正是同一根因在恢复判据和调度优先级上的残留，正常路径之外便出现错域、短路、双 owner、无人 owner 或 owner 根本未创建。 | job dispatch 后的接收/启动；定时 job 的 challenge/grant 外发与 executor 转交、无应答和手动 job 的 surface-ticket 答复/abort；七类失败时序、竞态终态与 broker 完成；assignment 级 interaction mirror、stream 投影、relay cursor 与有/无 prepared challenge 两种消费；usage 终结与 bundle 提交；owner-fence、abort-ticket、runtime 中止、pending interaction/effect 清理、cancel proof 生成/提交/接受；executor-only、single-machine 与 anchor+executor 装配，local/mesh adapter、数据面操作路由、owner relay、executor/owner 重启及相关 conformance。conversation 已闭合部分、scheduler CRUD/trigger、旧 DeliveryPipeline/queue 与第 26 单元产品装配继续排除。 | 保留 executor-owned `JobAssignmentWorker` 与既有领域隔离，以**execution 域路由 + 耐久事实派生义务 + 每步唯一 owner**收口。① **终态裁决与 executor 收敛**：所有 ticket/grant 答复、无应答、expiry、cancel、backpressure 先经 assignment ledger 的单一 `settleInteraction` 事务“写入或读取”唯一 winner；按 `(assignmentId,requestId)` 只判终态幂等，按 `assignmentId` 单飞并依 recordSeq 推进 mirror/stream。任何凭证在读取或返回既有终态前都先完成对应票据或 grant 的验真与全反绑；只有无终态的 fresh 凭证再检查当前时效和 job 活跃性。已有 answered 终态与原凭证全等时即使现已过期也回放，异凭证稳定冲突；已有非 answered 终态是合法竞态 winner，不再授权。尚无终态且 deadline 已过时由 ledger reconciler 幂等写 expired；owner 已写 grant 但到达 executor 时已过期不得延长授权。终态写入即完成业务裁决，broker 通过不回调耐久写的投影口按 winner 收束；mirror/stream 由 worker 从 ledger 恢复的 must-complete 义务继续，暂时失败不得反馈成 broker“未决”或第二终态。退避间隔有上界但义务无尝试上限；稳定合同冲突 fail-stop、保留可诊断义务，禁止静默丢弃。`bundle_sealed`、`execution-failed`、`halted`、stream final 与 cancel proof 均须先确认该 assignment 的 interaction reconciler 无欠账；暂时或稳定收敛失败只能保持未终结并上报，禁止失败分支越过欠账写终态。live accept、答复重放与 run-end/cancel 只唤醒同一 assignment reconciler；另增独立于业务重执行的 ledger 派生恢复枚举，覆盖所有尚未退休且存在 interaction 事实或 mirror/stream 落后证据的 job assignment（包括 started），但不得复用当前 `readAll()` 全历史扫描。assignment 日志提交是唯一线性化点；欠账索引只是可丢弃加速器，增量批次先应用再推进绑定 `{logId,lsn,prefixDigest}` 的源 checkpoint，崩溃在日志/索引/checkpoint 任意两步之间时分别以有界尾部重放或幂等重放补齐，checkpoint 禁止领先日志。索引缺失、落后、超前或校验失配时必须从 assignment 日志完整重建且与增量结果全等，重建完成前 fail-closed、不得把“空索引”解释为零欠账；正常启动按页读取未完成 assignment 并只追有界尾部。从耐久 envelope 重建仅含 submission/stream 的投影 binding，以 ledger mirror 水位和 spool 的稳定 `interaction:<recordSeq>` sourceId 集合/无孔前缀判缺，幂等补齐后从索引移除，绝不重跑 started runtime、不得形成第二事实；索引项必须分别保存 terminal、mirror 与 stream 的可验证完成证据，三者全部闭合后才能退休。`recover()` 不得因 cancellation 认领跳过 interaction reconciler；取消分支必须先经同一 reconciler 补齐 mirror/stream，再判断 proof 前提。无 runtime 时不得新授权。② **owner relay 与 grant outbox**：owner 渠道协调器唯一持有 relay 投影；mirror 投影从已验签 batch 保存 `requestId → 规范终态种类/finished outcome`，不得用 requestId 集合冒充终态证明，该映射只由耐久 mirror 重建。prepared challenge 的 finished 原子写 closed+cursor；无 prepared 时，仅当映射证明同 request 的 winner 为 auto-resolved/expired/cancelled 等非 answered 终态且与 finished frame 全等，requested 与 finished 才可只推进 cursor，answered 仍必须绑定 prepared。`channel-challenge-granted` 本身同时是 owner→executor 转交 outbox 的唯一耐久源；按 challenge/assignment 单飞枚举“已有 grant、尚无同 request 终态 mirror”的义务并重驱 `deliverGrant`，平台 callback 返回不算完成。只有 matching answered mirror 或合法竞态 winner 的 mirror 才关闭该义务；错 grant/错终态 fail-stop。进程在 grant fsync、转交请求/响应任一侧崩溃均从 owner 日志恢复。③ **取消收敛分源定主**：`executor.cancel`/abort-ticket 入口只需耐久写取消前缀并立即唤醒 live worker，不得在发出停止信号前等待 mirror、usage intake 或 proof；executor assignment reconciler 先中止 runtime，再从 ledger 独立推进 interaction/effect 清理、mirror/stream、耐久 usage report/final 与 proof。usage 是 proof 的显式前置义务，但远端 intake 不得阻止本地停止和可独立完成的清理。owner-fence 控制面由 owner dispatcher 以 JobJournal pending fence 为耐久 outbox，按 assignment/fence 单飞、封顶退避执行同 fence `executor.cancel → 查询/提交已生成 proof → owner 接受`，每轮重读 journal直至接受或另一合法 owner 终态关闭；worker 不轮询/提交 owner-fence。abort-ticket 则由 Job worker 独占 proof 生成、提交与恢复；两个来源竞争时复用账本最先耐久的唯一 proof，不生成第二终态。④ **生产装配**：在所有启用 job execution 的生产拓扑，由必然创建的 executor role 组合根提供产品 `JobRuntimePort` 并持有 `JobAssignmentWorker`、interaction/cancellation reconciler 与恢复生命周期；mesh/access-surface 只注册 adapter，禁止成为唯一 owner。唯一数据面 operations router 按已验证 ticket 或耐久 assignment binding 的 `execution` 判别路由：conversation 只进 Conversation worker，job 只进 Job worker，禁止按当前活跃对象试探；能力闭环前不得注册或宣告 job execution，incoming job 必须在耐久 accept 前类型化拒绝。Job worker 实现 surface-ticket 答复与 abort-ticket，复用同一 ledger 鉴权、reconciler 和取消半边；定时 job 只接受 channel grant。机械验收：覆盖七条失败时序；在 grant fsync/转交、terminal、mirror、stream、relay cursor、usage final、proof、owner accept 及索引批次/checkpoint 各边界前后注错并重启；显式覆盖“mirror 已齐而 stream 独缺”和“finished 已写、取消发生在 mirror 前”两条半提交路径，证明恢复索引不提前退休、取消认领不抑制 interaction 收敛，mirror/stream 完成前不得生成 proof；grant 无平台重投仍会转交或由合法竞态 winner 关闭，过期 grant 不越权；索引全量重建与增量结果全等，缺失/损坏期间零漏枚举，十万历史 assignment 下正常恢复只读 checkpoint 与有界尾部；started 在 terminal 后、mirror/stream 前崩溃零业务重执行；取消前缀耐久后立即停止 runtime，usage intake 不可用时本地可完成义务仍推进且 proof 保持待办；收敛失败不得写 stream final/execution-failed；ticket/grant/无应答与 expiry/cancel 竞态只产生一个 winner，非法凭证即使已有非 answered 终态也拒；broker 不回第二终态；并发 request 保持 assignment 级顺序；无 challenge 的非 answered 帧只推进 cursor，answered 缺 prepared 必拒；mirror 延迟跨任意次 owner-fence 尝试后仍接受同一 proof；executor-only、single-machine、anchor+executor 均存在唯一 job owner，手动 job 的 ticket 答复/abort local 与 mesh 全等且错域拒绝，定时 job grant 路径不回归；零第二事实、零永久等待。 | 已验证 |
| U24-08 | **P2，工作量：小（约 0.5 小时）。** Feishu `parseChallengeAction` 只检查必填字段，未拒绝 action 和 decision 的未知字段。受影响审查项：IR24-16。 | 渠道共用的 callback payload 没有唯一严格结构校验器，adapter 只能自行实现宽松解析。 | core callback DTO/validator、Feishu 映射、未知字段、decision 规范化与负例测试；token 的密码学与权威状态校验仍归 owner。 | 在 core 定义并导出渠道无关的 callback payload 严格结构校验器，精确限定 `{v,token,decision}` 与 `{allowed,reason?}`；Feishu 只负责把平台事件映射到该 DTO，原始事件留在独立 `raw`，owner 继续验 token 签名和 pending 状态。未知字段拒绝、合法回调及跨 adapter 同形通过即完成。**修复落地**：core 新增 `validateChannelChallengeCallback`（payload/decision/token 三层 exact-keys + reason 8KiB 上界；token 结构校验与 `validateChannelChallengeToken` 共用单一 `validateChallengeTokenStructure`，验签仍归 owner）；Feishu 删除手写解析改调该校验器，`raw` 独立保留。测试：core 校验器 3 用例（合法/未知字段×3/版本与越界 reason）、Feishu 15/15（含规范 token 正例与 payload/decision 未知字段负例）。**功能闭包已确认（2026-07-28）**：后续直接复用，不再重复功能审查；仅 callback DTO、严格校验器、token 结构或 adapter 映射变化时重开。 | 已验证 |
| U24-09 | **P1，工作量：中。** executor 结构性包测确认行为矩阵未覆盖新增渠道生命周期记录：conversation 缺 `channel-challenge-prepared/delivered/closed`，job 缺上述三类及 `channel-relay-cursor`、`channel-challenge-granted`；运行时全量键集合对账因此确定失败。进一步反查发现，前四类都有现成真实生产者与恢复消费者，而 `channel-challenge-granted` 无法给出“不适用”依据：它在 owner 日志耐久后仍有必须完成的 executor 转交，现有实现没有恢复 owner，此功能缺口归 U24-07。受影响审查项：IR24-22、24。 | 冻结记录 schema 新增类型时，没有同步维护“记录类型→真实产生场景→实际 full/guard 消费者→恢复行为→对抗向量”的穷举矩阵；测试文件又不在包类型检查输入中，构建无法替代该结构门禁。矩阵缺格不仅丢失结构证据，还掩盖了 `channel-challenge-granted` 的真实恢复义务。 | conversation/job journal 的八个缺失矩阵项、真实生产场景、full/guard replay、challenge outbox、job relay cursor 与 grant 转交恢复、损坏向量拒绝及 executor 包门禁；U24-02 的既有功能结论不重开，grant 恢复功能由 U24-07 承载，本问题负责把它纳入不可遗漏的结构门禁。 | 在两个既有 behavior matrix 中补齐八类：conversation 与 job 的 prepared/delivered/closed 均由真实 challenge 生命周期场景产生，并以重启后的 `pendingChannelChallenges`/`ChannelChallengeOutbox` 状态迁移作恢复 probe；job cursor 由真实 relay adoption 产生，以重启后的 `channelRelayCheckpoint`/续订水位作恢复 probe；job granted 由真实 callback grant 产生，并绑定 U24-07 新增的 grant-delivery outbox 恢复 probe，禁止标 N/A。每类同时经实际 full/guard replay，并提供改变身份绑定、顺序、水位或签名摘要的有效 corruption vector；禁止直接 append 伪造生产覆盖、只改期望或放宽全集断言。运行时 schema 键集合全等、真实生产、合法重放、恢复消费与对抗拒绝全部通过，executor 包门禁恢复全绿即完成。 | 已验证 |
| U24-10 | **P1，工作量：中。** 当前提交 `baef069` 首次加入 assignment v2 严格双读，同时 `requestInteraction` 已主动调用 `enableInteractionStreamProjection` 写入 `interaction-stream-projection-enabled`；父提交 `cf1a47e` 不认识任何 v2 记录，而规格明确要求先发布不主动写切换记录的兼容版本，再由后继版本启用 v2。受影响审查项：IR24-20、22、24。 | 把格式读取兼容与生产写入切换合并在同一可发布边界，没有以提交/版本形成可回滚的兼容栅栏。 | assignment v1/v2 schema、validator、reducer、全部 v2 写入点、旧日志读取、升级与回滚路径、提交拆分及兼容门禁；不改变 U24-07 已冻结的 v2 终态合同。 | 禁止把当前混合状态作为发布点：先形成只增加 v1/v2 严格双读、完整处理既有 v2、但不为 v1 assignment 主动写切换事实的兼容提交；既有 v2 assignment 允许继续写其收敛所需的 v2 水位和接受事实。再以独立后继提交启用新切换事实、stream 水位及本地完成记录；以后只允许从启用版回滚到兼容版。验收必须证明：兼容版新建及既有 v1 assignment 零 v2 写入；包含全部 v2 记录类型的真实日志可 reopen、完整 reducer/guard replay、恢复索引重建并完成 mirror、stream、proof/settlement 退休；启用版只在合法切换后写 v2；启用版数据回滚至兼容版仍可完整恢复；回滚到只读 v1 的版本被明确禁止。**兼容桥已落地**：ledger 默认关闭 v2 创建，single-machine 与 executor-only 两个生产组合根显式保持关闭；兼容 reader 可重放 proof/settlement 两种接受事实并正确退休，直接相关测试、Biome 及 core/executor/CLI 构建通过。独立中间提交 `8b55795` 已形成，后继交付已在两个组合根启用 v2 写入。 | 已验证 |
| U24-11 | **P1，工作量：中。** `createAgentJobRuntimePort` 将同一次 `AgentRuntime.run` 同时表示为 `running` promise 与 `AsyncYieldQueue` 错误：runtime 拒绝时队列先使 generator 抛错，随后 `await running` 不再执行，留下 rejected promise 无消费者；消费者因 stream 写入失败等提前退出时也没有 `finally` join 底层运行。全仓又无测试直接驱动该 adapter、`RuntimeHost.createJobRuntime` 和 executor-role 的真实启停装配，S6 conformance 仍手工创建 worker 并使用 fake runtime。受影响审查项：IR24-14、17、19、22、24。 | job runtime bridge 为一个运行建立了两个完成所有者，且没有统一的结构化并发生命周期；生产接缝在缺少真实装配门禁时被当作已闭合。 | agent runtime promise、yield 队列、generator 提前退出、abort/dispose、stream 消费失败、worker 异常终态、runtime-host 工厂、executor-role 创建/恢复/关闭及结构测试。 | 由 adapter 持有唯一 joined task：队列只传递 yield，运行结果/错误只由该任务结算；generator 用 `try/finally` 保证正常、异常、abort 和消费者提前退出均停止/释放 runtime，并始终 await 已挂接拒绝处理的底层任务，错误只向 worker 传播一次、零 unhandled rejection。以可控真实 `AgentRuntime` 驱动 adapter 与 `RuntimeHost.createJobRuntime`，再通过 executor-role 生产组合根验证创建、recover、stop/close 和失败回滚；覆盖 runtime reject、stream 消费失败、abort、dispose 失败及重复关闭，全部资源恰好释放一次即完成。**修复落地**：adapter 以单一 `settled` task 结算结果/错误，queue 只传 yield；abort、消费者提前退出、同步/异步 runtime 失败及 handle dispose 均 join 并恰好一次释放。executor role 以独立 lifecycle 固定 transport→recover 启动和 stop-accepting→worker→transport 关闭次序，启动/关闭失败继续释放其余资源并聚合主错误与清理错误；11 条直接生命周期测试、Biome 与 CLI 构建通过。 | 已验证 |
| U24-12 | **P2，工作量：小。** 合同 schema lint 将同文件中彼此无关的 `sha256:` 字面量和 `createHash("sha256")` 合并判定，误报 WAL/投影摘要；同时 interaction display 的协议字节摘要绕过公共 `byteDigest`。受影响审查项：IR24-11、20、22、24。 | 协议摘要单源门禁以文件级共现代替表达式级数据流，既产生误报，也未迫使真实协议摘要复用公共原语。 | core 协议字节摘要、WAL/投影内部二进制摘要、合同 lint 与 interaction display 回放。 | lint 只在同一 `sha256:` 构造表达式实际调用 `createHash("sha256")` 时判违规；interaction display 改用 `byteDigest`，并把 `WireSchemaIdentity` 纳入冻结符号。合同 lint、interaction display 4 项测试及全量构建通过即完成。 | 已验证 |
| U24-13 | **P1，工作量：中。** U24-07 主体收敛落地后，三个恢复入口仍返回完整数组：`recoverableJobAssignments()`、`recoverableJobCancellations()` 经全量日志枚举，interaction 欠账另由专用索引枚举；worker 随后把全部 pending 一次性展开进无界运行集合。受影响审查项：IR24-18、IR24-24。 | job 执行、取消和 interaction 恢复义务被拆成多套枚举语义，恢复查询没有统一的分页身份，消费端也没有容量驱动的拉取边界。 | assignment 日志、现役 interaction obligation 投影、执行/取消/interaction 三类恢复入口、worker 恢复调度、启动扫描、内存、I/O、并发任务及索引重建。 | 将现役 `job-interaction-obligation` 原位演进为唯一 `job-recovery-obligation` 派生投影，同一 assignment 条目表达执行、取消和 interaction 欠账；assignment 日志继续作为唯一事实源，旧专用语义退出，不并存第二索引。只暴露 continuation 分页接口；worker 使用固定并发队列，容量空出后再取下一页，删除全量扫描、全量数组和一次性任务展开入口。验收须证明投影可由日志完整重建且增量结果全等，缺失或落后不漏枚举；无关历史、欠账和页数增长时单轮扫描、内存、I/O 与运行任务均保持稳定上界。**修复落地**：现役投影已原位升级为统一恢复目录，旧 job 全量恢复 API 清零；分页 continuation 使用稳定 assignment 身份，worker 每页 32、固定并发 4，整页排空后才拉下一页。投影值严格反绑键与 envelope，损坏先重建再读取；执行、abort-ticket 取消和 interaction 欠账共同由 assignment 日志增量派生。直接证据覆盖 32+8 分页、并发峰值 4、stream/mirror/settlement 退休与索引重建路径。 | 已验证 |
| U24-14 | **P1，工作量：中。** `ExecutorJobOwner` 正常关闭执行 `stopAccepting()` 后直接 `worker.close()`，立即中止执行与恢复任务；若简单改为先 `worker.drain()`，无限重试的远端义务又可能使进程永久无法关闭。受影响审查项：IR24-14、IR24-19、IR24-24。 | 同一取消作用域混用了“停止新准入”“中止可重建的当前恢复轮”和“终止已接管业务执行”，worker 缺少与耐久义务边界一致的安全停轮状态机。 | owner/worker 启停、恢复调度、retry timer、in-flight assignment、远端 mirror/settlement、启动失败回滚、重复关闭、停机期间唤醒及 transport 释放顺序。 | 把生命周期收敛为两阶段：先停止新任务与新恢复调度，中止可由耐久事实重建的当前重试轮和延迟 timer，保留欠账供下次启动；已接管执行只推进至既有安全终态或明确耐久恢复点，再排空 in-flight，最后关闭 worker 并释放 transport。重复关闭、启动失败回滚和停机期间新增唤醒复用同一状态机，禁止另造停机协议。验收覆盖远端永久不可用、非空恢复、运行中关闭、重复关闭、启动失败及关闭竞争，证明既不提前丢失 assignment，也不因远端义务永久挂死。**修复落地**：worker 分离 per-execution 与 recovery abort；关闭先拒绝新准入，中止可重建恢复轮、timer 与队列，只中止尚在 owner 获取阶段且已有 received 恢复点的任务，已跨 start 的业务执行继续到 seal/其他安全点，seal 后远端重试可中止并由日志重驱。close 单飞幂等，owner 在 transport 前关闭；启动回滚在 owner 启动注册点提升顺序，保证已启动 owner 先于 transport 释放。直接证据覆盖运行中关闭不 abort、pre-start 等待可退出、永久 submit 等待不挂死、重复关闭及启动回滚。 | 已验证 |
| U24-15 | **P2，工作量：中。** 稳定且必建的 `ExecutorJobOwner` 在 executor-only 与组合拓扑存在两处构造路径，组合拓扑又把它建模为 `mandatory AccessSurface`。受影响审查项：IR24-21。 | 核心 owner、可选 adapter 与生命周期所有权没有单一装配边界；同一稳定能力被不同拓扑分别构造，profile/access-surface 分类开始反向支配必建核心资源。 | executor-only、single-machine、anchor+executor 组合根，owner 依赖装配、start/recover、stop、drain、close，mesh/channel/surface adapter 注册、profile 裁剪与启动回滚。 | 建立全部 executor 拓扑共用的唯一 `ExecutorJobOwnerAssembly` 稳定组合单元，集中装配依赖并持有 start、recover、stop、drain、close；executor-only 与组合拓扑只调用该单元，mesh/channel/surface 只接收 owner 引用并注册 adapter。通用有序装配显式区分必建 core unit 与 profile 驱动的可选 access-surface，不复制第二套装配引擎。验收须证明各适用拓扑恰有一个 owner，关闭与回滚顺序全等，关闭可选 adapter 不会裁掉或重复创建核心 owner。**修复落地**：`ExecutorJobOwnerAssembly` 成为唯一生产构造边界并持有 start、drain、stop、close；anchor+executor 与 executor-only 只在各自组合根注入依赖，所有 mesh/channel/surface 仅消费同一 owner 引用。统一装配引擎以 `core` 与 profile access-surface 显式分类，owner 创建和启动均为 core unit，关闭可选 adapter 不裁掉核心 owner。生产扫描仅余两个拓扑入口调用同一 Assembly，零直接 worker 构造；唯一实例、profile 关闭与回滚顺序证据通过。 | 已验证 |
| U24-16 | **P1，工作量：小（依赖 U24-13～15 完成）。** 现有 S6 场景仍在测试内手工构造 worker，新增证据仅分别 mock mandatory surface 与 executor-only lifecycle，尚未用真实生产组合根共同证明分页恢复、安全停轮与唯一装配。受影响审查项：IR24-22、IR24-24。 | 结构性验收绕过真实组合根，分别验证组件 happy path，无法暴露恢复、停机与装配边界组合后才出现的失败。 | executor-only、single-machine、anchor+executor 及可选 mesh/channel/surface 组合，非空恢复、固定并发、安全停轮、唯一 owner、重复关闭和启动失败回滚。 | 在 U24-13、U24-14、U24-15 完成后，以一套有限 conformance 套件驱动真实生产组合根，验证非空分页恢复、固定并发、远端不可用时安全停轮且不挂死、每个拓扑唯一 owner、重复关闭与启动失败回滚；fake 只允许位于外部平台、时钟和传输边界，禁止手工构造 worker 代替生产装配，也禁止重新横扫已通过功能。全部组合与负例通过且能对前三项分别提供可复核证据即完成。**修复落地**：S6 job conformance 已改由真实 `ExecutorJobOwnerAssembly` 驱动实际 ledger、local/mesh adapter 与提交链，不再手工构造 worker；有限配套用例固定验证 32+8 非空分页、并发 4、安全停轮、anchor+executor/executor-only 唯一 owner、profile 不裁剪 core、重复关闭及启动失败回滚。executor 恢复投影 4 项、CLI worker 4 项、生产 owner 4 项、装配顺序 3 项及 S6 local/remote job 2 项均通过。 | 已验证 |
| U24-17 | **P2，工作量：小。** 最终类型门禁中 core、owner-kernel、orchestrator、executor、runtime-host、rpc 均通过，server 在 `server.info` 的旧式历史状态分支把 `readonly DeliveryStatusNotice[]` 直接赋给可变 `ExecutionStatusNotice[]`，触发 TS4104；同分支 conversation/job 已显式复制。受影响审查项：IR24-12、20、24。 | 最终性历史响应的三个域没有共用一致的只读→响应快照边界；delivery 分支遗漏了与 conversation/job 相同的显式快照转换，静态合同出现单点分叉。 | server `server.info` 的 delivery 历史响应、三域最终性响应类型、server 类型门禁及直接 RPC 测试；不改变 wire schema、游标或最终性状态机。 | 在响应装配边界把 delivery 历史结果显式复制为本次响应快照，与 conversation/job 使用同一模式；不得放宽局部类型或去除上游只读合同。验收：server 类型检查零错误，`server.info` 直接测试通过，三域响应值和游标语义不变。**修复落地**：delivery 与另外两域一致在响应装配边界显式复制；上游只读返回、wire、状态机和游标均未改。server 类型检查零错误，`server.info` 27/27 通过，Biome 与 diff check 通过。 | 已验证 |
| U24-18 | **P1，工作量：小。** 新指纹最终验证中 server 全包 746/748；仅 `distributed-runtime-behavior.golden.json` 与 `distributed-runtime-structure.golden.json` 不匹配，普通测试未写盘。冻结准备只检查了已变更文件和合同 lint，没有运行这两项只读 golden 预检，因而把受当前 S6 行为/拓扑影响但尚未进入交付闭包的派生资产漏在冻结指纹之外。受影响审查项：IR24-14、20、22、24。 | 派生产物闭包按“当前已变更路径”反推，没有从受影响包的 golden 测试正向枚举所有派生资产及只读预检；声明的 127 文件闭包因此领先于实际结构/行为基线。 | server 的分布式运行时行为与结构 golden、其规范化生产输入、包图/拓扑清单、冻结文件集与指纹、server 全包及反向验收账本；不自动改变生产实现。 | 仅以显式 golden 更新模式生成当前实际值，逐字段审阅两份差异：每一项必须能反向归因到本单元已审查的生产行为、导出、包图或拓扑；出现未知、越界或弱化即回到实现修复，不得更新基线掩盖。差异全部合法后把两份 golden 纳入交付闭包，更新指纹并执行直接 golden 测试、两轮受影响闭包复审、独立审查和最终验证。两项只读 golden 与 server 全包通过即完成。**修复落地**：行为差异仅新增三域最终性响应中已实现的三个字段；结构 contract 新增可观测通知入队，19 条引用计数全部回溯到具体生产文件，并拆分出第 24 单元前基线漂移与本单元增量。两份 golden 已纳入 129 文件闭包，只读门禁 3/3 通过；两轮受影响闭包复审和独立审查零新增问题。 | 已验证 |

> **U24-13～U24-16 统一实施顺序**：`U24-13 → U24-14 → U24-15 → U24-16`。先稳定唯一恢复投影、分页接口和固定容量队列；再基于最终 worker 形态实现安全停轮与排空；随后统一所有拓扑的 owner 装配；最后以一套真实生产组合根验收覆盖整批闭包。禁止并行改动相互依赖的生命周期层，亦禁止边实现边扩充新的测试视角。
>
> **U24-14 实现红线**：不得继续使用同一个全局 abort 同时终止业务执行与可由耐久事实重建的恢复尝试。前者必须推进到安全终态或耐久恢复点，后者才允许在停止准入后中止当前轮并留待下次启动重驱。

> **2026-07-29 集中修复收口**：兼容桥提交 `8b55795` 已形成；当前后继交付统一启用 assignment v2，job interaction 的 mirror/stream/取消退休复用唯一账本谓词和同 fence 重驱，终态观察票据与活动写票据按 use 分离。U24-07、U24-09、U24-10、U24-11 的直接闭包与生产拓扑证据均已通过；问题状态转为“已验证”。本记录不替代两轮冻结终审。

> **U24-07 audit-only 退休残留（属于该问题的事实、根因、方案与验收）**：当前 `JobAssignmentWorker` 在开放副作用分支调用 `completeInteractionSettlement` 成功后直接返回；executor assignment 没有对应的耐久完成记录，`recoverableJobCancellations` 与 `hasPendingTicketCancellation` 仍只以 `cancel-proof-owner-accepted` 退休，而该记录要求 `halted` proof，不能表示 audit-only 完成。该分支的暂时失败也未进入既有封顶退避。根因是把 owner 的成功响应误当成 executor 义务完成事实，仍以一次调用栈代替耐久 reconciler。新增独立的 v2 本地接受记录，身份绑定 executor 可由耐久输入重算的 `assignmentId + ticketDigest + settlementVersion`；v2 stream 分支直接保存并复验本次已提交的完整 `InteractionSettlementStreamProof`，legacy 分支明确无 proof，禁止另造 `streamProofDigest`，也不得绑定 executor 不持有的 owner 内部 settlement fence，或复用、放宽 `cancel-proof-owner-accepted`。validator、reducer、恢复索引和行为矩阵共同识别 proof-accepted 与 settlement-accepted 两种互斥合法退休终态。worker 对暂时失败和响应丢失始终重提同一请求；owner 经认证入口全等幂等接受后，executor 才以同一稳定身份落本地记录，只有该记录耐久后才从恢复枚举退休，稳定冲突 fail-stop。该记录须服从 U24-10 两步兼容桥。验收覆盖 owner 写入前后、响应丢失、本地记录前后崩溃、暂时失败、重启重提、异 ticket/version/proof 拒绝、identity/proof 不可复验时 fail-closed 及退休后零重复提交。
>
> **当前落地状态**：独立 v2 接受记录、严格 validator/reducer、恢复索引双终态退休、worker 的 owner 接受→stream 终结→本地耐久接受次序及暂时失败退避均已实现；真实 assignment 日志覆盖 proof/settlement 两种接受事实、异 proof 拒绝、重启兼容读取与退休，worker 级测试证明恢复不重跑 runtime。兼容桥提交 `8b55795` 已形成，当前后继交付已启用 v2 写入；兼容与启用边界均已进入最终验证闭包。

> **U24-07 取消分支补充约束（属于该问题的解决方案与验收条件）**：executor reconciler 只可为 runtime/observer 已提供可验证结果的 effect 写入 `side-effect-completed`；崩溃后结果未知的开放外部 effect 必须原样保留为账本证据，禁止推定为 `aborted`，也禁止据此生成 `halted` 或 cancel proof。owner 恢复发现已验真的 abort-ticket 取消前缀而尚无可接受 proof 时，以 `assignmentId + ticketDigest` 为稳定幂等键，在写入 `uncertain(cause:"job-cancel-unknown")` 的同一耐久事务中建立唯一 `interaction-settlement-fence`，绑定已验证 ledger 前缀及其目标 mirror 水位；该记录是 audit-only 收敛义务，不进入 cancellation dispatcher，不作为 `CancelProof(owner-fence)` 的来源，也不产生第二取消结果或 proof。统一 mirror 授权谓词只接受两类耐久依据：正常 owner cancel-fence，或本记录；后二者都只开放绑定 assignment 的连续 mirror/stream 推进，禁止恢复 started、bundle、session、resource 或其他活动写权。用户裁决可以关闭业务 uncertainty，但不得删除该义务或使旧 assignment 失去 audit-only 提交资格；executor reconciler 继续补齐确定的 mirror/stream 欠账，owner mirror 达到记录水位且 executor 本地连续前缀均已耐久后写唯一 `interaction-settlement-completed` 并退休义务。未知 effect 继续保留证据并沿既有用户裁决流程终结。“零永久等待”仅指可确定义务最终完成或进入明确的 `uncertain`，不允许以伪造成功换取收敛。验收须覆盖：abort-ticket 前缀与 `interaction-finished` 已耐久、mirror 未提交且存在开放 effect 时 owner 重启；open 与 uncertain 原子且重启/重放只产生一个义务；该义务不出现在取消 outbox、不能生成 proof；用户裁决发生在 mirror 前后及旧 assignment 被新 assignment 取代后，audit-only mirror 仍按同一水位幂等完成而所有活动写稳定拒绝；水位完成只关闭一次；开放 effect 零伪造 completed/proof，迟到可验证结果与用户裁决按既有状态机幂等收束。

> **U24-07 投影完成证据补充约束（属于该问题的解决方案与验收条件）**：保留 `mirrored` 作为 owner mirror 的唯一耐久完成事实；stream 完成只由 executor assignment 日志中的幂等 `interaction-stream-projected` 记录证明，禁止再造聚合型完成记录。stream 记录由 assignment 级唯一 reconciler 在目标 `interaction-finished` 已耐久、spool 已按稳定 `interaction:<recordSeq>` sourceId 写成无孔前缀后追加，绑定 `assignmentId`、`upToRecordSeq`、`lastStreamSeq` 与既有规范 `streamDigest` checkpoint；不得另造摘要域。assignment 日志提交是唯一线性化点，`job-interaction-obligation` 索引只消费终态、`mirrored`、格式切换与 stream 水位记录，并在两条独立水位都覆盖适用终态前缀后退休；不得直接读取 spool 建立第二事实，也不得因 cancellation 认领跳过任一分支。mirror 与 stream 由同一 reconciler 调度但独立推进，任一暂时失败不得阻止另一分支；取消先中止 runtime，proof 仍须等待两条义务闭合。新合同以 `interaction-stream-projection-enabled(legacyUpToRecordSeq)` 为耐久切换事实：切换前前缀沿旧合同保留、不伪造完成，切换后终结的 interaction 必须具备两条完成事实；采用“兼容读取且仅为已启用 assignment 写水位 → 写切换事实并启用门禁”的两步桥，回滚只允许回到前一兼容版本。恢复按页进入设备级有界队列，本地批次重新准入，等待远端 mirror 时不持 permit 或锁。验收覆盖两条分支全部先后顺序、spool 写成后但 stream 记录前崩溃、记录响应丢失、取消竞争、索引重建、切换前后及旧 spool 已回收、恢复补投经 owner relay 落 closed/cursor 后 ACK、资源背压与重复恢复；新增记录必须进入规范、schema、真实生产/消费行为矩阵和 corruption vector。

> **U24-07 生产装配反证（2026-07-29 冻结准备重开）**：默认 single-machine 的角色恒为 `anchor+executor`，`planServeTopology` 将其路由到 `anchor-host`；该入口虽创建 `jobRuntime`，却没有消费它或创建 `JobAssignmentWorker`。全仓唯一生产 worker 仍位于拒绝 anchor 角色的 `runExecutorRole`，`meshSurface` 也只把 conversation ledger/runtime/data-plane 注入 mesh。现有 S6 job conformance 在测试内手工创建 worker，因而不能证明默认组合根成立。结果是默认 single-machine 与同机 anchor+executor 拓扑没有 job accept、恢复、ticket 答复和取消收敛 owner，U24-07 的“所有启用 job execution 的生产拓扑必然存在唯一 owner”尚未闭合。最优方案是把 job worker、恢复与关闭顺序抽成唯一共享的 executor-job owner 组合单元：`anchor-host` 与 `executor-host` 都在 authority、data-plane、ledger、`JobRuntimePort` 和拓扑对应的 submission/usage adapter 就绪后创建同一个 owner，再把该实例注入本地 operations router 与 mesh adapter；任何入口不得自行再造 worker，owner 就绪前不得注册或宣告 job execution。验收必须从真实 `runConfiguredServeTopology`/access-surface 组合根分别驱动 single-machine `anchor+executor`、分布式 `anchor+executor` 与 executor-only，证明每个适用拓扑恰有一个 worker，启动失败完整回滚，恢复先于接流量，关闭停止接收后完成既有收敛；local/mesh 共用同一状态机，测试不得手工构造 worker 代替生产装配。

> **U24-07 生产装配修复落地（2026-07-30）**：新增稳定 `ExecutorJobOwner` 组合单元，统一持有 worker、恢复、ready 门禁与关闭；anchor-host 的 owner 为 mandatory 核心装配项，不受 profile/mesh/channel/surface 开关支配，executor-host 复用同一 owner 与生命周期。mesh 只注入该实例且在 durable accept 前按 ready 类型化拒绝；local/mesh 共用由耐久 assignment binding 判别 execution 域的单一 router，conversation/job 不再互相试探。single-machine 的本地 `JobJournal` 注册晚于 owner 恢复时，worker 在任何 ledger start/runtime 副作用前等待对应耐久 owner 登记，关闭可中止等待，避免恢复任务丢失。启动回滚、正常关闭和 owner-start 并发均保持幂等；真实装配单元、域路由、等待/中止及 executor-only 生命周期直接证据 24/24 通过，CLI 类型检查仅保留既有 8 项凭据错误、无本轮新增。功能闭包核查未发现 U24-07 同根残留，两轮冻结终审、独立功能审查与最终验证均通过。

## 已排除问题

> 保存已证伪疑点供复审复用;使用与重开规则见工作台静态区。裁决只允许“已排除”或“已重开→问题编号”,重开时保留原行。

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | -------------- | -------- | -------- |

## 迟发现教训

> 仅登记“先前通过后才发现”的真实遗漏。检测动作必须可执行并写明适用范围;每个适用轮次追加执行证据。

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | ---------------------- | -------- | ---------------------------- | --------------------- |
| L24-01 | U24-07；根因与方案闭合复核 2/2 后发现 | 只核对 owner 的业务职责与 adapter 全等，未逐拓扑证明该 owner 在所有适用生产组合根中必然创建。 | 对每个权威或 must-complete owner，枚举 executor-only、single-machine、anchor+executor 及可选组件关闭形态；从组合根反查实例创建、启动、恢复、关闭和能力宣告，owner 仅由可选 mesh/channel/surface 持有即判未闭合。 | 补充复核：先确认 worker 不能由可选 mesh 持有；2026-07-29 冻结准备再次执行后确认修复只覆盖 executor-host，默认 anchor-host 仍无 worker，U24-07 再次重开。2026-07-30 冻结终审一：生产扫描仅有 anchor-host 与 executor-only 两个 Assembly 构造点；single-machine/anchor+executor 使用不受 profile 裁剪的 core unit，可选 mesh/channel/surface 零 owner 生命周期所有权。冻结终审二：从启动失败、正常关闭与 profile 关闭反向复核，所有适用拓扑仍恰有一个稳定 owner。 |
| L24-02 | U24-07；根因与方案闭合复核 2/2 后发现 | 把“可重建派生索引”当作充分结论，未逐崩溃点定义权威日志、索引与 source checkpoint 的写序及落后自愈。 | 凡方案新增耐久派生索引，必须明确唯一事实源、source checkpoint 身份、日志/索引/checkpoint 三步崩溃矩阵、落后/超前/损坏判定、全量重建等价及重建期间 fail-closed；否则方案不得闭合。 | 补充复核：U24-07 欠账索引已加入写序、checkpoint、尾部重放、全量重建与零漏枚举验收。2026-07-30 冻结终审一：同一 projectionId 以 reducerVersion 失配触发清空重建；commit-log 在日志锁内按 `{logId,lsn,prefixDigest}` 先 prepare/publish 再推进 checkpoint，读取前同步，损坏与错版本均从 authority log 重建，未形成第二事实源。冻结终审二：逐项复核日志已提交/索引未发布、索引已发布/checkpoint 未推进、损坏/超前/错版本与重建完成前读取，均由日志锁内同步或清空重建收敛。 |
| L24-03 | U24-09；U24-02 已标稳定闭包后发现 | 新增冻结记录类型时只审生产与 reducer，遗漏执行点行为矩阵，且包类型检查不包含测试文件，导致红色结构门禁未被构建暴露。 | 任何 record/schema 枚举变化都必须机械对账其行为矩阵、真实生产场景、full/guard 消费、恢复分类和 corruption vector；执行对应结构测试，不以类型检查或构建代替。 | 补充核查：conversation 差三类、job 差五类记录，已登记 U24-09。2026-07-30 冻结终审一：本批未新增或改变 AssignmentRecord/schema 枚举；统一恢复目录仅消费既有严格 entry，既有行为矩阵与 corruption vector 无新增格。冻结终审二：反向扫描当前差异再次确认零新记录判别项，故无行为矩阵新增义务。 |
| L24-04 | U24-06；两轮冻结终审通过后由独立功能审查发现 | 先前以“无生产构造”为依据排除隐藏日志治理旁路；后续新增生产构造时只核对顶层维护 runner，没有回查排除项重开条件，也没有从嵌套设施的物理 I/O 叶节点反向追到组合根注入。 | 任一先前不可达的存储设施进入生产装配时，必须先重查相关排除项；递归枚举其内部日志、投影、生命周期索引和资产存储，逐一证明由设备唯一 governor 注入。若外层已有 permit，还须展开到叶节点确认无嵌套、无持锁等待，禁止用外层治理代替内层设施所有权。 | 2026-07-30 独立功能审查：生产 `AssignmentStreamSpool` 触发 X23-11，正常与遗留两类内部 `FileAuthorityCommitLog` 均未注入 governor，重开 U24-06；同轮反查其余本批新生产设施，未发现第二个同根实例。 |
| L24-05 | U24-18；冻结准备与独立审查通过后由最终包测发现 | 派生产物闭包只从已变更文件反推，未从受影响包的 golden 测试枚举全部行为/结构基线并执行只读预检，导致未变更但已过期的派生资产不在冻结文件集。 | 冻结前对每个受影响包枚举 golden/schema/snapshot/结构基线测试；即使基线文件当前未变更，也须运行只读预检。发现 mismatch 后先生成并审阅差异，合法才纳入交付闭包，禁止留到最终包测首次发现。 | 2026-07-30 最终验证：server 两份分布式运行时 golden mismatch，触发 U24-18；同轮检查 server golden 目录仅此两份。 |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| V24-01 | 合同 schema 与受影响包静态闭包 | core `lint:contract-schemas`；core/executor/owner-kernel `tsc --noEmit`；CLI 同命令用于核对已知基线 | 旧反证基线 | 历史最终验证 / 低 / 约 1 分钟 | 旧基线曾通过；当前生产装配输入已变化 | `head+working-tree-diff-v1:0159fa1cfd8bba38a1e563c495f7e608586051e08997d77227e967055f0eb797` | 失效 |
| V24-02 | 当前交付物可完整构建 | `pnpm build`，17 包串行依赖构建 | 旧反证基线 | 历史最终验证 / 高 / 191.7 秒 | 旧基线 17/17 构建成功；当前输入已变化 | 同 V24-01 | 失效 |
| V24-03 | U24-07/09/10 的账本、取消、兼容与结构门禁 | executor 定向运行 assignment/job/data-plane 三文件及兼容用例 | 旧反证基线 | 历史最终验证 / 中 / 约 1 分钟 | 旧基线通过；当前生产 owner 与路由输入已变化 | 同 V24-01 | 失效 |
| V24-04 | S6 真实生产拓扑与负组合 | CLI S6 conformance 定向运行 conversation/job 的 local/remote、first-party/channel 与两条负组合 | 旧反证基线 | 历史最终验证 / 中 / 约 2 分钟 | 旧基线通过；当前组合根输入已变化 | 同 V24-01 | 失效 |
| V24-05 | 共享摘要与数据面终态观察 | core interaction-display；executor data-plane registry 定向用例 | 旧反证基线 | 历史最终验证 / 低 / 约 10 秒 | 旧基线通过；仅作历史证据 | 同 V24-01 | 失效 |
| V24-06 | CLI 全包既有红项归因与当前修复 | 对旧全包红灯逐项定向复跑 | 旧反证基线 | 历史最终验证 / 中 / 约 2 分钟 | 旧基线无未归因红项；当前输入已变化 | 同 V24-01 | 失效 |
| V24-07 | 格式与差异卫生 | 旧 changed-files Biome；`git diff --check` | 旧 20 文件变更 | 历史最终验证 / 低 / 秒级 | 旧基线通过；当前文件集已变化 | 同 V24-01 | 失效 |
| V24-08 | U24-07 生产 owner、域路由、晚注册恢复与生命周期直接闭包 | CLI 定向 6 文件：access surface、executor owner/lifecycle、assignment operations router、job recovery、channel coordinator | 当前相关生产文件与直接测试 | 集中修复直接验证 / 低 / 约 20 秒 | 6 文件、24/24 通过；CLI 类型检查只复现既有 8 项凭据错误且本轮新增错误为 0 | 当前相关子集；从属冻结指纹 `9ac174f4…` | 诊断 |
| V24-09 | 冻结前派生资产与差异卫生 | `pnpm --filter @zhixing/core lint:contract-schemas`；对 125 个非 Markdown 交付文件执行 `.\node_modules\.bin\biome.cmd check @files`；`git diff --check` | 127 个冻结交付文件中的合同、schema、源码、测试与 lint | 冻结准备 / 低 / 约 30 秒 | 合同 schema lint 通过；Biome 125 文件通过且零修改；diff check 通过 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | 有效 |
| V24-10 | 当前冻结交付物可完整构建 | 独立执行 `pnpm build`；启动前确认零同闭包验证进程；硬截止 600 秒 | workspace 生产源码、配置与 lockfile；两份 test golden 不在构建输入闭包 | 最终验证 / 高 / 250.6 秒 | U24-17 修复后的生产输入 17/17 构建成功；U24-18 只改变测试 golden，构建输入闭包未变，证据继续有效 | 生产输入从属冻结指纹 `770cb579…` | 有效 |
| V24-11 | 九个受影响包的类型闭包 | 构建结束后串行执行 `pnpm --filter <pkg> exec tsc -p tsconfig.json --noEmit --pretty false`，`<pkg>` 依次为 core、owner-kernel、orchestrator、executor、runtime-host、rpc、server、channel-feishu、cli；CLI 只允许既有 8 项凭据错误 | 九包源码 + V24-10 构建产物；两份 test golden 不在类型输入闭包 | 最终验证 / 中 / 约 3 分钟 | core、owner-kernel、orchestrator、executor、runtime-host、rpc、server、channel-feishu 零错误；CLI 仅复现既有 8 项凭据错误，本轮新增为 0 | 生产输入从属冻结指纹 `770cb579…` + V24-10 产物 | 有效 |
| V24-12 | 七个有测试脚本的受影响包全测 | 构建与类型检查后，逐包从包目录直接执行 `node node_modules/vitest/vitest.mjs run --reporter=verbose --reporter=json --outputFile.json=<workspace>/.tmp/unit24-validation/<pkg>.json`；顺序 core→owner-kernel→orchestrator→executor→channel-feishu→server→cli；每包首次结果用于定向归因，禁止为查看详情重跑整包 | 七包源码、测试、当前上游 dist 与冻结交付物 | 最终验证 / 高 / 已完成 | core 2611 + 4 隔离通过；owner-kernel 207/207；orchestrator 431/431；executor 485 + 3 隔离通过；channel-feishu 72/72；server 746 + 2 修复后直接门禁通过，合并为 748/748；CLI 首次全包 2552/2567，15 项均按运行手册在独立进程定向通过，合并为 2567/2567 | 冻结指纹 `770cb579…` + V24-10/11 | 有效 |
| V24-13 | 最终差异、派生闭包与冻结指纹一致 | 复核派生产物闭包；`git diff --check`；按冻结口径重算 129 文件路径集和 blob manifest，两次计算必须分别等于 `532dc1d8…` 与 `770cb579…`；读取登记后清理 `.tmp/unit24-validation` | 同一冻结交付物与最终机器结果 | 最终验证 / 低 / 约 1 分钟 | 派生闭包复核与 `git diff --check` 通过；重算 129 文件路径集 `532dc1d8…`、manifest `770cb579…` 均与冻结值全等；机器结果读取登记后已清理，Git 无临时残留 | 冻结指纹 `770cb579…` | 有效 |
| V24-14 | U24-13～U24-16 合并直接验证 | executor 恢复投影定向 4 项；CLI worker 恢复/停机 4 项、生产 owner 4 项、装配顺序 3 项、S6 local/remote job 2 项；CLI 类型检查对照既有基线 | 统一恢复投影、worker/owner 生命周期、两类生产组合根与直接测试 | 集中修复 / 中 / 约 3 分钟 | 17 项直接测试通过；唯一 5 秒耐久 IO 超时按 runbook 单例以 15 秒预算 1/1 通过；CLI 类型检查仅复现既有 8 项凭据错误，无本批新增错误 | 冻结指纹 `0e2c636a…` 的直接相关子集 | 有效 |
| V24-15 | U24-06 隐藏日志治理与互斥区重驱闭包 | executor `assignment-stream-spool.test.ts`；CLI `executor-data-plane-runtime.test.ts`；core 外层互斥单例；core 合同 lint | production spool、正常/遗留日志、共享 commit-log、唯一 governor 与直接测试 | 集中修复 / 中 / 约 2 分钟 | spool 28/28、data-plane 3/3、外层互斥重驱 1/1、合同 lint 通过；生产构造扫描仅一处且同一 governor 透传到两类日志 | 冻结指纹 `7062f610…` 的直接相关子集 | 有效 |
| V24-16 | U24-18 两份 server 派生基线与当前生产事实全等 | 显式生成后逐字段审阅；无更新模式运行 behavior/structure golden；当前三文件 Biome 与全差异 `git diff --check` | server 行为/结构 golden、RPC connection/server.info 及全仓生产包图扫描 | 冻结准备 / 低 / 约 3 分钟 | 行为三字段、结构 contract 与 19 条计数变化均有具体生产来源；基线漂移分期完成；只读门禁 3/3、Biome 与 diff check 通过 | 冻结指纹 `770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8` | 有效 |

## 终审记录

| 轮次   | 审查侧重                                       | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论   |
| ------ | ---------------------------------------------- | ------------ | -------- | ---------- | ------ |
| 第一轮 | 需求、架构、功能闭环、状态、回归               | 否           | U24-07 同根反证 | `d3f2d7e2dc9dcaf3da1349bfa15d42a806b79ccbed778c2516d18b08a8d6e6fe` | 冻结准备未通过，未计轮次 |
| 冻结终审一 | 需求、架构、功能闭环、状态、回归             | 是           | 0        | `git-clean-blob-manifest-v1:0e2c636a9f1351d04e56f95c898fe16de80e00a850871c17a219a6037b3764b9` | 通过，八项均 1/2 |
| 冻结终审二 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 是           | 0        | `git-clean-blob-manifest-v1:0e2c636a9f1351d04e56f95c898fe16de80e00a850871c17a219a6037b3764b9` | 通过，八项均 2/2 |
| 重开后冻结终审一 | U24-06 根因、共享日志消费者、生产装配、资源与跨项组合 | 是 | 0 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | 通过；R24-02/03/04/05/07/08 为 1/2，其余项复用 2/2 |
| 重开后冻结终审二 | 崩溃、背压、资源上界、异常终态、关闭与跨区组合 | 是 | 0 | `git-clean-blob-manifest-v1:7062f610ffe509301c2b12872c7d625219f3b6a78a5c743757f0d6c607aa8d88` | 通过；八项均为 2/2 |
| U24-17 冻结终审一 | 三域最终性响应类型、快照边界、wire/游标兼容与跨项组合 | 是 | 0 | `git-clean-blob-manifest-v1:1abf365c38589910cfef63e02e6c62e5399e765edd8200c656cadac1d4da450f` | 通过；R24-06、R24-08 为 1/2，其余项复用 2/2 |
| U24-17 冻结终审二 | 别名隔离、序列化值、旧/新最终性分支、异常终态与跨区组合 | 是 | 0 | `git-clean-blob-manifest-v1:1abf365c38589910cfef63e02e6c62e5399e765edd8200c656cadac1d4da450f` | 通过；八项均为 2/2 |
| U24-18 冻结终审一 | 两份 golden 的生产来源、结构/行为映射、基线漂移归因与派生闭包 | 是 | 0 | `git-clean-blob-manifest-v1:770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8` | 通过；R24-05、R24-06、R24-08 为 1/2，其余项复用 2/2 |
| U24-18 冻结终审二 | 逐项反查未知差异、包图越层、兼容弱化、通知背压与跨项组合 | 是 | 0 | `git-clean-blob-manifest-v1:770cb57975fce447f04e1f291d02e77d19099acb13291a2856f9d506102f99f8` | 通过；八项均为 2/2 |

## 独立审查覆盖表

> 本表只记录独立审查覆盖进度，执行规则见工作台静态区“独立功能审查”。本表按失效机制划分，禁止照抄审查结论复用表；必须常设跨区组合核查行。状态只允许“待审”“审查中”“已覆盖”“失效”“有问题”。

| 编号 | 风险区与风险面 | 登记输入与指纹 | 独立覆盖状态 | 结论与证据 | 重开条件 |
| ---- | -------------- | -------------- | ------------ | ---------- | -------- |
| I24-01 | 高风险：权威身份、凭证反绑、唯一事实源与安全拒绝 | 当前 129 文件冻结指纹；IR24-01～13、16 与协议/owner/executor 完整链 | 已覆盖 | U24-18 仅同步只读派生基线；协议、身份、权威记录、签名域、作用域及对应生产/消费入口未变，既有独立结论继续有效 | 协议、身份、权威记录、签名域、作用域或生产/消费入口变化 |
| I24-02 | 高风险：耐久恢复、资源上界与隐藏物理副作用 | 当前 129 文件冻结指纹；IR24-03、17～20、X23-03/X23-11 与适用 L23/L24 检测动作 | 已覆盖 | U24-18 未改变日志、索引、spool、治理或恢复边界；既有独立结论继续有效 | 日志/索引/spool/资源治理或恢复边界变化 |
| I24-03 | 高风险：生产装配、拓扑必建 owner、生命周期与包边界 | 当前 129 文件冻结指纹；IR24-14、15、21、22、结构 golden 及全部适用生产拓扑 | 已覆盖 | 结构 golden 的 contract 与 19 条引用计数已逐项反向映射到生产源码；结构门禁确认零环、零未声明依赖、server/executor 零互相 import，CLI 仍为唯一产品组合根 | 组合根、角色/profile、依赖注入、结构 golden 或生命周期变化 |
| I24-04 | 高风险：跨区组合——身份 × 恢复 × 资源 × 拓扑 × 验收 | I24-01～03 当前结论、两份 golden 及 IR24-24 反向对账 | 已覆盖 | 行为字段、可观测通知入队、包图与既有身份/恢复/资源合同组合无冲突；129 文件全部归项，L24-05 已执行，零新增问题 | 任一风险面边界、输入、状态或结论变化 |

<!-- registration-complete: unit-24.gen-1 -->
