# 单元登记:第 38 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:38
- **generation**:1(仅经用户显式授权递增;同单元的修复、复审、终审轮复位是本文件内的状态推进,不产生新登记)
- **登记时间**:2026-08-14
- **登记来源**:用户要求将第 38 单元独立审查及价值裁决后的全部当前问题转入正式问题列表

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、审查结论复用表、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:U38-13～U38-18 的专项实现、最小必要验证和同一冻结指纹四路冷启动对抗均已完成，六项更新为“已验证”；U38-01～U38-12 与 EX38-01～EX38-03 结论继续复用；独立审查清单受影响的 11 项已转为`[~]`并作废旧证据，尚未进入全单元终审或单元提交验证
- **连续无新增问题轮数**:0 / 2
- **交付物是否冻结**:是（仅冻结 U38-13～U38-18 专项交付物；全单元交付物须待 11 项`[~]`独立复审后另行冻结）
- **交付物文件集**:33 个专项冻结交付文件：core release/FileLock 生产与直接测试；CLI automatic update、managed service、app/device removal 生产与直接测试；server strict RPC/context 生产与直接测试；三份架构/规格/维护指南、最终验收账、release tooling/build/evidence 与 S7 派生检查
- **当前交付物指纹**:U38-13～U38-18 专项 `sha256:f7cc7f1074c5ee3b09dae73a31f179c073d17ada4e7c0f70307051cfc42f13df`（33 个生产、测试、发布脚本及直接架构/规格文件逐文件 SHA-256 后按路径排序再聚合；排除 workbench 状态文件）；全单元指纹待后续独立复审后重冻
- **架构来源**:`research/design/modules/distributed-runtime/distributed-runtime-charter.md`、`research/design/modules/distributed-runtime/always-online-and-local-execution-requirements.md`、`research/design/modules/distributed-runtime/specification.md`、`research/design/modules/distributed-runtime/s2-security-supply-chain-review.md`、`research/design/modules/distributed-runtime/release-and-maintenance-guide.md`、`research/design/modules/distributed-runtime/unit-38-final-acceptance-ledger.json`及已定稿开发清单D38-01～D38-10

## 固定边界

- **功能范围**:S10升级兼容、五目标发布与最终验收；交付stable-only自动更新、ProgramStore安全切版与恢复、应用移除、离线诊断、旧路径退役、发布工具和最终验收闭包
- **架构不变量**:现有release identity、ProgramArtifact、ProgramStore、DeviceLifecycleJournal、DeliveryAuthority、current authority和bound inactive server是唯一事实与执行边界；更新不阻塞启动和首个输入，已接纳工作不丢失，坏candidate不公开ready，current始终可安全使用或恢复
- **验收条件**:五目标真实安装/更新/恢复/移除闭合；切版、迟到delivery、两根health、pure surface和故障重启唯一收敛；P0/P1清零后按既定冻结终审、独立功能审查和单元提交验证完成封版
- **必要上下游**:直接复用第30～37单元已封版的owner/transfer/checkpoint/trust/exposure/supervisor/stop/removal合同，不重做其内部实现
- **明确不属于本单元**:自动failover、quorum、多活、全局或持续同步、恢复应用；多发布通道、灰度和企业策略；独立updater daemon；远程批量管理和应用商店分发；通用updater/lifecycle/manifest/storage/secret/诊断/遥测框架；benchmark、内部诊断采集、真实私钥、商店账号和外部发布动作

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| ------------------------ | ------------------------- | ---------------- | ---- |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |

## 审查结论复用表

> 每行一个可独立失效的完整功能或合同事实链，生产端、事实源、消费者、异常终态和测试不得拆开；无法独立指纹、独立失效或需重读多数其他项时合并。整表须覆盖固定边界、全部交付文件、关键原语和九类核查面。
>
> 常设一项跨项组合推演。其他项均已取得或复用本轮结论后，再审查组合项；组合项按编号汇总各项当前输入指纹与结论。任一其他项新增，或其边界、输入指纹、结论变化时连带失效。
>
> 只有覆盖全部登记输入且该项结论无问题的问题盘点或冻结终审可计次，每轮每项至多一次，证据列须引用审查轮及证据；某项发现问题只清零该项，同一输入达到 2/2 后才可持续复用。状态只允许“待审”“审查中”“通过”“失效”“有问题:编号”，独立深审只允许“0/2”“1/2”“2/2”。

| 编号 | 审查目标与核查面 | 登记输入（关键实现、全部生产点、消费路径、测试） | 最近通过的输入指纹（算法 + 值） | 重审条件 | 当前状态 | 有效独立深审 | 本轮结论与证据 |
| ---- | ---------------- | ------------------------------------------------ | ------------------------------- | -------- | -------- | ------------ | -------------- |

## 问题列表

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U38-01 | **P0/大。** 正式 installer 仍把首次安装、全等重放和已有安装升级共用为直接 `stage→activate→stable surface→receipt`；它只以 installer receipt 判反降级，未把已验 current pointer 纳入同一高水位，也未将 existing update 交给 accepted-work、exact-host 与 candidate health 生命周期。既有五目标程序树、稳定 bootstrap/launcher/remover 与首次安装闭包已验证，本次只重开 existing-install 安全更新及安装级反降级。**价值裁决记录（完整转入）：**原结论为 P0/大；对立核查确认签名正确不能替代设备侧安全点和健康门，保持现状、删要求或只改 receipt 均会留下可达坏切版，复用既有 ProgramStore、ProgramUpdateReceipt、current-host RPC handoff 与 lifecycle 是唯一完整且不新增第二生命周期的方案；评级与工作量不变。仅当产品明确取消 existing-install update、installer 只允许 first/exact replay 时重开范围裁决。最终结论：保留；用户体验达标、架构达标。 | installer 把 first/replay/upgrade 混为同一本地切版效果；verified current pointer 与安装 receipt 没有先形成一致高水位，安装完成也未与 lifecycle terminal 共享提交边界。 | 生产端：五目标 installer、ProgramStore current/stage/version、安装 receipt、稳定 bootstrap、authenticated local RPC 与 upgrade lifecycle。类型组合：first/exact replay/existing update、无显式 home、旧 sequence、同 precedence 异 identity、pointer/receipt 同步或任一领先、Unicode/空格路径。消费者：稳定 launcher、自动/显式更新、app remove 后重装与 release smoke。异常终态：高水位冲突或倒退在程序写前拒绝；stage、handoff、pointer、terminal、receipt 任一效果/响应丢失及连续重启不得公开坏版、误认重放或改写运行中的 bootstrap。测试：五目标正式 installer 穿过真实 ProgramStore/current-host/lifecycle，逐一覆盖上述分类和窗口。受影响审查项：IR38-02、IR38-06～IR38-08、IR38-12、IR38-16、IR38-25、IR38-30～IR38-31。 | 写前同时验真 current manifest/pointer 与安装 receipt：同 target 下只有 SemVer precedence 与 sequence 同向严格递增才判较新，全等 identity 才判同一；一维相等另一维变化、交叉排序或同 precedence 异 identity/bytes 均 fail-closed。无 current 的 first 与 pointer/receipt identity 全等 replay可直接安装或修复既有稳定 bootstrap；其他 existing 状态只 stage，并通过既有本机认证 current-host 连接（无宿主时复用现有按需启动）调用 `server.update.prepare`，禁止 installer 直接切 pointer或覆盖运行中的 bootstrap。pointer 已为 candidate 但 receipt 落后时，只能由 exact active/terminal lifecycle 历史补齐；无对应历史则按 first-install exact read-back，否则 fail-closed。只有同 operation upgraded terminal 且 current 全等才提交安装 receipt；handoff 不可达或失败保留旧 current/receipt 与可重驱 stage。完成：五目标全部分类及响应窗口下反降级、重放和恢复唯一收敛，无 installer 第二 lifecycle。 | 已验证 |
| U38-02 | **P0/中。** lifecycle phase 仍把“已发效果”当 achieved：stage rename 到 version 后恢复只查 stage；两根在同一 bound server 激活及真实 loopback health 前写 health/terminal；previous pointer 写回后未实际由稳定 launcher 启动、验活就终结，而当前 launcher 又只 spawn 一次，target/previous 切换会停在下一次人工启动前。**价值裁决记录（完整转入）：**原结论 P0/中；对立核查排除了只补测试或降低恢复合同，源码存在 rename、pointer、health 和 launcher 接力的可达断点；复用 ProgramStore、同一 DeviceLifecycleJournal、bound server、health RPC 与稳定 launcher 的 pointer-generation 接力即可闭合，无需新 phase、协调器或第二生命周期；评级与工作量不变。仅当升级不再承诺崩溃续做、自动恢复或健康后才发布时重开范围裁决。最终结论：保留；用户体验达标、架构达标。 | phase 未以 exact stage/version/current/运行 health 的 achieved fact 推进，稳定 launcher 也没有消费 pointer generation 变化完成 target/previous 进程接力。 | 生产端：ProgramStore stage/version/pointer/previous、稳定 launcher、两根 bound server、loopback health、journal terminal/receipt/release。类型组合：stage/version/current/previous，anchor/executor×managed/on-demand/foreground，rename/pointer/activate/health/terminal/receipt 各窗口、identity 漂移与连续重启。消费者：startup resumer、稳定 launcher、CLI/server status 与自动恢复。异常终态：只补未达步骤；坏 target 不得 ready，previous 未真实启动健康不得 restored/terminal，`health-verified+`不得重复运行效果。测试：真实 ProgramStore、生成 launcher 与两根 production root 逐 phase/响应窗口注入，穿过真实 loopback。受影响审查项：IR38-10、IR38-12～IR38-18、IR38-20、IR38-27、IR38-29～IR38-31。 | 让现有 phase 先做 exact achieved read-back：accepted target 可从全验 stage、version 或 current 恢复，rename/pointer 已实现只补 journal，冲突 fail-closed。生成的稳定 launcher 在 child 退出后仅当 `current.json` generation 已变化才重读并 spawn 该 exact version，同 generation 绝不重启。两根以 lifecycle gates closed 装配并在同一 bound server 激活 router、尚未发布 ready/开放业务前调用现有 loopback health；target 全等才 `health-verified→upgraded terminal→receipts→release→ready`。target 失败只恢复 exact previous pointer并退出，launcher 按新 generation 启 previous；previous 同样经 loopback health 后才写 rolled-back terminal/receipt/release/ready。完成：两根三形态、全部 phase 与连续重启唯一前滚，坏版零 ready，恢复完成即旧版已真实可用。 | 已验证 |
| U38-03 | **P0/中。** upgrade会close gate、freeze十owner并drain，却未复用既有`installLifecycleAdmission/onFrozen`；冻结source在gate→freeze后产生的delivery不保证进入settlement，已接纳结果可在耐久投递前被升级停止。最终结论：保留；用户体验达标、架构达标。 | 升级只收束owner快照，没有把frozen causal source与其迟到delivery绑定到同一operation并共同封闭。 | 生产端：ProgramUpgradeCoordinator、十owner freeze、七类source companion、DeliveryAuthority admission/sealed。类型组合：source空/非空、enqueue前后、late/fresh、三运行形态及重启。消费者：delivery pipeline、upgrade work-settled/ready与最终用户结果。异常终态：响应丢失、source终结、delivery重试/uncertain和连续重启下不得漏项、重复或早ready。测试：十owner×七source直接场景、barrier竞争与真实authority read-back。受影响审查项：IR38-12、IR38-26～IR38-27、IR38-31。 | 让ProgramUpgradeCoordinator在现有freeze回调处把artifact中的七类exact source与当前delivery安装进同operation的DeliveryAuthority admission；source owner仍按冻结`kind/id/revision`收束，delivery owner最后seal并read-back全部terminal后才允许`work-settled`。startup在任何producer恢复前按operation+artifact+phase恢复同一admission/sealed；fresh或冲突source在父提交前拒绝，迟到delivery恰一绑定原operation。完成：十owner、七source、enqueue前后、uncertain、响应丢失和连续重启下集合共同封闭且零早ready。 | 已验证 |
| U38-04 | **P1/中。** ProgramUpdateReceipt 仍是唯一更新事实，但 current-authority 方法域没有运行期状态变化提示，REPL 在连接后只读一次；读取又把 authority unavailable 折成 `visible:false`，doctor 因而覆盖本机已有行动；并发检查的 program-lock busy 还会被普通失败路径写回 receipt。既有 topology、pure projection、sticky action 与 success token 边界已验证，本次只重开消费活性、离线回退和 busy 终态。**价值裁决记录（完整转入）：**原结论 P1/中只覆盖单次读取和 doctor；新事实确认 lock busy 误报同属公开投影终态，从源 P38-06 合并；方案只扩 update 方法域及内部 availability 判别，不建设通用事件或轮询框架，评级与工作量不变。若自动更新改为仅显式命令且权威需求同步删除可见性时重开。最终结论：改写并保留；用户体验达标、架构达标。 | 同一 receipt/pointer/lifecycle/health 投影缺少连接内失效提示；“可达但无可见状态”和“current authority 不可达”未分型；正常 writer 竞争未从失败分类中排除。 | 生产端：后台/周期 controller、ProgramStore receipt、lifecycle achieved/terminal、current-authority update 方法域和连接 generation。类型组合：三形态、surface/empty、下载/等待/安装/成功/失败、断线/换代、authority unavailable、本机/远端 authority 的离线 doctor、lock busy、notice consume。消费者：REPL 输入区、CLI/server status、doctor 与第一方 surface。异常终态：状态变化及时可见；断线/旧连接零陈旧投影；离线不把非current本机receipt冒充authority；busy 不写失败；success token 只消费同代。测试穿过真实 connection/registry/mesh relay、ProgramStore、topology identity 和 doctor。受影响审查项：IR38-08～IR38-09、IR38-11、IR38-19～IR38-22、IR38-29～IR38-30。 | 复用现有 core-host notification，在 `server.update.status` 成功后把该 authenticated connection 登记到 update 方法域的窄订阅集；receipt 提交及会改变投影的 lifecycle achieved/terminal 后只向这些 exact connection 发无事实载荷的 `server.update.changed`，connection close/换代原子移除。surface 在初连、通知和换代后重读同一 status。内部读取显式返回 available-empty/available-visible/unavailable；doctor 仅在 topology read-back 证明本机就是 current authority 时用同一纯 builder作本机 effect-free 回退，否则报告稳定 unavailable 行动，绝不展示非current本机receipt。program-lock busy 返回当前 receipt（缺失则保持原展示）且零失败写。完成：健康静默，下载/等待/失败及时显示一个行动，pure surface 仍零 updater/home/listener。 | 已验证 |
| U38-05 | **P1/中，历史已验证范围。** anchor/executor 共用的 release/protocol/static-v1-schema/trust/home/role/endpoint snapshot 与 bound inactive server 身份已闭合；本轮发现的“激活、真实 loopback health 与 terminal 的先后关系”已按同根 achieved-phase 事实并入 U38-02，不在本行重复维护。原价值裁决继续成立：当前 v1 无 schema activation 扩面价值。 | 两根 health 输入身份分叉这一既有根因已经解决；运行效果何时足以推进 lifecycle 属 U38-02。 | 生产端、组合及测试仍限于两根共享 snapshot 的 exact identity 和坏 candidate 零业务分派；若 snapshot 字段、两根 builder 或 bound endpoint 合同变化则本行重开。受影响审查项：IR38-15、IR38-29～IR38-31。 | 继续复用同一纯 health snapshot；不在本行新增激活协调。U38-02 修复后以真实激活/loopback achieved fact消费本行 snapshot。完成：两根 snapshot 输入全等，错 identity 仍 fail-closed。 | 已验证 |
| U38-06 | **P2/小。** decoder接受2GiB总工件，下载、JSON/base64解码和展开会产生多份内存占用且无磁盘预检；当前没有五目标正式工件，也无证据表明真实产品接近该上限，实际损失是异常包或低容量设备的一次安全失败。**价值裁决记录（完整转入）：**原结论把实现自设的2GiB上限当作必须支持的产品场景，并据此要求流式解码、分页清理等中型基础设施；没有真实正式包或用户需求支持该前提。新决定改为事实化包体上限与容量预检，删除通用流式/清理扩面；若正式最小包体本身超过目标设备可安全处理的上限，再以真实产物和设备容量重开。最终结论：由P1/中降级并改写；用户体验达标、架构达标。 | 协议资源上限由宽泛实现常量决定，未与实际五目标正式产物和设备临时空间约束共用。 | 生产端：ProgramArtifact decoder、download/stage与release:check。类型组合：五target真实最大包、超限包、容量充足/不足、断流续传。消费者：updater、ProgramStore和current host。异常终态：超限/不足须在stage/pointer前失败且current不污染。测试：真实最大产物、上限边界、空间不足和续传。受影响审查项：IR38-04、IR38-09、IR38-17。 | 用一个版本化ProgramArtifact有限策略记录五目标最终包实测最大archive/expanded bytes与固定headroom，protocol decoder、release:check和updater共用，删除2GiB宽泛常量。下载前在ProgramStore所在卷按partial+artifact+最坏展开+durable temp预检，artifact验签解码后再按文件总量精确复验，继续复用4MiB range续传；任一不足在stage/pointer/lifecycle accept前结束。完成：五目标最大正式包可安装，边界外包和容量不足零current污染，内存随有限正式包上界而非任意2GiB增长。 | 已验证 |
| U38-07 | **P2/小。** `FileLock`已有 token、heartbeat、reclaim claim 与 compare-token release，但跨进程 active 判断只看 PID；崩溃后 PID 被无关长寿命进程复用时，已计算的 mtime age 即使超过 `staleMs`也被丢弃，锁会永久 busy。**价值裁决记录（完整转入）：**原独立结论同时包含 lock busy 写失败，已同根合并到 U38-04；本行只保留 PID reuse，仍为 P2/小且不新增租约服务。若生产平台提供不可复用进程身份或 OS 持有锁证明，则重开并优先替换本启发式。最终结论：改写后保留；用户体验达标、架构达标。 | `isLockOwnerActive()`把跨进程 PID 存活当充分条件，未消费调用点已取得的 heartbeat age；reclaim 前后的 owner 判定因此与既有 stale 合同分叉。 | 生产端：core FileLock acquire/heartbeat/reclaim/release；组合：同进程 token、跨进程 fresh owner、crashed owner、复用 PID、主锁/reclaim/claim、stale release；消费者限于本批验证的 program update。异常终态：fresh owner 不被抢、失主后恰一接替、旧 token 不删新锁。直接验收：真实 child process 控制 PID/liveness 与 mtime，覆盖 fresh/stale、reclaim 竞争、owner 崩溃和 release 响应丢失。受影响审查项：IR38-09。 | 保留 `activeTokens` 同进程优先；把各调用点已读取的 mtime age 传给同一活性谓词，跨进程仅 `PID alive && age<=staleMs` 为 active。heartbeat stale 时仍须先取得现有 `.reclaim` claim，再全等重读 owner token、age 后删除；release 继续 compare-token。完成：fresh owner 单飞，crash/PID reuse 后恰一接替，竞争者与 stale release 零误删，program current/stage/receipt 无交叉写。 | 已验证 |
| U38-08 | **P1/中。** workspace gate、固定 50 id 与摘要绑定已验证，但 release 工具仍只读取调用方填写的 target、`passed:true` 与十二项布尔 JSON；仓库只有 smoke 名称 exact-set，没有把每一名称绑定到真实候选安装路径的 target-local producer，source fingerprint 也未覆盖这些 release scripts。**价值裁决记录（完整转入）：**原结论还主张 cryptographic attestation、通用 runner 与防恶意发布者设施；对立核查删除这些扩面，只保留真实 OS/arch 候选执行链及其输入绑定这一提交断点，仍为 P1/中。若权威外部流水线以后提供等价真实系统结果及稳定证据合同，再据此重开。最终结论：保留但收窄；用户体验达标、架构达标。 | release gate 只验证声明/摘要自洽；十二个 scenario id 未绑定固定生产命令、隔离根和结果生成点，producer/gate脚本也不在被证据反绑的输入闭包内。 | 生产端：五目标签名/公证后 candidate bytes、target-local producer、十二项固定 smoke、release:check及其脚本输入。组合：正确/错误 OS+arch、manifest/artifact digest、单项失败/缺失、producer 重放、smoke 后 bytes/source/script 漂移。消费者：50 行 ledger、candidate index 与发布维护者。异常终态：未执行、错目标、错 bytes、缺格、失败或脚本漂移均零新 report/零 publish。测试：每目标 producer 的真实隔离程序根/home、12 行固定映射、摘要漂移与确定性重放。受影响审查项：IR38-06、IR38-24～IR38-25、IR38-28、IR38-30～IR38-31。 | 在现有 `release-tooling` 固定 `scenarioId→生产入口/参数/终态谓词` 十二行，并新增单一 `release-target-evidence` 脚本：从 `process.platform/arch`唯一推导 target，错 target 在运行前拒绝；自己读取最终 signed manifest/artifact并算digest，在隔离program root/home依固定顺序穿过正式 installer、launcher、update/doctor/app-remove/device-remove入口，逐行只由真实退出与终态生成结果，最后原子写带producer version、OS/arch、manifest/artifact digest和十二行结果的canonical report。将 producer/tooling/check 脚本、release guide与ledger纳入同一 source fingerprint；`release:check`只消费该report及既有外部签名/公证证据，并在publish前复验五目标、digest与输入未漂移。完成：调用方不能用裸`passed:true`代替执行；不新增通用runner、远程证明或外部发布。 | 已验证 |
| U38-09 | **P1/小。** stable release codec 与根版本生成器接受 prerelease/build，但共享 `compareReleaseSemver()` 用 `Number` 比 core、再按整串字典序处理后缀：超安全整数会精度丢失，数字 prerelease 次序错误，build metadata 也错误参与 precedence；codec 还把带前导零的数字 prerelease 误称为 canonical。**价值裁决记录（完整转入）：**原结论 P1/小；对立核查排除“stable 禁止全部后缀”，因为会无价值收窄已公开合同且 minimum rollback 仍需同一语义；修正唯一 parser/comparator 最小且无结构债务，评级与工作量不变。仅当全部 codec、生成器和文档统一收窄为纯 `major.minor.patch` 时重开。最终结论：保留；用户体验达标、架构达标。 | 版本 codec 与 precedence 各自解析同一字符串且都未完整实现 SemVer 2.0，造成接受语法、排序和 exact identity 三套事实分叉。 | 生产端：release codec、根版本生成器与共享 parser/comparator。组合：任意长度 core 数字、prerelease 数字/非数字/长度/前导零、无 prerelease、build metadata。消费者：release advance、installer/update upgrade/replay/downgrade、minimum rollback、lifecycle outcome 与 receipt 高水位。异常终态：非法版本首副作用前拒绝；same precedence 异 exact identity 仍由 sequence+receipt/manifest 冲突拒绝。测试：SemVer 2.0 规范正反例、超安全整数和全部直接消费者。受影响审查项：IR38-02、IR38-08～IR38-09、IR38-16。 | 让现有 codec 与 comparator 共用一个窄 SemVer parser：core 与数字 prerelease 用十进制字符串长度+字典序比较，不转 `Number`；数字 prerelease 禁前导零，数字低于非数字，逐项比较后短列表低，无 prerelease 高于有 prerelease，build 完全忽略 precedence。releaseVersion 原串仍是 exact identity，releaseSequence 与 receipt/manifest 继续拒绝同 precedence 异 identity。完成：所有 producer/consumer 对规范正反例、升级/重放/降级/rollback 得出唯一结果。 | 已验证 |
| U38-10 | **P1/小。** 合法 SemVer build metadata 会生成不可读的 ProgramStore current pointer：release codec 与共享比较器接受 `1.0.0+build.1`，但 `activateStaged()` 把原始 `releaseVersion` 拼入 version 目录，pointer codec 与生成的 stable launcher 只允许 `[0-9A-Za-z._-]+`。**价值裁决记录（完整转入）：**原结论为 P1/小并按 digest 分离 storage identity。对立审查已依次排除保持现状、禁止 build metadata 和只放宽 `+`：保持现状会让当前 codec 已接受的合法 stable 版本在 pointer 写后不可启动；禁用会让发布入口、比较器、manifest 与安装合同产生无用户价值的特例；放宽字符仍保留公开版本身份与物理路径耦合。新决定为保留 P1/小与原方案；该方案同时达到用户体验（合法发布不会把安装变成不可启动）和架构（已验 digest 作为唯一物理身份）最优。仅当全部权威输入和消费者统一取消 build metadata 支持、且不存在既有可读 pointer 时才重新裁决。最终结论：保留；用户体验达标、架构达标。 | 公开 release exact identity 被直接用作文件系统 storage identity，ProgramStore、pointer codec 与 stable launcher 对同一合法版本使用了不一致的路径合同。 | 生产端：release codec、ProgramStore stage/version/current/previous、pointer codec和生成launcher。类型组合：canonical core/prerelease/build、first/exact replay/existing update、target/previous、rename/pointer效果或响应丢失与连续重启，以及既有合法pointer目录。消费者：installer、upgrade lifecycle、stable launcher、cleanup及恢复。稳定身份：raw version+sequence+target+manifest digest仍是release exact identity；完整manifest digest的64位小写hex仅是新version目录identity。线性化点：`current.json`耐久写入及全等回读。异常终态：合法版本不得在rename/pointer后变为不可读；非法或identity冲突输入零pointer污染，重放只消费同一物理目录，旧pointer无需迁移仍按其已存directory读取。测试：真实ProgramStore和生成launcher穿过上述分类与窗口。受影响审查项：IR38-02、IR38-07、IR38-10、IR38-16、IR38-27、IR38-30～IR38-31。 | 在ProgramStore内用一个窄helper从已验manifest digest派生完整64-hex filesystem-safe version目录；raw SemVer只保留在manifest/pointer identity字段。新stage→version、read-back、current/previous、cleanup均消费该目录；生成launcher继续只消费pointer中的exact directory，不自行重算。保留pointer codec对既有安全目录的读取，禁止迁移或改写历史pointer。完成：合法core/prerelease/build在first/replay/update、target/previous、rename/pointer响应丢失及连续重启后均可启动和恢复；同digest唯一，旧pointer可读，非法/冲突输入零pointer污染。 | 已验证 |
| U38-11 | **P1/小。** 默认 no-genesis 单机按设计没有 mesh trust，但生产 `localIsCurrentAuthority()` 只比较 `trust.issuer.deviceId` 与 `localDeviceId`，host absent 时恒判 false，离线 doctor 因而不能证明本机就是 current authority；同时现有`loadCurrentManagedServiceState("inspect")`在读取configuration后仍要求既有SecretStore binding，无binding的默认单机在得到拓扑结论前就失败。**价值裁决记录（完整转入）：**原结论为 P1/小并复用 current-state 纯事实。对立审查已排除保持误报、要求 host 在线和新增 authority 机制：前两者会让默认单机在最需要诊断时失去真实行动，后者超过当前损失；现有 configuration/trust/localDeviceId 已足以无副作用地区分单机、配对 current 和不可判定。新决定为保留 P1/小与原方案；用户体验达到离线仍给唯一真实行动，架构达到复用单一事实投影且不新增 owner。仅当默认 no-genesis 单机或离线 doctor 从受支持产品场景中正式移除时才重新裁决。最终结论：保留；用户体验达标、架构达标。 | current-authority 本机回退用“已建立 home trust”替代单机/配对 topology 判定，且把SecretStore读取放在“无mesh配置”这一充分正事实之前，混淆了拓扑证明与凭据投影。 | 生产端：唯一configuration loader、current-state loader、trust/localDeviceId、doctor本机回退与update纯投影。类型组合：默认no-genesis单机（有/无config文件、binding/key均可缺失）、paired current/non-current、trust/binding/key缺失或损坏、host absent和离线重试。消费者：`zz doctor`及同一update行动投影。稳定身份与线性化点：一次只读configuration快照；无mesh即本机单机current，mesh存在时由同快照、existing-only trust与localDeviceId共同证明exact current anchor。异常终态：单机离线展示真实failed-safe/action-required与唯一行动；非current或歧义本机不得冒充authority；全程零网络、零配置创建、零SecretStore activation、零lifecycle效果。测试：真实临时home覆盖全部组合。受影响审查项：IR38-11、IR38-19、IR38-29～IR38-31。 | 在现有current-state模块增加窄的本机authority纯证明：先用唯一`loadConfig({homeDir,noAutoCreate:true})`取得一次快照；`mesh===undefined`立即判本机current且不访问SecretStore。mesh存在时把同一快照交给现有inspect内部加载，保持existing-only，要求binding/key/trust有效、trust issuer全等localDeviceId且有效角色包含anchor；缺失、损坏、漂移或非current均投影unavailable。doctor只在current-authority RPC unavailable时消费该证明并复用原update projection。完成：默认单机无binding仍给真实行动，paired current可回退，paired non-current/坏trust诚实unavailable，所有分支零写。 | 已验证 |
| U38-12 | **P1/中。** 十二项 target-local smoke 实际测试 workspace 源码而非已签候选程序：`release-target-evidence.mjs:runScenario()`虽使用candidate Node，入口仍是仓库Vitest与`packages/cli`测试文件；合同中的正式`entry`/`terminal`只参与摘要，所有行还复用独立基础probe的同一candidateExecutionDigest。**价值裁决记录（完整转入）：**原结论为 P1/中并让固定十二行直接执行候选程序。对立审查已排除把 workspace 全测加基础 candidate probe 视为充分证据，以及把十二行降为文档性门禁：归档/装配后的候选可在workspace源码全绿时缺失或损坏update、doctor、stop、RPC路径，当前门仍会签出passed；这正是发布门必须阻断的现时损失。新决定为保留 P1/中与有限十二行方案；用户体验达到被发布的就是实际验证过的程序，架构达到证据反绑候选bytes且不建设通用runner。仅当另一个同等直接、逐target且反绑exact候选bytes的发布门已经覆盖十二个用户终态时才重新裁决。最终结论：保留；用户体验达标、架构达标。 | target evidence producer 把“由候选 runtime 启动的workspace测试进程”误当作“候选程序行为”，固定合同、执行器和report都没有逐行反绑实际候选可执行bytes、entry、argv与耐久终态。 | 生产端：五目标signed baseline/candidate、release-target-evidence、固定十二行合同及release:check。类型组合：五目标OS/arch、十二journey、workspace与candidate一致/分叉、候选单项损坏/缺格、report重放及source/script/candidate漂移。消费者：50行ledger、release report、candidate index和发布维护者。稳定身份：target+signed manifest/artifact+version-local runtime/entry bytes+normalized argv+terminal read-back；每行execution digest独立，顶层digest只聚合基础probe与十二行结果。线性化点：全部十二行及基础probe通过、输入复验未漂移后原子写report。异常终态：候选任一正式入口或终态损坏必须使对应行失败并零新report/publish；workspace源码通过不得替代候选证据。测试：隔离program root/home执行正式launcher/installer/CLI/RPC，并以只篡改candidate的负向场景证明门禁。受影响审查项：IR38-25、IR38-30～IR38-31。 | 保留固定十二行且不建通用runner：把`testFile/testName`改为strict `id/entryKind/argvTemplate/terminalKind` exact-set，由同脚本内仅覆盖十二项的固定switch执行；每项只启动隔离根中的候选version runtime、stable launcher/installer或候选CLI/RPC公开入口，并以release流水线的exact signed baseline/candidate构造所需本地场景，禁止导入repository/Vitest。每行从实际executable bytes、解析后argv、公开输出/退出和耐久read-back生成独立command/result/execution digest，顶层聚合后由现有release gate复验。完成：五目标十二项逐格通过；删任一候选入口、改变候选行为或终态而workspace不变时对应行必失败，缺格/重放/输入漂移均零report/publish。 | 已验证 |
| U38-13 | **P0/小。** `collectProgramFiles()`用`localeCompare()`生成 ProgramArtifact 文件序，production `validateProgramArtifact()`却用字符串 ordinal `>=`验 canonical order；同一 release tooling 的 source/package fingerprint 行仍各自使用 locale collation。五目标真实树只要含大小写、下划线、隐藏目录或 Unicode 路径，即可让 producer bytes 被 release:check、installer 与 target-local smoke 的 production decoder 稳定拒绝。**价值裁决记录（完整转入）：**保持现状直接阻断 stable 发布；复用同一 ordinal predicate 即可闭合，无需新增框架。最终结论：保留 P0/小；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**ProgramArtifact 与 provenance 都在排序同一类 canonical path 行，若只修文件表仍会让 U38-14 的 source/package identity 随 locale 漂移；新决定把三处排序一起收束到同一窄谓词，评级、工作量和范围不变。 | canonical path order 是多执行点合同，但 producer、decoder 与 provenance fingerprint 没有消费同一 ordinal predicate。 | 生产端：`collectProgramFiles`、source/package fingerprint、ProgramArtifact 编码、production decoder、release:check 与 installer。类型组合：五目标真实依赖树，混合 ASCII 大小写、下划线、隐藏目录、Unicode/规范等价外观路径、exact replay与不同 locale。消费者：tree receipt、manifest source/package identity、签名工件、发布报告、installer 与 target-local smoke。异常终态：producer 自身排序不合法或输入漂移必须在 tree/report/publish/install 效果前拒绝；同一输入在任意 locale 产生同一 bytes/digest。测试：构造混合路径树，穿过真实 tree→artifact→production decoder→五目标发布门，并差分 source/package fingerprint。受影响审查项：IR38-04、IR38-24、IR38-25、IR38-30～IR38-31。 | 在 core release protocol 导出唯一 ordinal path comparator，`collectProgramFiles`、source/package 行排序和 `validateProgramArtifact` 全部复用；producer 写 artifact 前调用 production validator。补混合 ASCII/Unicode、隐藏目录、大小写与多 locale 反例并重建五目标证据。完成：同一树只有一种 canonical 顺序与 provenance digest，五目标 producer bytes 均被同一 decoder/installer exact 接纳，非规范输入零 report/publish。 | 已验证 |
| U38-14 | **P1/中。** `program-tree-receipt.json`只绑定 target/runtime/app tree/channel，未绑定构树时的 source/package closure；`fingerprintSourceTree()`又遗漏实际的`build-program-tree.mjs`与`build-release-artifact.mjs`等 producer。artifact builder 在树完成后才计算当前摘要，因此旧 tree 或旧 report 可被重新归因给新源码。**价值裁决记录（完整转入）：**原结论为 P0/中；签名 final bytes、strict decode 与十二项 smoke 仍 fail-closed，未证明每次构建都会立即伤害用户，因此 P0 过高；但受支持发布流程确可错误归因，改为 P1/中。重开 P0 条件：出现已生成或已发布的跨 source tree/report，或安全修复因此未进入签名 bytes。最终结论：降级；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**仅给末端 report 加摘要不能证明 tree 由该 closure 生成；新决定在构树前冻结并逐段全等复验同一 closure，producer 列表仍是有限 release 脚本 exact-set，不扩成远程证明。 | source/package closure、实际 producer 集合、tree receipt 与末端 report 各自在不同时间取样，签名 bytes 缺少贯穿构树到发布门的同一 provenance identity。 | 生产端：source/package fingerprint、program-tree builder/receipt、ProgramArtifact builder、target evidence 与 release:check。类型组合：source/package/任一实际 producer 单独变化、旧 tree/report 重放、构树后输入漂移、五目标 candidate/baseline。消费者：签名工件、manifest、smoke report、candidate index 与发布维护者。异常终态：任一 closure/producer digest 不全等时拒绝复用 tree/report并零新 publish；签名 bytes 必须能反查构树时同一冻结 closure。测试：分别只改 source、package、tree/artifact/evidence/check producer，旧 tree/report 均失效；输入不变重放产生同一 identity。受影响审查项：IR38-24、IR38-25、IR38-31。 | 定义唯一有限 release-producer path exact-set并纳入现有 source fingerprint；构树前计算 source/package digest，写入 strict tree receipt，artifact builder、target evidence 与 release:check各自重算并全等复验，写 report/publish 前再复验输入未漂移。完成：五目标签名 bytes、receipt、manifest与report贯穿同一 closure，旧 tree/report 不能冒充当前输入，且不新增第二事实源或远程证明。 | 已验证 |
| U38-15 | **P1/小。** `startAutomaticUpdateCheck()`返回`void`并丢弃 AbortController/promise；managed 入口只返回清 timer 的同步 disposer，anchor cleanup与executor teardown因此都不能拥有、abort或 await 已启动轮次。stop、应用移除或 host 换代返回后，旧轮次仍可继续 network、download、stage、handoff或 receipt 写入。**价值裁决记录（完整转入）：**该行为破坏 stable 自动维护的明确生命周期语义；复用现有入口并返回唯一轮次句柄即可闭合，不需 updater daemon。最终结论：保留 P1/小；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**只暴露 AbortController 仍会让 teardown 与最后写入竞争；新决定把 controller、exact promise和新轮次准入共同封装为 O(1) handle，cleanup 必须 await，评级与范围不变。 | 周期调度 owner 与单轮执行 owner 分离，既没有“最多一个 active round”的稳定 identity，也没有 cleanup 完成的线性化点。 | 生产端：自动更新 timer、单轮 controller、anchor/executor 两根及 managed/on-demand/foreground cleanup。类型组合：无轮次/单轮/连续 tick，fetch/download/stage/handoff/receipt 各窗口，stop/remove/host generation换代、abort效果/响应丢失。消费者：host shutdown、应用移除、ProgramStore/FileLock、旧版安全可用状态与 update projection。异常终态：cleanup 先拒新，再 abort并等待 exact round 到稳定终态；完成后零旧代网络/stage/receipt效果，stale completion不得清除后继。测试：两根三形态穿过真实 controller、ProgramStore与 cleanup 顺序，证明 active handle、listener与promise恒 O(1)。受影响审查项：IR38-08、IR38-27、IR38-29～IR38-31。 | 让现有单次与 managed 入口返回同一种 active-round handle，内部最多持有一个`controller+promise`；所有 tick 经同一 closed/active gate，已有轮次时不再启动。`stop()`先永久关闭该 handle、清 timer，再 abort并 await exact promise，finally仅清同代引用；两根 teardown 均 await 后才继续释放依赖。完成：正常周期与失败投影语义不变，停机、移除和换代返回时零在途旧代效果且资源恒 O(1)。 | 已验证 |
| U38-16 | **P1/中。** FileLock record只有`pid/token/createdAt`；`isLockOwnerActive()`把 heartbeat stale 直接等同于可接替，即使该 PID 仍是原进程。系统睡眠或进程暂停超过 staleMs 后，后继取得 reclaim claim便会删锁，旧进程恢复仍可继续其受保护写，形成双 owner；仅看 PID 又无法区分崩溃后的 PID reuse。**价值裁决记录（完整转入）：**真实可达的双写会破坏更新恢复与旧版可用性；保持 busy 优于冒险接替，复用现有 FileLock record 即可闭合，无需租约服务。最终结论：保留 P1/中；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**heartbeat 只证明调度新鲜度，不能撤销仍存活的 exact 进程；新决定将“active freshness”和“可安全 reclaim”分开，身份读取不确定时 fail-closed，仍不引入 fencing/lease。 | lock 缺少可跨进程复核的 process-birth identity，活性谓词把“心跳不新鲜”误当“原执行主体已消失”。 | 生产端：FileLock主锁、`.reclaim`、`.claim-*`的 acquire/heartbeat/reclaim/release，以及 ProgramStore/update controller 同 home 单飞。类型组合：live/crashed/sleep/pause、heartbeat fresh/stale、PID reuse、birth读取成功/失败、owner取得后崩溃、claim/release响应窗口和双进程竞争。消费者：partial download、stage、pointer、receipt与cleanup。异常终态：exact 活进程即使心跳 stale也不得被接替；仅证明记录中的 PID+birth 已不存在或已换代才可删锁；未知身份保持 busy，旧 token release零误删。测试：三平台窄 identity provider与真实 child process覆盖暂停、崩溃、PID/birth mismatch、legacy record及旧 token。受影响审查项：IR38-09、IR38-27、IR38-31。 | 在既有 LockRecord 增加 strict process-birth identity，并把 Linux `/proc`、macOS `ps`、Windows固定只读进程创建时间查询收束为 core 内唯一窄 resolver；主锁、reclaim与abandoned claim共用。heartbeat stale只触发身份复核，exact PID+birth仍活或无法证明时保持 busy；身份消失/换代后才在既有 reclaim claim内全等重读 token+birth并删除，release继续 compare-token。完成：暂停 owner单飞，崩溃/PID reuse后恰一接替，legacy/读取失败fail-closed，零租约服务或第二锁。 | 已验证 |
| U38-17 | **P1/中。** 应用移除先把 managed future launch 从 enabled 改为 disabled，再执行 current stop；`prepareProgramRemovalManagedService()`只返回成功提交 closure，stop/refuse/timeout或后续 exact unregister失败时没有 rollback，且 SecretStore binding 缺失会静默当作“无托管事实”。用户看到“未移除”时自动启动可能已被改变，或 remover 可能在启动项不可判定时删除程序。**价值裁决记录（完整转入）：**失败必须保持应用可用且公开结果与真实安装状态一致；复用现有 managed-service adapter、definition read-back 与 stop 即可闭合，无需新 lifecycle。最终结论：保留 P1/中；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**不能承诺外部 manager 永远允许补偿；新决定要求本操作只补偿自己造成的 enabled→disabled，并在补偿无法证明时保留程序、返回稳定可行动错误，禁止伪报原状。 | future launch与current host是两个独立系统事实，当前一次性编排没有冻结原 enabled 状态、区分本操作是否改变它，也没有 commit/rollback 的 exact read-back边界。 | 生产端：app remove、managed-service adapter、safe stop、unregister与remover handoff。类型组合：managed/on-demand/foreground，future enabled/disabled/absent/不可判定，definition exact/drift，disable/stop/reenable/unregister效果与响应丢失，stop refused/error/timeout。消费者：稳定 launcher、系统自动启动、当前 host、程序树 remover与用户结果。异常终态：任一 pre-commit失败只对本操作造成的 enabled→disabled执行 exact re-enable；原先disabled不得误启；事实不可判定或补偿未证实时保留程序并给稳定修复行动；仅current已停且future exact absent才handoff。测试：三平台真实 adapter/read-back与stop结果逐窗覆盖。受影响审查项：IR38-21、IR38-30～IR38-31。 | 将现有 prepare结果收窄为操作内 handle，冻结 exact spec/definition digest、before state与`changedByThisOperation`；adapter增加只启用同一已存在定义、绝不install/start/rewrite的`enableFutureExact`。remove流程在stop及commit任一失败时调用并read-back rollback，仅 changed=true才启用；prepare自身响应丢失也须read-back后返回handle或先恢复再抛。managed事实无法证明absent时首效果前fail-closed；成功只在stop安全终态且unregister read-back absent后handoff。完成：失败不会悄悄改变可证实的原future状态，补偿不确定时程序保留且结果诚实，成功时current/future/program三事实共同移除。 | 已验证 |
| U38-18 | **P1/小。** `removeDevice()`在获得 durable accept 后才选择模式；交互选 cancel、确认拒绝、非交互缺模式或本地 pre-decision异常都会直接抛出，未调用现有 durable abort；`lost`又在任何强确认前直接提交。于是 issuer abort未耐久、gate跨重启保持 active，用户的“取消”与真实 operation分叉。**价值裁决记录（完整转入）：**原方案要求 accept 前再确认；U37 的 accepted snapshot 是可逆冻结且为准确展示本机工作所需，前置重复确认增加摩擦而不增加安全。新决定保留 preflight accept，只修复所有退出分支的 durable abort 与 lost 强确认。仅当 accept 本身新增不可逆效果时重开前置确认。最终结论：改写并保留 P1/小；用户体验达标、架构达标。**价值裁决记录（本次收敛补记）：**离线 target无法立即证明自身gate已恢复，故不伪造“已取消”；同时当前`device.continue(cancel)`只按 targetName 寻找 active operation，无法安全处理 accept 响应丢失或同名旧操作。新决定给既有 cancel 分支增加 exact operationId 变体，仍复用同一 journal/abort reducer，既有按唯一target继续旧操作的入口不变。 | UI/pre-decision退出没有映射到同一 accepted operation 的既有 durable abort，取消入口又缺少调用方已知 operationId 的全等约束；危险 lost确认位于不可逆提交之后。 | 生产端：device remove/continue CLI、strict RPC、issuer accepted operation、target gate、强确认与既有abort reducer/mesh replay。类型组合：transfer/destroy/lost/cancel，交互/非交互，accept前纯输入错误、accept效果/响应丢失、accept后模式取消/确认拒绝/pre-decision异常、同target旧/新operation、target在线/离线、abort效果/响应丢失及连续重启。消费者：owner admission、removal resume、用户可见状态与target-local smoke。异常终态：accept后的退出只能给调用方生成的exact operationId写issuer-abort；错/旧operation零取消；在线取得cancelled，离线诚实显示取消已登记待设备确认；同cancel重放幂等且不得创建新operation。lost在提交前必须通过一次强确认。受影响审查项：IR38-22、IR38-30～IR38-31。 | 能在accept前判定的纯输入错误先拒绝；保留交互preflight accept。在既有`device.continue(cancel)`增加仅cancel可带的`targetName+operationId` strict变体，remove入口必须使用，authority全等核对该target唯一active operation后才复用现有 signed issuer-abort；原有后续continue按唯一target恢复的形态不变。accept响应丢失、模式cancel、确认拒绝及未发送不可逆decision的本地异常均重投同一exact cancel。在线等待target aborted，离线只在issuer-abort读回后返回pending-cancel并由现有startup/mesh重放。lost/destroy在continue前强确认，cancel不再二次确认。完成：取消只作用于本次operation，跨重启阻止后续不可逆决策并最终恢复gate，响应丢失幂等，零第二事实源。 | 已验证 |

### U38-13～U38-18 问题收敛事实矩阵

> F38-18～F38-23只绑定当前六项问题的根因、执行合同与验收条件；本轮不修改实现、不运行构建或测试，也不替代后续修复验证、独立复审或单元提交验证。

| 矩阵 | 直接变体 | 稳定身份与唯一事实 | 效果顺序、零副作用边界与直接验收 |
| ---- | -------- | ------------------ | ---------------------------------- |
| F38-18 · canonical order | 五目标；ASCII大小写、下划线、隐藏目录、Unicode；locale变化；artifact/source/package exact replay | 已验path字符串原串及core唯一ordinal comparator；ProgramArtifact files、source rows、package rows只消费该谓词 | 先ordinal排序，再由production validator验唯一递增，才编码/摘要；同树跨locale bytes/digest全等，任一非规范序零tree/report/publish/install。真实producer→decoder→五目标release gate直接证明 |
| F38-19 · source/package provenance | source/package/任一producer单变；旧tree/report；构树后漂移；五目标重放 | 构树前sourceTreeDigest+packageGraphDigest、有限release-producer exact-set、strict tree receipt及签名artifact/manifest/report | 构树写receipt，artifact/evidence/check逐段重算全等，publish前末次复验；任一输入漂移零复用/零report。只改每类输入均使旧tree/report失效，不变重放identity全等 |
| F38-20 · automatic active round | 两根三形态；无/单/连续tick；fetch/download/stage/handoff/receipt窗口；stop/remove/换代 | 每个host generation唯一active-round handle，内部至多一个controller+exact promise；ProgramStore仍是唯一更新事实 | tick先过closed/active gate；stop先close+clear timer，再abort+await，exact finally后才释放依赖。cleanup返回后旧代零网络/stage/receipt，stale completion不清后继，资源O(1) |
| F38-21 · FileLock process identity | live/crashed/sleep/pause；heartbeat fresh/stale；PID reuse；identity成功/失败；主锁/reclaim/claim与stale release | token+PID+process-birth identity、mtime heartbeat、现有reclaim claim；heartbeat不是所有权撤销事实 | fresh快速busy；stale只触发birth复核。exact进程活或未知均busy；不存在/换代才在claim内全等重读后删除。三平台provider+真实child证明暂停不双写、失主恰一接替、旧release零误删 |
| F38-22 · app removal compensation | managed/on-demand/foreground；enabled/disabled/absent/unknown；definition drift；disable/stop/reenable/unregister各效果/响应窗口 | frozen exact spec+definition digest+before state+changed flag；manager read-back与stop result是双事实 | prepare只形成可回滚handle；任一commit前失败仅补偿本操作的enabled→disabled并read-back，原disabled零误启；unknown/补偿不确定保留程序并给行动。仅stop safe且future absent后handoff remover |
| F38-23 · durable device cancel | transfer/destroy/lost/cancel；交互/非交互；accept效果/响应丢失；同target旧/新operation；target在线/离线；abort响应丢失及重启 | client生成的operationId+targetName、issuer exact active identity、signed issuer-abort与target-aborted；journal仍是唯一事实 | 纯输入错尽早拒绝；accept后pre-decision退出以exact operationId写issuer-abort，错/旧id零取消。在线收敛cancelled，离线读回issuer-abort后显示pending并由现有startup/mesh重放；lost/destroy先强确认。重放零新operation、零不可逆旁路 |

### U38-13～U38-18 问题收敛四路冷启动对抗复审

> 四路均针对上表修正后未再变化的同一份问题列表，从权威合同与当前源码重新构造F38-18～F38-23反例；本记录只证明根因、方案与验收可直接执行，不表示问题已经修复。

| 角色 | 主动反例与直接交界 | 结论 |
| ---- | ------------------ | ---- |
| 五目标工件规范与发布provenance | 以大小写/下划线/隐藏目录/Unicode及不同locale攻击artifact、source/package三类排序；再分别只改source、package、tree/artifact/evidence/check producer并重放旧tree/report，核查U38-13↔U38-14及U38-01/U38-06/U38-08/U38-10/U38-12。 | 单一core ordinal comparator必须覆盖全部canonical path行，production validator是编码前门；构树前closure写receipt并由后续各段重算全等，才能证明签名bytes来自当前输入。两项根因相邻但不重复，方案未恢复远程证明、流式工件或范围外发布能力。 |
| 自动检查与FileLock单飞/清理 | 让两根三形态在fetch、download、stage、handoff及receipt窗口stop/remove/换代，同时注入timer重入、进程sleep/pause、heartbeat stale、crash、PID reuse、birth读取失败与旧token release，核查U38-15↔U38-16及U38-03/U38-04/U38-07。 | active-round handle只解决同一host generation内的轮次所有权，PID+birth只解决跨进程锁所有权，二者不可互代。close→abort→await与exact reclaim共同保证旧轮次/旧进程零后继效果；未知身份fail-closed，无daemon、租约服务、第二锁或通用lifecycle。 |
| 应用移除与永久设备移除可信终态 | 对app remove枚举原future enabled/disabled/unknown、stop拒绝/超时、definition漂移及补偿失败；对device remove枚举accept响应丢失、同target旧/新operation、cancel、确认拒绝、非交互输入错、lost、target离线、abort响应丢失与重启，核查U38-17↔U38-18及U37 stop/removal合同。 | app remove是同机非耐久命令补偿，必须只恢复本操作改变的future并在不确定时保留程序；device remove已有耐久operation，取消必须以调用方生成的operationId全等反绑后写同journal abort并诚实区分cancelled/pending，不能按targetName误取消旧操作。两者共享“结果匹配事实”原则但事实源和终态独立，未新增第二lifecycle或重复确认。 |
| 产品体验/范围价值与历史边界 | 独立重估六项当前损失、评级与小/中工作量；对账U38-01～U38-12、EX38-01～EX38-03、第30～37合同，以及远程证明、通用updater/lifecycle/runner、租约、流式cleanup与范围外发布。 | 六项都在受支持安装维护/发布路径存在可达失败；改文字、保持现状或只补末端测试均不能消除用户损失。各方案复用既有protocol、receipt、runtime handle、FileLock、managed adapter与device journal，用户体验和架构均达标；既有验证边界和EX38-03重开条件未变化。 |

反证处置：首轮第三路发现仅凭`targetName`取消无法在 accept 响应丢失或同target旧操作并存时证明命中本次 operation，已将 U38-18、F38-23 和交界结论改为`targetName+operationId` exact cancel；随后对该份未再修改的问题列表重新执行四路复审，零新增反证。

### U38-13～U38-18 修复、验证与专项冻结记录

- **专项冻结指纹**：`sha256:f7cc7f1074c5ee3b09dae73a31f179c073d17ada4e7c0f70307051cfc42f13df`；33 个非 workbench 交付文件逐文件 SHA-256 后按路径排序聚合。
- **构建与静态验证**：core/CLI 最小必要 typecheck 均通过；S7 entry coverage 20/20 与 golden 通过；当前源码后的 workspace `pnpm build`产物完成于 2026-08-14 14:18 UTC，晚于最后生产源码输入，core/server/CLI 产物均生成；`git diff --check`通过。
- **直接验证**：core release protocol 8/8；release tooling 7/7（另 1 个主机 symlink 条件跳过）；automatic update/runtime 与 managed host 直接集 36/36；FileLock 11/11（含真实 child）；app/device remove 命令直接集 8/8；server strict RPC 63/63；managed-service 13/13 及 Windows 实际 absent-definition 单例通过；device durable abort 两条真实 mesh/journal 单例分别 1/1。
- **验证边界**：未运行包全测、模块回归、target×故障笛卡尔积或外部发布；Linux/macOS process-birth 由平台窄 provider 合同覆盖，Windows 以真实进程覆盖，发布证据仍由五目标 target-local gate 负责。

| 矩阵 | 修复后唯一事实、线性化点与直接结论 |
| --- | --- |
| F38-18 · canonical order | core 唯一 ordinal comparator 同时驱动 ProgramArtifact producer/decoder 与 source/package 行；编码前走 production validator。混合大小写、下划线、隐藏目录、Unicode 与 locale 变化只产生一种 bytes/digest，非规范输入在 tree/report/publish 前失败。 |
| F38-19 · provenance | 构树前冻结 source/package digest，有限 release-producer exact-set进入同一 source closure；tree receipt、artifact、target evidence、release:check逐段全等复验且发布前重算，任一 source/package/producer 漂移拒绝旧 tree/report。 |
| F38-20 · active round | 单次与 managed 入口共用每 generation 唯一 controller+promise handle；tick先过 closed/active gate，stop按 clear timer→abort→await exact promise→同代清理线性化，两根 teardown 返回后旧代零网络/stage/receipt效果且资源 O(1)。 |
| F38-21 · FileLock | 版本化 strict PID+process-birth+token 是锁身份；heartbeat stale只触发三态身份复核，live/unknown保持 busy，仅 absent/mismatch在既有 claim 内全等重读后回收，旧 token release零误删。 |
| F38-22 · app remove | 操作内 handle 冻结 spec/definition/before/changed；失败只 exact 恢复本操作造成的 enabled→disabled并read-back，原 disabled零误启。无 binding 默认单机仅在 manager、definition和binding三事实持续 absent 时提交；不确定或补偿失败保留程序。 |
| F38-23 · device cancel | accept 后所有 pre-decision退出以 targetName+operationId exact cancel写既有 signed issuer-abort；错/旧 operation零取消，在线收敛 cancelled，离线读回 abort 后 pending 并由现有 mesh/startup 前滚；lost/destroy先强确认。 |

#### 同根反证账

| 编号 | 反证 | 处置 |
| --- | --- | --- |
| C38-C13 | source closure 原引用不存在的 root `tsconfig.json`，且会跟随 pnpm `node_modules` symlink，把宿主依赖误纳入或使合法构建失败。 | 同根合并至 U38-14：root 配置改为实际 `tsconfig.base.json`，closure 排除 node_modules/dist/coverage/.turbo；release tooling 直接测试复核通过。 |
| C38-C14 | 默认单机首次安装可能没有 SecretStore binding；若一律 unknown fail-closed，用户无法移除；若继续旧 no-op，又不能证明 future launch absent。 | 同根合并至 U38-17：以稳定 manager identity 只读证明 manager/definition absent，并在 commit 前复核 binding仍 absent；Windows真实 task-not-found同时覆盖 HRESULT 0x80070002/0x80070003，实际 absent 单例通过。 |
| C38-C15 | macOS `ps lstart`受本地时区/DST影响，跨环境重读可能改变同一进程 birth identity。 | 同根合并至 U38-16：provider固定 `LANG=C`、`LC_ALL=C`、`TZ=UTC`后解析，未知仍 fail-closed；FileLock 11/11复核通过。 |

#### 同一冻结指纹四路冷启动对抗

| 角色 | 独立重造与结论 |
| --- | --- |
| 五目标工件规范与 provenance | 重造混合路径、多 locale、source/package/任一 producer单变、旧 tree/report与构树后漂移；F38-18/F38-19由同一 comparator与冻结 closure逐段全等解释，C38-C13已闭合，零新增反证。 |
| 自动检查与 FileLock | 重造两根三形态连续 tick、各效果窗口 teardown、pause/crash/PID reuse/birth unknown、claim/release竞争；F38-20/F38-21分别封闭进程内轮次和跨进程锁，C38-C15已闭合，零双 owner或旧代效果。 |
| 应用移除与设备永久移除 | 重造 enabled/disabled/absent/unknown、definition drift、disable/stop/rollback窗口，以及 accept/abort响应丢失、旧新 operation、在线/离线与重启；F38-22/F38-23均以 existing exact事实前滚，C38-C14已闭合，零误启、误删或错取消。 |
| 产品体验、范围价值与历史边界 | 从失败可行动性、正常路径无额外摩擦和架构比例性重估：六项均消除当前可达损失且只复用既有 protocol/receipt/runtime/FileLock/adapter/journal；U38-01～U38-12、EX38-01～EX38-03与第30～37合同未变化，未引入远程证明、daemon、租约、第二事实源、通用框架或范围外发布。 |

**差异审计**：历轮 U38-13～U38-18 反证均以“同根合并”“当前源码证伪”或“修复后复核通过”闭合；C38-C13～C38-C15为本轮首次记录且均已耐久处置。上述专项功能判断和四路对抗绑定同一未修改指纹；修复验证不替代后续 11 项`[~]`独立复审。

### U38-10～U38-12 问题收敛事实矩阵

> F38-15～F38-17只收敛当前三项问题的事实、执行合同与验收条件；不修改实现、不运行构建或测试，也不替代后续修复验证、冻结终审或单元提交验证。

| 矩阵 | 直接变体 | 稳定身份与唯一事实 | 效果顺序、零副作用边界与直接验收 |
| ---- | -------- | ------------------ | ---------------------------------- |
| F38-15 · release/storage identity | canonical core/prerelease/build；first/replay/update；target/previous；rename/pointer响应丢失；旧pointer与连续重启 | raw SemVer+sequence+target+manifest digest是release exact identity；完整manifest digest 64-hex是新version目录identity；pointer中的directory是历史安装唯一寻址事实 | 已验stage按digest目录rename并全验后才写pointer；pointer写是切版线性化点。新目录不含公开version字符，旧pointer原样可读且不迁移；冲突输入零pointer污染。真实ProgramStore+生成launcher覆盖写、读、启动、cleanup和恢复 |
| F38-16 · 单机/配对离线authority | no-genesis无config/无binding；单机已有本地事实；paired current/non-current；binding/key/trust缺失或损坏；host absent与重试 | `loadConfig(noAutoCreate)`的一次快照是拓扑线性化点；无mesh是默认单机current正事实；mesh存在时只由同快照+existing-only trust issuer+localDeviceId+anchor角色证明current | 先判无mesh，单机分支零SecretStore；paired分支才existing-only读取，任一歧义稳定unavailable。全程零网络、零配置创建、零SecretStore activation、零lifecycle效果；doctor只复用同一update纯投影 |
| F38-17 · exact candidate十二journey | 五目标OS/arch；固定十二行；workspace/candidate一致或分叉；候选单项损坏/缺格；report重放及source/script/candidate漂移 | signed baseline/candidate、version-local runtime/entry bytes、resolved argv与terminal read-back逐行绑定；每行独立execution digest，顶层仅聚合 | 基础probe后由固定十二项switch只执行候选正式入口，零repository/Vitest；逐项真实终态通过且前后输入全等后才原子写report，release:check再复验。candidate-only mutation必使对应行失败并零report/publish |

### U38-10～U38-12 问题收敛四路冷启动对抗复审

> 四路对同一份未再修改的问题列表抛开既有结论，分别从权威合同与当前源码主动重造F38-15～F38-17全部适用反例；本记录只证明问题描述可直接执行，不表示问题已修复。

| 角色 | 主动反例与直接交界 | 结论 |
| ---- | ------------------ | ---- |
| release/storage identity与重放恢复 | 以`1.0.0`、prerelease、`+build`、同precedence异identity、first/replay/update、target/previous、rename/pointer响应丢失、旧version格式和连续重启攻击U38-10，并核查U38-01/U38-02/U38-09与U38-12发布身份交界。 | 完整manifest digest目录消除合法version字符与路径合同分叉；raw version仍负责exact identity与反降级，pointer仍是历史寻址事实。旧pointer无需迁移，新旧读取均由同一pointer codec/launcher消费；没有恢复禁用build metadata或新增第二事实源。 |
| 单机/配对topology离线投影 | 以默认home无config/无binding、no-genesis已有本地receipt、paired current/non-current、trust/binding/key缺失或损坏、host absent及重复doctor攻击U38-11，并核查U38-04、EX38-01与第30～37单一信任事实。 | 必须在SecretStore前用`noAutoCreate` configuration快照判定无mesh；paired路径才existing-only读取并以issuer+localDeviceId+anchor角色全等证明。该分序同时闭合默认体验和零写合同，坏trust不被误当单机，没有新增authority机制。 |
| 五目标exact candidate执行与发布门 | 让workspace测试全绿但删除/篡改candidate的update、doctor、stop、RPC入口，注入单行缺格、argv/terminal错绑、同一全局digest冒充逐行执行、report重放及source/script漂移，核查U38-08/U38-10。 | 固定十二项descriptor+switch必须只启动candidate程序并逐行绑定executable/argv/terminal；行级digest与顶层聚合、写report前输入复验共同排除workspace旁路。基础candidate probe继续复用但不能替代十二journey，无需通用runner、远程证明或运行时report事实源。 |
| 产品体验/范围价值与历史边界 | 独立重估三项当前损失、P1评级和小/中工作量；对账U38-01～U38-09、EX38-01～EX38-02、第30～37合同，以及schema activation、2GiB、通用updater/lifecycle/event/runner/诊断和范围外发布能力。 | 三项均在受支持场景存在可达失败，保持现状或改文字会让合法安装不可启动、默认单机离线失去真实行动或错误候选通过发布门；方案分别复用ProgramStore、current-state与固定release producer，用户体验和架构均达标。既有结论与排除重开条件未变化，三项边界互不遮蔽。 |

### U38-10～U38-12 修复、验证与专项冻结记录

> 专项冻结指纹：`sha256:b7327c1d48662df31fbb77383cc472ffa39565d1d6f1a2a9f81ea370a2ea20cb`。冻结文件限本轮 12 个生产、测试、发布脚本及直接架构/规格文件；随后仅维护本 workbench 与独立清单状态，未再修改冻结交付物。

| 矩阵 | 修复后生产事实与线性化点 | 直接证据与终态 |
| ---- | ------------------------ | -------------- |
| F38-15 · release/storage identity | `programVersionDirectory()`是 stage、version、current/previous read-back 与 cleanup 的唯一新目录派生点，只接受已验`sha256:`完整摘要并返回64位小写hex；raw SemVer、sequence、target与manifest digest仍留在manifest/pointer exact identity，launcher只读pointer目录。 | `1.0.0+build.1`真实stage→activate→verify与exact replay通过；既有安全目录pointer原bytes可读且零迁移。rename/pointer响应丢失继续由同一digest目录与pointer achieved事实恢复。 |
| F38-16 · 单机/配对离线authority | `proveLocalCurrentAuthority()`只读一次`loadConfig(noAutoCreate)`；无mesh在任何SecretStore访问前返回本机current，mesh存在才把同一快照送入既有inspect，要求existing-only binding/key/trust、issuer全等localDeviceId且effective anchor。doctor只在RPC unavailable时调用。 | 真实空home证明current且目录仍空；paired缺凭据、坏trust稳定false且零activation；真实paired current anchor为true。所有分支零网络、零配置创建、零lifecycle效果。 |
| F38-17 · exact candidate十二journey | 固定合同只含`id/entryKind/argvTemplate/terminalKind`，同脚本十二项switch只调用candidate installer、stable launcher/installer或candidate CLI/RPC；每行全等校验entry与公开argv，并由实际runtime+entry bytes、结果和递归耐久终态形成独立digest。signed baseline/candidate在producer和release gate双重验真，全部输入复验后才原子写report。 | 结构测试证明零Vitest/`.test`入口、12项实现exact-set、行digest不可伪换/缺格；candidate入口缺失、bytes漂移、argv/terminal不符、doctor写home或输入漂移均在report前失败。五目标真实执行由同一target-local producer在发布候选上完成，不以workspace测试冒充。 |

| 验证 | 当前输入结果 |
| ---- | ------------ |
| CLI直接合同 | `update-controller.test.ts`与`managed-service-runtime.test.ts`共25/25通过，覆盖build metadata目录、旧pointer、no-genesis、paired缺凭据/current与坏trust；CLI类型检查通过。 |
| 发布证据合同 | `release-target-evidence.mjs`与`release-check.mjs`语法通过；`release-tooling.test.mjs` 5通过/1宿主symlink能力跳过；S7 20/20与registry golden通过。 |
| 构建 | 当前CLI源码输入执行一次`pnpm cli:build`成功；反证C38-C12只修改发布脚本与其合同测试，未重复同输入构建。 |

| 冷启动角色 | 主动反例与直接交界 | 同一冻结指纹结论 |
| ---------- | ------------------ | ---------------- |
| release/storage identity与重放恢复 | 从core/prerelease/build、first/replay/update、target/previous、旧pointer、rename/pointer效果与响应丢失反推所有目录消费者，核查U38-10↔U38-12。 | 新目录只有digest helper一个producer；全部消费者只认pointer目录，旧安全目录继续可读。raw identity未被削弱，SemVer/build metadata合同与U38-01/U38-02/U38-09均未回退。 |
| 单机/配对离线authority | 从无config/binding、paired current/non-current、坏binding/key/trust、host absent和重试重新检查SecretStore前置与doctor消费顺序，核查U38-11↔U38-12。 | 无mesh充分正事实先于秘密读取；mesh分支所有歧义fail-closed，doctor只复用本地更新纯投影。未增加authority、网络、activation或第二事实源。 |
| exact candidate与发布门 | 从五target、十二入口、candidate-only mutation、缺格/重复、report重放及source/script/candidate漂移重造反例，并执行L38-03检测；首轮发现并闭合C38-C12后重新全量冷审。 | producer逐行绑定真实candidate runtime/entry、公开argv及实际终态，baseline/candidate与source/package均在publish前复验；workspace通过无法掩盖候选损坏，零通用runner或远程证明。 |
| 产品体验/范围价值与历史边界 | 独立重估合法版本可启动、单机离线有真实行动、被发布程序即被验证三项当前用户价值，并核查U38-01～U38-09、EX38-01～EX38-02、第30～37及范围外发布边界。 | 三项均用既有事实源和固定编排闭合，未禁用build metadata、未恢复schema activation、第二事实源、通用updater/lifecycle/event/runner/诊断框架或外部发布；EX38-01～EX38-02无重开事实。 |

差异审计：L38-01～L38-03均已在真实生产调用图执行；既有C38-C01～C38-C11继续以原修复后事实复用，C38-C12同根并入U38-12后复核通过；没有未处置或跨根反证。

### U38 六项问题收敛事实矩阵

> 本矩阵绑定专项冻结指纹`534e9a05e53e00daaf50d40ca4df8817940ab4817728f50cb23f7b32a9d933ce`；F38-09～F38-14均已从当前生产源码、稳定身份、耐久事实、线性化点、零副作用终态与直接证据逐格重建并通过，不覆盖全单元终审。

| 矩阵 | 直接变体 | 稳定身份与唯一事实 | 效果顺序、零副作用边界与直接验收 |
| ---- | -------- | ------------------ | ---------------------------------- |
| F38-09 · existing-install/反降级 | 五target；first/replay/update；无显式home；旧sequence；同precedence冲突；pointer/receipt任一领先；stage/handoff/terminal/receipt响应丢失 | signed manifest/artifact bytes、已验current pointer、程序根外installation receipt；同target的SemVer precedence+sequence共同高水位 | 两维非同向严格排序或identity冲突在程序写前拒绝；first与identity replay才可直接安装/修复bootstrap，其他existing一律stage→current-host RPC→既有lifecycle且不改运行中bootstrap；upgraded terminal与current全等后才提交receipt。真实五目标installer逐窗验证旧版持续可用、连续重启唯一前滚 |
| F38-10 · achieved phase/真实health | stage/version/current/previous；两根三形态；全部phase；rename/pointer/activate/health/terminal/receipt窗口与连续重启 | lifecycle operation、pointer generation、exact installed manifest、同一bound server及loopback health snapshot | 每phase先read-back；稳定launcher只响应新的pointer generation。target/previous均在closed gates、router已激活但ready未发布时做真实loopback health，成功后才terminal/release/ready；坏target零ready并自动接力previous，`health-verified+`零二次运行效果 |
| F38-11 · current-authority可见性 | 后台状态变化、在线/断线/换代、available-empty/visible/unavailable、本机/远端authority离线doctor、lock busy、notice消费 | ProgramUpdateReceipt及pointer/stage/lifecycle/health纯投影、topology identity、exact connection generation；notification非事实源 | status登记方法域订阅，receipt/lifecycle durable变化后只发失效提示，close/换代移除；仅确认本机current时走effect-free回退，否则诚实unavailable；busy零receipt写。真实registry/mesh relay/doctor验证无更新静默、失败一个行动、旧token不清新通知 |
| F38-12 · target-local发布证据 | 五目标OS/arch；最终candidate bytes；十二smoke；错目标、缺格、单项失败、candidate/source/script摘要漂移与重放 | process平台推导target、signed manifest/artifact digest、固定十二行scenario表、producer/tooling/check脚本输入、canonical target report | producer错target首效果前拒绝；在隔离program root/home穿过正式入口，结果只能由运行终态生成并原子写。release:check复验五目标exact-set、外部签名/公证证据、报告digest及前后全部输入，异常零report/publish |
| F38-13 · SemVer 2.0 | 任意长度core；prerelease数字/非数字/长度/前导零；release对prerelease；build metadata；所有排序消费者 | 共用canonical parser；releaseVersion原串为identity，precedence不含build | 非法输入首效果前拒绝；数字标识用字符串幅值比较避免精度丢失；同precedence异identity仍由sequence+exact receipt/manifest拒绝。规范向量及release/installer/lifecycle/minimum-rollback消费者结果全等 |
| F38-14 · FileLock PID reuse | 同进程token、跨进程fresh owner、crash、复用PID、主锁/reclaim/claim、heartbeat fresh/stale、stale release | lock token、mtime heartbeat、PID liveness、reclaim claim | 同进程token优先；跨进程active要求PID live且age未超界。stale只在取得reclaim claim并再次全等读token+age后删除；release compare-token。真实双进程证明fresh不被抢、失主恰一接替、旧释放零误删及program文件零交叉写 |

### U38 六项问题收敛四路冷启动对抗复审

> 四路均在专项冻结指纹`534e9a05e53e00daaf50d40ca4df8817940ab4817728f50cb23f7b32a9d933ce`上抛开修复结论重新构造F38-09～F38-14反例；零新增反证，以下结论只完成六项问题收口，不替代全单元独立审查。

| 角色 | 主动反例与直接交界 | 结论 |
| ---- | ------------------ | ---- |
| installer/反降级与phase/health恢复 | 重造五target的first/replay/existing update、无home、旧sequence、同precedence异identity、pointer/receipt任一领先及stage/handoff/terminal/receipt响应窗口；再从rename后stage消失、pointer切换、target/previous进程接力、两根三形态真实health与连续重启反推U38-01↔U38-02，并对账U38-05/U38-06及第30～37单元exact-host合同。 | current pointer与安装receipt必须构成同向共同高水位；只有first/exact replay可直接安装，existing update必须进入既有current-host lifecycle。每phase以exact achieved fact推进，稳定launcher消费pointer generation完成target/previous接力，两者均经真实loopback health后才terminal/release/ready。方案覆盖全部窗口且未增加installer第二lifecycle。 |
| current-authority可见性与锁终态 | 从后台更新期间无新输入、连接断开/换代、current authority不可达、本机并非current、doctor离线、success/action token竞争和program-lock busy反推U38-04↔U38-07；再注入live/crashed owner、PID reuse、heartbeat fresh/stale、reclaim竞争及旧token释放。 | update方法域的exact connection订阅只承载失效提示，事实仍唯一来自current-authority纯投影；topology不能证明本机current时诚实unavailable，busy零失败写。FileLock仅在PID存活且heartbeat新鲜时判跨进程active，stale接替仍受既有reclaim claim和compare-token约束，未引入通用事件、轮询或租约服务。 |
| 五目标证据与SemVer发布身份 | 从错OS/arch、调用方裸`passed:true`、十二smoke缺格/错绑、candidate bytes与producer/check脚本漂移、确定性重放反推U38-08；再以超安全整数core、数字/非数字prerelease、前导零、列表长度、build metadata及全部排序消费者反推U38-09，并核查U38-01/U38-06的identity与资源边界。 | 固定十二行必须绑定正式入口、参数和终态谓词，由target-local producer在隔离根穿过真实候选生成canonical report，producer/tooling/check及指南/账本同入fingerprint。codec与比较器共用窄SemVer parser，precedence忽略build但raw version仍为exact identity；同precedence异identity继续fail-closed。无需通用runner或cryptographic attestation。 |
| 产品体验/范围价值与历史裁决边界 | 独立质疑六项当前用户损失、评级、工作量与方案比例；对账U38-03/U38-05/U38-06、EX38-01～EX38-02、第30～37单元合同及v1 schema activation、任意2GiB、daemon、第二事实源、通用框架、report运行时耐久化和范围外发布能力。 | 两个P0、三个P1和一个P2均有受支持场景的可达损失，复用现有ProgramStore/lifecycle/current-authority/FileLock/release工具即可闭合，评级与工作量有事实依据。六项边界互不遮蔽，U38-03/U38-05/U38-06无需重开，EX38-01～EX38-02重开条件未满足，已否定扩面均未恢复；同一份问题列表通过四路复审。 |

### U38-01～U38-08 固定事实矩阵

> 以下矩阵、专项审查、四路对抗与验证证据均绑定前一专项冻结指纹，只保留为历史基线；当前状态和待处理范围以上方“当前状态”及“问题列表”为准，不得用历史通过结论覆盖本轮重开问题。

| 矩阵 | 覆盖变体 | 稳定身份与唯一事实 | 线性化点、零副作用终态与直接验收 |
| ---- | -------- | ------------------ | ---------------------------------- |
| F38-01 · 五目标安装与反降级 | 五target；首次、exact replay、更新、app remove；无home；Unicode/空格；旧sequence；同identity异bytes；各安装/移除响应窗口 | signed index/manifest/ProgramArtifact最终bytes；程序根外installer receipt的`target+keyId+releaseVersion+sequence+manifestDigest` | 完整树/launcher全等回读后提交receipt；旧/冲突输入在程序写前拒绝；remove只删程序层并保留receipt/home；五目标真实安装、启动、重放、更新、移除与12项smoke穿过正式installer |
| F38-02 · ProgramStore/lifecycle恢复 | stage/version/pointer/previous；各phase效果前后与连续重启；自动/显式恢复；terminal/receipt丢响应 | upgrade operation identity、accepted artifact、ProgramStore pointer generation及每phase achieved谓词 | exact read-back先于phase推进；已实现效果只补journal，未实现只执行一次，冲突fail-closed；恢复只回到冻结已验source且通过F38-05，坏candidate不成为previous；terminal receipt可由同operation历史补齐 |
| F38-03 · frozen source/delivery闭包 | 十owner、七source；enqueue前后；late/fresh；retry/uncertain；三形态；响应丢失与重启 | accepted-work artifact与DeliveryAuthority同operation admission/sealed | freeze后安装exact source/delivery，source逐项终结，delivery最后seal/read-back；startup在producer前恢复；fresh/冲突source零父提交，late delivery恰一进入原operation，集合全terminal后才`work-settled` |
| F38-04 · current-authority更新体验 | foreground/on-demand/managed；surface/empty；离线、失败、换代、重启；action-required、updated、restored | current authority上的receipt+pointer+stage+lifecycle+health纯投影；receipt notice token | topology后仅真实host启动updater；surface本机零副作用；失败不覆盖sticky行动，success/restore exact compare-and-consume；同一current anchor全部消费者固定文案、一个行动，旧token零误清 |
| F38-05 · 两根candidate health | anchor/executor；三形态；release/protocol/static-v1-schema/trust/home/role/endpoint正确或漂移；ready各窗口 | exact bound inactive server与两根共用的同代纯health snapshot | runtime装配完成、ingress激活前全等验证并耐久read-back；成功激活同一server对象，失败保持inactive并走F38-02；坏candidate零ready/零分派 |
| F38-06 · 工件资源上界 | 五目标最大正式包；archive/expanded边界；容量充足/不足；断流续传 | 版本化有限ProgramArtifact策略及实际artifact bytes/file sizes | 下载前按同卷最坏预算预检，验签解码后精确复验；超限/不足在stage/pointer/lifecycle前结束，current不污染；现有4MiB续传不变 |
| F38-07 · program lock单飞 | 慢下载、双进程、live/crashed owner、PID复用、接替、release响应丢失 | core FileLock token、heartbeat、owner liveness与compare-token release | lock取得是writer线性化点；live owner不可被mtime抢占，失主后恰一接替，旧owner/stale release不删新锁；真实双进程验证partial/stage/receipt/current零交叉污染 |
| F38-08 · release固定门禁 | workspace gate；50 id齐全/缺失/额外/重复/错分类；report响应丢失 | 固定50 id exact-set、五目标最终证据与当前source/package输入摘要 | 所有gate与逐id验证完成后才原子写passed/publish；任何旁路或异常零新passed；report可确定性重建且不升级为运行时耐久事实 |

### 同根反证账

| 编号 | 首次出现事实 | 归属 | 耐久处置 | 状态 |
| ---- | ------------ | ---- | -------- | ---- |
| C38-C01 | 冻结指纹冷审发现`ProgramStore.verifyExpanded()`只校验artifact列出的文件bytes，未枚举版本目录中的额外文件或符号链接；既有version可夹带未声明代码而通过stage/version achieved read-back。 | U38-02 | 在既有ProgramStore内对`program/`执行有限递归exact-set回读：实际文件与由artifact推导的目录集合必须全等，拒绝符号链接及其他entry；直接测试注入额外文件、目录和链接并证明pointer/lifecycle零推进。 | 修复后复核通过：真实ProgramStore额外目录/文件反例均fail-closed，pointer不推进 |
| C38-C02 | 冻结指纹冷审发现稳定`launch.js`/installer由旧bootstrap Node直接import candidate应用，且health不核对实际`process.versions.node`；Node 22 patch换代时会用旧runtime运行新版本并可能误标healthy。 | U38-01、U38-05 | 稳定loader只读取current pointer并spawn该version目录内已验runtime与固定entry；候选health把manifest Node版本与实际进程版本全等反绑，漂移保持inactive并进入既有恢复。 | 修复后复核通过：五target的app/installer loader均反绑version runtime，runtime漂移health拒绝 |
| C38-C03 | 冻结指纹冷审发现current-authority status只消费receipt，未对账pointer、已验stage、active lifecycle与candidate health；过期或伪造receipt可长期显示虚假安装/成功状态。 | U38-04 | 抽取同一纯projection builder；current-authority入口收集receipt、pointer、已验stage、lifecycle和health后一次投影，任一身份矛盾稳定转为action-required，所有公开消费者继续只读该入口。 | 修复后复核通过：真实ProgramStore一致事实显示success，伪receipt稳定转action-required |
| C38-C04 | 当前源码显示installer只以installation receipt判反降级并直接activate；pointer领先、receipt领先或pointer已指candidate但receipt响应丢失时，原方案没有唯一分类。 | U38-01 | 同根合并：写前验真pointer manifest与receipt，以precedence+sequence形成共同高水位；除first/exact replay外均进入current-host RPC/lifecycle，terminal后才补surface/receipt。 | 已并入F38-09及U38-01执行合同 |
| C38-C05 | 两根在`runServer()`前写health/terminal，且生成的稳定launcher只spawn一次；target或previous pointer切换后当前启动会退出而不会自动接力新generation。 | U38-02 | 同根合并：phase先read-back；launcher仅在pointer generation变化后spawn exact current；target/previous均在同一bound server激活后经loopback health才terminal。 | 已并入F38-10及U38-02执行合同 |
| C38-C06 | 十二项smoke只存在名称exact-set，`release:check`只读取调用方布尔JSON；原“增加producer”未说明每个scenario的生产入口和结果来源。 | U38-08 | 同根合并：固定十二行scenario到正式入口/参数/终态谓词，target-local producer从真实执行生成canonical report，release gate拒绝裸布尔声明。 | 已并入F38-12及U38-08执行合同 |
| C38-C07 | `compareReleaseSemver()`把core转`Number`且codec允许数字prerelease前导零；仅修后缀排序仍会在大数字及非法canonical输入上分叉。 | U38-09 | 同根合并：codec/comparator共用窄parser，以十进制字符串比较任意长度数字并严格执行prerelease canonical规则。 | 已并入F38-13及U38-09执行合同 |
| C38-C08 | existing update在terminal后若重新物化stable surface，会覆盖仍由旧进程消费的bootstrap；首次安装pointer已耐久但receipt响应丢失又可能被误判为existing update。 | U38-01 | existing update只提交exact terminal receipt，零stable surface覆盖；仅`current`全等且无`previous`的first-install响应丢失允许按真实bytes修复surface/receipt，直接测试覆盖两窗。 | 修复后复核通过 |
| C38-C09 | 同candidate active prepare与terminal receipt历史未形成可加入的唯一恢复入口，response loss后installer可能重复接纳或无法补receipt。 | U38-01、U38-02 | `prepare()`加入同一active operation，terminal receipt从同operation历史exact重建；ProgramStore current、manifest digest与receipt全等后才完成重放。 | 修复后复核通过 |
| C38-C10 | `server.update.status`若先读投影再订阅，投影读取与订阅之间的durable变化会永久丢失提示。 | U38-04 | 同一handler先按exact connection generation订阅，再读取投影；读取失败仅回滚本次新订阅，close/换代和stale completion不误删新代。 | 修复后复核通过 |
| C38-C11 | `flushed`旧host在cleanup后若不由原coordinator切pointer，stable launcher观察不到generation变化；`old-host-stopped`效果成功但响应丢失还会停在同phase。 | U38-02 | anchor/executor仅在全部cleanup成功且同一bound handle已inactive后调用原coordinator；journal响应丢失时重读原operation并恰一续做，pointer generation变化后才由stable launcher接力。 | 修复后复核通过 |

| C38-C12 | U38-12 第一轮冻结冷审发现 target evidence 的离线 doctor 场景只把递归 home 对比布尔值写入 terminal，却未在值为 false 时拒绝，候选 doctor 即使产生隐藏写入仍可生成该行 evidence。 | U38-12 | 同根合并：递归枚举隔离 `ZHIXING_HOME` 的目录与文件摘要，执行前后不全等立即失败并零 report；合同测试冻结该 fail-closed 分支。 | 修复后复核通过：脚本语法、固定 producer 合同及输入漂移检查通过，重新冻结后无同根残留 |

### 冻结指纹专项功能审查与四路冷启动对抗

| 角色 | 冷启动重造反例与直接交界 | 冻结指纹结论 |
| ---- | ------------------------ | ------------ |
| 专项功能审查 | 逐格从当前生产调用图重建F38-01～F38-08，复验release/receipt、ProgramStore/lifecycle、DeliveryAuthority、current-authority projection、两根health、容量、FileLock、release门的稳定身份、唯一事实、线性化点、资源上界和零副作用终态；直接对账C38-C01～C38-C03。 | 八个矩阵格均能由冻结源码与V38-01～V38-05直接证据推出；测试结果未替代功能判断，三项新增反证均已穿过production helper/store/root修复并复核，无未处置格。 |
| 五目标安装发布/反降级与资源边界 | 重新从五target首次、exact replay、旧sequence、同identity异bytes、无home、Unicode/空格、app remove后重装、超archive/expanded/file、容量不足、断流续传及50-id/gate异常推导F38-01/F38-06/F38-08；核查U38-01↔U38-06↔U38-08。 | `affe174…e0d8`上release/index/manifest/artifact/外置receipt唯一绑定；stable app/installer loader启动pointer所指version runtime，旧/冲突输入零程序写；有限资源策略由decoder/store/release门共用，固定50 id与workspace gate无passed旁路。 |
| ProgramStore切版恢复与candidate health | 抛开既有结论重造stage/version/pointer/previous各效果与响应窗口、额外entry、五phase连续重启、两根三形态ready窗口、Node/runtime/trust/home/role/endpoint漂移；核查U38-02↔U38-05及EX38-01。 | exact文件/目录布局和manifest/artifact bytes共同构成achieved read-back；phase只补未达效果，previous仍走同lifecycle；anchor/executor均在同一bound inactive server激活前消费同一health builder，实际Node版本与signed manifest不等即零ready并进入既有恢复。 |
| frozen delivery/current-authority体验 | 重造十owner、七source、enqueue前后、late/fresh、retry/uncertain、seal响应丢失；再以surface/empty、current anchor换代、离线、sticky action、伪receipt、成功token响应丢失核对U38-03↔U38-04↔U38-05。 | upgrade沿既有DeliveryAuthority operation恢复admission/sealed，fresh source零父提交、late delivery恰一进入；surface本机零updater，公开status只经current authority并由receipt+pointer+stage+lifecycle+health纯投影，矛盾事实固定action-required，success token compare-and-consume。 |
| 发布证据/产品体验/范围价值与历史边界 | 重新质疑八项当前用户损失、方案比例、五目标交付路径与EX38-01～EX38-02；核查第30～37冻结owner/stop/removal/checkpoint合同及schema activation、2GiB、daemon、通用框架、外部发布边界。 | 五个P0/P1与三个P2均已由锁定范围内既有原语闭合且直接改善可安装、可恢复、诚实可见的用户体验；没有引入第二事实源、daemon或通用框架，v1 schema activation、任意2GiB、运行时report耐久化和范围外发布仍被排除，EX38-01～EX38-02未重开。 |

### 四路冷启动对抗复审记录

| 角色 | 主动反例与交界 | 结论 |
| ---- | -------------- | ---- |
| 五目标安装/反降级与资源边界 | 从app remove后重装旧sequence、同identity异bytes、无home、低容量、最大五目标包及`release:check`旁路反推；核对U38-01↔U38-06↔U38-08 | receipt必须在程序根外、资源策略必须同时约束archive/expanded/temp、passed必须无旁路；上述边界已写入F38-01/F38-06/F38-08，无需任意2GiB流式框架 |
| ProgramStore切版恢复与candidate health | 从rename/pointer/terminal响应丢失、坏candidate被previous再次选择、inactive server未激活、两根identity漂移反推；核对U38-02↔U38-05 | previous只表示已验兼容版本，health必须在同一bound server激活前由共享snapshot授权；逐phase achieved与terminal receipt修复已闭合全部窗口 |
| accepted-work delivery/current-authority体验 | 从freeze后迟到delivery、fresh source竞争、surface错误设备副作用、sticky行动被checking覆盖和旧success重复展示反推；核对U38-03↔U38-04↔U38-05 | 复用既有DeliveryAuthority admission/sealed即可共同封闭；公开体验只消费current authority同一纯投影与receipt token，不增加第二升级事实源 |
| 发布证据/产品价值/历史边界 | 独立重估八项当前损失、评级、工作量，反查EX38-01～EX38-02、第30～37冻结合同及范围外发布能力 | 五个P0/P1和三个P2均有当前可达损失且方案比例成立；schema activation、任意2GiB、daemon、通用框架、report运行时耐久化和外部发布仍被排除，EX38-01～EX38-02未触发重开 |

## 已排除问题

> 保存已证伪疑点供复审复用;使用与重开规则见工作台静态区。裁决只允许“已排除”或“已重开→问题编号”,重开时保留原行。

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | -------------- | -------- | -------- |
| EX38-01 | 原R38-02（P0/大）主张十三项耐久schema只有静态inventory、无production activation/writer闭包，会让不可回滚writer被误接纳。已验证事实：首个公开基线十三项均为`readMin=readMax=writeVersion=1`；`assertCompatibleUpgrade`对current/candidate/previous执行双向writer可读检查，当前没有writer版本跃迁或bridge。 | 未找到候选被接纳后写出previous/peer不可读格式的当前可达producer；全部writer仍为v1时接入十三日志activation只会增加第二投影与无用户价值复杂度。适用边界：当前v1稳定基线及没有不同writer版本的发布范围。 | `packages/core/src/protocol/durable-schema.ts`的十三项v1 exact-set；`packages/cli/src/update/update-controller.ts`的双向protocol/schema兼容门禁；第38单元独立审查价值裁决。 | 任一durable schema的生产`writeVersion`首次高于当前基线，或发布范围出现需要bridge/minimumRollbackVersion支持的不同writer版本。 | 已排除 |
| EX38-02 | 原R38-N01（P2/小）主张`StopDeps.killFn/taskkillFn`未调用字段与CLI README旧`credentials.json`迁移文字会增加维护和误接线风险。已验证事实：字段没有production caller，README文字不是运行时consumer，生产凭据路径已经是SecretStore-only。 | 没有运行、数据、安全或核心体验损失；为内部类型整洁改动测试兼容字段的收益低于churn，推测性维护风险不能构成当前问题。适用边界：字段保持无生产消费且文档不作为用户可达迁移入口。 | `packages/cli/src/serve/stop.ts`字段及其测试仅断言永不调用；`packages/cli/README.md`文字；第38单元独立审查价值裁决。 | 任一字段进入生产组合根/效果路径，或该文档成为用户可达安装迁移入口并给出错误操作。 | 已排除 |
| EX38-03 | 原 P38-19（P2/小）主张 ProgramStore cleanup 使用`readdir()`导致 O(n) 内存，且删除后没有额外缺失 read-back，应改为流式固定批次。已验证事实：cleanup 只枚举程序根，保留 current/previous/active stage；`rm()`成功完成或抛错，失败时下次仍按目录事实重试。 | 正常生命周期持续 cleanup，未发现受支持场景能形成足以导致资源失败的版本目录规模；原主张只证明理论 O(n) 与更强证据形式，不能证明当前用户损失，额外 read-back 也不改变恢复语义。适用边界：ProgramStore 版本保留有界且没有生产资源失败证据。 | `packages/cli/src/update/program-store.ts`当前 cleanup 调用链；本轮独立审查及价值裁决。 | 产品允许无上限保留版本，或出现真实 cleanup 内存失败、文件系统成功返回却未删除的生产证据。 | 已排除 |

## 迟发现教训

> 仅登记“先前通过后才发现”的真实遗漏。检测动作必须可执行并写明适用范围;每个适用轮次追加执行证据。

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | ---------------------- | -------- | ---------------------------- | --------------------- |
| L38-01 | U38-10；U38-09及IR38-02/10/16先前通过 | 只验证SemVer解析与排序，没有把公开identity允许字符逐一差分到ProgramStore目录、pointer codec和生成launcher的实际消费合同。 | 对所有进入文件名、pointer或生成loader的公开identity，枚举codec合法字符并与每个持久化/启动消费者做正反差分；至少包含`+build`并穿过真实write→read→launch。适用于第38单元版本与程序存储交界。 | 本轮转存：P38-10生产反例已执行该检测并登记U38-10。 |
| L38-02 | U38-11；U38-04及IR38-11/19/29先前通过 | current-authority回退只验证了已有trust的配对拓扑，漏掉“无trust本身是默认单机合法事实”的no-genesis生产组合。 | 每个topology/trust授权谓词必须同时枚举默认单机、paired current/non-current、trust absent/corrupt及host absent，并穿过真实composition root验证效果边界。适用于第38单元离线诊断与本机回退。 | 本轮转存：P38-11默认临时home反例已执行该检测并登记U38-11。 |
| L38-03 | U38-12；U38-08及IR38-25/30/31先前通过 | 证据审查核对了scenario id、摘要和candidate runtime，却未核对真正执行的program bytes/entry/argv/terminal，导致workspace源码测试冒充候选行为。 | 对所有发布候选运行证据执行“candidate-only mutation、workspace不变”检测，并逐行反绑实际可执行bytes、entry、argv和终态；任一候选损坏必须使门禁失败。适用于第38单元target-local证据与release:check。 | 本轮转存：P38-12调用链反例已执行该检测并登记U38-12。 |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| V38-01 | 当前源码可由全部workspace消费者构建，CLI跨包合同闭合 | `pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit`；`pnpm build` | 专项冻结36文件及workspace依赖图 | 修复直接验证/必要构建；17.2秒+153.6秒 | CLI类型检查零错误；17/17 workspace项目构建成功 | `534e9a05e53e00daaf50d40ca4df8817940ab4817728f50cb23f7b32a9d933ce` | 有效 |
| V38-02 | release/SemVer、ProgramArtifact有限合同与FileLock PID-reuse直接行为 | core release与file-lock定向文件 | 对应core生产文件与直接测试 | 修复直接验证；10项 | 10/10通过；超安全整数、prerelease/build及stale heartbeat+live PID均命中生产谓词 | 同上 | 有效 |
| V38-03 | ProgramStore、installation receipt、upgrade lifecycle、两根cleanup后pointer接力与真实health | CLI update直接闭包；最后失效增量为`upgrade-lifecycle`与`startup-server-owner` | CLI update/两根装配生产输入与直接测试 | 修复直接验证；最后增量5项 | 既有受影响闭包有效；最后修改5/5通过，含`old-host-stopped`响应丢失与inactive handle门 | 同上 | 有效 |
| V38-04 | current-authority通知、离线doctor、busy终态、认证RPC和connection generation | server context/update方法与CLI facade/REPL/doctor定向测试、registry golden | server/CLI生产入口、消费者与golden | 修复直接验证；66项+golden | 66/66通过；subscribe-before-read、失败回滚、close/换代、unavailable与零busy失败写成立 | 同上 | 有效 |
| V38-05 | 五target程序树、target-local producer、固定12 smoke/50-id、S7两根装配与派生资产 | `node --check scripts/release-target-evidence.mjs`；`node --test scripts/release-tooling.test.mjs`；`pnpm s7:lint` | release脚本/指南/ledger、S7 descriptor与registry golden | 修复直接验证；6项+20项 | 5通过/1宿主symlink能力跳过；S7 20/20及golden通过；evidence脚本语法有效 | 同上 | 有效 |

| V38-06 | U38-10/U38-11当前实现的ProgramStore storage identity、旧pointer与单机/配对离线authority纯投影 | 两份CLI直接测试；CLI类型检查；`pnpm cli:build` | 本轮5个CLI生产/测试文件及其CLI依赖输入 | 修复直接验证/必要构建；25项+类型检查+一次构建 | 25/25、类型检查、CLI构建均通过；空home零写，paired坏trust fail-closed | `b7327c1d48662df31fbb77383cc472ffa39565d1d6f1a2a9f81ea370a2ea20cb` | 有效 |
| V38-07 | U38-12固定十二项candidate-only producer、signed baseline/candidate与release gate | 两脚本语法；`node --test scripts/release-tooling.test.mjs`；`pnpm s7:lint` | 本轮release脚本、合同测试、指南/规格及S7现有descriptor/golden | 修复直接验证/派生资产；6项+20项 | 5通过/1宿主symlink能力跳过；S7 20/20与golden通过；C38-C12修复后失效闭包复跑通过 | `b7327c1d48662df31fbb77383cc472ffa39565d1d6f1a2a9f81ea370a2ea20cb` | 有效 |

## 终审记录

| 轮次   | 审查侧重                                       | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论   |
| ------ | ---------------------------------------------- | ------------ | -------- | ---------- | ------ |
| 第一轮 | 需求、架构、功能闭环、状态、回归               | 否           | —       | —         | 待开始 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 否           | —       | —         | 待开始 |

## 独立审查覆盖表

> 本表只记录独立审查覆盖进度，执行规则见工作台静态区“独立功能审查”。本表按失效机制划分，禁止照抄审查结论复用表；必须常设跨区组合核查行。状态只允许“待审”“审查中”“已覆盖”“失效”“有问题”。

| 编号 | 风险区与风险面 | 登记输入与指纹 | 独立覆盖状态 | 结论与证据 | 重开条件 |
| ---- | -------------- | -------------- | ------------ | ---------- | -------- |

<!-- registration-complete: unit-38.gen-1 -->
