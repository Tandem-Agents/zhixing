# 单元登记:第 15A 单元 · generation 1

- **unitId**:15A
- **generation**:1
- **登记时间**:2026-07-17
- **登记来源**:用户明确要求开始第 15 单元开发；架构裁决后当前代际收敛为第 15A 单元

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、分片账本、九类核查面行、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:完成；U15-53 测试预算与 U15-54 运行时校验分层收口，同一最终指纹两轮审查及全部验证通过
- **连续无新增问题轮数**:2 / 2
- **交付物是否冻结**:是；最终指纹 `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12`
- **交付物文件集**:65 个业务文件；core 37、owner-kernel 13、executor 3、CLI 3、server 8、架构文档 1；工作台文件不计入交付物
- **当前交付物指纹**:`sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12`（按文件路径顺序对每文件取 SHA-256，以 `相对路径=hash` 每行 LF 拼接后再取 SHA-256）
- **架构来源**:`research/design/modules/distributed-runtime/specification.md` 第 15A 提交边界、delivery 权威合同及 §5.5 状态外发白名单

## 固定边界

- **功能范围**:五类 delivery key、原子唯一索引、十五行生命周期、用户裁决、权威投影与 drain、conversation/job 结果原子入流的 owner 接线及测试；新权威流保持零生产流量
- **架构不变量**:AuthorityCommitLog 是新 delivery 流唯一事实源;来源事实与入队单 envelope/单 fsync;查重无 TOCTOU;外部调用前先落 attempt;unknown 不静默丢失;既有 DeliveryPipeline/queue 与生产路径保持唯一且行为不变
- **验收条件**:十五行逐边、五类生产者、同键并发、外部调用前后崩溃、三种裁决、投影重建、状态外发白名单与冻结文案、既有投递行为等价、受影响包全测与必要构建通过
- **必要上下游**:conversation/job committed 与状态事实、MutationBatch、ControlAdmission、Channel/Outbox sender、server/CLI delivery 装配
- **明确不属于本单元**:conversation 生产路径切换及过渡授权（第 15B 单元）、旧公开 enqueue/queue 退役及 scheduler 产品接入（第 26 单元）、正式 capability/permission/resource 治理、mesh 与跨机数据面

## 审查分片

- **是否启用**:否
- **决定依据**:本轮为渐进实现;开发完成后由审查阶段按交付规模决定
- **完整单元与跨切片必审项**:五类生产者到唯一索引、生命周期、恢复、投影与控制面的完整身份链

| 切片 | 审查闭包（边界、依赖、局部验收） | 输入基线 | 当前轮次 | 本轮进度 | 收敛状态 | 封版信息包（结论、保证、证据、重开条件） |
| ---- | ---------------------------------- | -------- | -------- | -------- | ---------- | ---------------------------------------- |

### 完整单元横向审查记录

| 轮次 | 完整输入基线 | 全范围与跨片核查 | 重开触发 / 切片 / 问题 | 结论 |
| ---- | ------------ | ---------------- | ---------------------- | ---- |

## 派生产物闭包

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| -------------------------- | ------------------------ | ---------------- | ---- |
| core delivery authority、types、queue、pipeline、validation/content | index/package exports、tsup entry、旧 enqueue 消费者、投影、内容物化、GC、发送与恢复；8KiB inline、480 字段与平台消息身份共用谓词 | core 类型检查、合并定向 63/63、build | 直接闭合；包全测通过 |
| protocol 标识谓词与全部投影适配 | assignment、commit、job、resource lease、delivery、transcript、control、conversation/job journal、executor 与 server RPC；spec 声明、protocol export 和 d.ts | 全仓本地上界扫描零分叉；core/owner/executor/server 类型检查；来源/耐久消费 480/481 定向 30/30，surface 22/22 | 单一 accepted-domain 闭合；包全测通过 |
| AuthorityTransactionContext.at | commit-log 单事务统一时间、既有 transaction 决策方 | core 全测与构建 | 闭合 |
| owner delivery participant/control | conversation/job 来源事实、内容错误分类、耐久 conflict 重放、ControlAdmission、观察者隔离、owner exports/build | owner 类型检查、participant 9/9、control 5/5、两域 staged 故障及冷重放反例、build | 直接闭合；包全测通过 |
| CLI/server/transport 装配 | 共享 ArtifactStore/CommitLog/DeliveryAuthority、endpoint transport registry、statusRevision 补读与重启重建 | CLI 5 项、server 20 项、Feishu 15 项定向测试 | 第 15A 单元影子装配闭合；不得接管 conversation 生产路径 |
| 旧队列与公开 enqueue 兼容 | scheduler 旧生产依赖、delivery-queue.json、对应旧测试 | 全仓调用检索、兼容队列并发/恢复测试 | 旧路径仍是唯一生产路径且行为保持；终局退役归第 26 单元 |
| distributed-runtime specification | inline 内容预算、平台消息身份、scheduler 展示投影与既有 wire/log 上界 | 实现常量与 validator/producer 对账、边界反例 | 声明面与生效面已同步；15B 生产切换合同未混入 |
| server distributed-runtime 行为与结构 golden | 新增 `delivery.resolve`、`deliveryStatus`、协议校验与 owner/executor/server import 拓扑 | 显式 golden 更新后的逐项差分；行为 2/2、结构 1/1 定向门禁通过 | 仅记录第 15A 预期公开面与真实依赖变化，无额外行为或非法边 |

## 关键原语核查

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| DeliveryEnqueueKey | AuthorityCommitLog delivery stream | 单 envelope 的唯一索引提交 | 同键并发与日志重建 | 五类 owner 生产者 / DeliveryAuthority | 五种键封闭校验；重放重建唯一索引 | 闭合：itemId 以提交时间与域分离熵生成 `dlv-<Ulid>`；全部入口共用精确形状谓词 |
| DeliveryProjectionCursor | AuthorityCommitLog + 内存投影/cursor 对 | 成功重放或提交后原子换代 | 非法输入、reducer/append 失败、peer append 与重建 | DeliveryAuthority 全部读写入口 | 异常不得把旧投影与空 cursor 组合后从零重复重放 | 闭合：投影与 cursor 仅从隔离副本整体发布；失败保留最后良好快照 |
| DeliveryIntent 内容 | 来源 CommitEnvelope + ArtifactStore | 内容先校验/外置 fsync，再由 enqueued 原子引用 | 超限、坏 ref、媒体引用、存储故障、重放与 GC | owner content compiler / pipeline materializer / transport / ArtifactStore GC | inline 规范字节 ≤8KiB；记录 ≤32KiB；ref 解引用为封闭 DTO；引用闭包可恢复；业务错误与基础设施错误判别封闭 | 闭合 |
| DeliveryAttempt | delivery stream | attempt-started fsync 后才允许外调 | 调用前崩溃、响应未知、端点离线、迟到结果 | pipeline / endpoint transport registry | 单 active attempt；unknown 裁决不依赖 readiness；同键有界重驱 | 闭合：序号、自动预算与手动授权分账；在线与重放共用授权及 unknown 选择谓词 |
| DeliveryOutcome | transport 结果经写前验证后的 delivery 记录 | candidate record 全验后 append/fsync | 畸形回执/错误、超界 messageId、append 后崩溃、重启重放 | transport / DeliveryAuthority / reducer | 非法外部数据零追加；messageId 与耐久标识共用 ≤480 谓词；回执按真实不可变字节摘要；错误零秘密 | 闭合：在线与 full reducer 共用 retry/terminal 选择谓词并拒绝 sibling transition |
| 用户裁决 | control stream + delivery stream | received、resolution、applied 同 envelope | 重复 request、epoch 迁移、迟到 outcome、同步/异步观察者故障 | ControlAdmission / DeliveryControlHandler | request 幂等；resolution 绑定耐久 open fact；提交后通知由纯事实投影且观察者不改变权威结果 | 闭合：resolution 与成功 applied 唯一伴随，并逐字段绑定耐久 request 与认证 principal |
| 来源事实原子入流 | conversation/job source stream | source fact 与 delivery-enqueued 同 envelope | source-only、delivery-only、重复提交 | owner participant / delivery reducer | committed 禁止额外 status delivery；五类映射双向封闭 | 闭合：AuthorityError/PublishDecision accepted-domain 单源，三类完整消费者一致 |
| Delivery 状态消费 | delivery stream statusRevision | lifecycle 事实提交后投影 notice | 响应丢失、断线重连、裁决后恢复、停机 | authority / live publisher / server.info / surface | revision 单调补读；全部非终态计入 deferred work | delivery 面闭合；生产接管归第 15B 单元 |

## 覆盖与核查

| 覆盖来源 | 来源项 | 核查面 | 对象或路径 | 问题盘点结论与证据 | 终审一结论与证据 | 终审二结论与证据 |
| -------- | ------ | ------ | ---------- | ------------------ | ---------------- | ---------------- |
| 架构 | 第15A单元 | 状态 | delivery 十五行 | 判别联合逐边覆盖，定向测试通过 | 十五行与本地原子终结逐边对照，无新增 | 历史 reducer 对 failed/uncertain 的行守卫仍弱于在线裁决，见 U15-51 |
| 架构 | 第15A单元 | 入口与生产端 | 五类入队生产者、旧路径兼容 | 五类构造与双向 companion 闭合；新流零生产、旧路径保持唯一 | 五类来源与精确 companion 对照，无新增 | 五类来源无新增；完整 reducer 消费点合同分叉见 U15-35 |
| 架构 | 第15A单元 | 消费端与继承面 | queue/pipeline/outbox/server/CLI | delivery 消费链与影子装配闭合；生产接管归第 15B 单元 | 新旧消费链及影子装配对照，无新增 | pipeline/outbox/status 消费链发现 U15-29～31、U15-33～34 |
| 架构 | 第15A单元 | 生命周期 | enqueue/attempt/result/resolution | delivery 定向测试覆盖十五行及新增反例 | 生命周期、三种裁决与状态历史复核，无新增 | retry、恢复、迁居、late outcome 发现 U15-29～30、U15-32～33 |
| 架构 | 第15A单元 | 并发与崩溃点 | 唯一索引、外部调用、写前校验 | 新权威链闭合；旧队列终局迁移/退役归第 26 单元 | 资格快照、预检及 started+failed 单 fsync 对照，无新增 | 异常后的投影/cursor 原子换代未闭合，见 U15-52 |
| 架构 | 第15A单元 | 异常路径与终态 | retry/failed/uncertain/late/offline | 闭合：离线 attempting、未知结果、缺 adapter、错误脱敏均有直接证据 | 瞬态/永久物化、redrive 与迟到结果复核，无新增 | full replay 可接受提前 failed/uncertain，见 U15-51 |
| 架构 | 第15A单元 | 安全边界 | anchorEpoch/control/SecretRef/transport result | 闭合：候选写前验证、真实回执摘要、错误零秘密、双向来源绑定 | 控制身份、来源字段与安全错误对照，无新增 | transport 首个消费点仍可假成功并泄漏原始诊断，见 U15-31；迁居绑定见 U15-32 |
| 架构 | 第15A单元 | 模块边界 | core/owner/server/cli/scheduler/outbox | delivery 模块边界闭合；15B 生产切换与第 26 单元退役均未混入 | 15A/15B/26 边界及零生产流量对照，无新增 | 未越过 15A；delivery control 未复用完整 reducer 合同见 U15-35 |
| 架构 | 第15A单元 | 测试与验收 | 逐边/生产者/恢复/回归/升级 | 直接验证通过；包全测、依赖构建及两轮终审待冻结后执行 | 新增对抗矩阵与原验收映射完整，无新增 | 现有测试未覆盖 sibling transition 反例及异常后缓存恢复，见 U15-51～U15-52 |

## 问题清单

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U15-01 | delivery-only conversation commit 仍进入旧外部发布链 | 发布决策未区分 delivery projection 与外部发布记录 | delivery-only、混合批次、replay | 以记录类型投影发布计划；delivery-only 不生成 sidecar，reducer 拒绝空外部决策 | 已解决 |
| U15-02 | epoch 迁移后的迟到 outcome 曾绑定当前 uncertain open fact | outcome 身份取自当前 authority，而非该 item 的耐久 open fact | late outcome、resolution、anchor migration | outcome 绑定 item 原 open fact；resolution 使用当前 authority epoch；迁移测试通过 | 已解决 |
| U15-03 | delivery participant 曾占用通用 JobCommitParticipant 接缝 | delivery 专属职责与未来扩展接缝混合 | staged delivery 与后续 job mutation 扩展 | 独立必需 delivery participant；通用 participant 只处理非 delivery mutation，禁止写 delivery stream | 已解决 |
| U15-04 | 两个既有真实 IO 用例贴近默认 5 秒时限并随机越线 | 测试预算未显式表达 Windows fsync 成本 | executor conversation/job 回归稳定性 | 保留测试内容并显式声明 15 秒预算；失败用例隔离重跑通过 | 已解决 |
| U15-05 | [P0] Scheduler 旧投递依赖与回归测试已删除，但 CLI 未装配 ConversationRunJournal、JobJournal、OwnerDeliveryParticipant 和 delivery resolution 生产入口；定时任务结果失去投递路径。集中修复已恢复 Scheduler 兼容投递、装配 DeliveryAuthority/participant/control/status。进一步复核确认生产 turn 仍由 `runTurnWithCommit → SessionRuntime.run → ConversationManager.recordTurn → appendRunRecord` 直接落旧事实，非测试代码零 `stageMutation` 调用；而当前 DispatchEnvelope 又强制携带 Manifest、Capability、PermissionSnapshotLease 与 ResourceLease，其真实签发、激活和 guard 被计划放在第 16～18 单元 | 把“删除公开生产接口”误作“允许旧能力先下线”；执行计划把 delivery 权威流、整套 S3 产品切换和尚未定义的 S4 前过渡授权形态合在同一单元，却未冻结从现有 ConversationManager/RuntimeHost 到 RunJournal/AssignmentLedger 的生产适配器、staged 写迁移、崩溃恢复入口及过渡凭证合同 | Scheduler 结果投递、conversation/job 进程内生产链、CLI composition root、全部既有写类工具、控制面、运行时执行、取消恢复、单机 golden，以及第 16～18 单元的凭证/治理接入边界 | 架构已拆分第 15A/15B 单元并冻结过渡授权与切换栅栏；第 15A 单元必须保持新流零生产、旧路径唯一，第 15B 单元再完成 conversation 生产切换 | 架构裁决完成；生产切换转交第 15B 单元，非第 15A 交付问题 |
| U15-06 | [P1] assertDeliveryCompanions 用当前 maxAttempts、当前状态文案和当前 intent 构造逻辑逐字节重算历史 enqueued；配置 3→5 已实证使合法历史重放报 corrupt | 在线派生与历史验证复用含可变默认值/实现细节的构造器，未区分“当时冻结事实”和“当前策略” | 五类 producer、conversation/job full replay、配置升级、文案演进、重启与版本兼容 | 在线只在入队时解析并冻结策略/内容；重放读取耐久 enqueued，不调用当前默认值或文案模板；以单源谓词验证来源种类、keyBody、同 envelope 关系和 enqueued 自洽，配合 U15-12 保持双向绑定；验收配置/文案变化后旧日志可重放且篡改仍拒绝 | 已修复；最终验证通过 |
| U15-07 | [P1] first-party conversation 中合法 turn-origin staged delivery 无 durable channel target 时直接抛 TypeError，整个 submitSealedBundle 失败且永久不可重试 | delivery participant 只有整批 accepted/rejected，没有把无路由业务预检映射为 MutationBatch 逐条 publish outcome | first-party TTY/RPC run、混合 mutation batch、publish-decision、FinalFrame conflict 计数与 PublishConflictNotice | 将无 durable route 表达为该 mutation 的稳定 AuthorityError/conflicted outcome；其余 mutation 与 run 结果照常提交；验收 first-party turn-origin 单条冲突、混合批次部分成功、重放结果稳定且通知可恢复 | 已修复；最终验证通过 |
| U15-08 | [P1] DeliverySendMeta 已携 idempotencyKey，但 OutboxSender、PostEntryInput、OutboxEntry 与 doSend 链路均丢弃该键 | 顺序层沿用旧消息模型，未同步 delivery-origin 身份合同 | Pipeline→Outbox→adapter、响应丢失重驱、同 item 多 attempt、后续渠道幂等 | Outbox entry 与发送端显式承载 delivery idempotencyKey，delivery-origin 必填、其他来源可选；同 item 全部重驱复用原键并逐层透传；补持久化/恢复/adapter 参数断言 | 已修复；最终验证通过 |
| U15-09 | [P1] sent 回执用 protocolDigest("DeliveryReceipt",1,{itemId,attempt,messageId})，与冻结的 receipt.digest=B(真实不可变回执字节)合同不符且该派生域未登记 | 把本地派生身份误作外部回执内容摘要，transport 结果又未提供可摘要的非秘密不可变回执 | DeliveryResult、pipeline、sent record、跨实现重放与摘要注册表 | 由 adapter 返回最小、非秘密、不可变的回执字节/规范载荷，pipeline 对原始字节计算 B(bytes)，日志只存摘要及必要平台消息身份；补固定向量与错误摘要域反例 | 已修复；最终验证通过 |
| U15-10 | [P1] materializer 与 transport 的原始 error/message 直接耐久写入 DeliveryFailure，可能含响应体、URL、header 或秘密 | 耐久失败边界缺少单一脱敏/类型化错误映射 | content materializer、所有 transport/adapter、retry/failed records、日志/备份/诊断面 | 在进入权威记录前统一映射稳定错误码和安全文案；原始诊断仅进入受控非耐久日志且同样脱敏；以含 token、URL、header、响应体的对抗输入证明权威日志零泄漏 | 已修复；最终验证通过 |
| U15-11 | [P1] conversation/job journal 为观察 delivery sidecar 移除 stream 过滤，导致每个聚合重放全局物理日志；DeliveryAuthority.observe 又弱于 full reducer，会跳过旧 revision 等非法记录 | 缺少“聚合流 + 同 envelope sidecar + 共享全局投影”的日志投影原语，以第二套轻量 reducer 代替结构能力 | 所有 conversation/job 首次打开与恢复、DeliveryAuthority 唯一索引、规模增长、坏日志 fail-closed | 提供按相关逻辑流/同 envelope sidecar 投影的事务能力，delivery 全局索引只线性推进一次；full 与 compact 路径复用同一合同谓词，投影适配不得弱化；验收复杂度不随聚合数乘全局日志量增长，合法/非法记录差分结论一致 | 已修复；最终验证通过 |
| U15-12 | [P1] full delivery reducer 可接受没有合法五类来源事实的 enqueued，现有测试用任意 dummy source 也能重建；当前只做 source→delivery 单向伴随断言 | 原子 companion 合同落在各来源 reducer，缺少 envelope 级反向完整性与唯一生产者约束 | 五类 producer、delivery-only/额外 enqueued、历史重放、内部误用与未来 mesh 输入 | 建立 envelope 级单源 companion 注册表，逐 kind 强制恰一合法来源事实与恰一匹配 enqueued，生产与重放同时执行 source→delivery 和 delivery→source；五类正例及 orphan、错 kind、错 key、重复/额外记录全部 fail-closed | 已修复；最终验证通过 |
| U15-13 | [P0] pipeline 在读取权威状态前统一以 transport readiness 提前返回；已耐久 attempt-started 的 item 在端点离线恢复时永久停留 attempting，既不进入 uncertain，也不按冻结策略完成有界重驱 | 新 attempt 的准入条件与未知结果的恢复裁决共用同一道 readiness 门禁，恢复状态机的时间推进错误依赖外部端点在线 | manual-resolution、expired idempotent-redrive、started 后调用前/调用后崩溃、端点离线、状态通知 | 按耐久状态调整顺序：queued/retry-wait 仅在 transport ready 时新建 attempt；attempting 无论 readiness 都先执行冻结的 unknown 裁决，只有实际重发等待端点就绪且 deadline 继续推进；验收两类策略在断线恢复与重启后均能收束 | 已修复；最终验证通过 |
| U15-14 | [P1] owner 生产端未建立统一内容编译边界：ref 未解引用并校验为 OutboundContentDto，发送端默认把 artifact 原始字节当 UTF-8 文本；合法 media/ref 无法正确发送，较大 final/staged 文本又会以内联记录突破 commit-log 记录上限并阻断整次提交 | 把领域内容、artifact 存储形态和 transport 物化混为一层，缺少入队前规范化、校验、外置以及发送侧对称物化 | 五类 producer 的 inline/ref/media、较大 final/staged 内容、ArtifactStore 生命周期、逐条 publish conflict、conversation/job commit 与历史重放 | 建立单源 delivery content compiler：入队前精确解码/校验 OutboundContentDto，超过耐久记录预算的 inline 内容先外置为 artifact，合法媒体引用保持结构化；发送侧只物化已验证 DTO；非法 staged 内容逐条 conflicted 而不拖垮 run；验收合法 ref/media、畸形/任意 artifact、超限 final/staged 与重放 | 已修复；最终验证通过 |
| U15-15 | [P1] recordOutcome 未在 append/fsync 前用单源 validator 校验候选记录；畸形 transport result 可先耐久写入，再由 reducer 抛错，留下导致后续启动永久失败的非法日志 | 错把 append 后 reducer 校验当成写边界校验，没有保证外部运行时数据在持久化前闭合 | transport result、sent receipt/messageId、failure 对象、delivery log 重放与 owner 启动 | 每个 lifecycle 候选记录在 append decision 返回前先完成快照与同源 schema/语义校验，reducer 继续复验；参数化注入畸形 transport outcome，证明零追加、错误稳定且重启成功 | 已修复；最终验证通过 |
| U15-16 | [P1] 默认 channel transport 对非 channel endpoint 报 ready，随后把 webhook 作为 unsupported 非重试失败；合法 webhook item 会消耗 attempt 并终态失败，而不是等待对应 adapter/secret 能力就绪 | 将“没有该 endpoint adapter”错误建模为业务永久拒绝，而不是可观测的能力 readiness | job result webhook、配置或重启后 adapter 可用、SecretRef 解析、attempt budget 与恢复 | 以 endpoint-kind transport registry/readiness projection 表达能力；未配置 webhook 时 readiness=false 且不产生 lifecycle 变更，配置后由 webhook adapter 负责 SecretRef 解析与稳定 outcome；验收 adapter 注册前后及重启恢复 | 已修复；最终验证通过 |
| U15-17 | [P1] delivery 已有耐久 statusRevision/history，但生产装配只发私有 failed/uncertain 事件且无真实消费者，也没有 resolved 实时事件；server.info 仅报告 queued，既不能按 revision 补历史，也低报 retry-wait/attempting/uncertain | 权威状态生产完成后未闭合用户可见的实时与断线恢复消费合同，且把队列计数误作状态投影 | failed/uncertain/resolved 实时通知、断线重连、控制动作发现、server.info、关闭时 deferred work | 与 U15-05 同轮接通 revisioned notice 查询与 live publisher；resolution 成功产生 resolved 通知，server.info 可从 revision 补读且 pending 覆盖全部非终态状态；验收响应丢失、重连补读、控制闭环与关闭判定 | 已修复；最终验证通过 |
| U15-18 | [P1] 本单元删除旧 delivery-queue.json 生产/读取路径，但新 Authority 启动未迁移或显式裁决旧队列；升级后既有 queued/retrying 投递会被静默遗留 | 在替换耐久事实模型时先移除旧路径，未建立可验证、可回滚的迁移或退役协议 | 旧 queued/retrying item、迁移崩溃点、幂等身份与来源 companion、回滚、旧文件退役 | 架构裁决第 15A/15B 均保留旧队列和公开 enqueue；第 26 单元接入 scheduler 后排空旧队列，再整体退役旧生产路径 | 架构裁决完成；终局退役转交第 26 单元，非第 15A 交付问题 |
| U15-19 | [P2] conversation/job 状态生产端曾向渠道外发全部非 committed 中间态，并使用英文占位文案 | surface 全量状态可观测合同与渠道用户通知合同未分层，产品外发白名单和文案此前未冻结 | conversation/job 全状态、渠道刷屏、重放 companion、产品文案与测试矩阵 | 按 §5.5 单源白名单仅外发 cancelled/failed/expired/uncertain，committed 由结果承载、job missed 由后续汇总承载，其余状态渠道零投递；文案逐字匹配冻结模板，以两域全状态参数化测试验收 | 已修复；owner 类型检查与定向测试 9/9 及最终验证通过 |
| U15-20 | [P0] 新权威实现直接替换了既有 `DeliveryPipeline`、`DeliveryQueue`、公开 `enqueue`/类型与 scheduler 依赖，并以 `SchedulerDeliveryCompatibility` 重写旧队列；server stats/flush 指向零生产的新流而非真实旧队列，旧回归测试也被替换 | 把“并存新组件、零生产流量”误实现为“占用旧组件身份后补兼容层”，没有以冻结边界对基线公开面、生产装配和测试做差分验收 | core delivery 公共 API、delivery-queue.json 行为、scheduler 结果投递、Outbox/Feishu 旧行为、server deferred work、升级恢复与第 26 单元接管边界 | 原样恢复旧 `DeliveryPipeline`/`DeliveryQueue`/scheduler 生产链及旧测试；新权威投影、queue view 与 drain 使用独立 `AuthorityDelivery*` 身份并影子装配，删除兼容重写；旧调用在参数、错误、统计、TTL、恢复和持久化上保持基线等价，新流仍零生产；验收基线差分、旧新两套定向测试与装配测试全绿 | 已修复；旧组件恢复为 HEAD 基线，新流改用 `AuthorityDelivery*` 独立身份；CLI 旧/新装配 5/5、server 20/20、Feishu 13/13 通过 |
| U15-21 | [P0] conversation 仅含 granted `delivery-enqueue` 的 MutationBatch 不写 `publish-decision`，但同 envelope 的 staged enqueue 反向守卫强制要求该决定；append 后 reducer 会拒绝已耐久 envelope | 把“无需外部物化进度”错误等同为“无需逐条终审决定”，混淆终态裁决与外部 publisher work | delivery-only 与混合 conversation batch、原子提交、崩溃重放、publish sidecar 与 delivery companion | 任意非空 MutationBatch 均写覆盖全部 mutation 的 `publish-decision`；仅对真正需要外部物化或冲突通知的 domain 写 `publish-progress`，delivery-only granted 不进入旧外部发布；补 delivery-only 提交、重放和混合批次测试 | 已修复；决定与外部进度谓词分离，delivery-only 定向提交/重放通过 |
| U15-22 | [P1] conversation/job committed 重放仍调用当前 `compileDeliveryContent`，会按当前阈值/规范重新解析并可能 `ArtifactStore.put`；合法历史能被未来编译策略拒绝或在重放时产生新写入 | 在线内容冻结与历史 companion 验证仍共用有副作用、会演进的编译器，U15-06 只移除了当前文案/maxAttempts 的字节比较，没有切断重放依赖 | 两域 committed full replay、内容 schema/阈值演进、只读恢复、ArtifactStore 故障与旧日志兼容 | 在线提交负责一次编译并冻结；重放只从耐久来源与 enqueued 读取事实，调用纯 key/身份谓词，不运行内容编译器、不写 ArtifactStore；补无写重放和策略变化后旧日志可恢复测试 | 已修复；两域 full replay 移除在线编译器，超阈值内容冷重放零 artifact 写入测试通过 |
| U15-23 | [P1] first-party conversation 无 final route、turn-origin 无 durable route，以及 job `deliveryPlan:none` 时仍预先编译/外置对应内容；无投递提交会因无关 ArtifactStore IO 失败或留下无用 artifact | 内容编译发生在路由可达性与逐条业务预检之前，未以“将产生权威 delivery intent”为副作用门禁 | first-party TTY/RPC、delivery-only conflict、job none、大结果外置、磁盘故障与 artifact 保留 | 只为确定会产生 enqueue 的 final/result/staged 项编译；无 route 项直接形成稳定 conflict 或零 delivery，不触碰 delivery artifact；补拒绝 ArtifactStore 下仍可提交的反例测试 | 已修复；first-party、turn-origin 无 durable route 与 job none 均在编译前裁决，两域超阈值无路由提交零 delivery artifact 写入测试通过 |
| U15-24 | [P1] `assertDeliveryEnvelopeCompanions` 仅按少数字段匹配任意对象，缺失 assignment/bundle/revision 等字段的伪 committed、任意非 committed state、弱结构 publish-decision 均可被当作合法来源；现有 core/CLI 测试还把这类 dummy 记录作为正例 | U15-12 只补了“有且仅有一个匹配对象”，未把五类来源事实的精确结构、状态白名单和 staged 决定投影纳入同一个反向合同谓词 | authority-first replay、五类 enqueue、malformed source、状态渠道白名单、未来 mesh/生产接管与坏日志 fail-closed | 在 core 定义 delivery companion 的精确投影谓词：严格字段封闭、标识/版本/状态校验、五类 key 绑定与 staged granted outcome；owner 生产/replay和 DeliveryAuthority 共用，测试拒绝缺字段、额外字段、错状态、错决定及重复来源 | 已修复；五类来源投影严格字段封闭并验证 staged granted outcome，malformed/错状态反例与 owner/CLI 正例通过 |
| U15-25 | [P2] 两个新 authority IO 测试文件依赖 Vitest 默认 5 秒时限；core 包全测并发真实 fsync 时 4 个用例在 5.1～5.3 秒漂移超时，而同用例隔离运行均通过 | 新增真实 IO 测试只验证了隔离正确性，未把包级并发负载下的时限预算写成显式测试合同 | `authority.test.ts`、`authority-pipeline.test.ts`、Windows fsync 负载及 core CI 稳定性 | 保留完整 IO 测试，在两个重 IO 文件显式声明 15 秒时限；先定向验证配置生效，再以 core 包全测证明并发负载下稳定通过 | 已修复；定向 35/35，通过后 core 包全测连续两次 2233/2233 通过 |
| U15-26 | [P1] transport registry 在 readiness / outcome policy 与外部 send 之间可被卸载；已 fsync `attempt-started` 后 registry 抛“未注册”，pipeline 将确定未外调误作未知外部结果，下一轮进入 uncertain | 发送资格分三次查询可变 registry，没有为一次 attempt 捕获稳定 adapter | adapter 动态注册/卸载、attempt-started、manual/idempotent unknown、恢复与安全日志 | transport 以一次 resolve 捕获已就绪 adapter，policy 与 send 始终使用同一快照；卸载只影响后续 attempt，不影响已取得资格的 attempt；适配器预检异常零状态变化且不写原始错误 | 已修复；单次 resolve 捕获 adapter，卸载竞态与预检异常对抗测试通过 |
| U15-27 | [P1] queued/retry-wait 在内容物化前即 fsync `attempt-started`，随后任意本地读取错误都写永久 `content-invalid`；瞬态 ArtifactStore I/O 会无外调却消耗并终结耐久投递 | 把确定的内容损坏与瞬态本地基础设施失败合并，并把可安全重试的发送前预检放在 attempt 线性化点之后 | inline/ref/media 物化、ArtifactStore missing/corrupt/瞬态 I/O、attempt budget、unknown 裁决 | 新 attempt 先物化；瞬态失败零写，永久错误以同一 envelope 原子写 started+failed，任一崩溃点不得留下假的开放 attempt；既有开放 attempt 的未知结果不得被本地错误覆盖 | 已修复；瞬态零 attempt、永久 started+failed 原子 envelope、redrive 未知结果保护测试通过 |
| U15-28 | [P1] `baseRetryDelayMs`、`maxAttempts` 与 idempotent `windowMs` 均接受安全整数，但直接做指数乘法/Date 加法；合法极值会使 `toISOString()` 抛错，明确 retryable 失败留在 attempting 并被后续误判 unknown | 数值合法性只验证 safe integer，未验证派生时间仍处于 canonical Date 定义域 | retry-scheduled、redriveUntil、明确失败、极值配置与恢复 | 以单源饱和时间运算生成 canonical deadline；合法极值不得抛错或改变明确结果分类，默认策略保持字节语义不变；补退避与 redrive 极值测试 | 已修复；共享 deadline 运算覆盖退避与 redrive，最大安全整数两类对抗测试通过 |
| U15-29 | [P1] pipeline 在调用 `recordOutcome` 前以自身时钟计算 `retryAt`，authority 随后以事务 `context.at` 拒绝早于提交时刻的值；零退避或日志排队超过退避时，明确 retryable 结果被异常路径误作 unknown，item 留在 attempting | 状态转移的权威时间在事务外派生，调用方时钟与日志线性化时刻形成双事实源 | attempting 与 uncertain 的 retryable outcome、零退避、日志锁竞争、恢复和状态事件 | 把退避策略输入交给 authority，由同一事务基于 `context.at` 饱和派生并返回权威 state/revision/retryAt；零退避和人为阻塞日志后仍稳定进入 retry-wait | 已修复；人为阻塞日志与零退避对抗测试通过，retryAt 取事务权威时刻 |
| U15-30 | [P1] 已存在 attempting attempt 的恢复仍调用当前 adapter `outcomePolicy`；该调用异常时会丢弃已 ready 的 transport，已冻结为 idempotent-redrive 的开放 attempt 无法重驱并最终误入 uncertain | 新 attempt 的策略选择与旧 attempt 的冻结策略恢复未分层，恢复错误依赖当前策略提供者 | manual-resolution/idempotent-redrive、adapter 策略演进或异常、重启、端点 ready 与 deadline | 仅 queued/retry-wait 新 attempt 查询并验证当前 policy；attempting 先完全按耐久 `attempt-started.unknownOutcome` 裁决，实际 redrive 只依赖已捕获 transport；策略异常对既有 attempt 无影响 | 已修复；恢复重驱不再读取当前策略，异常策略对抗测试通过 |
| U15-31 | [P1] transport 的 readiness、outcome policy 与 `DeliveryResult` 只受 TypeScript 类型约束：truthy 非 boolean `success` 可写成 sent，未知 policy kind 可被当作 idempotent；authority channel 结果又先经 Outbox 发出含原始 error/result 的日志和事件，再由 pipeline 脱敏 | 外部 adapter 运行时边界没有在首个消费点 fail-closed，安全映射发生在中间诊断面之后 | registry、direct/webhook/channel adapter、Outbox event/logger、receipt bytes、sent/retry/failed 与秘密泄漏 | 建立单源 runtime validator 和 authority-origin 安全结果边界：严格字段/判别/类型与 readiness `=== true`，任何畸形值零权威追加；原始 adapter error/result 在首个 Outbox/transport 诊断面前映射为固定安全分类，且不改变旧生产链语义；参数化验证假 success、未知 policy、错 receipt/messageId/额外字段和含秘密 failure/throw | 已修复；单源 runtime validator、authority-origin Outbox 脱敏及旧 Outbox 42 项回归通过 |
| U15-32 | [P1] transport 响应身份复用了含 `openedAnchorEpoch` 的 `DeliveryOpenFact`；attempt 在 epoch 7 发出、epoch 8 接管后仍处 attempting 或由新 anchor 打开 uncertain 时，原响应携带的 epoch 7 摘要会被当前 authority 拒绝，合法 late outcome 丢失 | 把“开放 attempt 的稳定响应身份”和“用户裁决所绑定的 epoch 化 uncertain fact”合并为一个摘要 | anchor 迁居前后、in-flight response、attempting→uncertain、行 3～5 与 9～11、用户裁决 fencing | 为 transport 响应使用仅由耐久 item/attempt/start 事实派生且不含当前 epoch 的稳定绑定；`DeliveryOpenFact` 只服务 uncertain 用户裁决；迁居发生在 uncertainty 前后时同一当前 attempt 响应均可验证，旧 attempt 仍拒绝 | 已修复；稳定响应绑定与迁居前后、旧 attempt 对抗测试通过；U15-02 的迁居验收已补全 |
| U15-33 | [P1] uncertain 的迟到成功与 retryable 失败会自动转 sent/retry-wait，但 durable status history 没有对应 notice；surface 看到 uncertain 后按 revision 补读得不到关闭事实。迟到永久失败虽有 notice，pipeline 仍以 claim 旧 revision 发布，竞争下会重复发布 uncertain 而漏掉真实 failed revision | 生命周期状态机、可补读状态合同和 live event 回执没有以同一权威转移结果闭合；spec 的 DeliveryStatusNotice 联合也缺少自动 late-outcome 关闭语义 | delivery 行 9～11、迁居/并发 claim、server.info last-seen revision、维护 surface 与实时事件 | 架构先冻结“自动 late outcome 如何关闭 actionable uncertain notice”的判别合同；authority 的 outcome API 返回权威 state/statusRevision/retryAt/notice，history 与 live publisher消费同一结果；逐行验证 sent/retry-wait/failed 均能关闭旧 alert 且 revision 精确 | 已修复；三类 late outcome 的 history/live 精确关闭通知、单 revision 单 notice 与旧裁决拒绝测试通过 |
| U15-34 | [P1] `flushIntervalMs` 接受任意非负安全整数，但 Node timer 仅支持 `2^31-1` 毫秒；更大合法配置会被运行时压成 1ms，形成高频 drain/busy loop | 配置校验使用语言数值域，没有验证实际 timer 原语定义域 | authority pipeline 启停、极值配置、CPU/磁盘负载和告警 | 将 timer interval 约束在运行时可表达上界内，或使用有界长延迟调度器；边界值和上界+1 参数化验证，禁止 warning 与高频循环 | 已修复；Node timer 上界与上界+1 参数化测试通过 |
| U15-35 | [P2] `DeliveryAuthority` full reducer 对 enqueued 执行 companion 断言，delivery control 的完整日志投影却直接调用弱一层的 `reduceDeliveryRecord`；当前 `authority.coordinate` 前置同步可兜底，但两个 full consumer 已形成合同分叉 | envelope companion 谓词没有作为完整 authority reducer 的单一公开入口，消费者靠调用顺序补偿 | DeliveryAuthority、ControlAdmission 冷启动/缓存恢复、未来完整日志消费者与 malformed source | 导出并复用唯一 full delivery reducer（结构/companion + lifecycle），内部候选状态检查保留纯 lifecycle reducer；参数化畸形 source 证明所有 full consumer 同结论 | 已修复；唯一 full reducer 已供两类消费者复用，绕过前置同步的畸形 source 测试通过 |
| U15-36 | [P1] §5.5 要求以 `openFactDigest` 配对 uncertain 的打开与关闭，两类 resolve 控制请求也强制该字段；但 conversation/job/delivery 三类 actionable uncertain notice 均未提供摘要，delivery 的用户裁决关闭通知也无法配对，run/job 的 committed 自动关闭还会被普通通知过滤 | 状态通知被声明为可执行裁决的完整 DTO，却未从控制请求反推字段，也未将打开、全部关闭出边和补读逐字段闭合 | 三域状态历史与实时广播、两类 resolve、自动提交/证明解析/用户裁决、迁居和断线重连 | 共享合同已补全：三域打开通知携 `openFactDigest`；run/job 任一 uncertain 出边产生携同一摘要的 `uncertain-closed`，含 committed 后继；delivery 三类打开/关闭通知同摘要；action→decision 映射冻结，history/live 同源且每 revision 至多一条。验收仅凭 notice 可裁决、旧 fact 拒绝、每个打开 fact 恰一关闭 | 已修复；三域打开与全部关闭类型、耐久投影、实时发布和补读同源；原子 control 提交也发布关闭通知，观察者同步/异步失败不改变权威结果；core 27/27、conversation 5/5、job 4/4、CLI 3/3 直接验证通过 |
| U15-37 | [P1] `applyDeliveryResolutionControl` 在 `delivery-resolved + control applied` 已原子耐久后，仍串行等待 `statusHistory` 和可返回 Promise 的 `onResolved`；任一读取或观察者失败都会把成功调用改成异常，且幂等重放不会再次发布通知 | 把可恢复的权威状态投影与非权威 live observer 放在耐久提交的返回关键路径上，违反“观察者故障不能改变权威结果” | delivery 三种用户裁决、响应丢失重放、CLI live 状态、直接 owner-kernel 消费者 | 用共享纯投影从本次耐久 resolution 事实生成 notice，不做提交后日志重读；live 分发与同步/异步观察者错误完全隔离，原控制结果立即返回；验证坏观察者不改变 applied/replayed 结果且历史仍可补读 | 已修复；纯事实投影替代提交后补读，同步/异步坏观察者不改变 applied/replayed，control 定向 5/5 通过 |
| U15-38 | [P1] conversation/job 对 staged delivery 内容编译使用无类型 `catch`，把 ArtifactStore 缺失、损坏和瞬态 I/O 与 JSON/schema 业务无效统一写成永久 `invalid` conflict；run/job 会在基础设施故障下照常 committed 并永久丢弃用户投递 | 内容编译器没有区分确定的内容合同错误与存储基础设施错误，逐条业务冲突吞掉了应使提交重试的故障 | 两域 staged delivery 的 inline/ref/media、ArtifactStore get/put/GC 竞争、publish-decision 与提交重试 | 由内容编译器抛出可识别的永久内容校验错误；owner 只将该错误逐条 conflicted，所有存储/未知错误原样上抛并保持整个 authority envelope 零写；两域参数化验证无效内容可终结、瞬态/缺失/损坏不提交且可重试 | 已修复；确定内容错误与存储故障判别分离，两域瞬态故障零写可重试，无效内容耐久 conflict 且冷重放不重读内容 |
| U15-39 | [P2] 新增 `DeliveryContent` 注册根及其嵌套 media 引用遍历，但现有测试只断言编译器返回 references，没有通过 `AuthorityCommitLog.collectGarbage` 验证外置内容和嵌套媒体在 sweep 中真实保留 | 新增 GC 消费分支没有端到端验收，关键“引用闭包可恢复”结论只由实现走查支撑 | 外置 delivery content、嵌套 media、终态/开放 item 保留、旧 orphan sweep 与坏根 fail-closed | 增加真实日志 + ArtifactStore GC 用例：过期 cutoff 下外置内容根与嵌套媒体均保留、无关 orphan 删除；畸形注册内容必须 fail-closed | 已修复；真实日志与 ArtifactStore sweep 证明内容根及嵌套媒体保留、orphan 删除、畸形根 fail-closed |
| U15-40 | [P1] delivery inline content 的 8KiB 编译阈值未在权威 validator 复验，且合法 TaskDefinition 的 `spec.name` 无上界并被三类 job delivery 同时复制到 `source.taskName` 和状态文案；超长但协议合法的任务名可使 enqueued 超过 32KiB，连带阻断 job commit/状态转移 | 内容外置策略、wire 资源上界和派生展示字段没有形成单源闭包，生产者依赖“通常较短”的隐含假设 | job result/staged/status 三类生产者、直接 DeliveryAuthority 调用、日志记录上限、恢复与未来 15B 切换 | 单源冻结 inline content 字节预算并由 compiler/validator 共用；scheduler taskName 使用确定、有界的展示投影且 source/status 共用，其他自由文本同样 fail-closed；验证超长任务名不阻断 job 权威转移、记录低于上限，绕过 compiler 的超限 inline intent 在追加前拒绝 | 已修复；8KiB inline 与 480 展示/诊断边界单源，超长 taskName 确定投影且记录低于上限，绕过 compiler 的超限 intent 被拒绝 |
| U15-41 | [P2] transport runtime validator 接受任意长度的字符串 `messageId`，而 sent record 的 `platformMessage.messageId` 随后按 ≤480 的 identifier 合同复验；成功外调返回超长 ID 时会跨过首个边界、在权威写前异常并被误归为 unknown | 外部响应 validator 与耐久记录 validator 对同一字段使用不同谓词 | channel adapter、receipt 映射、成功结果、unknown 恢复与后续消息引用 | 把平台消息身份的有界谓词纳入 transport 首个消费点并与耐久记录共用边界；超界输出稳定按畸形响应零追加处理，合法边界值可写 sent；补 480/481 参数化测试 | 已修复；首个运行时消费点与耐久记录共用 ≤480 标识谓词，480/481 边界测试通过 |
| U15-42 | [P1] job 协议与 owner job 投影允许最长 4096 的标识符，而 assignment、commit、executor 与 delivery 权威边界只允许 480；已实证 `validateTaskDefinition` 接受 481 字符 channel，随后同一冻结 plan 在 `prepareDeliveryEnqueues` 被拒绝，合法 job 可在提交 delivery companion 时永久失败；独立复核又确认 server RPC 与 artifact-retention GC 投影只验非空，会把超界值送到下游或弱化坏日志拒绝 | 同一 wire 标识合同在生产者、surface、耐久与恢复消费者各自维护不同可接受域 | TaskDefinition、JobOccurrence、job/resource-lease journal、dispatch、control、transcript、server RPC、artifact GC、job result/status/staged delivery、webhook binding 与历史重放 | 冻结协议标识统一为 ≤480 UTF-16 code unit；建立单一谓词，由 core protocol、delivery、surface、GC 及 owner/executor 投影适配复用，拒绝域在各自最早入口生效；参数化验证 480 接受、481 在来源处拒绝，并对全部执行点机械对账 | 已修复；协议、耐久、surface 与 artifact GC 共用单一谓词，来源、RPC、耐久及 GC 的 480/481 直接反例通过 |
| U15-43 | [P2] executor 的 received-only job control 恢复用例以非法逻辑流 `probe:job-control` 作为目标；多流投影开始在事务入口统一校验后，用例在模拟响应丢失前即被正确拒绝 | 测试夹具绕过了权威日志冻结的流命名合同，历史上仅因旧投影没有提前消费该参数而未暴露 | ControlAdmission 多流投影、job control received-only 恢复用例及同类自造流夹具 | 改用真实合法的 `job:<taskId>` 目标流，保留 received 后故障和首次耐久 ingress 恢复语义；全仓扫描不得再有非法 probe/test/dummy 流 | 已修复；两处改用真实 job 流，全仓同类扫描零残留，定向恢复用例 1/1 通过 |
| U15-44 | [P2] CLI 两个新增 authority setup 集成用例使用 Vitest 默认 5 秒时限；包全测并发真实 fsync 时分别在约 5.8 秒与 5.5 秒超时，隔离执行无逻辑失败 | 新增重 IO 用例未显式声明与 Windows 文件系统负载匹配的测试预算，重复了 U15-25 已识别的测试可靠性债务 | `setup-authority-delivery.test.ts`、CLI 包全测及 Windows fsync 负载 | 仅为两个真实重 IO 用例声明 15 秒预算，不删减断言或改动业务实现；定向与 CLI 包全测均通过 | 已修复；定向 3/3、CLI 包全测 159 文件/2320 项通过 |
| U15-45 | [P1] server 的 distributed-runtime 行为与结构 golden 未纳入冻结交付物；隔离门禁证明实际公开面与依赖拓扑已变化，旧基线会稳定失败 | 派生产物闭包只在表中声明应核对 golden，却未把两份具体基线列入文件集并执行最终差分 | `delivery.resolve` RPC、`server.info.deliveryStatus`、协议校验 import、owner/executor/server 依赖拓扑与最终 server 门禁 | 通过项目显式 golden 更新机制生成实际快照，逐项确认仅含第 15A 预期变化；两份 golden 纳入指纹，行为与结构定向门禁及 server 包全测通过 | 已修复；行为差异仅新增 RPC/状态投影，结构差异仅为真实 import 计数与既有允许边，定向 2/2、1/1 及 server 包全测 41 文件/704 项通过 |
| U15-46 | [P2] server 包全测并发负载下，distributed-runtime 行为 golden 超过默认 5 秒、结构扫描超过原 30 秒；隔离执行分别约 0.7 秒与 4.6 秒且业务/结构断言通过 | 两个真实文件系统与全仓扫描门禁的时限预算未覆盖 Windows 包级并发调度成本 | distributed-runtime 行为/结构 golden、server 包全测与 CI 稳定性 | 保留全部门禁内容，仅显式声明 15 秒/60 秒预算；定向与 server 包全测均通过 | 已修复；测试体与断言零变化，定向 2/2、1/1 及 server 包全测 41 文件/704 项通过 |
| U15-47 | [P1] spec 要求 `delivery-resolved` 与成功 control `applied` 同一原子提交；当前 full delivery reducer 只对 `enqueued` 执行 envelope companion 断言，孤立但结构合法的 `delivery-resolved` 可在冷重放中关闭 uncertain | 用户裁决的在线原子写入与历史重放合同未使用同一伴随谓词；正向用例只证明生产端会同写，未证明消费端拒绝孤立 resolution | delivery 三种用户裁决、ControlAdmission、DeliveryAuthority 冷启动、历史篡改/内部误用与状态补读 | 建立 `delivery-resolved` 与唯一成功 `delivery-resolve` applied 的双向单源伴随谓词，绑定同一 authority revision、结果类型及 resolution 身份；在线与全部 full consumer 复用；补孤立、错误结果、错误 revision、重复 companion 反例及三种正例 | 已修复；伴随唯一性、commit revision 与耐久 request/principal 逐字段绑定均由 full consumer 复验 |
| U15-48 | [P1] staged delivery 的 publish companion validator 把 `PublishRecord.conflicted.error: AuthorityError` 错按 `DeliveryFailure` 校验；两者错误码集合与消息上界不同，导致合法 publish 决定可在 append/fsync 后被 delivery reducer 判坏，非法 AuthorityError 也可能被接受 | 同一 PublishDecision 在 owner 权威 reducer 与 delivery companion 投影中各自实现了不同 accepted-domain，违反合同谓词单源 | conversation 混合 MutationBatch、staged delivery、publish-decision、append 后 reducer、冷启动重放与未来 publisher 接入 | 抽取并复用 AuthorityError/PublishDecision 的共享结构谓词；delivery 只投影其数据闭包可验证的身份与 granted outcome，不以 DeliveryFailure 代替；以合法长消息、非法错误码、计数/序号污染及混合批次验证写前与重放结论一致、非法候选零落盘 | 已修复；共享结构谓词覆盖 conversation、job 与 delivery companion 三类完整消费者 |
| U15-49 | [P1] 冻结标识合同要求所有 `dlv-` 身份为 `dlv-<26 字符 Crockford Base32 Ulid>`；当前 `deliveryItemId` 直接拼接 idempotencyKey 的 64 位十六进制摘要，生产、reducer 与 surface 又只按通用标识符校验，稳定生成不符合 wire 合同且不可按 ULID 排序的 itemId | 为响应丢失重放把对象身份错误等同为幂等摘要，绕过了全局前缀 ULID 单源 | 五类入队、唯一索引、日志重建、状态补读、控制请求及后续 mesh/迁居消费者 | 复用统一 prefixed-ULID 生成/验证原语，以 authority commit time 与域分离熵一次生成合法 `dlv-<Ulid>` 并耐久绑定到唯一索引；重放读取原 itemId，不从当前配置重造；生产端、reducer 与 surface 共用形状谓词，补固定向量、同键并发/响应丢失、重启及错前缀/错字母/错长度反例 | 已修复；固定向量及生产、耐久、GC、control、server 全入口形状对账通过 |
| U15-50 | [P1] `retry-risk-ack` 被定义为额外授权一次 attempt，但实现只置 `manualRetryCredits=1`，下一次 `attempt-started` 无论是否仍有自动预算都立即清零；pipeline、recordOutcome 与 reducer 又以总 attempt ordinal 对比 `maxAttempts`。例如 maxAttempts=3、attempt 1 uncertain 后裁决重试，手动 attempt 2 的 retryable 失败只剩一次自动机会，实际总发送仍为 3 而非自动 3 次 + 手动 1 次 | 把稳定 attempt 序号、自动发送预算和一次性手动授权合并为同一计数，多个执行点各自用 ordinal 推导预算 | 十五行 2/4/5/10/11/14、正常与迟到失败、任意未耗尽自动预算时的风险确认、pipeline/reducer/重放及 attempt 上界 | 分离 `attempt ordinal`、`automaticAttemptsUsed` 与待消费手动授权；风险确认后的下一次 start 明确消费手动授权但不占自动预算，是否 retry 由 authority 单源按剩余自动预算裁决，pipeline 不再重写 retryable；投影从耐久记录确定性重建，参数化覆盖 maxAttempts=3 时在 attempt 1/2/3 打开 uncertain、连续风险确认及重启 | 已修复；三种打开时机均证明自动预算与一次性手动授权独立且重启一致 |
| U15-51 | [P1] full reducer 的 `failed` 分支只验当前开放 attempt，可接受 `attempt=1,maxAttempts=3,error.retryable=true` 并直接终态；`delivery-uncertain` 分支也不验 frozen unknown policy，可在 idempotent redrive deadline 尚未到期时提前打开 uncertain。两条最小耐久日志均已实证被冷重放接受，分别违反十五行 5 与 7 的守卫 | 在线裁决与历史 reducer 各自实现状态转移条件；测试只覆盖 producer 生成的正向记录，没有以同一前态注入 sibling transition 反例 | 十五行 4～7、10～11，正常/迟到失败、恢复 redrive、冷启动、内部误用及未来日志消费者 | 抽取“失败应 retry 还是终结”和“started 应 redrive 还是 uncertain”的单源谓词，在线裁决与 full reducer 按各自数据投影复用；与 U15-50 的独立预算账本统一口径。参数化拒绝 early retryable failed、未到期 uncertain 及相邻错误 transition，并验证 nonretryable/预算耗尽 failed、manual/过期 uncertain 正例及冷重放 | 已修复；共享选择谓词与 sibling-transition 冷重放反例均闭合 |
| U15-52 | [P1] `DeliveryAuthority` 以可变 projection 配合 cursor 增量重放，但 `#transact/#synchronizeUnlocked` 任一异常只把 cursor 清空，不恢复 projection。已实证：合法 enqueue 后调用 `claim(windowMs=0)` 得到预期参数错误，紧接着 `list()` 从零把同一 enqueued 重放进旧投影并报“identity duplicated”，有效 authority 实例被一次失败调用永久毒化至重启 | projection 与 cursor 没有作为同一原子快照发布；异常恢复把“旧完整投影 + 空 cursor”组合成不可能状态 | DeliveryAuthority 全部读写入口、非法输入、reducer/append/存储异常、pipeline 自动 drain、响应重试与进程内可用性 | 将 projection+cursor 作为单一可替换快照；重放与决定在隔离副本上执行，仅成功后原子发布。失败保留最后已知良好快照，需全量重建时必须从全新空投影开始；入口预校验只作纵深防御。验证非法 claim 后读写仍正常，并注入 reducer、append、peer-tail/cursor 失败，断言后续读取为最后耐久状态或稳定原始损坏、绝不派生 duplicate corruption | 已修复；输入、reducer、响应丢失与 peer-tail 失败均保持原子快照语义 |
| U15-53 | [P2] core 包全测中两个新增 delivery IO 文件的 4 个用例在默认 5 秒处漂移超时，预算组合参数化用例随后贴到 15.2 秒；executor 新增 conversation delivery 原子派生用例也仅在包级负载下撞默认 5 秒；隔离逻辑均通过 | 新增真实文件系统用例未声明与包级并发负载匹配的测试预算，重复 U15-25 的可靠性债务 | core authority-outbox/queue/预算组合、executor assignment-ledger、Windows fsync 负载与 CI 稳定性 | 仅为实证超时文件或用例声明 15 秒，为实证超过该预算的参数化用例声明 30 秒；不改测试体和业务代码；隔离与受影响包全测均通过 | 已修复；core 隔离 8/8、参数化 3/3、受控并发包全测 2275/2275；executor 隔离 1/1、包全测 356/356 通过 |
| U15-54 | [P2] 新增运行时 `AuthorityError/PublishDecision` validator 放入冻结 contracts 区并使用 `unknown`，最终 `lint:contract-schemas` 明确拒绝 | 把可执行的外部数据校验误放进只承载冻结类型声明的 contracts 层 | core contracts 纯度、protocol 运行时出口、owner/delivery 消费者、结构 golden 与未来 schema 门禁 | 将 validator 原样迁入 protocol 运行时层，contracts 恢复纯类型出口，全部消费者统一从 protocol 引用；schema lint、类型检查、结构门禁、受影响包测试与全量构建通过 | 已修复；schema lint、core/owner 类型检查、结构门禁、受影响包全测及全量构建通过 |

## 已排除问题

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | ---------------- | -------- | -------- |
| U15-X1 | 删除 scheduler 对公开 enqueue 的直接依赖是否造成功能缺口 | 原排除只考虑第 26 单元产品接入边界，遗漏第 15 单元 S3 单机切换及“旧路径已拆、新路径未闭环不得合并”纪律 | Unit15 固定边界、生产装配检索与 Scheduler 暂存差异 | 已触发：旧投递删除且无新生产链接管 | 已重开→U15-05 |
| U15-X2 | CLI `tsc --noEmit` 的 credentials editor 既有报错是否由本单元引入 | 报错文件不在交付物，CLI tsup 构建与 delivery 行为装配测试通过 | 当前 32 文件指纹与 CLI 构建结果 | delivery 变更出现在错误链或构建失败 | 排除为既有非本单元问题 |
| U15-X3 | 第 15A 单元是否必须实现 delivery 终态记录的 27 天 sweep | §4.5 冻结的是共享权威日志保留合同；第 9 单元只交付 GC 基座，第 15A 边界是零生产流量的并存权威合同，目标与验收均不含共享日志压缩/删除机制。当前 `collectGarbage` 只回收未被日志引用的 artifact，不能据此把跨域日志 sweep 静默扩入 15A | 执行计划第 9、15A 项，§4.5，AuthorityCommitLog/ArtifactStore 接口与实现，冻结指纹 `f4748b4f...` | 15B 切换生产流量前仍无明确单元承接共享日志保留/压缩，或 15A 新增实际生产流量 | 排除为第 15A 实现问题；保留为后续生产切换前的架构计划核查项 |
| U15-X4 | CLI 既有 workscene 并发持久层用例在一次包全测中卡默认 5 秒 | 文件不在第 15A 交付物且与 delivery 输入闭包无交集；隔离复跑 1/1 仅 225ms，随后原始 CLI 包全测 2320/2320 通过 | `workscene-directory.test.ts`、冻结指纹 `5fb544c5...` 下的隔离与包级重跑 | 该用例在无第 15A 输入变化的独立运行中稳定逻辑失败，或失败链与 delivery 变更相交 | 排除为第 15A 问题；记录为一次范围外负载超时噪声 |
| U15-X5 | core 多次包全测中，既有 authority-storage 的 artifact 保护与 peer-lock 用例先后漂移撞默认 5 秒 | 两个失败集不稳定，文件与测试体均不在第 15A 交付差异；隔离分别 0.44 秒与 1.09 秒通过，无逻辑断言失败；受控并发全包绿色 | `authority-storage.test.ts`、候选指纹 `8f160adb...` 的漂移失败、隔离结果与 core 2275/2275 | 无本单元输入变化时独立运行稳定失败，或失败链与本单元 commit-log 语义变化相交 | 排除为第 15A 问题；不为范围外负载噪声扩大旧测试预算 |

## 迟发现教训

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | -------------------------- | -------- | ------------------------------------ | ---- |
| U15-L1 | U15-20；此前 U15-05/U15-18 边界裁决后仍记录“旧路径保持唯一” | 只检索了生产入口存在性，没有把“原样不动”展开为公共类型、实现、持久化语义、装配指向和原回归测试的基线差分 | 凡边界要求保留旧能力，逐项对 `HEAD`/冻结基线核对公开 API、运行时装配、耐久文件、错误/统计语义与原测试；兼容重写不能视为保留 | 本轮：差分确认旧组件身份、实现、scheduler 与测试均被替换，收齐为 U15-20 |
| U15-L2 | U15-21；U15-01 曾以“delivery-only 不生成 sidecar”结案 | 将终态决定记录和外部物化进度作为同一个布尔条件裁剪，未枚举单一 mutation kind 的最小批次 | 对每种 mutation 单独及混合构造批次，分别核对逐条终态决定、外部 work/progress 与反向 companion 三列，禁止以一个 `required` 谓词代替三类合同 | 本轮：delivery-only 最小批次静态走查确认决定缺失且反向守卫必拒 |
| U15-L3 | U15-22、U15-24；U15-06/U15-12 曾标记已修复 | 修复验收只观察当前派生 key 是否一致，未检查重放路径是否仍调用在线策略/IO，也未用最小畸形来源验证“合法事实”含义 | 所有“重放稳定/反向绑定”修复必须做两类反例：重放时在线编译器或存储写入不可用仍成功；来源记录逐字段删除/增加/污染均 fail-closed | 本轮：调用图与伪 source 正例测试证明两项修复未闭合，收齐为 U15-22/U15-24 |
| U15-L4 | U15-25、U15-44、U15-46；旧冻结前 authority IO 用例仅隔离验证通过 | 把隔离绿色当作重 IO/全仓扫描测试的可靠性证据，未在冻结前验证受影响包并发负载，也未显式声明时限预算 | 新增真实 fsync、文件系统或全仓扫描测试时，必须显式声明合理时限，并在冻结前至少一次置于受影响包全测负载中验证 | 本轮：core authority IO、CLI authority setup 及 server 行为/结构 golden 均在包全测负载下触发隐式或不足预算；保留测试内容并按实耗显式声明预算，定向验证通过 |
| U15-L5 | U15-26/U15-27；冻结终审第一轮记录零新增 | 第一轮验证了“attempt-started 在外调前”，但未继续枚举线性化点两侧全部本地前置步骤及其可变性/失败分类，误把“在前”当成“紧邻且资格已冻结” | 对每个外部副作用逐项枚举资格取得、内容准备、策略选择、adapter 绑定和外调；证明线性化点前失败零状态变化，确定的本地终结与 started 必须同 fsync，点后失败能区分确定未外调与结果未知，可变依赖必须捕获稳定快照 | 第二轮：transport 卸载、物化失败及 started→本地 failed 崩溃间隙证明发送前资格未形成单一原子闭包，收齐为 U15-26/U15-27 |
| U15-L6 | U15-28；冻结终审第一轮记录资源上界无新增 | 把 safe integer 当作派生时间安全的充分条件，未验证乘法、指数和 Date 定义域组合后的闭包 | 所有次数×退避、时间+窗口与 deadline 派生均以最小值、默认值和最大合法值做边界运算；合法输入不得产生 NaN/Infinity/RangeError，派生值仍须满足 wire validator | 第二轮：max safe delay/window 的静态代入证明 `toISOString()` 可抛错，收齐为 U15-28 |
| U15-L7 | U15-29/U15-34；冻结终审第一轮已记录时间与资源上界无新增 | 只验证派生值可表示，未检查权威时间是否在事务内产生，也未把配置域映射到实际运行时 timer 域 | 所有 deadline/定时器同时核对“由哪个线性化点定时”和“底层原语真实上下界”；用零值、上界、上界+1及事务排队反例验证 | 第二轮：调用方 retryAt 早于 commit、Node 将超界 timer 压成 1ms，收齐为 U15-29/U15-34 |
| U15-L8 | U15-30；冻结终审第一轮已记录 unknown 恢复无新增 | 审查了耐久策略的裁决结果，却未列出恢复路径仍读取的所有当前可变依赖 | 对每种开放状态列出恢复所需事实；已冻结策略不得再调用当前策略源，当前能力只控制是否实际外调，不得改写历史分类 | 第二轮：attempting 仍调用当前 outcomePolicy，异常会抑制合法 redrive，收齐为 U15-30 |
| U15-L9 | U15-31；U15-10/U15-15 曾标记已修复 | 把 TypeScript 接口当作运行时验证，并只检查最终权威记录，未追踪原始结果经过的中间 logger/event | 外部 adapter 输出在首个消费点做畸形对象与秘密载荷对抗；沿返回值、事件、日志、异常和耐久记录逐跳证明 fail-closed/零泄漏 | 第二轮：truthy success、未知 policy 与 Outbox 原始失败事件均绕过既有验收，收齐为 U15-31 |
| U15-L10 | U15-32；U15-02 曾标记已解决 | 迁居测试只覆盖“uncertain 已打开后改用其新快照提交”，没有保留真实 in-flight claim 的原响应身份跨 epoch 验证 | 身份字段逐一写明唯一用途；迁居分别插在 attempt-started 后、uncertain 前后，并始终用迁居前真实响应绑定验证当前 attempt，禁止测试重造新凭据 | 第二轮：原 claim 摘要含旧 epoch，当前 authority 复算/打开 fact 使用新 epoch，收齐为 U15-32 |
| U15-L11 | U15-33；冻结终审第一轮已记录状态历史闭合 | 逐行只断言内部 state，没有从“用户已看到上一条 actionable notice”反向验证每条出边是否可实时和补读关闭 | 对每个 actionable notice 枚举全部关闭出边；以 last-seen revision 验 history/live 均返回确切权威后继，事件不得用调用前快照猜 revision | 第二轮：late sent/retry 无 notice，late failed 事件可发布旧 revision，收齐为 U15-33 |
| U15-L12 | U15-35；U15-12/U15-24 曾标记已修复 | 只核对 owner producer 与 DeliveryAuthority，没有枚举同一日志的全部 full reducer 消费者 | 每个共享日志合同建立 full consumer 清单；所有 consumer 必须调用同一完整 reducer 入口，调用顺序兜底不能代替谓词单源 | 第二轮：delivery control 使用弱一层 reducer，收齐为 U15-35 |
| U15-L13 | U15-36；U15-17 曾标记已修复，U15-33 架构裁决后曾判断状态闭合 | 只检查 notice 是否存在与 revision 是否连续，没有从可执行 action 反推其全部必需参数，也没有把打开、全部关闭出边和控制请求逐字段对账 | 对每个带 actions 的 notice，从唯一公开补读接口出发列出执行对应控制请求所需的全部字段；再把打开通知、每类关闭通知和请求 guard 按事实身份逐字段对账，缺字段不得宣称控制闭环 | 集中修复后冻结前：三域 actionable uncertain notice 均缺 resolve 所需 `openFactDigest`；delivery 用户关闭与 run/job committed 自动关闭也无法配对，收齐为 U15-36 |
| U15-L14 | U15-37；U15-36 修复后直接验证曾声称观察者故障隔离 | 只在产品 EventBus 组合层注入坏 listener，没有直接对权威 API 的提交后读取与 Promise observer 逐点故障注入 | 每个“提交后通知”接口从 fsync 返回点向外枚举同步回调、Promise 回调、补读和事件总线；逐点抛错均不得改变已耐久结果，重放语义必须明确 | 当前冻结终审：`applyDeliveryResolutionControl` 直接等待补读和 observer，收齐为 U15-37 |
| U15-L15 | U15-38；U15-07/U15-14/U15-23 曾把无效 staged 内容裁决为逐条 conflict | 只验证了确定的 schema 错误，没有在同一 catch 边界注入 ArtifactStore missing/corrupt/瞬态 I/O | 任何“业务无效→逐条终结”的 catch 都必须以错误判别联合验证：确定业务错误可终结，存储/网络/未知错误不得被吞并为永久事实 | 当前冻结终审：conversation/job 两处无类型 catch 吞并全部编译故障，收齐为 U15-38 |
| U15-L16 | U15-39/U15-40/U15-41；此前闭包、资源上界和运行时验证均记录通过 | 分别验证编译器 references、常规大小输入和字段类型，没有沿真实 GC consumer、最终 logical-record 预算及下游耐久字段谓词做边界闭包 | 新增引用根必须跑真实 sweep；所有外置阈值须由 producer 与 validator 共用；外部字段逐一对账首个 runtime validator 与最终耐久 validator，并测试边界/边界+1 | 当前冻结终审：DeliveryContent GC 无端到端证据、taskName 可撑爆 enqueued、messageId 两层谓词不一致，收齐为 U15-39～U15-41 |
| U15-L17 | U15-42；修复后第一轮已记录 wire 资源上界与双层 validator 无新增 | 只对 delivery 自身 producer/validator 做边界闭包，没有反向比较上游 TaskDefinition/JobOccurrence 与下游 dispatch/delivery 对同一标识字段的完整可接受域 | 每个跨域字段建立 producer→冻结事实→全部消费者的 accepted-domain 对账；上游边界、surface 入口、最终耐久边界及各投影必须复用同一谓词，并以边界/边界+1 在最早入口和最终消费者同时验证 | 第二轮：481 字符 channel 被 TaskDefinition 接受而被 delivery 拒绝；同族扫描扩展到 job、resource lease、owner journal、executor、server RPC 与 artifact GC，来源及全部终端消费者均已补齐直接反例 |
| U15-L18 | U15-45；两轮终审与派生产物闭包表曾记录 golden 已闭合 | 只确认测试文件和结构门禁存在，没有把具体 golden 文件加入交付物清单并在冻结指纹下实际比较 | 任何公开 RPC、响应投影、包依赖或跨区 import 变化，冻结前必须枚举对应 golden 文件、执行只读比较并审阅差异；基线文件必须纳入交付物指纹 | 最终 server 验证：行为与结构门禁均在隔离后暴露稳定 golden mismatch；显式更新并逐项核实为第 15A 预期变化，收齐为 U15-45 |
| U15-L19 | U15-49；多轮标识 accepted-domain 审查曾记录通过 | 只统一了非空与 480 长度上界，没有把全局前缀注册表中的具体生成形状逐类对账，导致“字段可接受”被误当成“对象身份合法” | 新增任何带冻结前缀的身份时，必须同时对账生成器、wire validator、耐久 reducer、surface 与固定向量，并验证前缀、编码、长度及排序语义 | 本轮：沿 `itemId` 生产到补读/控制面核对，确认所有层共同接受 `dlv-<hex64>`，收齐为 U15-49 |
| U15-L20 | U15-50；十五行与单次手动授权此前记录通过 | 逐行只验证了“达到默认上限后再授权”，没有把 attempt 序号、自动预算和手动额度作为正交维度组合，测试夹具 maxAttempts=1 掩盖了提前 uncertain 的预算吞并 | 凡状态机同时存在序号、自动上限与人工追加额度，按额度来源分别建账，并在上限前、中、后触发人工授权；生产端、reducer 和恢复投影必须对剩余预算同结论 | 本轮：把 maxAttempts=3 代入 attempt 1/2 uncertain 路径，收齐 pipeline、recordOutcome、reducer 三处 ordinal 推导为 U15-50 |
| U15-L21 | U15-51；此前多轮记录十五行逐边闭合 | 只验证每行在线 producer 能生成正确记录，未反向证明 full reducer 会拒绝同一前态下的其他合法形状记录 | 状态表每个前态除正例外，必须把全部 sibling transition 作为反例注入 full replay；在线选择谓词与历史接受谓词须同源，不能以“生产端不会写”代替 reducer 守卫 | 本轮：attempting 前态下直接注入 early retryable failed 与未过期 idempotent uncertain，冷重放均错误接受，收齐为 U15-51 |
| U15-L22 | U15-52；此前 cursor 增量重放与冷启动测试通过 | 只测正常推进和重启，没有在“缓存投影已存在、事务失败、cursor 更新失败”三个条件交叉处验证快照一致性 | 任何缓存投影都把 state+cursor 视为一个原子快照；分别注入输入校验、decide、reducer、append 与 cursor/prefix 失败，下一次操作必须复用良好快照或从空状态重建，不得从零重放进旧状态 | 本轮：一次非法 redrive window 后紧接合法 `list()` 稳定派生 duplicate corruption，收齐为 U15-52 |
| U15-L23 | U15-47；此前控制原子提交与三种裁决正例均通过 | 只证明 producer 同写 resolution/applied，没有从耐久 request 反向绑定成功事实的全部身份字段 | 所有控制裁决按 received request→权威 fact→applied 三点逐字段对账 requestId、主体、epoch/fact、decision、认证 principal 与 authority revision；孤立、错配、重复 companion 均做冷重放反例 | 本轮：同 envelope 唯一性外继续发现跨请求事实可替换，已由 control full reducer 绑定耐久 request |
| U15-L24 | U15-48；PublishDecision 权威 reducer 曾通过 | 共享记录被 companion 投影复用时，只核对了目标业务字段，没有比较全部完整消费者的结构 accepted-domain | 共享判别联合出现第二个完整消费者时，先抽取公共结构谓词，再枚举所有 full consumer；投影适配只追加其可见业务绑定，不得重写公共字段校验 | 本轮：conversation、job 与 delivery companion 三点对账，统一 AuthorityError/PublishDecision 谓词 |
| U15-L25 | U15-53；U15-25 已识别同类问题但遗漏相邻新文件 | 只给当时失败的两个文件补预算，没有把“新增真实 IO 测试”作为完整集合持续对账 | 新增或扩展真实文件系统测试时，冻结前列出同交付物全部 IO 用例，记录显式预算或包级负载证据；只修实证超时，不按耗时阈值盲目扩张 | 最终验证：core 与 executor 三处新增 IO 测试仅在包级负载下超时；按实证补 15/30 秒并以两包全测闭合 |
| U15-L26 | U15-54；功能与两轮终审已通过后由最终 schema 门禁发现 | 只审 validator 的共享语义与消费闭包，没有核对可执行实现是否落在冻结声明区 | 新增运行时 validator 时先确认层级：contracts 只放冻结声明，protocol/runtime 放 `unknown` 输入与可执行校验；冻结前必须跑 schema lint 与结构门禁 | 最终验证：schema lint 拒绝 contracts 内 runtime validator；迁入 protocol 后门禁、结构、测试和构建全部通过 |

## 验证计划与证据账本

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| V15-01 | core delivery 合同、回归与派生产物 | `pnpm --filter @zhixing/core test`；`pnpm --filter @zhixing/core typecheck:contracts`；`pnpm --filter @zhixing/core lint:contract-schemas` | core 源码与测试 | 最终验证 / 高 | 受控并发包全测 138/138 文件、2275/2275；contracts typecheck、schema lint、源码类型检查与 build 通过 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过 |
| V15-02 | owner 五类生产者、原子性、状态外发与控制面 | `pnpm --filter @zhixing/owner-kernel test` | core dist + owner 源码与测试 | 最终验证 / 高 | 类型检查与包全测 5/5 文件、154/154 通过 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过 |
| V15-03 | executor 既有 conversation/job 合同不回归 | `pnpm --filter @zhixing/executor test` | core/owner dist + executor 测试 | 最终验证 / 高 | 新增 IO 用例预算定向 1/1；包全测 3/3 文件、356/356 通过 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过 |
| V15-04 | CLI/server/channel 的第 15A 影子 delivery 实例可重建，且既有生产路径保持唯一与行为不变 | `pnpm --filter @zhixing/cli test`；`pnpm --filter @zhixing/server test`；最后 `pnpm build` | 上游 dist + CLI/server/channel 装配 + owner/executor/runtime 生产链 | 最终验证 / 高 | CLI 159/159 文件、2320/2320；server 41/41 文件、704/704；结构 golden 1/1；全量构建通过 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过 |

## 终审记录

| 轮次 | 审查侧重 | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论 |
| ---- | -------- | ------------ | -------- | ------------ | ---- |
| 第一轮 | 需求、架构、功能闭环、状态、回归 | 完整 | 0 | `sha256:fc62116b1e8bd282a462407dff2a4f3d7bac9edf712dffbf2a1370fa8b9dd7fe` | 通过；十五行、五类来源、控制闭环、旧生产链与 15A/15B/26 边界无新增问题 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 完整 | U15-29～U15-35 | `sha256:fc62116b1e8bd282a462407dff2a4f3d7bac9edf712dffbf2a1370fa8b9dd7fe` | 未通过；七个根因族已沿生产、恢复、迁居、状态消费、诊断与完整 reducer 消费点收齐，转问题裁决 |
| 当前候选第一轮 | 提交后观察者、错误分类、GC 闭包、wire 资源上界与双层 validator | 完整 | U15-37～U15-41 | `sha256:d9895b2dc6e32c7e47c7d6081e8c9fc2b5665a6025f9290fe91044c60e5e435e` | 未通过；五项已按同根执行点横扫并转集中修复 |
| 修复后第一轮 | 需求、范围边界、五类来源、十五行生命周期、控制闭环、旧新生产链与派生闭包 | 完整 | 0 | `sha256:3dcf76ce89977a69f329c10de6fb39b6ecaf16b97cbf7daaa1447fd138301e31` | 通过；U15-37～U15-41 修复与完整架构合同对账无新增问题 |
| 修复后第二轮 | 并发、崩溃、安全、资源边界、恢复与跨域 accepted-domain | 完整 | U15-42 | `sha256:3dcf76ce89977a69f329c10de6fb39b6ecaf16b97cbf7daaa1447fd138301e31` | 未通过；job 上游与 delivery/dispatch 下游标识域分叉，已按全部执行点转集中修复 |
| 新候选第一轮 | 需求、范围、五类来源、十五行生命周期、控制闭环、旧新生产链、跨域 accepted-domain 与派生闭包 | 完整 | 0 | `sha256:1661835e453e2090238175925f1616453af9a6b61719885f70f8e685de162e1c` | 通过；协议、耐久和 surface 全执行点闭合，未越过 15A/15B/26 边界 |
| 最终候选第一轮 | 需求、范围、五类来源、十五行生命周期、控制闭环、旧新生产链及 accepted-domain 全消费者 | 完整 | 0 | `sha256:f4748b4f81175714de6cde5f8f608b4561dbf73afe003a9d1d1cf760af881024` | 通过；五类来源及十五行逐边闭合，新权威零生产且旧路径唯一，15A/15B/26 边界与全部终端消费者无新增问题 |
| 最终候选第二轮 | 并发、崩溃、安全、资源上界、恢复重放、GC、兼容与测试盲区 | 完整 | 0 | `sha256:f4748b4f81175714de6cde5f8f608b4561dbf73afe003a9d1d1cf760af881024` | 通过；外调线性化、策略快照、异常分类、控制观察者、完整 reducer、引用闭包和协议标识域无新增问题；共享日志 27 天 sweep 按 U15-X3 排除出本单元 |
| 夹具修复后第一轮 | 需求、范围、功能合同、恢复语义及既有完整事实包 | 完整 | 0 | `sha256:110b67f98bf973ca804aa7435e4ca1db47ce79056de99841be71a8ff1ef392a0` | 通过；唯一业务差异是测试目标流改为真实 job 流，received-only 故障插点、耐久 ingress 与原结果恢复语义不变，生产实现零变化 |
| 夹具修复后第二轮 | 权威流命名、安全边界、同类夹具、回归影响与指纹闭包 | 完整 | 0 | `sha256:110b67f98bf973ca804aa7435e4ca1db47ce79056de99841be71a8ff1ef392a0` | 通过；全仓无其他非法 probe/test/dummy 流，真实目标流通过统一 validator，定向恢复用例通过且不改变 15A/15B/26 边界 |
| 时限修复后第一轮 | 测试语义、断言完整性、业务实现与冻结事实包 | 完整 | 0 | `sha256:5fb544c5b57c7a5a3053f044ace3af97783f97a7b87f0203f7464c51b336b10c` | 通过；唯一业务差异是两个 CLI 重 IO 用例显式声明 15 秒预算，测试体、断言和生产实现零变化 |
| 时限修复后第二轮 | 同类测试预算、范围边界、输入闭包与回归影响 | 完整 | 0 | `sha256:5fb544c5b57c7a5a3053f044ace3af97783f97a7b87f0203f7464c51b336b10c` | 通过；预算仅覆盖两个新 authority setup 重 IO 用例，定向 3/3 通过，core/owner/executor 既有证据输入闭包未失效 |
| golden 同步后第一轮 | 公开 RPC、状态投影、行为兼容与派生闭包 | 完整 | 0 | `sha256:ef80c2d56d665bd376514c573e3c4c9cb984f70875c6843a7a0accdaccd7cac7` | 通过；行为 golden 仅新增 `delivery.resolve` 与 `deliveryStatus`，与第 15A 影子装配合同一致，无其他行为漂移 |
| golden 同步后第二轮 | 包拓扑、跨区 import、依赖方向与范围边界 | 完整 | 0 | `sha256:ef80c2d56d665bd376514c573e3c4c9cb984f70875c6843a7a0accdaccd7cac7` | 通过；结构 golden 仅记录协议校验与 owner/executor/server 真实引用变化，结构门禁 1/1 通过，无非法新边或 15B 生产切换 |
| server 时限修复后第一轮 | 门禁语义、断言完整性、golden 内容与业务实现 | 完整 | 0 | `sha256:47e06e57c0b944220d341f4b605c27591594caf0372f3d94ef67de2604d9d9e6` | 通过；唯一差异是行为/结构门禁时限分别改为 15 秒/60 秒，测试体、golden 与生产实现零变化 |
| server 时限修复后第二轮 | 预算边界、包级负载、范围与输入闭包 | 完整 | 0 | `sha256:47e06e57c0b944220d341f4b605c27591594caf0372f3d94ef67de2604d9d9e6` | 通过；预算覆盖已实证包级耗时且不跳过任何门禁，core/owner/executor/CLI 有效证据输入闭包未变 |
| 独立终审 | 完整交付物、原子伴随、生产与重放 accepted-domain、同族消费点与测试盲区 | 完整 | U15-47～U15-48 | `sha256:47e06e57c0b944220d341f4b605c27591594caf0372f3d94ef67de2604d9d9e6` | 未通过；用户裁决伴随合同与 staged publish companion 谓词均未闭合，转集中修复 |
| 再次独立终审 | 完整交付物、冻结身份注册表、额度组合、跨流原子性、生产/重放 accepted-domain 与同族测试盲区 | 完整 | U15-49～U15-50 | `sha256:47e06e57c0b944220d341f4b605c27591594caf0372f3d94ef67de2604d9d9e6` | 未通过；新增两项已沿全部执行点收齐，与 U15-47～U15-48 一并转集中修复 |
| 补充独立终审 | 完整交付物、十五行 sibling transition 反例、缓存投影/cursor 异常原子性及相关测试盲区 | 完整 | U15-51～U15-52 | `sha256:47e06e57c0b944220d341f4b605c27591594caf0372f3d94ef67de2604d9d9e6` | 未通过；两项均以最小运行复现确认，已沿在线生产、full replay、恢复与后续调用收齐，与 U15-47～U15-50 一并转集中修复 |
| U15-47～52 修复后第一轮 | 完整需求、五类来源、控制请求与伴随、身份注册表、预算组合、sibling transition、快照异常与派生基线 | 完整 | 0 | `sha256:728a84cbd67e36f4e50aee69f5b535f7151b6e31dd0946b11e437933a74c7324` | 通过；六项及同根执行点闭合，规范与结构 golden 同步，未发现新增真实问题 |
| U15-47～52 修复后第二轮 | 并发、崩溃恢复、响应丢失、跨 epoch/attempt、缓存快照、全消费者 accepted-domain 与未来接入边界 | 完整 | 0 | `sha256:728a84cbd67e36f4e50aee69f5b535f7151b6e31dd0946b11e437933a74c7324` | 通过；机械事实共享、裁决独立，未发现新增真实问题 |
| U15-53 修复后第一轮 | 测试体、断言、业务实现、同族 IO 文件与范围边界 | 完整 | 0 | `sha256:8f160adb0ff6bf9708994a7322c565298568a750792e9359f5b530c14853d492` | 通过；仅实证超时文件/用例新增显式预算，隔离 8/8 与 3/3 通过，业务实现零变化 |
| U15-53 修复后第二轮 | 时限边界、包级并发条件、冻结事实包与派生闭包 | 完整 | 0 | `sha256:8f160adb0ff6bf9708994a7322c565298568a750792e9359f5b530c14853d492` | 通过；同族文件已对账，旧范围外噪声按 U15-X5 排除，无新增真实问题 |
| 最终分层收口第一轮 | contracts/protocol 分层、共享 validator 语义、全部生产与重放消费者、测试预算及结构基线 | 完整 | 0 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过；contracts 恢复纯声明，运行时谓词在 protocol 单源，executor 预算只改变时限，结构差异仅为预期 import 合并 |
| 最终分层收口第二轮 | 出口拓扑、冷重放调用链、跨包构建、golden、回归输入闭包与范围边界 | 完整 | 0 | `sha256:a82136d1faaf403c4d717e0ab0f140cc244c5b8cf9950950359a64bb89a36c12` | 通过；无旧 contracts validator 引用或新增非法依赖，全部最终门禁属于同一未修改业务交付物，未发现新增真实问题 |

<!-- registration-complete: unit-15a.gen-1 -->
