# 单元登记:第 28 单元 · generation 1

<!-- 身份头部:登记时填写,登记后不可变 -->

- **unitId**:28
- **generation**:1(仅经用户显式授权递增;同单元的修复、复审、终审轮复位是本文件内的状态推进,不产生新登记)
- **登记时间**:2026-08-05
- **登记来源**:用户要求将第 28 单元独立审查及价值裁决后的当前问题、已删除问题分别转入正式问题列表和已排除问题

> 本文件是该单元代际的唯一状态源;登记协议、写序纪律与幂等判定见工作台主文档静态区。以下节结构、各表说明与表头、审查结论复用表、问题/排除/教训表、两轮终审行是固定骨架,只维护字段值与数据行。

## 当前状态

- **当前状态**:U28-01～U28-16 均已验证；U28-15、U28-16 已在同一冻结指纹上完成专项功能审查、四路冷启动对抗复审与反证差异审计；独立审查清单受影响项待重审
- **连续无新增问题轮数**:0 / 2
- **交付物是否冻结**:是（U28-15、U28-16 专项审查输入已冻结；独立审查清单受影响项待重审）
- **交付物文件集**:第 28 单元起点 HEAD `e0a56fad` 至当前工作区的 164 个非工作台路径，删除 0；CLI 44、core 48、executor 5、orchestrator 11、owner-kernel 18、runtime-host 5、server 13、tools-builtin 10、架构与规格 10
- **当前交付物指纹**:`git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca`；路径集 SHA-256 `2e352424a2e9c0f999c8892900345494cdfdafee544061eba14729c730473a4b`
- **架构来源**:分布式运行时总纲、可执行规格、持续在线需求与 S2 安全评审；memory、skill、workscene、work mode、runtime lifecycle、transcript/attention window、Task 子 agent、文件编排及其直接上游合同；第 12～27 单元冻结合同、适用排除项/迟发现教训及第 28 单元定稿开发清单

## 固定边界

- **功能范围**:第 28 单元（S7）编排、memory、skill、workscene、task-list、segment 与 lifecycle 的 staged/commit/publish 接入，以及 Task/DAG 子租约和双拓扑生产装配
- **架构不变量**:run 内写只进入 assignment staged overlay；owner 决策与 CommitEnvelope 是唯一提交事实；锚点 adapter 是 global 唯一物化点；failed/cancelled/uncertain 不外泄；同机与 mesh 只换 transport 不换语义；子租约按子先父后收敛
- **验收条件**:第 28 单元定稿开发清单 D28-01～D28-09 与独立审查清单 IR28-01～IR28-45；P0/P1 清零后按工作台完成冻结终审、独立功能审查和必要验证
- **必要上下游**:core 合同与严格 codec、executor assignment ledger/ResourceGovernor、owner-kernel conversation/job commit 与恢复、runtime-host/orchestrator/tools staged/query 消费、CLI/server/RPC 产品反馈及双拓扑装配
- **明确不属于本单元**:第 29～38 单元能力；通用 outbox/事件总线、监控、诊断、benchmark、性能采集或未来扩展框架；改变 memory、skill、workscene、task-list、segment 的既有产品能力与确认边界

## 派生产物闭包

> 表头固定。交付物文件集中的每个文件或同类组至少落入一行;派生项包括 lockfile、golden、schema/快照、生成清单及结构/导出基线。结论只允许“待核查”“通过”或“不适用:依据”;存在未归类文件或待核查项不得冻结。

| 交付物变化(文件或同类组) | 派生关系与必须同步/核对项 | 低成本检查与证据 | 结论 |
| ------------------------ | ------------------------- | ---------------- | ---- |

## 关键原语核查

> 表头固定,每个关键原语一行;五项必须落到具体事实,结论只允许“待核查”“通过”或“有问题:编号”。任一行未通过,问题盘点不得结束。

| 关键原语 | 唯一事实源 | 生效/线性化点 | 崩溃与竞争插点 | 生产者/消费者 | 时间、空间、重放与队列上界 | 结论 |
| -------- | ---------- | ------------- | -------------- | ------------- | -------------------------- | ---- |
| publish decision × batch | owner 日志中的不可变 `MutationBatch` 与 `publish-decision` | owner 同一提交决策写入；写入及两 execution 重放前均过公共联合谓词 | 合法 applied、错域/错键/错 revision、响应丢失与重启重放 | conversation/job producer；publisher、delivery participant、surface | outcome 与 batch record 一一对应；恢复进度按 batch 有界，重放幂等 | 通过 |
| 逐项 publish 反馈 | 同一 decision、batch 与 committed assignment | conversation final-outbox / history 和 job notice 与权威决定绑定后派生 | live/补读交错、回调失败、响应重放、部分冲突 | conversation/job owner；server、CLI、channel 与 scheduler notice 消费端 | 每个 global outcome 至多一个稳定身份；surface 去重有界且不重算权威字段 | 通过 |
| memory CAS 与同 run overlay | memory authority 日志 + 当前 assignment staged overlay；共享 logical-entry 投影 | staged receipt 只进 overlay；owner publish CAS 是外部生效点 | 首次/连续写、同 run 多写、并发 winner、delete/不匹配补位 | memory tool/flush；adapter、runtime search 与模型上下文 | search 只扩取 `limit + relevant overlay count`，去重排序后截断 | 通过 |
| skill window 声明 | assignment-bound GlobalQuery 的 catalog revision 与 runtime window generation | 每个新窗口首个 prompt 消费前由 runtime 唯一刷新 | clear/resume、run 内换代、缺查询上下文 | runtime 唯一 producer；skill index 与 prompt | 每窗口一次有界 catalog 读取，无构造期第二权限面 | 通过 |
| GlobalQuery 严格边界 | GlobalStatePort 权威结果 + core `query + result` 公共 validator | local read 前后、mesh server encode 前与 client decode 后 | accessor/隐藏字段、错 kind/id/scope、坏 digest、路径字段、串线响应 | 同机 assignment、anchor mesh server、executor mesh client；各 runtime 读消费者 | 闭合联合逐变体校验；沿用既有查询上界，不新增缓存或队列 | 通过 |
| descendant child 终结 | executor ResourceGovernor 耐久日志与稳定 reservation/usage 身份 | 同一 governor 事务保守 consume open usage，再最深优先终结子树 | provider/abort/timeout、半提交、终结响应不明、重启与并发 child | Task/DAG factory、assignment finalizer；usage intake 与父 run | delegation depth/预算有界；重放零重复计费，父终态前零 active descendant | 通过 |
| 七项直接交界 | 上述六项权威事实的稳定身份与提交顺序 | `U28-01↔02`、`U28-04↔06↔07`、`U28-03↔05` 各自在权威边界交接 | decision 重放、overlay 查询、wire fail-closed、child 查询失败后的资源收敛 | owner、runtime、mesh、governor 生产装配 | 不新增第二事实源、通用 outbox/codec/owner 日志或无界设施 | 通过 |
| global staged 精确前缀决策 | AuthorityCommitLog 与其精确 `authorityPrefixLsn` | 四域 planner 在同一日志/投影写栅栏内追平并产生不可变 delta 后，单次 fsync 提交 | 跨 owner 并发、cache 落后、部分冲突、响应丢失、崩溃与投影恢复 | conversation/job/control writer；schedule/memory/skill/workscene planner、投影与 facade | 每个合法前缀最多一个确定后继；fsync 后发布不可失败且重放有界 | 通过 |
| job publish 自主恢复 | job owner 日志中的 publish decision、granted plan 与 progress | CAS committed 后只按 granted seq 物化并耐久推进 progress | 混合冲突、任一物化边界崩溃、executor 离线与连续 owner 重启 | job owner；四域 materializer、facade 与兼容投影 | conflicted 零 apply；同 pending 身份单飞并由有界索引重驱至 settled | 通过 |

## 审查结论复用表

> 每行一个可独立失效的完整功能或合同事实链，生产端、事实源、消费者、异常终态和测试不得拆开；无法独立指纹、独立失效或需重读多数其他项时合并。整表须覆盖固定边界、全部交付文件、关键原语和九类核查面。
>
> 常设一项跨项组合推演。其他项均已取得或复用本轮结论后，再审查组合项；组合项按编号汇总各项当前输入指纹与结论。任一其他项新增，或其边界、输入指纹、状态或结论变化时连带失效。
>
> 只有覆盖全部登记输入且该项结论无问题的问题盘点或冻结终审可计次，每轮每项至多一次，证据列须引用审查轮及证据；某项发现问题只清零该项，同一输入达到 2/2 后才可持续复用。状态只允许“待审”“审查中”“通过”“失效”“有问题:编号”，独立深审只允许“0/2”“1/2”“2/2”。

| 编号 | 审查目标与核查面 | 登记输入（关键实现、全部生产点、消费路径、测试） | 最近通过的输入指纹（算法 + 值） | 重审条件 | 当前状态 | 有效独立深审 | 本轮结论与证据 |
| ---- | ---------------- | ------------------------------------------------ | ------------------------------- | -------- | -------- | ------------ | -------------- |

## 问题列表

> 每个根因只保留一行;“完整影响面”固定写明生产端、类型组合、消费者、异常终态和测试;状态只允许“待裁决、待修复、修复中、待验证、已验证”。已解决问题保留到单元完成。

| 编号 | 事实与证据 | 根本原因 | 完整影响面 | 最优解决方案与验收条件 | 状态 |
| ---- | ---------- | -------- | ---------- | ---------------------- | ---- |
| U28-01 | **P1，工作量：中；来源 P28-01。** `PublishRecord` 允许 granted workscene 携 `appliedResult`，公共 `validatePublishDecisionRecord` 却只允许 `t/targetRevision`，合法 owner 记录会在读取或重放时被判坏账；反向放宽字段后，conversation/job 的 batch-bound 重放又没有统一核对 outcome 与对应 mutation 的 kind、对象身份、对象 revision 和 `targetRevision`，畸形结果可延迟到物化时才失败。证据：`packages/core/src/contracts/records.ts:406-423`、`packages/core/src/protocol/contract-validation.ts:49-90`、`packages/owner-kernel/src/conversation-assignment.ts:4392-4550,10422-10521,10564-10629`、`packages/owner-kernel/src/delivery-participant.ts:451-543`。**价值裁决记录：**原结论曾由 P1/中收窄为 P1/小；冷启动事实链证明不仅要接受一个字段，还须闭合 decision×batch 的联合语义，恢复为 P1/中。仅当权威合同撤销联合校验时才重开。 | publish outcome 的结构校验与 batch 语义校验分叉：公共 validator 不认识合法 applied 分支，owner 私有谓词又未成为 producer、conversation replay 与 job replay 共用的单一合同。 | 生产端：conversation/job publish decision；类型组合：granted/conflicted、workscene 四操作、非 workscene、session/global；消费者：owner reducer、delivery participant、commit/replay、workscene publisher；异常终态：合法记录 fail-stop，或错 operation/sceneId/revision/targetRevision 在重启物化时形成永久待办；测试：两 execution 的生产、codec、batch-bound 重放及逐字段错绑负例。受影响审查项：IR28-03、08～10、23、27～28、36、39～40、45。 | 收敛为一个公共联合谓词：先严格校验 decision/outcome 结构，再以对应 `MutationBatch` 逐项校验；workscene granted 必须携完整 `appliedResult`，operation、sceneId、对象 revision 与外层 `targetRevision` 全等，非 workscene 必须省略。conversation producer/reducer 与 job participant 全部复用，删除私有分叉。完成标准：合法两域记录提交/重放通过，任何缺件、错域、错对象、错 revision 或额外字段均在进入投影前 fail-closed。 | 已验证 |
| U28-02 | **P1，工作量：中；来源 P28-03。** owner 已耐久产生逐项 granted/conflicted/applied 结果，但 conversation `FinalFrame` 只保留冲突数量，`publishConflicts()` 无生产调用者，成功 workscene applied 结果也无 surface 消费；job decision 同样没有进入 occurrence 维护投影。证据：`packages/owner-kernel/src/conversation-assignment.ts:4392-4543,4726-4735,4980-5034,10856-10864`、`packages/owner-kernel/src/job-assignment.ts:4292-4347`、`packages/rpc/src/session-events.ts:76-96`、`packages/owner-kernel/src/scheduler-user-notices.ts`。 **反证 C28-02：**专项证据对账发现 job 路径只有源码追踪与 notice-journal 单测，缺真实 job commit producer 的逐项通知断言；已补入真实 job commit producer 用例并验证通过。 | 权威 publish decision 没有派生到既有 conversation 控制事件与 job 维护通知，提交终态和用户可见终态断开。 | 生产端：conversation/job publish decision；类型组合：逐项 granted/conflicted、四类 workscene applied result、部分冲突与重放；消费者：FinalFrame、同会话 control observer、CLI/RPC/channel、job occurrence 维护投影；异常终态：CAS 冲突静默、成功 create 的 sceneId/revision 不可行动、断线补读遗漏或响应丢失后重复反馈；测试：conversation/job、部分冲突、applied、live/补读重放和双拓扑产品文案。受影响审查项：IR28-11、23、25、27、30、37、45。 | 只从 owner decision 派生反馈：conversation 保留 FinalFrame 计数，由 final-outbox live 发布和 `finalHistory` 补读共同从同一 decision 派生 `PublishConflictNotice` 与 workscene applied 结果，再投到既有 `scope:"control"` 通道；job 把同一逐项结果投到既有 occurrence/`SchedulerUserNoticeJournal` 维护链。反馈身份绑定 assignmentId+seq+decision，surface 按该身份去重，不重算 sceneId/revision，不建新通知基础设施。完成标准：live、断线补读和响应重放呈现同一可行动结果且不重复副作用。 | 已验证 |
| U28-03 | **P1，工作量：中；来源 P28-04。** Task/DAG child 在 provider 抛错、abort、timeout 或 `message_end` 前结束时可遗留 open usage，settle/release 失败或响应不明后也可保持 active。ResourceGovernor 日志、稳定 child identity、`flushAssignment` 与 worker 重试已是耐久恢复锚点，但 flush 只保守收敛 usage/report，不终结遗留 descendant child；assignment child 又被通用 expiry reclaim 排除。证据：`packages/orchestrator/src/subagent/factory.ts:213-237,349-470`、`packages/executor/src/resource-governor.ts:439-696,778-789`、`packages/core/src/protocol/resource-governor.ts:915-960,1059-1068`。**价值裁决记录：**原结论为 P1/中大，并因误判“没有耐久 owner”要求新增 assignment owner 义务日志；源码证明现有 governor 与 finalizer 已提供耐久重驱，缺口仅为 finalizer 未扫尾 descendant。新决定为 P1/中，禁止新增 owner 日志。仅当现有日志无法稳定枚举子树或 finalizer 不具备耐久重驱入口时重开。 | governor 已有单租约终结和 assignment usage finalizer，但缺少二者共用的“保守消费 open usage 后按最深优先终结 child 子树”事务；factory 只调用会因 open usage/active child 拒绝的单租约 settle/release。 | 生产端：Task/DAG child acquire、metering、settle/release 与 assignment flush；类型组合：父/子/孙 lease、open usage、active/settled/released、稳定 child identity；消费者：ResourceGovernor、assignment worker、后续 child 预算；异常终态：provider 或 child tool/GlobalQuery fail-closed、abort/timeout、terminal 响应丢失、嵌套/并发 child、重启重放；测试：上述插点、子先父、幂等与父终态零 active child。受影响审查项：IR28-13、15～16、27～28、30、36、45。 | 在现有 ResourceGovernor 内抽取幂等子树终结事务：先按预占上限消费目标子树 open usage，再最深优先 settle/release descendants 并 settle 目标 child；现有 child `settle` 作为 factory 正常快路径调用它，随后 release 目标 child，`flushAssignment` 在生成最终 report/watermark 前对全部遗留 descendant 复用。响应不明继续由现有终态幂等与 worker 重试收敛。完成标准：所有终态和重启插点最终零 active descendant、预算归还且 usage 不漏不重。 | 已验证 |
| U28-04 | **P1，工作量：中；来源 P28-05。** MemoryFlush 对 people/profile 的稳定 ID upsert 未读取当前 authoritative digest，首次落盘后第二次 flush 必被 CAS 拒绝；现有 runtime overlay 的预测 digest 又遗漏 `domain`，且通用 list helper 固定查询 `domain:"memory"`，不能为 people 写取得正确前像，同 run 第二写即使补 `expectedDigest` 仍会错绑。证据：`packages/orchestrator/src/runtime/create-agent-runtime.ts:797-853,1298-1348,2355-2426`、`packages/core/src/memory/global-state-adapter.ts:178-217,376-447,467-492,554-587`。**价值裁决记录：**原结论由 P1/中收窄为 P1/小；冷启动核对发现还须单源化 authority/runtime 的 entry identity 与 digest，恢复为 P1/中。仅当同 run 连续 flush 被产品合同取消时才重开。 | lifecycle flush 未读取 current digest，runtime overlay 又独立重算且不全等于 authority planner；CAS 前像、同 run 预测和域映射存在三套语义。 | 生产端：三域 MemoryFlush staged upsert；类型组合：首次创建、既有 people/profile、同 run 同键多写与真实并发；消费者：memory adapter、overlay 与后续检索；异常终态：持续冲突、预测摘要错绑、重放、并发 CAS winner；测试：首次/连续更新、people/profile 域、同 run 多写、并发冲突和重放。受影响审查项：IR28-19、25、27、29、37、45。 | 从现有 memory authority planner 提取唯一 entry identity/digest 投影供 adapter 与 runtime overlay 共用；flush 按 people→people、profile→memory/profile 查询当前权威+本 assignment overlay，并把全等 digest 写入 `expectedDigest`，journal 保持追加语义。完成标准：首次、跨 run 连续及同 run 连续 flush 均可提交，两个并发 assignment 仍只由 CAS 确定 winner，重放不改变前像。 | 已验证 |
| U28-05 | **P1，工作量：中；来源 P28-08。** mesh `GlobalReadResult` 只检查顶层 `kind` 后强制转换，没有逐变体 exact-keys、嵌套 DTO、摘要、path-free 字段或原 query 反绑。证据：`packages/cli/src/serve/global-query-mesh.ts:35-98,101-190`、`packages/core/src/contracts/state.ts:132-195`、`research/design/modules/distributed-runtime/specification.md:30-55,609-646`。**价值裁决记录：**原结论为 P1/中；曾因“正常 producer 尚未产生畸形结果”错误降为 P2/中。受支持的 wire 故障、对端异常和协议漂移本就是当前分布式场景，且规格冻结递归严格/path-free 合同，新决定恢复 P1/中。只有响应不再跨 wire或权威合同撤销该边界时才重开降级。 **反证 C28-01：**固定矩阵核对发现同机 `createAssignmentGlobalQueryPort` 仍直接透传 query/result，local 与 mesh 尚未共用同一严格合同；已修复并由 local/mesh 直接用例补证通过。 | GlobalQuery 的 local/mesh 入口未共用与请求同等级、且绑定原 query 的公共严格 validator，读取边界不能证明对象图与请求语义满足合同。 | 生产端：同机 assignment GlobalQuery、anchor mesh server 与 executor mesh client；类型组合：memory search/list/stats、trust、schedule、workscene list/get、skill catalog/get、config asset、asset index 及全部嵌套 DTO；消费者：memory/skill/workscene/runtime 管理读；异常终态：未知字段、错 kind/对象、坏摘要/revision、查询串线、路径泄漏与协议漂移；测试：同机 read 前后、服务端 encode 前、客户端 decode 后的全部正负变体及双拓扑。受影响审查项：IR28-35、40～41、43、45。 | 在 core 为现有联合建立一个 `query + result` 公共递归严格 validator，同机 read 前后、服务端 encode 前和客户端 decode 后复用；逐变体 exact-keys/类型/上界/摘要/path-free 校验，并反绑 scope/domain/id/kind 等查询条件，不新增 codec 框架。完成标准：全部合法变体通过，任何嵌套错形、查询串线、坏 digest 或路径字段均在进入 runtime 前 fail-closed。 | 已验证 |
| U28-06 | **P2，工作量：中；来源 NB28-01。** `readMemoryOverlay` 在 memory-search 的 base hits 上追加同 scope 的全部 staged memory，既不反绑请求 domain，也不重跑 query/ranking/limit；base 已先截到 limit，若 overlay 删除或改成不匹配，权威第 `limit+1` 个候选也无法补位。证据：`packages/orchestrator/src/runtime/create-agent-runtime.ts:825-837,2355-2426`、`packages/core/src/memory/global-state-adapter.ts:178-217,582-587`。**价值裁决记录：**原结论为 P2/小；冷启动核对证明精确 top-N 不能靠末端过滤修好，还须先按 overlay 规模扩取 base 并冻结 staged 排序语义，新决定为 P2/中。仅当权威 query 已能返回未截断候选或产品合同撤销精确 top-N read-own-writes 时才重开。 | overlay 只实现键覆盖，没有复用 authoritative search 的域过滤、匹配、排序和候选窗口语义。 | 生产端：同 run staged memory 与 memory-search；类型组合：跨 domain、匹配/不匹配 upsert、delete、重复 ID、排序边界和 limit 补位；消费者：runtime/model context；异常终态：其它域或无关记忆污染、错误 top-N、删除后漏项、超出上限；测试：跨域、无关词、更新覆盖、删除补位、排序去重与上限。受影响审查项：IR28-19、37、45。 | 先读取本 assignment overlay，按相关变更数有界扩取 base 候选，再用 U28-04 单源 entry 身份/投影及 authoritative search 的 domain/query 谓词合并去重；排序把匹配的 staged 写视为晚于 base、同为 staged 时按 recordSeq 降序，其余复用权威 comparator，最后 slice(limit)，不得伪造 durable `updatedAt`。完成标准：同 run read-own-writes 保持，跨域/不匹配记录不出现，删除或更新后正确候选补位且结果严格不超上限。 | 已验证 |
| U28-07 | **P2，工作量：小；来源 NB28-02（原 P28-06）。** 文档及源码旧注释把 skill-index 冻结为构造期 `onWindowOpen` lifecycle subscriber，生产实现由 runtime 在新窗口首 run 和 run 内换代显式刷新；第 28 单元已把 skill 读取权威收窄为 assignment-bound GlobalQuery，构造期没有合法查询上下文。证据：`packages/orchestrator/src/runtime/create-agent-runtime.ts:213-224,1056-1069,1781-1844`、`research/design/specifications/agent-runtime-lifecycle.md`、`research/design/specifications/skill-system.md`。**价值裁决记录：**原结论为 P1/中并要求迁移为 lifecycle consumer；该迁移会扩大构造期权限，而现有时序在 prompt 消费前刷新并保持等价体验。新决定为 P2/小，仅修正文档/注释。若 prompt 可在刷新前消费或出现第二 producer，才重开功能方案与评级。 | skill 读取权威迁移后，架构声明与代码注释未同步；当前 runtime-owned 刷新不是第二 producer。 | 生产端：新窗口首 run、run 内窗口换代与 clear/resume 后首 prompt；类型组合：catalog revision、window generation、缺查询上下文；消费者：skill-index、prompt 与维护者；异常终态：过期 prompt 或重复 producer（当前不可达）；测试：首窗、run 外换代、run 内换代。受影响审查项：IR28-24、37、39、45。 | 将 lifecycle/skill 文档和代码注释统一为 assignment-bound、runtime-owned、window-bound 刷新事实，保留现有三类回归断言；不迁移生产机制、不扩大 GlobalQuery 权限。完成标准：声明与唯一生产调用图一致，现有 prompt 时序测试保持。 | 已验证 |
| U28-08 | **P1，工作量：小；同根重开，来源 P28-12。** 原 U28-08 的四域精确前缀协调与派生恢复已验证，但 FileAuthorityCommitLog.#append 在 writeFile 与 sync 成功后仍调用 handle.stat，之后才设置 committed 和发布 prepared projection；stat 失败时 WAL 已耐久，调用却抛错且本进程主投影未发布。DurableProjectionIndex.publish 在 owner 预检后仅同步替换已预备状态，#loadLastLsn 已支持 NaN tail 的下一写边界复核。证据：packages/core/src/authority/commit-log.ts:1018-1065,1070-1125；durable-projection-index.ts:602-616。**价值裁决记录：**对立复核排除了保持现状和规则降级：磁盘故障是当前受支持场景，已有 NaN tail 与下一写边界校验可复用，无需新增设施；用户体验与架构均由最小改序闭合，评级、工作量和方案保持 P1/小。仅当提交合同不再要求 sync 后结果确定，或 post-fsync 路径已被证明零可失败工作时重开裁决。 | 提交线性化点之后仍执行非必要、可失败的 metadata I/O，破坏原 U28-08 已冻结的“fsync 后不可失败发布”要求。 | 生产端：conversation/job/control 四域提交；类型组合：schedule、memory、skill、workscene 及混合 batch；消费者：owner 调用结果、四域主投影、重试与 surface；异常终态：sync 成功后 metadata/close 故障、exact replay、进程内投影与重启；测试：AuthorityCommitLog 故障注入及四域 coordinator 提交回归。当前影响是用户收到失败但事实已提交，重试与界面终态失真。受影响审查项：IR28-07、09、23、25、27、29～30、36～37、45。 | 在 frame、nextTail 和全部 projection delta 均已预备后写入；handle.sync 成功即首先标记 committed，将 #verifiedTail 直接设为预备的 NaN tail，再同步 publish 已预备 delta，删除 post-sync stat。close 失败按 committed 分支吞掉；下一次写前由现有 #loadLastLsn 的 stat+frame-boundary 校验补齐 metadata。完成标准：sync 后 former-stat/close 故障不改变成功结果；exact replay 与重启全等；四域及混合 batch 零假阴性、零半套主投影，且 sync 前故障仍零提交。 | 已验证 |
| U28-09 | **P1，工作量：中；同根重开，来源 P28-14。** 原 U28-09 已建立 job publish pending/progress、启动恢复和按 (taskId,assignmentId) 单飞，但 JobJournal 在 committed replay 与 live commit 后都只 fire-and-forget 调用一次 resumePendingPublishing 并吞掉错误，wakePendingPublishing 也只尝试一次；AnchorSchedulerRuntime.start 仅启动时全扫一次，stop 未管理 participant 的重试生命周期。证据：packages/owner-kernel/src/scheduler-job-commit.ts:121-151；job-assignment.ts:4139-4143,4433-4437；packages/cli/src/serve/anchor-scheduler-runtime.ts:207-229。**价值裁决记录：**对立复核确认等待下一任务或重启会使已保存修改无限期不生效，不能后置；现有 pending、单飞和 scheduler 生命周期可直接复用，不引入新事实源。用户终态及时收敛与架构唯一 drain owner 同时达标；评级和工作量保持 P1/中。仅当 pending 已由等价的持续 owner 自动重驱，或 job 不再支持 global staged publish 时重开裁决。 **反证 C28-15～C28-17：**只在扫描后挂 waiter 会丢失并发 wake；删除当前页 pending 后复用 continuation 会跳过后续项；把结构坏账当暂态会无限重试。现已用 wake 代际复核、稳定 key 游标和明确的 committed-materialization 错误分类闭合，并由 pageSize=1、idle wake、结构坏账 fail-stop 用例验证。 | 把耐久 pending 事实误当成持续 drain owner；已有恢复记录和单次扫描，却没有进程在线期间对可恢复派生失败负责到底的唯一生命周期。 | 生产端：user/system job commit、exact replay、启动和停机；类型组合：四域 granted 外部副作用、pending/settled progress、多个 task/assignment 与并发 wake；消费者：SchedulerJobCommitParticipant、AnchorSchedulerRuntime 和四域 materializer；异常终态：连续暂态失败、无后续任务、一个 pending 失败时其他 pending、响应丢失、停机和连续重启；测试：可控退避、单飞、公平继续和最终 settled。当前影响是已提交修改可无限期不生效。受影响审查项：IR28-10、27～28、30、36～37、43、45。 | 让 SchedulerJobCommitParticipant 成为唯一 drain owner：增加由 AnchorSchedulerRuntime.start/stop 管理的 start、wake(taskId?)、stop 生命周期；start 先追平 pending，commit/replay 只发 wake，taskId 只作调度提示，每个 pass 都从现有 pending 索引全局分页，避免单项失败饿死其他任务。单一循环在无欠账时休眠，派生 materializer 暂态失败时保留 pending、按有界退避自动再试，成功推进 settled 后停止该项；合同/坏账校验则终止 drain 并把失败交还 AnchorSchedulerRuntime 生命周期，禁止吞掉或无限重试。scheduler 停止接收和等待在途后再停止 drain，取消 timer 并等待当前 pass，不遗留后台任务。复用现有 pending index 与 #running 单飞，不新增通用 outbox。完成标准：连续失败后无需新任务即可恢复；并发 wake 零重复；单项暂态失败不饿死其它 pending；结构坏账可见且 fail-stop；stop 后零 timer/重试；响应丢失和连续重启最终 settled。 | 已验证 |
| U28-10 | **P2，工作量：小；来源 NB28-03。** server→CLI 的公开 `PublishResultNotice` 只校验顶层身份和 `decision.t`，没有递归校验 mutation、AuthorityError、workscene applied result、exact keys 与 mutation×decision 联合；也未反绑外层 control envelope 的 conversationId/runId/seq。畸形值会令 renderer 抛错或生成错误文案，合法 payload 放入错误 envelope 还会投给错误会话，而 `CoreHostConnection` 会静默隔离订阅者异常。证据：`publish-result-presenter.ts:22-44,69-110`、`core-host-connection.ts:620-640`、`session-events.ts:41-59`、`state.ts:648-657`。**价值裁决记录：**原为 P2/中，因现有 validator 可复用降为 P2/小；严格拒绝后直接 return 仍会静默丢结果，故保留既有 writer 上的可行动失败反馈。**反证 C28-05：**完整 payload 校验仍无法发现 envelope 错绑，现将三项身份全等纳入同一接收合同。**反证 C28-09：**owner decision×batch 在日志读取时已严格验证；提交后再执行可能抛错的 runtime validator不会增加权威性，反而会截断已提交结果投影。producer 只做同一 validator 的合同测试，运行时严格边界放在 consumer。**反证 C28-10：**若 presenter 在确认输入为目标普通对象前解引用 envelope，`null` 或畸形外层值会在严格校验前抛错并被订阅层静默隔离；入口必须先安全识别目标候选，无法路由的目标畸形值进入单一 fallback 桶。仅当该 wire 不再承载受支持的结果反馈时关闭。 | notice 的公共严格合同、control envelope 反绑和验证失败后的用户反馈未形成同一接收闭包。 | 生产端：owner 派生、server live/history 传输；类型组合：conflicted 与 granted-workscene、所有 global mutation、未知键/错类型/错嵌套/错 envelope 身份及非对象外层值；消费者：CLI presenter/writer；异常终态：校验前解引用、renderer throw、跨会话误呈现、静默丢失、live/history 重复；测试：producer 输出 conformance、全联合正负例、三身份错绑、畸形外层、合法渲染、去重和静默异常回归。受影响审查项：IR28-39～40、45。 | 在 core 暴露一个复用现有 mutation、AuthorityError 与 workscene result validator 的 `validatePublishResultNotice`：exact keys、标识/整数及完整 mutation×decision 联合均严格；owner 派生输出以直接 conformance 测试反绑该合同，不在已提交投影热路径新增可失败步骤。CLI presenter先安全识别目标 envelope 候选，再严格校验外层与 notice 并要求 conversationId/runId/seq 全等；失败时不采信嵌套内容，只按已验证的 envelope 身份（无可用身份时用单一 fallback 桶）经现有 writer 恰一次提示用户重进会话或查询权威状态；合法结果按稳定 identity 去重。完成标准：所有畸形或错绑值均 fail-closed 且不静默，合法两类 outcome 的 live/history 行为不变。 | 已验证 |
| U28-11 | **P2，工作量：小；来源 NB28-04。** 权威规格两处仍把公开 outcome 字段写作 `worksceneApplied`，类型和实现已统一为 `appliedResult`；`create-agent-runtime.ts:633-635` 的当前接口注释仍称装配期构造单一 MemoryStore，而生产读取已由 assignment-bound GlobalQuery + staged overlay 承载。证据：`specification.md:1472,1652`、`state.ts:648-657`、`create-agent-runtime.ts:633-639,2340-2409`。**反证 C28-06：**`work-mode.md` 顶部已明确下文旧 Store 方案被当前分布式合同取代，其历史正文不再作为现行调用图；本项不得借同步之名横扫或改写被明确标旧的设计过程。 | 当前生效的冻结合同与公开接口注释未同步到当前字段和唯一读取调用图。 | 生产端：无行为修改；类型组合：workscene granted 与非 workscene outcome、personal/workscene memory scope；消费者：后续实现者与审查者；异常终态：按旧字段或旧读面再次实现合同分叉；测试：现行规格/类型名扫描与当前 GlobalQuery 调用图。受影响审查项：IR28-39、45。 | 仅把规格两处统一为 `appliedResult`，并把 `memoryScope` 接口注释改为“静态作用域约束 assignment-bound GlobalQuery 与 staged overlay”；不改生产逻辑，不重写已明确标旧的历史章节。完成标准：当前生效规格、公开类型、实现字段与唯一读取调用图一致，现行声明中旧字段/旧读面扫描为零。 | 已验证 |
| U28-12 | **P0，工作量：中；来源 P28-13。** MemoryStore 既有 profile、people、journal 只由 AnchorMemoryGlobalStateAdapter.#loadLegacyScope 装入易失 projection，read/control 临时合并，staged planner 的 durable read-view 完全不可见；首次 staged 更新可错误冲突或从 revision 1 覆盖旧内容，同日 journal append 会覆盖旧正文。证据：packages/core/src/memory/global-state-adapter.ts:115-167,230-243,303-325,418-466；logical-entry.ts:10-42；memory-store.ts:58-118；setup-delivery.ts:803-867。**价值裁决记录：**原方案曾要求独立迁移 checkpoint，后因现有 AuthorityCommitLog request/entry 可承担逐项进度而删除；该收窄保留。**反证 C28-12：**逐项记录不能证明空集合已经接管，也不能区分“从未导入”与“权威删除后物化尚未清掉的旧文件”，更无法阻止 cutover 后新出现的文件被下次启动误导入；因此必须补一个同日志内的单次 cutover 终态事实，并从既有 mutation 记录派生不会随删除消失的 seen-key 投影，而不是 sidecar checkpoint。数据丢失事实、P0/中和当前单元归属保持；用户数据无损与单一事实源同时达标。仅当产品明确放弃兼容既有 MemoryStore 数据，或该 cutover 已由等价权威事实完成时重开裁决。 **反证 C28-14、C28-19：**setup 若使用环境默认 home 会漏接显式 authority home；旧 journal 接管后仍落入 profile/people 的 expectedDigest 守卫会拒绝同日追加。现已统一显式根路径，并让 journal 保持 authority 串行追加语义；生产 setup、部分导入、空集、同日追加和重启用例均通过。 | cutover 既未把旧 MemoryStore 事实导入唯一 memory authority，也没有耐久记录“旧读面已经永久关闭”的终态；当前 entry 投影在删除后消失，不能证明该键已被 authority 接管；易失双读无法为 staged 写提供完整前像。 | 生产端：anchor 启动、workscene legacy migration 后的 memory 接管、control/staged 写；类型组合：personal 与全部已迁移 workscene scope、profile/people/journal、空集合、update/delete 与同日追加；消费者：memory planner、read/search、runtime 和 MemoryStore materializer；异常终态：逐项导入中断、响应丢失、源集合变化、权威删除但文件清理未完成、重启及 cutover 后新场景；测试：上述矩阵与旧读面关闭。当前影响是升级用户记忆发生真实且不可逆的数据丢失。受影响审查项：IR28-07、19、25、27～30、36～37、42～43、45。 | 在 setup-delivery 完成 workscene 迁移、但尚未开放 assignment/scheduler 写之前，枚举 personal 与当前 workscene-list，按 scope/category/id 固定排序冻结旧文件集合和 sourceSetDigest。现有 memory reducer 对每个被 authority 修改过的逻辑键维护可由日志重建、删除后仍保留的 seen-key 派生投影；导入时 seen-key 恒优先，防止残留旧文件复活。其余未接管键用仅由 scope+domain+category+id 派生的稳定 import requestId 经现有 memory authority 幂等追加，mutation digest 反绑完整内容，异载荷重放 fail-closed。全部条目完成后在同一 memory authority 追加唯一 memory-legacy-cutover 终态记录，绑定 scopeSetDigest+sourceSetDigest；空集合也必须落该事实。marker 存在后永久跳过旧文件导入并删除 read/control 的 legacy 合并，所有路径只读耐久投影；新建 workscene 天然属于 cutover 后。seen-key 仅为现有日志的有界可重建投影；不得建立 sidecar checkpoint 或第二事实源。完成标准：两类 scope、三域、空集合、部分导入、同日 append、update/delete、响应丢失和重启全等；authority 删除后残留文件、cutover 后新增文件均不复活；marker 前不开放 staged 写。 | 已验证 |
| U28-13 | **P2，工作量：小；来源 NB28-05。** conversation 的 projectPublishResults 把 owner decision 中的 AuthorityError 原样复制进公开 PublishResultNotice，CLI PublishResultPresenter 与 job schedulerPublishResultDraft 又直接把 error.message 拼入公开文案；当前 producer 会产生英文及 anchor、owner 等内部拓扑术语。证据：packages/owner-kernel/src/conversation-assignment.ts:10849-10873；packages/cli/src/runtime/publish-result-presenter.ts:103-126；packages/owner-kernel/src/job-assignment.ts:10553-10620；AuthorityError 联合见 packages/core/src/contracts/foundation.ts:65-82；GlobalStagedMutation 联合见 packages/core/src/contracts/state.ts:342-355。**价值裁决记录：**对立复核确认冲突、过期、离线和能力缺口都是当前可达失败；不处理不会破坏提交正确性，但会稳定损害公开产品体验。无需通知框架，复用稳定 code+kind 即可；用户体验与架构均达标，保持 P2/小。**反证 C28-13：**只让 CLI/job renderer 忽略原始 message 仍会让 conversation 的公开 RPC 事件携带内部诊断，因此产品化必须前移到 owner 的 PublishResultNotice 投影，同时由展示端复用同一映射。仅当公开 wire 与展示面均不再消费内部 AuthorityError.message，或全部当前 code 已由等价单源产品语义覆盖时重开裁决。 **反证 C28-18、C28-20：**旧 publishConflicts 查询仍会泄漏原始诊断；仅在 CLI/job 补行动而让公开 wire 只携原因，直接 RPC/channel 仍不可行动。现已让旧查询和新 notice 共用 owner 单源映射，公开 message 同时携原因与行动，展示端不读取内部 message。 | owner 产品投影和两个公开展示执行点共同信任内部诊断 message，缺少按稳定错误 code 与 mutation kind 定义的单一产品语义边界。 | 生产端：conversation publish feedback 与 job occurrence notice；类型组合：AuthorityError 全部稳定 code 与 GlobalStagedMutation 全部 kind（含 delivery-enqueue）；消费者：CLI、RPC/channel live 与补读、scheduler notice；异常终态：公开 wire、live/history 和重放中的英文、拓扑术语或不可行动提示；测试：两 execution 的参数化 code×kind、稳定去重、wire payload 与技术词扫描。受影响审查项：IR28-37、40、45。 | 在现有 owner-kernel 产品投影层新增纯数据映射，分别以 satisfies Record<AuthorityError['code'],...> 与 satisfies Record<GlobalStagedMutation['kind'],...> 穷尽错误原因/行动和修改类别。conversation 投影先用该映射生成不含内部诊断的公开 error.message，再写 PublishResultNotice；job writer 与 CLI presenter 复用同一映射生成各自上下文文案，任何公开面都不得读取原始 message。不建通知框架。完成标准：全部 code 与全部 mutation kind 均得到中文、可行动且不暴露拓扑的 wire 与展示结果；conversation/job 语义一致，合法 live/history 去重不变，任一联合新增成员而映射未同步时类型检查失败。 | 已验证 |

| U28-14 | **P1，工作量：中；来源 P28-15。** memory legacy import、seen-key 与 cutover marker 已生效，但 `/me` 仍读取并指导用户编辑 cutover 后永不补导的 legacy profile；people/journal 管理仍读取文件投影；turn maintenance 与 system `__journal-gc` 直接 condense/delete 文件，变化不进入 GlobalQuery 权威结果。`MemoryStore.delete` 还将全部 unlink 错误吞为 false，adapter 随后仍写 materialized 终态，save/delete 也未在效果耐久后再推进 checkpoint。现有 journal `memory-append` 只有 append 语义且没有 lifecycle meta/CAS，逐条 `memory-delete` 又不能把月度摘要替换与多条日记删除原子化，原方案仅写“复用 control mutation”仍不可执行。证据：`packages/cli/src/commands/info-commands.ts:445-459`、`packages/cli/src/serve/management-directories.ts:156-176`、`packages/cli/src/serve/turn-maintenance.ts:49-132`、`packages/cli/src/serve/command.ts:601-710`、`packages/core/src/memory/contracts.ts:20-37`、`packages/core/src/memory/logical-entry.ts:10-40`、`packages/core/src/memory/memory-store.ts:58-107`、`packages/core/src/memory/global-state-adapter.ts:360-435,700-780`。**价值裁决记录：**原结论把所有 legacy 直读一概视作第二权威，并断言 `/me` 新建文件本地也不可见；反证表明只读派生消费者本身不是 writer，且 `/me` 会立即读到该文件。未被推翻的是该文件永不进入 authority、journal 文件维护不改变权威结果、派生 I/O 失败仍可被确认。新决定为改写后保留 P1/中，只闭合现有产品入口、维护 owner 与 materialization checkpoint，不新增存储或恢复框架。仅当这些入口已退出受支持产品面，或已全部改走权威读写且派生确认具备确定恢复语义时，才可降级或关闭。**反证 C28-21～C28-23：**C28-21 证明现有 append+delete 无法原子表达 condensation，须在同一 memory authority 增加 control-only 的凝练事务；C28-22 证明当前公开管理与 lifecycle 合同只覆盖 personal，workscene 只需闭合三域派生物化，禁止扩展 workscene lifecycle；C28-23 证明“调用文件 API 未抛错”不足以确认物化，只有目标内容已耐久全等或删除已耐久生效/目标本就不存在才可推进 checkpoint。 | cutover 只迁移了数据与 run 内写链，没有把公开 memory 体验、journal 生命周期 owner 和 compatibility materialization 的成功判据统一到权威边界；同时缺少一个承载既有 journal 凝练语义的 authority 原子转移，文件投影仍被当作用户操作与维护生效面，失败派生还能被永久确认。 | 生产端：`/me`、memory management RPC、turn maintenance、system `__journal-gc`、anchor memory materializer；类型组合：personal profile/people/journal 的空值、已有值、append/update/delete、condense/expire，以及 personal/现存 workscene 三域的派生 save/delete；消费者：CLI、RPC、agent GlobalQuery 与 compatibility 文件视图；异常终态：cutover 后编辑、投影滞后、并发维护、LLM/写删 I/O 失败、效果后响应丢失、连续重启；测试：上述生产入口、personal 三域视图与 lifecycle、两类 scope 三域物化、故障注入和结构可达性。当前损失是身份设置只在本地命令可见、管理视图可与 agent 所见不一致、凝练/过期不降低权威检索噪音、失败派生永久失去重驱。workscene 不新增公开管理或 lifecycle，只验证既有派生恢复。受影响审查项：IR28-02、19、24～30、32、36～39、43、45。 | 复用现有 management RPC、`AuthorityRuntimeStack.globalState`、GlobalQuery、memory authority 与 materialization pending/checkpoint：①抽取现有 journal 衰减规则为消费 path-free DTO 的纯 planner，为 management 增加 profile 权威读面；`/me`、people、journal 只消费 personal DTO，空 profile 提示用户经现有对话 memory 工具设置，不再指导编辑文件。②把 turn maintenance 与 `__journal-gc` 收敛为同一个 anchor-only、触发源无关且 single-flight 的 personal journal service；它从 `memory-list` 生成计划，过期用带当前 digest 的 `memory-delete`，凝练在 LLM 成功后提交 control-only `memory-journal-condense`，以 month、可选目标 digest、全部 source id+digest 和 summary 为一个原子 memory reducer 事务：全量 CAS 后替换月摘要并删除来源，任一漂移零写入；requestId 绑定完整输入，响应丢失按原结果回放。该权威记录携完整 target-upsert/source-delete delta 并生成一个可重驱 pending，不得把此 mutation 放入 staged 联合。③materializer 对该 delta 幂等追平全部文件；save 采用同目录临时文件耐久写后原子替换，delete 仅把 ENOENT 视为幂等成功，其他错误抛出；全部目标效果全等后才写 materialized，任一步或 checkpoint 失败均保留/重放同一 pending。补空/已有 profile、people/journal 视图、append/update/delete、condense/expire、并发 CAS、LLM/I/O 故障、部分派生、效果后响应丢失、连续重启、personal/workscene 物化与旧生产读写面零可达测试。完成标准：公开视图与权威 GlobalQuery 全等；personal lifecycle 无半凝练、无误删且最终收敛；派生失败始终保留 pending，成功重放全等；非 anchor 与旧 JournalStore/loadProfile/PeopleStore 生产写读面零可达；不新增第二事实源、通用事务、恢复或通知框架。 | 已验证 |
| U28-15 | **P0，工作量：中；来源 P28-16。** 公共 `MemoryAppendPayload` 同时允许 `memory/person`、`memory/journal` 与专用 `people`、`journal`，宽联合 `memory-delete`、`MemoryLogicalEntry` 和 GlobalQuery 结果也允许同一组错域身份；不同权威键可物化到同一文件。memory 工具读取 person 走 people，写删固定走 memory，search 只查 memory；MemoryFlush 只校验非空 id，codec 对 journal date 只验 string，MemoryStore 直接以 id 拼路径。证据：`packages/core/src/memory/contracts.ts`、`packages/core/src/contracts/state.ts:338-359`、`packages/orchestrator/src/runtime/create-agent-runtime.ts:791-839,1283-1338,2344-2405`、`packages/core/src/memory/flush-engine.ts:232-251`、`packages/core/src/protocol/commit.ts:682-745`、`packages/core/src/protocol/global-query-validation.ts:332-371`、`packages/core/src/memory/global-state-adapter.ts:1248-1337`、`packages/core/src/memory/memory-store.ts:196-218`。**价值裁决记录：**原结论为 P0/中。对立复核排除了只在 MemoryStore 加 containment、仅修 runtime 映射或修改规则：前两者分别保留分裂权威键或可穿越输入，后者与 D28-04 的严格 id/category、路径收口及当前真实用户失败冲突；规格自身的重叠联合也会把两套权威事实投影到同一文件，因此根因改写为“产品类别没有唯一 canonical authority identity，且身份规则未贯穿全部边界”。新决定保持 P0/中，方案收敛为“唯一产品映射 + 末端 containment”，不建设通用 codec/存储框架；该方案同时恢复用户可见一致性并消除结构债务。仅当 canonical 映射、全边界校验和末端 containment 已全部成立，或产品正式移除对应 memory 能力时重开裁决。**反证 C28-24～C28-26：**只收紧 append 仍会让 delete、wire 返回和 overlay 以错域键操作同一产品对象；profile 与 people 分别查询后直接拼接不能保持同一全局排序/limit；只检查“解析后位于根内”仍会放过多级相对路径和跨平台保留名。三者均并入原根因，评级与工作量不变。**新增反证 C28-30～C28-31：**工具入口仍可绕过 canonical producer 边界，以及 workscene 查询曾按旧 category 分组并在跨域 limit 前各自截断；两项均已并入 canonical identity 根因修复，并在当前冻结指纹上复核通过。 | 产品类别没有唯一 canonical authority identity，同一身份规则未贯穿 producer、codec、planner、query/result 与最终 I/O 边界。 | 生产端：MemoryFlush、conversation/job runtime memory 工具和远端 executor；类型组合：profile/person/journal 的合法与全部跨域组合、append/delete、daily/monthly journal、合法/非法日历及路径型 id；消费者：`/me`、people/journal 管理、GlobalQuery CRUD/search、journal lifecycle、overlay 与 MemoryStore；异常终态：提交后越出 memory 根覆盖文件、非法日期阻断维护、person/profile 写入后不可见或被另一权威键覆盖、local/mesh 接受错域结果；测试：路径穿越与平台保留名、非法日历/组合、固定 profile、person 全 CRUD/search、排序/limit、local/mesh、LLM 漂移及内部月摘要。受影响审查项：IR28-02、19、27～30、35、37、39～40、42～43、45。 | 在 core 把写、删和 `MemoryLogicalEntry` 冻结为同一判别联合并提供一个小型 canonicalizer：公开 profile 只能是 `memory/profile/profile`；person 只能是 `people/<safe-id>`，`safe-id` 为 1～64 位小写 ASCII slug（`[a-z0-9]+(?:-[a-z0-9]+)*`）且排除平台保留 basename；公开日记只能是 `journal/<真实 YYYY-MM-DD>`，date 省略时由 owner 时间在规划阶段一次派生，`memory/person`、`memory/journal`、跨域 category/id 及额外字段一律拒绝；`journal/<YYYY-MM>` 只由 legacy 接管与 host-only 凝练内部意图产生。MemoryFlush、memory 工具、append/delete codec、planner、overlay、GlobalQuery query/result validator 全部复用该规则，非法 LLM 提取零 staged 写；memory search 对 profile 与 people 分别经现有 GlobalQuery 有界取候选，再按共享 comparator 合并去重并统一 slice(limit)。MemoryStore 作为末端防线按 storage category 再验固定 profile、safe-id 或 day/month，并要求解析结果是对应类别目录的直接子文件。完成标准：全部生产入口和 local/mesh 返回使用同一身份，非法组合、日期、绝对/相对/分隔符路径及保留名在提交或 I/O 前 fail-closed 且零副作用；合法 profile/person/daily journal CRUD/search、内部 monthly lifecycle、重放和派生全等。 | 已验证 |
| U28-16 | **P1，工作量：中；来源 P28-17。** turn maintenance 在首轮落定后静默 fire-and-forget，并以调用方局部 `done` 状态阻止同进程后续计划再进入 owner；每个待凝练月份会调用一次 light LLM，失败和结果均被调用链吞掉。现有 `SchedulerUserNoticeJournal` 有耐久 live/history 基座，但没有 journal-maintenance 分支、单调状态方法或 CLI 消费者；system `__journal-gc` 只有内部任务摘要，`/journal` 只显示当前计划。证据：`research/design/specifications/memory-system.md:317-321,357-378,532-538`、`packages/cli/src/serve/turn-maintenance.ts:51-79`、`packages/cli/src/serve/journal-maintenance.ts`、`packages/core/src/contracts/protocol.ts:506-535`、`packages/owner-kernel/src/scheduler-user-notices.ts`、`packages/cli/src/serve/access-surfaces.ts:157-163`、`packages/server/src/system-handlers.ts`。**价值裁决记录：**原结论为 P1/中，但方案只写“接入 surface”且 owner 不清。对立复核尝试删除透明要求、仅依赖 usage 或被动 `/journal`：均不能解释当前受支持的自动多月付费调用为何可以在用户不知情时发生，也不能提供失败终态；停用自动凝练又会破坏既有 journal 衰减体验。新决定保持 P1/中，将方案改写为复用现有全局用户通知权威的单一闭环；它不打断主对话、不要求确认，同时满足最优体验与最简架构。仅当自动付费凝练被正式移除，或已有等价的调用前提示和可补读结果闭环时重开裁决。**反证 C28-27～C28-29：**直接调用 `prepareRecords` 只追加事件，不能证明同 notice 的合法进度、响应不明重放或重启接管；只把事实写入 scheduler 流而没有 CLI presenter 与 `/journal` 最新状态仍不是用户透明；turn 侧 `done` 又形成第二生命周期状态。三者均并入原根因，评级与工作量不变。**新增反证 C28-32：**多月计划在 authority 效果成功但响应丢失时曾关闭旧 notice 并生成新 notice；现已改为在同 notice 内以 applied prefix 追平剩余来源，并在当前冻结指纹上复核通过。 | 触发源无关的全局 journal 维护已有唯一执行 owner，却没有与付费调用生命周期绑定、可恢复且被产品面实际消费的耐久 notice 事实。 | 生产端：turn maintenance 与 system `__journal-gc` 的同一 journal owner；类型组合：零计划、单月/多月计划、首次及重复付费调用、成功、失败、重试和计划漂移；消费者：现有 scheduler notice 权威、已连接第一方 CLI/RPC live、`server.info` history 与 `/journal`；异常终态：notice 写入或响应丢失、LLM/authority 失败、调用后崩溃、连续重试与重启、双触发竞争及停机；测试：调用前提示、逐月进度、成功/失败终态、稳定身份/去重、零计划零通知和唯一 owner。当前损失是用户可在无提示时产生一次或多次模型费用，且无法知道成功、失败还是等待重试。受影响审查项：IR28-37、39、45。 | 在现有 `SchedulerUserNoticeJournal` 增加窄化的 `journal-maintenance` 分支和一个幂等单调写方法，不建第二日志：owner 首次扫描后冻结 path-free 计划（month、目标前像、各 source id+digest），以完整计划摘要派生 noticeId；同一 notice 耐久记录 prepared/open、当前月份/attempt、完成数、失败与 closed，重复同一步全等回放，倒退或异载荷拒绝。任何 light LLM 调用前必须先落并确认对应 attempt 的 open/updated；响应不明先按 noticeId 对账，未证明耐久不得调用。把 journal service 收敛为组合根管理的 start/wake/stop owner：组合根在 `SchedulerUserNoticeJournal` 初始化 live cursor 后绑定并启动它，停机时先拒绝新 wake、等待在途，再由 scheduler runtime 释放 notice 订阅；start 先恢复未关闭计划，source 全等则以可见的新 attempt 重试，已被原子凝练则补 closed，发生 CAS 漂移则关闭旧计划并由新扫描产生新身份；失败保留原日志并写可行动 updated。turn 与 system 只 wake 该 single-flight owner，删除调用方 `done` 状态。复用现有 `scheduler.notice`/`server.info`，补一个按 noticeId+revision 更新的 CLI presenter，并让 `/journal` 展示同一最新状态；无明确渠道目标时不得跨渠道广播。完成标准：零计划零 notice；每次付费调用前已有可补读提示；多月进度、成功、失败、重试、停机和连续重启均从同一耐久计划收敛，live/history/`/journal` 文案一致且不重复；不新增通知框架、第二事实源或确认步骤。 | 已验证 |

## 已排除问题

> 保存已证伪疑点供复审复用;使用与重开规则见工作台静态区。裁决只允许“已排除”或“已重开→问题编号”,重开时保留原行。

| 编号 | 原疑点与已验证事实 | 排除依据与适用边界 | 证据与输入基线 | 重开条件 | 最终裁决 |
| ---- | ------------------ | ------------------ | -------------- | -------- | -------- |
| X28-01 | 原 P28-02 主张 global stager/query 的可选 capability、epoch 与 ordinal fallback 会破坏生产重放；核实 conversation/job 生产装配均经 `assignmentGlobalCapability` 取得唯一 capability，conversation ownerEpoch 来自同一 anchor authority，job 使用 envelope anchorEpoch，生产工具调用均携耐久 toolCallId。 | 可选参数和 ordinal fallback 在当前受支持生产调用图不可达；不存在当前用户损失，不为接口纯度扩建修复。本裁决只覆盖当前单一 capability/fence 与耐久 toolCallId 前提。 | `assignment-schedule-stager.ts:28-94,122-151`；`conversation-assignment-worker.ts:328-360`；conversation/job authority 装配；交付指纹 `06da446b...`；IR28-04。 | 生产出现独立 owner/anchor 代际、无 capability 的 global 写，或 staged schedule 可在没有耐久 toolCallId 时执行。 | 已排除 |
| X28-02 | 原 P28-07 主张 run context 缺失时的 segment repository fallback 是 assignment 第二写面；核实该分支只服务 conversation owner 取得 exclusive control 后的 run 外 `/compact`，D28-03 明确保留 `/clear`、`/compact` 等 run 外管理经 owner/control 执行。 | 当前 fallback 不从 active assignment 到达，也不会在 failed/cancelled assignment 后写入；它是明确保留的 control 面，不是 staged 旁路。本裁决只覆盖 owner + exclusive control 的 run 外调用。 | `segment-deps.ts:39-64`；`conversation-manager.ts:1775-1864`；D28-03；交付指纹 `06da446b...`；IR28-31、IR28-38。 | 该分支可从 active assignment、非 owner 主体或未取得 exclusive control 的路径到达。 | 已排除 |

## 迟发现教训

> 仅登记“先前通过后才发现”的真实遗漏。检测动作必须可执行并写明适用范围;每个适用轮次追加执行证据。

| 编号 | 对应问题与先前通过轮次 | 遗漏机制 | 后续必做的检测动作与适用范围 | 应用记录（轮次:证据） |
| ---- | ---------------------- | -------- | ---------------------------- | --------------------- |

## 验证计划与证据账本

> 状态只允许“待执行、有效、失效、诊断”。“待执行”必须写明当前证据缺口,输入闭包必须具体到可重复计算指纹;执行按各行输入闭包计算独立指纹。

| 编号 | 证明目标与当前缺口 | 最小命令或检查 | 输入闭包 | 阶段 / 成本 / 实耗 | 结果 | 证据输入指纹 | 状态 |
| ---- | ------------------ | -------------- | -------- | ------------------ | ---- | ------------ | ---- |
| M28-01 | publish decision 结构与 MutationBatch 联合合同；conversation/job 生产、重放及错绑均须共用单一谓词 | core 合同定向测试 + owner-kernel conversation/job 定向测试 | U28-01 生产文件、公共导出与直接测试 | 集中修复 / 小 | core 联合合同 6/6、delivery participant 9/9 通过；owner-kernel 类型检查通过 | git-delivery-manifest-v1:229b5eca013b629eb1040e22391a27bea4e899e60992a465bd38d097565bd8d6 | 有效 |
| M28-02 | owner decision 到 conversation control 与 job 维护通知的 live/补读/重放恰一次闭环 | owner-kernel、executor job producer 与 CLI/server 直接消费测试 | U28-02 conversation/job 生产、投影、产品消费与直接测试 | 集中修复 / 中 | owner-kernel 类型检查通过；scheduler notice 4/4、conversation durable conflict/live-history 1/1、真实 job commit producer 1/1、CLI presenter 2/2、server subscribe history 1/1 通过；C28-02 补证通过 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| M28-04 | 三域 MemoryFlush 的 authoritative+overlay CAS 前像与同 run 连续更新 | core memory adapter + orchestrator runtime 定向测试 | U28-04 memory identity/digest、flush 与 overlay | 集中修复 / 中 | core memory 5/5、orchestrator 同 run overlay 1/1 通过；两包类型检查通过 | git-delivery-manifest-v1:229b5eca013b629eb1040e22391a27bea4e899e60992a465bd38d097565bd8d6 | 有效 |
| M28-06 | memory-search 的 domain/query/rank/limit 与删除补位保持 authoritative 等价 | orchestrator memory-search 定向测试 | U28-06 overlay merge 与 U28-04 共享投影 | 集中修复 / 小 | orchestrator 跨域、删除补位与 top-N 定向用例 1/1 通过 | git-delivery-manifest-v1:229b5eca013b629eb1040e22391a27bea4e899e60992a465bd38d097565bd8d6 | 有效 |
| M28-07 | skill-index 声明与 assignment-bound runtime-owned 刷新调用图一致 | 文档差异核对 + 既有窗口刷新定向测试 | U28-07 lifecycle/skill 文档、注释与直接测试 | 集中修复 / 小 | 旧声明扫描无第二 producer；窗口边界刷新定向用例 1/1 通过 | git-delivery-manifest-v1:229b5eca013b629eb1040e22391a27bea4e899e60992a465bd38d097565bd8d6 | 有效 |
| M28-05 | 全部 GlobalQuery/GlobalReadResult 变体在 local/mesh 双端递归严格且反绑原 query | core validator + CLI local/mesh 定向测试 | U28-05 联合类型、local/mesh guard 与直接测试 | 集中修复 / 中 | core 全联合/错绑/accessor 3/3、CLI local/mesh 2/2 通过；core 构建通过；CLI 全量类型检查仅保留 8 个既有 config-editor/startup 错误，改动文件零新错误；C28-01 补证通过 | git-delivery-manifest-v1:50d2d2027dd418877c42b539fe5756af4aba73e1d53dc5705cb2421d83a64977 | 有效 |
| M28-03 | provider/abort/timeout/响应不明/重启下 descendant child 最深优先终结且 usage 不漏不重 | executor governor + orchestrator child 定向测试 | U28-03 governor/finalizer/factory 与直接测试 | 集中修复 / 中 | executor governor 22/22、orchestrator child boundary 2/2 通过；两包类型检查通过 | git-delivery-manifest-v1:229b5eca013b629eb1040e22391a27bea4e899e60992a465bd38d097565bd8d6 | 有效 |
| F28-01 | U28-01 decision×batch 完整事实链 | 冻结源码反向追踪 producer、公共谓词与两 execution replay | core validator；conversation/job producer/reducer；delivery participant | 专项功能审查 / 小 | 合法 decision 可提交/重放；错域、错键、错 revision 与 applied 缺件均在投影前拒绝 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-02 | U28-02 owner decision 到 conversation/job 可行动反馈 | 冻结源码追踪 live、history、重放与稳定身份 | final-outbox、publishResults/finalHistory、scheduler notice、server/CLI 消费 | 专项功能审查 / 小 | feedback 只由权威 decision 派生；部分冲突与 workscene applied 在 live/补读同形且按稳定身份去重；C28-02 真实 job commit producer 补证通过 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-03 | U28-04/U28-06 memory 三域连续写与同 run 精确查询 | 冻结源码追踪 shared projection、CAS 前像、overlay merge 与 top-N | logical-entry、memory adapter、runtime flush/search | 专项功能审查 / 小 | domain 进入 identity/digest；首次、跨 run、同 run 与并发 CAS 收敛；跨域/不匹配/delete 补位均保持 authoritative 等价 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-04 | U28-07 skill 声明与唯一生产调用图 | 冻结文档/源码双向扫描并复用窗口定向证据 | lifecycle/skill 文档、runtime 窗口刷新调用点与测试 | 专项功能审查 / 小 | assignment-bound runtime 是唯一 producer；首窗、run 外/内换代均在 prompt 前刷新，无构造期权限扩张 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-05 | U28-05 local/mesh GlobalQuery 请求与结果严格闭包 | 冻结源码逐变体核对 exact-keys、递归对象、query binding 与三处边界 | core validator、local port、mesh server/client 与直接测试 | 专项功能审查 / 小 | C28-01 修复后 local/mesh 共用单一谓词；accessor、错绑、坏摘要与 path 字段全部 fail-closed | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-06 | U28-03 descendant child 子树终结 | 冻结源码追踪 normal settle、assignment flush、usage/report 与重启幂等 | governor/finalizer/factory 及 22+2 项直接证据 | 专项功能审查 / 小 | open usage 先保守 consume，后代最深优先 settle/release；响应不明与连续恢复不复活、不重计，父终态前零 active descendant | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| F28-07 | 七项直接交界与生产装配 | 按固定矩阵双向核对 owner→surface、authority→overlay→query、mesh fail-closed→child finally | 七项生产入口、消费者、异常终态和直接测试 | 专项功能审查 / 小 | 三组直接交界无第二事实源、无旁路写、无漏反馈或资源泄漏 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| A28-01 | 冷启动角色：publish 严格合同与用户反馈 | 抛开既有结论重造错域/错键/部分冲突/响应丢失/重放 | U28-01↔U28-02 冻结调用图 | 对抗复审 / 小 | 公共联合谓词和 owner-derived feedback 闭合，C28-02 补证后重新复审，未发现新反证 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| A28-02 | 冷启动角色：memory/skill lifecycle 与同 run 语义 | 重造三域连续写、并发 CAS、跨域污染、delete 补位与窗口换代 | U28-04↔U28-06↔U28-07 冻结调用图 | 对抗复审 / 小 | shared projection、overlay 过滤/排序和唯一窗口 producer 闭合，未发现新反证 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| A28-03 | 冷启动角色：GlobalQuery wire 安全与双拓扑 | 重造 accessor、隐藏/额外字段、错 kind/id/scope、坏 digest、路径泄漏与串线响应 | U28-05 local/mesh 冻结边界 | 对抗复审 / 小 | 首轮发现 C28-01；修复后重新冷启动复审，local/mesh 三处边界全部 fail-closed | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| A28-04 | 冷启动角色：ResourceGovernor 子树终结与崩溃恢复 | 重造 open usage、嵌套/并发 child、终结半提交/响应不明、重启与查询失败 | U28-03↔U28-05 frozen governor/factory 边界 | 对抗复审 / 小 | 现有日志、稳定身份和最深优先 finalizer 足以收敛，零第二 owner 日志，未发现新反证 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| D28-01 | 历轮反证差异审计 | 机械对账七项原失败链、C28-01～C28-02、专项矩阵与四路记录 | U28-01～U28-07 当前记录及 F28/A28 证据 | 专项收口 / 小 | 原七项均为修复后复核通过；C28-01 已同根合并并修复后复核通过；C28-02 已补齐真实 job producer 证据；无发现消失、无未处置反证 | git-delivery-manifest-v1:ba9cbb456b3a0ebe5ed74f73ad7a6586e6a28961be7fea4aa60a9f265c48feeb | 有效 |
| M28-08 | 三类 writer 的四域规划绑定同一日志锁内精确前缀，准备失败零追加，fsync 后主投影零半套 | core durable projection/memory/skill/workscene 定向集 + owner coordinator 定向集 + 类型与构建 | AuthorityCommitLog、四域 reducer/adapter、coordinator、conversation/job writer 与生产装配 | 集中修复 / 中 | core 相关六文件同轮 43 项通过，资源型 5 秒超时项隔离 1/1 通过；owner coordinator 6/6；core、owner-kernel、orchestrator 类型通过；core、owner-kernel 与 CLI 构建通过 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| M28-09 | job decision/batch 仅重驱 granted 派生副作用，pending 按 task 有界且同身份并发单飞 | owner-kernel scheduler mutation commit 定向集 | job commit producer、pending reducer、progress、coordinator driver、启动与 exact replay | 集中修复 / 小 | 6/6 通过；覆盖 granted+conflicted、重启零重复及同 pending 两个并发恢复只调用一次；owner-kernel 类型与构建通过 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| M28-10 | PublishResultNotice 全联合、外层 control envelope 反绑及畸形值可行动恰一次反馈 | core 合同定向集 + CLI presenter 定向集 | 公共 validator/export、owner 派生、server envelope、CLI presenter/writer | 集中修复 / 小 | core 合同子集 11/11、CLI presenter 4/4 通过；畸形 payload、错绑身份、畸形外层和 live/history 去重均覆盖；CLI 构建通过，CLI 类型检查仅保留 8 个既有配置编辑器/startup 错误且改动文件零新错 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| M28-11 | 当前生效字段与 memory 读取调用图声明一致且不改写历史旧方案 | 定向文本扫描 + orchestrator 类型检查 | 当前 specification 两处、公开类型、create-agent-runtime 当前接口注释 | 集中修复 / 小 | `worksceneApplied` 与旧构造期 MemoryStore 声明扫描为零；orchestrator 类型通过 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| F28-08 | U28-08 精确前缀事务与派生恢复固定矩阵 | 冻结源码逐格追踪 read-view、纯规划、全投影 prepare、WAL fsync、同步 publish、pending/checkpoint | 三类 writer、四域 planner/main projection/derived materializer 与启动组合根 | 专项功能审查 / 小 | 旧 cache/错 revision/混合冲突只有日志锁内精确前缀可决定；任一 prepare 失败零 WAL；fsync 后同步发布不可失败，崩溃由日志尾与域 pending 有界重建；control/staged 共用同一栅栏且无第二事实源 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| F28-09 | U28-09 job 欠账、progress 与恢复固定矩阵 | 冻结源码逐格追踪 commit+decision、pending 派生、artifact 反绑、逐 seq driver 与 settled 移除 | user/system job、四域 derived driver、启动/重放/并发恢复 | 专项功能审查 / 小 | conflicted/delivery-enqueue 零 apply，granted 外部副作用按 seq 幂等；调用后崩溃或 progress 丢失可重驱，同 pending 身份单飞；启动只扫描 pending 投影而非每 task 全历史 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| F28-10 | U28-10 公开通知严格接收固定矩阵 | 冻结源码逐格核对 owner 派生、公共联合 validator、外层 envelope、filter、writer 与去重 | conflict/workscene grant、全部 global mutation、live/history、畸形对象与错身份 | 专项功能审查 / 小 | 合法结果同形呈现且稳定去重；畸形 payload/envelope、错绑身份和非对象输入均不进入 renderer，目标畸形值经单一 fallback 恰一次给出可行动提示；C28-10 修复后复核通过 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| F28-11 | U28-11 当前合同同步固定矩阵 | 当前规格、类型、接口注释和读取调用图双向扫描 | `appliedResult` 与 assignment-bound GlobalQuery/staged overlay | 专项功能审查 / 小 | 当前生效声明全等，明确标旧的历史设计未被横扫或重写 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| F28-12 | U28-08↔U28-09、U28-10↔U28-11 直接交界 | 冻结源码双向追踪主投影→derived progress、owner notice→wire contract | 四项全部生产入口、事实源、消费者与异常终态 | 专项功能审查 / 小 | owner commit 不等待可失败派生但欠账可恢复；progress 不重复主投影；通知只消费当前字段与权威结果，无运行时二次校验截断已提交投影 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| A28-05 | 冷启动角色：四域唯一线性化与不可失败主投影 | 重造旧 cache、两 writer 同 revision、混合冲突、prepare 失败、fsync 后崩溃 | U28-08 冻结事务与四域 reducer | 对抗复审 / 小 | 同锁 read-view 与全 delta 预备给出唯一后继；日志内无业务分支、零第二事务框架；C28-03/C28-07 均修复后复核通过，未发现新增反证 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| A28-06 | 冷启动角色：域派生物化与 job 欠账有界恢复 | 重造派生失败、progress 响应丢失、executor 离线、并发唤醒与连续重启 | U28-08↔U28-09 域 pending/job pending/progress | 对抗复审 / 小 | 首轮发现 C28-11；修复后同 pending 单飞、granted-only 与 bounded index 全部闭合，连续恢复最终 settled 且零历史乘法扫描 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| A28-07 | 冷启动角色：wire 严格性与可行动用户反馈 | 重造额外/错形字段、错 envelope、非对象外层、live/history 重复与 filter 边界 | U28-10 公开接收链 | 对抗复审 / 小 | 首轮发现 C28-10；修复后目标候选先安全分类再严格验证，畸形值 fail-closed 且不静默，合法反馈不重复 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| A28-08 | 冷启动角色：合同同步与范围价值 | 从当前生效合同反查字段、类型、注释与实现，并检查是否扩建范围外设施 | U28-11 与四项新增架构/实现 | 对抗复审 / 小 | 只同步当前字段和调用图；没有通用事务/outbox、通知框架、监控、诊断、benchmark 或信息采集扩面，用户体验与锁定范围内架构均未削弱 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| D28-02 | U28-08～U28-11 历轮反证差异审计 | 机械对账原失败链、C28-03～C28-11、F28-08～F28-12 与 A28-05～A28-08 | 四项当前记录、固定矩阵、直接测试及当前冻结指纹 | 专项收口 / 小 | C28-03～C28-09 均同根合并并修复后复核通过；C28-10/C28-11 首次登记后已修复并重新完成受影响测试、专项与四路复审；零发现消失、零未处置反证 | git-delivery-manifest-v1:58d3eb99a5afd2144d00ea6f1e309986d14d8708757561b2f7a8803e8d1897f5 | 有效 |
| M28-12 | U28-08 的 sync 后零假阴性、零半套主投影与 exact replay | core commit-log post-sync 故障注入测试 | AuthorityCommitLog、DurableProjectionIndex 与直接测试 | 集中修复 / 小 | post-sync stat/close 两类故障 2/2 通过；sync 成功后结果保持 committed，进程内投影与重开日志全等 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| M28-13 | U28-12 的 personal/全部现存 workscene 三域旧 memory 无损接管 | core memory adapter + CLI setup-delivery 定向测试 | legacy MemoryStore、memory authority/cutover reducer、生产 setup 与直接测试 | 集中修复 / 中 | core memory 9/9、CLI setup-delivery 22/22 通过；覆盖空集、部分导入、响应丢失、删除不复活、同日 journal 追加、显式 authority home 与 unreadable source fail-closed | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| M28-14 | U28-09 的全局 pending 公平持续重驱与 start/wake/stop 生命周期 | owner-kernel scheduler commit 定向测试 | SchedulerJobCommitParticipant、JobJournal、AnchorSchedulerRuntime 与直接测试 | 集中修复 / 中 | scheduler commit 10/10 通过；覆盖 idle wake、pageSize=1 公平扫描、暂态重试、并发单飞、结构坏账 fail-stop 与 stop 后零 timer | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| M28-15 | U28-13 的穷尽产品语义与 conversation/job/CLI 公开反馈一致性 | owner-kernel 映射测试 + CLI presenter 测试 | AuthorityError/GlobalStagedMutation 联合、owner 投影与 CLI 展示 | 集中修复 / 小 | owner 映射 1/1、CLI presenter 4/4 通过；公开 wire 同时携原因与行动，旧查询与新 notice 均不读取内部诊断 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| M28-16 | 四项最终编译、格式与交付闭包 | workspace build + 受影响文件 Biome + diff check + manifest 生成器 | 当前四项完整交付物 142 路径 | 集中修复 / 中 | workspace build 17/17 包通过；26 个受影响 TypeScript 文件 Biome 通过；工作区与暂存区 diff check 通过；路径集与分组可重复计算 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| F28-13 | U28-08 post-fsync 线性化固定矩阵 | 冻结源码追踪 prepare、write、sync、committed、NaN tail、publish 与 close | commit-log、四域主投影及故障注入测试 | 专项功能审查 / 小 | sync 前故障零提交；sync 后无 metadata I/O，先 committed 再同步发布；close 故障不改变成功，重启以现有边界校验恢复 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| F28-14 | U28-12 legacy memory 接管固定矩阵 | 冻结源码追踪启动顺序、稳定导入、seen-key、cutover marker 与唯一读面 | setup-delivery、MemoryStore、memory adapter/reducer 与九项直接测试 | 专项功能审查 / 小 | workscene 迁移后且业务写前接管；空集也落 marker；部分导入幂等续跑，删除后残留/新增旧文件不复活，journal 保持串行追加 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| F28-15 | U28-09 job pending 持续 drain 固定矩阵 | 冻结源码追踪 start/wake/stop、wake 代际、稳定 key 游标、退避与错误分类 | participant、job producer、scheduler runtime 与十项直接测试 | 专项功能审查 / 小 | commit/replay 只唤醒唯一 drain；并发 wake 不丢，删除当前项不跳过后项；暂态失败自动收敛，结构坏账可见并停止，停机零后台遗留 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| F28-16 | U28-13 公开产品语言固定矩阵 | 冻结源码逐成员核对穷尽映射、owner 净化、job/CLI 复用及 live/history | product-language、conversation/job producer、CLI presenter 与直接测试 | 专项功能审查 / 小 | 全部稳定 code/kind 有单源中文原因、行动与类别；公开 wire、旧查询及展示不泄漏内部 message，合法 live/history 去重不变 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| F28-17 | U28-08↔U28-12、U28-09↔U28-13 直接交界 | 冻结源码双向追踪日志 cutover、pending 生命周期与公开失败反馈 | 四项生产入口、事实源、消费者与异常终态 | 专项功能审查 / 小 | memory 接管与正常四域提交共用 authority 事实且 marker 前零业务写；job drain 的结构/暂态终态进入统一产品语义，无第二事实源或通知框架 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| A28-09 | 冷启动角色：post-fsync 线性化与不可失败发布 | 重造 sync/close 故障、exact replay、重启与四域混合提交 | U28-08 冻结调用图 | 对抗复审 / 小 | committed 线性化紧随 sync，之后仅同步不可失败发布；原 stat 假阴性路径消失，未发现同根残留 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| A28-10 | 冷启动角色：legacy memory 接管与数据无损 | 重造空集、部分导入、响应丢失、删除残留、同日追加、显式根路径和连续重启 | U28-12 冻结调用图 | 对抗复审 / 小 | 稳定 request、seen-key 与单次 marker 共同区分未导入/已删除/cutover 完成；生产顺序与 journal 语义闭合，未发现同根残留 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| A28-11 | 冷启动角色：job pending 持续重驱与生命周期 | 重造 lost wake、连续暂态失败、公平分页、结构坏账、并发唤醒与停机 | U28-09 冻结调用图 | 对抗复审 / 小 | 唯一生命周期 owner、wake 代际和稳定游标闭合全部反例；失败分类有确定终态，未发现同根残留 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| A28-12 | 冷启动角色：公开 wire/展示语言与可行动反馈 | 重造全部联合成员、旧查询、新 notice、live/history 和内部术语泄漏 | U28-13 冻结调用图 | 对抗复审 / 小 | owner 边界先净化并在 wire 中携原因与行动，job/CLI 复用同一映射；目标公开链无原始诊断读取，未发现同根残留 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |
| D28-03 | U28-08、U28-09、U28-12、U28-13 反证差异审计 | 对账原失败链、C28-12～C28-20、专项矩阵与四路记录 | 四项当前记录、直接测试及当前冻结指纹 | 专项收口 / 小 | C28-12～C28-13 已同根合并；C28-14～C28-20 均耐久登记并修复后复核通过；历轮发现零消失、零未处置反证 | git-delivery-manifest-v1:dde626590f6170838f00d8c96d0b0d509da1f39d83a4109af6511006276618c6 | 有效 |

| M28-17 | U28-14 的公开 personal memory 视图与 anchor-only lifecycle 权威边界 | core/CLI/server 定向测试与类型检查 | GlobalStatePort、management RPC、`/me`/people/journal、journal planner/service 与生产装配 | 集中修复 / 中 | core 最终定向集 44/44、CLI 最终定向集 24/24、server 定向与 golden 9/9 通过；core/server 类型检查通过；CLI 类型检查仅保留 8 个既有 config-editor/startup 错误且 U28-14 改动文件零新增错误 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| M28-18 | U28-14 的原子 condense 与两类 scope 三域派生物化恢复 | core memory adapter/MemoryStore 故障注入测试 | memory reducer、pending/checkpoint、durable save 与 strict delete | 集中修复 / 中 | 覆盖 target+sources 全量 CAS、并发漂移零写入、部分 save/delete 后 pending 保留、效果后响应丢失与连续重启；personal/workscene 的 profile、people、journal 均最终全等，delete 仅 ENOENT 幂等 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| M28-19 | U28-14 最终编译、格式与交付闭包 | workspace build + 受影响文件 Biome + diff check + manifest 生成器 | 当前 U28-14 完整交付物 158 路径 | 集中修复 / 中 | workspace build 17/17 包通过；27 个受影响文件 Biome 通过；`git diff --check HEAD` 通过；路径集与分组可重复计算 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| F28-18 | U28-14 公开 profile/people/journal 固定矩阵 | 冻结源码追踪 CLI→RPC→MemoryDirectory→personal GlobalQuery | `/me`、people、journal、空/已有 profile 与投影滞后 | 专项功能审查 / 小 | 三个公开入口只消费 authority DTO；空 profile 引导经现有对话 memory 工具设置；cutover 后文件编辑与投影滞后均不能改变公开结果 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| F28-19 | U28-14 journal lifecycle 原子性固定矩阵 | 冻结源码追踪 planner、stable request、全量 CAS 与单 reducer 事务 | append/update/delete、condense/expire、并发维护、LLM 失败与响应丢失 | 专项功能审查 / 小 | condense 先于 expire；目标与全部来源 digest 全等后才原子替换，任一漂移零写入；LLM 失败零 mutation，响应丢失 exact replay；新月摘要不被同轮 expire 误删 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| F28-20 | U28-14 派生 I/O 与崩溃恢复固定矩阵 | 冻结源码追踪完整 delta、durable save、strict delete、pending 与 checkpoint | personal/workscene 三域、部分派生、效果后响应丢失与连续重启 | 专项功能审查 / 小 | save 经同目录临时文件、文件 fsync、rename 与目录 fsync；delete 仅 ENOENT 成功；全部效果全等前不确认 checkpoint，失败 pending 持续重驱且最终恰一 materialized | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| F28-21 | U28-14 生产装配与 scope 固定矩阵 | 冻结调用图与结构可达性扫描 | anchor turn/system 触发、non-anchor、legacy readers/writers 与 workscene 边界 | 专项功能审查 / 小 | anchor 仅装配一个共享 single-flight service；turn 与 `__journal-gc` 只是触发源；non-anchor 零 service，旧 JournalStore/loadProfile/PeopleStore 零生产读写可达；workscene 只承担既有派生恢复 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| A28-13 | 冷启动角色：用户身份与管理视图一致性 | 重造 cutover 后文件编辑、空/重复 profile、投影滞后与 authority 更新 | U28-14 公开 personal memory 链 | 对抗复审 / 小 | authority 是唯一公开事实源，legacy 文件变化不可影响视图；空态有明确设置路径，重复 profile fail-closed；未发现新增反证 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| A28-14 | 冷启动角色：journal 生命周期与权威写入 | 重造并发维护、source/target CAS 漂移、LLM 失败、响应丢失与连续触发 | U28-14 planner/service/control mutation | 对抗复审 / 小 | single-flight 消除同 owner 并发，稳定身份与全量 CAS 保证零半凝练/误删；失败零确认且后续触发可收敛；未发现新增反证 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| A28-15 | 冷启动角色：派生 I/O 与崩溃恢复 | 重造部分 save/delete、fsync/checkpoint 响应失败、旧 pending 与连续重启 | U28-14 materializer 与 MemoryStore | 对抗复审 / 小 | 任一失败保留同 pending；重驱按全等效果幂等追平，较新 projection 不被旧 pending 覆盖；连续重启最终收敛且无假 materialized；未发现新增反证 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| A28-16 | 冷启动角色：生产装配与范围价值 | 重造 non-anchor 启动、双触发竞争、旧读写面复活与 workscene 扩面 | U28-14 当前装配、结构扫描与规格边界 | 对抗复审 / 小 | 唯一 anchor owner 与共享 service 闭合；公开/lifecycle 仅 personal，workscene 仅派生；未新增通用事务、恢复、通知、监控、诊断、benchmark 或信息采集设施 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |
| D28-04 | U28-14 反证差异审计 | 对账原失败链、C28-21～C28-23、F28-18～F28-21 与 A28-13～A28-16 | U28-14 当前记录、直接测试及当前冻结指纹 | 专项收口 / 小 | C28-21 由 control-only 原子 condense 修复，C28-22 由 personal lifecycle/workscene 派生边界修复，C28-23 由全等效果后 checkpoint 与 pending 重驱修复；历轮发现零消失、零新增同根反证 | git-delivery-manifest-v1:93f6a1747925512ca08713831369f8de964457615e99a6ef14e55eb1cee9c1f5 | 有效 |

| M28-20 | U28-15 canonical identity 与 CRUD/search 闭包 | core、orchestrator、tools-builtin、runtime-host 与 CLI 定向测试、类型检查和构建 | canonicalizer、三类 writer、local/mesh GlobalQuery、MemoryStore 与产品工具 | 集中修复 / 中 | 受影响测试全部通过；profile 固定身份、people 安全 slug、daily/monthly journal 分离、共享排序去重与统一 limit、最终路径 containment 均由同一判别联合约束 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| M28-21 | U28-16 付费 journal 凝练的耐久透明闭环 | owner-kernel 与 CLI journal service/consumer 定向测试、类型检查和构建 | SchedulerUserNoticeJournal、journal service、turn/system 触发、CLI presenter 与 `/journal` | 集中修复 / 中 | 零计划零通知；调用前 prepared/open 已耐久；成功、失败、重试、响应丢失、双触发、停机与连续重启均以同 notice 单调收敛，live/history/`/journal` 去重一致 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| M28-22 | 两项最终必要验证与交付闭包 | 受影响包类型检查、定向测试、package build、CLI build、diff check 与 manifest 生成器 | 当前 U28-15、U28-16 完整交付物 164 路径 | 集中修复 / 中 | 192 项直接相关测试通过；core、owner-kernel、orchestrator、tools-builtin、runtime-host 与 CLI 构建通过；`git diff --check HEAD` 通过；路径集与分组可重复计算 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| F28-22 | U28-15 产品身份与 CRUD/search 固定矩阵 | 冻结源码追踪 producer、codec、planner、overlay、query/result、local/mesh 与最终 I/O | profile/person/journal 全部合法与跨域组合、三类 writer 及受支持 CRUD/search | 专项功能审查 / 小 | 全部生产和 wire/I/O 边界复用同一 canonical identity；非法组合、日期、路径与保留名提交或 I/O 前零副作用，合法 CRUD/search、重放与派生全等 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| F28-23 | U28-16 付费 maintenance 通知与恢复固定矩阵 | 冻结源码追踪计划、notice、attempt、付费调用、authority 应用、重试与 stop | 零/单月/多月计划、turn/system、live/history、`/journal`、响应丢失与连续重启 | 专项功能审查 / 小 | 完整计划摘要派生稳定 noticeId；每次付费调用前先耐久落定 attempt，应用成功但响应丢失时以同 notice 的 applied prefix 续跑，stop 拒绝新 wake 并等待在途 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| F28-24 | U28-15↔U28-16 journal maintenance 直接交界 | 冻结源码双向追踪 daily/monthly canonical identity、planner、service、authority mutation 与 materializer | journal 公开日记、内部月摘要、凝练计划与派生恢复 | 专项功能审查 / 小 | daily/monthly 边界由同一 canonicalizer 固定；service 只消费 path-free 权威计划，内部月摘要只由 host control/legacy 产生，公开 writer 无法伪造 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| A28-17 | 冷启动角色：memory 产品身份与 CRUD/search 一致性 | 重造错域、错键、非法日期/路径、profile/person 分裂、跨域排序与 limit | U28-15 冻结生产调用图 | 对抗复审 / 小 | C28-30、C28-31 已同根修复；固定 profile、安全 people slug、共享 comparator/dedupe/limit 和末端 containment 闭合全部反例，无未处置残留 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| A28-18 | 冷启动角色：codec/planner/最终路径安全 | 重造 conversation/job/远端输入、local/mesh 畸形 query/result、LLM 漂移与跨平台路径 | U28-15 冻结边界调用图 | 对抗复审 / 小 | producer、严格 codec、planner、adapter、query/result validator 与 MemoryStore 末端使用同一身份规则；非法输入 fail-closed 且零 staged/I/O 副作用 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| A28-19 | 冷启动角色：付费 maintenance 的耐久通知与恢复 | 重造零计划、失败/重试、authority 效果后响应丢失、并发双触发、停机和连续重启 | U28-16 冻结生产调用图 | 对抗复审 / 小 | C28-32 由同 notice applied-prefix 追平修复；单一 owner、稳定身份、调用前耐久事实和单调终态闭合全部反例，无重复收费或静默失败路径 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| A28-20 | 冷启动角色：生产装配与范围价值 | 重造 non-anchor、双 service、旧 writer、缺 consumer 与通用基础设施扩面 | 两项当前装配、结构扫描与产品消费链 | 对抗复审 / 小 | 组合根只管理一个 anchor service，turn/system 仅 wake；公开视图与通知复用现有 GlobalQuery、SchedulerUserNoticeJournal 和 presenter，无第二事实源或低价值设施 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |
| D28-05 | U28-15、U28-16 反证差异审计 | 对账原失败链、C28-24～C28-32、F28-22～F28-24 与 A28-17～A28-20 | 两项当前记录、固定矩阵、直接测试及当前冻结指纹 | 专项收口 / 小 | C28-24～C28-29 均同根修复后复核通过；C28-30～C28-32 首次登记后完成修复并重新验证；历轮发现均有“同根合并”或“修复后复核通过”证据，零未处置反证 | git-delivery-manifest-v1:f67a9e6766a7f410d86027ce31f6ad3471d19a536a2a6db5accfa0abcbbb26ca | 有效 |

## 终审记录

| 轮次   | 审查侧重                                       | 矩阵是否完整 | 新增问题 | 交付物指纹 | 结论   |
| ------ | ---------------------------------------------- | ------------ | -------- | ---------- | ------ |
| 第一轮 | 需求、架构、功能闭环、状态、回归               | 否           | —       | —         | 待开始 |
| 第二轮 | 并发、崩溃、安全、资源上界、异常终态、测试盲区 | 否           | —       | —         | 待开始 |

## 独立审查覆盖表

> 本表只记录独立审查覆盖进度，执行规则见工作台静态区“独立功能审查”。本表按失效机制划分，禁止照抄审查结论复用表；必须常设跨区组合核查行。状态只允许“待审”“审查中”“已覆盖”“失效”“有问题”。

| 编号 | 风险区与风险面 | 登记输入与指纹 | 独立覆盖状态 | 结论与证据 | 重开条件 |
| ---- | -------------- | -------------- | ------------ | ---------- | -------- |

<!-- registration-complete: unit-28.gen-1 -->
