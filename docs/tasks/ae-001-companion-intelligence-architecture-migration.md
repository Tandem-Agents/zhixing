# AE-001 伴身智能目标架构迁移

> 状态：执行中<br>
> 当前检查点：A5-16c Device Administration 值班迁移写决定与 Product API binding 已实施，等待协调者复核<br>
> 完成度：5/8<br>
> 职责：在保持知行当前全部正式能力与首版发布边界不变的前提下，把生产实现完整迁移到 AE-001 定义的目标架构，并删除全部旧责任路径。
> 权威设计：[《AE-001：伴身智能架构演进》](../../research/design/architecture/evolutions/AE-001-companion-intelligence.md)

## 一、任务目标

在同一最终代码基线上达成以下结果：

1. 知行的产品语义、权威事实与共同应用行为形成唯一逻辑产品身份；模型、Runtime、Host、协议、进程和设备都不再反向定义知行。
2. Conversation、Workscene、Schedule、Advancement、Delivery 及现有管理责任分别形成真实的垂直领域边界，由同一责任者拥有合同、用例、规则和事实归属；Product API 只组合这些合同。
3. Intelligence Kernel、Correctness Substrate、ApplicationHost、产品表面和基础设施适配器的职责、依赖、状态与生命周期完整闭合，单机和多设备只在部署适配处发生差异。
4. 当前全部正式能力、公开行为、协议语义、持久化数据、安全边界、异常终态和首版产品体验保持不变；本任务不给用户增加新的概念、配置或操作负担。
5. 迁移完成后不存在旧业务入口、第二组合根、第二应用行为、第二状态机、第二事实源、双写、永久兼容桥、服务定位器旁路或以新名称保留的旧结构。
6. 目标架构由生产调用图、依赖方向、合同与事实所有权、生命周期、直接测试、制品和产品回归共同证明，而不是由目录、包名、接口数量或文档自报证明。

本任务只迁移架构，不增加产品能力。完成本任务不以新增个人连续性、主动触达、受控学习、记忆、多设备体验或生态能力为收益。

## 二、权威、对象与硬边界

### 2.1 权威关系

1. AE-001 的“一、原始构想”是最高产品依据，正文是本任务唯一目标架构权威；本文只负责执行范围、状态、证据和结束裁决，不得重新定义目标架构。
2. 当前生产源码、组合根、调用与消费链、状态写入、测试和制品用于证明现状、迁移可行性与行为基线，不能反向降低 AE-001 的目标。
3. 既有其他设计文档可能滞后，只作旁证。本文与 AE-001 冲突时必须修正本文；生产事实暴露 AE-001 无法唯一裁决的产品或架构问题时，停止对应工作包并把唯一决策点交给用户，不得在实现中暗自改写架构。

### 2.2 本任务迁移的对象

本任务迁移的是生产责任和依赖，而不是一组预设文件：

- 产品合同、应用用例、领域规则、状态机、权威适配和产品投影的所有权；
- Agent Runtime、RuntimeHost 与产品领域、宿主管理、安全内部对象之间的边界；
- CLI、Channel、Server/RPC 与应用行为之间的边界；
- Owner、Journal、Security、Confirmation、Resource、Persistence、Recovery、Assignment 与领域语义之间的边界；
- Provider、Tool、MCP、Storage、Channel、Executor、Mesh 与其上层端口之间的边界；
- 生产组合根的创建、启动、入口开放、换代、拒新、排空、关闭和残留清理；
- 单机与分布式拓扑的适配位置；
- 证明上述边界的依赖门禁、合同测试、结构证据、回归和现行架构文档。

实施必须从真实生产入口双向追踪，不能把本节当成封闭文件清单。

### 2.3 必须保护的产品与工程基线

除纯内部责任归位外，以下现有合同均不得回退或产生无授权的可观察变化：

- 模型—工具循环、上下文窗口、子 Agent、显式编排、多视角发散/收敛、任务列表、中断、取消、错误和明确终态；
- Conversation、历史持久化与恢复、Workscene、Schedule、Advancement、Delivery；
- Trust、Skill、用户可见设备关系及其现有管理行为；
- 工具、MCP、权限、确认、资源治理、工作区、安全拒绝和受控效果；
- Owner、Journal、Run、Assignment、全局状态、恢复和异常重驱；
- CLI、Server/RPC、消息通道及其公开命令、方法、事件和错误语义；
- 单机、Anchor/Executor、Mesh 与当前分布式运行时的正常和异常行为；
- 首次配置、Provider/MCP 管理、恢复备份与灾难恢复、维护/卸载，以及配置、秘密边界、构建、包导出、安装、Windows x64 制品与当前首版产品旅程。

### 2.4 明确排除

- 不新增、恢复或重建 Memory，也不接入 OWNWARD；未来记忆方案继续保持未决。
- 不新增个人连续性、主动触达、受控学习、新设备体验、插件市场、自修改或其他产品能力。
- 不把 distributed-runtime、多 Agent、插件、沙箱、协议或任何单项技术提升为产品或架构中心。
- 不推倒重写 AgentRuntime、Owner 或 distributed-runtime，不为了目标图一次性搬迁代码。
- 不预建万能领域、万能 `Capability`、动态插件内核、微服务体系或没有当前事实依据的扩展框架。
- 不把架构迁移变成首个版本发布的前置条件；发布问题和架构迁移分别管理。

## 三、建任务时的生产事实基线

本节记录开始迁移前必须重新验证的入口，不代表最终边界已经由文件名决定：

| 当前生产事实 | 目标迁移方向 |
|---|---|
| `@zhixing/cli` 依赖全部主要生产包，`cli/src/serve/command.ts` 是事实组合根 | 原地形成 ApplicationHost 后按责任迁出，CLI 回到进程入口与产品表面 |
| `access-surface`、`StartupRollback`、`CleanupRegistry` 和 shutdown chain 已有启动、补偿与逆序清理能力 | 作为 Host 生命周期资产继承，不把当前 `AssemblyContext` 原样改名成目标架构 |
| `ServerContext` 聚合会话、推进、场景、信任、技能、设备、迁移、投递和 Runtime 控制 | 逐步收敛为 Product API dispatcher 与有限 binding 依赖，handler 不再编排业务 |
| `AgentRuntime` 公共面包含安全、权限、工作区和管理查询等宿主内部对象 | 收窄为有限 Run Envelope / Event / Terminal 与查询端口 |
| `RuntimeHost` 直接认识 Workscene、Schedule 和专用工具 | 产品投影回到对应领域，RuntimeHost 只做通用运行装配 |
| 产品行为横跨 `core`、`owner-kernel`、`owner-services`、`server` 和 `cli/src/serve` | 先在现有目录形成垂直责任边界，再按真实需要决定 subpath 或拆包 |
| 几乎所有生产包依赖 `@zhixing/core`，根导出包含多类产品和基础设施实现 | 收紧公共 API 与依赖方向，领域合同随领域归位 |
| 用户可见设备管理与连接、epoch、Assignment、Mesh、执行位置在 CLI/Server 装配中交织 | Device Administration 与 Executor/Mesh 拓扑适配分离 |

旧 Memory 已经退场，不进入迁移登记表。A0 只建立**足以安全开始迁移和裁决迁移顺序的最小基线**，不是把整个产品预先反向工程一遍。发现名称与真实职责不一致时按生产调用链裁决。

A0 只需闭合以下五类决策输入：

1. 生产进程角色、入口、组合根与组件生命周期；
2. 公开合同、持久记录族、包导出和正式制品入口的可执行 exact-set；
3. 能代表 Surface、ApplicationHost、Domain、Kernel 与 Correctness 关键交界的产品回归基线；
4. 足以确定 A1、A2 及后续领域顺序的当前 owner/写入口/恢复主链、AgentRuntime/RuntimeHost/拓扑分叉和依赖方向迁移图；
5. 与首批迁移直接相关的测试、结构门禁、制品证据和已复现基线失败映射。

A0 不要求预先穷举每个产品旅程、错误分支、全部消费者或每个领域的完整状态机。未被当前迁移决策触及的细节必须绑定到实际改变该责任的 A1～A6 工作包：动手前沿该包生产入口与反向消费者即时取证，迁移时补齐直接保护证据，完成时删除旧路径。这样只改变证据取得的时机，不降低任何产品回归、架构闭包或最终退出标准。已接受的 A0 证据继续有效，不得重复取证；无法改变当前迁移顺序、边界、实现或验收的事实不得继续写入本文。

## 四、实施状态

每项 `[x]` 表示该检查点的生产责任、全部直接消费端、旧路径删除、保护性回归、必要文档和当前基线证据已经同时闭合。只移动文件、增加接口、通过编译或完成局部测试不得勾选。

- [x] **A0　冻结最小迁移决策基线。** 完整读取 AE-001 和验证手册；复用已接受的生产角色、生命周期、公开合同、持久记录与制品 exact-set，补齐一张紧凑的“当前责任 → 目标责任 → A 项/迁移顺序 → 保护证据”决策图，以及首批迁移所需的 Kernel/RuntimeHost/拓扑、依赖和验证映射。A0 只回答“从哪里开始、责任如何转移、用什么证明没有回退”，不预先穷举后续每条领域行为。所有正式能力仍必须在真正改变其责任的工作包中完成即时双向取证、直接回归和旧路径删除；未改变当前迁移裁决的未知不得阻塞 A0。退出证据是足以直接派发 A1/A2 且没有未归属生产角色或迁移责任的决策基线，不是搜索结果、文件清单或全产品百科。
- [x] **A1　收束 ApplicationHost 生命周期边界。** 从现有 `access-surface`、启动回滚、清理注册和关闭链形成每个生产进程唯一的 Host 对象与类型化组件贡献；明确 create/start/open、换代、拒新、排空、close 和失败补偿，组件只管理自身内部资源。配置来源和秘密材料继续由专责 Provider 拥有，Host 只冻结并下发组件所需的最小只读投影，领域、Kernel 和 Surface 不读取来源。Host 可以暂时委托旧领域实现，但不得新增产品逻辑、领域状态、运行期服务定位或第二组合根。以完整启动失败、部分启动回滚、并发关闭、在途排空和零资源残留直接证明生命周期。
- [x] **A2　用一条真实垂直切片证明领域模型。** 依据 A0 的依赖事实选择边界最小而行为完整的现有领域，先记录选择理由和明确不做；让该领域同时拥有自己的 Command/Query/Event 合同、应用用例、规则与状态机、领域权威适配、面向 Kernel 的 Run Envelope 投影、面向表面的投影和生命周期贡献。至少让一个真实现有表面经进程内或 RPC binding 调用同一应用端口，迁移后立即删除旧写入口。本项不得预建全局领域框架或为了模板美观拆包。
- [x] **A3　建立组合式 Product API 与纯绑定。** 只从 A2 已证明的领域合同建立 Product API Catalog、dispatcher 和进程内/RPC binding 模式；Catalog 只组合，不拥有业务类型、规则、状态或写入权。Command/Query/Fact Event/Progress Event 的语义与身份清楚，瞬时进展不能成为恢复事实；Progress Event 必须带运行身份与顺序，消费者发现缺口后重新 Query 权威投影。让 A2 领域的全部现有 Server/RPC、CLI、Channel 入口只承担各自实际存在的认证、连接、编解码、调用、订阅、错误映射和呈现；本机路径复用同一应用语义，但不被迫经过网络或无价值序列化。其他领域只能在 A5 完成自身垂直迁移时接入该模式，不得由 A3 先建立空合同或平行入口。
- [x] **A4　封闭 Intelligence Kernel。** 基于全部现有运行形态冻结最小 Run Envelope / Event / Terminal 合同和 Conformance Suite；收窄 `AgentRuntime` 公共面，隐藏安全管线、权限存储、工作区解析和宿主管理内部对象。产品对象到模型输入、运行事件到产品结果均经显式投影；模型输出只能形成提议、证据和运行终态，必须经领域决定与权威提交后才成为产品事实。A4 只负责 Kernel 合同及其边界：把 Workscene、Schedule 等产品特有装配移出 RuntimeHost，交给当前唯一产品责任者生成信封；尚未完成 A5 的领域可以通过登记退场点的最短单向端口提供投影，但不得在 A4 复制其状态机、权威或 Product API。RuntimeHost 只消费通用运行合同，对话、任务、本机、远端、取消、失败和资源释放行为必须等价。
- [ ] **A5　逐领域归位全部现有产品责任。** 按 A0 得到的无环依赖顺序迁移其余领域；每次只迁移一个事实所有权，并把合同、应用用例、状态机/Reducer、权威适配、既有 Kernel 投影的最终归属、Product API binding、Surface、生命周期、直接测试和旧入口删除作为同一责任链闭合。A2/A3/A4 已建立的合同、binding 与投影必须迁入最终唯一 owner 或直接复用，不得在 A5 重建第二套。领域只实现 Correctness 端口的业务适配，不得把通用提交、效果、安全、确认、资源、持久化或恢复机制复制进领域；Correctness 实现中的业务 Reducer、状态机和用户语义则必须随本项退出共享机制。A2 选中的领域保留在下表，但只有 A3 完成其 Product API 与全部现有 binding 后才能勾选；登记表所有行均为 `[x]` 后 A5 才能完成。A0 若发现未列出的正式产品事实，只有在生产事实与 AE-001 能唯一确定其窄边界时才可补行；没有独立事实的运行/交互方式应归入既有领域、Kernel 或 Surface，不得为了穷举虚构新领域。只有真正的产品语义歧义才暂停交由用户裁决。

  | 领域或管理责任 | 状态 | 必须成立的目标边界 |
  |---|---|---|
  | Conversation | [x] | 会话合同、用例和事实归域；历史与运行投影不被 Surface、Kernel 或 Server 拥有 |
  | Workspace Administration | [x] | 本机 workspace 身份、绑定、修订、操作交付与 reset 归域；Workscene、CLI fallback 与 Executor 只消费明确端口 |
  | Workscene | [x] | 场景身份、工作区与会话关系归域；RuntimeHost 不认识场景产品规则 |
  | Schedule | [x] | 调度产品规则与耐久事实归域；执行触发只消费 Kernel/Effect 端口 |
  | Advancement | [x] | 执行、评价、证据与成果准入边界保持现有语义且归属唯一 |
  | Delivery | [x] | “结果需要交付及其终态”归域；Channel 只实现发送效果 |
  | Trust Administration | [x] | 用户可管理的信任规则归域；Security Substrate 只负责执行决定 |
  | Skill Catalog | [x] | 技能资产与生命周期归域；Kernel 只消费不可变能力投影 |
  | Device Administration | [ ] | 用户可见设备关系、移除和值班迁移归域；Executor/Mesh 只实现物理效果 |
  | Backup & Recovery Administration | [ ] | 备份目标、恢复根、checkpoint 选择与灾难恢复产品用例归域；Correctness/Storage/Mesh 只提供签名、冻结、传输、安装与恢复机制 |

- [ ] **A6　收束可替换边缘与设备拓扑。** Provider、Tool、MCP、Storage、Channel、Executor、Mesh 分别实现需求方拥有的窄端口，不统一成万能 Capability，也不生成产品流程、文案或领域决定；同一实现承担多个角色时必须分别实现端口、依赖和生命周期合同。用户设备事实与物理连接、路由、Assignment 和执行位置彻底分开；单机与分布式只在 Host 的适配选择处分叉，领域和 Kernel 不出现 Anchor、epoch、Mesh 或本机/远端条件分支。扩展只能进入可组合区，不能替换权威、安全、恢复、资源和终态。
- [ ] **A7　删除迁移结构并完成同基线终验。** 删除所有旧委托桥、旧业务入口、第二组合根、根级宽泛导出、无主公共接口、可变 lazy ref、服务定位旁路和临时兼容；完成依赖、合同、事实所有权、Host 生命周期、Kernel、Authority/Effect、拓扑透明、变化影响面和产品回归门禁，更新正式架构与模块索引。冻结同一源码、测试、文档、构建产物和验收输入后执行最终门禁与六面对抗；发现真实问题即恢复对应 A 项和退出门为 `[ ]`，修复后重新冻结并重做受影响门禁及整轮对抗。

## 五、工作包与状态维护规则

1. 本文是迁移任务的唯一执行与状态契约，AE-001 是目标架构权威。协调者每轮先读取二者、第九节实时台账、执行记录和目标对话最新状态，独立核对仓库事实后再派发当前价值最高的一条问题链；执行者自报不能代替复核。实时台账记录当前裁决，追加记录保留历史证据；二者冲突时必须先按仓库事实纠正台账，不能靠翻阅对话猜测状态。
2. A0～A7 是阶段验收门，A5 各行是领域验收单元，都不是默认可整包派发的工作包。只有当前事实证明某个阶段或领域的全部剩余工作同时满足本节颗粒度上限、单一责任和单一验证闭包时，才允许整体派发；不得直接把“A0”“A4”“迁移某领域”等标题当成工作包。
3. 工作包必须写清：对应 A 项、当前检查点与基线、要改变的唯一责任、生产端与全部直接消费端、保护边界、明确不做、旧路径处置、最窄直接证据、完成条件和安全交接点。一个包以一个可独立证伪的架构结果为结构边界，例如一次 owner/写权转移、一条生命周期责任闭合或一项依赖反转；可以跨文件和包，但不得同时改变多个相互独立的事实所有权、生命周期或问题链。
4. 取证服务于当前架构结果，不单独追求“了解完整”。已有证据能裁决时立即进入实现；缺少事实时只追踪会改变本包边界、方案或验收的生产链，随即停止调查。不得为未来工作提前穷举产品旅程、错误矩阵、状态机或消费者；这些事实必须在实际迁移相应责任的工作包中即时核实并取得直接保护证据。
5. 默认工作包按 **2～4 小时真实有效工作量**设计。短于两小时的剩余工作，只能与同一责任链的必要前后步骤合并；不得为凑时长合并无关事项。派发前出现以下任一情形必须继续拆分：预计超过四小时；包含多个独立 owner、写权或生命周期转移；存在两个以上可以分别失败的未知或根因假设；需要彼此独立的验证闭包；跨越 A 项或 A5 领域边界；无法说清四小时内可到达的安全交接点。
6. 执行开始后不得无界探索。到达约四小时仍未满足完成条件时，必须在当前可构建、可运行且单一真相成立的安全检查点停止，记录已完成事实、未完成闭包、失败路径、有效证据和唯一下一步并反馈协调者；连续两条实质技术路径失败、影响面显著超出派发基线、出现未预期跨领域依赖或原任务假设被反证时，应提前触发同样的止损。唯一例外是已经启动、输入已冻结且没有安全中断点的单次确定性验证，可以等待该命令结束，但结束后不得顺带继续新工作。
7. 为控制颗粒度允许使用有明确退场点的最短单向适配，把同一责任迁移拆成相邻工作包；每个中间包仍必须只有一个事实源、一个写入口和可发布状态，禁止双写、双权威或半条不可运行生产链。工作包必须登记临时桥的唯一退场包，桥未删除前不得勾选对应 A 项或领域行；协调者应优先连续派发该责任链，不让临时结构长期存在。
8. 协调者派发前必须在第九节登记活跃工作包、派发基线和安全交接点。工作包只在真实完成、安全止损、出现必须决策的阻塞或用户询问时反馈；不得按分钟汇报或把局部测试进度冒充整体进度。协调者收到反馈后必须先独立复核，再更新当前接受基线、活跃/下一责任链、临时桥、失效证据和阻塞，随后决定提交、纠正或下一包；不能因执行时间尚短而跳过验收，也不能因执行时间已长而放宽边界。
9. 唯一检查点顺序为 A0 → A1 → A2 → A3 → A4 → A5 → A6 → A7。A5 内部按 A0 得到的无环依赖顺序推进；上一检查点没有在可构建、可运行、可发布状态闭合，不得启动下一项。
10. 状态只记录当前代码基线事实：首个工作包开始时改为“执行中”；进入 A7 对抗时改为“对抗复核中”；全部最终退出门成立后改为“待用户确认”。未经用户确认不得写“已完成”。
11. `[x]` 只在该项全部生产责任、消费链、旧入口清理、保护回归、必要文档和直接证据同时成立后更新。每次登记必须包含 `HEAD + 本任务相关工作区差异标识`、实际变更、命令或静态证据、结果、遗留问题和下一检查点；历史绿灯、mock 自报和仅看源码名称无效。
12. 后续任何源码、测试、现行架构文档、构建配置、制品或验收输入变化，只要可能影响已勾选结论，立即把对应 A 项、领域行和退出门恢复为 `[ ]`，作废旧证据并只重验失效闭包。目标架构发生实质变化时，A0 及全部受影响下游结论同时失效。
13. 迁移中发现同根遗漏，补回当前 A 项并闭合，不另建平行迁移主线；发现能力缺陷、产品增强或范围外重构，独立登记但不得混入本任务。
14. 执行者不得执行 Git 暂存、取消暂存、提交、历史改写或推送。协调者只能在独立复核一个工作包完整闭合后，按责任边界提交本包生产代码、测试、必要文档和本文状态记录；不得混入用户既有变更。
15. 只有 AE-001 与当前不可回退产品基线无法唯一裁决、且不同选择会改变产品定位、公开行为或目标架构时才交给用户。普通实现困难、测试失败和可自行修复的结构问题不得上抛；这些问题若触发本节止损，应交回协调者调整工作包，而不是转交用户。

## 六、实施约束

1. **行为等价优先。** 每次迁移只改变责任和依赖，不改变产品语义、默认值、错误、终态、持久化与恢复。确需改变公开行为或数据合同必须另行取得用户授权。
2. **一个时刻只有一个真相。** 允许为了安全拆包保留最短单向适配，但禁止双写、双权威、双状态机、双业务协议和无期限兼容；新路径接管后在同一工作包或预先登记的紧邻工作包删除旧入口，未退场前对应阶段不得完成。
3. **先边界，后搬迁。** 优先在现有目录用可见性、subpath、端口和依赖门禁形成责任；只有独立消费者、发布、生命周期、故障隔离或复用事实证明必要时才拆包。
4. **不制造新上帝对象。** Product API Catalog 不重定义领域；ApplicationHost 不编排用例；Correctness Substrate 不认识业务字段；Intelligence Kernel 不持有产品事实；`core` 不继续吸收新产品实现。
5. **静态、类型化、可追踪。** 初始生产图不用任意字符串注册、全局单例、可变 lazy ref 或运行期服务定位隐藏必需依赖；动态插件化不是本任务目标。
6. **产品复杂度不外泄。** 普通用户不需要理解 Host、Kernel、Owner、Mesh、设备角色或协议拓扑，不承担组装核心流程和维护系统正确性的责任。
7. **数据与安全不降级。** 内部格式如需迁移，必须提供有界、可恢复、单向且无双写的升级路径；权限、确认、秘密、资源、沙箱/执行边界和 fail-closed 语义不得为了解耦而放宽。
8. **不以架构名义补能力。** 迁移过程中禁止恢复 Memory、引入未来对象、扩展插件面、增加诊断/benchmark/遥测框架或实施与当前责任转移无关的重构。
9. **不阻断首版交付。** 每个中间检查点必须可构建、可运行、可发布；若迁移与首版问题冲突，先保持首版基线，架构任务停在安全检查点。

## 七、验证策略

执行任何验证前必须读取 [《跨单元验证运行手册》](../../research/design/workbench/verification-runbook.md)，遵守当前 Windows 低资源机器的串行、一次取证、失效闭包和上游先构建规则。

1. **渐进验证：** 工作包先跑能识别本次责任变化的最窄静态门禁、直接测试和受影响包构建；上游源码改变后先生成新 `dist` 再测下游。失败先归因，只重跑失效闭包，不并发运行构建、类型检查或重型测试。
2. **依赖与公共面：** 用可执行门禁证明包和关键源码目录无环，禁止依赖不可绕过；Kernel、领域、Correctness 与 binding 的公共 API 有预算，无根 barrel、内部对象或旧 subpath 旁路。
3. **合同与事实所有权：** 每项 Command/Query/Event 只由对应领域定义；每项事实的写入口、Reducer、Authority 与恢复路径 exact-set 唯一，Catalog、Server、CLI、Channel 和基础设施不能成为旁路写入者。
4. **多表面与 Host：** 进程内、RPC、CLI 和 Channel 命中同一应用用例并保持等价行为；依赖先启动、入口最后开放，拒新后排空，资源逆序释放，故障注入无 listener、timer、child process、连接或临时残留。
5. **Kernel 与正确性链：** 全部现有运行形态通过 Run Envelope / Event / Terminal Conformance；有外部后果的动作不能绕过领域决定、权威提交、安全/确认/资源准入、效果结算和恢复。
6. **拓扑透明：** 同一应用和 Kernel 合同可绑定本机或分布式适配器；领域、模型消息和 Surface 没有物理拓扑分支，设备管理与执行拓扑的测试分别具有识别力。
7. **产品回归：** A0 冻结的全部正式行为、协议、数据、异常终态和首版旅程按受影响闭包持续验证；CLI/REPL、RPC、Event、工具/Profile、配置 schema、持久记录族和包导出的 exact-set 必须能识别未授权增删或语义漂移。不得用改 snapshot、放宽超时/业务断言、增加豁免、删除保护性测试或 mock 命中代替生产路径。
8. **最终同基线门禁：** A0～A6 完成后冻结交付物，先以 `pnpm build` 生成当前源码对应的完整产物，再串行执行架构新增门禁、`pnpm contracts:typecheck`、`pnpm contracts:lint`、`pnpm runtime:baseline`、`pnpm runtime:package-exports`、根级 `pnpm lint`、`pnpm test` 和 canonical `pnpm package:check`。复用本轮机器结果和产物，不为了收集日志重复全测；纯观测或 benchmark 不得成为同步退出门。

## 八、最终退出门

只有以下条件在同一未修改产品基线上全部成立，技术工作才算完成：

- [ ] A0～A7 全部为 `[x]`，A5 当前登记表中的全部领域/管理责任均为 `[x]`，执行记录与当前工作区事实一致。
- [ ] AE-001 的核心命题和全部架构不变量均有生产实现、依赖或合同门禁及直接回归的可追溯证据，不存在只落实名称或目录的条款。
- [ ] 每个生产进程只有一个 ApplicationHost 组合根；配置/秘密来源归专责 Provider，Host 只消费最小只读投影且不持有领域事实、不编排产品用例、不提供运行期服务定位，启动、换代、排空和关闭无资源残留。
- [ ] 每项产品合同、应用用例、规则、事实、写入权威和恢复路径归属唯一领域；Product API Catalog 只组合，所有 Surface/Transport 只绑定和呈现。
- [ ] Fact Event 与 Progress Event 语义分离；投影、缓存、摘要、协议响应和 UI 状态不能成为事实源，跨领域同步依赖无环。
- [ ] Intelligence Kernel 只依赖模型、工具和运行合同，不导入产品领域、Surface、RPC 或设备拓扑；现有全部运行形态通过同一 Conformance Suite。
- [ ] Correctness Substrate 只提供通用提交、效果、安全、确认、资源、持久化与恢复机制，不拥有领域规则；任何领域也没有重造这些机制。
- [ ] Device Administration 与 Executor/Mesh 拓扑事实彻底分离；单机和分布式只在 Host 适配选择处分叉，distributed-runtime 没有成为产品或架构中心。
- [ ] CLI 不再是生产后端，Server 不再是应用服务定位器；不存在第二组合根、旧业务写入口、双权威、双状态机、永久兼容桥、根 barrel 旁路、可变 lazy ref 或等价改名残余。
- [ ] A0 最小迁移决策基线在第九节均有当前证据引用且没有未归属生产角色或迁移责任；CLI/REPL、RPC、Event、工具/Profile、配置 schema、持久记录族、包导出和制品入口 exact-set 无未授权漂移；各 A1～A6 工作包已对其实际影响的行为、状态、写入口和消费链完成即时双向取证与直接回归。
- [ ] 当前全部正式能力、数据、权限、安全、异常终态、恢复、单机/分布式行为和首版产品旅程无回退，用户没有新增架构学习与维护负担。
- [ ] 包依赖、公共 API、合同所有权、事实 exact-set、Host 生命周期、Kernel、Authority/Effect、拓扑透明和变化影响面门禁全部通过且能真实识别反向错误。
- [ ] contracts typecheck/lint、runtime baseline/exports、根级 lint/test/build 和 canonical package check 均在最终同一基线上通过；Windows x64 tarball 使用正式制品验证，无旧 `dist`、workspace 源码或 mock 替代。
- [ ] 正式架构、模块索引和必要工程文档与生产事实一致；任务没有把能力增强、未来记忆、插件平台或范围外重构写成已交付结果。
- [ ] A7 六面对抗具有实际动作、证据和结论，未发现未处置 P0/P1、资源残留或可推翻完成结论的反证。

满足后将本文状态更新为“待用户确认”，保留全部状态与证据并停止。未经用户确认不得把任务标记为“已完成”，不得继续完善架构、增加能力、暂存、提交、推送或发布。

### A7 六面对抗

1. **产品行为攻击：** 从不了解迁移的用户视角走查 A0 冻结的所有正式入口、异常与恢复，至少覆盖对话/历史、工作场景、调度、推进、多视角、任务列表/编排、投递、信任、技能、设备、恢复备份/灾难恢复、配置/MCP 和维护/卸载，主动寻找内部术语外泄、行为漂移、错误/终态改变、exact-set 漂移和多表面不一致。
2. **合同与所有权攻击：** 抛开任务勾选，从生产入口重新枚举 Command/Query/Event、事实、写入口、Reducer、Authority 和恢复路径，寻找中央业务模型、重叠 owner、旁路写入、事件冒充事实和跨域环。
3. **Host 与依赖攻击：** 从每个进程入口重新追踪创建、启动、开放、换代、排空与关闭，注入分阶段失败，寻找第二组合根、隐藏顺序、服务定位、反向依赖和资源泄漏。
4. **Kernel 与效果攻击：** 沿真实模型—工具调用攻击领域渗透、内部对象泄漏、取消/失败/未知终态、权限/确认/资源绕过及本机/远端结果不一致。
5. **拓扑、表面与制品攻击：** 对照进程内/RPC/CLI/Channel、单机/分布式与 Windows tarball，证明测试没有只命中 mock、旧 `dist`、workspace 源码、错误组合根或非正式入口。
6. **残留、变化影响面与范围攻击：** 反查旧入口、桥、别名、根导出、lazy ref、双轨和临时兼容；再只在隔离临时副本或门禁自带的负向 fixture 中实施最小反向变异，分别尝试让新增 Surface、Provider、设备拓扑或领域穿透其允许轴，证明变化影响面门禁能识别跨轴修改，不得改动冻结交付物或把变异内容带回工作区；同时确认迁移没有夹带新能力、Memory/OWNWARD 决策、动态插件框架或其他范围外结构。

任一对抗面发现真实问题，立即恢复对应 A 项、领域行和受影响退出门为 `[ ]`，状态退回“执行中”；任何交付物修改都会使本轮完整对抗失效。修复并重取失效门禁后，必须在新的同一未修改基线上重新执行全部六面，直至没有未处置反证。

## 九、执行记录

### 当前调度台账

本表是恢复调度时的首读状态，由协调者在派发前和独立验收后更新；不得由执行者自报直接覆盖。

| 项目 | 当前值 |
|---|---|
| 已接受基线 | `5fdb7cd6`；A5-16b 已由协调者独立复核并提交，`device.remove/device.continue` 的产品决定只经 Device Administration Command/Application/Product API，旧 ServerContext 写桥与 Mesh 产品入口为零；A5-16a 读取证据继续有效 |
| 当前 A 项 | A5：按无环依赖顺序逐领域归位现有产品责任 |
| 活跃工作包 | `A5-16c-duty-migration-application-v1` 已实施，等待协调者独立复核；三条 RPC 只经 Device Administration Command/Application/Product API，Mesh/Authority/Journal/Checkpoint/Transport 只保留物理机制 |
| 下一责任链 | 接受 A5-16c 后，对 Device Administration 做有限零残留审计并裁决当前设备卸载的责任归属；只有该领域完整闭合后才进入 Backup，不得提前进入 A6 |
| 打开的单向桥 | 无；`A5-DUTY-MIGRATION-PREPARE-01`、`A5-DUTY-MIGRATION-COMMIT-01`、`A5-DUTY-MIGRATION-CANCEL-01` 已随 `ServerContext.dutyMigration` 与 CLI 阶段闭包退场 |
| 已失效证据 | 无当前未恢复证据；A5-16c 受影响的领域应用、RPC binding、Host composition、S7、fresh exports 与构建证据已在当前工作区恢复，A5-16a/A5-16b 及 A5-15a～A5-15m 继续有效，等待协调者复核 |
| 阻塞/用户决策 | 无；当前工作区已形成可构建、可运行、单一 duty-migration 应用真相，等待协调者独立复核 |

### A0 基线索引

| 基线类别 | 当前证据引用 | 状态 |
|---|---|---|
| 生产角色、入口、组合根与生命周期 | `A0-01-process-roots-v1` + `A0-02-host-lifecycle-v1` + `A0-03-surface-command-lifecycle-v1`（本节；生产角色、入口、组合根及全部持久/非持久生命周期已闭合） | [x] |
| 公开合同与制品入口 exact-set | `A0-04-public-cli-rpc-event-v1` + `A0-05-tools-v1` + `A0-05-runtime-profiles-v1` + `A0-05-config-schema-v1` + `A0-06a-persistence-roots-v1` + `A0-06b1-persistence-schema-v1` + `A0-06b2a-authority-storage-schema-v1` + `A0-06b2b1-executor-delivery-schema-v1` + `A0-06b2b2-workspace-storage-schema-v1` + `A0-06b3a-distributed-transfer-storage-schema-v1` + `A0-06b3b-local-host-storage-schema-v1` + `A0-07a-package-export-build-v1` + `A0-07b-package-install-smoke-v1`（本节；CLI/RPC/Event、工具/Profile、配置 schema、P01～P15 持久合同、workspace package/exports/build/version，以及正式 tarball、隔离安装、CLI/helper smoke、默认零写与显式发布装配边界均已冻结；门禁未覆盖的 package-specifier/bin-shim/global-install/真实 registry 边界已显式登记，不冒充已验证能力） | [x] |
| 代表性产品回归与即时取证责任 | `A0-08a-setup-readiness-journey-v1` + `A0-08b1-conversation-lifecycle-journey-v1` + `A0-08b2a1-turn-admission-journey-v1(partial)`（首次配置、会话生命周期和请求准入已覆盖五层关键交界；其他产品行为在实际迁移相应责任的工作包中即时双向核实，不再作为 A0 前置穷举） | [x] |
| 迁移决策图：owner/写入/恢复、Kernel/Host/拓扑与依赖方向 | `A0-09-minimal-migration-decision-graph-v1`（本节；覆盖全部生产运行形态和现有产品/管理/Kernel/Correctness/边缘责任，固定 A1 首链、A2 Skill Catalog 切片与后续依赖偏序） | [x] |
| 首批直接测试、结构门禁、基线失败与 A1/A2 顺序 | `A0-10-initial-verification-map-v1`（本节；只覆盖 A1 持久 Host 外层唯一 owner 与 A2 Skill Catalog 首切片，严格区分现有证据、迁移期新增证据、当前门禁盲区及 A7 同基线终验） | [x] |

索引只保存会改变迁移裁决且可定位到本节记录、测试、门禁或制品结果的引用，不复制大段证据。任一类别失效时立即恢复为 `[ ]` 并同步受影响 A 项和退出门；五行全部为 `[x]` 且能够直接派发 A1/A2 时关闭 A0。详细产品状态机、错误和消费者仍由相应迁移包即时核实，不能因不在 A0 穷举而免除。

#### 执行期效率裁决

1. A0 已接受证据不得重复读取或重建，除非其精确失效条件真实触发。
2. A0 不再派发独立产品旅程、错误矩阵、Authority/Assignment 深层合同或全量消费者考古；A0-08b2a1 的未完成部分随 Conversation、Kernel、Correctness 或拓扑的实际迁移包按需闭合。
3. A0 剩余只允许 A0-09 和 A0-10 两条结果链；若单包需要拆分，只能按同一结果的安全交接点拆分，不能新增基线类别。A0-10 完成后立即裁决 A0 并进入 A1，不再以“再了解一点”为由延长前置阶段。
4. 质量标准保持不变：每个迁移包必须在动手前核实其受影响生产链，完成时取得直接回归并删除旧路径；A7 仍在同一基线上执行完整产品回归、结构门禁、制品验收和六面对抗。

#### A0-01 生产进程角色、入口与组合根 exact-set

证据 ID：`A0-01-process-roots-v1`。本表只冻结“哪个正式可执行入口会以什么运行形态创建哪一张生产依赖图”；公开命令/RPC/Event 的逐项 exact-set、完整 `create → start → open → replace → drain → close` 生命周期和领域 owner 仍分别属于后续 A0 工作包。

**入口集合。** 发布制品只有 `@zhixing/cli` 声明 bin，`zz` 与 `zhixing` 都指向同一个 `dist/index.js`；CLI 的 tsup 也只有 `src/index.ts → dist/index.js` 一个 entry。根包是 private，`pnpm cli` / `pnpm serve` 只是源码开发别名，分别把同一个入口调用为默认动作或 `serve` 动作。仓库内生产源码的可执行主模块守卫也只有 `packages/cli/src/index.ts:isExecutedAsMain`。因此正式 OS 进程入口 exact-set 是一个 CLI 入口、两个同义命令名；后台按需 child 和系统托管服务均使用当前 Node executable + 同一实际 entry script 重入 `serve`，不是第二份程序或第二组合根。

| 生产进程角色 / 运行形态 | 启动或公开入口 | 真实组合根 | 当前承担的装配责任 | AE-001 目标唯一归属 | 后续 A 项 | 代表性静态证据 |
|---|---|---|---|---|---|---|
| 交互终端 Surface（前台长运行） | `zz` / `zhixing` 默认 Commander action；开发别名 `pnpm cli` | `packages/cli/src/repl.ts:startRepl` | 创建终端 screen/input/rendering、`CoreHostConnection`、RPC facades、事件投影、会话 controller 与命令/typeahead；自身不创建 Server/领域 owner，宿主缺席时经 `spawnDaemon` 拉起另一个同入口的 Host 进程 | Surface + Product API binding；该进程自己的资源生命周期归唯一 ApplicationHost | A1、A3；Kernel 交界在 A4 | `packages/cli/src/index.ts` 默认 action；`repl.ts:startRepl`；`runtime/core-host-connection.ts:defaultCoreHostConnectionDeps` |
| 瞬态 CLI 控制 / 诊断 Surface（通常一次性；`serve logs --tail` 可持续读） | 同一 CLI entry 的非 `serve` action；包括运行控制、设备/值班管理、日志与纯帮助/诊断路径 | `index.ts` action 分发到 `runStatusCommand` / `runStopCommand` / `runLogsCommand`、`RpcManagementFacade` 设备与值班动作、`anchor-uninstall-command.ts` 与 `maintenance/{doctor,stop,prepare-uninstall}.ts`；纯 help/version 在 Commander 内终止且不创建产品图 | 按动作创建最短 RPC/本地读取或 OS 管理效果、呈现结果后释放；不拥有 Host 后端、领域事实或 Agent Runtime | Surface + Product API binding；动作语义归对应 Domain，连接/呈现不成为领域 owner | A1、A3、A5 | `packages/cli/src/index.ts` action imports；`runtime/device-removal-command.ts`、`runtime/duty-migration-command.ts` 的 `CoreHostConnection`/facade；`serve/status.ts`、`serve/stop.ts`、`serve/logs.ts` |
| 设备配对临时装配（命令期） | 同一 CLI entry 的 `pair` action | `serve/mesh-pair-command.ts:runPairCommand` | 命令期创建 SecretStore、DeviceKey、capacity、`FileMeshBootstrapStore` 与 issuer/joiner pairing transport/listener，完成后停止 storage maintenance；明确不加载 Agent Runtime | Device/Trust Domain 用例 + ApplicationHost 命令生命周期 + Mesh/Secret Infrastructure | A1、A5、A6 | `index.ts` 动态 import；`mesh-pair-command.ts:runPairCommand` 的 issuer/joiner 分支与 `finally` |
| 恢复备份 / 灾难恢复临时装配（命令期） | 同一 CLI entry 的 `backup` 及 recovery-root actions | `serve/backup-command.ts` 的命令入口 / `openContext` 与 `serve/disaster-recovery-command.ts` 的恢复入口 | 命令期创建 recovery checkpoint/target、Mesh bootstrap/control-plane、恢复根与 trust 投影，完成单次设置、验证、恢复或根控制后关闭 | Recovery Domain 用例 + ApplicationHost 命令生命周期 + Storage/Mesh/Secret Infrastructure | A1、A5、A6 | `index.ts` backup action imports；`backup-command.ts:openContext` / `ProductionMeshControlPlane`；`disaster-recovery-command.ts` |
| 本机工作区管理临时装配（命令期） | 同一 CLI entry 的 `workspace` actions | `runtime/workspace-command.ts:withLocalWorkspaceFacade`；`create-scene` 还经 `runWorkspaceSceneCreateCommand` 连接常驻 Host | 既有本机 workspace owner 可达时只用 client；否则临时创建 Mesh bootstrap、AuthorityRuntime、ExecutorRuntimeSubstrate 与 workspace owner/host，在 `finally` 关闭并释放；场景创建另经 Product API 交付消费凭据 | Workspace/Workscene Domain + Product API binding；临时资源生命周期归 ApplicationHost，物理目录适配归 Infrastructure/Topology | A1、A3、A5、A6 | `workspace-command.ts:withLocalWorkspaceFacade` 的 reachable/fallback 分支、`setupAuthorityRuntime`、`host.start` 与 `finally`；`runWorkspaceSceneCreateCommand` |
| Anchor Host + 本地 Executor（默认持久拓扑） | 隐藏 `zz serve`；REPL 按需 `spawnDaemon`；托管服务以同一 entry 重入 `serve --managed ...`；开发别名 `pnpm serve` | 公共预装配 `serve/topology-command.ts:runServeCommand` → `anchor-role.ts:run` → `serve/command.ts:runServeCommand/runServerProcess`；plan 同时动态加载 `@zhixing/executor` | 当前事实总组合根：配置/秘密、Mesh/Authority、owner、RuntimeHost/Agent Runtime、Scheduler、MCP/tools、投递/Channel、Server/RPC、工作场景及本地 Executor/assignment/data-plane 均在此装配并由 CleanupRegistry/回滚链管理 | 每进程唯一 ApplicationHost；Domain、Product API、Kernel、Correctness、Surface、Infrastructure 只作为 Host 选择和持有生命周期的组件贡献，不再由 CLI 文件本身拥有 | A1 主入口；A2～A6 分责迁出 | `role-topology.ts` 默认 `roles=[anchor,executor]`、plan `anchor-host/loadExecutor=true`；`command.ts:runServerProcess`、`setupAssemblyUnits`、`createServerContext`、`runServer`；`channels.ts` 动态装入 Feishu |
| Anchor-only Host（持久拓扑） | 与上一行相同的 `serve` 三种启动模式；设备角色投影只有 `anchor` | 同一 `topology-command` → `anchor-role` → `command.ts:runServerProcess`，但不加载 executor module | 与 Anchor Host 相同的权威、Server/RPC、调度、Channel、投递和领域装配，不创建本地 Executor cleanup owner/runtime factory | 每进程唯一 ApplicationHost；Topology 只决定是否选择 Executor adapter | A1；A2～A6 | `role-topology.ts:planServeTopology` 返回 `anchor-host/loadExecutor=false/activeCleanupOwners=[anchor-host]`；`command.ts` 对 `executor?` 的条件装配 |
| Executor-only Host（持久拓扑） | 与上一行相同的 `serve` 三种启动模式；设备角色投影只有 `executor` | `topology-command.ts:runServeCommand` → `executor-role.ts:run` → `executor-role-runtime.ts:runExecutorRole`，并动态加载 `@zhixing/executor` | 创建本机 Server/RPC、AuthorityRuntime、ExecutorRuntimeSubstrate、job/local-conversation owner、workspace host、data-plane 与 MeshRuntimeAssembly；明确不加载 Anchor owner/listener/global authority | 每进程唯一 ApplicationHost + Executor/Topology adapters；Conversation/Job 等事实仍归领域与 Correctness 合同 | A1、A4、A5、A6 | `role-topology.ts` 返回 `executor-host/loadExecutor=true`；`executor-role.ts` 注释与动态 import；`executor-role-runtime.ts:runExecutorRole`、`createServerContext`、`runServer` |
| 恢复根建立期临时拓扑（Host 同一 OS 进程的串行前置阶段，不是并发第二 Host） | 任一 `serve` 形态在 trusted-home 尚无 recovery root 时进入 | `topology-command.ts` **await** `runRecoveryRootEstablishmentTopology` → `RecoveryRootEstablishmentRuntime`，完成后 stop 原 bootstrap，再重新 prepare 并进入上述唯一 role host | 临时创建 `MeshServiceRegistry`、`ProductionMeshControlPlane` 与 recovery checkpoint receiver，只开放 `mesh.endpoint` / `recovery.checkpoint`；激活并等待 issuer 断开后停止 | ApplicationHost bootstrap/replace 阶段 + Recovery/Topology Infrastructure，不拥有常驻产品领域 | A1、A6 | `topology-command.ts:73-108` 的串行 await/stop/reprepare；`recovery-root-establishment-runtime.ts` 的 start/wait/stop 与 service exact-set |

**启动形态而非新角色。** `foreground`、`on-demand` 和 `managed` 是上述三种持久 Host 图的进程启动模式：`self-exec.ts` 以 `ZHIXING_DAEMON_CHILD` 区分按需 child，`daemon.ts:spawnDaemon` 重入同一 entry 的 `serve`，`managed-service.ts` 固定生成 `serve --managed --managed-home ... --managed-secret-backend ...`。它们改变 stdio、ready marker 和 OS 托管/换代责任，不改变 role plan 或组合根；A0-02 必须分别冻结其 replace/drain/close 事实。

**反向排除与完整性检查。** 下列候选均已找到生产消费者或启动方式，但不增加上述集合：

| 候选 | 分类与排除证据 |
|---|---|
| `@zhixing/server`、`@zhixing/executor`、`@zhixing/channel-feishu` 及其 `src/index.ts` / package exports | 均无 package bin、无 main guard；Server 分别由 Anchor/Executor root 调用 `createServerContext/runServer`，Executor 由 topology 动态加载，Feishu 由 Anchor 的 `channels.ts` 接入面动态加载，都是库/组件而非独立生产进程 |
| `roles=[]` 或仅 `surface` 后执行 `serve` | `planServeTopology` 精确返回 `host=disabled` 且 `runConfiguredServeTopology` 直接返回，不创建 Host；Surface 的真实运行形态是 REPL/瞬态 CLI，而不是一个空 `serve` 进程角色 |
| 按需 daemon 与 Windows/Linux/macOS managed-service helper | 都以同一 CLI entry + `serve` 重启正式 Host；安装/查询/停止服务的 PowerShell/systemctl/launchctl child 只是 OS 管理效果，不拥有知行依赖图 |
| Windows `checkpoint_child_bridge.exe` / 非 Windows native addon | `mesh/checkpoint-child-bridge.ts` 只暴露有界目录 I/O bridge；Windows child 按请求拉起、没有配置/身份/协议/Host 生命周期，native addon 也只是同一 Infrastructure adapter |
| Bash、ripgrep、clipboard、credential helper、evidence executable、`taskkill` 等 child process | `node:child_process` 生产引用反查后均为工具或系统效果子进程；不读取产品启动配置、不承载公开入口、不创建 Server/owner，因此不是知行进程角色或组合根 |
| `scripts/*`、`playground/chat.ts`、package build/pretest、测试 fixture/mock root | 根 package 明确把 playground/build/test 作为开发脚本；无发布 bin，测试路径不在 CLI entry 的生产 import/dispatch 链中 |
| `help` / `--help` / `--version`、未知命令 gate | 由 Commander 或命令 gate 在 CLI 入口内直接呈现/拒绝，不创建生产依赖图；是同一入口的元数据终止路径，不是组合根 |

完整性结论：正向从 root/package manifests、唯一 bin、CLI main guard 与全部 action 分发得到上述入口和形态；反向从 `role-topology` 的 `disabled/anchor-host/executor-host` exact-set、两类 host module、恢复根串行阶段、所有无 Host 的命令期装配以及全仓生产 `child_process` 引用逐项归类。没有发现第二个 package bin、第二个 production main guard、未分类的 role plan、独立 Server/Channel executable 或能绕过 `topology-command` 启动常驻 Host 的入口。A0-01 因而闭合角色/入口/组合根集合；生命周期尚未取证，A0 第一行和 A0 本身继续保持 `[ ]`。

#### A0-02 持久 Host 家族与恢复根前置阶段生命周期

证据 ID：`A0-02-host-lifecycle-v1`。本基线中的 Host 是一个 OS 进程内由 role plan 选中的唯一常驻产品图；`anchor-host` / `anchor-local-executor` / `standalone-server` 等 cleanup owner 只是注册权限标签，不是额外 Host。生命周期当前分散在 OS 启动管理、CLI topology 选择、role 组合根、Server 端点、耐久停机协调与 Infrastructure 释放中；下表冻结这些当前事实，不把它们预先命名为目标 `ApplicationHost`。

**六阶段共同事实与责任。**

| 阶段 | 当前唯一责任者与持有物 | 前置不变量与成功可观察点 | 失败、取消、响应丢失或重入的当前终态 | A1 迁移单元与保护不变量 | 代表性生产证据 |
|---|---|---|---|---|---|
| `create` | `topology-command.ts:runServeCommand` 唯一读取 home、冻结 startup config/credential generation、SecretStore、capacity、Mesh bootstrap 和本地 workspace owner；`role-topology.ts` 唯一选中 Anchor 或 Executor root。两类 root 都在 owner/runtime 产生效果前 `bindServer` 占有最终 home endpoint，此时只回 503。 | startup check 必须 `ready`，当前角色必须经 trust/config 授权，未完成的本机 lifecycle 操作必须先投影为 admission closed。成功点是最终 host/port 已由当前 PID 原子监听但 REST/RPC/WS 未激活，且已取得资源都有 rollback/outer-finally 责任者。 | 配置/秘密未 ready 时不创建 Mesh/role 图；端口冲突由 OS `listen` fail closed。Anchor 的 `StartupRollback` 逆序执行、尝试全部项并聚合失败；Executor root 进入自身 finally 式清理并聚合原始/清理错误。topology 外层始终 stop bootstrap maintenance 并 release workspace owner。 | A1 先形成每进程唯一 create owner 和最小只读投影；保留“最终端点先于 producer 效果仲裁单例、未激活只回 503”。 | `topology-command.ts:39-129`；`role-topology.ts:planServeTopology/runConfiguredServeTopology`；`command.ts:657-737`；`executor-role-runtime.ts:92-217`；`server.ts:BoundZhixingServer` |
| `start` | Anchor root 的 `setupAssemblyUnits(pre-server)` 按固定顺序启动 MCP → Authority → 可选 Executor data plane → Conversation/local owner → job owner/asset → Mesh → lossless data plane → job-owner admission → Channel/Delivery，随后启动 checkpoint owner、Workscene recovery、Advancement recovery 与 Scheduler。Executor root 由 `runExecutorRole` 单点顺序启动 MCP、Authority、workspace host、data plane、Mesh transport + job owner、local conversation owner；`ExecutorJobOwnerLifecycle` 明确 transport-first/owner-second 并自带失败回滚。 | 每个单元只能在依赖就绪后 start；旧 host-stop 还未证明 terminal 时，Mesh/owner/Delivery/Scheduler 以 admission closed 启动且不重放冻结工作。成功点是全部选中 owner 已恢复且 `HostStopCoordinator.resumeActive` 允许进入 open。 | Anchor 在 `StartupRollback.commit` 前任何 start 失败都由同一幂等 cleanup handle 逆序补偿；Executor 各 start 失败后仍逐层关闭，job lifecycle 同时失败则 `AggregateError`。响应丢失后，耐久 stop journal 只从已提交 phase 继续，不重做已完成效果。 | A1 把当前顺序收为类型化 component contributions，保留依赖先行、owner 恢复先于入口、同一清理句柄在 startup/normal close 之间只有一个 owner。 | `access-surfaces.ts:createAssemblyUnits`；`access-surface.ts:setupAssemblyUnits`；`command.ts:836-1246,1517-1611`；`executor-role-runtime.ts:218-729`；`executor-job-owner.ts:ExecutorJobOwnerLifecycle` |
| `open` | `@zhixing/server` 的 `BoundZhixingServer.activate/startServer/runServer` 唯一把 503 endpoint 切换成 REST/RPC/WS，然后获取 PID discovery lock 并绑定 shutdown。role root 只在其后发布 `ServerStateFile ready/running`；background 才发布 `.ready`。 | 最终 endpoint 已占有、ServerContext 已建立，旧 host 耐久停机已恢复/释放 admission。对外可观察成功为健康 HTTP 200 + PID lock；background 还要 `.ready`，只有三者齐备 daemon parent 才报 ready。 | `startServer` 激活或 PID 写入失败会主动 close endpoint。Anchor 当前在 `runServer` 激活后才 activate Delivery/Scheduler、装 post-server bridge、注册完整 cleanup 并 commit rollback；因而“公开 handler 已开放、但 Host 尚未 ready/尚未被完整 cleanup 接管”是真实窗口，不得写成已有的 entry-last 不变量。Executor root 则在 owner/startup-stop 恢复后才调 `runServer`。 | A1 必须把 open 收口到“全部必要组件已 start 且正常 close 责任已接管”之后，同时保留 inactive 503、端点单例、PID/ready/health 可观察语义。 | `server.ts:104-206,219-365`；`lifecycle.ts:runServer`；`command.ts:2174-2388`；`executor-role-runtime.ts:730-793`；`server-state.ts`；`daemon.ts:startupHandshake` |
| `replace` | 图内换代的共同裁决是 endpoint generation + 耐久 lifecycle journal；启动方式的换代责任见下表。Anchor root 还以 frozen admission identity 在 open 前二次校验 trust/config，Mesh trust 变化时调 `coordinateManagedHostTrustTransition`；新 Host 只有在 exact old endpoint/process/service 已独立读回 stopped 后才把旧 stop journal 终结并释放冻结工作。 | 新图必须与当前 trust/config/spec identity 完全一致，且最终 endpoint 只能有一个 OS owner。成功点是旧 Host terminal 已被 endpoint/process 或 exact managed service 回读证明，新图才 resume admission。 | 无法证明旧 Host 已停时新图保持全 owner gate closed；启动中 plan/spec 变化则 Anchor 拒新、reconcile 并拒绝 open。并发 managed reconcile 合并为每 home 一个 dirty loop。Executor-only 当前没有 Anchor 的 `onTrustApplied` / admission revalidation；除 device-removal 外，运行中 trust/role generation 变化不由其 root 触发换代，这是 A1 必须收口的现状缺口。 | A1 建立一个 Host generation/replace owner，不改 trust/device 产品事实；保留旧图证明 terminal 前零新工作、无并发双图窗口。 | `managed-service-runtime.ts:coordinateManagedHostTrustTransition/capture/verifyManagedHostAdmission`；`topology-command.ts:waitForManagedHostTurn`；`host-stop-lifecycle.ts:resumeActive`；`command.ts:784-789,1500-1611,2133-2182`；`executor-role-runtime.ts:620-729` |
| `drain` | `HostStopCoordinator` 是 RPC/信号/设备退役中的耐久拒新与 accepted-work 排空 owner：`accepted → gate-closed → work-settled → flushed → ready-to-stop`。它冻结 10 类 owner exact-set 到 artifact，按 strategy settle，逐 owner read-back，checkpoint 耐久事实并等待物理协调点。Anchor 绑定 conversation/inbound/job/channel/scheduler/delivery/lease/permit；Executor-only 绑定 local conversation + job，其余 owner 是明确空集。 | 先 close admission 再冻结；当前观察到的工作必须是 frozen generation 的子集；所有 read-back 通过且耐久到 `ready-to-stop` 后才允许进程清理。 | 同 request/operation 重放从 journal phase 继续；flush 失败保持 gate closed；响应丢失不重做已记录 effect。RPC `server.shutdown` 和 Server 信号路径先 prepare 再清理。但 Anchor 的 managed-role-change 与 on-demand idle 当前直接 refuse/requestShutdown，不经耐久 prepare；它们依赖后续 cleanup 的 abort/drain。这是已证实的另一 A1 收口点。 | A1 将所有已接受工作的正常 Host 停止入口统一到 refuse/freeze/settle/read-back/flush，不弱化现有 strategy、耐久重驱和响应丢失语义。 | `host-stop-lifecycle.ts`；`server/rpc/methods/server.ts:buildServerShutdownMethod`；`server/lifecycle.ts:prepareSignalShutdown`；`command.ts:1280-1585,2133-2147,2390-2424`；`executor-role-runtime.ts:536-729,794-799` |
| `close` | Anchor 由一个 `CleanupRegistry` LIFO 链负责：先停 idle/evidence/inbound/recovery 与在途执行，再关 scheduler、owner/data-plane/Mesh、post-server bridge、Authority/workspace/conversation、Channel/Delivery/MCP，然后 Server HTTP/WS，最后 state/ready/PID；外层 topology 再释放 bootstrap maintenance/workspace lease。Executor-only 不注入 Anchor registry：它先 shutdown Server 与标记 stopping，再顺序 close local conversation、evidence、workspace、job owner + Mesh、data plane、Authority maintenance、MCP 和 state files。 | 正常停止应已到 `ready-to-stop`；startup failure 只要有一个已取得资源就必须有 rollback/cleanup owner。成功点是 listener、timer、connection/subprocess、owner、maintenance、state/ready/PID 和 topology lease 均释放。 | `CleanupRegistry.runAll` 幂等、LIFO 且单项失败不阻断其他项，但只记录不向调用者重抛；Executor-only 逐段继续并最后聚合清理失败。当前 Executor-only 的 RPC `server.shutdown` 会让内部 `runServer` registry 关 Server/release lock，但不会 resolve 外层 `waitForRoleShutdown` 的 promise，因而 owner/workspace/data-plane 会留在进程中；信号和 device-removal 才进入外层完整 close。 | A1 统一“停止请求完成 → 唯一 Host close → 零残留”，保留尝试全部清理、逆序依赖和幂等；必须为 Executor-only RPC 路径补上外层 Host 终止信号。 | `cleanup-registry.ts`；`startup-rollback.ts`；`shutdown-chain.ts`；`command.ts:2183-2435`；`executor-role-runtime.ts:730-873,1017-1044`；`executor-job-owner.ts:ExecutorJobOwnerLifecycle.close`；`topology-command.ts:127-129` |

**三种拓扑的条件差异。**

| 拓扑 | 图与开口 | replace / drain | close 与明确不适用 |
|---|---|---|---|
| Anchor + local Executor | 一次 `runServerProcess`、一个 endpoint、一个 registry、一个 runner；`activeCleanupOwners=[anchor-host,anchor-local-executor]` 仅允许同一栈注册条件组件。 | Anchor 的 trust/admission 换代、HostStop 和 device/anchor lifecycle 统筹全图；local Executor 工作作为 accepted-work owner 被冻结/排空。 | 同一 registry 中的 `anchor-local-executor` 项先于其依赖释放；不存在第二 Host 或第二 shutdown runner。 |
| Anchor-only | 与上一行相同的 Anchor root/Server/owner/Channel/Scheduler，但 `loadExecutor=false`、`activeCleanupOwners=[anchor-host]`。 | HostStop exact-set 仍完整，Executor/job/local-workspace 条件端口为空/未装配；Anchor trust/replace 路径不变。 | 不注册 `anchor-local-executor` cleanup；Executor data-plane/job/local-workspace 的 start/drain/close 明确不适用，不得为对称而虚构。 |
| Executor-only | 独立 `runExecutorRole` 的本机会话 Server，只启动 Authority(executor)、workspace、data-plane、job/local-conversation owner 和 Mesh；Anchor global owner、Scheduler、Channel/Delivery 不适用。 | HostStop 只有 local conversation/job 非空 owner；device-removal 会排空并唤醒外层 close。Anchor 的 trust-change replace 未装配，是当前缺口而非“不需要换代”。 | 使用独立 Server registry + root 手工聚合 cleanup，未形成第二常驻图；RPC 停机未打通 root close 的缺口已反绑 A1。 |

**三种启动模式的差异与换代责任。** role plan 与 Host 图不由模式改写；是否自动选中某模式由 `resolveHostLaunchPlan` 决定（未建立 trust 的单机默认 Anchor + Executor 为 on-demand；trusted-home 的 Anchor 为 managed；Executor-only 依 `executorAutoStart` 为 managed/on-demand；显式 `serve` 仍可形成 foreground）。

| 模式 | OS/入口责任 | ready 语义 | replace 与 close | 失败/残留裁决 |
|---|---|---|---|---|
| `foreground` | 当前终端直接 await 同一 role root；无父 daemon、无 OS 自动重启。 | 发布 state/PID/health，不发布 `.ready`；终端横幅为人类成功点。 | SIGINT/SIGTERM 负责触发停机；trust identity 换代若导致 Anchor 自停，没有自动 successor，由用户/上层再启。 | 启动失败直接返回错误；正常 close 后无后台父进程责任。 |
| `on-demand` | REPL/CoreHostConnection 的 `spawnDaemon` 以同一 Node + CLI entry 生成 detached child，父进程只持有日志 fd 到 spawn 完成立即关闭。 | 父进程必须同时观察 live PID、`.ready`、health 200；超时但 child 未证明死亡只返回 `starting`，不杀 child。 | Anchor root 有 60s idle reaper，无 RPC/渠道/用户任务时直接 requestShutdown；下次客户端请求是 successor 触发者。Executor-only 当前没有 idle reaper，会持续运行到信号/device-removal，应作为 A1 现状缺口处理。 | self-exec 不支持或 child 已退出才报 `failed`；handshake 超时属未决的响应丢失窗口，稍后 discovery 可重用仍活的 Host。 |
| `managed` | `reconcileManagedService` 每 home 串行 inspect/install/enable/start，OS service 使用同一 CLI entry + `serve --managed`；child 进场先 `waitForManagedHostTurn`，旧健康 Host 尚在时不创建 role 图。 | 与 on-demand 一样发布 `.ready`；无交互输出。 | plan/spec 不再 managed 时 disable future start；旧 Host 退出后 OS 才能启动 successor。Anchor trust generation 变化会 refuse + shutdown；新 child 仍要重读 plan、等旧 endpoint 消失并再 reconcile。 | 并发 reconcile 合并为 dirty loop；plan 在 install 后改变会精确 disable 旧 future definition。若 discovery 认为旧 Host 已离开但旧 endpoint 仍占用，新 Host 由 `listen` 原子失败，不存在两个已开口图。 |

本闭包内真实 replace 触发集合为：启动前 admission/spec generation 二次校验；managed service definition/launch-plan 换代；恢复中的 old-host stop operation；planned anchor transfer 或 trust apply 导致的 Host generation 变化；本机 device removal / anchor uninstall 完成后的进程终止；以及下述 recovery-root bootstrap → 常驻 Host 串行替换。前三类由 endpoint/service/journal 仲裁，设备与值班变更仍由其既有领域 lifecycle 决定产品事实，Host 只消费“应停止/应重建”的结果。当前不存在热替换单个 owner/runtime 后继续沿用同一 Host generation 的生产路径。

**恢复根建立是常驻 Host 之前的串行 bootstrap/replace，不是第二 Host。** `runServeCommand` 只在 trusted-home 同时缺少两个 recovery public key 时 **await** `runRecoveryRootEstablishmentTopology`。该 runtime 的 create 只建 `MeshServiceRegistry`/checkpoint receiver/`ProductionMeshControlPlane`；start 只开 `mesh.endpoint` 与 `recovery.checkpoint`；open 明确不包含 Server/RPC/owner/Scheduler/Channel 业务入口；replace 必须先观察 recovery activation 且 issuer 断开；drain 对产品 accepted work **不适用**，只等 checkpoint 提交/连接断开；close 在 `finally` 中 stop control plane、dispose receiver 和信号 handler。随后 topology 明确 stop 旧 bootstrap storage maintenance、重新 prepare Mesh 并双 key 回读通过后，才获取 workspace owner 并调用唯一 role Host。信号/取消会先 stop 临时 runtime，之后 reprepare 因双 key 未成立而 fail closed；启动失败或重入 `start/stop` 不会绕过 outer `finally`。因此恢复根图与常驻 Host 之间没有并发双图窗口；A1 只应将它纳入 Host 的有界前置阶段，A6 仍拥有其 Mesh/Storage Infrastructure 实现。证据：`topology-command.ts:73-108,127-129`、`recovery-root-establishment-runtime.ts`、`ROOT_ESTABLISHMENT_SERVICE_EXACT_SET`。

**高风险反例裁决与 A1 安全输入。**

1. Anchor + local Executor 只有一个总生命周期：单个 root/endpoint/registry/runner 是正向证据，CleanupRegistry `activeOwners` 对注册时标签的 exact-set 验证是反向证据；未发现第二 Host。
2. Executor-only 的 Authority/workspace/data-plane/job/local-conversation/Mesh/Server 都有 start 与失败清理，未遗漏图组件；但其 root 与内部 `runServer` 是两层停止等待，RPC 停机未唤醒外层、trust-change 未触发 root replace、on-demand 未装 idle close。这三项都已由生产路径确证，A1 不得把它们当成“无差异”保留。
3. 恢复根旧 bootstrap 必须在 reprepare 前 stop，role root 又在 reprepare 与双 key 回读后才可达；静态调用图反证了并发双图。
4. A1 的最小实施顺序因而是：先接管 topology 外层冻结/释放与现有 startup handles，再分别把 Anchor/Executor 组件转为有类型的 start/close contributions，随后收紧 open 门和统一 Host 停止/换代信号，最后删除旧 root 的平行等待/清理权。全程保留端点原子单例、旧 Host terminal 前拒新、耐久 accepted-work 重驱、逆序尽力清理、background 三重 ready 和 topology outer lease 零残留。本项只给出责任单元与退场顺序，不预设目标文件结构。

#### A0-03 REPL Surface 与命令期临时装配生命周期

证据 ID：`A0-03-surface-command-lifecycle-v1`。本节以 `packages/cli/src/index.ts` 唯一 main guard 和真实 Commander action 为正向入口，以 `CoreHostConnection`、本地 owner/transport、Mesh control-plane、checkpoint target、OS helper/child 和全部 `finally/close` 为反向资源集合。它只冻结非持久进程图的当前事实；被 Surface 按需拉起或经 managed reconcile 启动的常驻 Host 仍只引用 `A0-02-host-lifecycle-v1`，不把 daemon/OS service 的退出责任误归给命令进程。

**非 `serve` action exact-set 与唯一分类。** `index.ts` 当前共有 34 个 Commander `.action`；剔除唯一持久 `serve` action 后，余下 33 个全部落入下表。`app` / `device` / `duty` / `backup` / `backup root` / `workspace` 仅是无 action 的命令分组，不增加运行形态；`--help`、`--version`、参数解析失败和 unknown-command gate 是 Commander/main guard 的零装配终止路径。

| action 集合（共 33） | 数量 | 真实组合根 / 图分类 | 生命周期边界 |
|---|---:|---|---|
| 默认 action | 1 | `startRepl`：长运行终端 Surface + `CoreHostConnection` | REPL session 自有 screen/input/render/RPC 投影；其发现/拉起的 Host 属 A0-02 |
| `status`、`doctor`、`serve logs` | 3 | 本地状态/配置/日志与有界 OS/HTTP 检查 | 不创建产品 owner；`logs --tail` 只是当前命令持续轮询 |
| `stop`、`app remove` | 2 | 精确 Host-stop RPC；`stop --maintenance` / app remove 另包一层 managed-service prepare/commit/rollback | Host accepted-work drain 属 A0-02；命令只拥有 RPC、OS helper 和本地投影清理 |
| `device list/remove/continue/status`、`duty targets/migrate/continue/cancel` | 8 | 每次创建一条 `CoreHostConnection + RpcManagementFacade`；`device remove --current` 走同构的 anchor-uninstall facade | 连接期资源归命令；设备移除/值班迁移的耐久状态机归 Device/Trust 领域 |
| `pair` | 1 | issuer 或 joiner 的 Secret/Bootstrap/Pairing listener/socket 临时图 | 不加载 Agent Runtime；提交后的 managed reconcile 只是有界 OS 效果 |
| `backup setup/verify/status/recover/recover-finish`、`backup root rotate/invalidate/approve-reset/reset` | 9 | recovery checkpoint/target、必要时临时 Mesh control-plane 和灾难恢复 target | 目标/连接为命令期资源；恢复/根/安装事实归 Recovery/Correctness |
| `workspace status/list/create/create-scene/rename/repath/remove/reset` | 8 | 已有 local host client，或临时 Authority + Executor substrate + workspace owner/host；`create-scene` 外加一条常驻 Host Product API 连接 | 本机 workspace 和 Workscene 是两个提交边界，凭耐久 consumption credential 串联 |
| `help` | 1 | `program.outputHelp` | 零装配立即返回；根 `--help/--version` 与拒绝路径同类但不是 action |

**`create → connect/open → replace → drain → close` 事实矩阵。**

| 运行图 | create 与 connect/open；成功观察点 | replace / drain 或明确不适用 | 失败、响应丢失、重复调用与 close 的真实终态 | AE-001 归属、A1 保护点与代表性证据 |
|---|---|---|---|---|
| REPL Surface | 根 action 先配置诊断、运行 `runStartupCheck(mode=repl)`；`startRepl` 建 screen/input/render、唯一 `CoreHostConnection`、facades、事件投影和 conversation controller。连接优先 discover 本机 endpoint；缺席时 reconcile managed 或 detached `spawnDaemon(serve)`；当前设备不应运行本机 Host 时创建 `CurrentAnchorSurfaceRpcClient` 并临时启动 Mesh control-plane。`controller.start`、欢迎区和 input controller/readline 就绪后才接受用户输入。 | `CoreHostConnection` 以 generation 保留订阅并处理断线、无响应 Host、版本换代；配置 reload 先请求旧 Host `strategy=drain`，再 `reconnect`、刷新本地视图并重挂 observer。远端 current-anchor trust 换代会关闭旧 ingress 并在下一请求选择新 issuer。REPL 本身不排空 Host owner；活动 turn 的 Ctrl+C 先 RPC abort 并等待宿主终态，Surface 再退出。 | 正常退出先停 tail/确认/事件 presenter/controller/input，再最后 `coreHost.dispose`，随后归还 screen/readline；dispose 清订阅、版本 timer、已建立及在途 client，远端 client 同时 stop control-plane 和 bootstrap maintenance。终端模式另有 process-exit reset。真实缺口有两层：`startRepl` 没有覆盖全装配的外层 `try/finally`；且 current-anchor `surfaceClient.connect` 若在被 `CoreHostConnection` 激活前失败，未激活 client 不会被 connection 留存给 dispose。两者都由 index catch 的 `process.exit(1)` 收口，只有终端 exit hook 保证显示复原；OS 进程不会残留，但 A1 不能把它当成可组合关闭合同。 | Surface + Product API binding；A1 应收束一个 session 级 create/open/close owner，并让未激活 current-anchor client 也受失败补偿；同时保留 A0-02 Host 分界、远端 Surface 的有限 Mesh 图和 turn terminal 语义。证据：`index.ts` 默认 action，`startup.ts:runStartupCheck`，`repl.ts:startRepl`，`core-host-connection.ts:discoverOrSpawn/activateSurfaceClient`，`surface-core-host-link.ts`。 |
| 一次性 management Surface（device/duty/current uninstall） | `withManagement` 每次创建 `CoreHostConnection`，ensure 后才创建无连接所有权的 `RpcManagementFacade`；交互选择用短生命周期 readline。连接可能复用/拉起 A0-02 Host，或使用当前 Anchor 的临时 Mesh Surface。RPC 返回领域状态后才呈现成功。 | 命令本身无 replace API，但连接建立期沿 `CoreHostConnection` 的无响应/版本换代规则；无 accepted-work drain。设备移除以 operation/request identity、status/continue/cancel 恢复；值班迁移以 transfer/request identity prepare/continue/cancel；这些是领域恢复，不是 Surface replace。 | 每个 `withManagement` 均在 `finally` dispose connection，readline 每次 question 后 close；已激活或仍在建立且最终返回的 client 会被 dispose。唯一例外与 REPL 同源：current-anchor client 的 `control.start` 若在激活前拒绝，`discoverOrSpawn` 没有 close 该局部 client，命令最终依赖 `process.exit` 回收。设备移除在不可逆决定前失败会重试 cancel；决定分发响应丢失会读回 status 并给出继续命令；值班 prepare-only 与 continue/cancel 显式保留终态。 | Surface + Device/Trust Product API；A1 接管 client/prompt 及未激活 client 的失败补偿，不吞并领域状态机。证据：`device-removal-command.ts`、`duty-migration-command.ts`、`anchor-uninstall-command.ts`、`core-host-connection.ts:discoverOrSpawn/dispose`。 |
| `status` / `doctor` 离线诊断 | 读取 home config、PID/state/ready/backup 投影；`status` 对活 endpoint 发一次 HTTP health，并通过 managed adapter inspect OS service。输出报告即成功。 | replace、accepted-work drain 均不适用；并发 managed spec 变化用最多三次 identity read-back，而不是创建 Host。 | HTTP response 被 resume，OS helper 由 managed adapter 等待 close；helper 有 abort/timeout、超时 kill 与 timer/listener 清理。读取失败投影为稳定诊断，不保留 client/listener。 | Diagnostics Surface + Infrastructure projection；A1 保留 effect-free doctor 边界。证据：`serve/status.ts`、`maintenance/doctor.ts`、`managed-service.ts:runManagedServiceCommand`。 |
| `stop` | 读取并锁定 exact PID generation；managed 实例先冻结 exact service inspection。创建单次 RPC client，connect/auth 后可先读 blockers，再以 PID/port/start identity 派生 request ID 请求 `server.shutdown`。RPC 接纳后轮询 exact lock/process，物理终止并清旧 state/ready 才宣布 stopped。 | Host replace 不适用；drain 由 A0-02 `HostStopCoordinator` 承担。若 endpoint 已换代，本命令返回 nothing-to-stop，绝不停止新实例；拒绝或超时不发送强杀。 | RPC client `finally close`。响应丢失后以 exact lock/process 读回裁决；重复调用在无 lock、旧死进程或 generation 已变时幂等收口。managed helper 仅在定义和 endpoint 未漂移时 stop exact。 | Surface + Host lifecycle binding；A1 保留“请求不等于 terminal”、无强杀和 exact generation。证据：`serve/stop.ts:runStopCommand/defaultRpcShutdown/prepareManagedExactStop`。 |
| `stop --maintenance` / `app remove` | 先用 managed adapter 停用未来自启并读回，再调用同一 stop；maintenance 成功只 commit disabled，app remove 还在 Host 安全停止后注销未来 service definition。 | replace/drain 不另建；Host drain 仍只由 stop RPC。managed definition/launch plan 变化会 fail closed。 | stop 拒绝/失败或后续读回不成立就 rollback 原 future-start；rollback 失败以 AggregateError 暴露。OS PowerShell/systemctl/launchctl child 全部有 timeout/abort/close 边界。 | Application management Surface + OS Infrastructure；A1 把 prepare/commit/rollback 作为一个命令 scope，不能降级为“调用 stop 后退出”。证据：`maintenance/stop.ts`、`maintenance/prepare-uninstall.ts`、`managed-service-runtime.ts`。 |
| `serve logs` | 默认一次读文件；`--tail` 先读尾部，再每 500ms stat，增长时按范围打开文件。首轮输出即 open，持续输出是命令存活点。 | replace、领域 drain 不适用；日志 truncate/delete 只重置 offset。 | 每次 range read 都在 `finally` 关闭 handle；生产没有自己的 signal handler/AbortSignal，Ctrl+C 直接终止 CLI 进程，未形成可嵌入的显式 tail close。无 child、listener 或持久 owner。 | Diagnostics Surface；A1 若统一 Surface scope，应给持续 tail 一个明确取消贡献而不改变轮询语义。证据：`serve/logs.ts:tailLog/defaultReadRange`。 |
| Pair issuer | 解锁 SecretStore、加载/创建设备 key、capacity 和 `FileMeshBootstrapStore`；必要时建立初始 home。写 durable continuation/secret 后才公开邀请；按配置开 TCP listener 和/或 relay candidate，首个 peer 胜出才进入 pairing commit。bootstrap-complete ack、continuation/offer secret 清除及设备配置完成后才宣布完成；之后可 reconcile managed service。 | Host replace/drain 不适用。断线后不是热 replace：`commit-ready` continuation + pairwise secret 让下一次命令重新开 listener/transport 并验证 resume proof；过期且未提交的 continuation 才清除。 | 候选 loser 由 AbortController cancel，等待中的 socket 随 abort destroy；选中 transport 在 `finally destroy`，listener 在 `finally closeServer`，最外层始终 `store.stopStorageMaintenance`。提交前且没有可恢复 commit-ready 时删除 pairwise/offer secret；提交后响应丢失保留可验证 resume 状态。 | Device/Trust Domain + command lifecycle + Mesh/Secret Infrastructure；A1 只接管 listener/socket/maintenance/reconcile effect。证据：`mesh-pair-command.ts:runPairCommand/issuePairing/acceptPairingConnection`。 |
| Pair joiner | 同一 Secret/bootstrap 前置，不创建 listener；按 invitation 依次尝试 direct/relay socket，完成 recovery onboarding、proof、trust/config import、managed reconcile，并回 bootstrap-complete。 | replace/drain 不适用；`proof-ready/bootstrap-ready` continuation 允许下次命令重连 issuer 并校验同一 committed frame。 | socket 始终 destroy，最外层停止 storage maintenance；commit 尚不可能时清 secret/continuation，commit 已可能时保留恢复材料。所有交互 readline 均 finally close。 | 与 issuer 相同，但本地配置/Secret commit 属 Device onboarding 领域，不能被 Host lifecycle 重做。证据：`mesh-pair-command.ts:joinPairing/resumeJoinerPairing/completeJoinerBootstrap`。 |
| Backup / recovery-root 命令 | `openContext` 读取 home/config/Secret、key、trust/issuer 和 backup binding；目录 target 直接 open，paired target 才临时 prepare Mesh 并 start `ProductionMeshControlPlane`。setup/verify/rotate/reset 等在 checkpoint/target 回读或 trust lifecycle commit 后输出成功；status/approve-reset/invalidate 不开 target。 | replace、accepted-work drain 不适用；重复调用由 checkpoint service、root lifecycle 与 binding/status 投影裁决，不能以进程内状态续跑。 | 每个目录 target/paired control 都由局部 `finally close/stop`；control start/wait 失败也 stop。真实残留边界是 `openContext` 创建的 `FileMeshBootstrapStore`/capacity 没有命令级统一 `stopStorageMaintenance`，`status`、`approve-reset`、`invalidate` 同样无外层 close；index action 最终 `process.exit` 终止进程，所以不会留下 OS 进程，但当前没有可复用的显式零 maintenance 合同。 | Recovery Domain + Correctness/Storage/Mesh；A1 必须补齐 command-scope maintenance owner，同时保留 checkpoint 回读和 root 安全状态机。证据：`backup-command.ts:openContext/connectPairedTarget/openTargetBinding` 及各入口 `finally`。 |
| Disaster recovery / finish | recover 建 signal owner，读取 Secret/trust，必要时分别启动 evidence Mesh 和 paired inventory Mesh/目录 target；发现、验证 checkpoint 后由 `DisasterRecoveryTarget` prepare/import/commit，并等待 post-install receipt 才宣布接管。recover-finish 直接从 authority log 读取当前 installation 后 tombstone。 | replace 不适用；drain 是 signed transfer 的 prepare/commit/abort 与 post-install receipt，不是 Host drain。重入先读 current installation，已成为 issuer 时只等待/报告既有 generation。 | SIGINT/SIGTERM 会 abort；任何 prepare/commit 异常都尝试 signed target.abort。最外层移除 signal listeners，并并行 close inventory target/control 与 evidence control。与 backup 相同，context `FileMeshBootstrapStore` maintenance 没有统一 stop，只由一次性进程终止兜底。 | Recovery Domain + Correctness/Storage/Mesh；A1 统一 signal/target/control/maintenance scope，不能改变 signed abort 或安装 receipt。证据：`disaster-recovery-command.ts:runDisasterRecoveryCommand/openRecoveryEvidenceMesh/openInventoryTargets`。 |
| Workspace 已有 owner client | `withLocalWorkspaceFacade` 先读本机 home 并探测 local transport；可达时只创建逐请求 local client/socket，不读 Host Product API、不创建 Authority/Executor。delivery callback 完成后才 confirmDelivered。 | replace/drain 不适用；owner generation 变化通过 transport secret/owner lock fail closed。响应丢失由 outbox pending + consumption credential 重放，命令循环先交付 recovered result 再确认。 | 每个 socket 在响应后 end；命令无额外 owner/lease。业务失败只有在失败呈现成功后才 confirm，呈现失败则保留待交付事实。 | Workspace Domain + local binding；A1 保留“交付确认晚于消费者成功”。证据：`workspace-command.ts:withLocalWorkspaceFacade/useLocalWorkspaceClient`、`local-workspace-owner.ts:callLocalWorkspaceHost`。 |
| Workspace 本地 fallback | 只有 local host 不可达时才读取 startup config/Secret，prepare Mesh、要求 executor role、原子 acquire workspace owner；随后创建 MCP、`ExecutorRuntimeSubstrate`、`setupAuthorityRuntime(enableAnchor=false, enableLocalExecutor=true)` 和 `LocalWorkspaceManagementHost`，transport 发布后执行同一 client 操作。若抢 owner 失败会重探 raced host；不可达则拒绝，不建第二 owner。 | replace 不适用。`LocalWorkspaceManagementHost.close` 先进入 draining、abort retry/attempt、unpublish/close transport，再等待 drain/recovery/mutation tail；已 commit 操作用 outbox retry/degraded 状态收口。 | 外层 `finally` 顺序 close host、run authority startupCleanup、dispose MCP、stop Mesh bootstrap maintenance、release owner lease；start transport 失败也 close/unpublish。该图有完整显式 close，不依赖进程退出。 | Workspace Domain + command ApplicationHost + Executor/Storage Infrastructure；A1 应直接收编这条现成 scope，不改变 owner race 和 durable outbox。证据：`workspace-command.ts:withLocalWorkspaceFacade`、`local-workspace-management-host.ts:start/close`、`local-workspace-bootstrap.ts`。 |
| `workspace create-scene` 双提交路径 | 外层先创建/ensure 一条 `CoreHostConnection + RpcWorksceneFacade`；内层以 workspace local client/fallback 提交 `authorizeForControl`，再用 consumption credential 摘要形成稳定 workscene request ID 调 Product API，最后第二次 local facade 读回 workspace revision。 | 两个领域都无热 replace；远端 Host 连接遵守 `CoreHostConnection` 换代。local owner lock 保证每次 fallback 唯一；两次 facade 在当前无常驻 local owner 时可能顺序装配两次临时图，但第一图已完整 close 后第二图才开始，不存在双 owner。 | 外层 `finally coreHost.dispose`；每次 local facade 各自遵守上述 client/fallback close。scene RPC 响应丢失后以相同 consumption credential request ID 去重，未消费 workspace result 可在下次交互/命令恢复；成功只在 scene 和 workspace revision 都唯一读回后输出。 | Workscene + Workspace 两个 Domain，经 Product API 与耐久交付桥接；A1 只统一两层 scope，不能合并为一个 owner 或改成双写。证据：`workspace-command.ts:runWorkspaceSceneCreateCommand`。 |
| help/version/参数拒绝 | Commander 或 main guard 直接渲染/拒绝，`help` preAction 明确跳过 runtime support；没有配置、Secret、RPC、owner、listener 或 child。 | replace/drain 不适用。 | 同步返回或 exit；无可释放资源。 | 纯 Surface metadata，A1 不应为对称性创建空 Host。证据：`index.ts:preAction/help/isExecutedAsMain/rejectUnknownCommandPath`。 |

**高风险反例与完整性结论。**

1. REPL 进程与它拉起的 Host 始终是两个生命周期：Surface 只持 RPC 或有限 current-anchor Mesh client；detached/managed Host 的 ready、idle、drain 和 close 仍由 A0-02 负责。`CoreHostConnection.dispose` 只断接入面，不停止一个健康 Host；current-anchor client 在激活前 start 失败缺失败补偿，是 Surface scope 的当前缺口，不是第二 Host。
2. Pair issuer listener、direct/relay 候选、joiner socket和 storage maintenance 都有明确 close；managed reconcile 可能启动 A0-02 Host，但只通过有界 OS helper 等待结果，未把常驻 Host 变成 pair 子资源。Backup/DR 则暴露了相反的可迁移缺口：target/control 已关闭，context maintenance 仍依赖进程终止，A1 必须显式接管。
3. Workspace local owner 只有 file-lock 唯一签发者；reachable 与 fallback 是竞争后重探的互斥路径。`create-scene` 的远端 Workscene API 和本机 Workspace Authority 是两个不同事实 owner，稳定 credential/request ID 只桥接提交，不形成双写或第二 local owner。
4. 生产 child-process 反查没有增加组合根：`daemon.ts` 只 detached 重入 A0-02 `serve`；`managed-service.ts` 的 OS helper 有 abort/timeout/close；clipboard helper 有 1.5s timeout；checkpoint bridge、tool/evidence/secret helper 仍是有界 Infrastructure/工具效果。全仓生产 main guard 仍只有 `packages/cli/src/index.ts`，role plan 仍只有 A0-01/A0-02 已冻结集合。
5. A1 的非持久图最小迁移顺序是：先为 REPL 建立覆盖启动失败与正常退出的单一 Surface scope；再让 `CoreHostConnection` 对未激活 current-anchor client 也执行失败补偿，并把持续 tail 和 managed helper 变成显式有界贡献；随后收编 pair、backup/DR、workspace 的既有 close 顺序并补齐 backup/DR maintenance；最后删除 index action 与各组合根中重复的 process-exit 兜底所有权。领域 commit/recovery、A0-02 Host 和产品成功文案均不迁入生命周期对象。

反向漏项结果：从 33 个非 `serve` action、无 action 的分组/option gate、唯一 main guard、`CoreHostConnection` 的 local/on-demand/managed/current-anchor 四条连接分支、workspace owner race、所有生产 `node:child_process` 引用及 A0-01 role plan 回扫，没有发现未分类 action、新组合根、第二 main guard或隐蔽长运行 child。A0-01～A0-03 因而共同闭合“生产角色、入口、组合根与生命周期”第一类基线；已识别的显式 cleanup 缺口是 A1 的迁移输入，不是不明事实。A0 其余六类基线仍未闭合，A0、完成度和最终退出门保持不变。

#### A0-04 CLI、RPC 与 Event 公开合同 exact-set

证据 ID：`A0-04-public-cli-rpc-event-v1`。本节冻结当前用户或第一方 Surface 能协商的三类合同；“公开”以唯一 CLI/REPL dispatcher、生产 `HandlerRegistry` 以及真正跨 Server bridge 的 notification 为准，不把 README 示例、包内 `EventBus`、测试 fixture 或 A0-03 action 数量当作集合来源。下列集合都可由标明的生产描述符重新机械取得；工具/Profile、配置 schema、持久记录族、包导出和制品入口尚未取证，因此 A0 第二类索引继续保持 `[ ]`。

**机械重取入口与差集规则。** CLI 外层由 `packages/cli/src/index.ts` 导出的唯一 Commander `program` 递归读取 `commands/options/registeredArguments`；REPL 由 `repl.ts:startRepl` 对 `DefaultCommandRegistry` 的静态注册与 `SkillCommandSource` 动态注册规则共同决定。RPC 由 `packages/server/src/rpc/methods/index.ts:buildBuiltinRegistry/captureBuiltinRegistryDescriptor` 取得；当前 61 个导出的 `build*Method` 与 61 个 registry builder 调用双向差集为空，`HandlerRegistry.dispatch` 对集合外方法固定 `METHOD_NOT_FOUND`。Event 外层由 Server 的 `connection.notify/broadcast` 发布点、`SESSION_NOTIFICATIONS`、scheduler/confirmation/skill/status bridge 与第一方 `onNotification` 订阅点双向取得；包内事件只有确实经过这些 bridge 才进入公开集合。任何后续变更必须同时重取 producer/registry、第一方 consumer 和下述差集，不能只改手工数量或 golden。

**CLI Commander exact-set。** 发布 bin 别名只有 `zhixing`、`zz`，均进入同一个 root；Commander 命令本身没有 alias。递归描述符共有 40 个节点：34 个有 action 的路径与 6 个只承载子命令的 group。完整路径如下（`[]` 可选、`<>` 必填）：

| 分组 | exact-set | 可见性、option 与当前责任 |
|---|---|---|
| root/运行控制 | 根 action `zhixing`；`help`；`status`；`stop`；`doctor`；group `app` 与 `app remove` | root 公开 `-V, --version`，内部隐藏 `--log`；`stop --maintenance`。根 action 进入 REPL Surface，其他动作分别属于 Host/安装维护/诊断 binding，目标归 A1/A3；所有节点另有 Commander `-h, --help`。 |
| 配对与设备 | `pair [invitation]`；group `device`；`device list`；`device remove [device-name]`；`device continue <device-name>`；`device status <device-name>` | `pair` 的 `--listen`、`--advertise`、`--relay`、`--relay-only`、`--executor-auto-start` 全部隐藏；remove 有 `--permanent/--current/--duty-device/--recovery-backup/--mode/--confirm`，continue 有 `--mode/--confirm`。Surface binding 目标归 A3/A5/A6。 |
| 值班迁移 | group `duty`；`duty targets`；`duty migrate [device-name]`；`duty continue <transfer-id>`；`duty cancel <transfer-id>` | migrate 另有 `--prepare-only`；当前 CLI 只编排/呈现，耐久迁移状态归 Device/Trust 责任链，目标归 A3/A5/A6。 |
| 备份与恢复根 | group `backup`；`backup setup`；`backup verify`；`backup status`；`backup recover`；`backup recover-finish`；group `backup root`；`backup root rotate`；`backup root invalidate`；`backup root approve-reset`；`backup root reset` | setup 有 `--directory/--device`，recover 再有 `--backup`；finish 有 `--confirm-old-device-isolated`；四个 root 动作分别有 `--confirm-save-new-code`、`--confirm-disable`、`--confirm-reset`、`--approval + --confirm-save-new-code`。目标归 A3/A5/A6，checkpoint/签名事实不归 Surface。 |
| workspace/workscene | group `workspace`；`workspace status`；`workspace list`；`workspace create <name> <path>`；`workspace create-scene <name> <path>`；`workspace rename <current-name> <name>`；`workspace repath <name> <path>`；`workspace remove <name>`；`workspace reset` | rename/repath/remove 各有 `--revision`；reset 有 `--generation/--confirm`。本机 workspace fallback 与远端 workscene Product API 是当前两种 binding，不是两个 workscene 事实 owner；目标归 A3/A5。 |
| 托管内部入口 | 隐藏 `serve`；可调用 `serve logs` | serve 的 `--managed` 仅直接请求隐藏 help 时可见，`--managed-home/--managed-secret-backend` 隐藏；logs 有 `--tail/--lines`。这是安装/托管入口而非普通用户命令；仍属于可调用合同并由 A1/A3 保护。 |

README 的 root-help marker 锁定公开顶层 `zhixing/help/status/stop/doctor/app/pair/device/duty/backup/workspace`，未知路径再由 `rejectUnknownCommandPath` 按同一 Commander 树拒绝；`serve` 不在公开顶层 help，但没有第二 dispatcher 或别名旁路。根包的 `pnpm cli` / `pnpm serve` 只是 private 开发脚本，不属于发布命令合同。

REPL slash 静态 exact-set 是 22 个：`/help`、`/status`、`/stop`、`/model`、`/usage`、`/context`、`/tasks`、`/new`、`/clear`、`/resume`、`/advancement`、`/name`、`/compact`、`/work`、`/exit`、`/config`、`/mcp`、`/trust`、`/security`、`/tasklist`、`/task`、`/skills`。`config/mcp/skills` 在无 terminal chrome 时只从 list/help/typeahead 隐藏，手工输入仍落到同一 handler 并返回明确不可用结果，不是退役入口。动态 exact-family 是每个 enabled skill catalog record 对应 `/<skill-id>`；非 skill builtin 优先，撞名 skill 不注册，display name 不同时只作为 registry alias，`skill.changed` 后刷新。skill catalog 成员本身留给 A0-05。当前 `packages/cli/README.md` 表格列 18 项：相对生产 registry 少 `/stop`、`/advancement`、`/mcp`、`/tasklist`、`/task` 五项，同时多列已不在生产 registry 的过时 `/enter` 一项。这是冻结基线中的双向文档差异；生产 registry 是唯一事实，README 不能覆盖它。

**Server/RPC canonical exact-set（61）。** 除 `auth`、`health` 的 `requiresAuth=false` 外，其余 59 个均先要求已认证连接；unknown/retired method fail closed。当前 owner、消费者和目标按同一产品责任分组如下：

| 当前责任 | canonical methods | 第一方 consumer / 目标归属 |
|---|---|---|
| 传输认证与健康（2） | `auth`、`health` | `RpcClient` 必调 auth；CLI 健康检查使用 REST `/api/health`，RPC `health` 无当前仓库内直接 consumer。属于 Infrastructure binding，A3/A6。 |
| Conversation/Advancement（22） | `session.abort`、`session.advancementCancel`、`session.advancementConfirm`、`session.advancementDetail`、`session.advancementRevise`、`session.clear`、`session.compact`、`session.contextBudget`、`session.delete`、`session.history`、`session.list`、`session.new`、`session.rename`、`session.resolve`、`session.resume`、`session.security`、`session.send`、`session.subscribe`、`session.taskList`、`session.taskListUpdate`、`session.unsubscribe`、`session.usage` | CLI `RpcConversationFacade`/controller 与 event presenters 消费除无直接 caller 的 `session.resolve` 外全部方法；Channel 不经 RPC，而经 `InboundRouter/ConversationManager` 进入同一 Conversation owner。目标归 A3/A5，Kernel 交界归 A4。 |
| Workscene（7） | `workscene.create`、`workscene.delete`、`workscene.enter`、`workscene.exit`、`workscene.list`、`workscene.rename`、`workscene.setWorkdir` | `RpcWorksceneFacade` 与 workspace `create-scene` 远端路径；事实 owner 是 workscene directory/owner，不是 handler。目标归 A3/A5。 |
| Scheduler（6） | `schedule.abortRun`、`schedule.create`、`schedule.delete`、`schedule.list`、`schedule.run`、`schedule.update` | `RpcSchedulerFacade` 消费后五个；`schedule.abortRun` 无当前仓库内直接 caller。mutating/run 入口要求稳定 Surface principal 与 durable control principal；目标归 A3/A5。 |
| Host / app 管理（7） | `server.info`、`server.shutdown`、`server.uninstall.preflight`、`server.uninstall.begin`、`server.uninstall.continue`、`server.uninstall.cancel`、`server.uninstall.status` | `CoreHostConnection`、stop 与 `RpcManagementFacade`；uninstall status 无当前直接 caller。shutdown 与全部 uninstall 方法只允许 loopback，目标归 A1/A3。 |
| Device（4） | `device.continue`、`device.list`、`device.remove`、`device.status` | CLI management facade；Device/Trust owner，目标归 A3/A5/A6。 |
| Duty migration（4） | `dutyMigration.cancel`、`dutyMigration.commit`、`dutyMigration.prepare`、`dutyMigration.targets` | CLI management facade；迁移 owner/恢复链，目标归 A3/A5/A6。 |
| Skill（3） | `skill.archive`、`skill.list`、`skill.setState` | CLI management facade/skills UI；Skill owner，目标归 A3/A5。 |
| Trust（2） | `trust.list`、`trust.revoke` | CLI management facade；Trust owner，目标归 A3/A5/A6。 |
| Confirmation（2） | `confirmation.list`、`confirmation.resolve` | `RpcConfirmationBroker`；list 按 observer scope，resolve 要求 origin match，持久/full decision 还要求可信已认证 loopback。Correctness substrate 只持协议/裁决，目标归 A3/A4/A5。 |
| Delivery（1） | `delivery.resolve` | 当前无仓库内直接 RPC caller；要求稳定 Surface principal 与 durable control principal，事实归 Delivery owner，目标归 A3/A5。 |
| LLM host effect（1） | `llm.complete` | CLI management/provider 路径；只允许 authenticated loopback，属于受约束 Infrastructure effect，目标归 A3/A4/A6。 |

静态 consumer 反查在 61 个 method 中命中 56 个；当前没有仓库内直接第一方 client 调用的 exact 差集是 `delivery.resolve`、`health`、`schedule.abortRun`、`server.uninstall.status`、`session.resolve`。它们仍是生产注册、可调用、受同一认证/权限约束的公开方法，不能因“无当前第一方 caller”从基线删除；迁移时需由产品合同裁决其保留或退场。

生产拓扑没有改变方法名和 handler 语义，但改变物理 binding：Anchor Server 与 Executor-only Server 都因 `startServer(opts.registry ?? buildBuiltinRegistry())` 暴露同一 61-method registry；Executor ingress 先把 22 个 session + 2 个 confirmation 方法交本地 Conversation router，再把 current-anchor relay 集合中的其余 29 个方法送当前 duty device，8 个设备本地方法 `auth/health/server.shutdown/server.uninstall.{preflight,begin,continue,cancel,status}` 留给本机 canonical handler。Anchor 的 current-duty router 对同一 53-method relay 集合在 duty 不在本机时转发，否则落回 canonical handler。`disabled` 和纯 Surface 形态没有 Server。当前 `s7-registry` role golden 把 executor-only / executor-surface RPC 集合记为 `[]`，与上述生产默认 registry 直接冲突；这是 A0 的结构基线失败，后续迁移必须修正或改变生产事实后重取，不能把 golden 当作当前 exact-set 证明。

**第一方可观察 Event/notification exact-set（18）。** 公开 outer method exact-set 及其当前 producer/consumer 如下；没有专用 workscene/device notification，它们当前通过 RPC 结果、状态查询和通用 `publish:result` 投影，不得虚构事件合同。

| bridge | notification exact-set | producer、consumer、owner 与目标 |
|---|---|---|
| Conversation/Run（9） | `session.delta`、`session.complete`、`session.final`、`session.status`、`session.assignmentStream`、`session.event`、`session.changed`、`session.activity`、`session.postTurnControlIntent` | Server 从 Conversation/Run/observer 事实和投影发布；CLI facade/controller、run/control presenters 全部有对应订阅。observer/full payload 过滤由 Server bridge 执行；Surface 只呈现或重新查询，不回写为权威。目标归 A3/A5，run projection 边界归 A4。 |
| Confirmation（2） | `confirmation.pending`、`confirmation.resolved` | ConfirmationHub bridge；pending 的完整 request 只发 origin authenticated loopback，resolved 发 observer summary；`RpcConfirmationBroker` 消费。目标归 A3/A4/A5。 |
| Schedule（4） | `schedule.accepted`、`schedule.started`、`schedule.completed`、`schedule.disabled` | Scheduler 内部八类事件仅后五类被 bridge；completed/failed 都投影为 `schedule.completed`，内部 task 过滤，created/updated/deleted 不公开。CLI scheduler facade 消费全部四个。目标归 A3/A5。 |
| Skill（1） | `skill.changed` | skill mutation 成功后广播 authenticated connections；REPL 刷新动态 slash source，目标归 A3/A5。 |
| 长操作状态（2） | `job.status`、`delivery.status` | management access surface / command 生产并可由 `server.info` 声明 finality；当前没有仓库内 `onNotification` subscriber。它们仍是生产可观察 transport contract，目标归 A3/A5。 |

`session.event` 的 outer method 内还有两个必须防漂移的有限 discriminant 集合。Run UI projection exact-set 为 23 个：`agent:run_end`、`agent:run_start`、`context:tokens_snapshot`、`interrupt:fired`、`interrupt:warn`、`lifecycle:hook_failed`、`lifecycle:prompt_rebuilt`、`lifecycle:warning`、`llm:request_start`、`orchestration:node_end`、`orchestration:node_start`、`orchestration:run_end`、`orchestration:run_start`、`orchestration:validation_failed`、`retry:attempt`、`retry:exhausted`、`retry:success`、`security:rule_sedimented`、`security:steward_review`、`segment:emergency_floor`、`segment:new_started`、`segment:transition_failed`、`segment:transition_start`；另有 lifecycle close sentinel `run:closed`，它不是可重放 AgentEvent。Control projection exact-set 为 13 个：`advancement:completed`、`advancement:contract_cancelled`、`advancement:contract_confirmed`、`advancement:contract_draft`、`advancement:contract_failed`、`advancement:exited`、`advancement:proxy_enqueued`、`advancement:proxy_recovered`、`advancement:recovery_failed`、`advancement:review_deferred`、`advancement:run_reviewed`、`lifecycle:warning`、`publish:result`。`session.changed` 的 change exact-set 是 `renamed/deleted/cleared/taskList`。完整 `AgentEventMap`、Scheduler created/updated/deleted、Channel EventMap 和包内 EventBus 都未跨上述 bridge，属于内部事件而非公开合同。

**高风险交界裁决。**

1. CLI 本机路径不是统一 Product API：status/doctor/logs、workspace local fallback 与 `create-scene` 的本地 Authority 装配仍直接调用 Infrastructure/Domain；远端路径走 RPC。request ID、credential/lock 和唯一 owner 保住当前语义，但 binding 重复是 A3 的迁移输入，不能在本包把它说成已纯化。Channel 同样直接使用 `InboundRouter/ConversationManager`，不经 Server RPC；它消费同一对话 owner，却仍保有第二份应用编排。
2. RPC handler 不只是薄协议适配。`session.send` 当前编排 admission、advancement contract/draft/confirmation、perspectives、stream 与 lifecycle warning；device/duty/uninstall handler 也解释业务错误和终态文案。领域事实仍写回 owner/journal，handler 不是第二事实 owner，但其用例编排和产品语言必须在 A3/A5 迁出，不能原样包装成 Product API。
3. notification 是领域事实、Run Envelope 或内部事件的只读投影/传输进度，不是权威写入口。CLI/REPL subscriber 只渲染、刷新或通过显式 RPC 发起下一条命令；未发现 Surface 把收到的 notification 直接写回 owner 的反向链。
4. `ServerContext` 是当前聚合/service-locator 位置，不是合同或领域事实 owner；它把 Conversation、Scheduler、Trust、Skill、Device、Delivery 等能力交给 handler/bridge。A3 必须建立 Product API 后让 Server/Channel/CLI 成为纯 binding，而不是把 `ServerContext` 改名为目标层。
5. 单机与分布式 binding 共用同一 61-method registry、18 个 notification method 及严格认证/observer 语义；差异只在上述 local/current-anchor/canonical 路由和 notification 跨 Mesh 回送。当前 executor role golden 与生产默认注册不一致是已确定的结构失败而非未知；除此之外未发现第二 registry、动态 RPC method、未注册 builder、影子 notification bridge 或 Surface 权威写回。

完整性结论：正向从唯一 Commander root、REPL registry、`buildBuiltinRegistry`、Server bridge 与 notification 发布点取得三个集合；反向从 README/help/typeahead/gate、全部 handler builder、CLI facades/Channel/Mesh、`onNotification`、Event projection 和拓扑 router 逐项分类。CLI 40 个节点、RPC 61 个方法、18 个 outer notification 及三个有限 inner discriminant 集合均无未分类成员；已实现未注册的 RPC builder 差集为空。README 相对 slash registry 少 5 项、多 1 项的双向差集、executor role golden 空集合及 5 个注册但无直接第一方 caller 的方法都已明确登记，不构成集合未知。A0-04 因而闭合本包三类公开合同；A0 第二类仍缺后续 surface/制品集合，A0、完成度和退出门保持不变。

#### A0-05 工具、Runtime/Profile 与配置 schema exact-set

本节形成三个可独立失效和机械重取的证据：`A0-05-tools-v1`、`A0-05-runtime-profiles-v1`、`A0-05-config-schema-v1`。这里的“工具”以真实 `ToolDefinition` 工厂、`createAgentRuntime` 装配和生产 Host 消费为准；“Profile”区分 Agent 角色投影、装配完成后的 `RuntimeExecutionProfile`、LLM role 和 Host assembly profile；“配置 schema”记录当前可持久类型、实际 loader/validator/default/consumer，而不把 TypeScript 接口误写成已有严格运行时 schema。三类事实只冻结现状，以下差异均是后续 A3/A4/A5/A6 的迁移输入，不在 A0 修复。

##### `A0-05-tools-v1`：生产工具集合与组合规则

**机械重取入口。** 纯 builtin 的唯一工厂表是 `packages/tools-builtin/src/factories.ts:BUILTIN_TOOL_FACTORIES`；Profile 派生工具 `Task` 由 `createAgentRuntime` 后置创建；依赖 Scheduler、TaskList、Workscene 或 MCP 的工具由 `packages/runtime-host/src/builtin-extra-tools.ts:createBuiltinExtraToolsAssembly` 创建；Executor-only 仅在 `packages/cli/src/serve/executor-role-runtime.ts` 追加 MCP catalog。固定工具全集是下列 20 个名称，MCP 是唯一动态 `ToolDefinition` 名称族：

| 类别 | canonical exact-set / family | 工厂依赖、启用条件与当前 owner | AE-001 目标与后续检查点 |
|---|---|---|---|
| 纯 builtin（10） | `read`、`write`、`edit`、`glob`、`grep`、`bash`、`load_skill`、`save_skill`、`admit_skill`、`web_fetch` | 前六项无注入；`load_skill` 需 assignment skill loader；`save_skill` 需 saver，并按 main/work mode 缺省；`admit_skill` 同时需要 admission port 与独立 main-LLM 裁判，任一缺失即装配失败；`web_fetch` 读 `config.network.proxy`。`BUILTIN_TOOL_CAPABILITIES` 与工厂键编译期 exact-key，当前 authority write 只有 `save_skill/admit_skill`。 | Kernel 只消费工具合同与效果结果（A4）；文件/进程/网络 adapter、Provider 和 Skill 资产归 Infrastructure/Domain（A5/A6）。 |
| Profile 派生（1） | `Task` | 只有 Profile 明确列名且从当前 base tools 能派生至少一个 child tool 时后置装配；子 Agent 的白名单固定为 `read/glob/grep/web_fetch`，不含 `Task`，因此不能递归派发。 | 子 Agent/编排归 Kernel（A4），资源和 assignment 由 Correctness/Domain 提供。 |
| Host 固定 extra（9） | `schedule`、`task_list`、`workmode_enter`、`workmode_exit`、`workscene_change_approve`、`workscene_list`、`workscene_rename_current`、`workscene_set_workdir_current`、`workscene_clear_workdir_current` | Anchor RuntimeHost 始终装 `schedule/task_list`；main 且 Workscene directory 在场时装 enter/change/list；workscene 恒装 exit，有 directory 时再装 rename/set/clear。`BUILTIN_EXTRA_TOOL_CAPABILITIES` 只登记可写 owner 能力，故 schedule 有 manage/run 两条而只读 `workscene_list` 不在该能力表，不能把表行数当工具 exact-set。 | Schedule/TaskList/Workscene 是领域投影（A5）；RuntimeHost 不再认识专用产品工具，通用注入归 A4/A6。 |
| MCP 动态族 | `mcp__<serverId>__<sanitizedTool>` | 只来自已启用且已连接 server 的同步 catalog；server id 最长 40、不得含 `__`，工具名清洗并限制 64 字符，同 server 冲突追加 `-2/-3`。`readOnlyHint:true` 才按 query/read-only/parallel，其余缺省按 invoke/mutating/fail-closed；结果上限 100K。 | MCP host、transport、命名和调用是 Infrastructure adapter（A6），Kernel 只见中性工具合同。 |
| Skill 动态内容 | **不产生动态工具名** | enabled Skill catalog 被 `load_skill` 与系统提示消费；Skill CLI slash 动态族已在 A0-04 冻结。Skill id 不注册为 Agent `ToolDefinition`，不得与 MCP 动态工具混算。 | Skill 事实与资产归 Skill Domain（A5），Kernel 只消费不可变投影（A4）。 |

`createAgentRuntime` 的装配顺序固定为：逐项解释 `profile.enabledTools`（`Task` 暂跳过；其余必须命中 builtin 工厂，否则 fail-fast）→ 无条件追加调用方 `extraTools` → 由 base tools 建安全/权限注册 → 按 Profile 后置装 `Task`。生产可达矩阵如下；数字只统计固定名称，均可再加当前 MCP catalog：

| 生产运行形态 | Profile builtin/派生 | 固定 extra | 最终固定集合与条件 |
|---|---|---|---|
| Anchor main conversation，有 workspace | main 全集 11 | schedule/task_list + enter/change/list | 16；Workscene directory 是后三项的条件。 |
| Anchor main conversation，无 workspace | `web_fetch/load_skill/save_skill/Task` | 同上 | 9；`admit_skill` 与全部 workspace 文件/进程工具按构造剔除。 |
| Anchor workscene，有 workspace | power 全集 11 | schedule/task_list + exit/rename/set/clear | 17。 |
| Anchor workscene，无 workspace | `web_fetch/load_skill/save_skill/Task` | 同上 | 10；显式 scene identity 仍决定 trust/permission/lifecycle。 |
| Anchor ephemeral / job | main 全集 11 | schedule/task_list | 13；不装 workmode 工具。job 可由已验 assignment 的 `instruction.tools` 缩成任意可用子集，未知名在创建 runtime 前拒绝；`instruction.model` 只覆写该 job 的 main model。 |
| Executor-only conversation | main 或 power 的 11/4 项 | 无固定 extra | 11 或 4；只追加 MCP，不存在 schedule/task_list/workscene 工具。 |
| Executor-only job | main 11 项 | 无固定 extra | 11；同样只允许 assignment 子集并可 job-local 覆写 model，再追加被请求的 MCP。 |
| Task 子 Agent | 从父 base snapshot 过滤 | 无 | 有 workspace 时最多 `read/glob/grep/web_fetch`；无 workspace 时实际只剩 `web_fetch`；不继承 Host extra 或 MCP。 |

**冲突、保留名和反向差集。** `Task` 是大小写敏感的唯一特殊保留名；Profile 中任何其他未知名都拒绝，模型调用未装工具则返回 tool-not-found。生产 fixed extra 与 builtin 无重名，MCP 前缀及同 server 消歧也使当前生产 catalog 无冲突；但公共 `extraTools` 注入口没有统一 duplicate-name 拒绝，core 后续 name map 为 last-write-wins，且冻结执行 Profile 会去重。这是 A4/A6 必须收口的弱边界，不能把“当前生产调用方恰好不冲突”升级成 Kernel 不变量。包导出中的 `createScheduleTool`/`TaskListService` 不是 builtin 工厂成员；测试/历史工具名不在上述组合链也不属于生产集合。CLI README 仍只声称 `read/write/edit/glob/grep/bash/schedule` 开箱可用，既漏掉当前 Profile/Host 工具又把 schedule 混入纯 builtin；它不是 exact-set 来源。

##### `A0-05-runtime-profiles-v1`：Runtime、Profile 与角色投影

| 当前投影 / 分支 | exact-set 与组合入口 | 当前语义 owner、直接消费者及条件 | AE-001 目标与后续检查点 |
|---|---|---|---|
| `mainProfile` | role=`main`；有 workspace 时工具 11 项，无 workspace 时 4 项；display name 来自已解析全局 Agent identity；user-facing/can-spawn 元数据为 true。 | RuntimeHost main、ephemeral、job 与 ExecutorRuntimeSubstrate main/job 消费；默认 Profile 是 `createAgentRuntime` 未传 profile 时的隐式 `mainProfile()`。它只是身份/指令/工具选择投影，不拥有对话、权限或模型事实。 | 作为 Kernel 输入投影迁到 A4；Host 只冻结并传入（A1/A6）。 |
| `powerProfile` | role 仍为 `main`；同样按 workspace 11/4 二分，另带 workscene identity instructions；主 LLM role 由组合根显式选 `power`。 | 只由 RuntimeHost 与 ExecutorRuntimeSubstrate 在 conversation scope 为 workscene 时创建。scene trust、permission context、lifecycle sceneId 来自独立 `worksceneIdentity`，不得由 Profile 名或 workspace 路径反推。 | Workscene Domain 提供不可变投影（A5），Kernel 消费（A4）。 |
| `subAgentProfile` | role=`sub`；工具白名单 4 项、无 `Task`；user-facing/can-spawn 元数据为 false。 | 只由 orchestrator subagent factory 从父 base tool snapshot 过滤；任务正文在独立 user envelope，不写入 Profile。 | Kernel 内部子 Agent 运行合同（A4）。 |
| job-derived Profile | 以 main Profile 为基线，仅按已验 `instruction.tools` 过滤 enabledTools；不是持久 Profile。 | RuntimeHost 与 ExecutorRuntimeSubstrate 各有同构组合；assignment 是工具/model 请求的事实 owner，Profile 只是本次执行投影。 | Job/assignment Domain + Correctness（A5），Kernel 消费冻结投影（A4）。 |
| `RuntimeExecutionProfile` | 字段 exact-set 为 `tools/mcpServers/providerIds`。`createAgentRuntime` 在全部 builtin/extra/MCP/Task 装配和首窗打开后，从最终工具名、`executionMcpServers` 及 `main/light/power` provider id 生成；三组标识均拒绝空值、超过 480 字符及 NUL/CR/LF，去重、canonical 排序并冻结内外数组，公开 getter 每次返回 clone。 | `SessionRuntime.executionProfile()` 经 RuntimeHost adapter 与 Server perspective 原样透传；conversation 赋权前强制存在并再次规范化。Authority 依冻结 environment 派生：有 workspace 三组原样 clone，无 workspace 只移除 `admit_skill/bash/edit/glob/grep/read/write` 七个 workspace 工具，MCP/provider 不变。本机在 `journal.assign` 前对三字段做 canonical equality；远端把 tools/MCP 及由 provider/MCP 降解出的 credential bindings 封入严格 `ExecutionManifest`，执行端重建 runtime 后再验 tools/MCP。它是一次装配的非秘密能力投影，不是 AgentRoleProfile、LLM role、Host Profile 或事实 owner。 | A4 Kernel 是该装配投影的唯一 producer；A5/Correctness 只消费其冻结副本完成 environment 选择、assignment/receipt/recovery，A6 只提供 Provider/MCP/credential/capability 校验。迁移不得让 Authority、Host 或 Executor 各自重建第二份 Profile 事实。 |
| LLM role registry | `main` 必填，`light/power` 可选并 fallback 到 main；默认模板推荐 main=`deepseek/deepseek-v4-pro`、light=`deepseek/deepseek-v4-flash`，power 无推荐。 | Provider resolver 和 Agent runtime 消费；main 是普通对话/质量敏感单发，light 是摘要/净化等后台任务，power 是 workscene 主循环。它不是 AgentRoleProfile。 | Provider adapter/配置 source 归 A6，Kernel 只依赖 main/light/power 端口（A4）。 |
| Host `ServerProfile` | 唯一值 `full`；surface exact-set 为 `mcp/authority-runtime/executor-data-plane/conversation/mesh-control/asset-maintenance/lossless-data-plane/channel/delivery/confirmation-bridge/conversation-recovery`。 | `packages/cli/src/serve/profile.ts` 驱动 Anchor Host assembly；启动方式不选择另一档。它不是 Agent Profile，也不允许用户持久配置选择。 | A1 把 assembly contribution 收入 ApplicationHost；各 surface 归 A3/A5/A6。 |

`AgentRoleProfile` 的字段 exact-set 是 `name/role/instructions/constraints/tone?/enabledTools/capabilities?`；当前真正影响运行的只有身份渲染和 enabledTools，`capabilities.canSpawnSubAgents/userFacing` 明确尚无生产消费者，能否派生仍由 `Task` 是否存在决定。生产 Profile 选择没有用户持久配置、没有第二 registry：Anchor 走 RuntimeHost，Executor-only 走 ExecutorRuntimeSubstrate；二者对 Agent Profile/LLM role 同源，但外部工具集合按拓扑不同。调用方可以通过库 API 显式传自定义 Profile，却没有当前 CLI/Channel/Executor/MCP/Skill 组合根从用户配置产生任意 Profile。

`RuntimeExecutionProfile` 的 Authority 边界不是第二事实源。`prepareConversationAssignment` 先规范化三字段，再按 Authority 已选环境裁剪，按目标 executor readiness 拒绝缺失 tool/MCP；每个 provider id 必须映射到 `provider-<id>` credential binding，MCP 只在对应 binding 存在时追加。赋权结果暂存完整 Profile + 当前 device digest；本机 ledger receipt 重新比对 manifest 的 tools/MCP/credential bindings、当前 readiness 和 device generation。持久/远端边界只保存带 digest 的严格 `ExecutionManifest`：provider id 不另立 wire 字段，而是降解为 sealed credential binding；崩溃恢复从 journal 的 assignment manifest 重驱，`ConversationAssignmentWorker` 重建 runtime 后比对 tools/MCP，不依赖进程内 Profile map。因而恢复事实是 manifest/assignment，不是可变 Profile；完整 Profile 只负责赋权前装配一致性和本机 receipt 前防漂移。

安全、权限与资源事实也不由上述 Profile 拥有：workspace 解析顺序为 runtime workscene override → global config → 交互 cwd（CI 则 none）；scene identity 决定 scene trust/permission，普通 workspace 决定 workspace trust，无根为 global；permission store 可注入复用否则新建；conversation/job 分别取 interactive/scheduler capacity，Task 另取 orchestration capacity。反向符号审计还分类了两个生产 `*Profile` 命名候选：core `ToolExecutionProfile={timeoutMs,maxOutputBytes}` 是 `SecurityPipeline` 内按工具名选择的执行约束表（默认表 `bash/shell/read/glob/grep/write/edit/multiedit`，未知工具回落 60 秒/2 MiB），没有角色、assignment、持久或用户 Profile 选择语义，归 `A0-05-tools-v1` 的安全消费边界；private `MeshServerSecurityProfile={ca,minVersion,maxVersion,honorCipherOrder,secureOptions,sessionIdContext}` 只由 device identity + trusted peers 冻结生成并投影 TLS secure context/credential rotation，归 A6 Mesh Infrastructure，不是 Runtime/Profile registry。`ProfileCapabilities`、各 Profile options/input、manifest 的 `modelProfileRev`、managed `*Profiles` 数组和 PowerShell `-NoProfile` 分别是从属字段、版本号、拓扑分组或进程参数，不增加生产 Profile 合同。由此，AgentRoleProfile 四种生产投影、一个 RuntimeExecutionProfile、LLM 三角色、Host ServerProfile 以及两个同名内部策略候选均已有唯一分类。

##### `A0-05-config-schema-v1`：持久配置、默认与秘密引用边界

**唯一来源与校验现实。** 用户功能配置只读取一个 JSONC 文件：caller 显式 `homeDir` → `ZHIXING_CONFIG_PATH` 精确文件 → `ZHIXING_HOME/config.jsonc` → `~/.zhixing/config.jsonc`；没有 cwd/project 配置层。文件缺失时默认创建模板和 workspace 目录；JSONC 允许注释和 trailing comma。当前 loader 把解析结果直接 cast 为 `ZhixingConfig`，只立即校验 `workspace.root` 是绝对路径；启动语义校验只拒绝顶层 `providers`、旧顶层 `channels` 和 `messaging.<id>.credentials`，另由 Mesh validator 对 `mesh` 做 strict exact-key 校验。因此下表的 TypeScript 形状是“正式可持久字段集合”，**不是**已有的全树 unknown-key/type fail-closed schema；其余错误由各消费者在使用时失败、忽略或降级。

| 顶层键（10，均 optional） | 嵌套 exact-set、缺省与有效性 | 生产消费者 / 条件分支 | 当前责任与 AE-001 目标 |
|---|---|---|---|
| `mesh` | `enabledRoles:[anchor|executor|surface]` 必需且不得重复；`executorAutoStart?:boolean`；`anchorListen?:{bind:{host,port},advertised?:{host,port}[]}`；`relayRegistration?:{host,port}`。本分支 strict reject unknown/undefined，端口 1..65535、advertised 最多 8 且唯一；启用 anchor 必须有 direct 或 relay 可达，非 anchor 不得配置 listener。 | startup/topology/managed/pair/backup/DR；无 trust 时生产默认角色仍为 anchor+executor，trusted-home 必须显式配置并受 trust roles 二次授权。 | Device/Trust/Topology Domain（A5）与 Host/Infrastructure（A1/A6）。 |
| `llm` | `{main:{provider,model,thinking?},light?,power?}`；main 对需要 LLM 的启动必需，light/power 缺省复用 main。`thinking` 联合为 `off/on/effort{string}/budget{number}`，与所选 model control 不相容时运行时忽略并 warning，不发送错误参数。 | Provider role resolver、Agent/Advancement runtime、配置编辑器；job instruction 可仅对该次执行覆写 main model。 | 配置 source/Provider adapter（A6）→ Kernel role ports（A4）。 |
| `messaging` | `Record<id,{type?,options?,defaultTarget?:{to}}>`；条目存在即启用，type 缺省为 id；秘密字段禁止写入。 | Channel setup/rotation/delivery；凭证不全时该 channel warning 后跳过，不阻断 Host。 | Channel binding（A3）+ Delivery/Conversation Domain（A5）+ Secret adapter（A6）。 |
| `mcp` | `{servers?:Record<id,{type?:stdio|http,command?,args?,url?,enabled?,tools?:{include?,exclude?}}>} `；type 缺省 stdio、enabled 缺省 true；非法 server id 或 disabled 条目在 `parseServerSpecs` 被跳过。 | Anchor/Executor MCP hub、probe/接入编辑器；secret map 对 HTTP 原样注入 headers，对 stdio 原样注入 env。当前 `tools.include/exclude` 没有生产读取点。 | MCP/Secret/Network Infrastructure（A6），工具投影进 Kernel（A4）。 |
| `agent` | `{displayName?:string}`；空白/缺省回落 `知行`。 | `createAgentRuntime` 设置 Agent identity，Profile/提示/用户文案读取其投影。 | 产品 identity 配置 source（A6）→ Kernel/Surface 投影（A4/A3）。 |
| `intent` | `{cancelKeywords?:string[]}`；追加而非替换内置取消词，和 confirmation approve/deny 集冲突时 IntentClassifier fail-fast。 | Channel inbound intent classification。 | Conversation/Product API binding（A3/A5）。 |
| `workspace` | `{root:string,protectedPaths?:string[]}`；root 必须绝对；未配置时交互会话回退 cwd，CI/显式 none 无根。 | workspace root 进入环境、安全信任与文件工具；当前 `protectedPaths` 除类型/历史规格外没有生产消费者。 | Workspace Domain/Correctness（A5），路径 adapter（A6）。 |
| `network` | `{proxy?:auto|off|string}`，缺省 auto；auto 读大小写 HTTP(S)_PROXY/NO_PROXY，off 禁用，显式 URL 覆盖环境。 | web_fetch、MCP HTTP hub/probe/查源及本地状态描述。 | Network Infrastructure（A6）。 |
| `advancement` | `{sessionTokenBudget?:number}`；缺省 `20_000_000`，当前无结构/正数校验，原值直接进入 AdvancementController。 | Advancement session token fuse。 | Advancement Domain 配置（A5），配置 source（A6）。 |
| `modelCapabilityOverrides` | `Record<modelId,{optimalMaxTokens?,riskMaxTokens?}>`；按去 vendor 前缀后的末段 lowercase 命中，优先于内置阈值；未知模型缺省 64K/128K。当前无数值/顺序校验。 | Agent segment/attention 及 Advancement runtime 的 main model capability。 | Context/Kernel policy（A4）与配置 source（A6）。 |

**Provider 与 SecretStore 引用边界。** Provider preset registry exact-set 为 `deepseek/minimax/siliconflow/qwen/kimi/glm/openai/anthropic`；配置编辑器当前只曝光 `siliconflow/deepseek`，自定义 provider 仍可由 SecretStore binding 提供 `baseUrl/protocol`。`ROLE_SPECS` exact-set 为 `main/light/power`。公开配置只保存 provider/channel/MCP id 与功能选择；设备本地 `ZhixingCredentials` 进程投影的顶层 exact-set 是 `version?`、`providers?`、`channels?`、`mcp?`，当前唯一 schema 不写 version。Provider binding 包含必需 `apiKey` 及可选 `baseUrl/protocol/quirks/modelOverrides/modelInputCapabilities/models`；Channel/MCP binding 为各 id 的 string map。startup 先解锁 `SecretStorePort`，再通过 provider/channel/MCP 最小投影交给对应装配；API key 没有 env fallback，公开配置 schema 也没有可生效的明文或 `env:VAR` 凭据引用。旧 `providers/channels` 和 `messaging.*.credentials` 会被启动语义校验拒绝，其他未知字段即使因整体非 strict 而被保留也不会成为秘密来源。本包只读取类型、loader 与投影调用链，没有读取任何真实 SecretStore 或秘密值。

**非 schema 组合输入。** `ZHIXING_CONFIG_PATH`/`ZHIXING_HOME` 只选择配置和数据根；`HTTP_PROXY/HTTPS_PROXY/NO_PROXY` 只在 network auto 模式选择 egress；隐藏 `serve --managed-home/--managed-secret-backend` 与 `ZHIXING_SECRET_BACKEND` 只让 OS 托管 Host 固定同一 home 和 SecretStore adapter，且仅 managed 模式接受，不是用户 config schema。配置编辑器 registry 只有 `model/messaging/mcp` 三节，基础 `/config` 只显示 model+messaging，`/mcp` 才注入 runtime catalog/probe；其余字段依赖手工 JSONC 或领域命令。`writeConfig` 声称完整写入，但 `applyConfigPatch` 只复制 llm/agent/intent/workspace/network/mesh/messaging/mcp：它能保留文件中已有的 `advancement/modelCapabilityOverrides`，却不能通过传入值新增或改变二者。这是当前写入口差异，不得在迁移时静默丢字段。

**正反差集与完整性结论。** 正向从三个工具工厂/装配点、三个 Agent Profile factory、`createAgentRuntime` 的 ExecutionProfile producer、一个 Host Profile registry、一个 LLM role registry、`ZhixingConfig`/loader/validator/template/SecretStore projection 得到上述集合；反向从 RuntimeHost、SessionRuntime adapter、Server perspective、ExecutorRuntimeSubstrate、conversation authority/assignment/manifest/recovery worker、subagent、job assignment、CLI/Channel/MCP/Skill/Provider/Advancement/Workspace/Network 全部直接消费者核对。再以全仓生产 `*Profile*` 类型/工厂/registry/consumer 反查并分类 Tool/Mesh 两个内部策略候选，没有注册但不可达的固定工具、生产可达未登记的 Profile 类合同、第二配置文件或用户 Profile schema。已解释的现状差异共八类：README 工具列表不是当前 exact-set；Profile 注释漏写无 workspace 仍有 `save_skill`；Profile capabilities 尚未消费；`extraTools` 无全局重名拒绝；整体 config 非 strict schema；`workspace.protectedPaths` 与 MCP `tools.include/exclude` 未消费；writer 不能新增/改变 `advancement/modelCapabilityOverrides`；`config-loader` 注释仍称下游解析环境变量 API key，而生产 resolver 只读 SecretStore 投影、没有 env fallback。这些差异均已反绑实际入口和 A3/A4/A5/A6，故不构成 A0-05 未知，但任何迁移不得把它们误写成已受保护合同或靠静默兼容继续扩散。A0 第二类仍缺持久记录族、包导出和实际制品，索引保持 `[ ]`。

#### A0-06a 生产持久事实族、派生/临时记录与存储根 exact-set

证据 ID：`A0-06a-persistence-roots-v1`。本证据以 A0-01～A0-05 的生产组合根为正向入口，以生产 `node:fs` 写调用、`File*`/Store/Repository/Journal/Ledger/Spool 构造、root/path helper、maintenance/retention、backup/restore 和删除入口为反向集合。它冻结“什么会落到哪里、谁写、谁恢复、谁删”，不冻结具体 record 字段、格式版本、兼容读取矩阵、cutover 或 migration 状态机；后者全部留给 A0-06b。

##### 根选择与 scope 规则

1. 主数据根唯一原语是 `getZhixingHome()`：`ZHIXING_HOME`，否则 `HOME|USERPROFILE|~` 下的 `.zhixing`。conversation、workscene、permission、skill、server/runtime 和 distributed-runtime 的生产 path helper 均从此根派生；逻辑 id 经 `toSafePathSegment`，不得由迁移代码另拼第二种目录键。
2. 配置/秘密有一个已存在的条件分叉：caller 显式 `homeDir` 优先；否则 `ZHIXING_CONFIG_PATH` 是精确配置文件且其 dirname 成为 `resolveHomeDir` 的 config/SecretStore 根；再否则回到 `getZhixingHome()`。普通启动检查可因此让 config/secret 根与主数据根分离；持久 `serve` 组合根显式传 `getZhixingHome()`，使 Host 的 config、SecretStore 与数据根同址。A6 必须保留该当前行为或在产品裁决后一次性收口，不能静默把两个根混为一谈。
3. 另有三类显式外部 scope：用户选定的 workspace/文件工具目标由 Workspace/Tool 效果 owner 管理，不是知行状态根；目录备份 target 必须是独立文件系统目录，paired target 在对端设备根；Skill admission 使用 OS temp。跨平台 managed-service definition 在 macOS/Linux 位于 OS 用户服务目录而非 `ZHIXING_HOME`。这些都必须通过注入/冻结身份进入 adapter，不能从文件名反推领域 owner。

##### `A0-06a-persistence-roots-v1` 主表

| ID | canonical 物理根 / 参数化模式 | 类别与当前 record family exact-set | creator/writer → reader/recovery | 删除、维护与备份边界 | 当前 owner → AE-001 目标 |
|---|---|---|---|---|---|
| P01 | `<config-home>/config.jsonc`；`<config-home>/secret-vault.{json,key,backend.json}` 及相邻 init/read/write lock、原子 tmp；平台钥匙串中的同一 vault 主密钥；兼容输入 `<config-home>/credentials.json` | 用户配置；SecretStore 加密事实；平台 backend binding/封装密钥。`credentials.json` 只有现行启动迁移 reader、不是当前 credential writer。 | `loadConfig/writeConfig` 与 config editor；`createPlatformSecretStore`/`EncryptedVaultSecretStore`；startup/config/Provider/Channel/MCP/device-key 消费，锁定 backend 后重启重开。 | 配置写入原子替换；SecretStore 自有精确 put/delete/list 与锁；程序/设备生命周期只经 SecretStore API 清理所授权 ref，不把 vault 当普通目录扫。默认 recovery checkpoint 不把 config/secret 变成领域副本。 | 配置 source、SecretStore/OS keyring adapter → Infrastructure（A6）；各 Domain 只消费 secret ref/冻结投影（A5）。 |
| P02 | `<home>/conversations/<cid>/`；`<home>/workscenes/<scene>/conversations/<local-cid>/`，各含 `meta.json`、`transcript/index.json`、`transcript/<six-digit>.jsonl`、`snapshots/<time>.json` | conversation 身份/设置、task-list 与 segment metadata；用户历史原文的 run/clear shard；摘要/attention 恢复快照是派生缓存。普通与 WorkScene conversation 只是 scope 不同，合同同源。 | `ConversationRepository`、`ShardedTranscriptStore`、`SnapshotStore`；Conversation owner 先提交运行事实再投影用户历史，CLI/server history、bootstrap/segment/summary/session restore 读取。 | conversation 删除移除单个完整目录；WorkScene conversation 走唯一 physical cleanup；transcript retention 只删 inactive old shard，snapshot retention 只删非在用旧快照，不能改写原文索引。full recovery checkpoint 的 `conversation-content` 消费该内容，但 checkpoint 是备份，不成为 live owner。 | Conversation Domain 拥有身份、历史与恢复语义（A5）；文件存储/retention adapter → Infrastructure（A6），Host 只编排开启/关闭（A1）。 |
| P03 | `<home>/workscenes/.cleanup/{scenes,conversations}/<digest>.json`；WorkScene 的 live 物理容器仍是 P02 的场景 conversations 路径 | 可重建、可续跑的物理删除 traversal cursor，不是 WorkScene 事实。WorkScene registry/tombstone/activity 的唯一事实位于 P06 Anchor WAL。 | `createWorksceneStorageCleanup` 在已提交删除后按页写 cursor；启动/删除重驱消费，完成即删 cursor。 | scene 删除清整个场景系统目录并回收该 scene 的 conversation cursors；conversation 删除只清目标目录。`getWorkSceneIndexPath()` 与场景 `meta.json` 目前没有生产 writer/reader，不能把路径注释当成第二 registry。 | WorkScene Domain 决定删除（A5）；cleanup cursor/有界 walker → Infrastructure/Correctness Substrate（A6/A4）。 |
| P04 | `<home>/permissions/global.json`、`<home>/permissions/{main|workspace-<hash>|scene-<id>}.json` | 用户 global/context 信任规则事实；session 与 builtin rule 明确只在内存。 | `TrustAdministrationExecutionApplicationService` 唯一决定显式创建、贡献累计/沉淀、规则身份/元数据与作用域，管理应用负责 Query/revoke；最终 repository adapter 只把这些投影映射到 `PermissionStore` 的 create/list/snapshot/revoke 机制。SecurityPipeline 仅消费已绑定 context 的只读 match/matchFrozen 投影，运行状态与签名 assignment snapshot 由同一 Trust 应用读取同一底层事实。 | context/global 仍由既有机制精确重写或删除；session 重启即失效，builtin 只随 runtime 装配。无独立备份 owner。不能把 SecurityPipeline 无注入时的空只读 source 或 `rootDir:null` 测试 fallback 当生产持久根。 | Trust Administration Domain（A5）拥有规则生命周期；Security/签名快照只消费只读执行投影（A4）；PermissionStore 文件机制 adapter → Infrastructure（A6）。 |
| P05 | production 仅有 OS temp `<tmp>/zhixing-skill-admission/candidate-*`；历史 `<home>/skills/{index.json,own/,linked/,archived/,usage/,.staging/}` 若存在也不属于 production root | assignment admission candidate 是可丢临时输入；builtin skills 是源码资产。旧 home Skill 目录没有 production creator、reader、writer、scanner、migration 或 delete owner，是惰性数据而非兼容输入、投影、usage 状态或事实源。 | Skill Catalog admission 应用经 path-free staging 端口取得候选，`SkillAdmissionWorkspace/acquireToStaging` 只在 OS temp 操作；确认后正文入 P06 shared CAS 并 stage assignment `skill-admit`。正式 catalog/usage 只来自 P06 Authority/CAS 与 assignment overlay。 | safe/escalate/error/confirm/过期终态及 TTL sweep 精确清 OS temp；不枚举、不修改也不删除旧 home Skill 目录。备份依 P06 authority+artifact，不把惰性旧目录变成兼容或恢复 source。 | Skill Catalog Domain（A2，A5 登记待 A3 binding 完成）；CAS/assignment correctness（A4）；admission temp 文件 adapter（A6）。 |
| P06 | `<home>/distributed-runtime/artifacts/sha256/<prefix>/<digest>`；`authority/` 与 `executor-authority/` 各有 `authority.log`、按需 `bad-tail/`、`projections/<64hex-directory-digest>/...`、locks/tmp，只有无文件头 legacy WAL 才使用 `authority.log.identity`；`derived/artifact-lifecycle/...` | 共享 CAS；两份不同 owner 的权威 WAL；每个 WAL 下可重建 durable projection；artifact retained-reference/lifecycle 派生索引。projection 物理目录名是 `protocolDigest("DurableProjectionDirectory",1,{projectionId})` 去前缀后的 64 hex，逻辑 id 写在 manifest。Anchor family exact-set：trust/pairing/checkpoint/recovery activation、device lifecycle/credential exposure/control admission/resource、surface grant、conversation/session/run/assignment/transfer/advancement/deferred intent/post-adoption review、schedule/job/notice/delivery、WorkScene/Skill/Rubric/global mutation；Executor family exact-set：assignment/job/run/interaction receipt、data-plane ticket、resource governor、workspace binding/probe 与 executor device lifecycle。 | `setupAuthorityRuntime`/`FileMeshBootstrapStore` 创建；各 Domain owner/participant 只向自己的 stream append，`ConversationRunJournal`、Scheduler/Job/Delivery、global adapters、executor ledger 与启动恢复从 WAL/CAS 重放；projection 缺失可从 WAL 重建。 | WAL 坏尾隔离、projection compaction/rebuild、CAS GC 和 artifact lifecycle maintenance 各自有唯一 owner；禁止把两个 WAL 合并成一个 owner。恢复 checkpoint 读取冻结 prefix、CAS、conversation content 与 execution assets；derived index 不是备份 source。 | 领域状态机归各 Domain（A5）；WAL/CAS/严格 codec、事务、projection、retention → Correctness Substrate（A4），物理 I/O → Infrastructure（A6）。 |
| P07 | `<home>/distributed-runtime/{executor-capability-directory.json,executor-snapshot-version.json,permission-snapshots/<digest>.json,execution-assets.json}`；`workspace-bindings/{root-manifest.json,artifacts/,generations/<generation-storage-key>/directory-established,catalogs/<generation-storage-key>/authority.log...}`；`workspace-probes/probe-log-established`；`capacity/` | 同一根内责任不同：E01～E04 分别是本机发布 lineage、已验 capability registry、不可变 permission 快照与可重建 execution-asset index。workspace root manifest 是 active generation/capability revision 的 v1 根事实；initial generation 的 workspace facts 与 probe facts位于 P06 Executor 主 WAL，successor workspace generation 才有独立 P07 WAL。raw absolute path 直接内联 workspace record，`artifacts/` 当前没有 workspace payload producer；两个 marker 只证明各自 authority stream 的物理建立。`capacity/` 仅是 `statfs` 探测落点，没有 record/schema。 | `setupAuthorityRuntime` 是唯一生产组合入口：装配四类发布记录、`WorkspaceBindingCatalog` 与 `WorkspaceProbeHandler`。本机 workspace 管理写 catalog；capability descriptor、assignment prepare/recovery preflight、WorkScene 与 Advancement 消费 path-free workspace 投影，只有本机 EnvironmentPort 解析 raw path；WorkScene 本地/mesh probe 消费签名 request/result。 | reset 只允许 degraded generation，经 durable reservation、successor genesis/receipt 后原子切 root manifest；旧 generation 保留供 receipt chain，不原地修复或逐代删除。probe retention只向 Executor WAL 追加 retire。正常 Host close只停 recovery/maintenance；current-device removal删除四个发布目标及 workspace/probe/capacity整根，普通 binding remove/reset不删用户目录。P07这些根没有独立 backup/restore consumer。 | Workspace/Device/Skill/Rubric Domain（A5）；签名/high-water/generation switch、严格 replay/receipt/lease equality → Correctness（A4）；本机 registry、WAL/CAS/marker/capacity adapter → Infrastructure（A6）。 |
| P08 | `<home>/distributed-runtime/assignment-streams/assignments/<sha256(assignment-id)>/{identity.json,index/<WAL-family>,frames/sha256/...}` 与 `tombstones/`；`evidence/<executor-id>.jsonl` | 每 assignment 的耐久交互 stream bytes/order/consumer epoch、ACK、prune/reclaim 状态；永久 reclaim tombstone；Executor evidence pending/completed 幂等账本。assignment、ticket 与 Advancement 状态权威仍在 P06，spool/evidence 都不是用户 transcript 或第二业务 owner。 | `ExecutorDataPlaneRuntime` 唯一装配 spool，local/mesh stream consumer 与 interaction recovery 读取；Anchor access surface和 Executor-only Host各为本机 executor创建同一 `EvidenceJournal`，evidence handler按request重放结果。 | spool先耐久 `pruned/deleted/reclaimed`，再写 tombstone 并删 assignment dir；identity缺失可从首个`opened`重建。evidence只compact超过27天的completed，pending永久保留待重驱；两根都没有current-device通用清理入口。 | Execution/Advancement Domain（A5）消费；stream/evidence durability → Correctness（A4）；文件 spool/retention → Infrastructure（A6）。 |
| P09 | `<home>/distributed-runtime/{mesh-endpoints.json,mesh-peers.json,mesh-bootstrap-completions.json,mesh-pairing-continuation.json}`；`surface-asset-temporary/sha256/.../.presence/...`、`surface-asset-partials/`、`mesh-artifact-partials/` | endpoint 与 transport-trust 本机投影、bootstrap idempotency；pairing crash continuation；Surface/mesh 上传的临时 CAS、presence quota marker 与 resumable partial。正式 trust、surface grant/asset refs 与 retained lifecycle 在 P06 WAL/CAS。 | `FileMeshBootstrapStore`、Mesh runtime/pair command、`createSurfaceAssetAuthority` 与两个 resumable receiver 创建；Mesh bootstrap/reconnect、pair resume、asset lifecycle recovery 读取。 | terminal/expired pairing路径精确清 continuation；artifact lifecycle先记presence/partial、digest完成后提升，再按耐久cleanup/retention回收。current-device removal删endpoint/peer/completion与三类asset staging，**不删 pairing continuation**；后者没有通用device-removal owner，不能笼统称“全部pairing staging已删”。 | Device/Trust/Surface Asset Domain（A5/A3）；Mesh/transport/upload adapter（A6）；grant/ref 一致性（A4）。 |
| P10 | `<home>/runtime/local-workspace-operation-outbox/{operations.ndjson,tmp...}` + sibling `.established`；`local-workspace-management.owner.lock`、`local-workspace-management.transport.json`/tmp、Unix `workspace-<home-digest>.sock`（Windows 为 named pipe） | workspace 管理 committed-forward outbox；owner lease、transport secret 与 endpoint 是临时进程协调记录。Workspace binding 事实本身在 P07。 | local workspace command/fallback 与持久 Executor Host竞争唯一owner；IPC listener与transport secret先发布。`host-status/status/list`不读outbox；`pending/acknowledge`只要求outbox-ready；新`prepare`与仍为prepared的commit要求outbox-ready且Host为ready；已有`committed/completed` identity可在recovering（已进入handler时也不以draining为拒绝条件）重放/前滚。client只读transport publication并经同一endpoint调用。 | outbox恢复后`#drainLoop`在recovering前滚最老committed，清空后才把Host置ready；ack以marker-first + log重放收敛compaction。Host close先撤transport publication并关listener，再把已接受执行收束到durable safe point，外层topology最后释放owner lock；crash残留由下一owner的lock/stale与publication清理处理，device removal精确清整个runtime根。 | Workspace Product API/Domain（A3/A5）；outbox correctness（A4）；local IPC/lease/文件 adapter（A6），生命周期归 ApplicationHost（A1）。 |
| P11 | `<home>/distributed-runtime/recovery-backup-targets.json`；paired target `<home>/distributed-runtime/recovery-checkpoints/<checkpoint-id>/{envelope.json,manifest.json,chunk-*}`；incoming `<home>/distributed-runtime/recovery-checkpoint-incoming/<checkpoint-id>/{envelope.json,chunk-*.partial}` 与 root-establishment/lifecycle bindings；用户选择的独立目录 target 同 checkpoint layout | backup target选择配置；完整 authority checkpoint package；paired 接收 staging 与 recovery-root串行 bootstrap/lifecycle binding。P06保存本机checkpoint envelope/chunks与created/replicated/verified/superseded生命周期事实；发布target是冻结备份，不是live Authority。 | backup/DR/pair/recovery-root command、checkpoint owner/service与`FileRecoveryCheckpointTarget`/`FilePairedCheckpointStaging`写；inventory、verify/restore、root establishment/lifecycle和uninstall preflight读。 | target以hidden staging目录写、manifest-last、目录rename线性发布；paired incoming可按chunk进度续传，target发布后rename-retire并精确清除。retention只retire已被新verified checkpoint supersede超过窗口的target package；配置无删除API。device removal只清incoming，不删已发布`recovery-checkpoints`或外部target。 | Recovery/Device Domain（A5）；checkpoint completeness/identity → Correctness（A4）；target/bridge/filesystem adapter → Infrastructure（A6），bootstrap阶段由 ApplicationHost（A1）串行。 |
| P12 | `<home>/distributed-runtime/conversation-transfer-staging/<transfer-id>/{artifacts,partials}`；`anchor-transfer-staging/{candidate-claims,journals/<id>,transfers/<id>/{artifacts,partials,promotion-partials}}`；`disaster-recovery-staging/` 同类 candidate/journal/transfer 私有树 | conversation transfer只以私有CAS/partial接收，控制状态与最终committed base均写P06；planned/DR另有target-wide candidate WAL、每transfer私有WAL/CAS与promotion partial。candidate大对象可落P06共享CAS，但私有WAL引用不使其成为P06业务事实；最终trust/install/owner及DR post-install receipt才由P06 Authority拥有。 | Mesh conversation target、planned source/target、DR target与启动前completion扫描读写；candidate/private journal在响应丢失、崩溃或安装后重启时驱动前滚。 | conversation abort才精确清私有目录；**committed conversation staging当前无通用终态清理owner**。planned/DR install与post-install完成后删`transfers/<id>`并释放readiness，但保留candidate/private journal用于exact replay；unprepared release另删planned journal。current-device removal只整根删DR staging，不删conversation/planned staging；不得由通用retention猜删。 | Conversation/Device/Trust Domain（A5）；transfer atomicity/WAL/receipt → Correctness（A4）；staging/CAS/Mesh adapter → Infrastructure（A6），Host replace/启动前completion顺序归 A1。 |
| P13 | `<home>/server.{pid,port,state,ready,token}`；`<home>/logs/server/server.log` 与 rotation；legacy `<home>/server.log` | 同一物理根下四个不同生命周期的subfamily：pid/port发现辅助；ready/background generation空marker与state生命周期投影；持久本机RPC token；plain-text active/rotated日志。它们均非业务Domain事实。 | listener成功后写pid/port；discovery/stop/status读pid，port文件当前无production reader。state/ready由`ServerStateFile`写，daemon handshake/status/mesh projection读。Anchor与executor-only启动复用或生成token，Server auth与本机client读。on-demand daemon显式把stdout/stderr重定向active log，log lifecycle与`serve logs`治理/读取。 | 正常或exact stale-stop清pid/port/state/ready；crash可残留。token跨Host generation复用，stop/device removal/program uninstall均不删且无production删除owner。active日志跨重启保留并按size/count/age rotate/prune；active已存在时legacy不迁移，只有active缺失且copy-exclusive成功才删除legacy。 | pid/port/ready/state生命周期归ApplicationHost（A1）；token认证与Server binding归A3/A4；文件、兼容读取与retention归Infrastructure（A6）。 |
| P14 | Windows `<home>/distributed-runtime/managed-service/<service-id>.xml`；macOS `~/Library/LaunchAgents/<service-id>.plist`；Linux `~/.config/systemd/user/<service-id>.service`，外加 Task Scheduler/launchd/systemd user registration；完整ID为`dev.zhixing.host.<sha256(osUser + NUL + canonical-home)前24hex>` | canonical definition文件与OS manager registration是两个相邻、非事务的Host launch投影；均由当前config/SecretStore/trust/role plan重算，不是第二产品配置、第二Host或Domain事实。 | 唯一adapter按file→manager安装并read-back；Windows反读strict Task projection，macOS/Linux只读manager enabled/running/absent。manager存在时，缺失/漂移definition或Windows manager投影漂移使`matches:false`并在install前拒绝；manager absent时install进入definition物化：缺文件则原子创建、byte-equal orphan复用，mismatched orphan仍由`writeDefinition`拒绝而不覆盖。 | managed→其他mode只disable future，不删definition。normal exact unregister先删OS registration、再删definition并回读absent；若manager删除后file删除失败，带可重建spec的maintenance/program uninstall与current-device cleanup可按manager absent收敛并遗留文件。无SecretStore时maintenance还要求absent projection `matches:true`，program uninstall更要求definition file absent。 | ApplicationHost（A1）消费；OS service adapter与definition I/O → Infrastructure（A6）。 |
| P15 | `<home>/logs/{llm-raw/llm-raw-<pid>-<time>.log,llm-error/llm-error-<pid>-<time>.log,keypress-<pid>-<time>.log}` | 无schema version的人类诊断文本；raw仅REPL `--log` process singleton，error forensic在Agent run错误时自动写最近一次请求摘要，keypress仅同一诊断开关下记录输入路径。 | CLI render/EventBus与input instrumentation是唯一writer；stderr提示/operator人工诊断是reader。生产反查无Domain、Authority、恢复、backup或产品查询consumer。 | raw/error目录在写入及部分CLI启动时best-effort按mtime各保留最近7个且目录中所有普通文件都参与；keypress位于`logs/`根，当前无retention/delete owner。raw fd与keypress unref polling timer为进程期资源，均不构成业务事实。 | Observability/CLI Infrastructure（A6/A3），不进入Kernel/Domain。 |

##### 跨根唯一事实、删除与排除裁决

1. **Conversation 双根不是双写权威。** P06 conversation/run/assignment WAL 决定 owner commit、恢复与转移；P02 transcript 是用户历史原文、meta 是用户视图/identity，snapshot 是派生上下文。迁移必须保持先提交事实、再投影历史及其响应丢失重驱，不能把 transcript 变成 assignment WAL，也不能因 WAL 存在而删除用户历史。
2. **Anchor 与 Executor 不共用 owner。** 两者只共享 P06 CAS；`authority/` 负责 Anchor 的 global/device/conversation/scheduler/delivery，`executor-authority/` 负责本机 assignment/execution/resource/environment。P07 workspace root manifest 又只选择一个 active generation；initial generation复用 executor WAL，successor 才有独立 catalog WAL。A4/A5 迁移不得并行写旧新 generation或把共享 CAS误判为共享 authority。
3. **WorkScene、惰性旧目录与资产缓存不是第二 Domain。** WorkScene registry、Skill、Rubric 事实都在 P06；P03 cursor 是 cleanup，P05 只含 admission temp，P07 execution snapshot 是不可变 cache。历史 `<home>/skills` 若存在也没有生产 reader/writer/cleanup，不能成为 Skill 兼容 source 或第二事实；当前没有 WorkScene `index.json/meta.json` 生产 writer、没有 `RubricStore` 生产构造、没有 prompt-asset authority producer，这些候选都不是 production root。
4. **任务/调度/推进/交付没有额外文件根。** conversation task list在 P02 meta；schedule/job/notice/delivery与 advancement events在 P06 owner WAL。`core AdvancementStore` 的 `<home>/advancement/...` 实现无生产构造，当前 `SessionAdvancementStore.sweepOrphanDirs/removeConversation` 明确 no-op；`InMemoryTaskStore` 只在内存。不能因 `advancement-gc.ts` 做 conversation 判活或类型名含 Store 就虚构独立耐久目录。
5. **显式副作用与应用状态分离。** builtin write/edit、file/glob/grep、input material和 evidence file reader按用户选择读写 workspace/任意授权文件，是 Tool/Workspace 效果，不是知行自有存储；`InputMaterialRegistry` 本身纯内存。Assignment Skill admission 的 OS temp是唯一产品自建的外部临时根，已列 P05。
6. **同根 Server 文件不是同生命周期。** P13 的 pid/port 与 ready 按当前 Host generation创建并在有序关闭或 exact stale-stop清理；state在运行与收束过程中提供阶段观测，但成功 cleanup也删除，只有异常退出/cleanup失败才残留；token跨Host重启复用且没有production删除owner；active log也跨重启存在并只受rotate/prune治理。P14 definition文件与OS registration同样是分段发布/删除，而不是一个文件事务。A1/A3/A6不得按`server.*`或“托管服务”名称统一创建、信任或删除。
7. **其他排除项。** REPL 唯一 `UsageTracker` 显式 `rootDir:null`，故注释中的 `<home>/usage.json` 当前不可达；`RubricStore`、`AdvancementStore`、WorkScene index/meta helper只有库/测试/旧注释；旧 `SkillStore` 与 Skill home path helper 已从源码、导出和生产组合根删除，正式 `admit_skill` 只消费 assignment-scoped OS temp workspace；`MemoryStore`/`MemorySecretStore` 是进程内实现且旧 Memory 模块已退场；test-utils/golden/build scripts不在生产 main guard；P07 `capacity/`只为空目录与 fs probe落点。它们均不增加 record family。

**机械重取与全等规则。** 正向集合固定从 `packages/cli/src/index.ts` 的 A0-01 运行形态进入 `startup.ts`、`serve/command.ts`、`executor-role-runtime.ts`、`setup-delivery.ts`、`access-surfaces.ts`、`mesh-runtime-assembly.ts` 和本地 workspace/backup/DR 命令，再沿构造参数到上述 P01～P15。反向集合固定扫描非测试生产 `node:fs` writer、`new File*/Store/Repository/Journal/Ledger/Spool`、`getZhixingHome/resolveHomeDir`、`FileAuthorityCommitLog/FileArtifactStore/FileDurableProjectionIndex`、maintenance registry、backup source/target及所有 `rm/unlink/retire/compact`。反向候选逐项归入 P01～P15或上述排除项，差集为空；P05 production 集合现在只包含 assignment admission OS temp，Skill home 目录及其旧 Store/path/materializer 均不在生产调用图，P13 仍按 pid/port、ready/state、token、log 四个 subfamily 独立核对生命周期，顶层 P01～P15 分组不变；没有未分类 production root、没有同义 Memory root、没有第二 config/authority/conversation/workspace/Skill owner。

**A0-06b 状态。** `A0-06b1`～`A0-06b3b` 已逐项冻结 P01～P15 的 schema/version、reader接受域、compatibility、partial/recovery与删除责任；本表仍负责顶层root、creator/owner和生命周期，后续按各相邻证据的精确失效规则恢复。A0-07仅剩package export、build/tarball与安装入口，不得借制品调查重开持久格式枚举。

#### A0-06b1 P01～P05 schema、version、兼容与迁移 exact-set

证据 ID：`A0-06b1-persistence-schema-v1`。本证据只消费已冻结 P01～P05 的 owner 与生产组合根；字段名来自当前 writer，读取结论来自所有生产 reader/validator/恢复与维护入口。表中的“无显式版本”表示文件没有可由 reader 判别的 schema version，不以 TypeScript 类型名或当前实现年龄虚构 v1。

| ID / A0-06a 族 | 当前写出 schema 与版本 | production writer → reader / codec | 未知版本、损坏、缺字段与 partial 终态 | compatibility / cutover / 删除与目标归属 |
|---|---|---|---|---|
| C01 / P01 `config.jsonc` | **无显式版本**；JSONC 顶层正式字段沿 `ZhixingConfig`，允许注释/trailing comma。当前 loader 仅立即约束 `workspace.root` 为绝对路径，Mesh 与少数退休字段由后续 semantic validator 检查，不是全树 exact-key codec。 | 首次 `loadConfig` 可直接写模板；配置编辑器经 `writeConfig → writeJsonAtomic` 完整 JSON 写入。startup、Provider/Channel/MCP、workspace、identity、intent、network、advancement/context consumers 读取同一对象。 | 缺文件时普通启动创建模板，`noAutoCreate` 才返回空对象；读/JSONC 解析失败抛 `ConfigSchemaError`，不降级。unknown keys 可被读取并在 writer 合并中保留；缺字段由各 consumer 自行默认/拒绝。原子 writer 失败保留旧文件并清本次 tmp；首次模板写不是版本迁移。 | 无 legacy reader、upgrade/cutover 或 migration marker。A6 配置 source/文件 adapter；各 A5/A4 消费方只取自己的字段。 |
| C02 / P01 `secret-vault.json` | 外层 exact `VaultEnvelope {v:1,nonce,ciphertext,tag}`，nonce/tag 分别 canonical base64url 12/16 bytes；AES-256-GCM AAD=`zhixing-secret-vault:v1`。明文 `VaultPayload {v:1,entries}`，entry key=`<kind>:<base64url(bindingId)>`，value 为 string；payload 当前不拒额外顶层字段。 | `EncryptedVaultSecretStore.put/delete` 在文件锁内读全 payload、变更后以 mode 0600 tmp+fsync+rename；`get/list/unlockState` 认证解密并校验 ref kind/binding。Provider startup、Channel/MCP、device-key 与 credential coordinator 只经 `SecretStorePort` 消费。 | 文件缺失视为空 v1 payload；envelope unknown version/额外字段/非 canonical 编码、payload 非 v1/坏 entry、认证或解密失败均报不可用，不以空库继续。tmp 发布失败清理；已发布 vault 不被 partial 覆盖。 | 仅 v1，零兼容 reader、零原地升级；精确 entry delete 归 SecretStore，vault 无普通生命周期扫描/删除。SecretStore adapter → A6。 |
| C03 / P01 backend binding 与主密钥 | `secret-vault.backend.json` exact `{v:1,backend}`，backend=`windows-dpapi|macos-keychain|linux-secret-service|machine-bound`。Windows `.key` 是 DPAPI opaque bytes，machine-bound `.key` 是 32-byte seed；macOS/Linux keyring value 是 canonical base64url 32-byte key，均**无独立 schema version**。 | `BoundPlatformMasterKeyProvider` 读 binding 与 vault/key presence，缺 binding 时按平台/现存事实唯一推断并用 private-file hard-link publish；各 backend provider 读取/创建 key，managed context 只允许 reopen。 | binding JSON/keys/平台不匹配、key 缺失/长度错误或 keyring locked 都 fail closed 为 locked/unavailable；已有 vault 不允许静默新建另一把 key。binding/private-file tmp 只用于未发布写，失败清理。 | binding 只有 v1，无 migration；“缺 binding + 可唯一识别旧 backend”是唯一兼容接管并在成功 unlock 后补 binding。managed 空根禁止初始化。Infrastructure/Secret adapter → A6。 |
| C04 / P01 SecretStore 内 credential catalog | manifest ref exact `CredentialManifest {v:1,generation,credentialSchemaVersion?,entries,retired?}`；generation entry exact `{kind,id}`，kind=`provider|channel|mcp`；marker exact `{v:1,pid,createdAt,state,generation,entries}`。每个 generation value 是单条 JSON credential：provider exact 字段白名单并要求 string `apiKey`，channel/MCP 是 string map。 | `writeCredentials/replaceCredentialSet`：staging marker → generation entries逐条 read-back → manifest 单点激活 → committed marker → retired cleanup；`loadCredentialSnapshot/loadCredentials` 严格 parse manifest/marker/entry并按 manifest 组装进程投影。 | manifest/marker unknown v、extra/missing/duplicate/坏 id、active entry 缺失或坏 credential 均报错；不会退回旧 generation。`credentialSchemaVersion` 只校验正安全整数，当前不限定“支持版本”，缺省时 writer 不写。提交状态不可观测时禁止逆向覆盖；下次 load 按 active manifest 前滚收敛 marker/retired。 | manifest v1 是并发/cutover 协议，不是业务 schema upgrader；当前没有凭据 schema 升级器。generation 清退与 marker recovery 是同版本事务恢复。credential authority 属 SecretStore/A6，各消费者只得最小投影。 |
| C05 / P01 legacy `credentials.json` | **无必需版本**；exact 顶层 `version?/providers?/channels?/mcp?`。`version` 若存在只要求正安全整数；provider/Channel/MCP entry 校验与 C04 相同。 | startup `loadCredentialSnapshot({legacyHomeDir})` 是唯一 production reader：只接受单一普通非 symlink/hardlink 文件，读取前后绑定 dev/ino/size/mtime/digest；SecretStore 无 manifest 时走 C04 generation commit，有 manifest时比较 canonical content。 | 解析/字段损坏、源替换或 SecretStore 内容不同均拒绝且保留原文件；commit 未知保留源等待重试；已激活但收尾失败报告 pending。相同或成功导入后才 unlink 精确源并 fsync 目录。`credentials.json.<pid>.<hex>.tmp` 仅按 exact pattern清退。 | 这是 P01 唯一一次性 plaintext cutover；active manifest + canonical equality 或新 manifest 成功激活是完成条件，没有第二 reader/writer。完成后源被精确删除，不建兼容壳。 |
| C06 / P01 SecretStore locks / claims / atomic tmp | 共享 lock current exact `{v:1,pid,token,createdAt,birth}`；reader 还识别 unversioned legacy `{pid,token,createdAt}`。`.claim-*`、`.reclaim` 与 vault/key/binding tmp 是短暂发布中间物，不是事实副本。 | vault mutate/exclusive、master-key init 与 credential set coordinator 使用同一 `acquireFileLock`；lock heartbeat 更新 mtime，token owner 精确 release。 | unknown v/corrupt/unreadable lock fail closed 为 busy；unversioned legacy 可由原 token release，但永不按 stale 自动 reclaim。v1 仅在 stale 且确证 pid absent/birth changed 时回收；abandoned claim 过 stale 后可删。 | unversioned lock 是窄兼容 reader，不改写升级；生命周期只归持锁 operation/下一次安全 reclaim。Correctness/A6，不成为 Domain record。 |
| D01 / P02 conversation `meta.json` | **无显式版本**；writer 字段为 `id/name/createdAt/lastActiveAt/isDefault/archived/preferredModel?/preferredProvider?/scope/taskListState?/segmentMetadata?`。 | `ConversationRepository` create/ensure/rename/archive/touch、committed task/segment mutation与 clear view-layer 以 `writeAtomic` 重写；CLI/Server list/session/history/owner activation 读取，read-only startup browser 只最小检查 `id/name/lastActiveAt`。 | repository 对缺失、I/O、坏 JSON 一律返回 null；parseable 对象不做全字段 validator，unknown 字段保留，已退休 `capabilityState` 读时丢弃并在下次写自然清除。缺 task/segment 合法；Windows replace orphan 没有 meta 专属恢复 reader，目标缺失时按 conversation identity 缺失处理。 | 无版本协商或 migration state；`capabilityState` 是惰性字段清理而非独立迁移。完整 conversation delete 删除所属目录；Conversation Domain A5，文件 adapter A6。 |
| D02 / P02 transcript `index.json` | typed writer `TranscriptIndex {version:1,conversationId,activeShardId,lastClearAt?,shards[]}`；shard meta=`{id,file,createdAt,isActive}`。 | `ShardedTranscriptStore` 在 init/rollover/clear/rebuild 原子写；主历史/bootstrap/owner读面用同 Store，降级 browser 用 `FileTranscriptReadSource`，retention 用只读 index reader。 | 坏/缺 JSON：主 Store先收尾 orphan tmp，再由 shard 重建；无 shard 才为 null/new。**兼容差异**：主 Store对 parseable index 只 cast、不检查 version/shape；read-only browser和retention明确只接受 version 1（retention异常只 warning/跳过）。因此 parseable unknown version 当前可能在主链继续、在只读/维护面触发重建或跳过，不能写成统一 strict codec。 | 只有 writer v1，无显式旧版 upgrader；index 是 shard 派生投影，可重建，不是 cutover source。clear 半提交由 shard尾记录补 `lastClearAt`。Conversation/Context A5/A4，persistence A6。 |
| D03 / P02 transcript shard `NNNNNN.jsonl` | 首行 `header {type:"header",version:1,conversationId,shardId,createdAt}`；后续 `run {type:"run",runIndex,timestamp,messages,usage?,source?,advancement?,perspectives?,runId?}` 或 `clear {type:"clear",timestamp}`，run/clear **无显式版本**。 | `appendRunRecord` 与 Authority commit projection 的 `appendCommittedRunRecord` 追加，`appendClear` 写清空事实；history/bootstrap/server/CLI/recovery与 index rebuild 倒读，retention不读正文。 | 追加截断/坏 JSON 行被跳过，该 run 视为未持久化；缺惰性新 shard视为空。主 Store只按 `type` 接受记录且不核 header version；read-only reader要求 header v1并对 run/clear做最小 shape校验。未知 discriminant跳过，没有通用兼容 reader或重写。 | 无 shard migration/cutover；原文 append-only，retention只按 index删除超期 inactive whole shard。Conversation Domain唯一拥有用户历史语义（A5），A6只持久化。 |
| D04 / P02 `snapshots/<time>.json` | exact writer `SegmentSnapshotFile {version:1,conversationId,createdAt,coveredThroughRunIndex,structuredSummary,tokensBefore,tokensAfter}`。 | Segment/Context owner经 `SnapshotStore.write` 新建原子文件；bootstrap `list` 按 createdAt 降序读取，retention只读取 v1+createdAt后决定 whole-file删除。 | 缺目录为空；坏 JSON、unknown version或缺最小字段由 bootstrap跳过，retention也不删不可判定项。快照全部可丢，不参与 transcript 权威恢复。 | 无兼容/upgrader/cutover；按 clear/retention退役 whole file。Context派生缓存 A4/A5，adapter A6。 |
| W01 / P03 WorkScene cleanup cursor | exact `{v:1,targetDigest,stack,digest}`，stack为安全相对路径段栈且有深度/64KiB上限，digest=`protocolDigest("WorksceneCleanupCursor",1,payload)`。 | `createWorksceneStorageCleanup` 在已提交 scene/conversation 删除后分页写 fixed tmp+fsync+rename；同一 cleanup owner在启动/重驱读取并推进。 | missing 建 `{v:1,...,stack:[[]]}`；unknown v、extra/missing key、target/digest/path错误、坏 JSON或过大文件均先删除 cursor，再从 target根重新遍历。发布失败删 tmp；重复遍历/删除幂等。 | 无 legacy兼容或 schema migration；“删坏 cursor + 重扫”是派生进度重建，不是数据 cutover。完成即删 cursor。WorkScene Domain A5决定删除，A4/A6负责有界执行。 |
| R01 / P04 permission global/context | writer统一 `{version:1,rules,...}`；global另有 `scope:"global"`，context另有 `contextStorageKey/contextPath?`；rule字段=`id/pattern{tool,argument}/decision/scope/createdAt/lastMatchedAt/matchCount/contextId?/contextPath?/contributors?`。 | Trust Administration 应用决定显式/沉淀写入与管理撤销；最终 repository adapter 调用 `PermissionStore.create/revoke` 的既有 `.tmp`+rename机制。SecurityPipeline 只读 match/matchFrozen，Trust 管理/运行投影及 assignment snapshot 经同一 adapter list/snapshot；session/builtin永不落盘。 | **reader不检查 `version` 或顶层 exact keys**：unknown/missing version继续逐 rule sanitize；坏 JSON/非对象/缺 rules降级为空。未知 scope/坏核心字段丢整条；时间/计数缺失默认0，坏 context/contributor只丢该可选投影。tmp无恢复 reader。 | 无 schema upgrader/cutover；sanitize/default 是 best-effort兼容，不是 strict v1。context reset写空 v1，resetAll删除 permissions根内 JSON/tmp。规则生命周期归 Trust Administration A5，只读执行投影归 Security A4，文件 adapter A6。 |
| S01 / P05 legacy `own|linked/<dir>/SKILL.md` 与 `archived/` | **已退出 production schema exact-set**；历史文件没有当前版本合同。正式外部接入仍把用户显式选择来源中的 `SKILL.md` 当 admission 输入，但不从 home Skill 目录发现。 | 没有 production scanner、reader、writer、materializer 或 consumer；`setupAuthorityRuntime` 两个 Anchor generation 均不构造 Skill filesystem Store。正式正文只由 admission/save 写 P06 CAS、catalog entry 的 `contentRef` 引用。 | 缺失、损坏、unknown frontmatter、附属文件或任意旧目录布局均不被程序观察，因此不会触发容忍、规范化、修复、迁移或删除。旧 bytes 保持惰性；不能再把历史 reader/writer 接受域当作当前兼容承诺。 | 无 upgrader/cutover/cleanup owner；Skill Catalog 事实与 Kernel 投影只来自 P06 Authority/CAS 和领域应用。 |
| S02 / P05 legacy `skills/index.json` | **已退出 production schema exact-set**；历史数组形态不再是当前 catalog schema或默认值来源。 | 没有 production reader/writer；set-state/create/admit/update/archive 只提交 P06 Skill authority record，管理 Query 读取同一 Authority projection。 | 坏 JSON、unknown key、缺项和旁路修改都不被扫描、降级或覆盖；文件保持惰性且不能遮蔽/创建当前 Skill。 | 无 version/upgrader/cutover/delete owner；当前 revision 与状态由 Authority/CAS 唯一承担。 |
| S03 / P05 legacy `skills/usage/<id>.json` | **已退出 production schema exact-set**；历史 `{lastHitAt,hitCount}` 文件不再是 usage 输入或投影。 | 没有 production reader/writer；`load_skill` 成功后只经稳定 assignment operation stage `skill-usage {skillId,occurredAt,hitDelta}`，最终 catalog usage 由 Authority/assignment Correctness 链提交和投影。 | missing/坏值/旁路写入不被观察，也不会被新 usage 覆盖或删除；builtin 继续零 usage，用户 Skill 的 artifact/parse/stage 失败不虚报命中。 | 无 migration或 filesystem cleanup owner；usage事实只属于 Skill Catalog/Authority链。 |
| S04 / P05 legacy Skill import/materialization | **已退役，无 current record family、版本、reader 或 cutover marker。** | Anchor 启动不再扫描 `<home>/skills`，不存在 `#importLegacyCatalog`、`skill-legacy:*` request、`intent:skill-materialization`、materialization pending/ack、启动物化重驱或 commit 后本机回写。 | 旧目录无论完整、损坏或部分写入都零枚举、零读取、零写入、零迁移、零删除。staged mutation apply/refresh 仅验证已提交的 Authority request/digest/revision并同步 durable projection，不产生 filesystem side effect；exact replay和异载荷冲突继续由 Authority/assignment身份收敛。 | 无兼容、迁移或清理义务；外部 Skill 只能经显式 `admit_skill` 接入。恢复该链即使测试可绿也属于第二事实/写入路径，必须由 S7 负向门禁拒绝。 |
| S05 / P05 assignment admission temp | production root唯一为 OS temp `zhixing-skill-admission/candidate-*`；候选是来源文件树，唯一必读合同为无版本 `SKILL.md` 的 string name/description，确认 token/digest/expiry只在 admission 应用的进程内状态。 | Skill Catalog admission 应用经 path-free port 请求 `SkillAdmissionWorkspace.prepareStaging` 建目录，`acquireToStaging`复制用户显式选择的本地源；审查后原文入 CAS并 stage `skill-admit` mutation。 | safe/escalate/error/confirm/过期终态 discard；首调前 TTL sweep orphan。缺/坏 SKILL.md、无 name/description、digest变化或过期均拒绝并删除候选；崩溃只留可丢目录。无 record upgrader。 | 与惰性 `<home>/skills` 无调用或路径关系；不得把 admission temp 扩为 compatibility reader、watcher 或第二 Skill Store。 |

**版本、兼容与迁移裁决。** P01～P05 当前显式版本只有 vault/backend/credential manifest+marker、transcript index+header、snapshot、cleanup cursor和permission writer；其中 transcript主 Store与permission reader并不严格执行所写版本，必须作为 A4/A6 迁移保护差异保留或由后续明确收口，不能假称全体 fail closed。当前唯一旧格式接管是 C05 plaintext credentials，成功后精确删源；S01～S04 已退出 production exact-set，旧 Skill home 数据不再具有 reader、writer、兼容、迁移、物化或删除合同。index重建、坏cursor重扫、permission sanitize、坏snapshot跳过、SecretStore generation前滚均是同格式恢复/派生降级，不得被后续包误命名为schema migration。

**生产组合根与正反全等。** 正向从 `startup.ts`/config editor、`setup-delivery.ts` Anchor generation、`access-surfaces.ts` Conversation owner、`conversation-directory.ts`管理面、`createAgentRuntime → createAssignmentSkillPorts` 与 WorkScene committed-delete cleanup追到当前13个 production subfamily；反向从 P01～P05 全部 `JSON.parse`/JSONC parse、`writeAtomic/writeJsonAtomic`、vault/private-file/lock writer、conversation/transcript/snapshot repository、permission persist、Skill admission temp和 retention/delete入口回扫，成员全部归表。`setupAuthorityRuntime`、core/CLI production source、package exports 与构建入口都没有 Skill home Store/path/import/materializer；S01～S04 是明确排除的惰性历史数据，不是未分类格式。没有第二 production writer/reader或跨 P06/P07 才能裁决的owner；P06～P15 其余 schema、WAL/CAS、workspace、传输、恢复、Server与制品未被本次重开。

**证据失效。** `ZhixingConfig` loader/writer/semantic validation、SecretStore envelope/backend/key/lock或credential generation协议、legacy credential入口、Conversation meta/Transcript/Snapshot codec、WorkScene cursor、Trust Administration 规则合同/应用、最终 PermissionStore repository adapter、PermissionStore 格式/reader/writer、Security 只读执行投影、assignment permission snapshot、assignment admission root或其任一 production consumer变化时，恢复本证据与A0第二类索引；任何重新出现的 Skill home path/Store/scanner/import/materializer/pending/ack、Anchor filesystem注入或对应导出也立即恢复 P05/S01～S04、`A0-06a-persistence-roots-v1` 与 A2。若改动同时改变其他物理root、creator/owner或删除生命周期，也恢复 `A0-06a-persistence-roots-v1`；仅P06～P15其他变化由后续A0-06b证据承担。

#### A0-06b2a P06 Authority 存储基础容器 schema、版本、兼容与恢复 exact-set

证据 ID：`A0-06b2a-authority-storage-schema-v1`。本证据只冻结 P06 共享 CAS、Anchor/Executor 两份主 WAL、legacy identity、坏尾隔离、WAL 内 durable projection 和跨日志 artifact-lifecycle 派生索引的**物理基础设施合同**；逻辑 record body 仍由各领域 codec/owner 决定，不因装进严格 WAL frame 就自动获得同一字段或兼容规则。

##### 物理 subfamily 与恢复合同

| ID / 物理位置 | 当前写出 schema / version | 唯一 writer 与全部生产 reader | 损坏、partial、兼容与恢复终态 | 维护、删除与目标责任 |
|---|---|---|---|---|
| A01 共享 CAS：`artifacts/sha256/<digest前2hex>/<完整64hex digest>`；同目录 `<完整64hex>.tmp-<pid>-<16hex>`，root 下 `.artifact-store.lock` | artifact bytes 无内嵌版本；引用合同是 `digest=sha256:<64 lower-hex>` + non-negative safe `bytes`，路径只由 digest 决定。`put`/stream writer 以 0600 `wx` 临时文件、fsync、rename、目录屏障发布，不覆盖已有对象。 | P06 唯一物理 writer/reader 是同一 root 的 `FileArtifactStore`；`setupAuthorityRuntime` 与 `FileMeshBootstrapStore` 虽在不同启动阶段构造实例，均指向同一 `<distributed-runtime>/artifacts`。领域 owner 只经 `put/get/has/referenceForDigest/readRange/withPresentReferences` 使用；其他同类构造位于 P07/P08/P09/P12 已冻结的不同 root，不是第二 P06 CAS。 | 已存在目标必须同时满足期望 byte length 与 SHA-256 才视为同一对象，否则 `artifact-corrupt`；完整 `get/has/referenceForDigest` 复算 digest，`readRange` 只先校验物理 size 后做范围读，不能把它写成逐范围复算全文件 digest。missing：`has=false`，需要内容的读取 fail；stream 长度或 digest 不符在发布前删 tmp。内容地址碰撞边界是“同 digest+bytes 的现存 bytes 复算仍等于该 digest”，没有覆盖/同名替换分支。 | CAS 自身无 schema migration。显式领域删除与 `collectArtifactGarbage` 均持 `.artifact-store.lock`：先由 retained-reference/artifact-lifecycle 在冻结 head 上判活，再只删达到 cutoff 的未引用 canonical 文件；旧 tmp 可按 cutoff 回收，未知文件名不枚举为 artifact。CAS/校验/GC → A4，文件 I/O/lock/tmp → A6。 |
| A02 WAL 文件头与 identity：`{authority,executor-authority}/authority.log`；仅 legacy 使用 sibling `authority.log.identity` | 新建 WAL 写 72-byte file header：magic、**file format v2**、header length、32-byte random logId、前40字节 SHA-256；身份直接在 WAL。既有 headerless WAL 是兼容格式：帧保持 v1，logId 存在同样 72-byte header codec 的 sidecar，当前不会原地升级成 v2 文件。 | 每个 root 的 `FileAuthorityCommitLog.#ensureInitialized` 是唯一判型/创建入口；`originCheckpoint/checkpoint/read/append/projection/artifact-lifecycle` 只消费其冻结 `logId`。legacy sidecar 缺失时由 `log-migration` maintenance 以 tmp+hard-link 原子创建；并发已有值复读。Anchor 与 Executor 各有独立 logId、lock、owner，绝不共享 identity；只共享 A01 CAS。 | file header 长度/magic/version/checksum不符不被宽松接受为新版本：若前缀不成立只可能按 headerless legacy 继续判帧，非合法 v1 frame 随后 fail closed。legacy identity 损坏/未知版本 fail；WAL 缺失但 legacy identity 残留直接 `commit-log-corrupt`，不得静默建立第二日志；WAL 与 sidecar 都缺失则安装全新空 v2 log，旧 projection 随后因 logId不匹配重建且不能冒充备份。versioned WAL 不需要 sidecar。checkpoint/projection 的 `logId` 不匹配触发派生重建或权威 checkpoint 拒绝，不能靠改 identity 续接。 | 没有 versioned→legacy 或 legacy→versioned rewrite/cutover；sidecar 创建只是为既有 headerless WAL 补稳定身份。WAL/identity/prefix 证明 → A4；原子文件发布 → A6。A0-06a P06 原“各含 identity”在此恢复为条件 subfamily。 |
| A03 WAL frame + transaction envelope：`authority.log` header 后连续 frames；legacy 文件从 offset 0 起 | frame header 16 bytes；reader exact-set 为 legacy frame v1 与 current frame v2。两者都有 magic、version、header length、1..16MiB payload length及32-bit complement、trailer magic/length/complement；v2 trailer 另有 positive safe LSN、32-byte prefix digest和 trailer 前52字节 SHA-256。**没有 CRC 字段**。payload 是 canonical JSON `CommitEnvelope v1`，外层 exact keys 为 `v/lsn/at/entries/envelopeDigest`；entry exact keys 为 `stream/body`，body只受 canonical JSON与32KiB inline上限约束，领域字段严格性不在此层。 | `FileAuthorityCommitLog.#append` 是唯一 WAL writer；`encode/decode/scanAuthorityWalFrames` + `decodeCommitEnvelope` 是唯一物理 codec。所有 Domain append/readStream/readTail/readSnapshot、durable reducer、恢复/transfer/checkpoint 经同一 codec。v2 writer将 envelope digest与 `AuthorityLogPrefix v1(logId,previousDigest,lsn,envelopeDigest)` 写入 frame proof；legacy writer继续 v1 frame而无 frame-level LSN/prefix。 | unknown frame/envelope version、magic/长度/complement/trailer/checksum、非 canonical JSON/time、非连续 LSN、envelope digest、v2 frame LSN/prefix不一致均 fail closed。一次 append 的多个 logical records同属一个 envelope/frame：短写只形成 incomplete tail，不暴露部分 logical transaction；完整 frame fsync 后即为权威提交，崩溃发生在 projection publish 前时由 WAL重放。legacy checkpoint必须从头扫描验证；v2可在帧边界常数级复验。 | WAL没有普通 retention/compaction owner；planned Anchor prefix install 是显式拓扑安装的整 WAL 原子替换，不是后台压缩。outer envelope严格性不得外推到领域 body；领域 reducer/codec → A5，frame/envelope/prefix/事务原子性 → A4，append/fsync → A6。 |
| A04 incomplete tail 与 `bad-tail/authority-<time>-<random>.bin` | quarantine 文件是无版本 raw bytes，仅保存从最后完整 frame 边界开始的截断尾部；文件名不是协议身份。 | `#readAndRecover/#synchronizeDurableProjection/readTail` 扫描发现**不完整** header/payload/trailer时，`#quarantineTail` 是唯一 writer：先 0600 `wx` 写并 fsync raw tail、同步目录，再把 WAL truncate 到 `validBytes`。仓库没有生产 reader、replay、retention或删除该目录的入口。 | 只对 incomplete tail 自动隔离；完整但 magic/checksum/envelope/prefix错误是 corruption，直接 fail closed，不能借 quarantine 跳过。隔离写或 truncate 失败则恢复失败，不报告成功；完成后 verified tail 只覆盖保留的合法前缀。 | forensic raw tail不是 authority、backup或可重放事务；目前“无生产删除 owner”是冻结事实。坏尾原子恢复 → A4/A6。 |
| A05 WAL 内 durable projection：`<wal-root>/projections/<64hex directory digest>/{manifest.json,write-intent.json,segment-*.{data,offsets},directory-*.json,retirement-*.json,tmp...}` | 目录名是 `protocolDigest("DurableProjectionDirectory",1,{projectionId})` 去 `sha256:` 后的 64 hex，逻辑 id不直接作路径。manifest **format v3**，含 projectionId、positive reducerVersion、generation、按 source-id 键控的 `{logId,lsn,frameEndOffset,prefixDigest}` checkpoints、delta/base/compaction/retirement roots；segment entry **v1**（52-byte header、key/value length、tombstone、ordinal、SHA-256 + canonical JSON），directory page/retirement page/write-intent各 **v1**，offset table是无独立版本的 uint64-BE表。projection value再以 exact `{v:1,key,value}` 绑定 key。 | `FileAuthorityCommitLog.durableProjection` 唯一创建并在同一 WAL lock 下 synchronize/reduce/prepare/publish；`FileDurableProjectionIndex` 唯一物理 writer/reader。manifest rename是派生发布点；WAL可先提交而 projection仍只在内存 overlay，重启从持久 checkpoint补放。相同 id+reducer复用同一注册，版本冲突拒绝。 | missing manifest/index、format/reducer/source-logId/checkpoint不匹配、segment/page/checksum/bound-value损坏均视为**派生失效**：清 projection root、安装空 manifest并从权威 WAL重放；未知版本没有兼容 reader或 upgrader。write-intent v1以 target generation + created/transient files +可选 manifest digest区分提交前回滚与提交后清理，残留 intent幂等重驱；generation单调性是提交判据。JSON manifest/page/intent要求 canonical且严格校验已知字段/版本，但其 object validator并非统一 exact-key拒绝，额外字段可被读取后规范化丢弃，不能误写成全体 strict exact-set。 | projection flush/compaction、write-intent recovery、read-view lease与retirement page负责派生文件换代；清理失败只留不被 manifest引用的 orphan并保留/继承清理凭据。projection root没有独立跨进程 file lock：WAL-owned projection受 `.commit-log.lock`保护，内部 manifest写由串行队列治理；generic file-lock格式引用 A0-06b1 已冻结事实，不复制第二份 schema。Correctness/index → A4，物理 segment/page/tmp → A6。 |
| A06 跨日志 artifact lifecycle：`derived/artifact-lifecycle/<A05 projection files>` | 复用 A05 storage format v3，逻辑 `projectionId=artifact-lifecycle`、`reducerVersion=3`，value同样以 bound-record v1存储。checkpoint source keys是每个输入 WAL 的 logId；值为完整 durable checkpoint。内部 state/owner/dead/pending/release/fact/merkle/grant/reservation/quota/adoption/temporary key族是由 WAL重算的派生值，本包不展开其领域字段。 | `ArtifactLifecycleIndex` 是唯一 owner/writer/reader；`FileMeshBootstrapStore` bootstrap阶段以 Anchor WAL初始化，同一物理 root进入完整 `createSurfaceAssetAuthority` 后以去重后的 Anchor+Executor日志重绑。Surface asset授权、跨日志 retained-at-head、temporary reconcile/reclaim与CAS deletion gate消费；两个 WAL仍是唯一业务事实。 | source集合、logId或checkpoint不一致，projection/storage value损坏，或可重建的 source checkpoint异常时，关闭临时 reconciliation cursor、reset整个 index并从各 WAL origin有界重放；bootstrap一源到runtime两源因此是同一derived root重建，不是第二 lifecycle owner。若权威 WAL本身完整 corruption，derived index不能替代或修复它。 | lifecycle reconcile/retirement/adoption/temporary cleanup是可重驱维护；它只提供多日志精确判活、release identity、quota与临时进度，不进入backup source，也不拥有领域事实。跨日志判活/GC fence → A4，projection I/O → A6。 |

##### production projection 注册 exact-set 与 owner 分界

从全部 production `durableProjection(?:<...>)?(` 调用反向取得 12 个 WAL 内注册合同：`authority-retained-references@4`（每个 `FileAuthorityCommitLog` 构造时无条件注册）；`conversation-identities-v1@1`、`conversation-transfer-current-v1@1`、`deferred-global-intents-v1@1`（随对应 control/transfer/intent consumer 在 Anchor 或 Executor 条件注册）；Anchor 侧 `global-schedule-authority-v1@1`、`job-publish-pending-v1@1`、`global-workscene-authority-v1@1`、`workscene-session-activity-v2@1`、`global-skill-authority-v1@1`、`global-rubric-authority-v1@1`、`surface-asset-grants@7`；Executor 侧 `executor-job-interaction-obligations@5`。`artifact-lifecycle@3` 是 A06 独立跨日志 index，不位于任一 WAL 的 `projections/` 注册表。实际目录只在相应 topology/consumer首次触发时出现；“调用点 exact-set + projectionId/reducerVersion冲突拒绝”才是机械集合，不能把磁盘 orphan目录或某一拓扑快照当 registry。

**lock/tmp 边界。** A01 CAS只使用自身 `.artifact-store.lock`；Anchor/Executor各自只使用自己root的 `.commit-log.lock`，两者没有共享日志锁。锁文件内容、stale/wait语义复用A0-06b1已冻结的generic file-lock合同，本证据不复制第二schema。WAL创建/legacy identity/planned prefix分别可能产生 `.authority-<random>.tmp`、`.authority-identity-<random>.tmp`、`.authority-planned-<random>.tmp`；正常错误路径自行删除，进程崩溃遗留项没有生产reader或目录sweep，不是恢复凭据。projection 的 `.manifest.tmp`、`.write-intent.tmp` 与 `.<segment/page>.tmp` 由 A05 write-intent/重建边界处理。bad-tail若在raw quarantine已持久而WAL尚未truncate时崩溃，下一次仍从未改WAL发现同一tail，允许再生成一份forensic副本后重试truncate；没有“quarantine文件即提交标记”的旁路。

##### 生产组合根、正反全等与排除

正向从 `setupAuthorityRuntime`、`FileMeshBootstrapStore/createMeshRuntimeBootstrap`、`createSurfaceAssetAuthority` 追到同一 P06 artifacts root、两份主 WAL及一个 derived lifecycle root；反向从 `FileArtifactStore` 全部内容写/读/GC、`FileAuthorityCommitLog` append/read/checkpoint/install、WAL header/frame/envelope codec、`FileDurableProjectionIndex` 全部 file writer/recovery、12个 production reducer注册点和 `ArtifactLifecycleIndex` source/reconcile入口回扫，集合与 A01～A06 全等。P07 workspace catalog、P08 assignment spool、P09 temporary CAS、P12 transfer/DR staging虽复用这些类或格式，但root、owner和生命周期已经由各自 P 行冻结，不能计为第二 P06 CAS/Authority；test-support与测试构造不进入集合。两份主 WAL只共享 CAS和底层格式，没有共享 logId、commit lock、checkpoint、projection或领域 owner。

**A0-06a 恢复与证据失效。** 本包反证并恢复 P06 的两处路径简写：projection物理目录不是raw projectionId，`authority.log.identity`也不是每个新 WAL的必有文件；P06 root/owner其余结论不变。以后若 `ArtifactRef`/`FileArtifactStore` path或校验/GC、WAL file/frame/envelope codec与支持版本、legacy identity判型、tail quarantine、commit/checkpoint/prefix、durable projection任一format/reducer registry/write-intent/rebuild/compaction、artifact-lifecycle source/reducer/reconcile，或 P06 production constructor/root变化，恢复 `A0-06b2a-authority-storage-schema-v1` 与 A0第二类索引；若同时改变物理顶层root、owner或删除生命周期，再恢复 `A0-06a-persistence-roots-v1`。领域 logical body字段变化只恢复其后续领域 schema证据，不得把 A03 外层 frame一并假判失效。

#### A0-06b2b1 P07 Executor 发布快照族 schema、绑定、兼容与恢复 exact-set

证据 ID：`A0-06b2b1-executor-delivery-schema-v1`。本证据只冻结 P07 的 capability directory、snapshot-version、permission snapshots 与 execution-assets 四个发布 subfamily；`workspace-bindings/`、`workspace-probes/`、`capacity/` 仍留 A0-06b2b2。四类记录虽由同一个 `setupAuthorityRuntime` 组合入口装配，却没有统一文件事务、统一版本或统一可重建语义。

##### 物理 subfamily、schema 与失败合同

| ID / 物理位置 | 当前写出 schema / version 与绑定 | 唯一生产 writer 与全部 reader / consumer | 损坏、partial、兼容与恢复终态 | 事实类别、删除与 AE-001 归属 |
|---|---|---|---|---|
| E01 `<distributed-runtime>/executor-snapshot-version.json` | exact **v2**：`v/executorId/deviceDigest/deviceRevision/deviceGeneratedAt/inventoryDigest/inventoryRevision/inventoryGeneratedAt/capabilityDirectoryEstablished`。两个 digest 必须为 `sha256:<64hex>`，revision 为 positive safe integer，时间为 canonical ISO；没有 v1/unknown-version reader。`deviceDigest=LocalTransitionConfiguration` 绑定当前 non-secret readiness、workspace catalog 与 configuration，`inventoryDigest=LocalTransitionInventory` 再绑定 permission high-water 与 execution-asset digest/null。 | `FileExecutionSnapshotVersionStore` 只有 `setupAuthorityRuntime` 在启用 local Executor 时构造；Anchor+local、Executor-only 与 workspace local fallback 共用同一 writer。`synchronize` 是唯一 revision allocator，`assertCapabilityDirectoryCoherence/markCapabilityDirectoryEstablished` 是唯一 reader/建立标记；无其他进程直接消费该 JSON。 | bad JSON、extra/missing key、unknown v、坏 id/digest/time/revision均 fail closed。相同 executor+两个 digest 复用 revision/time；device digest 变化使 device+inventory 各加一，仅 inventory digest 变化只加 inventory；executorId 改写拒绝。每次 fixed `.tmp` + fsync + rename + parent sync；遗留 tmp不参与读取、下次写前清。version 缺失但 directory 已存在会拒绝；version 已标 established 而 directory 缺失也拒绝；唯有 `established=false + directory尚缺` 的首次中断可重入完成。 | 本机单调发布 lineage 与首次建立判据，是 Correctness 事实而非 cache；A4 保留 revision/配对防回滚，A6持有 file/lock/tmp。current-device removal删主文件；没有 schema migration/cutover。 |
| E02 `<distributed-runtime>/executor-capability-directory.json` | outer exact **v1** `{v,generation,executors}`；generation non-negative且 executorId排序唯一。entry 要求 `executorId/deviceKeyId/active/descriptorRevisionHighWater/inventoryRevisionHighWater/versionHighWater/permissionSnapshotHighWater/credentialBindingHighWater`，可选 `snapshot`；unknown key拒绝。snapshot exact `{descriptor,inventory}`：两者均 v1、分别由 Executor device key签名，并强制同 executorId、同 signer key、`descriptor.revision=inventory.capabilityRevision`、credential binding集合/revision全等。descriptor冻结 protocol、workspace、tools、MCP、credential与evidence；inventory冻结 config三 revision、asset三 revision、permission high-water与 credential revision。目录 generation/high-water本身是本机未签名的耐久接受事实。 | `ExecutorCapabilityDirectory.accept/#commit → FileExecutorCapabilityDirectoryStore.save` 是唯一 writer；文件锁内以 expected generation CAS，state发布仍是 fixed tmp 原子替换。本机 `publishLocalExecutorSnapshot` 与 Anchor 对远端同步结果的 `acceptExecutorSnapshot` 是两条 producer调用链；assignment owner/manifest匹配、resource reporter、workspace catalog与当前/活动 snapshot查询是 readers。`refresh`允许另一个受信本机进程提交更高 generation。 | topology条件明确：启用local Executor时E01/E02成对，E01已标established后E02缺失fail closed，只有首次bootstrap可从 `{generation:0,executors:[]}` 起步；Anchor-only没有E01，E02缺失会重新打开空内存directory并等待remote executor再同步。bad JSON、unknown v/key、坏签名、身份/credential/revision/high-water不一致均拒绝；相同 generation不同内容视为 rewrite。完全相同 descriptor+inventory revision是 replay；descriptor重写、inventory不递增、任一 version/permission/credential high-water回退均拒绝。device transition清当前 snapshot但保留 high-water；当前生产没有 `revokeDevice/transitionExecutorDevice` caller，远端 trust变化只更新 live authorization，使旧 entry 即使仍标 active也无法从 `snapshotFor/activeSnapshots` 读出。 | 本机已验 capability registry与防回滚事实；local Executor entry不能随意重建，Anchor-only的remote view可由再次同步恢复。A4拥有验证/high-water/CAS，Device/Workspace/assignment领域只消费投影（A5），A6持有文件适配。current-device removal删主文件；远端 device removal不清历史 high-water。无旧版本兼容或 upgrader。 |
| E03 `<distributed-runtime>/permission-snapshots/<digest去前缀>.json` | 每文件 exact `TrustRuleSnapshot` **v1**：`v/snapshotVersion/rules/generatedAt/digest/signature`；snapshotVersion positive，rules按 id canonical排序且id唯一，portable rule只允许 pattern/decision/scope/createdAt及合法可选 contextId/contributors，运行度量和 contextPath不入快照。digest覆盖 unsigned canonical内容，signature由 Anchor device key覆盖含digest payload；文件名必须等于digest。目录没有 manifest/high-water文件，高水位由扫描全部 canonical文件名后的 max(version)重建。 | `FileTrustRuleSnapshotCatalog.publishRules` 是 Anchor assignment prepare的唯一本地签发/分配入口；同一 canonical rules content复用既有 snapshot，首次新内容取 `max+1`。Mesh `install-permission` 调 `publish` 是 Executor的唯一远端安装入口。assignment prepare/latest、capability inventory、Executor `AssignmentLedger` dispatch接纳与每次工具调用按 lease digest/version复验是 readers；`snapshotFor`只读已载入不可变 map。 | `open/reload` 对任一 canonical文件的 bad JSON、unknown v/key、坏规则/digest/signature、文件名不匹配、重复 digest/version/content 全目录 fail closed；未知文件名、subdir和 `.tmp` 不进入 catalog。remote publish同digest幂等，但同 version或同content出现另一digest拒绝；local publish只分配连续新高水位。单文件固定tmp原子发布；目录扫描可跨重启复用全部历史版本且当前无per-snapshot retention/GC。目录/某历史文件丢失不会从 lease 重建原 signed snapshot：新签发会得到新的version/time/digest；活动 dispatch缺digest返回 retryable capability-gap，已活动执行缺digest直接拒绝工具授权。 | Anchor签名、按assignment生命周期保留的不可变Correctness输入，不是第二Permission Domain owner，也不能笼统称为可重建cache。远端assignment前可由Anchor重发原snapshot；current-device removal才删整个目录。A4拥有lease/snapshot一致性，Permission/assignment领域A5提供当前规则，A6持有目录/锁/tmp。无schema migration。 |
| E04 `<distributed-runtime>/execution-assets.json`；asset body仍在 P06 `artifacts/sha256/...` | on-disk index exact `ExecutionAssetSnapshot` **v1**：`v/snapshotRevision/skillCatalogRevision/skills/rubrics/promptAssets/generatedAt/digest/signature`。skills是完整 strict `SkillCatalogEntry`（含contentRef、entry digest/revision/state/usage），rubrics/prompt是 exact `{id,kind,revision,digest}`；三数组签名前按id排序，outer digest与Anchor签名覆盖全部内容。传输 `ExecutionAssetBundle` 也是v1 `{snapshot,artifacts[{ref,contentBase64}]}`，但bundle不落此文件。首个实际index revision从empty baseline 1之后的2开始；相同内容身份复用现值。snapshot没有executor/device/generation字段，签名key+digest是其跨设备绑定，anchorEpoch只用于读取authority的调用上下文。 | `FileExecutionAssetCache.publishFromAuthority` 是唯一原始publisher：Skill authority生产skills，Rubric authority生产rubrics；当前global router没有prompt-assets owner，查询失败被窄化为空数组，故当前promptAssets无producer。Mesh `install-assets → installBundle` 是remote writer；先验证bundle完整性并逐个put P06 CAS，最后才install签名index。read/bundle、local assignment global-query（Skill）、Advancement rubric library、inventory assetVersions、planned-anchor readiness与DR readiness是consumers。 | disk `current/install/publisher` 对bad JSON、unknown v/key、坏inner entry/digest/signature严格失败；`read`与`bundle`经safe-current把corrupt index视为cache miss，且缺失/损坏body时按entry过滤或bundle失败。install相同digest幂等，已有index时revision必须严格增加，rollback/rewrite拒绝。bundle拒绝非canonical base64、缺失/重复/多余body、ref bytes/digest不符；body逐个CAS成功而index发布前崩溃只留下P06未引用对象，重试幂等、GC责任仍属P06。当前publisher按id规范化skills后又复用Skill catalog排序validator；当usage/createdAt排序与id顺序不一致时会在写前fail，这是当前可达writer约束而非兼容格式。 | 真正可重建的签名本机cache：index可由P06 authority记录+CAS再发布，正文事实不归P07。A4拥有签名/revision/bundle完整性，Skill/Rubric A5拥有内容事实，A6持有index与CAS adapter边界。current-device removal只删index、不删P06 body；无schema upgrader。 |

##### 发布顺序、partial publication 与重启裁决

1. **没有四文件事务。** Anchor prepare先把E03规则快照耐久发布；远端同步又是两个独立canonical Mesh请求：先`install-permission`，有bundle才再`install-assets`。每个请求返回目标Executor在该步完成后的新capability snapshot；owner只接受最终成功返回的目标快照，第二步失败时本次assignment不选中该remote candidate，但已安装permission或部分CAS body保留供幂等重试。
2. **本机capability publication固定顺序。** `publishLocalExecutorSnapshot`先读取E04（corruption窄化为absent）并计算device/inventory digest，随后E01 `synchronize`发布revision，签出descriptor/inventory，E02 `accept`原子提交directory，再可选提交credential exposure，最后E01 `markCapabilityDirectoryEstablished`。因此首次崩溃允许`version(established=false) → directory missing/present → replay → mark true`前滚；`directory present + version missing`或`version established + directory missing`均拒绝，不能由删另一边“修复”。
3. **inventory只声明已经本机可读的版本。** permission high-water来自E03已发布目录；asset三revision来自E04当前签名index或empty revision 1。device配置变化才推进descriptor/config三revision；只变permission或asset只推进inventoryRevision。descriptor/inventory由同一device key签名并先通过一致性校验，E02提交后才对assignment selector可见。
4. **重启复用与损坏边界不同。** local Executor的E01/E02配对严格重开并保留high-water；Anchor-only的E02缺失会暂时回到空remote view；E03全目录严格扫描并保留历史lease输入；E04作为cache可缺失并以empty revision参与本机publication，普通query把corrupt视为miss，但严格publisher/install不会自动覆盖corrupt主文件。不存在统一的“删坏文件重建”恢复器或legacy reader。

##### 删除、恢复、consumer全等与排除

- current-device lifecycle在完成排空/退役后由`cleanupExecutorDeviceLocalState`精确删除E01、E02、E03目录、E04以及另包的workspace/probe/capacity目标；它不删除P06 Anchor/Executor WAL或共享CAS。四类writer的正常锁释放负责相邻`.lock`，fixed `.tmp`由下一次writer清理；device-removal清单没有把这些sibling辅助文件另列为领域删除目标。
- remote trust更新只重建live `authorizedDeviceIds`：旧E02 entry保持durable high-water但查询不可见。core提供的`revokeDevice/transitionExecutorDevice`当前没有生产调用点，不能把库API写成现行device-removal writer。
- full recovery checkpoint中名为`execution-assets`的coverage实际保存P06 Authority WAL所引用的CAS闭包，不复制E04文件，更不复制E01～E03。Authority generation安装后由`publishFromAuthority`重建E04；E01/E02依当前本机readiness重新建立，remote E03由后续assignment同步。E03历史文件若在仍有活动lease时丢失，没有从checkpoint/lease重建原digest的旁路，按上表fail closed。
- 正向从三个production `setupAuthorityRuntime` caller（Anchor Host、Executor-only Host、workspace fallback）到四个构造与Mesh snapshot service；反向从四个精确path、全部class构造、directory/ledger/global-query/rubric/readiness consumers、DR read与device-removal清单回扫，production成员与E01～E04全等。直接路径命中之外只有测试；`execution-assets` checkpoint coverage是P06 artifact closure。workspace generation/probe/capacity未进入本证据。

**A0-06a纠正与证据失效。** P07物理root、creator和current-device删除集合不变；初次A0-06a把permission/assets一起写成“可重建/再发布投影”被本证据部分反证并恢复：E01/E02是本机单调Correctness事实，E03是活动执行依赖的不可变签名快照，只有E04 index是可由P06再发布的cache。以后若四个path/constructor、E01 v2/配对顺序、E02 state/descriptor/inventory/high-water或authorization、E03 snapshot/catalog/lease消费、E04 snapshot/bundle/global producers/CAS install、Mesh发布顺序、DR/device cleanup任一变化，恢复`A0-06b2b1-executor-delivery-schema-v1`与A0第二类索引；若同时改变顶层root、owner或删除生命周期，再恢复A0-06a P07。workspace generation/probe/capacity变化只由A0-06b2b2承担。

#### A0-06b2b2 P07 workspace binding/probe/capacity schema、generation、恢复与删除 exact-set

证据 ID：`A0-06b2b2-workspace-storage-schema-v1`。本证据冻结 P07 剩余三个物理根；它引用 A0-06b2a 的 WAL/CAS 基础格式，但不把 initial workspace/probe stream、successor catalog WAL 和空的 capacity 目录误写成一个 owner。生产唯一组合入口是启用 local Executor 且注入 `deviceCapacity` 的 `setupAuthorityRuntime`；若库调用者省略 capacity，这三项端口物理缺席，不形成第二持久实现。

##### 物理 subfamily、schema/version 与失败合同

| ID / 物理位置 | 当前 schema、版本与 identity/generation 绑定 | 唯一 writer、reader 与直接 consumer | 损坏、partial、恢复与删除终态 |
|---|---|---|---|
| W01 `<distributed-runtime>/workspace-bindings/root-manifest.json` | canonical exact **v1**：`version/state/catalogGeneration/logId/capabilityRevision`，degraded 时必须有 `degradedReason`，且只在 degraded 时可有 exact `pendingReset{requestId,confirmationDigest,previousCatalogGeneration,catalogGeneration,preparedAt}`。generation 为 `catalog-initial` 或 `WorkspaceBindingCatalogGeneration@1` 的 sha256 digest；successor 必须由 previous generation + confirmation digest 唯一派生。 | `WorkspaceBindingCatalog` 是唯一 writer/reader；manifest `.lock` 内以 canonical expected-current CAS，random tmp + fsync + rename。它选择唯一 active generation，并把 capability descriptor revision 单调回写；本机 workspace management 的 status/reset 与 E01/E02 publication 间接消费。 | bad JSON、extra/missing/unknown version、contradictory state、坏时间/digest/generation均 fail closed；healthy open还要求 active WAL checkpoint `logId` exact equality。**manifest 缺失不是 corruption**：当前实现按 fresh `catalog-initial` 重开 Executor 主 WAL并新写 manifest，不扫描已有 successor，因此曾切到 successor 后丢根文件会回到仍保留的 initial facts，旧 successor成为 orphan。写中断可能留下 random tmp且没有 sweep reader。 |
| W02 initial/successor generation identity：P06 `<executor-authority>/authority.log`；P07 `workspace-bindings/catalogs/<generation-storage-key>/authority.log...` | initial=`catalog-initial`，复用 Executor 主 WAL；successor 才使用独立 `FileAuthorityCommitLog`，物理 frame/identity/bad-tail/lock/tmp规则引用 A0-06b2a。没有独立 generation manifest：identity 由 W01 `catalogGeneration+logId`、确定性 storage key，以及 successor WAL 中 exact `catalog-reset` receipt 共同绑定。 | W01 只给一个 generation 写权；`#logForGeneration` 对 initial 返回主 WAL，对 successor 按 generation 创建/缓存独立 log。reset recovery 和启动沿 receipt 的 `previousCatalogGeneration` 反向读取整条 successor chain；assignment/Surface不直接打开这些 WAL。 | missing/mismatched active logId使 catalog degraded；坏 frame/record按 P06 fail closed。旧 generation不被 reset、retention或正常 close删除，继续只读支持 receipt replay/chain验证；current-device cleanup才删整个 workspace-bindings 根。两个 WAL family共用基础格式但不双写同一 generation。 |
| W03 workspace logical records，stream `executor:workspace-bindings` | 无 record-level version；codec exact discriminant 为 `directory-established`、`catalog-reset`、`binding-created`、`binding-updated`、`binding-removed`、`binding-request-recorded`、`legacy-binding-staged`、`legacy-migration-activated`、`legacy-migration-abandoned`。当前 main-guard producer 可达前六种；后三种有严格 reader与内部 migration method/port，但全仓无非测试调用者，属于现存 compatibility surface而非当前产品写链。`LocalWorkspaceBinding` exact `{bindingRef,revision,displayName,absolutePath,workspaceBindingRevision}`；request/body字段exact，request digest、revision连续性、name/path规范化与reset receipt均重算校验。 | `WorkspaceBindingService` 唯一 reducer/writer；local workspace管理create/rename/repath/remove/reset调用。raw absolute path **直接内联 WAL body**，只经本机 `resolveWorkspace` 暴露；E02 capability/WorkScene/assignment只见 `bindingRef/displayName/workspaceBindingRevision`。record受通用32 KiB inline上限约束。 | unknown tag/key、坏digest/time、重复identity/name、revision跳跃、tombstone/request/reset矛盾均使 generation corrupt并令 W01 degraded。create/update/remove按request id+digest committed-forward replay；binding remove只写tombstone，不删用户目录。catalog reset撤回全部旧binding，旧WAL仍保留且不原地修复。 |
| W04 `<workspace-bindings>/generations/<generation-storage-key>/directory-established` | 内容固定 `workspace-binding-directory-v1\n`、mode 0600，但 reader只检查存在，不解析内容；权威建立事实是同 generation WAL 的 `directory-established{deviceId}`，successor首事务还必须含 W03 `catalog-reset`。无其他 generation identity/manifest 文件。 | `WorkspaceBindingService.#open` 是唯一 creator/reader。每个 active/回溯 generation各有自己的 marker目录；目录名来自严格 storage key。 | marker缺失而WAL建立事实有效时重建；marker存在但对应log缺失，或marker/任一workspace stream record存在却无建立事实时fail closed。marker内容损坏当前不可观察，不能把它当权威或备份。正常close/reset不删marker。 |
| W05 `<workspace-bindings>/artifacts/` | 这是 successor `FileAuthorityCommitLog` 配置的 P06-compatible CAS backing root；当前 W03 exact records没有 `ArtifactRef` 字段，raw path也不外置，因此没有 workspace payload writer/reader或当前必有文件 schema。 | `setupAuthorityRuntime`只构造一个 `FileArtifactStore` 并传给所有 successor logs；没有第二 P07 CAS、没有将 P06 shared artifacts当作 workspace catalog backing 的调用。 | 根可能保持未物化；不得按目录名虚构 raw-path artifact、backup或恢复事实。若未来 workspace record 引入 artifact ref，会使本证据失效；当前只有 whole-root device cleanup删除该配置目标。 |
| W06 probe facts：P06 Executor 主 WAL stream `executor:workspace-probes` | 无 record-level version；exact reader集合为 `probe-log-established`、legacy read-only `probe-started`、当前 writer `probe-started-v2`、`probe-completed`、`probe-retired`。v2 start内嵌签名 `WorkspaceProbeRequest` v1，绑定request/device/binding、仅 `environment.probe` 的 Anchor grant、immediate root lease digest/目标Executor/时效；result v1绑定request/binding/workspaceBindingRevision/probe枚举/executor并由Executor签名。legacy start缺request，只能以key前缀绑定completion requestId。 | `WorkspaceProbeHandler` 是唯一 durable writer/reducer；WorkScene create/validation由Anchor `EnvironmentProbeOwner`签发，本机直接调用或经canonical Mesh `environment.probe`到目标Executor。handler只从 W01 active EnvironmentPort解析raw path/revision；marker目录不承载结果。 | bad tag/key/JSON映射、签名/digest/key/executor/revision/time或start→complete→retire顺序不一致均fail closed。completed同payload可跨重启返回且不再要求grant仍有效；started未completed会在同请求重试时重新校验当前时效、再执行probe并提交，已过期则拒绝而不是伪造completion。相同key换payload冲突。默认27天/6小时maintenance只对completed追加最多64项一页的retire；未完成start不retire，旧WAL bytes不由该retention物理删除。 |
| W07 `<workspace-probes>/probe-log-established` | 内容固定 `workspace-probe-log-v1\n`、mode 0600，但reader仅检查存在。它只证明 W06 stream 曾完成物理建立，不证明任一probe request/result。 | `WorkspaceProbeHandler.#open` 唯一 creator/reader；setup启动后立即开启retention，shutdown只停timer/runner。 | marker缺失而WAL建立事实有效时重建；marker存在而Executor主log缺失，或marker/任一probe record存在但缺建立事实时fail closed。marker残留不触发重建业务事实，也没有独立retire/delete；whole-device cleanup删其目录。 |
| W08 `<distributed-runtime>/capacity/` | **无持久 record、无 schema/version/codec。** `createDeviceCapacityRuntime`只 `mkdirSync`；Node pressure probe仅对该目录 `statfsSync`，policy `version:1` 与队列/permit/诊断全部在内存，不能登记成盘上记录。 | topology Host、workspace fallback、pair、backup/DR与managed-service各按同一path构造进程内arbiter/storage governor；无文件writer、directory enumeration、reader或恢复owner。 | 正常close不删目录，重启重新创建内存状态；目录缺失由下一次构造创建，权限/statfs失败按capacity调用失败。current-device cleanup删整个空probe root；没有retention、backup或partial publication。 |

##### generation reset、消费与跨根裁决

1. **active generation与线性化点。** reset只接受十五分钟 durable preview 窗口内、绑定当前 degraded generation 的确认；确认的 `issuedAt` 与 token 均来自同一耐久 operation，重放时摘要稳定，窗口外的新 reservation 仍 fail closed。W01先耐久写`pendingReset`，maintenance owner再创建确定性successor。successor首事务原子写`directory-established + catalog-reset`，receipt中的request/digest/previous/current/logId/capabilityRevision/time必须与reservation及checkpoint全等；W01从degraded old原子替换为healthy successor才是唯一切换点，随后才发布E01/E02 capability。切换前W01仍指向old但degraded gate关闭全部business mutation并只允许发布空的degraded workspace投影，切换后只有successor可写，旧service发布也会因generation不等被拒绝，因而没有双写窗口。
2. **crash/replay边界。** reservation后未建log则按同一generation重建；successor genesis已提交但W01未切则重读receipt并完成切换；W01已切则启动只开该successor并校验logId。已切换generation不原地修复；全部旧successor按receipt chain保留。唯一例外是W01整体缺失会走fresh initial路径，这是当前恢复行为而不是成功reset语义。
3. **直接消费者。** E01 local transition digest含`catalogGeneration/state/workspaces`，E02 descriptor只发布path-free workspace。conversation assignment prepare从已验descriptor冻结device/binding/revision，Executor dispatch/recovery preflight再由active W01/W03解析path并复验revision；Advancement evidence同样同时核对descriptor与local resolve。WorkScene目录从E02聚合候选，create前经W06本机/Mesh probe验证。没有consumer直接读取marker、P07 artifacts或capacity下文件。
4. **删除、backup与owner。** 普通workspace remove/reset均不删除用户选择目录；reset也不清旧generation。正常Host close只停止probe retention、catalog recovery和successor WAL maintenance。`cleanupExecutorDeviceLocalState`在current-device生命周期整体删除`workspace-bindings/`、`workspace-probes/`、`capacity/`；这三个P07根没有独立checkpoint、backup、DR restore或per-generation GC consumer，初始workspace/probe事实所在的P06 Executor WAL仍按P06合同裁决，不能据此把P07 root复制成第二备份owner。

**正反全等与排除。** 正向从 Anchor+local Host、Executor-only Host与workspace local fallback三处`setupAuthorityRuntime`进入唯一catalog/probe构造；反向按`WorkspaceBindingCatalog/WorkspaceBindingService/WorkspaceProbeHandler/FileArtifactStore/createDeviceCapacityRuntime`全部production constructor、三个精确root、W01/W03/W06 writer、capability/assignment/WorkScene/Advancement consumers、maintenance与device cleanup回扫。结果为W01～W08全等：没有第二workspace owner、第二probe log、隐藏generation manifest、workspace artifact payload、capacity record或P07 backup reader。测试的内存log/fixture、未被production调用的workspace legacy migration methods，以及P06通用WAL/CAS codec不增加P07 writer。

**A0-06a纠正与证据失效。** A0-06a P07把`generations/<gen>`、`catalogs/<gen>/<WAL-family>`与“本地raw-path facts、artifacts”并列作为范围简写；本证据恢复为严格storage-key目录、successor `authority.log`、无generation manifest、raw path内联WAL且P07 artifacts当前无payload producer。P07顶层root、唯一setup creator和whole-device删除集合不变，E01～E04证据也未失效。以后若W01 codec/CAS写、generation key/log/receipt chain/reset顺序、W03 record/reducer/raw-path投影、两个marker、probe v1/legacy reader/retention/Mesh binding、capacity root/statfs、assignment/WorkScene/Advancement consumer或device cleanup变化，恢复`A0-06b2b2-workspace-storage-schema-v1`与A0第二类索引；若同时改变顶层root/owner/delete生命周期，再恢复A0-06a P07。P08～P15及package/tarball变化不使本证据失效。

#### A0-06b3a P08/P09/P11/P12 跨设备传输与恢复耐久链 exact-set

证据 ID：`A0-06b3a-distributed-transfer-storage-schema-v1`。本证据沿四条生产链冻结“产生 bytes → 本机/mesh 传输 → 私有暂存或冻结 checkpoint → Authority 消费/终态”的当前合同；P06 WAL/CAS 基础格式只引用 `A0-06b2a-authority-storage-schema-v1`。凡物理使用 P06 CAS 但仍由私有 WAL/candidate 引用的对象，语义 owner 仍由引用它的状态机决定，不能因共享 backing store 升格为 live Authority。

##### record family、schema/version、writer/reader 与终态

| ID / A0-06a 族与物理位置 | 当前格式、版本与绑定 | 唯一 producer、reader/consumer | partial、重启、retention/delete 与目标责任 |
|---|---|---|---|
| D01 / P08 assignment spool：`assignment-streams/assignments/<sha256(assignment-id)>/{identity.json,index/,frames/}`、`tombstones/<key>.json` | `identity` exact v1 `{v,assignmentId}`，缺失时可从 per-assignment WAL 首个 `opened` 重建；tombstone exact v1 `{v,assignmentId,reclaimedAt}`。WAL record自身无显式版本，但严格 discriminant exact-set 为 `opened/frame/consumer-qualified/consumer-revoked/connection/offered/ack/degraded/pruned/deleted/terminal/reclaim-armed/reclaim-disarmed/reclaimed`；frame CAS body是严格 `StreamFrame` v1，绑定 `ExecutionRef`、seq、logical digest、content refs、verifier checkpoint、可选source/interaction。 | `ExecutorDataPlaneRuntime`唯一以 P06 source artifacts 构造`AssignmentStreamSpool`；writer/interaction projection写，local/mesh surface-ticket与owner-relay按consumer key/epoch订阅和ACK，ticket/assignment恢复只提供授权与terminal条件。目录key、identity、opened、ExecutionRef和digest chain交叉校验，未知tag/key、坏frame/CAS或身份冲突fail closed。 | sourceId相同且内容相同幂等，换内容拒绝；ACK耐久后才prune，先记`pruned`再删frame、再记`deleted`；terminal且consumer义务满足后延迟24小时，先记`reclaimed`、再发布tombstone、最后删assignment目录，重启可继续。spool不是assignment或transcript owner；A4负责顺序/ACK/reclaim，A5 Execution消费，A6持文件/CAS。 |
| D02 / P08 `evidence/<executor-id>.jsonl` | 无显式文件/record版本；production writer只写canonical `pending{t,at,request,requestDigest}`与`completed{t,at,requestId,requestDigest,result}`。恢复`#load`要求整行canonical、识别两种tag，却**不做record顶层exact-key校验**，故核心字段合法时额外顶层字段会被接受并在内存投影中丢弃；pending会以strict `validateEvidenceRequest`重算digest，completed要求已有pending、record requestId/digest匹配、`at`为string且result outer exact为`capability-gap`或`bundle`。bundle自身仍经strict codec与签名验证，但恢复reader不再把bundle的`requestId/requestDigest/executorId`同pending request交叉全等；同request的后续合法completed也不与先前result比较，而是以日志最后一项覆盖内存result/completedAt。坏JSON/非canonical/未知tag、坏pending request/digest、无pending的completed、record digest冲突或坏result/bundle使整账本corrupt。 | Anchor Host access surface与Executor-only Host各为本机executor装配同一路径`EvidenceJournal`。首次`complete()`在尚无durable result时会验证bundle并把其`requestId/requestDigest/executorId`同调用request交叉全等后才append；若已有result，则只要求传入request digest与已存pending一致、result canonical相等便返回，因此production writer不会正常追加冲突completed。重启后的`replay/begin`会把来请求的digest及canonical request同pending全等，但随后直接返回恢复reader最后接受的result，不补做bundle→pending三字段绑定；`ExecutorEvidenceHandler`在journal replay前仍严格验证来请求、目标executor与current owner。 | append是0600单行写；响应丢失时pending重新执行效果，completed直接重放当前恢复结果。默认27天compact只删过期completed、永久保留pending；发生rewrite时按writer shape用`.tmp-<pid>`后rename，因此会删除reader曾容忍的额外record顶层字段，并只保留每个request当前内存中的最后result。当前没有临时文件sweep或device-removal删除owner。宽松顶层reader、last-completed-wins与恢复期较弱bundle绑定是迁移输入，不是兼容承诺或能力；账本仍不是Advancement evidence权威或用户历史。 |
| D03 / P09 `mesh-endpoints.json`、`mesh-peers.json`、`mesh-bootstrap-completions.json` | endpoint `{v:1,descriptors}`、peer `{v:1,peers}`均要求canonical且成员走严格descriptor/identity/trust-root codec，但outer reader只检查`v+array`，当前不会拒绝额外outer key。completion是**无显式版本**的canonical `peerDeviceId→offerId` map，reader只要求string value；同peer换offer冲突。 | `FileMeshBootstrapStore`唯一读写；pair command接受endpoint/peer，Mesh bootstrap/reconnect读取，terminal bootstrap先标completion再删rendezvous secret/continuation。原子writer用random tmp、fsync、rename、directory sync并在finally清tmp。 | 缺文件分别为空目录/空数组/空map；坏JSON、非canonical或成员校验失败fail closed。它们是P06 trust/pairing Authority的本机transport投影/idempotency，不是第二trust owner。current-device removal整项删除；A5 Device/Trust消费，A6 Mesh adapter拥有文件。 |
| D04 / P09 `mesh-pairing-continuation.json` | 所有variant显式 v1；issuer phase exact family为`offer-secret-pending/offered/secret-pending/commit-ready`，joiner为`secret-pending/proof-ready/bootstrap-ready`，保存 invitation/attempt/PAKE/proof/trust/bootstrap材料。Store只做canonical JSON与`v/side/phase/offerId/localDeviceId`等浅层shape校验，**不是全字段strict codec**；各恢复phase由pair command重新验证邀请、密码学证明、trust event与当前身份。 | `FileMeshPairingContinuationStore`由pair命令与Mesh启动catch-up创建；每阶段先durable save再推进外部/Authority效果，`commit-ready`与`bootstrap-ready`允许响应丢失后前滚。single sibling lock串行save/clear，random tmp原子替换。 | terminal成功、明确过期/失败且可安全撤回时按exact offerId清文件与secret；不匹配offer拒删。current-device removal清单**没有**此文件，故中断后continuation可能跨该清理边界保留，只能由pair恢复/终态owner收口。A4保护阶段幂等与proof binding，A5 Pairing拥有语义，A6持文件。 |
| D05 / P09 surface/mesh asset临时族：`surface-asset-temporary/sha256/...`、`.presence/`、`surface-asset-partials/`、`mesh-artifact-partials/` | temporary CAS复用P06-compatible digest/path/bytes格式；resumable partial无record版本，文件名 exact `<digest>.<declared-bytes>.part`，只允许连续append或byte-equal前缀重放。presence marker exact `formatVersion:1/ref/scopeIdentity`，位置同时绑定digest与scope；legacy-adoption marker exact v1只服务lifecycle接管。mesh partial使用同一receiver但最终target是P06 CAS。 | Surface上传先在P06-derived lifecycle记temporary presence，再写临时CAS/partial并settle；grant/adoption按P06权威ref验证后将bytes提升。Mesh artifact receiver为assignment/transport写P06 CAS；asset coordinator/derived index在启动和周期maintenance读presence/cleanup候选。 | digest/长度不符拒绝并在已完成坏partial时删除；finalize验证后写target CAS并耐久删partial。Surface已接管临时副本由durable cleanup候选回收，正式P06 bytes仍按retained refs/保留窗；orphan presence staging有bounded sweep。device removal删三根，但P06正式CAS/grant不随之删。 |
| D06 / P11 `recovery-backup-targets.json` | strict canonical v1 exact `{v,currentTargetId,bindings}`；binding exact为directory `{kind,targetId,directory}`或paired `{kind,targetId,deviceId}`，targetId唯一且current必须存在；directory读取时resolve绝对路径。 | backup setup唯一`select` writer；configured checkpoint owner与DR inventory读取，旧target binding保留以便重放created obligation到原target。相同targetId换物理身份拒绝。 | missing=未配置，unknown v/key、坏current或重复target fail closed。random tmp+fsync+rename发布，但writer异常没有finally sweep；当前无clear/delete API，device removal也不删除配置。它是target选择配置，不是checkpoint或Authority。 |
| D07 / P11 checkpoint envelope与正式target package：P06本机CAS + checkpoint stream；`<target>/<checkpoint-id>/{envelope.json,manifest.json,chunk-##########.bin}` | envelope strict v1 exact并绑定X25519/HKDF/AES-GCM算法、recipient key、1MiB chunk seq/bytes/digest、purpose、domain revisions/upToLsn、nonce/wrapped key、issuer signature和envelope digest；full coverage exact为global/conversation authority、conversation content、execution assets。target manifest strict v1 exact `{v,targetId,checkpointId,envelopeRef,chunks}`；目录名、manifest、envelope及chunk exact-set互绑。 | `AuthorityCheckpointService`先capture完整Authority、把envelope/chunks存P06 CAS并append `checkpoint-created`，再由`FileRecoveryCheckpointTarget`或paired bridge发布，成功后记`replicated`；verify从target读取、解密/重算完整性后记`verified/superseded`。DR inventory、restore/root activation与uninstall preflight只接受完整可读package。 | target在隐藏tmp目录写envelope/chunks、manifest-last、sync后rename目录为唯一发布点；同ID同envelope幂等，冲突拒绝，失败finally删owned tmp。坏/missing manifest不进inventory，read fail closed。verified后才supersede旧包，默认27天后先target retire并记cleanup progress；P06 local checkpoint closure由artifact lifecycle管理，target package不是live Authority。外部directory target还冻结fs identity并拒绝与source同device。 |
| D08 / P11 paired incoming与root binding：`recovery-checkpoint-incoming/<checkpoint-id>/{envelope.json,chunk-*.partial}`、root `root-establishment[.pending].json`、`root-lifecycle-<checkpoint-id>.json[.pending]` | incoming envelope沿D07 strict v1；partial无独立版本，以envelope descriptor绑定seq/length/digest。两类binding**无显式version**但strict exact `{homeId,sourceDeviceId,targetId,checkpointId,recipientKeyId}`；root-establishment是root-wide单值，lifecycle按checkpoint分文件。paired wire command/result均strict v1并绑定home/source/target/recipient generation。 | pair bootstrap、恢复根前置Host及常驻Mesh root lifecycle的`PairedCheckpointReceiver`唯一writer；sender先query durable progress再按≤256KiB range续传，inventory/read/activate-root与root-establishment/lifecycle consumer反读exact binding。 | envelope same-id变更、range越界、full partial digest错、binding冲突均拒绝。全部chunk完成后先让D07 target发布，再把incoming目录rename为envelope-digest绑定的retired名并精确删除；崩溃可从partial或已发布target幂等续跑。binding以pending→rename发布并跨incoming package保留；current-device cleanup整根删incoming，明确保留`recovery-checkpoints`正式target。 |
| D09 / P12 conversation transfer staging：`conversation-transfer-staging/<id>/{artifacts,partials}` | 私有CAS沿digest/path/bytes，partial沿D05 receiver格式；严格v1 `ConversationTransferManifest`绑定conversation/source/target/owner epoch、lastLsn、content assets与authority base `{checkpoint,records,reducerVersion,sessionState}`。`prepared/frozen/imported/committed/aborted/tombstoned`控制record及`conversation-transfer-committed-base` v1都写P06 Authority WAL，staging没有第二journal。 | Mesh target按manifest/source proof拉取、校验reducer/session/authority base，再把closure从私有CAS复制到P06 CAS；随后P06先记frozen/imported。commit把signed commit与committed base同一Authority事务落账后才publish session，source收到commit后fence/tombstone；启动/mesh catch-up按双方P06状态重驱丢失响应。 | partial/CAS可安全按digest续传；abort先记P06 aborted再精确删私有目录。**commit路径没有调用staging cleanup，当前已提交私有副本无retention/delete owner**；current-device cleanup也不含此根。权威仍是P06 commit/base与conversation owner，不能把残留私有bytes当第二owner。 |
| D10 / P12 planned anchor：`anchor-transfer-staging/candidate-claims`、`journals/<id>`、`transfers/<id>/{artifacts,partials,promotion-partials}` | candidate WAL strict v1 exact records：claimed/ready/prepared/terminal/release-delivered，并与target-wide candidate、home/trust/source/target/transfer identity绑定；每transfer私有WAL写strict v1 `mode:"planned"` records及ready reservation，CAS/partial同P06/D05基础格式。candidate WAL使用P06 shared CAS作外置payload backing，private WAL使用private CAS；二者owner不因物理CAS共享而合并。 | source/target owner先durable claim+ready+prepare，source fence后传checkpoint/catalog/pages/retained artifacts到private CAS；target在commit前经`promotion-partials`复制exact closure到P06 CAS，再以`installPlannedAnchorPrefix`原子安装源prefix、trust与`transfer:anchor-current`。启动前completion从P06 current installation + private journal + SecretStore exact key前滚，candidate最后terminal。 | import/commit/abort均按同identity幂等；P06 install是owner线性化点，之前candidate/private状态不是当前Authority。abort删private transfer root/key但保留journal/candidate；unprepared release另删transfer+journal；install/post-install完成删`transfers/<id>`并释放readiness，candidate/private journal无通用GC，供exact replay。 |
| D11 / P12 disaster recovery：`disaster-recovery-staging/candidate-claims`、`journals/<id>`、`transfers/<id>/{artifacts,partials,promotion-partials}` | candidate WAL strict v1 exact claimed/verified/install-decided/terminal；prepare存canonical JSON，verified与install decision为canonical artifact ref且实际bytes位于P06 shared CAS。private WAL outer exact v1 `{v,t:"disaster-recovery-record",recordJson}`，inner为strict v1 `mode:"disaster-recovery"`状态机；私有CAS/partial与promotion partial同上。 | DR command从D07 package、现场root和peer trust evidence建立candidate，私有import保存Authority record set/catalog；candidate先耐久install decision及candidate refs，随后`installPlannedAnchorPrefix`把source prefix、revoke/transition/trust、`transfer:anchor-current`原子装进P06。启动前completion校验P06 installation、private journal、candidate decision与issuer key；consumer recovery/read-back完成后P06再写strict v1 post-install receipt。 | verified/decision/terminal重放必须canonical equal；有install decision后禁止abort。abort删private transfer root并释放readiness；install后先保留private root满足重启completion，post-install receipt落P06后才删`transfers/<id>`。candidate/private journal继续保留；current-device removal可整根删DR staging，但只有在设备生命周期完成排空/退役后执行，最终trust/install/receipt始终只在P06。 |

##### 跨根线性化、恢复与 Authority 裁决

1. **P08只耐久传输，不决定工作事实。** P06 assignment/ticket先赋权，spool按同一`ExecutionRef`保存可重放frame与ACK；interaction/evidence结果经consumer或Advancement owner再提交业务事实。ACK、prune或evidence completed都不能替代assignment terminal、run commit或transcript。
2. **P09投影/partial不可反写trust或grant。** endpoint/peer/completion与continuation只让Mesh重新连接/继续pair；P06 pairing/trust event才是入网线性化点。Surface/mesh partial完成只证明bytes校验通过，P06 grant、retained ref或assignment authorization才允许采用；device removal删除本机投影不撤销P06事实。
3. **P11有三段耐久边界。** P06 `checkpoint-created`固定本机closure及原target generation；target目录rename只证明冻结副本已发布，随后P06 `replicated/verified`才建立可恢复/可supersede事实。paired incoming和root binding只提供续传/串行激活前置，不能成为inventory Authority；崩溃分别从created obligation、incoming progress、已发布target或P06 activation commit前滚。
4. **P12三条链共享“私有验证后单向安装”，但删除不同。** conversation在P06 commit/base原子事务处切owner；planned/DR在P06 `installPlannedAnchorPrefix`处切当前Anchor/trust generation。promotion partial只是从private CAS到P06 CAS的可重试拷贝，candidate decision/private journal只守住安装前后幂等。source fence、target commit响应、post-install响应任一丢失都由P06 current installation/commit与私有记录交叉读回，不产生第二状态机。
5. **删除集合不是目录前缀猜测。** current-device cleanup精确包含P09 endpoint/peer/completion、asset temp/partials、P11 incoming和P12 DR staging；不包含P08 spool/evidence、P09 pairing continuation、P11正式checkpoint/target配置、P12 conversation/planned staging。各自上表所列terminal/retention owner是唯一自动清理者；不存在通用staging retention、backup源或按目录年龄猜删。

**正反全等、兼容与排除。** 正向从Anchor+local/Executor-only Host、pair/backup/DR命令、Mesh runtime与恢复根前置Host进入`ExecutorDataPlaneRuntime`、两个`EvidenceJournal`装配、`FileMeshBootstrapStore`/pair continuation、surface/mesh receivers、checkpoint owner/targets、conversation/planned/DR transfer target；反向从四族精确root、全部`FileAuthorityCommitLog/FileArtifactStore/FileResumableArtifactReceiver`构造、record discriminant、inventory/restore/recovery、maintenance/retention和`rm/retire/device-removal`回扫，成员与D01～D11全等。P06 assignment/trust/grant/commit/install/lifecycle records、P02 transcript、P07 execution snapshots只是上述链的权威输入/终点，不重复计为P08/P09/P11/P12格式；测试fixture、内存transport与协议类型但无production writer的候选不增加成员。本四族没有schema upgrader或旧格式migration；D02 journal顶层宽松与恢复bundle弱交叉绑定、D03 outer宽松、D04 shallow continuation以及各处缺版本都是当前reader事实，不得伪称strict v1或扩写成兼容承诺。

**A0-06a纠正与证据失效。** P08/P11顶层root、owner与生命周期简写成立；P09“device removal删除本机投影/staging”被精化为不含`mesh-pairing-continuation.json`，P12“conversation commit/abort后均清理”被反证为仅abort清理、commit后无owner，二者已在P09/P12主表恢复。D02初次把writer shape误写成恢复reader exact合同、并把首次`complete()`的bundle交叉绑定外推到恢复期，现已按四阶段调用链恢复；它只影响P08 schema/compatibility子证据，不影响P08 root/owner、非Authority裁决、retention/delete或D01/D03～D11。以后若D01 spool codec/ACK/reclaim、D02 writer/`#load`/replay/complete/compact或handler pre-replay guard、D03～D05 Mesh/pair/asset writer或cleanup、D06～D08 checkpoint envelope/target/incoming/binding/lifecycle、D09～D11 transfer candidate/private WAL/promotion/install/post-install或device-removal exact-set变化，恢复本证据与A0第二类索引；若同时改变顶层root、唯一owner或删除生命周期，再恢复A0-06a对应P项。P01～P07/P10/P13～P15不因此失效。

#### A0-06b3b P10/P13～P15 本机 Host 存储 schema、兼容与生命周期 exact-set

证据 ID：`A0-06b3b-local-host-storage-schema-v1`。本证据把本机 workspace 管理入口、三种持久 Host 拓扑与 foreground/on-demand/managed 启动形态共同使用的协调记录、认证事实、生命周期投影、托管定义和诊断日志拆成独立 subfamily。它们可以共享 `<home>` 或同一 Host 进程，但不会因此成为第二 Host、第二 Domain Authority 或统一生命周期记录。

##### record family、schema/version、writer/reader 与终态

| ID / A0-06a 族与物理位置 | 当前 writer schema、版本与 reader 接受域 | 唯一 producer、reader/consumer | 原子性、恢复/清理/retention 与目标责任 |
|---|---|---|---|
| H01 / P10 outbox：`runtime/local-workspace-operation-outbox/operations.ndjson` 与 sibling `.established` | checkpoint envelope exact `{checkpoint,digest}`，checkpoint v1 exact `{v,outboxId,nextSeq,confirmedThroughSeq,confirmedPrefixDigest}`；event v1 exact `{v,eventSeq,previousDigest,digest,operation}`。operation exact discriminant 为 `create/rename/repath/remove/reset`，状态为 `prepared/committed/completed/abandoned`。writer写canonical NDJSON；reader严格校验字段、digest、序号与状态转换，但不要求每行文本等于canonical序列化。 | `LocalWorkspaceManagementHost`经唯一`LocalWorkspaceOperationOutbox`写；同一Host的mutation prepare/commit/recovery/ack读，workspace status/list不读outbox。P07 workspace binding仍是业务事实，outbox只保证committed-forward。 | append后fsync；compact写临时文件、rename并sync目录。ack先耐久marker，再按marker重放/compact，故marker领先log也可恢复；missing file/marker组合、坏序列或digest冲突fail closed。prepared默认15分钟后abandon；committed前滚，completed/abandoned交付后ack。close保留未收束durable operation供下次恢复，device removal才整根删除。Correctness归A4，Workspace Domain消费归A5，文件实现归A6。 |
| H02 / P10 owner/transport与请求 gate：`local-workspace-management.owner.lock`、`local-workspace-management.transport.json`与tmp、Unix socket / Windows named pipe | owner lock沿A0-06b1通用v1 lease合同。transport publication是canonical strict v1 `{v,user,token}`，token为43字符base64url；request/response均strict v1并绑定同一transport secret。Unix socket名称由home digest决定且chmod 0600；Windows pipe由OS命名空间持有，无盘上socket schema。 | local workspace command fallback与持久Executor Host竞争同一owner；唯一server先listen，再用temp→rename发布secret。`#handle`先拒绝created/closed，然后让`host-status/status/list`直达；其余请求先过outbox-ready。`pending/acknowledge`到此即可；`prepare`与commit一个prepared identity再要求state=`ready`；commit一个已`committed/completed` identity跳过writable gate，completed直接返回，committed触发/复用`#drainLoop`。 | recovering时查询可用，outbox就绪后pending/ack可用且committed/completed可重放，只有新prepare/prepared commit仍被拒；ready时全部合同可用；draining时transport先unpublish/close阻止新连接，但已经进入handler的查询、outbox-only请求与durable replay不因state本身被拒，close等待drain/recovery/mutation settle。`#drainLoop`只前滚committed，队列清空才置ready；crash残留由下一owner收口。ApplicationHost归A1，Product API归A3，IPC/lease归A6。 |
| H03 / P13 `server.pid`、`server.port` | PID当前writer为pretty JSON v2：`pidFileVersion,pid,port,startTime,startedAt`及可选`host,argv,kind,version,logPath`；非原子直接写。reader只要求数值pid/port，缺version归一为v1、`startTime:null`，额外字段、未知数值version和不合规可选字段仍会接受；坏JSON/非数值返回null。port是plain decimal，无显式版本且当前无production reader。 | listener成功后`acquireLock`覆盖写；discovery/status/stop/daemon只以PID metadata定位候选并以健康/进程/generation事实继续验证，port文件只是shell-friendly外化。 | 正常release best-effort删pid/port且吞删除错误；injected cleanup只删当前pid匹配项，stop的stale cleanup还核对generation tuple。残留PID不能获得listener所有权，也不是Host事实源。生命周期归A1，process-file adapter归A6。 |
| H04 / P13 `server.state`、`server.ready` | state无显式版本，writer为pretty JSON `{phase,pid,startedAt,lastHeartbeat,port?,host?,exitReason?,extensions?}`；fixed `.tmp`→rename，无fsync。reader直接`JSON.parse`后按类型使用，没有顶层exact/canonical validator，失败返回null。ready是空文件、无schema，仅background generation使用。 | `ServerStateFile`写starting/ready/running/heartbeat/stopping/stopped；daemon parent以ready+pid alive+HTTP health联合判ready，status与mesh compatibility读state；foreground也写state但不写ready。 | ready前先写state再marker，随后running；有序关闭写stopping/stopped后删state/ready，executor-only也走显式cleanup。crash或清理失败可留下最后投影，下一发现/启动链不能把残留marker单独当ready。它们是Host lifecycle projection，归A1/A6。 |
| H05 / P13 `server.token` | 无显式版本。新token writer写64字符小写hex加换行，随后POSIX chmod 0600（Windows无POSIX chmod语义）；直接写、无rename/fsync。既有reader仅`trim()`并要求长度至少32，**不校验hex或exact长度**；不足32会重新生成，任意更长非空文本会被接受。discovery client另只要求trim后非空。 | Anchor+local与executor-only Server启动均调用`loadOrCreateToken`；Server RPC auth比较token，本机CLI/discovery/stop客户端读取同一文件。 | 满足接受域的旧值跨Host generation和重启复用。stop、device removal、program uninstall都没有删除入口，当前无production删除owner；partial短写会被下一次启动替换，长/非hex残留会继续接受。它是持久本机RPC共享认证事实，不是generation marker。认证边界归A3/A4，Secret/file adapter归A6。 |
| H06 / P13 active/rotated/legacy server log：`logs/server/server.log`、`server-<timestamp>-<seq>.log`、legacy `server.log` | 无schema version的人类文本。on-demand daemon parent以append fd把child stdout/stderr显式重定向active；Host log lifecycle负责scan/rotate/prune。active不存在且legacy存在时才尝试exclusive copy；成功复制后删除legacy，active已存在、`EEXIST/ENOENT`或copy失败时legacy保留。 | Host启动/daemon output是writer；`serve logs`只读/跟随active，operator可读rotated/legacy；没有Domain、Authority或恢复reader。managed OS definition当前未显式重定向stdout/stderr到active。 | rotate为exclusive copy后truncate active；active超过8MiB触发，rotated best-effort受5文件、14天、64MiB总量约束。正常close只停maintenance，不删active/rotated；日志跨重启保留。Observability归A3/A6。 |
| H07 / P14 Windows Task Scheduler：`distributed-runtime/managed-service/<service-id>.xml` + OS task | 完整service ID为`dev.zhixing.host.<sha256(osUser + NUL + canonical-home)前24hex>`。definition为UTF-16LE BOM XML、Task schema 1.4，bytes由当前config/SecretStore/trust/role plan canonical生成；OS inspect把Task投影解码成exact identity/principal/logon-trigger/action/settings并与spec核对。 | 唯一adapter先分别读取definition与Task。Task存在时，文件缺失/不等或Task投影不匹配都会令`matches:false`，install/disable/stop/unregister在各自exact gate fail closed。Task absent时整体state=`absent`；install仍进入definition materialization，文件缺失则原子创建，byte-equal orphan幂等复用，mismatched orphan由`writeDefinition`拒绝且不覆盖，然后才`/Create /F`并严格read-back。 | file→manager安装与manager→file删除均非事务。normal unregister先`/Delete`、再删definition并回读absent；若Task已删而file删除失败，下一inspect为absent（canonical orphan时`matches:true`），带spec的program uninstall/current-device cleanup可按manager absent收敛并留下file。registration只启动A0-02同一Host，归A1/A6。 |
| H08 / P14 macOS LaunchAgent / Linux systemd user：`~/Library/LaunchAgents/<service-id>.plist`、`~/.config/systemd/user/<service-id>.service` + OS registration | 使用与H07相同的完整service ID。macOS是UTF-8 plist 1.0；Linux是UTF-8 unit、无显式format version；definition bytes canonical且file侧按byte equality核对。launchd/systemd manager inspect只投影registration的absent/enabled/disabled/running，存在时`manager.matches`恒true，不反读已加载definition bytes，故弱于Windows。 | shared `inspect/install`仍把file equality与manager projection合成`matches`：manager存在且file缺失/漂移会在install前拒绝；manager absent则进入与H07相同的物化分支，missing创建、equal orphan复用、mismatched orphan仍拒绝。mac install为bootstrap+enable，Linux为可选linger+daemon-reload+enable，随后都按manager state与file bytes read-back。 | exact unregister为manager-first、file-second，Linux删除后另daemon-reload；normal路径全清。manager删除后file删除失败时，带可重建spec的maintenance/program uninstall只看manager absent便可commit；无SecretStore时maintenance还要求`matches:true`、program uninstall另要求file absent。current-device cleanup在expected/actual均absent时直接返回，缺spec时也跳过unregister，故三平台orphan均可越过该边界。该现状是迁移输入，不是兼容承诺。 |
| H09 / P15 `logs/llm-raw/*`、`logs/llm-error/*` | 均为无版本人类文本。raw仅REPL `--log`启用，按process单例fd记录LLM request/stream/end；error在Agent run失败时自动记录错误、最近请求摘要及system prompt前缀。内容可能敏感，但不具领域codec。 | CLI render/EventBus/error instrumentation唯一写；stderr提示与operator人工诊断读。反向没有Domain、Authority、checkpoint、backup、recovery或产品API consumer。 | 两目录在写入及若干CLI启动入口best-effort按mtime各保留最近7个，目录内所有普通文件都参与而不按prefix过滤；close依赖进程fd退出，无业务恢复。归Observability/CLI Infrastructure（A3/A6）。 |
| H10 / P15 `logs/keypress-<pid>-<time>.log` | 无版本人类文本，仅`--log`时sync append键盘输入诊断；process内lazy singleton。 | CLI input instrumentation唯一写，只有operator人工读取；无产品consumer。 | 当前无rotation、retention或delete owner；stdin状态poll使用2秒unref timer且无显式dispose，文件/timer随进程终止。它既不参与Host ready/drain，也不进入backup或Domain。 |

##### 跨根生命周期、唯一责任与迁移裁决

1. **P10只有一个management owner，但请求 gate 分三层。** 持久Executor Host与命令期fallback竞争同一lease、发布同一transport、打开同一outbox；client路径从不自建第二workspace owner。`host-status/status/list`不需要outbox；`pending/acknowledge`只需要outbox-ready；只有创建新prepared事实或把prepared推进为committed才需要Host-ready。已有committed必须由`#drainLoop`前滚，completed可按原identity直接重放，所以recovering不是durable replay的拒绝理由；close撤入口后仍等待已经接受的这些操作settle。
2. **P13四类 subfamily 不可统一清理。** listener/进程所有权不由pid/port赋予；state/ready只是当前generation投影；token跨generation持久认证；server log跨重启受retention治理。正常stop精确清pid/port/state/ready却保留token与日志，异常残留也必须分别由discovery、认证或log lifecycle处理，不能用`server.*`前缀推导共同生命周期。
3. **P14 definition与OS registration是相邻投影，不是事务或第二Host。** 完整identity是`dev.zhixing.host.<24hex>`，install以file→manager、unregister以manager→file推进。manager存在时file或manager投影drift会fail closed；manager absent时install进入当前canonical definition物化，但writer只创建missing或复用byte-equal orphan，仍拒绝并保留mismatched orphan。OS manager成功删除后definition可残留，maintenance/uninstall/device cleanup又按各自spec可用性与absent判定收敛，不能把现状误写成all-or-nothing或“任何drift都在install前拒绝”。registration最终仍只启动A0-02已冻结的同一CLI Host组合根。
4. **P15只产生诊断副作用。** raw/error/keypress没有schema consumer、恢复责任或backup入口；LLM raw/error的bounded prune与keypress无retention是三个不同治理事实。即使日志含请求、提示或输入，也不能据此提升为Run/Conversation/Authority事实。
5. **current-device cleanup与程序卸载不是通用home清扫。** current-device路径精确删除P10 runtime root并尝试P14 unregister；manager已absent时exact unregister直接返回，缺spec时callback也不处理definition。带可重建spec的program uninstall同样把manager absent视为已提交；只有无SecretStore binding分支额外要求definition file absent。它们都不删除P13 token/log。P13 Host stop、P14 OS unregister和P15 prune各有独立owner，不存在跨P10/P13～P15的通用retention、backup或恢复状态机。

**正反全等、兼容与排除。** 正向从foreground/background/managed/on-demand Host、local workspace command/fallback、status/stop/logs/doctor、managed install/reconcile/uninstall与current-device cleanup进入P10/P13～P15；反向从`runtime/local-workspace-*`、`server.*`、`logs/server`、三平台definition路径、Task/launchd/systemd adapter、`llm-raw/llm-error/keypress` writer以及全部`write/rename/unlink/rm/prune/releaseLock/cleanup/unregister`入口回扫，成员与H01～H10全等。P10 Unix socket与Windows named pipe只是OS endpoint，P14 OS registration不是盘上第二schema，P15进程fd/timer不是持久record；测试fixture、内存transport、历史`server.log`以外无production reader的候选均不增加成员。P10请求级gate、宽松PID/state/token reader、definition/registration分段终态、manager-absent orphan接受/拒绝差异和keypress无retention均只登记为迁移输入，不扩写成兼容或产品承诺。

**A0-06a纠正与证据失效。** P10顶层root、唯一owner、committed-forward和device-removal结论成立；初次H02的“全部durable mutation双gate”已由查询/outbox-only/new-write/durable-replay四类恢复。P13的四种生命周期及H03～H06不受影响。P14顶层owner和“不形成第二Host”成立；初次H07/H08的裸hash identity与统一drift拒绝已恢复为完整ID、manager-present drift、manager-absent materialization及outer absent convergence四段。以后若P10 `#handle/#tryInitializeOutbox/#drainLoop`、outbox codec/ack、lock/transport/socket/pipe，或P14 `buildManagedServiceSpec`、definition writer、三平台manager inspect/install/unregister、maintenance/uninstall/device cleanup变化，恢复本证据与A0第二类索引；只有同时改变root、唯一owner或删除生命周期时才恢复A0-06a对应P项。H01、H03～H06、H09～H10及P01～P09/P11/P12不因此失效。

#### A0-07a 生产 package、exports、构建图与版本绑定 exact-set

证据 ID：`A0-07a-package-export-build-v1`。本证据冻结源码工作区到 `dist` 的静态交付边界，不用已有 `dist`、README 或旧制品反推当前源码。`pnpm-workspace.yaml` 的匹配式为 `packages/*`、`packages/channels/*`、`apps/*`，当前 `apps/` 不存在；`pnpm -r list --depth -1 --json` 机械取得 18 个 project node：私有根项目 `zhixing`、下表 16 个 manifest-declared public package 和私有 `@zhixing/test-utils`。根项目 `name=zhixing,private=true,packageManager=pnpm@10.8.0`，没有 `main/types/exports/files/bin/publishConfig`，只有 `@biomejs/biome@^2.4.14` 与 `tsx@^4.21.0` 两个 dev dependency；它只承担版本/脚本/toolchain 协调，不是发布包。递归 package 命令当前选择 17 个非根 workspace package。

##### package manifest、依赖与公开面 exact-set

共同合同：根和 17 个 package 均为 `version=0.1.0`、`type=module`、`engines.node=">=24.0.0"`；17 个 package 的 `files` 都含 `dist` 且均无 `main`。除 `@zhixing/test-utils` 明确 `private:true`、无 `publishConfig` 外，其余 16 个未设置 `private` 且均为 `publishConfig.access=public`。所有 library export 都是显式、无 wildcard 的 `{types:./dist/**.d.ts, import:./dist/**.js}` ESM 条件；CLI 没有 `exports/main/types`，唯一正式入口是两个同目标 bin `zhixing`、`zz` → `./dist/index.js`。顶层 `types=./dist/index.d.ts` 只存在于 executor、mesh、orchestrator、owner-kernel、owner-services、rpc、runtime-host、secrets、server、tools-builtin；其余 library 仍由每个 export 的 `types` 条件提供声明。下表 `未跨包消费` 是当前仓库非测试生产 AST 的差集，不表示可任意删除的外部合同。

| ID / package 与交付身份 | `exports` exact-set；特殊 manifest 字段 | production `dependencies`（workspace；外部） | 当前生产 consumer、未消费键与 AE-001 归属 |
|---|---|---|---|
| K01 `@zhixing/channel-feishu` / public Channel | `.` | core；`@larksuiteoapi/node-sdk@1.60.0` | CLI 动态装配消费 root；无未消费键。Surface/Channel binding → A3/A6。 |
| K02 `@zhixing/cli` / public executable | 无 exports；bin=`zhixing,zz`；`files=dist` | channel-feishu、core、executor、mcp、mesh、network、orchestrator、owner-kernel、owner-services、providers、rpc、runtime-host、secrets、server、tools-builtin；`chalk@5.6.2`,`cli-highlight@2.1.11`,`commander@14.0.3`,`marked@18.0.3`,`qrcode@1.5.4` | 没有 library consumer；bin 是 A0-01 已冻结进程入口。Surface → A3，组合根/Host 启动选择 → A1，后端实现须在 A5/A6 迁出。 |
| K03 `@zhixing/core` / public shared library | `.`,`./advancement`,`./authority`,`./context`,`./contracts`,`./conversation`,`./delivery`,`./environment`,`./events`,`./loop`,`./orchestration`,`./persistence`,`./protocol`,`./resilience`,`./resources`,`./rubrics`,`./scheduler`,`./security`,`./test-support/s7-durable`,`./test-support/s7-durable-harness`,`./transcript`,`./types` | 无 workspace；`cron-parser@5.5.0`,`fuse.js@7.3.0` | 几乎全部生产包消费；未跨包消费 `context,conversation,events,loop,orchestration,resilience,scheduler,test-support/s7-durable,transcript,types`。最小公共原语/Correctness → A4；领域合同归位与根面收窄 → A5/A7。 |
| K04 `@zhixing/executor` / public adapter | `.`,`./assignment-ledger`,`./assignment-stream-spool`,`./data-plane-ticket-registry`,`./runtime-role` | core、owner-kernel、runtime-host；无外部 | CLI 消费 root、stream-spool、ticket-registry；未跨包消费 assignment-ledger/runtime-role。执行适配 → A6。 |
| K05 `@zhixing/mcp` / public adapter | `.` | core、network；`@modelcontextprotocol/sdk@1.29.0` | CLI/runtime-host 消费；无未消费键。工具边缘适配 → A6。 |
| K06 `@zhixing/mesh` / public topology adapter | `.`,`./anchor-transfer-ready`,`./blind-relay`,`./blind-rendezvous`,`./bootstrap`,`./bootstrap-authority`,`./canonical`,`./checkpoint`,`./checkpoint-owner`,`./checkpoint-service`,`./checkpoint-target`,`./credential-exposure`,`./device-identity`,`./device-key-store`,`./device-readiness`,`./errors`,`./full-checkpoint`,`./handshake`,`./outbound-tunnel`,`./paired-checkpoint-target`,`./pairing`,`./recovery-package`,`./recovery-root`,`./replay-window`,`./request-channel`,`./service-registry`,`./transport`,`./trust-chain`；`files` 另列 Windows helper exe+descriptor | core；`@peculiar/webcrypto@1.7.1`,`@peculiar/x509@2.0.0`,`reflect-metadata@0.2.2` | CLI 消费除 `blind-relay,canonical,device-readiness` 外的 25 键；这 3 键当前无跨包生产 consumer。设备/拓扑适配 → A6。 |
| K07 `@zhixing/network` / public Infrastructure | `.` | 无 workspace；`undici@7.25.0` | CLI、mcp、tools-builtin 消费；无未消费键。Network port implementation → A6。 |
| K08 `@zhixing/orchestrator` / public Kernel assembly | `.`,`./advancement`,`./confirmation`,`./orchestration`,`./profile`,`./runtime`,`./security`,`./subagent`,`./tools` | core、network、providers、tools-builtin；无外部 | CLI/runtime-host 消费 root、advancement/profile/runtime；未跨包消费 confirmation/orchestration/security/subagent/tools。封闭 Intelligence Kernel → A4。 |
| K09 `@zhixing/owner-kernel` / public Correctness/temporary domain mix | `.`,`./confirmation-hub`,`./control-admission`,`./conversation-assignment`,`./conversation-manager`,`./delivery`,`./ephemeral-run-buffer`,`./job-assignment`,`./run-turn`,`./runtime`,`./test-support/s7-durable`,`./types` | core；无外部 | CLI/executor/rpc/runtime-host/server 消费 root、confirmation-hub/conversation-manager/run-turn/types；其余 7 键无跨包生产 consumer。通用 Authority/Commit/Recovery → A4，领域 reducer/语义 → A5。 |
| K10 `@zhixing/owner-services` / public domain-candidate library | `.`,`./advancement`,`./advancement/controller`,`./advancement/proxy-content`,`./advancement/proxy-scheduler`,`./advancement/recovery-maintenance`,`./advancement/review-dispatch` | core；无外部 | CLI/server 只消费 root，6 个 advancement 子路径均无跨包生产 consumer。领域单元候选 → A5。 |
| K11 `@zhixing/providers` / public provider adapter | `.` | core；`@anthropic-ai/sdk@0.85.0`,`jsonc-parser@3.3.1`,`openai@6.33.0` | CLI/orchestrator 消费；无未消费键。Provider/config source → A1/A6。 |
| K12 `@zhixing/rpc` / public transport binding | `.`,`./confirmation-bridge`,`./connection`,`./event-bridge`,`./session-broadcast`,`./session-events`,`./session-turn-stream`,`./session-wire` | core、owner-kernel；无外部 | CLI/server 消费除 confirmation-bridge/connection 外 6 键；这 2 键当前无跨包生产 consumer。Product API RPC binding → A3。 |
| K13 `@zhixing/runtime-host` / public Runtime assembly | `.`,`./builtin-extra-tools`,`./runtime-host`,`./segment-deps`,`./session-adapter`,`./workmode-tools`,`./workscene-port` | core、mcp、orchestrator、owner-kernel、tools-builtin；无外部 | CLI/executor 消费 root/session-adapter；其余 5 键当前无跨包生产 consumer。Runtime factory → A4，生命周期贡献 → A1。 |
| K14 `@zhixing/secrets` / public Infrastructure | `.` | core；无外部 | CLI 消费；无未消费键。SecretStore adapter/source boundary → A1/A6。 |
| K15 `@zhixing/server` / public transport host | `.` | core、owner-kernel、owner-services、rpc；`ws@8.20.0` | CLI 消费；无未消费键。Product API transport/认证/连接 → A3，listener 生命周期 → A1。 |
| K16 `@zhixing/test-utils` / private test infrastructure | `.`；`private:true`，无 publishConfig | 无 dependency；peer=`vitest@^3.1.1` | 只被测试源码消费，非生产依赖、非发布候选；verification Infrastructure → A7。 |
| K17 `@zhixing/tools-builtin` / public tool adapter | `.` | core、network；`diff@9.0.0`,`glob@13.0.6`,`turndown@7.2.4` | CLI/orchestrator/runtime-host 消费；无未消费键。Tool implementation → A6，Kernel 只消费工具合同 → A4。 |

开发依赖也已逐包反查：17 包共同声明 `@types/node@^24.0.0`,`tsup@^8.4.0`,`typescript@^5.8.3`；除 owner-services、rpc、runtime-host 外其余 14 包声明 `vitest@^3.1.1`。cli、core、executor、mesh、orchestrator、owner-kernel、providers、secrets、server、tools-builtin 以 `devDependencies.workspace:*` 消费私有 test-utils；没有生产源码消费它。额外开发依赖只有 CLI 的 `@types/qrcode@1.5.6`、mesh 的 `@cipherman/pake-js@0.1.1`、server 的 `@types/ws@^8.5.13`、tools-builtin 的 `@types/turndown@^5.0.5`。所有 package 均无 optionalDependencies，除 test-utils 的 vitest 外无 peerDependencies。

##### 构建入口、输出与依赖顺序

1. **共同 tsup 合同。** 17 个 package 都有 `build`；除 mesh 为 `node scripts/build-checkpoint-child-bridge.mjs && tsup` 外均直接 `tsup`。全部 config 明确 `format=esm,sourcemap=true,clean=true,target=node24`，输出目录使用 tsup 默认 `dist`；除 CLI `dts=false` 外均 `dts=true`。当前 lock 实际解析 `tsup@8.5.1`：未覆写的 `bundle` 默认为 true，ESM 未覆写的 `splitting` 为 true，manifest `dependencies+peerDependencies` 被 external；test-utils 另显式 external `vitest`。CLI 唯一增加 JS shebang。所有 package tsconfig 继承根 strict ES2023/Node16 config，固定 `rootDir=src,outDir=dist`、包含 `src/**/*.ts`、排除测试；没有 `paths/baseUrl` 或 project references。
2. **entry exact-set。** 单一 `src/index.ts` 的是 channel-feishu、mcp、network、providers、secrets、server、test-utils、tools-builtin；CLI 是命名 entry `index=src/index.ts`。executor 为 `index,runtime-role,assignment-ledger,assignment-stream-spool,data-plane-ticket-registry`；orchestrator 为 `index,runtime,security,profile,subagent,confirmation,tools,orchestration,advancement` 目录/文件入口；owner-kernel 为 `index,runtime,types,conversation-manager,run-turn,ephemeral-run-buffer,confirmation-hub,control-admission,conversation-assignment,job-assignment,delivery,test-support/s7-durable`；owner-services 为 root 与 6 个 advancement 路径；rpc 为 root、connection、session-wire、session-turn-stream、session-broadcast、session-events、confirmation-bridge、event-bridge；runtime-host 为 root、session-adapter、runtime-host、builtin-extra-tools、segment-deps、workmode-tools、workscene-port。mesh 的 28 个 entry 与 K06 exports 一一对应，唯独公开键 `./pairing` 映到源/产物 `pairing-public`。
3. **core 特例与 entry/manifest 全等。** core 的 22 个 export target 均有同路径 tsup entry；另构建 `src/confirmation/index.ts` 和 `src/identity/index.ts` 两个物理 entry。二者由 core root barrel 重导出但没有独立 package export，也没有跨包深导入，因此是 `files=dist` 内的非正式子路径产物，不是第二公开入口。除此之外，所有 manifest `import/types/bin` target 都能由相应 entry 推导，无“正式 target 未构建”；也没有第二 bin/main。
4. **mesh native 前置。** Windows 分支仅允许 x64，使用系统 `csc.exe` 从 C# 源生成 `build/Release/checkpoint_child_bridge.exe`，再以 package version/bytes/SHA-256 写 schema v1 descriptor；非 Windows 分支运行 `node-gyp rebuild`。manifest 只把 Windows exe+descriptor 额外列入 `files`；实际 tarball成员、安装后 helper与Windows smoke由A0-07b裁决，本证据不把源码目录或已有 helper当正式制品。
5. **根 build 与 DAG。** 根 `build` 先执行 `release:version:check`，再 `pnpm -r run build`；pnpm 10.8.0 默认 topological sort、workspace concurrency 4，不包含根自身。由 production+dev workspace edge 得到稳定层级：`network,test-utils` → `core` → `channel-feishu,mcp,mesh,owner-kernel,owner-services,providers,secrets,tools-builtin` → `orchestrator,rpc` → `runtime-host,server` → `executor` → `cli`；同层可以并行，不把一次输出顺序冻结成串行合同。全仓没有通用 package `typecheck` script；只有 core 的 `typecheck:contracts`（contracts index/invariants，noEmit）和 `lint:contract-schemas`（结构脚本 + schema/invariants tsc）。CLI 因 `dts=false` 不从 package build 取得 declaration/typecheck，这只是当前门禁输入，不推导为目标能力。

##### 生产 import、门禁、版本与差集裁决

- **生产 import 全等。** 用 TypeScript AST 从 17 包的非测试 `src/**/*.ts` 重取 import/export/import-type/字面量 dynamic-import，再对 manifest反查：所有跨包 specifier 都指向已声明 `dependencies.workspace:*` 及目标显式 export，缺包、缺 export、未声明 production workspace dependency均为 0；跨 package相对源码导入为0，所有tsconfig path alias为0。唯一反向差集是 orchestrator manifest 声明 `@zhixing/network@workspace:*`，其生产源码没有 import；它是当前未消费依赖和多余DAG边，不是隐藏能力。test-utils只出现在测试源码并由实际消费者声明devDependency。
- **公开但当前仓库生产未消费。** 各包未消费键已在K03/K04/K06/K08～K10/K12/K13逐项列出；它们仍是manifest公开面，A7必须按真实外部/结构职责裁决，不能因本仓暂无consumer直接删除。core与owner-kernel的`./test-support/*`虽由public manifest正式导出，其中跨包消费也只发生在另一份test-support构建入口，属于结构验证基础设施而非产品runtime。core的两个额外built entry相反：有物理产物但没有独立export，只经root公开。由此当前105个public export键、1个private test export键和CLI双bin均有唯一分类。
- **现有 runtime export gate 不是全包 exact-set。** `scripts/check-runtime-package-exports.mjs` 直接导入23个既有`dist`文件，核 selected symbol identity/retired name；只对core逐个遍历manifest export，验证`types/import`指向可访问/可加载dist并禁止`./memory`。它没有遍历其余15个public package的manifest，不核CLI bin/files/top-level types、完整export key/target或生产import/dependency差集，且直接文件路径不能证明installed package resolution。故其现有识别力保留，但不得用该门禁或旧dist自证A0-07a全等；A0-07b再以新build、真实pack/install补制品闭包。
- **统一版本绑定。** 根`package.json.version=0.1.0`是唯一release version source；17个package（包括private test-utils）当前必须先人工保持同值，CLI生成投影必须字节等于`export const ZHIXING_RELEASE_VERSION = "0.1.0";`。`release-version.mjs`先严格校验根canonical SemVer，再递归`packages/**/package.json`；check模式对任一package mismatch或CLI投影stale fail closed。`--write`若有package mismatch也拒绝，只在全部已相等时重写CLI生成文件；它不修改package manifest、lockfile或任何`apps`。本轮只运行check模式并通过，没有执行同步或写入；通过只证明静态版本绑定，不证明发布或tarball版本。

**正反全等、当前差异与证据失效。** 正向从workspace/root/package manifests、105+1个exports和CLI双bin、17份tsup/tsconfig、root build与version script取得集合；反向从生产AST import、所有entry/target、相对导入/alias、workspace edges、runtime export gate和CLI生成版本回扫，未发现遗漏package、第二bin/main、隐藏export、正式target未构建或未声明生产依赖。已知迁移输入只有：orchestrator未消费network dependency；core两个非exported built entry；一批正式export当前无仓内生产consumer；runtime export gate非全包exact-set；CLI build不产d.ts且没有独立typecheck。以后若workspace patterns或package manifest、production import、root/package scripts、tsup/tsconfig/lockfile解析、mesh helper build、`check-runtime-package-exports.mjs`、release-version脚本/CLI投影任一变化，恢复本证据与A0第二类索引；实际pack内容、workspace协议改写、安装解析、发布顺序、Windows helper运行只使A0-07b失效，不得反向用其结果改写本静态集合。

#### A0-07b 正式 tarball、安装/运行入口、发布装配与 Windows x64 smoke

证据 ID：`A0-07b-package-install-smoke-v1`。本证据以 A0-07a 的 K01～K15、K17 为输入，在 `HEAD 25fac98902e1e8e745e8840748c6fcaa94ff605a` 上只运行一次 canonical `pnpm package:check`；它先 fresh build，随后从该次 `dist` 逐包生成并消费真实 tarball，不使用进场旧制品自证。正式 tarball identity exact-set 是 `@zhixing/channel-feishu`、`cli`、`core`、`executor`、`mcp`、`mesh`、`network`、`orchestrator`、`owner-kernel`、`owner-services`、`providers`、`rpc`、`runtime-host`、`secrets`、`server`、`tools-builtin` 各 `@0.1.0`，即排序后的 16 个 `zhixing-<name>-0.1.0.tgz`；私有 `@zhixing/test-utils`、根项目和源码目录均不进入 pack/install 集合。本次 16 个 tarball 按包名与 bytes 聚合得到 SHA-256 `356aac58b581d1f2ed7b1885dddc39bdc96c6cba53387c722b7cac7d9b38ecc3`，该摘要只标识此次 fresh-build tarball 集合，不是发布签名或 registry integrity。

##### canonical package check 状态机与实际证明

| 阶段 | 当前实现、通过事实与信任边界 |
|---|---|
| C01 平台与隔离 | `package-check.mjs` 只接受 `win32/x64`；在仓库根创建唯一 `.zhixing-package-check-*`，把大小写等价的 npm cache/userconfig/verify 环境键移除后，改绑临时 cache 与空 npmrc。没有读取真实 npmrc、registry 身份或用户 `ZHIXING_HOME`。Windows `.cmd` 仅经固定 basename、拒绝换行并逐参数 quote 后交给隐藏 `cmd /d /s /c`。 |
| C02 结构与 fresh build | 结构测试 3/3 通过：退役 updater/installer owner 与脚本保持缺失；16 个 public manifest 的版本、Node、license、public access、`files=dist` 与无 `preinstall/install/postinstall/prepare` 成立；无确认参数的 publish 子进程只输出“未写入 npm”。随后唯一一次根 `pnpm build` 成功，含 release version check、17 package DAG build 与 Windows x64 mesh helper 生成。 |
| C03 pack 与成员/依赖 | 按包名排序逐个执行一次 `pnpm pack`，每包必须只新增一个 tgz。解包后的 manifest 版本为 `0.1.0`、没有生命周期脚本，所有 production dependency 均为 exact registry SemVer，且不存在 `workspace:/file:/link:`。成员采用允许集合而非“每项必有”的白名单：`package.json`、可选 README/LICENSE、`dist/**`；只有 mesh 可再含 `build/Release/checkpoint_child_bridge.exe` 与 descriptor，任何源码/workspace/其他资产都会失败。descriptor exact keys、schemaVersion 1、win32/x64、packageVersion、bytes 与 SHA-256 均和 exe 全等。 |
| C04 隔离安装闭包 | 临时 consumer 把 16 个 tgz 都列为直接 `file:` dependency，运行 `npm install --ignore-scripts --no-audit --no-fund --package-lock=false`，实际安装 232 个 package；再逐个读取安装后 16 个 manifest，版本均为 `0.1.0`。第三方 production dependency 的生命周期脚本声明与审计 JSON 全等，当前唯一项是 `protobufjs@7.6.6 postinstall`；因安装显式 `--ignore-scripts`，本轮只证明声明集合与静态副作用审查，没有执行该脚本。 |
| C05 library entry | 对 15 个带 `exports` 的 public library，门禁遍历**源码 manifest**的每个 export `import` target，并从安装目录拼出物理文件 URL 后逐一成功 import。因此它证明 fresh tarball 安装后所有已登记 JS target 文件可加载；它没有通过 `import("@zhixing/pkg[/subpath]")` 走 Node package-specifier/packed-manifest exports resolution，也没有加载 `types` target，不能把结果扩大成这两类验证。 |
| C06 CLI entry 与产品 smoke | 从安装目录直接 import/执行 `@zhixing/cli/dist/index.js`：`--version`、`--help`、`help`、隐藏顶层命令不泄漏、空 home `doctor`、`stop --maintenance`、`app remove`、非交互首次运行 code 2 与唯一 TTY 指引均通过。A0-07a 已冻结 source manifest 的 `zz/zhixing → ./dist/index.js` 双 bin，但本门禁没有把 packed manifest 的 bin 与 source manifest 作全等比较，也没有调用 npm 生成的 bin shim或执行 global install；故“CLI 正式 JS 入口可运行”成立，“packed bin 合同及两个 shim 均可运行”仍是明确门禁缺口。 |
| C07 Windows helper | 从安装后的 mesh 包重新校验 descriptor，再由独立 Node child 通过 `FileRecoveryCheckpointTarget.openPaired()` 实际启动 packaged exe、关闭 pipe/target并正常退出；这证明 helper descriptor、位置、架构、摘要及基本 child-process 生命周期，不证明全部 recovery 业务路径。 |
| C08 uninstall、用户数据边界与清理 | 临时 `ZHIXING_HOME` 预置 `sentinel.txt`，完成 CLI/helper smoke 后在 consumer 中以 `npm uninstall --ignore-scripts @zhixing/cli` 卸载，sentinel 仍存在。该断言只证明卸载没有删除该既有 sentinel；helper smoke会在隔离 home 使用 `helper-smoke/`，门禁没有比较 home exact tree/内容，因此不外推“全过程零写”。`finally` 递归移除整个临时根；结束后仓库 `.zhixing-package-check-*` 为 0，`checkpoint_child_bridge/csc/tsup` 遗留进程为 0，未触碰真实用户 home。 |

##### 发布装配、正式入口与不能自证的边界

1. **唯一默认发布入口零写。** 根 `package:publish` 只进入 `publish-npm.mjs`；没有 `--confirm-publish` 时在创建临时目录、读取环境授权或调用 npm 之前即输出“未写入 npm”并成功退出。C02 已在清空 `ZHIXING_ALLOW_NPM_PUBLISH` 的子进程中直接验证此路径。`package:check`、普通 build/pack 和发布默认入口均不写 registry。
2. **显式写分支的静态状态机。** 只有命令行含 `--confirm-publish` 且 `ZHIXING_ALLOW_NPM_PUBLISH=1` 才继续：先运行 `package:check -- --skip-build`，再检查 `npm whoami`、账号 TFA、`@zhixing` 组织 developer/admin/owner 身份及已存在包的 collaborator 写权限；随后按16包内部依赖拓扑冻结 tarball，15个非CLI包先以 `zhixing-candidate` 发布或核对同版 SHA-512 integrity，CLI候选再生成并核对锁定其余15包版本/integrity的 shrinkwrap 后发布。最后从registry只安装 `@zhixing/cli@0.1.0`，核16包版本与lock/remote integrity并直接跑CLI JS version/help；只有全部成立才把 CLI `latest` 移到该版本，任一失败都不会提前移动 `latest`，临时根由 `finally` 删除。
3. **本轮绝未执行写分支。** 没有运行 `--confirm-publish`、`npm login/whoami/profile/org/view/access/publish/dist-tag`、registry install或远端查询，因此只冻结其源码控制流，不能声称身份/权限、candidate可见性、真实registry依赖解析、integrity或latest已被验证，更不能声称已经发布。显式 publish 内部用 `--skip-build` 检查当前制品，随后又重新 pack 候选，且没有消费本次 fresh-build摘要；两次命令间源码/dist不变与候选一致性目前是外部操作纪律，不是跨命令的耐久绑定。
4. **正式安装和入口边界。** 现行唯一用户安装/升级/删除路径是 `npm install -g @zhixing/cli[@version|@latest]`、`zz`/`zhixing` 与 `npm uninstall -g @zhixing/cli`；仓库没有原生 installer、自有 updater、第二发布脚本或 package lifecycle installer。package check 实际证明的是16个本地tgz作为直接依赖的临时 consumer，不是只安装 CLI 的registry/global路径；library entry走物理文件URL，CLI走直接dist，未验证package specifier、packed manifest 的 exports/bin 字段全等、声明文件或bin shim。这些是已知迁移/门禁输入，不得用README、source manifest或静态publish脚本补成已验证事实。

**正反全等、当前差异与失效。** 正向从根`package:check/package:structure:test/package:publish`、16个public manifest/files/bin/exports、CLI与mesh正式入口取得交付链；反向从所有pack/publish/install/uninstall/dist-tag、生产安装文档、retired updater列表、生命周期脚本审计与helper调用回扫，未发现第二installer/updater、第二publish入口、未分类public tarball或源码/workspace资产泄漏。名称相近的`unit-delivery-manifest.mjs`只为单元审查生成Git工作区差异指纹，不pack/install/publish package，`release-version.mjs`只维护A0-07a已冻结的版本投影，二者均不是第二npm交付入口。当前差异精确为：installed package-specifier/packed exports未走Node resolver；packed manifest的exports/bin未与source全等；`types`未加载；`zz/zhixing` bin shim与global install未执行；第三方生命周期脚本只审计不执行；uninstall只保护sentinel存在而非home exact-tree；真实registry确认分支未运行且与先前fresh-build摘要无跨命令绑定。这些不阻止冻结当前交付事实，但必须留给A7门禁归属与最终发布授权裁决。若public package集合/version/dependency/files/bin/exports、build/pack行为、package-check/structure/audit、CLI/helper入口、安装文档或publish状态机任一变化，恢复本证据与A0第二类索引；仅A0-07a静态package/import/build事实变化则同时按其规则恢复A0-07a。A0-01～A0-06b3b不因本轮制品取证失效。

#### A0-08a 首次配置、配置编辑与运行就绪产品旅程

证据 ID：`A0-08a-setup-readiness-journey-v1`。本证据冻结的是“生产入口如何观察同一份公开配置与设备本地秘密、何时允许进入可写产品、怎样提交修改及失败后向用户承诺什么”；A0-05 的字段 schema 与 A0-06b1 的物理格式/兼容仍是底层输入，不在此复制。当前没有顶层 `zz setup`：首次设置的唯一用户入口是无子命令的 `zz`/`zhixing`；Commander 中同名路径只有 `backup setup`，属于恢复备份旅程，不能当作配置入口。

##### 生产入口、观察者与组合链 exact-set

| ID | 生产入口与用户表面 | 配置就绪判断、动作与终态 | 当前责任与直接证据 |
|---|---|---|---|
| J01 | 默认 `zz`/`zhixing` | 先以 `mode=repl` 执行 `runStartupCheck`；`ready` 后才创建 REPL/CoreHost 连接。schema/semantic/SecretStore/non-TTY 缺项均退出码 2，取消显示“已取消配置”并退出 0，未捕获异常退出 1。Host 后续不可达时 REPL 可进入只读历史浏览/重试壳，但不把它冒充正常可写就绪。 | `packages/cli/src/index.ts`、`repl.ts`；入口 owner 是 Commander/default Surface，配置裁决 owner 是 `startup.ts`。 |
| J02 | 首次设置编辑器 | 只有 `runStartupCheck` 发现主模型缺项且当前是 TTY 才自动打开，仅编辑 `model` section；标题/欢迎语随 `repl/host/pairing` 变化，阻断集合不随 mode 变化。完成后重读两份耐久事实并返回 `ready`，取消返回 `cancelled`。 | `startup.ts`、`config-editor/index.ts`、`checks/model.ts`；不存在第二 setup command 或独立 setup writer。 |
| J03 | hidden `serve`，含 foreground/on-demand/managed Host | `topology-command.ts` 先固定 `getZhixingHome()`、按进程模式创建 SecretStore，再以显式 `homeDir` 运行相同 startup gate；非 ready 时 cancelled=0、其余=2，只有 ready 才继续 Mesh、恢复根前置与常驻 Host 图。managed SecretStore 不获缺失 backend/key 的交互初始化权。 | `index.ts`、`serve/topology-command.ts`、`secrets` platform store；Host 图不拥有第二配置真值。 |
| J04 | 配对设备配置阶段 | issuer/joiner 会把已裁决的 Mesh role/reachability 写回同一 `config.jsonc`；joiner 在信任/endpoint 已提交后，以显式设备 home、`mode=pairing` 复用 startup gate。取消和非 ready 都转为配对产品错误，既有安全事实和Mesh配置保留，用户重跑 `zz pair` 续做；ready 才宣布本机模型服务已准备好。 | `serve/mesh-pair-command.ts`；它是 Mesh slice 的系统 writer 与同一 ready gate 的消费者，不是第二配置事实源。 |
| J05 | 本机 workspace 管理 fallback | 常驻本机管理 Host 不可达时，命令期 fallback 以显式 `getZhixingHome()`、`mode=host` 复用 startup gate；不 ready 统一拒绝“当前配置不足以启动本机工作区管理能力”，ready 后才创建 Mesh/MCP/Executor/Authority 临时图。 | `runtime/workspace-command.ts#withLocalWorkspaceFacade`；成功只授权本次 fallback 装配，不产生第二 readiness。 |
| J06 | REPL `/config`、`/mcp` | 两者要求 chrome/TTY，暂停 REPL 后重读最新 config/credentials；`/config` 编辑 model+messaging，`/mcp` 编辑 MCP 并可做 discovery/probe。取消静默返回且 editor 不提交；完成时先等 active turn，再 drain/reconnect Host并刷新 Surface。代码还按Mesh launch selection变化条件reconcile managed service，但当前两组section都不编辑Mesh，正常结果是`not-required`。提交已成功而 reload/reconcile 未确认时明确提示“配置已保存”并要求 `zz status`，不回滚耐久配置。 | `runtime/config-command.ts`、`config-editor/**`、REPL command registry；二者复用 editor，但不是首次启动 gate。 |
| J07 | `zz doctor` | 只读检查 `<getZhixingHome()>/config.jsonc` 是否存在、能否由 `loadConfig(noAutoCreate)` 解析，再检查 backup/offline Host/managed 状态；缺文件报 `setup-required` 并指向“运行 zz”，读取失败报 `local-state-unreadable`。所有正常报告（含问题）CLI 均退出 0，只有检查函数抛到 action 才退出 1。 | `index.ts`、`maintenance/doctor.ts`；不解锁 SecretStore、不读 credential generation、不跑 semantic/mesh/checkModel，因此不是运行就绪判定器。 |
| J08 | ready 后生产消费者 | 同一 `StartupCheckResult.ready` snapshot 供 Provider roles、MCP catalog、Channel setup、RuntimeHost/Executor substrate 使用；`createExecutorReadinessSource`再投影实际工具/MCP、provider/MCP credential binding 与 credential generation，`setupAuthorityRuntime`签发 executor capability/version。该投影服务分配/执行一致性，不回写配置，也不是第二配置 source。 | `serve/command.ts`、`serve/executor-role-runtime.ts`、`serve/executor-readiness.ts`、`setup-delivery.ts`、`serve/access-surfaces.ts`。 |

正向调用 `runStartupCheck` 的非测试生产集合只有 J01、J03、J04、J05 四处；J02 是其内部交互分支，J06 是运行中编辑/换代，J07 是较弱的离线观察者，J08 是 ready snapshot 的消费者。反向从 `loadConfig/writeConfig`、SecretStore/credential generation、config editor writer、配对Mesh writer、Provider/MCP/Channel consumer、setup delivery 和错误文案回扫，配置写入口精确为缺失时模板自动物化、editor完整替换和J04 Mesh slice回写三类；没有第二首次配置 wizard、未分类 config writer 或绕开 ready gate后直接创建主 REPL/Host runtime 的入口。

##### 有限状态、默认、写入与恢复裁决

| 状态/转换 | 当前生产行为、用户观察与耐久终态 |
|---|---|
| S01 全新或 config 缺失 | `loadConfig` 默认会原子创建 `config.jsonc` 模板并 best-effort 创建默认 workspace；模板的 main=`deepseek/deepseek-v4-pro`、light=`deepseek/deepseek-v4-flash`、power 未启用、messaging 空。foreground SecretStore unlock 还可初始化本机 backend/key。由于主 provider key 缺失，TTY 进入 J02，非 TTY 返回缺项并退出 2；managed 不能借交互补齐秘密。故“首次编辑取消”不等于 home 零变化，自动模板/workspace/SecretStore bootstrap 可能已经存在。 |
| S02 public config 结构损坏 | JSONC 读取/解析失败、不可读或 `workspace.root` 非绝对路径时返回 `schema-error`，先于 SecretStore 构造/读取；用户得到文件路径和修复/删除后重试行动，不能进入 REPL/Host。不存在 silent default 覆盖坏文件。 |
| S03 public config 语义损坏 | 顶层废弃 `providers`、`channels`、`messaging.*.credentials` 或无效 Mesh 配置由 semantic validator 返回逐项原因/修复并退出 2；同样先于 SecretStore。当前 `mode` 注释声称决定 messaging 检查，但真实阻断代码只运行同一 semantic/mesh + main-model 集合，不能把注释当合同。 |
| S04 SecretStore/credential 不可用 | store 为 locked/unavailable、vault/generation 损坏、凭据读取失败均返回独立 `secret-store-error`；provider/channel/MCP秘密只取SecretStore投影，API key没有环境变量fallback，秘密值不写回public config或产品错误。legacy `credentials.json` 只有合法且不与active store冲突时一次性进入generation并删除源文件；损坏或冲突时保留源并fail closed。 |
| S05 主模型缺项 | 唯一硬阻断项是 `llm.main.provider`、`llm.main.model` 和 `credentials.providers[mainProvider].apiKey`。TTY 进入编辑器，非 TTY列出缺项并退出2；light/power 缺失或无 key不阻断并在运行期回退 main。Channel 与 MCP 缺项/disabled/connecting/error也不阻断 startup。 |
| S06 本地结构性 ready | public config 通过结构/semantic/mesh校验，SecretStore快照可读且S05三项存在即返回 ready。editor只曝光`siliconflow/deepseek`，生产preset另有六项，自定义provider可由SecretStore binding提供`baseUrl/protocol`；startup不验证provider id是否能resolve、自定义binding完整性或真实网络/API key可用性，这些可能在后续provider resolve/调用失败。因此“ready”仅是本地装配前置，不是外部服务可用承诺。 |
| S07 editor 取消 | `runConfigEditor` 只在 `completed` 调 writer；取消/Ctrl+C 对 editor working state 本身零提交。startup caller返回cancelled，REPL内 `/config`/`/mcp` 静默恢复主循环；但S01中编辑器之前的自动bootstrap不会回滚，配对路径也明确保留已完成安全步骤。 |
| S08 editor 耐久提交 | writer固定顺序为先`writeConfig`（支持的顶层字段按输入替换，messaging/MCP id map整体替换，未知/未提及字段沿现存值保留，temp+rename发布）再`writeCredentials`（stage entries/read-back→active manifest switch线性化→committed/retired cleanup）。两者各自原子边界清楚，但没有跨config+SecretStore的共同事务：前者成功、后者失败会留下新public config + 旧credential generation，外层报告异常，下一次startup重新裁决并可再次要求补key；A0-05已冻结writer不能新增/改变`advancement/modelCapabilityOverrides`的现状差异。 |
| S09 startup 编辑完成 | J02 完成后重新从磁盘读取 config 和协调后的 credential snapshot，再返回 ready；editor自身已用S05阻断完成，但 caller没有在 reload 后重新跑完整 semantic/mesh/checkModel。当前单 writer假设下结果可用，若 editor writer/外部并发写入变化须重新冻结此结论。 |
| S10 运行中编辑与 Host 换代 | J06提交后等待当前 turn 到边界，向旧 Host请求drain shutdown，再 reconnect/spawn唯一后继并刷新本地状态/observer/task list；旧 shutdown响应丢失但新连接与refresh成功时仍返回refresh后再抛旧错误，Surface据此显示“已保存但重载未确认”。Channel connected/connecting/error/disconnected只作为换代后状态反馈，不反向决定提交成功。 |
| S11 MCP/Channel degraded | MCP `enabled`缺省true、显式false跳过，非法server id跳过；其SecretStore string map对HTTP注入headers、对stdio叠加到SDK安全环境白名单，危险启动变量会被过滤，未提供binding时不会把任意父进程环境当credential source。`connectAll`以all-settled把连接失败保持为connecting/retry，不阻断Host open。消息通道配置项存在即尝试启用，adapter/credential/连接失败被记录并跳过或投影状态，不阻断主产品。编辑器中的MCP新增probe失败只阻断该候选接入，不升级为全局not-ready。 |

##### 跨表面一致性、真实差异与迁移输入

1. **配置路径存在三方分裂。** 无显式home的J01 startup按`ZHIXING_CONFIG_PATH`读取/写入精确文件，且SecretStore位于其dirname；J03/J05/J04用显式`getZhixingHome()`/设备home，因而忽略该精确文件覆盖。J06虽按`getGlobalConfigPath()`读取并在header显示精确覆盖文件，却把`resolveHomeDir()`作为显式home传给`writeConfig`，若覆盖文件名不是`config.jsonc`，会读取`<dir>/<custom>.jsonc`而写`<dir>/config.jsonc`；J07也固定检查`<getZhixingHome()>/config.jsonc`。因此只设置外部`ZHIXING_CONFIG_PATH`时，默认预检、运行中编辑、Host和doctor可能观察不同文件；这是真实迁移输入，不能声称各表面对同一配置事实完全一致。
2. **doctor 是弱观察者而非 ready gate。** 缺失config时它effect-free且正确指向`zz`；但已有可解析却缺main key/语义不完整的config、locked/unavailable SecretStore或credential generation问题不会被doctor检查，仍可能报healthy，且问题报告通常退出0。它与startup的“能否进入产品”语义不等价。
3. **提交和换代只做到分层收敛。** 单文件config和单个credential generation各有原子提交，config→credentials、耐久提交→Host reload/reconcile之间没有跨层事务。产品文案已经区分“保存成功”与“运行时换代确认”，后续ApplicationHost迁移必须保持此可观察边界或显式重做合同，不能靠回滚假设掩盖partial终态。
4. **默认模板有过时用户语义。** 新建config的power注释仍称“进入工作场景（`/enter`）后接管”，而A0-04已冻结当前registry不存在`/enter`。它不改变ready判定，但属于首次用户可见默认模板漂移；A0只登记，不能在事实冻结包顺手修复。
5. **当前测试证据有薄弱点。** `startup-secret-store.test.ts`识别legacy迁移/冲突/locked与schema/semantic先后，providers config/credential tests识别原子替换与generation切换，config-command tests识别drain/reconnect/响应丢失，doctor tests识别缺失config零创建与错误脱敏，MCP/Channel tests识别非阻断失败；但没有直接覆盖完整J01 non-TTY文案/exit、startup editor completed/cancelled和“config已写、credentials失败”跨store partial，也没有覆盖parseable incomplete/locked store下doctor与startup的差异。这是现有保护证据缺口，不用测试替代上述生产调用图。

**正反全等、owner 与证据失效。** 当前唯一公开配置事实是A0-05 C01～C11定义的config文件，唯一秘密事实是SecretStore active credential generation；editor是二者的用户写入口，模板物化与配对Mesh回写是同一config owner下的系统writer，startup是可写主产品前置裁决，Provider/MCP/Channel/Runtime/Setup Delivery只消费快照，doctor只做离线弱观察。正向从Commander/default、四个`runStartupCheck`生产callsite、两个REPL编辑入口到runtime消费闭合；反向从config/credential全部writer、path resolver、SecretStore、provider resolver、MCP/Channel setup、executor readiness、错误/exit和现有直接测试/正式CLI README回扫，没有未分类writer、第二ready owner或隐藏setup入口。上述四项真实差异均已定位到唯一调用链，不以README或测试覆盖为不存在。若Commander/default/serve/pair/workspace入口、`startup.ts`、config path/loader/semantic/default、SecretStore/credentials generation、editor/checks、REPL config command/Host reload、doctor、Provider/MCP/Channel consumer、executor readiness/setup delivery或相关用户文案/直接测试任一变化，恢复本证据与A0第三类索引；只有字段schema或物理owner同时变化才按既有规则再恢复A0-05/A0-06b1。A0-08b继续冻结日常对话、历史、上下文和运行终态，本证据不外推这些旅程。

#### A0-08b1 日常会话选择、创建、恢复、历史、命名与清空旅程

证据 ID：`A0-08b1-conversation-lifecycle-journey-v1`。本证据冻结“用户怎样取得一个当前会话指针、哪些耐久事实决定列表/恢复/历史/名称/清空/删除、连接与进程重启后怎样重新投影”；它引用 A0-04 的公开 method/notification exact-set、A0-06a P02/P06 的物理事实边界和 A0-06b2a 的 WAL/CAS 基础格式，不展开单轮模型、工具、segment/attention/context/compact、取消与 run 终态。当前 REPL registry **没有** `/history`：用户历史入口是启动或 `/resume` 后的最近三轮尾巴、Host 不可达时的只读浏览器，以及第一方 `session.history` 倒读分页；不得因本包标题而虚构一个 slash 命令。

##### 生产入口、事实链与跨表面 exact-set

| ID | 生产入口/行为 | 事实写入、读取与可观察终态 | 当前 owner、投影与 AE-001 归属 |
|---|---|---|---|
| C01 | 默认 REPL ready 后自动选择 | 先挂 notification listener，再以 `session.list` 取 `lastActiveAt` 新→旧的非archived user域候选；WorkScene id 永不成为启动落点。逐项 `resumeIfExists` 消除 list→resume 间已删除竞态；全部失效或空集合才 `session.new`。恢复成功显示最近历史尾巴，历史读取失败只丢增强展示，不推翻已选择指针。 | `repl.ts` / `selectInitialConversation` 只拥有 Surface 指针和呈现；列表、touch、创建由 Product API→Conversation owner执行。迁移落点为 A3 Surface binding + A5 Conversation。 |
| C02 | `/new` | canonical Host 路径经 `session.new` 创建 user `meta.json + transcript` 空壳并返回 id/name，随后 Controller 退订旧会话、切换并订阅新会话、刷新任务尾巴。创建失败保持旧指针；RPC 响应丢失不自动重放，可能已经产生一个未选中的空会话。 | `buildSessionNewMethod`→`ConversationDirectory.create`→`ConversationRepository.create`；Surface 指针不是产品事实。常规路径的 Authority session 直到首次输入或后续耐久生命周期操作才由 `ensureSession`/control admission 建立。 |
| C03 | `/resume [名称或 id]` | 无参数展示最多15个候选；有参数先 exact id、再大小写不敏感名称子串，唯一命中切换，多命中要求更精确，空集合/无命中不改指针，同一 id 短路。Server `resume` 先 touch，再把连接加入 observer，随后恢复 advancement，运行时窗口推迟到首次 send 才装填；NOT_FOUND 被启动候选路径转成“继续找下一项”，其他错误上抛。 | Controller 是选择状态 owner；Conversation meta 是列表/name/last-active读源，Manager/owner只投影 active/busy/recovery。迁移落点 A3/A5；单轮窗口装填留 A0-08b2/A4。 |
| C04 | 历史浏览与分页 | canonical Anchor `session.history` 对非活跃会话也可倒读，默认20、最大200，cursor=`{shardId,runIndex}`；非法 limit/cursor fail-fast，未知会话返回空页；local-only 路由先校验当前 Authority，不存在/非本机可用id按owner gate拒绝。REPL启动与`/resume`仅取并投影最近3个run、只显示用户文本与最后助手文本，不把tool steps当完整浏览器；降级只读浏览器直接以不可写 store 列最近5个会话、每会话1个run，Enter重试/q退出，非TTY直接退出。 | Transcript shard/index 是用户原文 owner；history是只读 Product API/Surface投影，attention window不从此公开。index缺失/损坏由reader从shard重建，坏JSONL行跳过；cursor指向已retention清走shard时返回空older页。 |
| C05 | `/name <名称>` 与首轮自动命名 | 手动入口 trim 后要求非空，经 `session.rename` 原子写 meta并向observer广播`session.changed/renamed`；未知id=NOT_FOUND，当前没有长度上限。首个成功turn后，仅user/non-ephemeral且名称仍等于id时以light model生成、去引号/压空白/最多20 code points后fire-and-forget写同一meta；失败不影响turn且无补偿重试。 | `ConversationRepository.rename` 是名称writer；Server/auto-name producer共享同一事实与broadcast，不形成第二name store。Auto-name在两次“仍是默认名”检查后到写入间仍有小TOCTOU，可覆盖同期手动命名，是当前迁移输入。 |
| C06 | `/clear` | Surface预登记本地echo，发送稳定requestId；durable owner先写`window-op/clear`及domain revision，再由同一可恢复projector追加Transcript ClearRecord、清meta中的task-list/segment等视图状态并重置活动窗口，完成后广播`cleared`。身份、名称、preferences与旧物理shard保留；正常history/window读取遇clear即止，retention后续收旧shard。busy拒绝且不写盘；不存在=NOT_FOUND；投影中断由启动恢复重驱。 | Authority lifecycle fact是操作/重放owner，Transcript clear boundary与meta/runtime是其产品投影。CLI收到本地echo只消重，其他Surface清空当前会话会刷新task-list并重建空屏。A5负责语义，A4/P06保持原子/重放。 |
| C07 | `/resume` 候选 Ctrl+D 删除 | 第一次Ctrl+D只标记确认，第二次调用`session.delete`稳定requestId。durable owner先写delete lifecycle/tombstone，再由同一projector释放runtime/queued sends、删除整个conversation目录并完成advancement级联，全部投影完成才retire journal；busy拒绝并要求先abort，NOT_FOUND不改本地状态。删除非当前会话只刷新候选；删除当前会话后自动`session.new`，若新建失败则Surface可能仍指向已删除id并明确报错。 | Delete Authority 与 projection/recovery 属 A5+A4；目录递归删除是领域生命周期，不是retention。`session.changed/deleted`使其他Surface清除观察；REPL收到其他Surface删除当前会话时异步新建，失败只呈错、不复活旧事实。 |
| C08 | RPC/notification 与连接恢复 | `session.list/new/resume/history/rename/clear/delete`均在A0-04 canonical registry；`session.changed`仅`renamed/deleted/cleared/taskList`，`session.activity`只提示非当前入口有新动态。Controller启动/切换维护subscribe，CoreHost换代后强制重订阅并做durable run reconcile；进程重启不持久化Surface当前指针，重新执行C01。 | Observer名册/连接是瞬时Server投影，conversation id/meta/transcript/Authority才是耐久事实。`clear/delete`facade跨断线以同一requestId重试；`new/rename/resume`无同等重试，断线后结果可能不确定，用户靠重新list/resume/read确认。 |
| C09 | executor-only 本机继续与远端owner路由 | `session.list`合并当前本机Authority会话与fenced远端owner结果并排序；首次本机`new/resume/rename/clear/delete/send`均要求用户已显式接受local-only。新建经`LocalConversationOwner.createConversation→ensureSession`直接建立本机Authority id；history从owner transcript tail读取。非本机current owner经Mesh转发同一method，frozen/importing返回busy，已不可用本机id返回not-found。 | 与canonical Anchor路径不同，本机only路径没有先建P02目录壳再惰性建Authority的阶段；它直接以P06 session state为事实并保持相同RPC wire/notification。两种binding不得在迁移时混成双writer或强行假设同一创建事务，落点A3/A5/A6。 |
| C10 | 消息 Channel 自动会话选择 | inbound根据接入面稳定派生id：thread、按群、群内按人或DM；DM使用`default`，可与CLI主会话相交。Channel没有list/resume/name/clear/history UI，而是按每条平台消息确定id并直接调用`ConversationManager.admitTurn`；稳定messageId形成turn identity，输出仍经同一owner/transcript并可投影给RPC observer，非观察连接只收content-free `session.activity`。 | Channel绕过session RPC handler但不建立第二Conversation事实owner；它是A0-04已登记的第二应用编排路径，后续A3/A5必须收束为纯binding而不能改变确定性id或跨Surface同看语义。 |

上述入口 exact-set 还明确排除三个伪候选：CLI启动参数没有旧 `--continue/--resume/--name` 双轨；`/compact`操作attention派生视图而不清原文，留给A0-08b2；WorkScene conversation使用独立全域id/目录与进入返回锚，留给后续WorkScene旅程。普通用户会话的列表、启动恢复与Channel确定性选择不能外推到这三者。

##### 状态、竞态、失败与恢复裁决

| 状态/反例 | 当前生产终态与保护边界 |
|---|---|
| 空home/空列表 | C01只创建一条新user conversation并把它选为Surface当前项；常规Anchor路径会立即留下P02空壳，local-only则立即建立P06 Authority session。 |
| 选择/删除确认取消 | `/resume`无参数只是只读列候选，typeahead退出或未提交候选不会发RPC；inline delete第一次Ctrl+D仅进入Surface二次确认态，未确认即不写任何conversation事实。没有对已提交`new/resume/name/clear`的通用“取消”操作，不能把UI退出外推为服务端回滚。 |
| 坏或缺meta | `ConversationRepository.get/list`把无法解析的meta视为不存在并从列表跳过，不会用默认值覆盖坏文件；启动继续下一个候选，最终可新建。Transcript存在而meta坏的孤立数据不会通过正常列表被恢复。 |
| list→resume并发删除 | `resumeIfExists`只把NOT_FOUND收敛为null，启动继续下一个候选；显式`/resume`则呈现未找到并保持旧指针。Server touch/本机owner current-authority gate是实际二次确认，不依赖列表快照。 |
| 清空并发 | 活动turn时clear返回BUSY且不改变盘；获排他点后先耐久记录再投影，响应丢失以同一requestId重放。ClearRecord之后追加的新run可见，之前run保持物理存在但不再被正常history/window读取。 |
| 删除并发 | 活动turn时delete返回BUSY；既有直接测试以stale send和concurrent send/delete攻击过删除后重建，当前owner/tombstone/projector使旧id不会被晚到send复活。投影失败保留pending lifecycle并由Host恢复；全投影完成后才退休Authority journal。 |
| 连接中断/重连 | notification订阅由facade在新client上重建，Controller重订阅当前id；Surface仍可能错过断线窗口内的瞬时提示，但耐久list/history/lifecycle可重读。`new/rename/resume`响应丢失不具稳定重放合同；`clear/delete`具备。 |
| 名称竞态 | 手动rename是最后完成写者生效；自动命名只在首轮且两次检查默认名，但最终写前无共同CAS，仍存在覆盖同期手动rename的窗口。auto-name失败、Host重启或错过首轮均不补命名。 |
| 清空/删除的跨Surface投影 | 非当前id变更不改本地指针；当前id的renamed更新标题，cleared重建空视图，deleted触发新建。Surface处理失败只造成呈现/指针降级，不回滚已经权威完成的clear/delete。 |
| 历史分页与清理 | clear是逻辑可见边界；retention才物理删除旧shard/无用snapshot。history本身只读；canonical Anchor对未知id返回空页而resume/rename/clear/delete为NOT_FOUND，local-only history还受current-authority gate约束。read-only browser不写index，因此Host故障时浏览不会改变历史。 |

##### Owner 裁决、当前差异与保护证据

1. **“当前会话”不是全局产品事实。** REPL `ConversationController.active`只是每个Surface的选择指针；Server observer名册也是连接期投影。耐久conversation identity、名称与列表顺序来自常规路径的P02 meta，原文/clear来自Transcript，accepted run与clear/delete lifecycle来自P06 Authority。后续A3不能把某个连接指针或ServerContext提升成Conversation owner。
2. **常规路径存在有意但需迁移显式化的两阶段身份。** `session.new`先让空会话在P02 meta/list中可见，首次admitted input才以确定性`session-create:<id>`建立Authority；clear/delete可从目录存在性bootstrap同一Authority lifecycle。local-only C09则先建Authority。当前是两种拓扑binding差异，不是“所有创建已经由单一事务完成”；A5迁移必须保留用户可观察结果并消除第二事实解释空间。
3. **历史表面有限且不对称。** 第一方RPC有完整cursor分页，REPL只显示3轮tail且没有`/history`，Host失败浏览器默认只看5个会话各1轮；Channel没有历史浏览入口。这是当前产品基线，不得用RPC能力推导每个Surface都有同等UI，也不得把attention/context视图当历史事实。
4. **响应丢失保证不一致。** clear/delete具备stable requestId reconnect replay；new/rename/resume只做单次request。后者若服务端已提交而响应丢失，Surface会显示失败或保留旧指针，随后可由list/resume重新观察，但没有自动exactly-once收敛。它是后续Product API/ApplicationHost迁移输入，不在A0补协议。
5. **现有直接证据与缺口。** `conversation-controller`/`session-commands`/candidate-delete测试识别候选竞态、指针/observer、当前/非当前删除与clear回声；server `session-rpc`/`conversation-manager`测试识别inactive history、分页、subscribe-before-recovery、busy clear/delete、broadcast和删除不复活；directory/repository/transcript测试识别空壳、clear边界、index自愈与物理删除；local-conversation-rpc与channel inbound测试识别local consent/owner路由及确定性id共享。当前没有端到端测试直接覆盖`new/rename/resume`服务端已完成后的响应丢失、auto-name最终TOCTOU、当前会话删除成功但自动new失败后的后续用户操作，也没有证明REPL完整分页浏览（生产本就没有该UI）。这些是保护证据缺口，不推翻已由调用链观察到的行为。

**正反全等与证据失效。** 正向从ready REPL的C01、四个范围内slash命令（`new/clear/resume/name`；`compact`已按边界排除）、候选inline delete、canonical Server/RPC、executor-only local router和Channel inbound取得生产集合；反向从A0-04 registry、`ConversationDirectory/Repository`、Transcript reader/store、`ConversationManager`、`ConversationProtocolRuntime` lifecycle recovery、local owner/session-state adapter、observer notification、history presenters及上述直接测试回扫，未发现第二公开创建/选择/命名/clear/history入口、未分类writer或第二Conversation Authority。`/history`不存在已由registry与dispatcher双向确认，不能用注释/任务措辞补成入口。若REPL startup/controller/session commands/candidate delete/history browser、session RPC/registry/facade/notification、ConversationDirectory/Repository/Manager、Transcript clear/history/retention、ConversationProtocolRuntime lifecycle/session-state、local owner/router/Mesh route、Channel id binding/inbound、auto-name或相关保护测试任一变化，恢复`A0-08b1-conversation-lifecycle-journey-v1`与A0第三类索引；若同时改变物理root/schema或Authority容器，再按既有失效规则恢复A0-06a/A0-06b2a。单轮run、compact/context、cancel/terminal变化只恢复A0-08b2，不误伤本证据，除非它改变本节列明的生命周期交界。

#### A0-08b2a1 普通用户请求准入、身份与排队旅程（部分安全检查点）

证据 ID：`A0-08b2a1-turn-admission-journey-v1`。本轮因流程纠正只保留已经由生产调用链直接闭合的“公开入口 → durable accepted/queued”事实；它不是完整 A0-08b2a1 证据，不覆盖 Authority/Assignment 深层状态机、accepted 后执行、取消/不确定、终态、恢复或上下文。

| 公开入口 | 入口身份与绑定 | durable accepted/queued 前的实际结果 |
|---|---|---|
| ready REPL 普通文本 | `prepareUserTurnInput` 在 Surface 阻止空白输入；`ConversationController` 为当前会话预分配 `turnId`，并在调用 `conversation.send` 前先挂 outcome waiter。RPC facade 以同一 `conversationId/turnId/input` 发 `session.send`，连接断开重连只用原参数重试。 | send 响应或更早到达的本地 delta 会把 Surface 标成 accepted；响应返回 `runId` 后 Controller 绑定 durable watch。waiter/当前指针只是瞬时 Surface 状态，不是准入事实。 |
| 第一方 `session.send` | canonical Anchor 由认证后的稳定 `rpc:<clientId>` principal 绑定输入；显式 conversation 必须存在，省略时可先创建。executor-only 本机 owner 要求 `continueLocally` 明示同意；远端 current owner 经认证 Mesh 原样转发 method/params，`frozen/importing`、owner 不可达或身份不符在准入前拒绝。 | `ConversationManager` 先做会话串行化和每会话容量门，再以 `{conversationId,surfacePrincipal,turnId}` 形成稳定 admission identity。durable admission 成功后才返回 `runId`；公开 `SessionSendResult` 不暴露内部 `immediate/queued/replayed` 差异，因此调用方只观察 accepted 或 RPC `NOT_FOUND/BUSY/validation/internal` 等拒绝。 |
| 消息 Channel inbound | current-owner 与 Host `acceptingNew` gate 先于 intent/confirmation/agent；普通 agent 消息按 thread/group/per-user/DM policy 得出 conversationId，durable 模式要求平台 `messageId`，并派生 `turnId=channel:<channelId>:<messageId>` 与 channel responder principal。控制意图和确认回复不属于本表普通请求。 | 复用同一 `ConversationManager.admitTurn→admitDurableTurn`；`full` 明确回复队列已满，Host 拒新明确要求稍后重发，其余准入异常当前被合并成场景切换/目录变更提示，`not-found` 无回复。accepted/queued/replayed 没有单独接收回执，后续内容投递不在本轮范围。 |

**最小 owner 与线性化裁决。** 三类入口都不拥有 `runId` 或 durable queue：Surface/Channel 提供稳定 `turnId` 与接入面 principal，Conversation Product API/Manager 负责会话串行化和内存容量门，durable owner 在一次 input admission 中确定 `runId` 并提交 admitted + queued 后，生产入口才可返回 accepted。相同 conversation/principal/turn 与相同 payload 的响应丢失重放取得同一 run；相同 identity 配不同 payload fail closed。普通消息的内存 pending 上限当前为每会话5条，已满可在 durable 写入前拒绝；一次 durable admission 已成立后，内部 immediate/queued/replayed 调度差异不再被公开响应区分。Assignment identity 只在后续实际调度阶段产生，**不**是 accepted 响应的前置事实；后续迁移不得让 Surface、RPC connection、Channel adapter或内存队列成为第二准入 owner。

**当前可观察差异与安全交接。** REPL trim 空白后不发送，而直接 RPC/Channel 的文本非空校验只检查长度；Channel 不显示 accepted/queued 状态，且把多类准入错误合并成同一提示；第一方 RPC 也不公开 immediate/queued。现有 Controller、session RPC/ConversationManager、Channel inbound、local owner/router与first-party Mesh直接测试可作为这些入口、先挂waiter、容量/拒绝和身份重放的反向证据索引，本轮没有运行它们。尚未核实并明确保留为按需义务：durable owner全部冲突与并发状态、Assignment接管、Host排空后的已接受工作、远端响应丢失、重启重放、执行/取消/终态/恢复及其通知投影；不得据本节宣称这些闭合，它们必须在实际改变 Conversation、Kernel、Correctness 或拓扑责任的工作包中即时核实。若上述三类公开入口、`session.send` registry/handler/facade、stable surface identity、Channel binder/inbound、`ConversationManager.admitTurn`或durable admission返回边界变化，恢复本部分证据与代表性产品回归索引。

#### A0-09 最小迁移决策图

证据 ID：`A0-09-minimal-migration-decision-graph-v1`。本图只压缩 A0-01～A0-08b2a1 已接受事实中会改变责任转移和先后顺序的内容；详细 exact-set、schema、产品终态和测试清单仍由原证据及实际迁移包即时取证负责。

| ID | 当前唯一责任/写入口/恢复主链（仅迁移相关） | AE-001 目标责任 | A 项与前置顺序 | 迁移时必须即时闭合的保护证据 |
|---|---|---|---|---|
| M01 | `@zhixing/cli` topology/role roots 组合三种持久 Host 拓扑；`StartupRollback/CleanupRegistry`、Server open、拒新/排空与 root close 分担生命周期，recovery-root 是 role Host 前串行前置图。 | 每进程唯一 ApplicationHost；组件只贡献自身 start/close，Host 不进入用例。 | **A1 首链**；先接管持久 Host 外层，再收编内部组件和非持久进程 scope。 | A0-01/02 的角色、三启动模式、ready、bootstrap 无双图、启动补偿、endpoint 单例、拒新后排空、逆序 close 与零残留。 |
| M02 | REPL、普通管理命令、pair、backup/DR、workspace fallback 各自临时装配 screen/client/listener/maintenance/owner/target；业务事实仍由对应 owner 提交。 | Surface/command ApplicationHost scope + Domain/Product API + Infrastructure adapter。 | A1 在 M01 稳定后逐类收编资源 scope；应用调用留 A2/A3/A5，物理 adapter 留 A6。 | A0-03 的33个非serve action、Host/Surface分界、owner race、响应丢失与每类 `finally/close`；不得把命令 scope 变成第二 Host。 |
| M03 | config editor/配对 writer 写唯一 `config.jsonc`，SecretStore generation 持秘密；startup gate 生成只读 ready snapshot，Host/Runtime只消费。 | Configuration/Secret Provider + ApplicationHost 冻结投影。 | A1 只接收投影；A6 收窄 Provider/Secret adapter；不建配置 Domain。 | A0-05 config 与 A0-08a：默认/校验、公开/秘密隔离、取消/partial commit、doctor弱观察及Host reload。 |
| M04 | `ServerContext`、RPC handler、CLI Controller/Facade 与 Channel InboundRouter 当前聚合或复制部分应用编排；registry/notification 是公开 binding，不是事实 owner。 | Domain-owned Product API Catalog；Server/RPC、CLI、Channel 为纯 Surface/Transport binding。 | A2 先用一个真实领域证明端口；A3 只从该合同建立 dispatcher/binding模式；其余随A5迁入。 | A0-04 与 A0-08：CLI/RPC/Event exact-set、认证/observer、进程内与远端语义、错误映射和现有多表面差异。 |
| M05 | ConversationDirectory/Repository、Transcript、ConversationManager 与 ConversationProtocol/Authority 分别承载 identity/history、内存准入及耐久 run/lifecycle；Surface current pointer 非事实。 | Conversation Domain + Product API；通用耐久/恢复经 Correctness ports。 | A5；前置 A3/A4。它是高耦合主链，**不选作 A2**。 | A0-06 P02/P06、A0-08b1/b2a1；迁移包即时补齐尚未冻结的 Authority/Assignment、排空、重放、取消/终态和全部直接消费者。 |
| M06 | Skill Catalog 领域应用、P06 Authority/CAS 与 assignment staged mutation 已共同拥有管理、save/admit/load/usage、RPC/REPL/dynamic slash 及 Kernel/Executor 不可变投影；旧 home 目录、legacy import、文件物化和 writable Store 已退场。 | Skill Catalog Domain 拥有合同、生命周期和事实；Product API 组合；Kernel消费不可变能力投影。 | **A2 首个垂直切片已闭合** → A3 完成全部 binding；A4只保留投影端口，A5表项随后闭合。 | A2-01～A2-07：领域应用与唯一 subpath、commit-after-fact、assignment exact replay、三态 admission、load/usage、Kernel/Executor 同 revision 全等，以及旧目录零触碰/旧 Store 零残余。 |
| M07 | 本机 workspace owner/catalog/outbox/reset 与 CLI fallback 拥有 workspace 事实；Workscene directory/owner、专用工具和 RuntimeHost 装配拥有 scene 事实及其 workspace/conversation 关系。 | Workspace Administration 与 Workscene 两个 Domain；各自 Product API，Workscene只依 Workspace/Conversation端口。 | A5：Workspace、Conversation 先于 Workscene；A4先把产品专用 runtime输入改为显式 envelope投影。 | A0-03、A0-04、A0-06 P03/P07 与既有workscene路径事实；保护owner竞争、双提交credential、generation/reset、scene CRUD/会话清理。 |
| M08 | Scheduler、schedule journal/owner、RPC/工具/notification 当前共同表达调度规则、运行与恢复。 | Schedule Domain；Kernel/Effect/Delivery 只提供执行与交付端口。 | A5；前置 A4。可选结果投递只消费 Delivery 应用端口，不据此虚构两个领域的迁移全序。 | A0-04/05/06 的方法、事件、工具和持久族；迁移包即时核实普通任务、触发、abort、恢复和用户通知。 |
| M09 | Advancement store/evidence、Server `session.send` 分支与 CLI presenters 当前编排合同草案、执行、评价和成果准入。 | Advancement Domain + Product API；Kernel生成候选/证据，Correctness提交。 | A5；前置 Conversation、Skill Catalog 与 A4；结果投递经 Delivery 应用端口集成，但不要求 Delivery 整域先迁移。 | A0-04 RPC/Event、P08 evidence及A0-08b准入交界；即时闭合评价、response-loss、重驱和终态，不改产品合同。 |
| M10 | Delivery authority/outbox/receipt 与 Channel sender承担“需交付”事实和发送效果；部分入口仍聚合于Server/CLI。 | Delivery Domain拥有义务与终态；Channel/Transport只实现效果。 | A5 的基础领域之一，A4后可先于Schedule/Advancement迁移；A6再收窄Channel adapter。 | A0-04 Event/RPC、A0-06相关authority/artifact/transport族；保护幂等、receipt、失败/未知终态、重放与渠道投影。 |
| M11 | Trust/grant/ref 与用户管理入口、Security执行决定、设备拓扑当前在owner/Server/CLI/Mesh交织。 | Trust Administration Domain；Security留Correctness，Mesh留Infrastructure。 | A5 Trust 先于 Device；A6才删除拓扑对象向上渗透。 | A0-04、A0-06 P06/P09、配置/配对旅程；保护撤销、principal、fail-closed与安全消费。 |
| M12 | Device关系、移除和值班迁移的产品状态机与 endpoint/epoch/Assignment/Mesh 物理状态在CLI/Server装配交织。 | Device Administration Domain；Executor/Mesh Topology adapters。 | A5 Device 依 Trust；完成产品语义归位后 A6迁物理拓扑。 | A0-03/04/06 的device/duty/transfer链；保护签名、current owner、接管/取消、单机一等与多设备等价。 |
| M13 | backup target、recovery-root与DR命令拥有产品选择/安装流程；checkpoint、staging、签名、CAS/Mesh提供耐久机制。 | Backup & Recovery Administration Domain + Correctness recovery ports + Storage/Mesh adapters。 | A5 依 Trust；目录目标不依赖 Device，配对设备目标只消费 Device 应用端口；A6收窄存储/传输，不归ApplicationHost。 | A0-02/03、A0-06 P11/P12；保护bootstrap串行、冻结checkpoint、signed abort/receipt、partial cleanup和用户数据边界。 |
| M14 | `AgentRuntime`驱动模型—工具—窗口—子Agent，`RuntimeHost`还直接装配Workscene/Schedule/专用工具；ExecutionProfile由运行装配产生并被assignment校验。 | 封闭 Intelligence Kernel + 通用 Runtime Assembly；领域生成 Run Envelope。 | A4，位于 A3 后；尚未迁移领域只允许有退场点的单向投影。 | A0-05 tools/Profile 与代表性A0-08；即时闭合Run Envelope/Event/Terminal、context/compact/cancel、local/remote/job/subagent等价及ExecutionProfile唯一producer。 |
| M15 | owner-kernel/core distributed contracts提供Authority、Journal、Assignment、Security、Confirmation、Resource、Effect与Recovery，内部仍夹有领域record/reducer。 | Correctness Substrate通用机制；业务schema/reducer随各Domain退出。 | A2先证明一个领域adapter；A4冻结Kernel效果端口；A5逐事实迁出业务语义。 | A0-06 P01～P15；每包核实唯一writer、strict codec、commit/replay/partial终态，禁止双写、兼容reader和第二Authority。 |
| M16 | Provider、Tool、MCP、Storage、Channel、Executor、Mesh及Anchor/Executor路由实现物理能力；当前拓扑选择仍能上渗到CLI/Server/领域装配。 | Infrastructure/Topology按需求方窄端口实现；单机/分布式只由Host选择adapter。 | A6，必须在A5产品语义归位后；A1只管理其生命周期贡献。 | A0-01～07的角色、工具/config、持久根、package/export/tarball；保护能力exact-set、身份/签名、安装制品和拓扑透明。 |
| M17 | `core`根导出、跨包依赖和CLI/Server聚合使Domain/Kernel/Correctness/binding边界可被旁路。 | 有预算的Domain/Kernel/Correctness/Binding subpath与无环依赖。 | 各A项随责任即时收口；A7删根barrel/旧桥并同基线终验。 | A0-07 package/export/build/install事实；每包门禁禁止旧import/第二入口，A7复验正式tarball而非workspace/旧dist。 |

**生产角色覆盖与依赖裁决。** A0-01 的持久 `anchor + local executor`、`anchor-only`、`executor-only` 全部归 M01；REPL、瞬态控制/诊断、pair/backup/DR、workspace/create-scene 全部归 M02，并由 M03～M13 指向各自事实 owner；Server/RPC/Channel 归 M04，Agent/Runtime归 M14，Authority与边缘归 M15/M16。因而没有未归属生产角色，也没有把运行形态虚构成新领域。迁移主序固定为 `A1 → A2(Skill Catalog) → A3 → A4 → A5 → A6 → A7`；A5只冻结真实硬依赖：`Workspace + Conversation → Workscene`、`Trust → Device`、`Trust → Backup & Recovery`、`Conversation + Skill → Advancement`。Schedule/Advancement 对 Delivery、配对备份对 Device 均通过应用端口集成，不据此强制整域迁移全序；其余领域由协调者按当时最小失效闭包排序。

##### A1 首条生产责任链

首条责任链固定为 **持久 Host 外层生命周期唯一 owner**：从 `runServeCommand/topology-command` 到现有三类 role root 的共同边界接管 home/config/secret/role/topology 冻结、recovery-root 串行前置、role-root 委托、stop/terminal 等待与最外层 lease/cleanup；内部 Server、Owner、Mesh、Runtime、领域实现及其既有启动/关闭次序先原样委托。该责任链必须同时适用于 foreground/on-demand/managed，因为三者共享同一 role Host 图而只在OS启动与ready观察上分叉；不得只为一种模式制造第二 Host 类型。完成时旧外层平行等待/释放责任必须删除，并以三拓扑、三模式、bootstrap无双图、部分启动回滚、并发close、入口最后开放和零残留直接证明。协调者必须沿单一生命周期结果和独立验证闭包把该责任链拆成默认 2～4 小时的工作包，不得把整条责任链一次派发。选择它而非先搬组件，是因为它只转移一个生命周期所有权、复用已有资产、不进入产品语义，却为所有后续组件贡献和命令scope提供唯一挂载点。

A1 后续偏序为：持久 Host 外层 owner → Anchor/Executor 类型化组件贡献 → 统一 open/refuse/drain/replace/close 信号并删除旧 root 平行权 → REPL 与各命令期 Surface scope。executor-only stop唤醒、trust换代、on-demand idle、REPL未激活client、backup/DR maintenance等已知缺口分别由实际改变其生命周期的紧邻 A1 包核实和保护，不前置到A0补调查。

##### A2 首个真实领域切片

首切片固定为 **Skill Catalog**。理由是：它已有有限且真实的管理/使用合同（RPC `skill.list/setState/archive`、`skill.changed`、REPL `/skills`与动态skill入口、Agent `load/save/admit`），有明确Skill/CAS/物化/legacy/usage事实边界，有真实Surface，也有供Kernel和Executor消费的不可变能力投影；同时不依赖Conversation主状态机、Workscene/workspace双owner、Schedule/Delivery恢复、Trust/Device拓扑，因此是边界最小、行为完整且依赖风险最低的现有领域。

A2 必须在一个切片内让Skill拥有Command/Query/Fact Event、应用准入与状态规则、权威适配、Kernel投影、Surface投影和生命周期贡献，让至少一个现有RPC或进程内Surface只调用同一应用端口，并删除对应旧写入口；同时保护frontmatter scan与Authority物化差异、CAS原文与本机投影、legacy重试、archive/state/usage、main/work技能分区、动态slash和本机/Executor资产一致性。明确不做：不建设全局领域框架/Product API Catalog，不改Agent Loop/MCP，不迁移其他领域，不借切片修复既有产品差异。A3随后只从该已证明合同形成Catalog/dispatcher和全部Skill binding；A4只消费Skill不可变投影，不能再反向拥有catalog事实。

**延后未知的唯一归属。** A0-08b2a1未展开的Authority/Assignment冲突、重启重放与Conversation恢复只在M05的A5包即时核实；accepted后的context/compact/cancel/Event/Terminal只在M14的A4包核实；Host排空和已接受工作边界在M01/M02的A1包核实；远端response-loss、current-owner/Mesh路由在M12/M16的A5/A6包核实；每个其余领域的完整消费者与错误矩阵只在改变该行的A5包核实。它们均有唯一落点，不改变A1首包或A2切片，也不再成为A0前置。

#### A0-10 首批迁移验证映射

证据 ID：`A0-10-initial-verification-map-v1`。本映射只回答 A1 持久 Host 外层唯一 owner 和 A2 Skill Catalog 首切片“迁移时用什么直接证伪”，不把既有测试名、历史绿灯或 A7 全量门禁冒充当前迁移结果。以下文件均为当前可复用输入；实际修改使其输入失效后，执行包仍须按验证手册先重建上游、再串行运行对应包的直接测试与类型/构建闭包。

##### A1：持久 Host 外层生命周期唯一 owner

| 识别目标 | 当前可复用的最窄直接证据 | A1 阶段必须新增或强化的最小证据 |
|---|---|---|
| 三拓扑、共同组合根与正常启动 | `packages/cli/src/serve/__tests__/role-topology.test.ts` 识别 `anchor+executor`、`anchor-only`、`executor-only` 的唯一 role loader/local owner/cleanup owner exact-set；`topology-command.test.ts` 识别公共 preflight 与 role side effect 顺序；`startup-server-owner.test.ts` 识别 endpoint 先绑定且复用同一 handle。 | 一份参数化 `ApplicationHost` 生产装配合同覆盖三拓扑 × foreground/on-demand/managed：每次只创建一个 outer Host，冻结 home/config/secret/role/topology 投影，role root 只作为组件贡献被调用一次，全部组件 ready 后才 open，不允许旧 root 与新 Host 并行拥有 wait/finally/lease。 |
| recovery-root 串行替换、启动失败与回滚 | `recovery-root-establishment-runtime.test.ts` 证明普通业务入口保持关闭；`topology-command.test.ts` 证明 preflight 未 ready 不创建 role 状态且失败停止 bootstrap maintenance；`packages/cli/src/serve/__tests__/startup-rollback.test.ts` 证明反序、隔离失败、commit 后才移交 cleanup。 | 对 create/start/open 每一阶段做 component fault injection；证明 bootstrap Host 完整 close 后才 prepare/start 常驻 Host、无 listener/owner 双图窗口；任一失败只补偿已取得资源一次，未 open 的入口不可见。 |
| 拒新、排空、替换、并发与幂等 close | `host-stop-lifecycle.test.ts` 覆盖 immediate/drain/cancel、response-loss replay、flush failure resume 与 terminal read-back；`managed-service-runtime.test.ts` / `managed-service-reconciler.test.ts` 覆盖 revoke 后关 admission、同 home reconcile 合并、失败 successor 与 response-loss read-back；`packages/server/src/__tests__/lifecycle.test.ts` 覆盖直接/request shutdown 幂等。 | 用同一个 Host gate 证明 stop/replace 先拒新、已 accepted work settle 后再释放；并发/重复 close、启动中 close 与 managed replace 只执行一次终止链并共享终态；明确 executor-only shutdown 唤醒、trust-generation replace 和 on-demand idle 分支均不再绕过 outer owner。 |
| 逆序释放与零残留 | `packages/cli/src/serve/__tests__/shutdown-chain.test.ts`、`packages/server/src/__tests__/cleanup-registry.test.ts` 与 `packages/cli/src/serve/__tests__/distributed-runtime-golden.test.ts` 识别 LIFO、失败隔离、幂等、owner 约束和当前 lifecycle golden；`packages/cli/src/serve/__tests__/daemon.test.ts` 识别 child/log-fd/ready/health 失败边界。 | 建一个由真实 production component descriptors 驱动的 leak harness，逐项断言 listener、timer、child、RPC/Mesh connection、lease、PID/port/ready 及临时资源零残留；不能只核对 callback 名单或 mock close 被调用。 |
| 架构依赖边界 | canonical `pnpm s7:lint` 已检查 production cleanup 注册必须走 descriptor、`CleanupRegistry` construction 必须绑定 topology owner，并由 registry golden 锁定当前 role/entry 集合。 | 强化同一结构门禁：所有持久生产入口只能构造一个 `ApplicationHost`；CLI/topology/role root 不得再直接拥有外层 open/wait/finally/cleanup，Host 不得导入领域 handler、ServerContext 服务定位或运行期可变 lazy ref；组件只暴露类型化 lifecycle contribution。旧 outer owner 路径删除必须由负向 mutation 证明可被检出。 |

**A1 首条责任链的验证顺序。** 各工作包只运行与本包实际责任变化对应的上表直接测试：以 `pnpm --filter @zhixing/cli exec vitest run` 串行运行受影响的 CLI 文件，涉及 Server 生命周期时再以 `pnpm --filter @zhixing/server exec vitest run` 运行 `lifecycle.test.ts` 与 `cleanup-registry.test.ts`；若 S7 扫描输入变化，再运行一次 canonical `pnpm s7:lint`；最后只运行实际受影响包的 typecheck/build。当前没有运行这些命令，也没有已复现的 failing test/gate。已证的是两个**识别缺口**而非失败：现有 S7 cleanup catalog 对 executor-only 外层释放没有完整 production descriptor，现有 lifecycle golden只钉住旧 shutdown callback 顺序，二者都不能证明未来唯一 `ApplicationHost`；相关 A1 工作包必须在改变对应责任时补强并以新证据验收。A0-02 已登记的 executor-only stop 唤醒、trust换代、on-demand idle 等接缝是生产迁移输入，不得伪写成已失败的测试。

##### A2：Skill Catalog 首个垂直切片

| 识别目标 | 当前可复用的最窄直接证据 | A2 切片必须新增或强化的最小证据 |
|---|---|---|
| Catalog 查询、状态、usage 与持久提交 | `packages/core/src/skills/catalog-application.test.ts` 与 `global-state-adapter.test.ts` 识别 management/save/admit/load/usage、main/work、disabled shadow、revision、CAS、assignment exact replay及 commit 后 fact；`frontmatter.test.ts` 只钉住中性 parser/stringifier。 | 已由 A2-01～A2-04 建立 Skill Domain Command/Query/Fact Event 与唯一应用服务/Authority adapter；A2-07 进一步证明 staged apply/refresh 只验证 durable Authority request/digest/revision，不写 filesystem。任何旧 Store、materializer或 commit 前 event 回流均由 S7 mutation 拒绝。 |
| save/admit 的拒绝、确认、幂等与临时清理 | core catalog application/admission、tools-builtin Skill、orchestrator assignment port与真实 ledger用例共同识别脱敏、坏输入拒绝、artifact-bound confirmation、TOCTOU、token过期、OS-temp orphan sweep、同 assignment overlay读己写、response-loss exact replay和零半成品。 | A2-02/A2-03 已把两条工具链改绑同一 Domain application port；A2-07 保留 OS-temp staging 与正式 `admit_skill`，同时删除无生产价值的旧 home Store tests/fixtures。成功/拒绝/过期清理继续由正式 application→adapter 链证明，不再要求或允许 legacy import durable pending。 |
| 真实 Surface、公开合同与动态入口 | `packages/server/src/rpc/__tests__/management-methods.test.ts` 识别 `skill.list/setState/archive` 参数、NOT_FOUND 与成功后 `skill.changed`；`packages/cli/src/skills/__tests__/manager-controller.test.ts` 识别 REPL 管理动作和 onMutate；`packages/cli/src/commands/__tests__/skill-command-source.test.ts` 识别动态 slash、builtin-first 冲突和真实 dispatch；server canonical registry golden锁定公开 method/event。 | A2 首包至少把现有 `skill.*` RPC 作为一个真实 binding 接到同一 application port，并证明进程内调用与 RPC 对同一 command/query 得到等价结果和错误；handler/CLI controller 不得持有规则或直接写 Store。其他 Skill binding 可按 A3 完成，但本包迁移到 Domain 的旧写入口必须当场删除，不能双写。 |
| Kernel/Executor 只读投影与能力使用 | `packages/orchestrator/src/runtime/assignment-skill-port.test.ts` 识别 assignment-scoped artifact write、path-free catalog、usage与无 durable assignment 时 fail closed；`packages/cli/src/serve/execution-asset-cache.test.ts` 识别 version-bound path-free cache和不完整/revision rewrite拒绝；builtin `load_skill/save_skill/admit_skill` 直接测试保护工具语义。 | 新增 immutable Skill capability projection conformance：Kernel/Executor 只能消费 id/name/description/body-ref/state/revision 等冻结投影，不得得到 Store/path/Authority；本机与 Executor 对同一 revision 的 main/work分区、disabled过滤、正文与资产摘要一致，旧 catalog 或 partial asset fail closed。 |
| 唯一 owner 与依赖方向 | canonical `pnpm s7:lint` 当前覆盖唯一 catalog subpath、管理/save/admit/load/projection owner、dynamic slash、Authority staged replay、旧 Store/path/import/materialization exact-zero，并以反向 mutation检出旁路 writer、commit前 event、Kernel path、home scanner或根导出回流；`pnpm runtime:package-exports` 证明 core 根不暴露新增合同或旧 Store。 | 迁移后继续以现有 owner/public budget机械取集：Server/CLI/Orchestrator/Kernel/Infrastructure 不得定义 Skill 业务合同、读取 Skill home、直接写 Skill Authority/CAS或绕过应用端口；A3只建立纯 binding，A7在正式tarball同基线复核所有导出和旧路径零残余。 |

**A2 当前最窄证据与后续边界。** A2-01～A2-07 已按每次真实失效闭包串行取得 core catalog/global adapter、tools-builtin binding、orchestrator Correctness/ledger、Server RPC、CLI动态 slash与execution-asset、S7和package-export证据；已删除的 `store*.test.ts` 不再是正式能力证据，S01～S04 旧 home reader/writer接受域也不再是产品兼容基线。A2-07 仅重取本次改变的 global adapter、builtin/render、dynamic slash/manager、setup inert-directory、S7、package exports和 core→CLI build闭包；根级全量与正式制品仍只在 A7 同一冻结基线执行。若 A3 改变 binding 或 A4/A5 改变投影/事实归属，只恢复对应应用证据，不把已退役 Store/legacy链重新纳入测试清单。

##### 只能在 A7 同一冻结基线执行的终验

A1/A2 实施包不得把以下全仓结果当作局部迁移前置或重复运行：先 `pnpm build`，再串行执行全部新增架构门禁、`pnpm contracts:typecheck`、`pnpm contracts:lint`、`pnpm runtime:baseline`、`pnpm runtime:package-exports`、根级 `pnpm lint`、`pnpm test` 与 canonical `pnpm package:check`，随后完成六面对抗。A1 的最终归属重点由 Host/依赖攻击、真实分阶段故障与制品进程入口反查；A2 的最终归属重点由产品行为、合同/所有权、Kernel/效果、Surface/制品与残留攻击。迁移包只取得本节列出的直接闭包和受影响构建；A7 才能证明全产品、全包、Windows tarball 与所有迁移结果处于同一未修改基线。

**完整性与失效。** 两条映射分别覆盖正常、拒绝/回滚、并发/幂等/恢复、资源清理/零残留和依赖边界；现有证据、待新增证据、门禁盲区和 A7 终验没有混淆。没有真实反证改变 A1 首链或 A2 Skill选择，A0-08b2a1 延后事实仍按 A0-09 的唯一 A1/A4/A5/A6 落点处理。若 A1 首链或 A2 切片选择变化，或上述 test/golden/S7/contracts输入、生产 role root、Skill writer/binding/projection任一变化，恢复本证据、A0第五行及A0；仅其他领域测试变化不使本映射整体失效，由实际迁移包即时取证。

### 工作包记录

每个完成、安全止损、阻塞或纠正的工作包在本节追加一条记录，不覆盖旧记录。记录模板：

```text
### <A 项 / 工作包名称>

- 基线：<HEAD + 本任务相关工作区差异标识>
- 包边界：<唯一架构结果、预计工作窗口、强制拆分条件与安全交接点>
- 当前责任与问题：<生产入口、owner、消费链和可复现事实>
- 本轮迁移：<唯一责任变化、旧路径删除、明确未做>
- 直接证据：<命令/静态门禁、结果及其识别力>
- 交接类型：<完成 / 安全止损 / 阻塞；若止损，记录触发条件和唯一下一步>
- 失效与恢复：<被作废的旧证据、恢复的勾选或无>
- 遗留问题：<真实未闭合事项或无>
- 下一检查点：<可由下一协调轮直接恢复的位置>
```

### A0-01：冻结生产进程角色、入口与组合根 exact-set

- 基线：`HEAD 642fe25b8a8df7e1e764cb107916172df1d82f2c + task-doc:coordinator-dispatch + A0-01-process-roots-v1`；进场索引为空，协调者已有差异仅为本文状态/调度台账。
- 包边界：只闭合生产 OS 进程角色、启动/公开入口和真实组合根的双向 exact-set；未追完生命周期、公开命令/RPC/Event exact-set、owner、Kernel/Topology 迁移或任何实现变化。
- 当前责任与问题：唯一发布 executable 是 CLI，但同一 entry 同时承载 Surface、命令期局部装配和三种持久 Host 拓扑；Anchor Host 当前还是事实总组合根，Executor-only 和恢复根阶段另有真实角色专属图。若只按 package 名或 `serve/command.ts` 单点记录，会漏掉 Executor-only、命令期本地 owner 及恢复根串行图。
- 本轮迁移：无生产迁移；新增 `A0-01-process-roots-v1` 事实表，把 9 个运行形态逐项反绑到 AE-001 的 Surface/Product API、ApplicationHost、Domain、Infrastructure/Topology 责任与 A1/A3～A6 检查点，并登记排除证据。
- 直接证据：完整读取任务、AE-001 与验证手册；执行 `git status --short` / `git rev-parse HEAD`；读取 root 与全部 package manifests、CLI tsup/README；以 `rg` 反查 package `bin`、main guard、CLI action imports、role plan/role loaders、`createServerContext/runServer`、Executor/Channel 装配、managed/daemon self-exec、恢复根串行调用及生产 `node:child_process` 引用。结果为仅 `@zhixing/cli` 一个 bin 声明、仅 `cli/src/index.ts` 一个生产 main guard、所有 role plan 与 child 候选均已分类；`git diff --check` 无错误，最终索引为空且工作区只修改本文。本包按约束未运行测试/构建。
- 交接类型：完成；角色、入口、组合根三集合无未分类候选，规模未触发四小时或多未知止损。
- 失效与恢复：无旧证据被作废；本记录只补充 A0 第一行证据引用，不改变其 `[ ]`。
- 遗留问题：每行的 `create → start → open → replace → drain → close`、三种 Host 启动模式的换代/回滚、Surface 与瞬态图的释放边界尚未闭合，且公开合同 exact-set 仍待独立 A0 工作包。
- 下一检查点：A0-02 直接以 `A0-01-process-roots-v1` 九行及启动形态说明为冻结输入，逐行追踪完整生命周期；若发现新 executable、main guard、role plan 或独立组合根，先恢复本证据引用并纠正集合。

### A0-02：冻结持久 Host 家族与恢复根前置阶段生命周期

- 基线：`HEAD 9457495a5800b3f8921f067d892c942f63e241dc + task-doc:coordinator-dispatch + A0-02-host-lifecycle-v1`；进场索引为空，协调者已有差异仅为本文活跃/下一工作包台账。
- 包边界：只闭合 Anchor + local Executor、Anchor-only、Executor-only 三种持久 Host 与 foreground/on-demand/managed 三种启动模式的 `create → start → open → replace → drain → close`，以及恢复根常驻 Host 前串行阶段；未处理 REPL Surface、瞬态 CLI/pair/backup/workspace 装配、公开合同 exact-set 或任何迁移。
- 当前责任与问题：当前生命周期并无单一 Host owner；OS service/daemon、topology bootstrap、role root、Server activate、HostStop journal、CleanupRegistry 和 topology outer-finally 分别拥有一段。静态生产图另证明 Anchor open 早于完整 cleanup 接管，managed-role-change/idle 绕过耐久 prepare，Executor-only RPC 停机未触发 root close、且缺 trust-change replace/on-demand idle close。这些都是 A1 需要收口的当前接缝，不是本包实施缺陷修复。
- 本轮迁移：无生产迁移；新增 `A0-02-host-lifecycle-v1` 六阶段矩阵、三拓扑/三模式条件表、恢复根前置链、高风险反例裁决和 A1 最小责任迁移顺序；未修改生产代码、测试、AE-001 或其他文档。
- 直接证据：完整重读本文、AE-001 与验证手册；以 `rg` / `Get-Content` 正反追踪 `topology-command`、`role-topology`、`command:runServerProcess`、`executor-role-runtime`、`access-surfaces`、`BoundZhixingServer/runServer`、`HostStopCoordinator`、`StartupRollback/CleanupRegistry/shutdown-chain`、managed reconcile/self-exec/daemon 与 recovery-root runtime；静态交叉核对已有 host-stop、rollback、shutdown、managed preflight 与 recovery-root 测试的断言边界。生产事实已能唯一裁决，因此按包约束未运行测试/构建。`git diff --check` 与最终 Git 边界在本记录后执行。
- 交接类型：完成；三种拓扑、三种启动模式与恢复根前置阶段的全部适用阶段均有唯一当前责任者、成功点、失败/重入终态与残留裁决，无未分类 Host 分支或独立未知。
- 失效与恢复：无旧证据被作废；A0 第一类索引增加本证据 ID，但因 Surface/瞬态图生命周期尚未闭合仍保持 `[ ]`，A0、完成度和最终退出门均不变。
- 遗留问题：A1 必须依次消除入口早开、Anchor 非耐久停机旁路、Executor-only 外层停止/换代/idle 缺口及两种 cleanup 错误传播语义的分裂；这些已是可直接派发的实施单元，不需要重做 A0-02。REPL Surface 与命令期装配的 lifecycle 仍待 A0-03。
- 下一检查点：A0-03 以 `A0-01-process-roots-v1` 的前五种 Surface/命令期运行形态为冻结集合，追踪 create/connect/open/replace/close 和失败回滚；不再调查本记录已闭合的持久 Host。

### A0-03：冻结 REPL Surface 与命令期临时装配生命周期

- 基线：`HEAD 920ff597d376eb5bc28fc7c8147a1a138a83605e + task-doc:coordinator-dispatch + A0-03-surface-command-lifecycle-v1`；进场索引为空，协调者已有差异仅为本文活跃/下一工作包台账。
- 包边界：只闭合 A0-01 前五种非持久运行形态及全部 33 个非 `serve` action 的 `create → connect/open → replace/drain → close`；未重查持久 Host 内部、公开 RPC/Event/工具/Profile/schema/制品 exact-set，未修改生产实现、测试、AE-001 或其他文档。
- 当前责任与问题：REPL 正常尾链、已激活 CoreHost/current-anchor client、pair listener/socket、workspace owner/host/outbox 和 checkpoint target/control 均已有真实 owner，但它们分散在 Surface、domain command 与 Infrastructure 内。生产图同时证明三处 A1 输入：REPL 启动异常没有统一外层显式 close；current-anchor client 在激活前 start 失败没有局部补偿；backup/DR context maintenance 只靠一次性 CLI 进程终止，而非命令 scope stop。`create-scene` 还会串联远端 Product API 与本机 workspace authority，但 lock + credential/request ID 证明它们不是双 owner。
- 本轮迁移：无生产迁移；新增 `A0-03-surface-command-lifecycle-v1` action exact-set、生命周期矩阵、高风险反例与 A1 非持久图迁移顺序，并将 A0 第一类索引更新为 `[x]`。A0、完成度、其余索引行和退出门保持原状态。
- 直接证据：完整重读本文、AE-001 与验证手册；执行 `git rev-parse HEAD` / `git status --short` / staged diff 核对；以 `rg` / `Get-Content` 正反追踪 `index.ts` 34 个 action、唯一 main guard、`startRepl`、`runStartupCheck`、`CoreHostConnection`、`CurrentAnchorSurfaceRpcClient`、status/stop/logs/maintenance、device/duty/uninstall、pair issuer/joiner、backup/root/disaster recovery、workspace local transport/owner/host/outbox、managed/daemon/clipboard 及全仓生产 child-process 候选，并用既有直接测试名称交叉核对当前覆盖主题。生产调用图与资源释放实现已能唯一裁决，本包未运行测试/构建；`git diff --check` 与最终 Git 边界在本记录后执行。
- 交接类型：完成；五类非持久形态、33 个 action、所有适用/不适用阶段和反向资源候选均已分类，未出现新组合根或两个独立未知。
- 失效与恢复：无旧证据被作废；A0 第一类由 A0-01/A0-02/A0-03 共同闭合并更新为 `[x]`，不影响尚待取证的其余六类。
- 遗留问题：A1 必须把 REPL 启动失败、current-anchor client 激活前失败补偿、持续 tail 取消、backup/DR storage maintenance 和散落的 process-exit 兜底收进显式 scope；同时原样保留 A0-02 Host 分界、pair continuation、recovery checkpoint/signed abort、workspace outbox/owner lock 与 Workscene 幂等 request ID。公开合同 exact-set 尚未冻结。
- 下一检查点：A0-04 直接以 A0 第一类三个证据 ID 为冻结输入，枚举 CLI/RPC/Event、工具/Profile、配置 schema、持久记录族、包导出和制品入口 exact-set；不得重做 A0-01～A0-03 生命周期调查。

### A0-04：冻结 CLI、RPC 与 Event 公开合同 exact-set

- 基线：`HEAD 6395128e4371a276c56e9b16dbc328bb66c26fb1 + task-doc:coordinator-dispatch + A0-04-public-cli-rpc-event-v1`；进场索引为空，协调者已有差异仅为本文接受基线、活跃/下一工作包台账。
- 包边界：只闭合用户或第一方 Surface 可达的 Commander/REPL 命令、Server/RPC 方法和跨 Server bridge 的 Event/notification；不处理工具/Profile、配置 schema、持久记录族、包导出、tarball、生命周期复查或任何架构迁移。预计范围在单一事实表内闭合，没有触发多未知或四小时止损。
- 当前责任与问题：CLI 外层只有一个 Commander root，但同时包含公开用户命令、隐藏托管入口、本机直达与 RPC binding；REPL 另有 22 个静态 slash 命令及 enabled-skill 动态族。Server 当前有 61 个严格注册方法，handler 仍兼任大量用例编排与产品语言，`ServerContext` 只是聚合位置；18 个 notification method 只是 owner/Run/内部事件的传输投影，Surface 不据此成为事实 owner。Executor 与 Anchor 共用方法/事件语义但物理路由不同，Channel 仍是绕过 RPC 的第二 application binding。
- 本轮迁移：无生产迁移；新增 `A0-04-public-cli-rpc-event-v1`，登记三类机械重取入口、完整 exact-set、producer/consumer/owner/目标归属、拓扑分流、有限事件 discriminant 和五项高风险交界裁决。A0 第二类索引只增加本证据引用并保持 `[ ]`；A0、完成度和退出门均不变。
- 直接证据：完整重读本文、AE-001 与验证手册并核对 Git；以 `node --import=tsx/esm --input-type=module` 递归读取唯一生产 Commander tree，得到 `nodes=40/actions=34/groups=6/aliases=[]`；对 22 项静态 slash registry 与 README 表格 18 项执行 `Compare-Object` 双向差集，得到 README 缺 `advancement/mcp/stop/task/tasklist`、多 `enter`；以 `rg` 对 `packages/server/src/rpc/methods` 导出的 builder 与 `buildBuiltinRegistry` 调用做双向差集，得到 `builders=61/registered=61/only-builders=0/only-registry=0`；读取 canonical registry 得到 anchor 61、仅 `auth/health` 无认证、当前 golden 的 executor-only/executor-surface 都为 0；再正反追踪全部 handler、CLI facades、Channel/Conversation、local/current-anchor router、Server bridge、`onNotification` 和有限 projection 集合。静态事实已经能唯一裁决，本包没有运行测试、构建或制品验收；一次尝试从错误的旧式 golden 路径读取 consumer 差集因文件不存在而未形成证据，随后以当前 `__goldens__/canonical-registry.golden.json` 和生产调用点完成重取。
- 交接类型：完成；CLI、RPC 和 Event 三个集合均可机械重取，producer/registry/consumer 或明确无 consumer、当前语义 owner和 AE-001 目标归属已分类，无未注册 builder、第二 registry、动态 RPC method、影子 notification bridge或未分类命令路径。
- 失效与恢复：A0-04 初次记录的 README 单向差集因漏掉 `/stop` 且未检查过时 `/enter` 而失效；本轮从生产 registry 与 README 表格重取双向差集后恢复，CLI 生产 exact-set 及 RPC/Event 证据未受影响。本记录只补充 A0 第二类的部分证据，不改变其 `[ ]`。若 Commander tree、slash registry/skill source、builtin RPC registry、Server bridge/projection、第一方 subscriber 或拓扑 router 变化，必须恢复本证据并重取三个集合。
- 遗留问题：当前生产事实暴露三个后续必须显式裁决的基线差异：README 的 slash 表相对生产 registry 少 `/stop`、`/advancement`、`/mcp`、`/tasklist`、`/task` 五项并多过时 `/enter` 一项；role golden 把实际会默认注册 61 个方法的 Executor Server 记为 0；`delivery.resolve`、`health`、`schedule.abortRun`、`server.uninstall.status`、`session.resolve` 已注册但没有仓库内直接第一方 client caller。前两项不能作为迁移绿证，后一项不能未经产品合同裁决就删除。
- 下一检查点：A0-05 以本证据的三类 public surface 和差异规则为冻结输入，独立冻结工具/Profile 与配置 schema exact-set；之后再由持久记录、包导出和制品工作包补齐 A0 第二类。不得重做 A0-01～A0-04 已闭合事实，若新工作发现第二 CLI/RPC/Event 入口则先恢复本证据。

### A0-05：冻结工具/Profile 与配置 schema exact-set

- 基线：`HEAD 54da1eb66026e23917e2b1f49d28f0d438151b21 + task-doc:coordinator-dispatch + A0-05-tools-v1 + A0-05-runtime-profiles-v1 + A0-05-config-schema-v1`；进场索引为空，协调者已有差异仅为本文接受基线、活跃/下一工作包台账。
- 包边界：只闭合生产工具工厂/固定与动态集合、实际 Agent/RuntimeExecution/LLM/Host Profile 组合，以及用户持久配置字段、默认、验证、秘密引用和消费者；不重查 A0-04 CLI/RPC/Event，不处理持久记录/存储/版本迁移、包导出/制品、生命周期、产品旅程或任何实现/README/AE-001 修改。工具与 config 子证据在初次取证中成立，Profile 子证据的初次完整性结论随后失效并由下方纠正记录恢复。
- 当前责任与问题：纯 builtin、Profile 派生、Host extra 和 MCP 动态工具当前分属四个组合点；Anchor 与 Executor-only 的外部工具集合不同。AgentRoleProfile 是身份/工具/scene 输入投影，RuntimeExecutionProfile 是装配后能力与 Provider/MCP 依赖的冻结投影，事实分别归 assignment、Workscene、Provider、权限与资源 owner；`ServerProfile=full` 又是另一类 Host assembly 描述。配置有一个 JSONC 来源和十个顶层正式字段，但只有 Mesh 分支 strict，整体 loader 不是运行时结构 schema，SecretStore 才是 provider/channel/MCP 秘密的唯一来源。
- 本轮迁移：无生产迁移；新增三个证据 ID，冻结 20 个固定工具名 + MCP 动态族、八类生产 runtime 组合、AgentRoleProfile 四种生产投影 + 一个 RuntimeExecutionProfile 合同 + 三个 LLM role + 一个 Host profile、两个内部 `*Profile` 策略候选，以及十个配置顶层键及嵌套/default/consumer/SecretStore 边界。逐项反绑 A3 Product API、A4 Kernel/Correctness、A5 Domain 与 A6 Infrastructure；A0 第二类索引只追加证据并保持 `[ ]`，A0、完成度、其他索引和退出门不变。
- 直接证据：完整读取本文、AE-001 与验证手册并核对 `HEAD`/Git 边界；以 `rg`/`Get-Content` 正反追踪 `BUILTIN_TOOL_FACTORIES/BUILTIN_TOOL_CAPABILITIES`、default Profiles、`createAgentRuntime`、subagent factory、`BuiltinExtraToolsAssembly`、MCP naming/mapping、RuntimeHost/ExecutorRuntimeSubstrate/job/capability catalog、Host Profile、`ZhixingConfig`/loader/writer/semantic validator、Mesh strict validator、LLM role/recommendation/provider preset、config-editor sections、全部十键生产消费者以及 README。初次取证没有追到 `RuntimeExecutionProfile` 的 SessionRuntime/Authority/assignment/recovery 链，故当时的 Profile 完整性自报无效；补证见下方纠正记录。本包未访问真实 home/SecretStore/外部系统，未运行测试、构建或制品验收。
- 交接类型：初次 Profile 完整性结论失效；工具与 config 结论保留，Runtime/Profile 子证据由下方同工作包纠正记录恢复。
- 失效与恢复：协调复核以 `packages/core/src/types/runtime-execution-profile.ts` 为直接反证，作废“无未分类生产 Profile”及“四类 Agent/Profile 投影即可覆盖 Profile”的结论；`A0-05-runtime-profiles-v1` 必须补齐完整 producer/consumer/authority/recovery 链后才恢复。builtin/extra/MCP 工厂、配置 schema 与 A0-04 未因此失效。
- 遗留问题：当前已确定而未修复的迁移输入为：README 工具列表漂移；无 workspace Profile 注释漏 `save_skill`；Profile capabilities 未消费；公共 `extraTools` 无重名拒绝；config 整体非 strict；`workspace.protectedPaths` 与 MCP `tools.include/exclude` 未消费；`writeConfig` 不能新增/改变 `advancement/modelCapabilityOverrides`；`config-loader` 的环境变量 API key 注释与实际 SecretStore-only resolver 不符。此外持久记录族、配置/凭据版本与 legacy migration、包导出和实际制品尚未冻结，不能勾选 A0 第二类。
- 下一检查点：续做同一 A0-05，重取 `A0-05-runtime-profiles-v1`；恢复前不得进入 A0-06。

### A0-05 纠正：补齐 RuntimeExecutionProfile 生产合同

- 基线：`HEAD 54da1eb66026e23917e2b1f49d28f0d438151b21 + task-doc:coordinator-dispatch + A0-05-runtime-profiles-v1:runtime-execution-profile-correction`；索引保持为空，进场工作区只有本文既有 A0-05 差异。
- 包边界：只纠正 `A0-05-runtime-profiles-v1` 的 Profile 类 exact-set、完整性结论、失效条件和记录；保留已成立的工具与配置 schema 证据，不进入持久记录/存储/包导出/制品 A0-06，不修改生产代码、测试、AE-001、README 或其他文档。
- 当前责任与问题：初次记录把 AgentRoleProfile、LLM role 与 ServerProfile 当成完整集合，漏掉独立的 `RuntimeExecutionProfile`。生产事实证明它在 Agent runtime 完成装配后生成，经 SessionRuntime adapter 进入 conversation Authority；Authority 冻结环境和 executor readiness，完整三字段用于本机 canonical equality，tools/MCP 与 provider 派生 credential bindings 进入 durable manifest，恢复 worker 依 sealed manifest 重建并校验 runtime。它不是前三类 Profile 的别名，也不是可忽略的命名候选。
- 本轮迁移：无生产迁移；在主证据表补入 `tools/mcpServers/providerIds` exact-set、标识规范化/去重/排序/冻结/clone、无 workspace 七工具裁剪、本机三字段一致性、device generation/receipt guard、远端 sealed manifest 降解与崩溃恢复边界；唯一 source 归 A4 Kernel，A5/Correctness 与 A6 只消费冻结投影和派生合同。另以生产 `*Profile*` 反向审计分类 `ToolExecutionProfile`、`MeshServerSecurityProfile` 及所有从属/非合同命中，未发现第二个同类遗漏。
- 直接证据：逐项读取 `packages/core/src/types/runtime-execution-profile.ts`、`packages/core/src/environment/selection.ts:executionProfileForEnvironment`、`packages/orchestrator/src/runtime/create-agent-runtime.ts` 的创建/冻结/getter、`packages/runtime-host/src/session-adapter.ts`、`packages/server/src/perspectives/controller.ts`、`packages/owner-kernel/src/types.ts` 与 `conversation-assignment-authority.ts`、`packages/cli/src/setup-delivery.ts` 的 normalize/prepare/binding/receipt guard、`conversation-owner-runtime.ts` 的 Authority 转导、`conversation-protocol-runtime.ts` 的本机 assign 前 equality/恢复 wrapper、`conversation-assignment-worker.ts` 的远端/恢复校验，以及 executor ledger 的 runtime binding guard/recoverable assignment 入口；再以 `rg` 双向扫描全部生产 Profile 类型、factory、registry、composition 与 consumer，类型候选收敛为 AgentRole/RuntimeExecution/Server/ToolExecution/MeshServerSecurity 五类及 Server `ProfileSpec`。静态调用图足以唯一裁决，按约束未运行测试、构建或制品验收；最终 `git diff --check` 通过（仅 Git 的既有 LF→CRLF 提示），`HEAD` 仍为接受基线，索引为空且工作区只有本文。
- 交接类型：完成；`A0-05-runtime-profiles-v1` 已恢复，工具/Profile/config 三类证据现均有机械重取入口、正反消费者、条件分支、默认/规范化规则与 AE-001 目标归属。
- 失效与恢复：恢复此前因漏登 RuntimeExecutionProfile 而失效的 Profile 子证据；工具/config 子证据、A0-04 与 A0 第一类证据始终未失效。以后若 `RuntimeExecutionProfile` 字段、`freezeExecutionProfile`、environment selection、SessionRuntime adapter、Authority prepare/binding、manifest credential lowering、local equality/receipt guard 或 assignment recovery worker 任一变化，必须恢复本子证据并重取；不得只看 AgentRoleProfile factory。A0 第二类仍因持久记录、包导出和实际制品未冻结而保持 `[ ]`。
- 遗留问题：A0-05 已知迁移输入仍为原记录所列八类工具/config 差异；RuntimeExecutionProfile 当前还有一个必须保留的边界事实：完整三字段只在赋权前和本机 assign 前比较，durable/remote 合同将 providerIds 降解为 credential bindings，不能在迁移中误写成 wire 上仍有 providerIds，也不能让 Host/Authority/Executor 重建第二 Profile source。没有 A0-05 范围内未分类 Profile。
- 下一检查点：协调者独立复核本纠正后，A0-06 可直接以 `A0-05-config-schema-v1` 的单一 config/SecretStore 边界冻结持久记录族、存储根与版本/迁移入口；不得重做工具/Profile/config 调研。本轮不进入 A0-06。

### A0-06a：冻结生产持久事实族、派生/临时记录与存储根 exact-set

- 基线：`HEAD 8c721788b17c33f326e2e759b2aa3a221c461859 + task-doc:coordinator-dispatch + A0-06a-persistence-roots-v1`；进场索引为空，工作区只有协调者在本文登记 A0-06a 的实时台账差异。
- 包边界：唯一结果是冻结生产可达的物理根、record family、scope、creator/writer、reader/recovery、维护/删除/备份、当前 owner 与 AE-001 目标归属；不展开字段 exact-set、格式/版本号、兼容读取、cutover/migration 状态机，不处理 package exports、构建/tarball/安装入口，也不实施迁移。安全交接点是 P01～P15 与反向 writer/store/path/maintenance/backup/delete 集合全等。
- 当前责任与问题：主数据根由 `getZhixingHome()` 选择，config/SecretStore 另有显式 home 与 `ZHIXING_CONFIG_PATH` 分叉；当前生产落盘责任收敛为配置/秘密、conversation 历史与快照、WorkScene cleanup、权限、Skill 物化与 admission temp、Anchor/Executor WAL+CAS+projection、Executor snapshot/workspace/probe、assignment stream/evidence、Mesh/asset staging、本机 workspace outbox/IPC、recovery checkpoint、transfer/DR staging、Server lifecycle/log、managed-service metadata、诊断日志十五组。核心边界是 P06 Anchor/Executor 两 WAL 不共 owner、P02 transcript 不等同 P06 run/assignment 权威、P03/P05/P07 都不是第二 Domain owner、checkpoint/staging/日志不提升为 live facts。
- 本轮迁移：无生产迁移；只在本文新增 `A0-06a-persistence-roots-v1`、更新 A0 第二类证据引用并保持 `[ ]`。显式排除了无生产构造的 WorkScene index/meta、`RubricStore`、`AdvancementStore`，`rootDir:null` 的 `UsageTracker`，进程内 Task/Memory store，P07 capacity probe空根，测试/build/golden，以及用户授权 workspace 文件副作用；旧 Memory 没有当前生产根或同义残余。
- 直接证据：正向从 `packages/cli/src/index.ts` 全部 A0-01 生产形态进入 startup/serve/executor/setup-delivery/access-surface/mesh/local-workspace/backup/DR 组合根，沿构造参数核对 `getZhixingHome`/`resolveHomeDir`、conversation/workscene/permission/skill paths、Authority/Executor setup、assignment spool、recovery/transfer/managed-service/log factory；反向以 `rg` 扫描非测试 `node:fs` 写调用、`File*`/Store/Repository/Journal/Ledger/Spool 构造、home/root helper、maintenance/retention、backup source/restore target 和 `rm/unlink/retire/compact`，所有候选归入 P01～P15或明确排除项，差集为空。只做只读源码/Git与静态查询，未访问真实 home、SecretStore、秘密或外部系统，未创建/删除目录，未运行应用、测试、构建或制品验收；最终 `git diff --check` 通过，索引为空且工作区只有本文。
- 交接类型：完成；生产持久根与 record-family exact-set 已闭合，没有未分类生产 root，也没有必须进入 schema/version 才能裁决 owner 的差集。
- 失效与恢复：A0-01～A0-05 证据未失效。以后若 home/config root selection、任一生产组合根的持久依赖、path helper、WAL/CAS/projection/Store/Repository/Spool factory、maintenance/retention/backup/restore/delete入口，或新增生产 `fs` writer 发生变化，必须恢复 `A0-06a-persistence-roots-v1` 与 A0 第二类索引；仅 record 字段/版本变化由 A0-06b 证据承担，除非同时改变 owner 或物理根。
- 遗留问题：没有 A0-06a 范围内未知。已经观察但刻意未裁决的边界包括 `credentials.json`、旧 server log，以及 versioned WAL/projection/transcript/snapshot/workspace/mesh/checkpoint/outbox 的字段、版本、strict/兼容读取和迁移语义；这些不是本证据的缺口，而是 A0-06b 的完整输入。包导出与正式制品仍留 A0-07，因此 A0 第二类保持 `[ ]`。
- 下一检查点：协调者独立复核本证据后进入 A0-06b；从 P01～P15 的唯一 owner 反向冻结 schema/version、兼容读取、cutover 与 migration exact-set，不得重新枚举根，不得顺带进入 A0-07。

### A0-06a 纠正：拆分 Server 同根记录的真实生命周期

- 基线：`HEAD 8c721788b17c33f326e2e759b2aa3a221c461859 + task-doc:A0-06a-persistence-roots-v1 + P13-lifecycle-correction`；索引仍为空，进场工作区只有本文的协调台账和 A0-06a 既有差异。
- 包边界：只重取 P13 的 pid/port、ready/state、token、server log 生命周期，并以同类风险最窄反查 P01～P15 的 close/delete/uninstall/retention断言；不重做根枚举，不进入 schema/version/legacy migration，不修改生产实现或其他文档。
- 当前责任与问题：初次 P13 把同在 `<home>/server.*` 下的记录误写成统一 Host-generation 资源，并错误声称 close 删除 token。生产事实是 pid/port为listener发现辅助，ready为background generation marker，state为foreground/background生命周期投影，token为跨Host重启复用的本机RPC认证事实，server log为独立retention治理的诊断记录。协调反证关于 token成立；继续核对还表明 state虽会在运行/收束中持久写阶段与heartbeat，正常有序cleanup最终仍会删除state和ready，只有异常退出或cleanup失败留下最后观测，故也不能把state写成无条件保留的终态事实。
- 本轮迁移：无生产迁移；P13保持一个物理根行，但拆成 pid/port、ready/state、token、log 四个 subfamily，分别登记creator/reader/跨重启/删除owner与A1/A3/A4/A6归属。新增同根生命周期裁决、恢复全等结论和A0-06b失效边界；A0第二类仍为 `[ ]`，未进入A0-06b。
- 直接证据：`packages/cli/src/serve/token.ts` 的 `loadOrCreateToken` 先读并复用满足接受条件的已有值、生成时写文件并尝试 mode 0600；Anchor `command.ts` 与 executor-only `executor-role-runtime.ts` 都把同一值交给 Server auth，`packages/server/src/client/discovery.ts` 与 `packages/cli/src/serve/stop.ts` 跨进程读取。`packages/server/src/process-lock.ts` 证明 `acquireLock` 在listen后覆盖pid/port而 `releaseLock` 只删这两项；`server-state.ts`、`shutdown-chain.ts`、executor cleanup和stale stop证明有序关闭依次写stopping/stopped后删state/ready，异常进程退出才会留下残余；Server log activation/lifecycle证明active log不随close删除、rotations才按策略prune。再反查anchor uninstall、device removal、managed unregister与所有 `rm/unlink`，没有token删除调用；最窄复扫P01～P15其余删除断言，没有把SecretStore、checkpoint、WAL、projection、outbox、managed definition或retention数据误判为Host marker。
- 交接类型：完成；P13物理根与record-family exact-set不变，生命周期子证据已恢复，没有第二个同类错误。
- 失效与恢复：协调复核作废初次 P13 的“close删除token”及其统一生命周期结论；本记录恢复 `A0-06a-persistence-roots-v1`。P01～P12/P14/P15的根、owner与生命周期证据未失效。以后若token create/read/delete、process lock acquire/release、ServerState cleanup、stop stale cleanup、uninstall/device-removal清理清单或server log retention任一变化，必须单独恢复P13；若只改变字段或兼容接受规则，交给A0-06b，除非同时改变生命周期。
- 遗留问题：无 A0-06a 范围内未知。token的精确接受/替换规则、lock/state字段与旧log兼容迁移矩阵仍是A0-06b输入，不能从本次生命周期纠正推导schema合同；包导出与正式制品仍留A0-07。
- 下一检查点：协调者独立复核本纠正后进入A0-06b；从已恢复的P01～P15 owner和subfamily出发冻结schema/version/compatibility，不得再把共享路径前缀当统一生命周期，也不得进入A0-07。

### A0-06b1：冻结 P01～P05 schema、version、兼容与迁移 exact-set

- 基线：`HEAD d13de7b65d551cb5ecb3c752ea13b09dec063daa + task-doc:coordinator-dispatch + A0-06b1-persistence-schema-v1`；进场索引为空，工作区只有协调者在本文登记 A0-06b1 的实时台账差异。
- 包边界：只消费 `A0-06a-persistence-roots-v1` 的 P01～P05，冻结 config/SecretStore/credential cutover、Conversation meta/transcript/snapshot、WorkScene cleanup cursor、permission records、Skill 本机物化/legacy import/admission temp 的当前 writer schema、版本识别、全部 production reader/codec、损坏与 partial 终态、兼容/cutover/删除责任；不重取 P06～P15 根，不进入 WAL/CAS、workspace、传输、恢复、Server、package export 或制品，也不实施迁移。
- 当前责任与问题：P01～P05 并不存在统一的 `v1 + strict codec` 合同。SecretStore envelope/backend/credential generation、cleanup cursor 是严格版本协议；conversation meta、Skill 文档/index/usage 无显式版本；transcript 主 Store不校验 parseable index/header version，而 read-only/retention面要求 v1；permission writer写 version 1但 reader不检查版本并逐 rule sanitize。真正旧格式接管只有 plaintext `credentials.json` 与按 id 导入 authority 的本机 Skill catalog。取证还反证 A0-06a P05 曾把无 production caller 的 `<home>/skills/.staging` 当成当前 admission root；正式 `admit_skill` 只使用 assignment-scoped OS temp。
- 本轮迁移：无生产迁移；新增 `A0-06b1-persistence-schema-v1` 的 17 个 subfamily，逐项反绑 producer、consumer、失败/恢复、兼容/cutover和 A4/A5/A6 目标责任。同步恢复 A0-06a P05：从 root exact-set剔除 home `.staging`，把 `SkillStore.prepareStaging/sweepStaleStaging/discardStaging` 归入“实现存在但无生产构造消费”，保留 OS temp admission workspace为唯一生产临时根。A0 第二类索引只增加本证据引用并保持 `[ ]`；A0、完成度和退出门均不变。
- 直接证据：完整重读本文、AE-001 与验证手册并核对 Git；正向逐项读取 `providers/config-loader` 与 config writer/consumer、`secrets` vault/platform backend/file-lock、credential loader/coordinator/legacy source、`core` ConversationRepository/ShardedTranscriptStore/FileTranscriptReadSource/SnapshotStore、CLI WorkScene storage cleanup、PermissionStore、SkillStore/paths/frontmatter、Anchor Skill global-state adapter、assignment Skill port与 builtin `admit_skill`；反向以 `rg` 核对所有相关 `JSON.parse`/JSONC parse、atomic/private-file writer、constructor、startup/recovery/retention/delete caller和 staging methods consumer。结果为表中成员与生产 writer/reader集合全等；`SkillStore` 上落到 home `.staging` 的三个实现只有定义/测试直接消费，production 同名 admission port 经 `createAgentRuntime → createAssignmentSkillPorts → SkillAdmissionWorkspace` 唯一落到 OS temp。未访问真实 home、SecretStore或秘密，未运行应用、测试、构建或制品验收。
- 交接类型：完成；P01～P05 没有未分类格式、第二 writer/reader、错误统一兼容结论或必须进入 P06/P07 才能裁决的 owner。各项无版本、strict、best-effort、sanitize/default、legacy-compatible 与 fail-closed 语义均已按实际 reader分别登记。
- 失效与恢复：本包发现并作废 A0-06a P05 关于 `<home>/skills/.staging` 是当前生产 admission temp 的子结论，已在同一物理根表、排除项、全等规则与本记录中纠正恢复；P01～P04、P05 其余 owner/root、P06～P15及 A0-01～A0-05证据未失效。以后若本证据列出的 loader/writer/codec、compatibility/cutover、retention/delete或 production consumer变化，恢复 `A0-06b1-persistence-schema-v1`；若同时改变物理 root、creator/owner或生命周期，再恢复 A0-06a。
- 遗留问题：无 A0-06b1 范围内未知。transcript与permission读面的版本宽松、config/meta/index/usage的弱结构校验、SecretStore generation前滚以及两条 legacy接管均是已冻结的当前迁移输入，不是未决问题。P06～P15 的 schema/version/compatibility仍待 A0-06b2/A0-06b3，package export和实际制品仍待 A0-07，因此 A0 第二类保持 `[ ]`。
- 下一检查点：协调者独立复核本证据后进入 A0-06b2，从 P06～后续已冻结 owner继续冻结 schema/version/兼容与恢复合同；不得重新枚举 P01～P05，也不得把本包的宽松 reader事实悄然改写成 strict或提前进入 A0-07。

### A0-06b1 纠正：拆分 Skill reader 容忍、CAS 原文与 Authority 规范化 writer

- 基线：`HEAD d13de7b65d551cb5ecb3c752ea13b09dec063daa + task-doc:A0-06b1-persistence-schema-v1 + S01-compatibility-correction`；索引保持为空，进场工作区只有本文既有 A0-06b1 差异与协调者将 S01 标为当前失效的实时台账修改。
- 包边界：只纠正 S01/S04 对 unknown frontmatter、原始 `SKILL.md` artifact、Authority materialization与 legacy import 收敛的描述，并最窄反查 S01～S05 其余“容忍/保留/覆盖/删除”断言；不重取 P01～P04，不进入 P06 schema、A0-06b2、源码或测试修改。
- 当前责任与问题：初次 S01 把 scan/read“能解析并忽略 unknown key、且自身不写盘”错误外推为 writer也保留 unknown frontmatter。生产 `materializeAuthority` 实际只把 authority entry 的 `name/description` 与 `parseFrontmatter(document).content` 交给 `stringifyFrontmatter`，因此会规范化重写本机 `SKILL.md`。S04 legacy import又先把未经 frontmatter重序列化的本机文档放入 CAS，再通过 durable pending进入同一 materializer；CAS artifact与本机投影承担不同保真合同。
- 本轮迁移：无生产迁移；S01现明确区分宽松只读阶段、本机原文暂存、CAS artifact与规范化 writer，补充 unknown key、frontmatter排版/顺序、正文边缘空白及附属文件的真实边界。S04现把 authority entry事实接管与 materialization pending收敛分开：commit前失败重试 import，commit后不再重复接管，物化失败由 pending恢复重试；本机 `SKILL.md` 不承诺与 CAS字节等价。
- 直接证据：逐行核对 `packages/core/src/skills/store.ts` 的 `scanSource/readDocument/materializeAuthority/create/update/fork/admit/readIndex/writeIndex/readUsage/recordHit`、`packages/core/src/frontmatter.ts` 的 parse/stringify trim与仅传入字段序列化、`packages/core/src/skills/global-state-adapter.ts` 的 `#importLegacyCatalog/#materializePending/reduceSkillDurableProjection/refreshStagedMutations`，以及 `GlobalMutationCommitCoordinator.recoverDerivedState` 的启动重驱。反向检查 S02 index 的 ordinary read/write与 authority exact replacement、S03 usage完整覆盖、S05 admission raw document入 CAS与 temp删除，未发现第二个错误的保留/删除断言。本包未运行测试、构建或制品验收。
- 交接类型：完成；`A0-06b1-persistence-schema-v1` 的 S01/S04 子证据已恢复，P01～P05 其余结论保持成立。
- 失效与恢复：协调反证作废初次“unknown frontmatter保留”及 S04 未区分 CAS/本机投影的表述；本记录以 production read → authority artifact → materialization writer三段链恢复。实时台账恢复为“无当前未恢复证据”，A0 第二类继续 `[ ]`。以后若 Skill frontmatter parser/stringifier、artifact内容、materializer、legacy import、durable pending或恢复入口变化，必须恢复 S01/S04，不能只检查 scan。
- 遗留问题：无本次纠正范围内未知。附属文件当前不属于 authority contentRef、existing本机目录可能继续留有附属文件，但新物化无法从该 artifact重建它们；这是已冻结的当前边界，不在 A0 实施修复。
- 下一检查点：协调者独立复核本纠正后才可进入 A0-06b2；不得复用初次 S01 的笼统“保留”措辞，也不得把通用 materialization pending扩写成 P06 migration schema。

### A0-06b2a：冻结 P06 Authority 存储基础容器 schema、version、兼容与恢复 exact-set

- 基线：`HEAD 3cc989877796294ebe6cdce7edbfd30120326d05 + task-doc:coordinator-dispatch + A0-06b2a-authority-storage-schema-v1`；进场索引为空，工作区只有协调者在本文登记本工作包的实时台账差异。
- 包边界：只消费 A0-06a P06，冻结共享CAS、Anchor/Executor主WAL、legacy identity、frame/envelope、bad-tail、WAL内durable projection与跨日志artifact-lifecycle的物理schema、版本、兼容与恢复；不展开任何领域record body，不进入P07～P15、包导出、产品旅程或架构实施。
- 当前责任与问题：P06不是一个统一“v1日志”。新WAL是header v2 + frame v2 + envelope v1；headerless legacy保持frame v1并以sidecar固定logId。projection又独立使用storage format v3与各自reducer version，artifact-lifecycle只是从一到两份WAL重建的跨日志derived index。A0-06a还把协议摘要目录简写成raw projectionId、把legacy identity写成每个WAL必有文件，本包据生产codec纠正了这两个物理子事实；顶层root、两owner与共享CAS结论未变。
- 本轮迁移：无生产迁移；新增 `A0-06b2a-authority-storage-schema-v1` 的A01～A06、12项production projection注册exact-set、lock/tmp边界、正反全等和失效规则；A0第二类只增加证据引用并保持 `[ ]`，A0、完成度与退出门均不变。
- 直接证据：完整读取本文、AE-001与验证手册并核对Git；逐段读取 `artifact-store.ts`、`artifact-references.ts`、`wal-frame.ts`、`commit-log.ts`、`durable-projection-index.ts`、`artifact-lifecycle-index.ts` 的所有物理writer/reader/codec/recovery关键路径；再以 `rg` 从 `setup-delivery.ts`、`mesh-bootstrap-store.ts`、`mesh-runtime-bootstrap.ts`、`surface-asset-authority.ts` 正向核对P06构造，以全仓production `new FileArtifactStore/new FileAuthorityCommitLog/new ArtifactLifecycleIndex/durableProjection` 反向分类P06与P07/P08/P09/P12复用实例。结果为A01～A06与12个WAL projection注册点全等；确认SHA-256而非CRC、`readRange`只验size、legacy identity条件存在、incomplete tail才quarantine、projection损坏可重建而权威WAL损坏不可由derived替代。本包未运行应用、测试、构建或制品验收，未访问真实home、秘密或用户数据。
- 交接类型：完成；CAS、两WAL/identity/frame/envelope、bad-tail、durable projection与artifact-lifecycle均有唯一writer/reader、版本识别、partial/损坏终态和维护/恢复owner，未触发projection/lifecycle独立拆包止损条件。
- 失效与恢复：A0-06a P06的raw projectionId路径与identity必有子结论被本包作废并在同一表恢复；P06其余root/owner/lifecycle及P01～P05、P07～P15未失效。以后按本证据末尾失效规则精确恢复；领域payload变化不得误伤物理frame证据。
- 遗留问题：无A0-06b2a范围内未知。manifest/page/write-intent不是统一exact-key拒绝、legacy WAL不主动升级、bad-tail/WAL crash-orphan tmp没有生产retention、`readRange`不复算全文件digest，均是已冻结的当前迁移输入，不是本包实施问题。P07～P15 schema/version/compatibility和正式package/tarball仍未冻结，所以A0第二类继续 `[ ]`。
- 下一检查点：协调者独立复核后进入A0-06b2b，直接从A0-06a P07冻结Executor/workspace记录；不得重做P06、把P07/P08/P12复用同类容器算成第二P06 owner，或提前进入A0-07。

### A0-06b2b1：冻结 P07 Executor 发布快照族 schema、绑定、兼容与恢复 exact-set

- 基线：`HEAD 2dd7c9ff43b60df182afad6c0adce149fb24f1af + task-doc:coordinator-dispatch + A0-06b2b1-executor-delivery-schema-v1`；进场索引为空，工作区只有协调者在本文登记本工作包的实时台账差异。
- 包边界：只消费A0-06a P07前四个主目标，冻结capability directory、snapshot-version、permission snapshot与execution-assets的schema/version、签名/摘要/identity/high-water绑定、全部production writer/reader、partial publication、重启/删除/恢复；明确不进入workspace-bindings/probes/capacity、P06、P08～P15、A0-07或任何实现。
- 当前责任与问题：四类记录由同一setup-delivery入口装配但不是一个事务，也不是同类cache。生产先发布permission或asset，再以E01分配本机revision、E02接受签名descriptor/inventory并最后标记directory established；活动Executor还要按lease digest保留E03历史规则。初次A0-06a把permission/assets一起写成可重建投影，会让后续迁移错误删除E01～E03的防回滚/活动执行输入；本包已按真实consumer纠正。E04另有一个已冻结writer约束：outer signer按id排序skills，inner Skill catalog validator按usage/createdAt/id次序验收，两种次序不同时发布在落盘前失败。
- 本轮迁移：无生产迁移；新增E01～E04主表、发布/partial矩阵、删除/DR/remote-trust裁决、正反全等与失效规则；同步纠正A0-06a P07类别与恢复表述，A0第二类索引增加本证据但保持 `[ ]`。未修改代码、测试、AE-001或其他文档。
- 直接证据：完整重读本文、AE-001与验证手册并核对Git；正向从`access-surfaces.ts` Anchor Host、`executor-role-runtime.ts` Executor-only与`workspace-command.ts` local fallback三处`setupAuthorityRuntime`，沿`setup-delivery.ts`的四个发布构造、`publishLocalExecutorSnapshot`、assignment prepare、generation install和Mesh snapshot service追踪；逐行读取`executor-snapshot-version-store.ts`、core `manifest.ts`/contracts、`permission-snapshot.ts`、`execution-asset-snapshot.ts`、`execution-asset-cache.ts`、`execution-snapshot-mesh.ts`与global query validators；反向以四个精确path和四个class constructor检索全部production读写，再核对Executor ledger、conversation/runtime Skill、Advancement Rubric、DR readiness、full-checkpoint retained artifact closure、remote authorization与device-removal cleanup。结果为production writer/安装者只由`setupAuthorityRuntime`装配，E04另有DR readiness只读构造；直接path consumer另有current-device精确cleanup。P06 checkpoint coverage不复制P07 index，workspace/probe/capacity未进入。本包只做只读静态取证，未运行测试、构建或制品验收，未访问真实home、秘密或用户数据；`git diff --check`与最终Git边界在本记录后执行。
- 交接类型：完成；E01～E04的schema/version、producer/reader、identity/revision绑定、全部可达损坏/partial终态与恢复/删除责任已正反全等，没有第二publisher、未分类格式或必须进入workspace generation才能裁决的前置。
- 失效与恢复：A0-06a P07的root/creator/delete集合保持；初次“permission/assets均可重建”子结论被本包作废并按E01～E04分类恢复。A0-06b2a及P01～P06/P08～P15未失效。以后按本证据末尾规则精确恢复，不能因P06 CAS承载body就把E04算成P06 cache owner，也不能把E03 lease digest当成历史snapshot重建源。
- 遗留问题：无A0-06b2b1范围内未知。E04 skill双重排序、corrupt index在普通query中降级miss但strict publisher不自愈、E03无per-snapshot retention、remote trust只隐藏而不持久revoke directory entry，以及device cleanup不把sibling lock/tmp列为领域目标，均是已冻结的当前迁移输入，不在A0实施修复。P07 workspace generation/probe/capacity仍待A0-06b2b2，P08～P15与package/tarball仍未冻结，故A0第二类保持 `[ ]`。
- 下一检查点：协调者独立复核后进入A0-06b2b2，只冻结`workspace-bindings/`、`workspace-probes/`与`capacity/`；不得重做E01～E04、提前展开P08或把workspace generation并回本包的非事务发布链。

### A0-06b2b2：冻结 P07 workspace binding/probe/capacity schema、generation、恢复与删除 exact-set

- 基线：`HEAD e1673674a7710b1414731a92667fcb2cf322fd14 + task-doc:coordinator-dispatch + A0-06b2b2-workspace-storage-schema-v1`；进场索引为空，工作区只有协调者在本文登记本包的实时台账差异。
- 包边界：只消费A0-06a P07后三个主目标与A0-06b2b1的E01/E02投影边界，冻结root manifest、initial/successor generation、workspace records/marker/artifacts、probe facts/marker与无record capacity目录；不重取E01～E04、P06或P08～P15，不进入package/tarball或实现。
- 当前责任与关键裁决：W01 root manifest是唯一active-generation线性化点；initial workspace与全部probe facts同处Executor主WAL但各有独立stream/reducer owner，successor workspace才使用独立P07 WAL。raw path内联严格workspace record，configured artifacts当前没有workspace payload producer；两个marker只证明对应WAL stream建立；capacity只创建目录供`statfs`，盘上没有policy或恢复事实。W01缺失会按fresh initial重开而不扫描successor、incomplete probe重试必须重新满足时效、旧generation与未完成probe无专属retention，均是已冻结现状而非本包修复。
- 本轮迁移：无生产迁移；新增W01～W08主表、reset/crash矩阵、consumer/delete/backup裁决、正反全等与失效规则。同步精化A0-06a P07路径和artifact简写，A0第二类索引追加本证据并保持 `[ ]`；A0、完成度和退出门未改变，未修改代码、测试、AE-001或其他文档。
- 直接证据：完整重读本文、AE-001与验证手册并核对Git；正向从`access-surfaces.ts`、`executor-role-runtime.ts`与`workspace-command.ts`三类production caller进入`setup-delivery.ts`唯一构造，逐段读取core `workspace-binding-catalog.ts`、`workspace-bindings.ts`、`workspace-probe.ts`及protocol validators、P06 commit-log inline/artifact约束、CLI local workspace facade/host、WorkScene/Mesh probe、assignment preflight/recovery与Advancement evidence；反向以三精确root、全部constructor、`catalogGeneration/workspaceBindingRevision` consumer、maintenance/cleanup和backup/recovery关键词回扫。最窄`rg`确认排除测试后catalog/probe constructor各只有`setup-delivery.ts`一处，legacy migration只有返回port和内部委托而无production caller，workspace/probe删除/backup类命中只有current-device cleanup，workspace records无`ArtifactRef`，capacity fs操作仅`mkdirSync+statfsSync`。结果为W01～W08与所有production writer/reader全等。只做只读静态取证，未运行应用、测试、构建或制品验收，未访问真实home、秘密或用户数据；`git diff --check`通过，HEAD仍为`e1673674a7710b1414731a92667fcb2cf322fd14`，索引为空且工作区只有本文。
- 交接类型：完成；P07剩余workspace/probe/capacity的schema/version、身份/generation绑定、partial/crash、maintenance/delete/recovery与consumer集合已闭合，没有第二owner、隐藏manifest、marker/cache权威化或按目录名虚构的record。
- 失效与恢复：A0-06a P07的generation路径与raw-path artifact简写被本包精化并恢复，顶层root/creator/delete未失效；A0-06b2b1及P01～P06/P08～P15未失效。以后按本证据末尾规则精确恢复，不得因P06物理WAL格式变化外的领域record调整误伤A0-06b2a。
- 遗留问题：无A0-06b2b2范围内未知。manifest缺失回到initial、random tmp无sweep、legacy workspace writer port无production caller、旧generation不GC、marker内容不验、started-only probe不retire与capacity目录跨重启保留，均是已冻结的当前迁移输入，不在A0实施修复。P08～P15 schema/version/compatibility及package/tarball仍未冻结，故A0第二类保持 `[ ]`。
- 下一检查点：协调者独立复核后进入A0-06b3，从A0-06a P08～P15 owner逐包冻结传输、恢复、宿主与诊断记录；不得重做P07、把Executor主WAL中的workspace/probe streams算成第二P06 owner，或提前进入A0-07。

### A0-06b3a：冻结 P08/P09/P11/P12 跨设备传输与恢复耐久链

- 基线：`HEAD fe776d1306742e99df70d34997ee378d47ee4a6e + task-doc:coordinator-dispatch + A0-06b3a-distributed-transfer-storage-schema-v1`；进场索引为空，工作区只有协调者在本文登记本工作包的实时台账差异。
- 包边界：把 P08 assignment stream/evidence、P09 Mesh 本机投影/配对 continuation/asset partial、P11 backup target/checkpoint/incoming/root binding 与 P12 conversation/planned-anchor/DR staging 作为同一条“bytes 产生、传输、暂存或冻结、Authority 消费、终态清理”耐久链闭合；只引用 P06 基础 WAL/CAS 格式，不重取 P01～P07，不进入 P10/P13～P15、package/tarball、产品旅程或架构实施。
- 当前责任与关键裁决：P08 spool/evidence 只保证执行流与请求效果可重放，不拥有 assignment、Advancement 或 transcript；P09 endpoint/peer/completion、continuation 与 partial 是 Mesh 投影/续传状态，P06 trust/grant 仍是唯一权威；P11 checkpoint 是由 P06 closure 生成并经 target manifest-last 发布的冻结备份，incoming/binding 只承担续传和 root 激活前置；P12 三条链均在私有校验/提升后单向提交 P06，conversation 以 commit/base 原子事务、planned/DR 以 `installPlannedAnchorPrefix` 为唯一 owner 切换点，candidate、private WAL、promotion partial 不形成第二状态机。
- 本轮迁移：无生产迁移；新增 D01～D11 主表、五项跨根线性化/Authority 裁决、正反全等与精确失效规则。同步纠正 A0-06a 两处过宽简写：current-device removal 不删除 `mesh-pairing-continuation.json`；conversation transfer 仅 abort 路径清 staging，commit 后私有副本当前没有 retention/delete owner。P08/P11 顶层 root 与四族唯一 owner 未变，A0 第二类只增加本证据并保持 `[ ]`。
- 直接证据：完整重读本文、AE-001 与验证手册并核对 Git；正向从 Anchor+local/Executor-only Host、pair/backup/DR 命令、Mesh runtime、恢复根前置 Host 与 Surface/assignment transport 进入四族生产构造，逐段核对 `assignment-stream-spool.ts`、`evidence-journal.ts`、`mesh-bootstrap-store.ts`、pair continuation、resumable artifact receiver、checkpoint owner/target/paired receiver、conversation transfer 与 planned-anchor/DR target/command；反向以精确 root、全部 `FileAuthorityCommitLog`/`FileArtifactStore`/receiver 构造、record discriminant、inventory/restore/catch-up、maintenance/retention 与 cleanup/device-removal 入口回扫。结果为 D01～D11 writer/reader/consumer 全等，且确认 P06 assignment/trust/grant/install、P02 transcript、P07 snapshot 只是权威输入/终点而非本四族成员。只做只读静态取证，未运行应用、测试、构建或制品验收，未访问真实 home、秘密或外部系统；`git diff --check` 与最终 Git 边界在本记录后执行。
- 交接类型：完成；四族 schema/version/兼容、身份绑定、线性化、partial/crash/replay、retention/delete/backup 和跨根关系均已闭合，没有第二 Authority、遗漏 writer/reader、按目录名虚构格式或必须扩入 P10/P13～P15 才能裁决的前置。
- 失效与恢复：A0-06a P09/P12 上述两处简写被本包作废并在 D04/D09 与删除集合裁决中恢复；P08/P11、P01～P07 及 P10/P13～P15 未失效。以后按本证据末尾 D01～D11 变化条件精确恢复，只有顶层 root、唯一 owner 或删除生命周期同时变化时才恢复 A0-06a 对应 P 项。
- 遗留问题：无 A0-06b3a 范围内未知。endpoint/peer outer reader 非 exact、pair continuation 为浅层 shape 校验且越过 device cleanup、backup-target writer 异常无 tmp sweep、P08 spool/evidence 无 device cleanup、conversation commit 后 staging 无 cleanup、planned/DR candidate 与 private journal 无通用 GC，均是已冻结的当前迁移输入，不在 A0 实施修复。P10/P13～P15 与 package/tarball 尚未冻结，所以 A0 第二类继续 `[ ]`。
- 下一检查点：协调者独立复核后冻结 P10/P13～P15 的本机宿主运行记录，再由 A0-07 冻结 package exports 与正式制品/安装入口；不得重做 P08/P09/P11/P12，或把共享 P06 CAS 物理载体误算成第二业务 owner。

### A0-06b3a 纠正：拆分 Evidence Journal writer、恢复 reader 与 bundle 绑定强度

- 基线：`HEAD fe776d1306742e99df70d34997ee378d47ee4a6e + task-doc:A0-06b3a-distributed-transfer-storage-schema-v1 + D02-reader-correction`；索引保持为空，进场工作区只有本文既有 A0-06b3a 差异与协调者将 D02 标为失效的实时台账修改。
- 包边界：只重取 D02 `EvidenceJournal` 的 production writer、`#load`、replay/begin、complete 与 compact，复核 P08/跨根/失效规则；D01、D03～D11、P10/P13～P15、代码、测试和其他文档均不在范围。
- 当前责任与问题：初次 D02 把 TypeScript writer union 的两种shape误写成恢复record exact合同，又把首次`complete()`写入前的bundle `requestId/requestDigest/executorId`交叉校验外推成`#load`合同。生产reader实际接受canonical且核心字段有效的额外record顶层字段，恢复bundle只验证自身strict shape/signature；重复合法completed按日志最后一项覆盖。replay虽把当前来请求与pending canonical全等，仍不重新把恢复bundle三字段绑回pending。
- 本轮迁移：无生产迁移；D02现分别登记writer shape、reader接受域、首次complete强绑定、恢复/replay弱绑定、last-completed-wins与compact规范化重写。该宽松接受域只是当前迁移需要保护或显式收敛的输入，不是产品能力、兼容承诺或本包修复授权。
- 直接证据：逐行核对 `packages/orchestrator/src/advancement/evidence-journal.ts` 的record构造、`#append/#load/replay/begin/complete/compact/isExecutionResult`，并沿 `executor-evidence.ts:collect/#collectOnce`确认journal replay前仍执行strict request、目标executor与current-owner guard；反查core `validateEvidenceRequest/validateEvidenceBundle`确认嵌套request/bundle自身为strict codec，但journal record顶层没有`hasExactKeys`。静态事实足以唯一裁决，未运行测试、构建或制品验收。
- 交接类型：完成；D02的writer shape、恢复接受域、调用期/恢复期绑定强度、replay与compact终态已经一致，P08 record-family exact-set、非Authority裁决、27天retention及无device cleanup结论继续成立。
- 失效与恢复：协调反证作废初次 D02 的“record exact”与“恢复bundle继续三字段交叉绑定”表述；本记录按 writer → reader → replay/complete → compact 四段链恢复 `A0-06b3a-distributed-transfer-storage-schema-v1`。D01、D03～D11、A0-06a P08 root/owner/lifecycle与其他既有证据未失效。
- 遗留问题：无本次纠正范围内未知。record顶层额外字段会被reader接受后静默丢弃、重复completed恢复时last-wins、恢复bundle不与pending三字段全等，均是已冻结的当前迁移输入，不在A0修复；不得在后续把这些宽松行为描述成正式兼容能力。
- 下一检查点：协调者独立复核本纠正后才可进入 P10/P13～P15；若 Evidence Journal writer/reader、bundle validator、handler pre-replay guard、replay/complete或compact任一变化，必须恢复 D02，不能只检查typed record定义。

### A0-06b3b：冻结 P10/P13～P15 本机 Host 运行、管理与诊断记录

- 基线：`HEAD 0e315cb7a5788caf136046afc6c9c54df6a86fb3 + task-doc:coordinator-dispatch + A0-06b3b-local-host-storage-schema-v1`；进场索引为空，工作区只有协调者在本文登记本工作包的实时台账差异。
- 包边界：把 P10 本机 workspace management outbox/lease/IPC、P13 Server discovery/state/token/log、P14 三平台托管 definition/OS registration 与 P15 LLM/keypress 诊断日志作为一条“Host发现与互斥→入口发布→持久认证→状态投影→托管注册→诊断治理”责任链冻结；不重取 P01～P12，不进入 package export/tarball、产品旅程或架构实施。
- 当前责任与关键裁决：P10只有一个workspace management owner，IPC先发布让status/list可观察恢复，但mutation必须等outbox与Host ready；P13 pid/port、state/ready、token和log共享home却有四种生命周期，token与日志跨Host重启而generation投影有序删除；P14 canonical definition与OS registration分段安装/删除，只启动同一Host且不具Domain权威；P15没有任何产品、Authority、恢复或backup consumer。上述记录都不能凭共同路径、Host进程或“持久化”名称合并成第二Host或业务事实源。
- 本轮迁移：无生产迁移；新增 H01～H10 主表、五项跨根生命周期/Authority 裁决、正反全等与精确失效规则。同步精化 A0-06a：P10不是“恢复完才开放全部IPC”，而是“先开放查询、mutation fail closed”；P14 normal unregister虽按manager→file删除，但manager成功后definition删除失败可由外层按manager absent收敛并遗留文件。两项顶层root、唯一owner与产品边界未失效，A0第二类只增加本证据并保持 `[ ]`。
- 直接证据：完整重读本文、AE-001与验证手册并核对Git；正向从`packages/cli/src/index.ts`的serve/workspace/status/stop/logs/doctor/managed/current-device入口进入`local-workspace-management-host.ts`、`local-workspace-operation-outbox.ts`、`local-workspace-owner.ts`、`serve/command.ts`、`executor-role-runtime.ts`、`topology-command.ts`与Server lifecycle；逐段核对`process-lock.ts`、`server-state.ts`、`serve/token.ts`、client discovery/auth、server log lifecycle/migration、`managed-service.ts`/runtime/reconciler及`llm-chunk-dump.ts`、`keypress-dump.ts`。反向按全部精确路径、constructor、`write/rename/unlink/rm/prune/releaseLock/cleanup/unregister`与device-removal调用回扫；结果为H01～H10 writer/reader/consumer全等，Unix socket/Windows pipe、OS registration、process fd/timer和历史legacy log均已按真实职责分类。只做只读静态取证，未运行应用、测试、构建或制品验收，未访问真实home、秘密、外部系统或OS注册状态；`git diff --check`通过，HEAD仍为`0e315cb7a5788caf136046afc6c9c54df6a86fb3`，索引为空且工作区只有本文。
- 交接类型：完成；P10/P13～P15 的schema/version、reader接受域、writer/consumer、原子/partial终态、恢复、清理、retention与跨平台差异已经闭合，没有第二Host、第二Authority、遗漏production writer/reader或按目录名虚构的统一生命周期。
- 失效与恢复：A0-06a P10的入口开放顺序和P14的删除终态被本包精化并已在主表、跨根裁决与实时台账恢复；P13/P15及P01～P09/P11/P12未失效。以后按本证据末尾H01～H10条件精确恢复，只有root、唯一owner或删除生命周期同步变化时才恢复A0-06a对应P项。
- 遗留问题：无 A0-06b3b 范围内未知。outbox transport早于恢复开放、PID/state reader宽松、token接受任意至少32字符且无删除owner、server log migration只在active缺失时发生、macOS/Linux manager inspect不核加载definition bytes、manager删除后可能遗留definition、keypress无retention与显式timer dispose，均是已冻结的当前迁移输入，不在A0修复，也不得包装成正式兼容承诺。package exports与正式制品/安装入口仍未冻结，所以A0第二类继续 `[ ]`。
- 下一检查点：协调者独立复核后进入A0-07，只冻结package exports、build/tarball与安装入口；不得重做P01～P15，或把Host临时协调状态、持久token、托管registration和诊断日志合并成通用状态owner。

### A0-06b3b 纠正：恢复期请求 gate 与 managed-service orphan definition

- 基线：`HEAD 0e315cb7a5788caf136046afc6c9c54df6a86fb3 + task-doc:A0-06b3b-local-host-storage-schema-v1 + H02/H07/H08-correction`；索引保持为空，进场工作区只有本文既有 A0-06b3b 差异与协调者将 H02/P10、H07～H08/P14 标为失效的实时台账修改。
- 包边界：只重取 `LocalWorkspaceManagementHost.#handle/#tryInitializeOutbox/#drainLoop` 与 `buildManagedServiceSpec/NodeManagedServiceAdapter.inspect/install/unregisterFutureExact`、maintenance/program-uninstall/current-device cleanup；H01、H03～H06、H09～H10、P01～P09/P11/P12、代码、测试和其他文档均不在范围。
- 当前责任与问题：初次H02把所有outbox请求概括为outbox-ready+Host-ready，实际查询完全不读outbox，pending/ack只依赖outbox-ready，新prepare/prepared commit才需要ready，committed/completed则按durable identity在recovering或已进入handler的draining阶段继续前滚/重放。初次H07/H08漏掉`dev.zhixing.host.`前缀，又把manager存在时的drift、manager absent时的orphan和outer cleanup收敛混成统一拒绝。
- 精确纠正：P14三平台共享同一file+manager组合器。manager存在时file缺失/漂移会令`matches:false`，Windows还严格核Task投影；manager absent时install会进入definition物化，但`writeDefinition`只创建missing、幂等复用byte-equal orphan，mismatched orphan仍抛`definition-drift`且不覆盖。normal unregister按manager→file推进；manager已删而file删除失败后，带spec的maintenance/program uninstall和current-device cleanup可按manager absent收敛并留下orphan，无SecretStore的program uninstall则明确要求file absent。
- 本轮迁移：无生产迁移；已同步修正P10/P14主表、H02/H07/H08、跨根裁决、正反全等、精确失效规则和实时台账。初次A0-06b3b记录作为历史保留，由本纠正记录明确恢复；A0第二类仍为`[ ]`，未进入A0-07。
- 直接证据：逐行读取`packages/cli/src/runtime/local-workspace-management-host.ts`的handler分支、writable/outbox gate和committed drain；读取`packages/cli/src/serve/managed-service.ts`的完整service ID派生、file/manager合并inspect、definition writer、三平台manager inspect/install/unregister；再沿`managed-service-runtime.ts`的maintenance/program uninstall以及`command.ts`、`executor-role-runtime.ts`、`device-removal-cleanup.ts`反查outer convergence。静态调用图确认Windows/macOS/Linux共享file lifecycle而manager read-back强度不同，也确认协调简写中的“orphan可覆盖”只对missing创建/byte-equal复用成立，mismatched bytes不会被覆盖。未运行测试、构建或制品验收，未访问真实home、秘密、外部系统或OS注册状态；`git diff --check`通过，HEAD仍为`0e315cb7a5788caf136046afc6c9c54df6a86fb3`，索引为空且工作区只有本文。
- 交接类型：完成；H02已按recovering/ready/draining和请求kind冻结，H07/H08已按完整identity、manager present/absent、file equal/drift与normal/partial cleanup冻结，没有扩大其他证据失效范围。
- 失效与恢复：协调反证只作废初次H02/P10的统一双gate，以及H07/H08/P14的裸hash和统一drift表述；本记录按两条实际状态机恢复`A0-06b3b-local-host-storage-schema-v1`。H01、H03～H06、H09～H10及所有较早证据未失效；以后按主证据末尾精确条件恢复，不能只检查request union或definition模板。
- 遗留问题：无本次纠正范围内未知。recovering/draining中的outbox-only与durable replay、manager-absent orphan可越过部分maintenance/uninstall/device cleanup、macOS/Linux不核已加载definition bytes，均是当前迁移输入，不是兼容或产品承诺；mismatched orphan在install中仍fail closed，不能误记为自动覆盖能力。
- 下一检查点：协调者独立复核本纠正后才进入A0-07；不得重做H01/H03～H06/H09～H10或提前调查package/tarball。

### A0-07a：冻结生产包清单、package exports、构建图与版本绑定

- 基线：`HEAD ed6e4f2492c876b70ec7aed871e212677ce88114 + task-doc:coordinator-dispatch + A0-07a-package-export-build-v1`；进场索引为空，工作区只有协调者在本文登记 A0-07a 的实时台账差异。
- 包边界：只冻结 workspace/project/package 集合、manifest 公开字段、workspace/external/dev 依赖、production import 与 exports 差集、tsup/tsconfig/mesh helper/root build DAG 及 release version 绑定；不读取旧 dist 作为源码证明，不执行 build/test/pack/install/package-check，不进入真实 tarball、安装后解析、发布顺序或 Windows x64 smoke。
- 当前责任与关键裁决：根 project 是私有版本/脚本协调点；16 个 package 是 manifest-declared public 候选，CLI 只以双 bin 交付，15 个 public library 共 105 个显式 ESM export 键，test-utils 是另 1 个私有测试 export。所有 production AST import 都通过目标 package export 且有 workspace production dependency；没有跨包相对源码旁路或 tsconfig alias。构建图由17个package的tsup entry与workspace DAG组成，不把旧dist、源码路径或test-utils当正式产品入口。
- 本轮迁移：无生产迁移；新增 K01～K17 主表、共同 manifest/dev 依赖、entry/输出/DAG、production import反查、runtime export gate覆盖边界、version check/write合同、正反全等与精确失效规则。A0第二类索引只追加本证据并保持`[ ]`，A0、完成度、其他索引和退出门均未改变。
- 直接证据：完整重读本文、AE-001与验证手册并核对Git；读取`pnpm-workspace.yaml`、根及17个package manifest、17份tsup/tsconfig、core contracts tsconfig、mesh helper build、runtime export gate、release-version脚本与CLI版本投影。以`pnpm -r list --depth -1 --json`重取project集合，以只读topological `pnpm -r --workspace-concurrency=1 --sort exec node -e ...`核对17包顺序；用TypeScript AST静态枚举非测试production import并与manifest/exports双向差分，用只读config import核对entry/target，结果为缺包/缺export/未声明dependency/跨包relative/alias/正式target未构建均0。运行`pnpm release:version:check`通过；未运行runtime export gate、build、test、pack、install或package-check，避免以旧dist自证。末尾`git diff --check`通过，HEAD仍为`ed6e4f2492c876b70ec7aed871e212677ce88114`，索引为空且工作区只有本文。
- 交接类型：完成；workspace包、manifest surface、依赖方向、生产consumer、构建entry/输出/顺序和统一版本约束在当前源码基线上双向闭合，未发现第二bin/main、隐藏正式export或未构建正式入口。
- 当前差异与遗留：A0-07a无未分类未知。已冻结的迁移输入为orchestrator声明但不消费`@zhixing/network`；core额外构建但不独立export的confirmation/identity；多组正式export暂无仓内production consumer；runtime export gate仅覆盖core全manifest与跨包selected symbols，不是全包exact-set；CLI build不产d.ts且无独立typecheck。这些不在A0修复，也不包装成外部兼容承诺。正式tarball成员、workspace依赖改写、clean install/bin/helper运行、发布装配及Windows x64 smoke仍待A0-07b，因此A0第二类保持`[ ]`。
- 失效与恢复：workspace pattern、任一manifest字段/dependency/export/bin/files/version、production import、root/package build脚本、tsup/tsconfig/lock解析、mesh helper、runtime export gate、release-version脚本或CLI版本投影变化时，恢复`A0-07a-package-export-build-v1`与A0第二类索引；仅tarball/install/publish/smoke变化恢复A0-07b。A0-01～A0-06b3b均未失效。
- 下一检查点：协调者独立复核并提交本证据后进入A0-07b，在全新build基础上冻结16个正式tarball、安装/运行入口、workspace协议发布改写、发布顺序及Windows x64 smoke；不得重做K01～K17静态集合，若制品反证manifest/build边界则先恢复本证据。

### A0-07b：冻结正式 tarball、安装/运行入口、发布装配与 Windows x64 制品 smoke

- 基线：`HEAD 25fac98902e1e8e745e8840748c6fcaa94ff605a + task-doc:coordinator-dispatch + A0-07b-package-install-smoke-v1`；进场索引为空，工作区只有协调者在本文登记 A0-07b 的实时台账差异。
- 包边界：复用 A0-07a 已接受的16个public package与exports/build静态集合，只冻结fresh build→真实tarball→隔离安装→installed entry/CLI/helper smoke→uninstall sentinel，以及默认零写/显式确认发布装配；没有重取K01～K17，没有进入产品旅程、A1或任何registry写操作。
- 当前责任与关键裁决：package check是Windows x64本地制品验证owner，临时consumer不是发布或用户安装事实源；npm是正式global安装/卸载owner，CLI双bin只是manifest正式入口；publish脚本是唯一registry装配入口且默认零写，`latest`是所有candidate与registry安装复验后的最后一步。门禁直接文件URL/dist执行、ignore-scripts与sentinel存在性已和package-specifier/bin-shim/global install/lifecycle execution/home exact-tree严格分开。
- 本轮迁移：无生产迁移。新增16包tarball identity、C01～C08状态机、发布确认顺序、证据/信任边界、门禁缺口、正反全等和精确失效规则；A0第二类索引在事实闭合后标为`[x]`，A0总项、完成度、其余索引和退出门保持`[ ]`。
- 直接证据：完整读取本文、AE-001、验证手册的Windows build/`.cmd`/package-check规则，读取根与package manifest、`package-check.mjs`、`npm-delivery-structure.test.mjs`、`publish-npm.mjs`、install-script audit及正式安装/维护文档。只运行一次`pnpm package:check`并以exit 0完成：结构测试3/3、fresh全仓build、16包逐包pack/inspect、隔离consumer安装232包、installed entry与CLI/helper smoke、CLI卸载sentinel保护均通过，聚合tarball SHA-256为`356aac58b581d1f2ed7b1885dddc39bdc96c6cba53387c722b7cac7d9b38ecc3`。未预跑或重跑build/structure/pack/install/package-check，未运行publish确认分支或任何npm身份/权限/registry命令。结束后`.zhixing-package-check-*`、`checkpoint_child_bridge/csc/tsup`进程均为0；`git diff --check`通过，HEAD未漂移，索引为空且工作区只有本文。
- 交接类型：完成；同一fresh-build基线上的16个tarball、packed dependency改写、允许成员、isolated install、直接installed targets、CLI行为与Windows helper真实子进程均闭合，发布默认零写和显式registry状态机也有唯一源码入口。没有第二交付路径或未分类制品入口。
- 当前差异与遗留：A0-07b无未分类未知。已知门禁缺口是Node package-specifier、packed manifest exports/bin 与 source 全等、`types`、npm bin shim、global install、第三方lifecycle真实执行、home exact-tree及真实registry确认分支未验证；publish确认分支也未把先前fresh-build摘要耐久绑定到其再次pack的候选。这些只作为后续门禁/发布输入，不包装成通过事实，不授权执行registry写入。
- 失效与恢复：public package/version/dependency/files/bin/exports、build/pack、package-check/structure/audit、CLI/helper入口、正式安装文档或publish状态机变化时，恢复`A0-07b-package-install-smoke-v1`与A0第二类索引；若同时改变workspace/import/build静态集合，再恢复A0-07a。A0-01～A0-06b3b未失效。
- 下一检查点：协调者独立复核本证据后，按当前台账冻结产品旅程、行为、错误、终态与恢复；不得把A0-07b的门禁缺口误当功能缺陷顺手修复，也不得进入A1。

### A0-08a：冻结首次配置、配置编辑与运行就绪产品旅程

- 基线：`HEAD 7f419eaf12a28cca1c44108e6b9e359c26d84cbe + task-doc:coordinator-dispatch + A0-08a-setup-readiness-journey-v1`；进场索引为空，工作区只有协调者在本文登记A0-08a的实时台账差异。
- 包边界：只冻结首次/既有/损坏配置进入默认REPL、Host、配对和本机workspace fallback的共同startup gate，REPL `/config`/`/mcp`提交与Host换代，以及doctor对同一事实的观察边界；复用A0-04命令集合、A0-05 config/Profile与A0-06b1持久schema，不重取日常对话、工作场景/任务、设备/备份/维护旅程，也不进入A1。
- 当前责任与关键裁决：public config与SecretStore active generation分别是公开/秘密唯一事实，`runStartupCheck`是主产品结构性ready owner，editor是唯一用户writer，缺失模板物化与配对Mesh回写是同一config owner下的系统writer，runtime/setup-delivery只消费并签发投影，doctor只是不会解锁秘密或运行semantic/model gate的弱离线观察者。顶层不存在`zz setup`，首次配置只能从默认`zz`触发；四个startup生产callsite、两个运行中编辑入口及全部writer/consumer已正反闭合。
- 本轮迁移：无生产迁移。新增J01～J08入口矩阵、S01～S11状态/提交/恢复矩阵、配置与秘密写入顺序、跨表面行为、正反全等与失效规则；A0第三类索引只追加本证据并保持`[ ]`，A0、完成度、其余索引和退出门均未改变。
- 直接证据：完整读取本文、AE-001和验证手册；静态读取CLI Commander/default/serve/doctor、`startup.ts`、REPL/CoreHost与config command、config editor/checks、providers path/config/credential loader、SecretStore、pair/workspace fallback、Provider/MCP/Channel装配、executor readiness/setup delivery，并以`rg`从`runStartupCheck`、config/credential writer、path override、ready/error文案和直接测试反向回扫。没有运行应用、测试、构建或制品门禁，没有访问真实home、SecretStore、凭据、Provider/MCP网络或外部账号。
- 交接类型：完成，等待协调者独立复核；首次设置、编辑、提交、cancel/error、运行时消费与reload终态均有唯一生产链，未发现第二配置事实源、第二setup wizard或绕过startup ready后创建正常REPL/Host主图的入口。
- 当前差异与遗留：已登记四项真实迁移输入：`ZHIXING_CONFIG_PATH`在默认startup、REPL编辑writer、显式home Host/fallback与doctor间产生文件选择分裂；doctor可能把parseable但not-ready的配置报告healthy且问题报告退出0；config→credentials→Host reload没有跨层事务；默认模板仍向首次用户提及已退役`/enter`。现有直接测试还缺完整startup editor/cancel、跨store partial和doctor/readiness差异的识别性覆盖；这些不在A0修复，也不包装成目标能力。
- 失效与恢复：Commander/default/serve/pair/workspace入口、startup、config path/loader/validator/default、SecretStore/credential generation、editor/checks、REPL config command/Host reload、doctor、Provider/MCP/Channel/runtime/setup-delivery消费或相关文案/直接测试变化时，恢复`A0-08a-setup-readiness-journey-v1`与A0第三类索引；字段schema或持久owner变化再恢复A0-05/A0-06b1。A0-01～A0-07b未因本轮静态旅程取证失效。
- 下一检查点：协调者独立复核本证据后进入A0-08b，冻结日常对话、历史、上下文与运行终态；不得把本轮已知差异当作修复授权，也不得提前迁移ApplicationHost。

### A0-08b1：冻结日常会话选择、创建、恢复、历史、命名与清空旅程

- 基线：`HEAD 1864c61cfbc73c6f0745f64eafde4631811a3d4e + task-doc:coordinator-dispatch + A0-08b1-conversation-lifecycle-journey-v1`；进场索引为空，工作区只有协调者在本文登记A0-08b1的实时台账差异。
- 包边界：复用A0-04的session RPC/notification exact-set、A0-06a P02/P06与A0-06b2a物理事实，只冻结ready后会话选择、新建、恢复、历史、名称、clear及同根inline delete旅程；排除单轮模型/工具、segment/attention/context/compact、abort与run终态，也不进入WorkScene、Task/Schedule/Advancement/Delivery、设备/备份/维护或A1。
- 当前责任与关键裁决：Surface当前指针和observer名册均非耐久产品事实；常规Anchor路径由P02 meta承载立即可见的identity/name/list、Transcript承载原文与clear边界，P06 Authority承载accepted run及可重放clear/delete lifecycle。`session.new`先建P02空壳、Authority按首次input或lifecycle操作惰性建立；executor-only local-only路径则由`LocalConversationOwner.createConversation→ensureSession`先建Authority并用同一RPC wire投影。两种拓扑不是一个创建事务，Channel也只是绕过RPC handler的第二应用编排路径而非第二Conversation owner。
- 本轮迁移：无生产迁移。新增C01～C10入口/表面矩阵、九类失败与竞态终态、owner/拓扑裁决、保护证据缺口、正反全等与精确失效规则；确认生产没有`/history` slash命令，真实历史表面是启动/恢复3轮tail、降级只读浏览器和`session.history`分页。A0第三类索引增加本证据但保持`[ ]`，A0、完成度、其余索引和退出门均未改变。
- 直接证据：完整读取本文、AE-001与验证手册；静态追踪`repl.ts`、session command/Controller/facade/history/candidate-delete/read-only browser，Server session registry/handlers、ConversationDirectory/Manager，core ConversationRepository/Transcript，CLI ConversationProtocolRuntime/session-state/local owner/router/Mesh及Channel InboundRouter/binder；再由A0-04公开集合、path/writer/reader/recovery/notification和相关CLI/server/core/owner/channel直接测试反向回扫。没有运行应用、测试、构建或制品门禁，没有访问真实home、秘密或外部系统。
- 交接类型：完成，等待协调者独立复核；公开入口、全部生产writer/reader、clear/delete恢复、跨Surface投影和异常终态已双向闭合，没有未分类会话生命周期入口或第二Authority。
- 当前差异与遗留：登记四项迁移输入而不修复：常规与local-only创建的P02/P06先后不同；`new/rename/resume`没有clear/delete同等级的response-loss stable replay；auto-name最终写前仍有覆盖同期手动rename的TOCTOU且失败不重试；RPC具完整历史分页但REPL没有`/history`且只显示有限tail。直接测试还缺前三项关键response-loss/TOCTOU/new-after-delete-failure的端到端识别力，这些不包装成当前承诺。
- 失效与恢复：REPL startup/controller/session command/candidate/history、session RPC/registry/facade/notification、ConversationDirectory/Repository/Manager、Transcript clear/history/retention、ConversationProtocolRuntime lifecycle/session-state、local owner/router/Mesh、Channel identity/inbound、auto-name或相关测试变化时，恢复`A0-08b1-conversation-lifecycle-journey-v1`与A0第三类索引；物理root/schema/Authority容器同时变化再恢复A0-06a/A0-06b2a。A0-01～A0-08a未因本轮静态取证失效。
- 下一检查点：协调者独立复核后进入A0-08b2，冻结单轮模型—工具—上下文、compact/cancel与run终态；不得重做本包会话身份/历史生命周期，不得把已登记差异当作A0修复授权或提前迁移Conversation/Product API。

### A0-08b2a1：普通用户请求准入、身份与排队旅程（安全交接）

- 基线：`HEAD 02e54a49c7e1126612ae85707312c0ab0eae9dad + task-doc:coordinator-dispatch + A0-08b2a1-turn-admission-journey-v1(partial)`；进场索引为空，工作区只有协调者在本文登记本工作包的实时台账差异。
- 包边界与实际产出：按后续流程纠正提前止损，只登记已打开且可直接闭合的 REPL 普通文本、第一方 `session.send`、Channel inbound 三类公开入口，到稳定 conversation/surface/turn identity、ConversationManager容量门及 durable admitted+queued 后返回 accepted 的最小事实；没有继续扩展 Authority/Assignment、执行、终态、恢复或其他旅程。
- 当前责任与迁移输入：Surface/Channel产生稳定turn与principal，Product API/Manager拥有入口串行化和容量投影，durable owner提交后产生run identity；公开wire不区分immediate/queued/replayed，Assignment不构成accepted前置。REPL与直接RPC/Channel的空白校验、Channel准入回执/错误呈现存在可观察差异，均只登记、不修复。
- 直接证据：静态读取REPL input/Controller/RPC facade、Server `session.send`、stable RPC identity、ConversationManager admission、ConversationProtocolRuntime admission边界、Channel binder/inbound、executor-only local owner/router与first-party Mesh，并读取相关既有直接测试的最窄断言作为反向索引。没有运行应用、测试、构建或制品门禁，没有访问真实home、秘密或外部系统。
- 交接类型：安全交接，**部分事实已接受**。已登记事实足以作为代表性产品回归和后续 owner 迁移输入，但不能证明 Authority/Assignment 深层合同、Host 排空、远端响应丢失、重启重放、执行/取消/终态/恢复已经闭合。
- 下一检查点：不再单独补做 A0-08b2a2。未核实集合按上方执行期效率裁决绑定到实际改变相应责任的 A1～A6 工作包；A0 直接进入 A0-09 迁移决策图。

### A0-09：形成最小迁移决策图

- 基线：`HEAD 022246b4317d6a679181434f3921ca1e5fea4213 + task-doc:coordinator-dispatch + A0-09-minimal-migration-decision-graph-v1`；进场索引为空，工作区只有协调者在本文登记A0-09的实时台账差异。派发文本的完整hash不存在，仅短前缀`022246b4`解析到本HEAD，故记录以Git对象事实为准。
- 包边界：完整读取本文当前规则与AE-001，直接复用A0-01～A0-08b2a1已接受证据，只压缩会改变迁移顺序的当前owner/写入口/恢复主链、Kernel/RuntimeHost/拓扑与依赖；没有重取exact-set、持久schema、制品、产品旅程、Authority/Assignment深层状态机或全部消费者。
- 当前裁决：17行决策图覆盖全部持久Host、Surface/命令图、配置、binding、现有领域/管理责任、Kernel、Correctness、Infrastructure/Topology及package边界。A1首链固定为三拓扑/三模式共同的持久Host外层生命周期唯一owner；A2首切片固定为Skill Catalog；A5新增并唯一归属既有`Workspace Administration`与`Backup & Recovery Administration`，没有把运行形态或技术机制虚构成领域。
- 顺序与保护：主序为`A1→A2(Skill)→A3→A4→A5→A6→A7`，A5只记录有证据的依赖偏序；每行都绑定迁移时必须即时闭合的原A0证据和保护类别。A0-08b2a1未展开事实已分别绑定A1/A4/A5/A6，不再作为A0前置。
- 静态证据：本文A0索引、A0-01～A0-08b2a1已接受摘要与失效规则，以及AE-001职责、状态、允许依赖、禁止依赖和检查点顺序；本包未读取新的生产链，说明现有信息已足以唯一裁决。没有运行应用、测试、构建、制品或高成本查询，没有访问真实home、秘密或外部系统。
- 协调者复核：以 AE-001 领域/Host/Kernel/Correctness 边界和当前生产入口反查后接受 A1 首链、A2 Skill Catalog 切片、Workspace 与 Backup & Recovery 两项现有管理责任；同时依据目录备份目标和可选投递事实，删除“Device 必须先于 Backup & Recovery”“Delivery 整域必须先于 Schedule/Advancement”两项虚假全序，只保留真实应用端口关系。
- 交接类型：完成并通过协调者独立复核；“迁移决策图”索引已标`[x]`，A0总项、完成度和全部退出门仍为未完成，其他既有证据未失效。
- 下一检查点：A0-10只映射A1首链与A2 Skill切片的直接测试、结构门禁和已复现基线失败，独立裁决A0；不得重开迁移顺序、继续产品考古或提前进入A1实现。

### A0-10：建立首批验证映射并关闭 A0

- 基线：`HEAD b09d9f5eb159372175913ac235167574d5436c62 + task-doc:coordinator-dispatch + A0-10-initial-verification-map-v1`；进场索引为空，工作区只有协调者在本文登记 A0-10 的实时台账差异。
- 包边界：只消费已接受的 A0-09 A1 首链与 A2 Skill Catalog 选择，以最窄测试名/识别性片段建立现有直接测试、结构门禁、必须新增证据、真实基线失败与 A7 终验边界；没有重开迁移顺序、产品旅程、Authority/Assignment 深层合同、其余领域测试或生产实现。
- 当前责任与问题：A1 已有 topology/preflight/rollback/stop/reconcile/cleanup/golden 证据，但不能证明一个未来 `ApplicationHost` 覆盖 executor-only outer cleanup并删除所有旧 outer owner；A2 已有 Store/Authority/tool/RPC/REPL/Executor/S7/contracts 证据，但不能证明新 Skill Domain 的唯一合同、writer、fact-event 时序和 Kernel path-free依赖。两项均无本轮或已接受证据中真实复现的 failing test/gate；上述是必须先补强才能迁移的识别缺口，不是历史绿灯。
- 本轮迁移：无生产迁移；新增 `A0-10-initial-verification-map-v1`，把 A1/A2 的正常、失败/拒绝、并发/幂等/恢复、清理/零残留与依赖边界分别绑到当前可复用测试和迁移期最小新增门禁，并把全量 build/contracts/runtime/root/package check及六面对抗明确留给 A7。同一映射足以直接拆分并派发 A1 首条生产责任链，A0 第五行与 A0 标为 `[x]`，完成度改为 `1/8`，检查点转到 A1；所有最终退出门保持 `[ ]`。
- 直接证据：完整读取本文当前规则、A0索引、A0-09与验证手册，复用 A0-01～A0-09 已接受证据；只以 `rg --files`、测试标题和必要的最小断言片段核对 CLI/server lifecycle、S7 cleanup/role、runtime lifecycle golden、Skill Store/admission/tool/RPC/REPL/Executor asset与 writable-owner门禁。没有运行测试、构建、制品或高成本命令，故没有把历史结果登记成本轮绿灯。
- 交接类型：完成，等待协调者独立复核；A1 首条责任链和 A2 后续切片均已有可直接拆分执行的验证清单，没有会改变两项选择的反证或未处置 A0 决策缺口。
- 失效与恢复：A0-01～A0-09 无证据失效。若 A1/A2 选择、对应生产入口/owner，或本证据列出的测试、S7/contracts/golden输入变化，恢复 `A0-10-initial-verification-map-v1`、A0第五行与A0；详细迁移行为变化只恢复实际受影响的A1/A2工作包，不重开全A0。
- 遗留问题：无 A0 决策缺口。executor-only stop/trust/idle、Skill materialization/legacy边界及待新增架构门禁均已绑定相应 A1/A2 实施包，不构成 A0 前置；A7 全量同基线门禁与六面对抗未运行且不得提前运行。
- 下一检查点：协调者复核后把 A1 首条责任链“持久 Host 外层生命周期唯一 owner”按单一生命周期结果和独立验证闭包拆成 2～4 小时工作包；责任链只迁移 home/config/secret/role/topology冻结、recovery-root串行前置、role-root委托、stop/terminal等待与outer lease/cleanup，内部组件与产品语义先原样委托，不开始A2。

### A1-01：建立持久 ApplicationHost 的第一个真实生产闭环

- 基线与差异：`HEAD f5e5f617b9e34df3f3e2bd53e11e9bdd1dd4e375 + A1-01-persistent-application-host-v1`；进场索引与工作区为空。本包新增 `packages/cli/src/serve/application-host.ts` 及其直接测试，修改 `topology-command.ts` 与直接测试，并因生产扫描输入迁移而同步 S7 coverage 实现/变异测试；协调纠正又最窄修改 `scripts/s7-registry-golden.mjs` 的跨平台精确比较。本文是唯一文档变化，未执行任何 Git 写操作。
- 责任迁移：三种 process mode（foreground/on-demand/managed）继续只在 `topology-command.ts` 完成模式选择、managed preflight、startup/SecretStore 就绪和 Host 创建，随后全部进入同一个 `PersistentApplicationHost`。该 Host 冻结 home/process mode 与本次 startup/options/secret 引用，唯一创建 device capacity、prepare Mesh、取得本机 workspace lease、形成冻结 role configuration/bootstrap、单次委托既有 role root，并统一负责最外层释放；`topology-command.ts` 已删除 Mesh、recovery-root、workspace lease、role loader 和 `finally` 平行 owner。
- 串行与失败终态：trusted-home 缺恢复根时，Host 先运行有限 recovery-root topology，完整停止第一份 Mesh bootstrap maintenance，才 prepare 第二份常驻 bootstrap；只有第二份同时具备 recovery root/backup key 才允许取得 workspace lease 并委托 role root。正常返回、recovery-root 失败、第二次 prepare 失败、role root 失败都汇入同一 terminal path；已取得的当前 Mesh maintenance 与 lease 各自至多释放一次，cleanup 的一项失败不阻断另一项尝试，主失败与 cleanup 失败同时存在时以 `AggregateError` 保留全部事实。
- 行为保护：`runConfiguredServeTopology`、Anchor/Executor role root、Server/owner/Mesh/Runtime 产品装配及内部 start/open/stop 顺序均未改变，只成为 Host 的不透明既有组件。参数化直接测试覆盖 foreground/on-demand/managed × anchor+executor/anchor-only/executor-only 九种组合均使用同一 Host class，另覆盖恢复根无双图、role root 单次委托、成功/恢复失败/第二 bootstrap 失败/role 失败/重复 run 的精确一次释放；旧 topology owner 的负向源码断言和 S7 mutation 会识别平行 owner 回流。
- 实际验证：首次从根运行 `pnpm exec vitest ...` 因根包不提供 vitest binary 而在收集前失败，按验证手册改用 package-filter 入口；五个指定文件最终为 44/44（Host 15、topology-command 6、其余三文件 23），且协调者已在当前未修改 CLI 输入上独立复核，纠正轮没有重复运行。`pnpm --filter @zhixing/cli exec tsc --noEmit` 首次发现 production factory 的无约束 generic 与 `ServeOptions` 不相容，收窄 factory 输入后复跑通过；变更文件最窄 Biome check 通过；`pnpm cli:build` 通过。S7 扫描输入确已改变，因此把 managed/recovery 结构门禁迁到真实 Host。
- S7 协调纠正：初次记录把 canonical S7 在 `s7:registry-golden:write` 临时写出的 LF 文件上通过误记为 clean Windows 工作区事实；协调者在 CRLF checkout 上复现 registry stale，故该结论一度失效。纠正轮先运行 `pnpm s7:registry-golden` 复现，再经 canonical `pnpm s7:registry-golden:write` 生成并人工对账：golden 的 clean-filter object 与 HEAD 都是 `071f21b073918fee3766618cb98bd909049493a2`，`git diff` 为空，说明 ApplicationHost/S7 inspector 迁移没有改变 canonical registry JSON；唯一差异是 `i/lf + w/crlf`，旧 checker 以 raw string 比较造成假失败。`scripts/s7-registry-golden.mjs` 现只把 CRLF/CR 归一为 LF 后继续执行完整 exact compare，不放宽 JSON、角色、方法或 entry coverage。恢复真实 CRLF checkout 后重新运行一次 canonical `pnpm s7:lint`，coverage/mutation 21/21 与 registry golden 全部通过；golden 文件无内容差异，CLI 源码未再变化，故 44 项直接测试与 CLI build 证据继续有效。
- 当前边界：本包没有统一 HostStop、replace、idle、拒新/排空或 role root 内部 lifecycle，没有迁移 Server/Owner/Mesh/Runtime 产品装配，也没有建立 component descriptor 框架。`A1-HOST-DELEGATE-01` 是唯一打开的单向委托；A1 与全部最终退出门保持 `[ ]`。
- 下一检查点：协调者独立复核后，只在 A1 内选择下一条能独立闭合的持久 Host 生命周期责任（优先 create/start/open 或 stop/drain/close 中能保持既有产品次序的一条），即时补齐真实 component/rollback/cleanup 证据；不得因本包已有 Host class 而提前进入 A2。

### A1-02：闭合 Executor-only RPC 停机到 ApplicationHost 的唯一终止链

- 基线与差异：`HEAD cba7dd31b4d32caf311b91da1812193a9cc4f2b5 + A1-02-executor-role-terminal-v1`。A1-01 已由协调者独立复核并提交；本包新增 `packages/cli/src/serve/executor-role-terminal.ts` 及其直接测试，修改 `executor-role-runtime.ts` 的生产等待边界，并因该生产扫描输入变化同步 S7 coverage 与反向 mutation。除本文外没有修改产品文档、AE-001 或其他责任链，没有执行 Git 写操作。
- 当前责任与断链：RPC `server.shutdown` 已先完整执行既有 `HostStopCoordinator.prepare`（拒新、冻结、settle、read-back、flush 与 response-loss 语义不变），再经 `ServerContext.requestShutdown` 触发 `RunningServer.shutdown`；但旧 `waitForRoleShutdown` 只观察 SIGINT/SIGTERM 与永久设备移除，未观察 `RunningServer.waitForShutdown()`，所以 Server 已真实 terminal 后 executor role 仍不返回，内部 cleanup 与 `PersistentApplicationHost` 外层 Mesh maintenance/workspace lease 释放均无法到达。
- 本轮迁移：新增唯一 executor role terminal，把 RPC 驱动的 `RunningServer.waitForShutdown()`、信号和永久设备移除汇入同一幂等边界。RPC 路径只观察 Server terminal，不新增 prepare 或 shutdown；信号路径仍先且只调用一次既有 `HostStopCoordinator.prepare`，随后调用并等待 `RunningServer.shutdown/waitForShutdown`；设备移除沿既有耐久 terminal promise 到达同一 Server terminal。信号/移除竞态以单一 `roleStop` promise 合并，Server 自身 shutdown 仍幂等；只有真实 Server cleanup 完成才唤醒 role，随后 `runExecutorRole` 执行既有内部 cleanup 并返回，A1-01 的唯一 `PersistentApplicationHost` 再释放外层资源。旧平行 `waitForRoleShutdown` 已删除，SIGINT/SIGTERM listener 在成功或失败终态都精确移除，原始 prepare/shutdown 失败继续向现有 role failure 路径传播。
- 直接证据：`pnpm --filter @zhixing/cli exec vitest run src/serve/executor-role-terminal.test.ts --maxWorkers=1` 首次 4/5，通过项已证明 RPC terminal、signal、竞态和失败边界；唯一失败是 device-removal 用例少等待一个由无 prepare 分支产生的 microtask，生产 `shutdown` 尚未来得及被 spy 观察，修正测试同步点后复跑 5/5。随后串行运行 `pnpm --filter @zhixing/cli exec vitest run src/serve/executor-role-terminal.test.ts src/serve/host-stop-lifecycle.test.ts --maxWorkers=1` 为 20/20，覆盖 RPC 不提前返回、信号 prepare 一次、设备移除、三来源竞态、失败监听器清理及既有 HostStop durable/replay 合同；`pnpm --filter @zhixing/server exec vitest run src/__tests__/lifecycle.test.ts src/rpc/methods/__tests__/server-shutdown-lifecycle.test.ts --maxWorkers=1` 为 14/14，证明 RPC 仍先 prepare 且 `RunningServer.waitForShutdown` 只在 CleanupRegistry 完成后成立。A1-01 已通过的 44 项测试未重复运行。
- 静态与构建证据：`pnpm exec biome check packages/cli/src/serve/executor-role-terminal.ts packages/cli/src/serve/executor-role-terminal.test.ts packages/cli/src/serve/executor-role-runtime.ts scripts/s7-entry-coverage.mjs scripts/s7-entry-coverage.test.mjs` 对适用的 3 个 TypeScript 文件检查通过、未写入修复；`pnpm --filter @zhixing/cli exec tsc --noEmit` 通过；`pnpm cli:build` 通过。S7 生产扫描输入确已变化，新增 mutation 会把真实 `waitForExecutorRoleTerminal` 装配替换成非生产等待并要求门禁失败；本轮只运行一次 canonical `pnpm s7:lint`，coverage/mutation 21/21 与 registry golden 全部通过。没有运行包全测、根级 lint/test/build 或制品验收。
- 保护与边界：Server 公共合同、RPC handler、HostStop 状态机、Anchor Host、managed trust replace、on-demand idle、role root 内部资源顺序和 ApplicationHost 外层 owner 均未改动；没有新增 coordinator、journal、轮询或超时兜底。`A1-HOST-DELEGATE-01` 仍是唯一打开的单向桥；本包只闭合 executor-only 的一条 terminal 接缝，不足以勾选 A1 或任何最终退出门。
- 交接类型：完成，等待协调者独立复核。
- 失效与恢复：A0 与 A1-01 证据无失效；若 executor role 的 Server 创建/等待、`ServerContext.requestShutdown`、HostStop prepare、device-removal terminal、role cleanup 或 ApplicationHost role 委托任一改变，则恢复 `A1-02-executor-role-terminal-v1` 并只重验上述直接闭包；其他 A1 分支变化不使本证据整体失效。
- 遗留问题：无本包内遗留。A1 尚未统一其他 open/refuse/drain/replace/close 生命周期，也未迁移 role root 内部组件贡献；这些保持在后续 A1 工作包，不能由本包结果推导为已完成。
- 下一检查点：协调者先沿真实 RPC handler、RunningServer terminal、executor cleanup 与 ApplicationHost outer release 独立复核本链；接受后继续 A1 的下一条单一持久 Host 生命周期责任，不进入 A2。

### A1-03：闭合 Anchor 内部停止触发的唯一耐久终止入口

- 基线与差异：`HEAD 3d07c58b99f27c01f97d2c13d49c44703bd55ef6 + A1-03-anchor-internal-stop-v1`；进场工作区与索引为空，A1-01、A1-02 已由协调者独立复核并提交。本包新增 `anchor-internal-stop.ts` 及直接测试，修改 Anchor 生产组合根 `command.ts`，并同步 S7 production inspector 与反向 mutation；本文是唯一产品文档变化，未执行任何 Git 写操作。
- 原断链与责任迁移：managed trust/role generation 变化原来只执行 `refuseNewMessages + serverCtx.requestShutdown`，idle reaper 直接执行裸 shutdown，永久本机设备移除则拥有一份独立 `HostStop.prepare + raw requestShutdown`。三者现统一调用唯一 `AnchorInternalStopPort`；该端口以冻结的 home/Host identity 生成一个 Host 生命周期稳定 request id，首次来源冻结 reason/strategy，并发或重复来源共享同一 in-flight promise。每次尝试都先调用既有 `HostStopCoordinator.prepare`，只有 durable `ready-to-stop` 成立后才触发真实 `ServerContext.requestShutdown`，成功后终生只触发一次；prepare 或 shutdown trigger 失败会清除本次 in-flight，但保留同一 identity 与首次请求，下一次触发只允许原样重驱，不新增 coordinator、journal、gate 或 cleanup owner。
- 产品与终止边界：managed trust 变化仍由 `coordinateManagedHostTrustTransition` 先立即拒新，prepare 失败继续向 reconciler 可观察地传播；on-demand idle 仍只在既有 `shouldIdleExit` 为真时请求 drain，失败被显式消费并由下一 idle tick 以同一 identity 有界重试，不产生 unhandled rejection；device removal 仍先完成 admission/accepted-work、耐久 terminal 与既有 cleanup，local-device 路径继续先删除 device key 再请求 stop，Server 尚未建立时继续使用既有 pre-start terminal flag。内部回调不等待整个 Server cleanup；Anchor role 仍只在既有 `await runner.waitForShutdown()` 证明真实 terminal 后返回，随后 A1-01 的唯一 `PersistentApplicationHost` 才释放外层 Mesh maintenance 与 workspace lease。RPC `server.shutdown`、Server signal、Executor-only A1-02 链和 HostStop 状态机均未修改。
- 直接与结构证据：CLI 直接闭包为 4 文件 40/40，其中 `anchor-internal-stop.test.ts` 6/6、`managed-service-runtime.test.ts` 15/15、idle policy 4/4、HostStop 15/15；40/40 已包含新增端口的 6 项，不重复相加。新增端口用例覆盖 managed/idle/device 三种策略、三来源并发与重复合并、prepare 失败不 shutdown、同 identity/body 重驱、shutdown trigger 失败重驱及成功后精确一次触发。S7 新门禁要求生产组合根恰有一个端口、三个来源、唯一 `serverCtx.requestShutdown` binding、prepare 先于 shutdown、idle 显式消费失败且 role 仍等待 `runner.waitForShutdown()`；三条反向 mutation 分别恢复裸旁路、删除 prepare await、增加第二 owner，均必须失败。canonical `pnpm s7:lint` 为 21/21，registry golden 通过。
- 静态与构建证据：首次 CLI typecheck 精确发现新端口对象方法参数缺少显式类型、闭包读取未保持可选值收窄；改为显式 `AnchorInternalStopRequest → Promise<void>` 且在建闭包前冻结非空 request 后，重跑新增端口测试 6/6，`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` 通过。变更 TypeScript 的最窄 Biome check 通过，`pnpm cli:build` 通过；没有运行 CLI 包全测、根级 lint/test/build、package check 或制品验收。
- 保护、失效与遗留：A0、A1-01、A1-02 证据未失效，`A1-HOST-DELEGATE-01` 仍是唯一打开的单向桥。本包未迁移 Anchor 内部组件 descriptor、start/open 次序、完整 cleanup registry、Executor trust/idle 或其他 A1 责任；因此 A1、完成度与最终退出门保持不变。若 Anchor 三个 internal source、HostStop prepare、Server shutdown binding/terminal、device removal cleanup/key 顺序、idle policy 或 ApplicationHost outer release 任一变化，则恢复 `A1-03-anchor-internal-stop-v1` 并只重验本闭包。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。
- 下一检查点：协调者沿三条生产 source、端口首次请求冻结与失败重驱、`runner.waitForShutdown` 和 ApplicationHost outer release 独立复核；接受后继续 A1 的下一条单一 Host 生命周期责任，不进入 A2。

### A1-04：闭合 Anchor 的 entry-last 开放边界

- 基线与差异：`HEAD 5cf0a8eee0f57684444de60a224aa1e7b0f1349f + A1-04-anchor-entry-last-v1`；进场工作区与索引为空，A1-01～A1-03 已由协调者独立复核并提交。本包修改 `@zhixing/server` 的 prepared/activation 生命周期、Anchor 唯一生产组合根及其现有接入面/清理时序注释，新增 Server/Host 直接证据并强化 S7 managed-host 结构门禁；本文是唯一产品文档变化，未执行 Git 写操作。
- 原窗口与责任迁移：旧链在 `runServer/startServer` 内先执行 `BoundZhixingServer.activate`，随后才由 Anchor 激活 Delivery/Scheduler、绑定 session broadcast、装配 confirmation/recovery contribution、注册完整 normal cleanup 并 `startupRollback.commit`，公开 REST/RPC/WS 因而存在“已经 200/可 RPC、但 Host 尚未被完整关闭责任接管”的窗口。现在 `startServer` 先在同一 `BoundZhixingServer` 上建立 handler、RPC/WS connection 集合与 bridge，再等待类型化 `activationGate`；`runServer` 在 gate 内先建立唯一 CleanupRegistry owner、注册 `server.close`、绑定幂等 shutdown 与 prepared `RunningServer`，然后调用 Anchor 的 `beforeActivate`。Anchor 在该回调内完成原有 Delivery/Scheduler activation、session projection、manual surface resume、core/owner teardown 注册、post-server contribution 与 rollback→normal-owner 移交，全部成立后 gate 才释放并由同一 handle 一次性激活。没有第二 listen、ServerContext、registry、lazy runner 或运行期定位。
- 发布、失败与保护边界：公开入口激活后，既有 `beforePublish` 健康门仍先于 discovery；随后才写 PID/port，并由 `publishReady` 写 `ServerStateFile ready/running`、background `.ready` 与 heartbeat/banner。gate 前失败关闭 inactive endpoint且不写 PID/ready；activate、健康门、PID 或 ready/state 发布失败都调用同一 registry 的 `startup-error` 终止路径，CleanupRegistry 幂等保证已取得资源只产生一次实际补偿，注入模式继续由 Anchor tail cleanup 精确释放本进程 discovery。信号 handler 只在 open+publication 全部成功后安装。standalone `startServer/runServer` 与 Executor-only 未传 gate 时仍按原调用默认开放，RPC/HTTP/WS wire、HostStop、领域 owner、A1-02/A1-03 terminal 和外层 `PersistentApplicationHost` 均未改动。
- 直接证据：Server 最终直接闭包为 2 文件 27/27：`server.test.ts` 12/12 来自同基线首次组合运行，真实 loopback 在可控 deferred gate 中证明同一端口 HTTP 与 WS 持续 503，gate 后同一 `httpServer` 健康 200 且 RPC `health` 可调用，gate reject 关闭端口；`lifecycle.test.ts` 在补入启动期 shutdown 竞态后最终为 15/15，证明 `beforeActivate → activate/health → PID publish` 顺序，contribution、activate、ready publication 三类失败的端口/PID 零残留与精确一次补偿，以及 shutdown 抢先时不再继续发布 discovery/ready。CLI `startup-server-owner`、access-surface、shutdown-chain、startup-rollback 直接闭包为 4 文件 24/24，反绑实际 Anchor 组合根的 Delivery/Scheduler、normal cleanup、post-server contribution、rollback commit 与 state ready 顺序；中间对已失效 lifecycle 子集的重跑不重复计入上述最终数字。
- 结构、静态与构建证据：S7 managed-host inspector 现读取真实 `command.ts`、`server/lifecycle.ts` 与 `server.ts`，要求唯一 bind/run/gate/publication、`server.close` 先接管、全部必要 Anchor 前置位于 gate 内、gate 先于 `BoundZhixingServer.activate`、ready 位于 publication hook，并以反向 mutation 识别恢复早激活、把 Delivery 移到 gate 后、第二 bind 和提前 ready。canonical S7 的失败轮分别暴露动态 cleanup owner 不符合 descriptor literal、两条 mutation 没有真正改变受检生产结构；均按根因修正，启动期 shutdown 防假 ready 输入补齐后，在最终当前源码上重取 `pnpm s7:lint` 为 coverage/mutation 21/21 且 registry golden 通过，未更新 golden。`pnpm exec biome check` 对 9 个变更 TypeScript 文件通过；最终 Server canonical typecheck/build、CLI canonical typecheck 与 `pnpm cli:build` 均通过。没有运行包全测、根级 lint/test/build、package check 或制品验收。
- 保护、失效与遗留：A0 与 A1-01～A1-03 的责任结论未失效，A0-02 所记录的旧早开放窗口由本证据明确取代；`A1-HOST-DELEGATE-01` 仍是唯一打开的单向桥。若 `BoundZhixingServer` prepared/activate/close、`startServer` connection/bridge 设施、`runServer` cleanup/shutdown/discovery/publication、Anchor Delivery/Scheduler/post-server contribution/cleanup/ready 顺序或对应结构门禁任一变化，恢复 `A1-04-anchor-entry-last-v1` 并只重验上述 6 个直接文件、S7 与 Server→CLI build 闭包。本包没有处理 Executor trust/idle、managed successor、完整 component descriptor、所有 cleanup leak harness 或委托桥退场，因此 A1、完成度和最终退出门保持不变。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。
- 下一检查点：协调者从同一 bound endpoint 的 503/activate/PID/ready 时序、Anchor 必要前置、三类失败补偿和 S7 反向 mutation 独立复核；接受后继续 A1 的下一条单一 Host 生命周期责任，不进入 A2。

### A1-05：闭合 Executor-only 的 trust-generation 换代与 on-demand idle 终止链

- 基线与差异：`HEAD c5838a0b3271dff440c104f0d4c11788a2774133 + A1-05-executor-trust-idle-stop-v1`；进场工作区与索引为空，A1-01～A1-04 已由协调者独立复核并提交。本包新增 `executor-internal-stop.ts` 及直接测试，在唯一 Executor role root 接入 trust/idle，给既有 local conversation owner 增加只读 activity probe，并同步最窄测试与 S7 managed-host 结构门禁；本文是唯一产品文档变化，未执行 Git 写操作。
- 原缺口与责任迁移：Executor-only 的 `MeshRuntimeAssembly` 原未绑定 `onTrustApplied`，所以 trust/role generation 变化不会让当前 generation 退场；该拓扑也没有 on-demand idle reaper。现在 role root 从同一 `loadCurrentManagedServiceState` 快照取得完整 managed admission identity，pre-open 阶段只以 `verifyManagedHostAdmission` fail closed，不在 Server/stop owner 尚未成立时伪造运行期换代；同一 `RunningServer` 建立后绑定唯一 `ExecutorInternalStopPort`，随后在发布 ready 前再次复核，并把后续 Mesh trust callback 交给既有 `coordinateManagedHostTrustTransition`。identity 完全相同只执行既有 reconcile、保持 Host；mode、trust、role 或 service generation 的 canonical identity 真实变化才拒新并请求停止。
- 唯一耐久终止：trust 变化和 idle 共用 `executor-internal:<pid>:<startedAt>` 这一 Host-generation 稳定 request id。端口冻结首次 reason/strategy，并发和重复来源共享同一 in-flight；每次尝试严格执行既有 `HostStopCoordinator.prepare` 后触发同一 `RunningServer.shutdown`，且只有 `waitForShutdown` 证明真实 terminal 才成功。prepare 或 shutdown trigger 失败不假终止、不换 identity/reason/strategy，下一来源按同一请求重驱；成功后不再重复 prepare/shutdown。A1-02 的 RPC、SIGINT/SIGTERM、永久设备移除仍由原 `waitForExecutorRoleTerminal` 汇入同一 Server terminal，role 只在 terminal 后执行既有内部 cleanup，再由唯一 `PersistentApplicationHost` 释放外层 Mesh maintenance/workspace lease。
- Executor idle 事实：reaper 仅在 `processMode === "on-demand"` 装配，不借用 Anchor 的 Channel/Scheduler 字段。每 tick 先观察真实本机 Server connection 与当前 Anchor Mesh connection；二者均不在场时，再读取远端 job durable accepted-work，以及 local conversation 的 active command/transfer/turn、pending final/assignment、recovery backlog、active lease，并遍历所有 `current/frozen/importing` 本机 conversation authority 的 deferred-intent 投影。只要存在 `status === "pending"` 的 intent 就继续保活；`confirmed/discarded` 已终态记录和普通无待办 durable conversation 本身不保活。全部事实为空才以 `drain` 请求同一内部 stop。该 probe 只走 authority/list reader，不调用会冻结、等待或改变准入的 removal/HostStop preflight；检查不重入，异步失败被显式消费并由下个 tick 以同一 identity 重试，timer 在 role cleanup 首步清除，已在途检查也在组件释放前收束。
- 直接证据：首轮串行 6 文件曾得到 61/62，纠正 durable final 断言后为 62/62；协调复核随后以 HostStop accepted-work 闭包反证 deferred intent 漏项。本轮新增用例直接证明“普通空会话不保活 → 仅有 pending deferred intent 时阻止 idle → 经正常 discard 成为终态且其余 activity 为空后允许 idle”。受影响 `executor-internal-stop.test.ts` 与 `local-conversation-owner-lifecycle.test.ts` 首次组合为 22/23，新用例和 Executor idle 9 项均通过；唯一失败的既有重驱用例是存储维护 `backpressured:ioOperations`，按验证手册用包内 Node/Vitest 入口在独立新进程重取 1/1，通过与首次其余结果合并为 2 文件 23/23。连同输入未变的 executor terminal 5、HostStop 15、managed transition 16、Anchor idle policy 4，当前有效直接闭包为 6 文件 63/63，新增 intent 项已包含在 local-owner lifecycle 14 项中，不重复相加。
- 结构、静态与构建证据：S7 managed-host inspector 现要求 Executor root 恰有一个 managed trust callback、一个内部 stop owner、两个来源、完整 admission capture、on-demand-only activity 判定、cleanup 收束和 terminal 后返回；反向 mutation 分别移除 durable prepare、断开 trust binding、忽略 current Anchor Mesh presence，均必须失败。deferred-intent 纠正没有改变上述门禁合同，但修改的 `local-conversation-owner.ts` 属于 S7 production records，故按输入失效规则重新运行一次 canonical `pnpm s7:lint`，coverage/mutation 21/21 且 registry golden 通过。当前源码上重新运行 `pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` 与 `pnpm cli:build` 均通过；首轮最窄 Biome 证据因两文件输入变化不再作为当前证据，本轮完成门未要求重取。没有运行 CLI 包全测、根级 lint/test/build、package check 或制品验收。
- 保护、失效与遗留：Anchor 两种拓扑、A1-02 RPC/signal/device-removal terminal、HostStop journal/response-loss/replay、Executor start/open/cleanup 次序、Server wire、领域/权限/资源策略和三种 process mode 的既有选择均未改动；idle probe 没有调用 `preflightForDeviceRemoval`，也没有新增 trust source、reconciler、journal、Host owner、轮询兜底或产品事实。A0 与 A1-01～A1-04 的责任结论未失效，`A1-HOST-DELEGATE-01` 仍是唯一打开的单向桥；因此 A1、完成度和最终退出门保持不变。若 Executor managed admission capture/verify/coordinate、Mesh trust callback、远端 job/local closure/deferred-intent 任一 accepted-work reader、deferred intent 状态合同、HostStop prepare、RunningServer terminal、idle timer cleanup、role cleanup 或 ApplicationHost outer release任一变化，恢复 `A1-05-executor-trust-idle-stop-v1` 并只重验本闭包。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。
- 下一检查点：协调者沿相同/变化 admission、pre-open 与 ready 前复核、trust/idle 共用 identity、全部 presence/accepted-work（尤其 pending deferred intent 与其终态）事实、真实 Server terminal、内部与 outer cleanup 独立复核；接受后继续 A1 的下一条单一 Host 生命周期责任，不进入 A2。

### A1-06：闭合 Executor-only 的 entry-last 开放与失败补偿边界

- 基线与差异：`HEAD 9524083c355d57daf0ad47d3e9a9e11304001fd5 + A1-06-executor-entry-last-v1`；进场工作区与索引为空，A1-01～A1-05 已由协调者独立复核并提交。本包只修改 Executor 唯一生产 root、既有 Server lifecycle 注释、直接结构测试与 S7 managed-host 门禁；没有新增 listener、Server、registry、组合根或产品合同，未执行 Git 写操作。
- 原窗口与责任迁移：Executor 原先先等待 `runServer` 完成 endpoint activate 与 PID 发布，随后才创建 `ExecutorInternalStopPort`、绑定运行期 trust transition、再次执行 `onTrustApplied` admission 复核，最后在 `runServer` 之外发布 state/ready；因此 stop owner 或最终 admission 失败时存在短暂公开入口。现在同一 `BoundZhixingServer` 仍在任何 effectful owner 之前由 OS 仲裁，但 `runServer.beforeActivate(openingRunner)` 在 endpoint 仅返回 inactive 503 时依次绑定 A1-05 的唯一 durable stop owner、安装 trust-generation transition，并等待最后一次 managed admission 复核；三项全部成立后同一 handle 才激活 REST/RPC/WS。旧 post-open stop/admission 路径已删除，stop 只捕获 gate 提供的同一 prepared `RunningServer`，没有可变 late runner 或第二 shutdown owner。
- 发布与失败补偿：Executor 的既有 `ServerStateFile.markReady → markRunning` 已移入同一次 `runServer.publishReady`，因此仍在 endpoint activate 与 PID/port lock 之后、heartbeat/timer 与 terminal wait 之前发布。gate 内 identity 变化继续先拒新，经既有 HostStop durable prepare、同一 prepared Server shutdown 与真实 terminal 收口，`runServer` 不会返回或发布 ready；gate、activate、PID 或 state/ready 任一步失败都由 Server 唯一 registry 关闭 inactive/active endpoint并释放自身 lock，再由 Executor 既有逐段 cleanup 收束 state、owner、timer、Mesh、Authority、MCP 与外层 ApplicationHost 资源。`resumeActive`、RPC/signal/device-removal、trust/idle durable identity、accepted-work、Executor 内部清理和 `PersistentApplicationHost` 外层释放顺序均未改变。
- 直接证据：CLI 定向闭包 4 文件 34/34：`startup-server-owner.test.ts` 4/4 直接读取唯一生产 root，要求 stop owner、trust binding、最终 `await onTrustApplied()` 全部位于 `beforeActivate` 与 `publishReady` 之间，ready/running 位于 publication callback 且 stop 捕获 `openingRunner`；`executor-internal-stop.test.ts` 9/9、`executor-role-terminal.test.ts` 5/5、`managed-service-runtime.test.ts` 16/16 继续证明同 identity retained、变化 identity durable terminal、prepare/terminal 与重复竞态。Server `lifecycle.test.ts` 15/15 证明 beforeActivate 与 publishReady 失败都走同一 shutdown owner、端口/PID 零残留；A1-04 已接受且 `server.ts` 输入未变的真实 loopback 12/12 继续证明同一端口 gate 前 HTTP/WS 503、gate 后健康 200/RPC 可用，未重复执行。因此本包当前直接闭包为实际重取 5 文件 49/49，外加未失效的 A1-04 loopback 保护证据，不重复相加。
- 结构、静态与构建证据：S7 现同时要求 Anchor 与 Executor 唯一 production root 使用同一 `runServer` activation gate。Executor 约束精确检查唯一 run/gate/publication、internal stop/trust/final admission 全部位于 gate 内、ready 位于 publication，并检查 stop 绑定同一 `openingRunner`；三条反向 mutation 分别恢复无 gate、把最终 admission 改为不等待、把 shutdown 退回 post-open `localConversationServer`，均必须失败。当前基线一次 canonical `pnpm s7:lint` 为 coverage/mutation 21/21 且 registry golden 通过，未更新 golden；变更 TypeScript 的最窄 Biome check、`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` 与 `pnpm cli:build` 均通过。
- 保护、失效与遗留：本包没有迁移领域、Product API、Kernel、Executor 手工 cleanup、其他命令期 Surface 或 A1-HOST-DELEGATE-01，也没有扩大 idle/HostStop accepted-work 集合；A0 与 A1-01～A1-05 责任结论未失效，A1、完成度与最终退出门保持不变。若 Server `beforeActivate/publishReady`、Executor bound endpoint/runServer、internal stop/trust admission、PID/state/ready/heartbeat、terminal/cleanup、managed admission identity 或对应 S7 inspector 任一变化，恢复 `A1-06-executor-entry-last-v1`，只重验上述 CLI 4 文件、Server lifecycle、S7 与 CLI typecheck/build；只有 `server.ts` gate/activate 实现变化时才重取真实 loopback。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。
- 下一检查点：协调者从真实 Executor root 反查同一 bound handle 的 inactive gate、stop owner/trust/final admission 次序、变化 identity 的 durable terminal、ready publication 与失败 cleanup；接受后继续 A1 的下一条单一 Host 生命周期责任，不进入 A2。

### A1-07：把 Anchor pre-server 单元收束为类型化生命周期贡献

- 基线与差异：`HEAD d297317e1e1f622d12a52941c70241ab9e68bf24 + A1-07-anchor-pre-server-lifecycle-contributions-v1`；进场工作区与索引为空，A1-01～A1-06 已由协调者独立复核并提交。本包新增 `assembly-lifecycle.ts` 与直接测试，修改 Anchor pre-server 单元、activation gate、shutdown-chain 和受影响直接测试，并同步 S7 production coverage/mutation 与 canonical registry golden；没有修改 post-server 单元、Executor root、领域行为、公开协议或其他产品文档，未执行 Git 写操作。
- 责任迁移与桥关闭：原 `AssemblyStartupCleanups` 是 12 个可选字段组成的可变袋；各 `AssemblyUnit` 把 handle 写入袋中，`command.ts` 再按产品字段判断并逐项注册，`shutdown-chain.ts` 还可在 handle 缺失时直接调用运行对象，形成启动补偿、正常关闭与 fallback 三份责任表达。现在 `AssemblyLifecycleContributions` 只接受静态 `AssemblyLifecycleIdentity`，descriptor 同时冻结 owner/role/id 与既有 normal-registration stage；资源取得时单元立即通过 `acquire` 或 `contribute` 把同一 `StartupRollback` 幂等 handle 登记为贡献。除名称 exact-match 外，`StartupRollback` 以实例私有 `WeakSet` 判断 handle object provenance，贡献层因此拒绝另一个 rollback 创建的完全同名 handle；不暴露 cleanup 内容、没有全局 registry。重复 identity、stage 移交后的迟到贡献和 foreign handle 均 fail closed，条件单元不创建资源时不制造空贡献。`AssemblyStartupCleanups`、`startupCleanups`、命令层逐字段搬运及 shutdown-chain 的运行对象 fallback 已从生产源码完全删除，故该旧桥已关闭。
- 移交与顺序：activation gate 内 `registerCoreCleanup` 的 `CoreCleanupResources.lifecycleContributions` 是必填 readonly 类型输入，并直接先后移交 `foundation` 与 `surface`，不存在 optional 参数或 optional-chain 静默跳过；不消费贡献的 `registerTailCleanup` 改用只含 state/lock 的窄参数。post-server contribution 完成后，命令根只调用一次 `runtime` 移交。三段静态顺序精确保留原 authority/workspace/local-conversation → heartbeat → channel/delivery/MCP → post-server → mesh/executor-data-plane/job-status/assets/job-owner/lossless-data-plane 注册关系，因此 CleanupRegistry 的既有 LIFO、owner exact-set、条件 topology 与单项失败隔离不变。全部贡献移交后 `assertTransferred` 才允许 `startupRollback.commit`；gate 任一步失败时 registry 与 rollback 即使都观察到同一 handle，也只实际清理一次。
- 直接证据：初次串行定向命令覆盖 10 文件并取得 51/51；补入 handle identity 与“资源取得后 setup 失败仍逆序尝试全部补偿”断言后，初次闭包为 10 文件 52/52。协调反证纠正后只重跑失效的 `assembly-lifecycle.test.ts` 6/6、`startup-rollback.test.ts` 4/4、`shutdown-chain.test.ts` 14/14，共 3 文件 24/24；合并同一工作区未失效的其余 7 文件 29/29，当前直接闭包为 10 文件 53/53。新增反例证明另一个 rollback 产生的同名 handle 被拒绝、每个 rollback 只识别自己的 object；`@ts-expect-error` 负例把 core 缺少贡献固定为编译错误。其余证据继续识别 identity/exact-set、重复登记、setup rollback、三段/LIFO normal close、条件不适用、gate 接管后 commit 及真实单元取得资源前后的顺序。
- 结构、构建与保护：S7 collector 现在从 `ASSEMBLY_LIFECYCLE_DESCRIPTORS` 的 literal exact-set 机械取得 12 个生产 descriptor，同时拒绝 direct registry bypass、缺失/未知 owner、恢复字段袋、丢失任一真实贡献、删除 rollback provenance 校验、把 core 输入退回 optional、恢复 optional-chain 或删除 transfer 完整性断言；纠正后一次 canonical `pnpm s7:lint` 为 21/21 且 registry golden 通过。canonical write 的人工差异仍仅为 12 项 source 从 `command.ts`/`shutdown-chain.ts` 迁到 `assembly-lifecycle.ts`，owner/role/id/role mapping 均未变。最窄 Biome、CLI canonical typecheck 与 `pnpm cli:build` 通过。A1-04 entry-last、post-server 自注册、Scheduler 非 AssemblyUnit lifecycle、HostStop/trust/idle、Server wire、领域对象与 Executor-only 手工 cleanup 均未改变。
- 失效、遗留与状态：若 `createAssemblyUnits` pre-server exact-set、任一单元的资源取得/handle、descriptor identity/owner/role/stage、三段 transfer、StartupRollback commit、CleanupRegistry LIFO 或 S7 collector/golden 任一变化，恢复本证据并只重验上述直接闭包、S7 与 CLI typecheck/build。`A1-HOST-DELEGATE-01` 仍打开，post-server 生命周期贡献与 Executor 手工 cleanup 尚未迁移，完整零残留 Host 退出条件也未由本包独立闭合，因此 A1、完成度与最终退出门保持不变。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。
- 下一检查点：协调者从 12 个生产资源取得点反查同一 handle 的 rollback/normal-close 双路径、条件拓扑和实际 LIFO，再核对字段袋/fallback/逐项搬运确已归零；接受后继续 A1 的下一条单一生命周期责任，不进入 A2。

#### A1-07 协调复核纠正：rollback provenance 与 core transfer fail-closed

- 协调反证：初次实现只比较 `handle.name`，另一个 `StartupRollback` 可构造完全同名 handle；`ShutdownChainResources.lifecycleContributions?` 与两处 optional-chain 又允许生产 core 关闭注册缺参后静默跳过 foundation/surface transfer。两点均使“同一 rollback 的同一 handle 已强制转交”结论失效，但不影响 12 项 descriptor、单元迁移、三段顺序和既有产品行为。
- 最窄修复：`StartupRollback.owns()` 只查询该实例私有、注册时写入的 handle `WeakSet`；`contribute()` 同时校验静态 identity 和实例 provenance。shutdown-chain 拆成 `TailCleanupResources` 与 `CoreCleanupResources`，后者必填 lifecycle contribution 并无条件执行两段 transfer，前者不再携带无关 heartbeat/lifecycle 字段。没有公开 cleanup、全局登记、第二 owner 或新的产品分支。
- 本轮实际证据：`pnpm --filter @zhixing/cli exec vitest run src/serve/__tests__/assembly-lifecycle.test.ts src/serve/__tests__/startup-rollback.test.ts src/serve/__tests__/shutdown-chain.test.ts --maxWorkers=1` 为 3 文件 24/24；`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` 通过并消费 core 缺参的编译期负例；canonical `pnpm s7:lint` 为 21/21 且 registry golden 通过，反向 mutation 可检出 provenance 删除、core 字段 optional 化与 optional-chain；`pnpm cli:build`、受影响路径的最窄 Biome check 与 `git diff --check` 通过。原 7 个未变直接测试 29/29 继续有效，故最终 A1-07 直接闭包为 10 文件 53/53，不重复相加。
- 状态与失效：`A1-07-anchor-pre-server-lifecycle-contributions-v1` 已在同一工作区恢复，等待协调者独立复核；A1 仍未勾选。以后若 `StartupRollback.register/owns`、handle object identity、`contribute` provenance、core/tail 参数类型、两段直接 transfer 或对应 S7 mutation 任一变化，精确恢复本纠正证据；无需重验未变化的其他 7 个直接测试。

### A1-08：收束 Executor-only 非 Server 资源的类型化生命周期所有权

- 基线与差异：`HEAD 6b7731cc49c97e8bac0745906d701a10b475bb16 + A1-08-executor-role-lifecycle-v1`；进场索引为空，A1-01～A1-07 已由协调者独立复核并提交，工作区只有协调者预先登记的本文台账差异。本包新增 `executor-role-lifecycle.ts` 及直接测试，修改 Executor 唯一生产 root、生产顺序结构测试和 S7 inspector/mutation；未修改 Server endpoint/state/heartbeat/idle timer、Anchor root、领域、公开协议或其他产品文档，未执行 Git 写操作。
- 责任迁移与 exact-set：旧 `runExecutorRole` 在尾部以可选引用和七段逐对象 `try/catch` 直接清理非 Server 资源；现在唯一 `ExecutorRoleLifecycle` 静态冻结并依次关闭 `localConversationOwner.close`、`evidenceHandler.stopAccepting`、`localWorkspaceHost.close`、`executorJobOwnerLifecycle.close`、`executorDataPlane.close`、`authorityRuntime.stopStorageMaintenance`、`mcpHub.dispose`。该顺序是原生产依赖顺序，不从取得顺序推导：本机 accepted work 与 evidence admission 先关闭，job owner 与 Mesh 仍由既有 `ExecutorJobOwnerLifecycle` 共同拥有并按原 owner→transport 顺序释放，data plane、Authority maintenance 与 MCP 保留到其当前消费者收束。生产 root 的旧七段 direct cleanup 与 job/Mesh fallback 已归零；Server 组仍先于本 owner 收口，`ServerStateFile.cleanup` 仍在其后，原有边界不变。
- 首个安全点与失败补偿：MCP 在构造后、`connectAll` 前登记；Authority 在 `setupAuthorityRuntime` 内通过本 lifecycle 私有 `StartupRollback` 于首个副作用前登记，返回的同一 provenance handle 被 adopt/commit，setup 中途失败仍由同一 rollback 补偿；workspace host 在构造后、`start` 前登记；evidence handler 与 data plane 在构造后、首个消费或 start 前登记；local conversation owner 的 effect-free assembly create 完成后、`start` 与公开消费前登记。协调续做确认 Mesh 构造后曾存在 `bindDeviceRemovalLifecycle` 首个 `await` 失败而 job/Mesh 尚无贡献的窗口，现已把同一个 `ExecutorJobOwnerLifecycle(jobOwnerAssembly, mesh)` 的创建与幂等 handle 登记移动到 Mesh 构造后的第一安全点，并在任何后续 `await`、bind 或 start 前 seal；未恢复 direct fallback，也未拆分第二 owner。其余六项反查未发现同类无人负责窗口。
- fail-closed 与错误语义：descriptor exact-set、重复/迟到/遗漏贡献、Authority foreign handle provenance 均 fail closed；`seal` 只在七项齐全后成立。正常终态与部分启动失败调用同一幂等 handle，重复 close 只执行一次；单项 cleanup 失败不截断其余项。role 原始失败优先保留，若同时存在 cleanup failures 则以 `AggregateError` 同时保留；仅 cleanup 失败时聚合返回。已删除未参与任何合同判断的 `#ownedHandles` 死状态，没有引入全局 registry、optional cleanup bag、动态服务发现或通用 lifecycle 框架。
- 直接与结构证据：`pnpm --filter @zhixing/cli exec vitest run src/serve/__tests__/executor-role-lifecycle.test.ts src/serve/startup-server-owner.test.ts src/serve/__tests__/executor-job-owner-surface.test.ts src/serve/executor-role-terminal.test.ts --maxWorkers=1` 为 4 文件 22/22（新 lifecycle 7、生产顺序 4、job/Mesh surface 6、terminal 5）。新增 production source 断言与 S7 结构约束均要求 job/Mesh lifecycle 的构造、贡献早于 Mesh 后第一个 `await`；反向 mutation 在登记前插入 await 必须失败。S7 初次 canonical 运行 20/21，唯一失败是既有 data-plane mutation 仍匹配迁移前单行调用、实际上没有改变输入；将其改为稳定 identity token mutation 后重新运行 `pnpm s7:lint` 为 21/21 且 registry golden 通过，未更新 golden、未放宽生产断言。
- 静态、构建与状态：`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit`、`pnpm cli:build`、6 个本包代码/门禁路径的最窄 Biome check 与 `git diff --check` 通过；没有运行 CLI 包全测、根级 lint/test/build、package check 或制品验收。若七项 descriptor/顺序、任一取得点、Authority rollback provenance、Mesh/job 首 await 边界、role failure/cleanup aggregation、旧 direct-cleanup 负向集合或对应 S7 mutation 任一变化，恢复 `A1-08-executor-role-lifecycle-v1` 并只重验本闭包。`A1-HOST-DELEGATE-01` 仍打开，Anchor post-server contribution、Server/state/timer 边界和委托桥退场未由本包处理，因此 A1、完成度与最终退出门保持不变。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。下一检查点由协调者在 A1 内从 Anchor post-server contribution、Server/state/timer 生命周期或 `A1-HOST-DELEGATE-01` 退场中选择最高价值的单一责任链，不进入 A2。

### A1-09：收束 Executor Server endpoint、运行状态与 timer 的类型化生命周期所有权

- 基线与差异：`HEAD 136ea9fd812af256873fca136b173e9420f5fd84 + A1-09-executor-server-lifecycle-v1`；进场索引为空，A1-01～A1-08 已由协调者独立复核并提交，工作区只有协调者预先登记的本文台账差异。本包新增 `executor-server-lifecycle.ts` 及直接测试，修改 Executor 唯一生产 root、生产顺序测试和 S7 inspector/mutation；未修改 `@zhixing/server` 的公开合同或 CleanupRegistry、A1-08 non-Server exact-set、Anchor root、领域或其他产品文档，未执行 Git 写操作。
- exact-set 与阶段 owner：唯一 `ExecutorServerLifecycle` 静态冻结五个 subfamily：`inactiveBinding.close`、`runningServer.shutdown`、`serverState.lifecycle`、`heartbeatTimer.clear`、`idleTimer.clearAndSettle`。`bindServer` 返回后立即登记 inactive binding，故 managed-admission/state/projection 及后续任一 setup 失败都有补偿；`ServerStateFile` 构造后、首次 projection await 前登记。heartbeat 与 on-demand idle timer 只能在 Server 已接管后建立，方法内部在 `setInterval` 返回的同一同步段立即持有 timer；idle owner 同时持有唯一 in-flight check 并在关闭时等待其 settle。条件性 idle 不创建空贡献，没有 optional cleanup bag、动态发现或第二 registry。
- endpoint 单向接管：外层 binding owner 只保留到 `runServer.beforeActivate`。该回调发生在 Server lifecycle 已把 `server.close` 注册进自身 CleanupRegistry 之后；`transferToRunningServer` 又要求 `RunningServer.server` 与原 binding 的 `httpServer` object、host、port 全等，才把阶段从 binding 切到 running Server。若错误发生在 gate 前，外层 owner 关闭 inactive endpoint；gate 到达后，所有 open/publication/terminal 失败都只由同一 RunningServer 驱动 Server registry，外层不再直接关闭 binding。`runServer` 返回后还以 object identity 断言是同一 transferred runner，生产尾部的 `localServerBinding?.close` 已删除；没有以 close 幂等掩盖双 owner，也没有复制 PID/connection 清理。
- 状态、timer 与失败终态：A1-06 的 inactive gate、internal stop/trust/final admission 顺序和 `publishReady` 内 `markReady → markRunning` 保持不变，只改由 lifecycle 的窄方法调用同一 `ServerStateFile`。终止先清 idle timer并等待当前 check，再清 heartbeat；`markStopping("graceful")` 失败不会阻断 endpoint 终止，且错误仍进入聚合集合。只有 inactive binding `close()` 或 `RunningServer.shutdown("executor-role-stop")` 成功证明 endpoint terminal 后才尝试 `markStopped`；endpoint 终止失败本身继续聚合，但会阻止虚假 stopped 外化。无论 stop 是否失败，后续 `cleanupState` 都独立尝试清除 state/ready。A1-08 的 `ExecutorRoleLifecycle.close` 仍位于 Server stop 尝试之后、state/ready cleanup 之前；重复 stop/cleanup 复用同一 Promise，最终仍由 `throwExecutorRoleFailures` 保留 role 原始失败并聚合清理失败。
- 直接与真实资源证据：协调反证纠正后，失效的 `executor-server-lifecycle.test.ts` 单独重取为 9/9：除五项 exact-set、真实 loopback inactive 503、endpoint provenance/单向接管、timer/in-flight idle settle、重复关闭及 fail-closed cleanup 外，新增 binding close 与 RunningServer shutdown 失败时 `markStopped` 零调用的直接反例，并证明 `markStopping` 失败时 endpoint 终止、条件满足后的 `markStopped` 与独立 state cleanup 继续执行，前后状态错误全部可观察。输入未变的 `startup-server-owner.test.ts` 4/4 与 `executor-role-terminal.test.ts` 5/5 继续有效，因此当前定向闭包为 3 文件 18/18；A1-06 已接受且输入未变的 Server lifecycle 15/15/真实 loopback gate 证据也继续有效，均未重复执行或重复计数。
- 结构、静态与构建证据：S7 新增五项 descriptor exact-set、binding/state 首安全点、同 endpoint provenance、transfer/entry-last、timer settle、endpoint-terminal 条件、三段关闭顺序和旧 direct cleanup 负向约束；除遗漏 binding owner、绕过 transfer 或恢复尾部 direct binding close 外，新增反向 mutation 会把条件化 `markStopped` 恢复成无条件调用并必须失败，门禁不再用固定调用次数冒充终态证明。canonical S7 初次在测试前因 inspector 把 `bindServer` 语句内部的 `await` 误作“登记后的首个 await”而自误判，修正为从登记点向后取首 await；本轮纠正后再次运行 `pnpm s7:lint` 为 21/21 且 registry golden 通过，没有放宽 owner 合同或更新 golden。`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` 与 `pnpm cli:build` 通过；未运行 CLI 包全测、根级 lint/test/build、package check 或制品验收。
- 失效、遗留与状态：若 `bindServer/runServer.beforeActivate` 接管点、Server `server.close` registry 顺序、endpoint provenance、publishReady state 次序、heartbeat/idle timer 或 in-flight check、RunningServer shutdown、state terminal/cleanup、A1-08 相邻关闭顺序或 S7 mutation 任一变化，恢复 `A1-09-executor-server-lifecycle-v1` 并只重验本闭包。Anchor post-server contribution 与 `A1-HOST-DELEGATE-01` 尚未处理，因此 A1、完成度和最终退出门保持不变。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。下一检查点由协调者在 Anchor post-server contribution 与 `A1-HOST-DELEGATE-01` 退场中选择最高价值的单一 A1 责任链，不进入 A2。

#### A1-09 协调反证纠正：真实 Server terminal 才能发布 stopped

- 协调复核发现初版 `#stopOnce` 把 `markStopped` 与 endpoint 终止并列为无条件失败隔离步骤；binding close 或 RunningServer shutdown 抛错时仍可能留下虚假 stopped，且后续 state cleanup 若也失败会使该错误投影长期残留。本轮没有改变公开 Server/ServerStateFile 合同或重做生命周期 owner，只让 endpoint 终止尝试返回真实成功值，并以该值作为唯一 `markStopped` gate；state cleanup 仍独立执行，多失败仍完整聚合。
- 纠正只使 lifecycle 单测与相应 S7 输入失效：最终直接重取 9/9，canonical S7 21/21 且 registry golden 通过，CLI typecheck/build 通过；production-order 4/4、executor terminal 5/5、A1-08 non-Server owner 与 A1-06 loopback 输入均未变并继续复用。若 endpoint close/shutdown 返回值、`markStopped` 条件、state cleanup 隔离或相应 mutation 任一变化，只恢复本纠正证据与 A1-09 对应失效闭包。

### A1-10：收束 Anchor activation gate 的类型化运行期生命周期贡献

- 基线与差异：`HEAD 9c3c0b03b03d4b604f3b278df1718154a0ccf0fe + A1-10-anchor-runtime-lifecycle-contributions-v1`；进场索引为空，A1-01～A1-09 已由协调者独立复核并提交，工作区只有协调者预先登记的本文台账差异。本包扩展 A1-07 的 `AssemblyLifecycleContributions`、迁移 Anchor 唯一组合根与相关 surface，新增运行期直接测试并同步 shutdown-chain、S7 与两份 golden；未修改 Server/State/PID/heartbeat、HostStop、领域、公开协议、Executor owner 或其他产品文档，未执行 Git 写操作。
- exact-set 与首个安全点：新增静态 `ANCHOR_RUNTIME_LIFECYCLE_DESCRIPTORS`，唯一六项为 `confirmationBridge.dispose`、`execution.abortAllAndWait`、`conversationProtocol.stopRecovery`、`scheduler.stop`、`inboundRouter.refuseNew`、`evidenceHandler.stopAccepting`，owner/role/id 保持原 canonical 合同。Evidence Handler、ConversationManager、InboundRouter 在各自构造/返回后的首个同步安全点登记；scheduler 复用原 `StartupRollback` handle 并在首次后续 await 前贡献；confirmation bridge 创建后立即登记；conversation recovery 则在 `startRecoveryLoop()` 前先登记配对的 `stopRecoveryLoop()`。条件不适用时不制造空 handle；`transferExactTo` 以当前拓扑 expected-set 拒绝 missing、extra、duplicate、wrong-stage，既有 rollback provenance 继续拒绝 foreign，stage 移交后拒绝迟到贡献。
- 移交、顺序与旧路退场：prepared endpoint 仍保持 inactive；post-server setup 完成后依次移交 `post-server`、A1-07 既有 `runtime`、新 `activation` 三段，全部贡献转交并 `assertTransferred` 后才 commit rollback、释放 gate。注册顺序保留当前生产 LIFO：正常关闭实际为 evidence stop → inbound refuse → scheduler stop → recovery stop → execution drain → A1-07 runtime 逆序 → confirmation dispose → 既有 state/surface/foundation/Server/tail；单项失败仍由 CleanupRegistry 隔离并继续其余项，同一幂等 handle 使 gate failure 与 normal close 不会双释放。`command.ts` 的五段 direct registration、confirmation surface self-registration、`AssemblyContext.cleanup` 旁路及 `registerCoreCleanup` 无生产消费的 optional common scheduler 占位均已删除；A1-07 原 12 项 descriptor、entry-last 与产品启用条件未改变。
- 直接证据：定向命令覆盖 `anchor-runtime-lifecycle.test.ts` 5/5、`assembly-lifecycle.test.ts` 6/6、`shutdown-chain.test.ts` 14/14、`distributed-runtime-golden.test.ts` 1/1、`startup-server-owner.test.ts` 4/4、`access-surface.test.ts` 3/3，合计 6 文件 33/33。新增反例直接识别六项 descriptor/stage/owner exact-set、真实 LIFO、单项失败隔离、同 handle 的 rollback/normal-close 恰一次、missing/duplicate/foreign/wrong-stage/late fail closed、条件资源零空贡献，以及生产取得点、三段 transfer、commit 和 ready publication 顺序；没有复跑 A1-09、Server loopback 或 CLI 包全测。
- 结构、golden 与构建证据：S7 collector 从两张有限 literal descriptor 表机械取得 18 项 Anchor lifecycle，并要求六项各有唯一 production contribution、取得早于首个真实 effect、三段 transfer 严格有序、exact-set 完整、旧 direct/self-register/common-scheduler owner 归零。四条反向 mutation分别恢复 confirmation self-registration、把 recovery start 移到 handle 之前、错绑 activation stage 和新增第二 direct owner，均必须失败。首次 canonical 运行只因 inspector 把 ConversationManager 回调体内未执行的 `await` 误作构造后 effect 而失败；改绑真实 `executorDataPlane.start` 后，canonical write 的人工差异仅为六项 source 迁至 `assembly-lifecycle.ts` 并删除无生产调用的 `anchor-host/common/scheduler.stop`，owner/role/id 无漂移。最终 `pnpm s7:lint` 为 21/21 且 registry golden 通过；CLI canonical typecheck、`pnpm cli:build` 与最窄 Biome 通过。
- 失效、遗留与状态：若六项资源构造/启动、conversation recovery start/stop、scheduler generation handle、condition/topology、三段 transfer、LIFO 顺序、StartupRollback provenance、CleanupRegistry descriptor collector/golden 或 entry-last commit/ready 任一变化，恢复 `A1-10-anchor-runtime-lifecycle-contributions-v1` 并只重验上述 6 文件、S7 与 CLI typecheck/build。Anchor binding/ServerStateFile/heartbeat/PID-lock 与 `A1-HOST-DELEGATE-01` 仍未收束，故 A1、完成度和最终退出门保持不变。
- 交接类型：完成，等待协调者独立复核；无本包内遗留。下一检查点由协调者在 Anchor endpoint/state/timer 与 `A1-HOST-DELEGATE-01` 退场中选择最高价值的单一 A1 责任链，不进入 A2。

### A1-11：收束 Anchor Host shell 的唯一类型化生命周期

- 基线与边界：`HEAD 410ace9bec4041e0814cc09f090e38c94ba27792 + A1-11-anchor-host-shell-lifecycle-v1`；进场索引为空，工作区只有协调者预先登记的本文台账差异。本包只收束 Anchor/Anchor+local Executor 常驻 root 的 Host shell；未改 Executor-only A1-08/A1-09 owner、HostStop、accepted-work、领域、公开协议、standalone Server 默认行为或 `A1-HOST-DELEGATE-01`，未执行 Git 写操作。
- exact-set 与首个安全点：新建有限 `AnchorHostShellLifecycle`，七项责任固定为 `serverLogLifecycle.stop`、`endpoint.close`、`authorityCheckpointOwner.stop`、`serverState.lifecycle`、`heartbeatTimer.clear`、`idleTimer.clearAndSettle`、`processDiscovery.release`。Server log、inactive binding、state file 与 checkpoint owner 均在取得后首个安全点纳入构造期已登记的同一 `StartupRollback` handle；heartbeat/idle 只能在 normal owner 接管后创建，idle 停止先 clear timer 并等已开始 check settle。条件不适用时不伪造 log/checkpoint 资源，activation 前以当前拓扑 exact-set 直接 fail closed。
- 同 endpoint 单向接管：`runServer` 只在注入 `CleanupRegistry` 时接受类型化 `ServerLifecycleOwner`；activation gate 中把 prepared server 的 `httpServer` object identity、host 与 port 与已取得 binding 三重全等后，把同一幂等 handle 注册进 normal `CleanupRegistry`。公开 `StartServerOptions` 没有可声明“caller 已拥有 cleanup”的字符串开关；普通 `startServer` activation failure 始终由 Server 默认关闭，持久 Host 只经包内 lifecycle 入口调用实际 owner 的 `cleanupActivationFailure()`，且该方法运行的正是上述同一 handle。Anchor 入口在 A1-10 的 18 项 contribution 完整移交并 commit rollback 之前仍只返回 503；激活后还要通过同 object 的 active proof，才可发布 discovery 与 ready/running。
- 诚实终态与旧路退场：终止操作先停 idle/heartbeat、等待在途 idle check 并尝试 `markStopping`；随后在旧 endpoint 仍占有 OS 端口时校验并删除本 process generation 的 PID/port，令合法 successor 无法落入“校验后、删除前”窗口，再关闭当前 binding/running server。只有 endpoint close 真实成功才尝试 `markStopped` 与 state/ready cleanup；若 endpoint close 失败，则不伪造 stopped，并用冻结 generation 恢复仍存活 endpoint 的 discovery。发现发布部分失败也只删除与本 generation 全等的记录，不删除 foreign generation。`markStopping`、state、discovery、checkpoint 或 log 的单项失败都不截断其他安全动作，失败以 `AggregateError` 保留。`command.ts` 中本 exact-set 的 direct registration、可选引用袋、idle direct timer/fallback 及 `registerCoreCleanup/registerTailCleanup` 全部删除，`shutdown-chain.ts` 与其专属测试退役。canonical catalog 将原 9 个散落 Anchor shell cleanup descriptor 收束为唯一 `cleanup:anchor-host:server:hostShell.stop`；无 lifecycle owner 的通用注入模式不再冒充 Anchor production owner。
- 直接与保护证据：CLI 新 Host shell、production order 与 lifecycle golden 为 3 文件 15/15；A1-07/A1-10 未改贡献的 assembly、runtime、surface、rollback 保护为 4 文件 18/18，合计 CLI 7 文件 33/33。Server 的 bound activation、lifecycle、state、cleanup registry 与 log lifecycle 为 5 文件 76/76；最后改动的 lifecycle 单文件另行重取 15/15，不重复计入 76。反例覆盖七项 exact-set、duplicate/foreign/missing/late，同 handle rollback/normal/repeat，异组 endpoint，gate 失败、真实 loopback 503→200→端口释放，idle in-flight settle，endpoint 失败禁止 stopped/cleanup 并恢复 discovery、单项失败继续与同 generation release。
- 结构、构建与失效：S7 现机械取得七项 shell 资源与唯一 cleanup descriptor，要求 production root 的取得/接管/发布、同 endpoint provenance、idle settle、endpoint-terminal 条件和 discovery generation 全部成立；反向 mutation 能检出无条件 stopped、不等 idle check、丢 lifecycle owner、丢 discovery publication 和恢复 direct ready。canonical write 差异只收束上述 shell descriptor/source，A1-07/A1-10 的 18 项 owner/role/id 未漂移；最终 `pnpm s7:lint` 为 21/21 且 registry golden 通过。Server 上游 build、CLI canonical typecheck、`pnpm cli:build`、适用 TypeScript 路径的最窄 Biome 与 `git diff --check` 通过；MJS 结构脚本被 Biome 配置排除，由 canonical S7 直接执行取证。未运行包全测、根级全量、package check 或制品验收。若七项 exact-set/取得点、endpoint provenance/transfer、activation-failure ownership、A1-10 transfer 顺序、state/discovery 终态条件、timer settle、cleanup descriptor/golden 或 standalone Server 默认任一变化，恢复 `A1-11-anchor-host-shell-lifecycle-v1` 并只重验上述失效闭包。
- 交接类型：已由协调者独立复核并纳入提交 `686822e0`；无本包内遗留。A1 与最终退出门继续保持 `[ ]`；唯一下一检查点是退场 `A1-HOST-DELEGATE-01`，不进入 A2。

#### A1-11 协调反证纠正：真实 activation owner 与 successor-safe discovery

- 反证与根因：初版 `StartServerOptions.activationFailureCleanupOwner?: "server" | "caller"` 只靠可伪造字符串声明外部 owner，direct caller 可在没有 cleanup handle 时跳过 inactive binding 关闭；初版 discovery 又在 endpoint close 后执行 `readLock → releaseLock`，合法 successor 可在两步间绑定同一端口并覆盖 discovery，随后被旧 Host 删除。两处都恢复了 `A1-11-anchor-host-shell-lifecycle-v1` 的 endpoint/discovery 子证据，未扩大到七项 exact-set、A1-07/A1-10 contribution 或 `A1-HOST-DELEGATE-01`。
- 纠正结果：公开 `startServer` 已无跳过清理字段，包根也不导出内部 owner-aware 入口；默认路径自行关闭，Host 路径实际调用同一 `StartupRollback` handle 的 `cleanupActivationFailure()`，后续 registry/rollback 重入不产生第二 close。discovery 的 generation check/release 已移到 endpoint close 之前；真实端口竞争反例在该窗口只能得到 `EADDRINUSE`，旧 endpoint terminal 后 successor 才能绑定并发布且记录保持。endpoint close 失败会恢复旧 generation discovery；partial acquire 失败只条件清理本 generation，foreign record 不受影响。
- 本轮有效证据：`server.test.ts + lifecycle.test.ts` 为 2 文件 28/28，`anchor-host-shell-lifecycle.test.ts` 为 12/12，合计 3 文件 40/40；其中公开伪造 owner 与真实 successor 竞争两个新增对抗均先隔离通过，未重复计数。`pnpm --filter @zhixing/server build`、`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit`、`pnpm cli:build` 与 12 个受影响 TypeScript 路径的最窄 Biome 均通过。S7 新增公开字段/包根出口、实际 owner cleanup、release-before-close、restore 与对应反向 mutation；首次 canonical 重取暴露测试输入漏读 `packages/server/src/index.ts`，补入真实扫描集合后最终 `pnpm s7:lint` 为 21/21 且 registry golden 通过。
- 状态与失效：A1-11 纠正已由协调者独立复核并纳入提交 `686822e0`；A1 与最终退出门仍为 `[ ]`。以后若 `StartServerOptions`/包根导出、owner-aware lifecycle 调用、同 handle cleanup、discovery generation 校验与 release/restore 顺序、endpoint close 终态或相应 S7 mutation 任一变化，只恢复本纠正与 A1-11 endpoint/discovery 失效闭包；其余未变 A1 证据继续复用。

### A1-12：退场持久 Host 委托桥并关闭 A1

- 基线与 exact-set：`HEAD 686822e0edf255f5178f12da2373193ec185f2a1 + A1-12-persistent-host-composition-v1`；进场索引为空，工作区只有协调者预先登记的本文台账差异。旧桥 exact-set 为 `PersistentApplicationHostDependencies.roleLoaders/runRoleTopology`、`ServeRoleLoaders`、`ServiceHostModule`、`runConfiguredServeTopology`、`anchor-role.ts` 与 `executor-role.ts`；这些对象只承担第二层模块选择/转发，没有领域事实、产品用例或独立资源。`command.ts:runServeCommand`、`executor-role-runtime.ts:runExecutorRole`、`@zhixing/executor` 动态模块、`planServeTopology` 及真实 Anchor/Executor 内部装配均是保留的生产组件，不按名称误删。瞬态 `workspace-command.ts` 的本机 fallback 直接消费 Executor runtime，是 A0-03 已冻结的命令期有限装配，不是持久 Host 委托桥或第二常驻组合根。
- 唯一组合与装入链：`topology-command.ts` 仍只做 process mode/preflight 并唯一创建、运行一次 `PersistentApplicationHost`。Host 在 recovery-root 串行 bootstrap 完整停止并重新 prepare 后冻结 roles/configuration，自行调用 `planServeTopology`；disabled 不装入 role，anchor-only 只动态装入 `command.ts`，anchor+executor 同时装入 `command.ts + @zhixing/executor`，executor-only 同时装入 `executor-role-runtime.ts + @zhixing/executor`。每条有效拓扑均先以同一次 `Promise.all` 取得全部所需模块，随后才调用唯一真实 role component；任一装入失败时 role 零启动，Host 仍经同一 terminal path 各自精确一次释放已取得的 Mesh maintenance 与 workspace lease。旧 loader bag、通用 host module、平行 runner 与两个 wrapper 文件全部删除，没有服务定位、字符串注册、空 component 框架或第二 Host。
- 行为与 A1 阶段裁决：三种 process mode 仍经过同一 Host class，三种有效角色拓扑、recovery-root 替换、capacity/Mesh/workspace lease、entry-last、HostStop/accepted-work、role 内部类型化生命周期、真实 Server terminal 与 outer release 顺序均未改变。Host 只规划有限拓扑、冻结依赖、装入并等待组件；没有导入领域 handler、持有领域状态或编排产品用例。冷启动从唯一 CLI main/`serve` action、`createPersistentApplicationHost → host.run`、动态 role imports 与生产 `PersistentApplicationHost` 构造反查，常驻图只有这一条组合链；旧桥名称在生产/类型/测试/构建输入中为零，残余命中仅为 S7 retired 负向门禁与本历史记录。A1-01～A1-11 已接受的 create/start/entry-last、拒新/排空、terminal、类型化 cleanup、失败聚合与零残留证据共同闭合 A1；A1 因此在当前工作区标为 `[x]`，最终退出门继续留待 A7 同基线验收。
- 直接与结构证据：Host/拓扑定向闭包为 3 文件 39/39：`application-host.test.ts` 22/22、`role-topology.test.ts` 11/11 来自修正测试参数后的最终重取，`topology-command.test.ts` 6/6 来自同一源码首次组合命令的未失效通过项；覆盖 3 process mode × 3 topology、disabled/invalid fail closed、三类所需模块装入失败时 role 零启动与 outer 精确释放、恢复根无双图、role 正常/失败终态和重复 run。Server 结构测试 1/1 直接把动态 import 生产边反绑唯一 Host；显式 golden 候选只有 `cli/serve` type-import 计数差异，另以 `git show HEAD` 对 133 个进场生产文件机械重取后确认这些计数已是接受基线事实，并非本包新增依赖或 owner 漂移，更新后 test 可继续识别当前 package graph 与 Host 动态边。
- 门禁、构建与失效：canonical `pnpm s7:lint` 为 coverage/mutation 21/21 且 registry golden 通过；门禁同时要求唯一 Host 调用、有限三模块动态入口、两条真实 role 调用、拓扑分支、Promise-all-before-run，并以反向 mutation 检出第二 role 调用、跨拓扑分支、wrapper 文件恢复、旧 runner/loader 回流。`pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit`、`pnpm cli:build` 与 5 个适用 TypeScript 文件的最窄 Biome 均通过；build 产物保留真实 `command` 与 `executor-role-runtime` chunk，不再生成两个 wrapper entry。`git diff --check` 通过，索引为空，工作区只含 A1-12 实现/测试/门禁/golden 与协调者预先登记后续写的本文；没有运行 CLI 包全测、根级 lint/test/build、package check 或制品验收。若 topology-command 的 Host 构造/调用、ApplicationHost 的 plan/import/invocation/outer release、role-topology plan、wrapper 负向 exact-set、动态构建边或相应 S7 mutation 任一变化，恢复 `A1-12-persistent-host-composition-v1` 与 A1，只重验本闭包。
- 交接类型：完成，等待协调者独立复核 A1；无本包内遗留，不得由本对话进入 A2。

### A2-01：收归 Skill Catalog 管理 Query/Command/Fact Event

- 基线与边界：`HEAD c52cd212a96d9e4b3ee6b84803bde3355d139a12 + A2-01-skill-catalog-management-domain-v1`；进场索引为空，工作区只有协调者预先登记的本文台账差异。本包即时沿 `skill.list/skill.setState/skill.archive → ServerContext → createSkillDirectory → GlobalStatePort → skill.changed` 及 REPL/RPC facade 消费链取证，只迁移 list/set-state/archive 管理行为；未改 `save_skill/admit_skill/load_skill`、usage、legacy import、CAS/本机 materialization、Kernel/Executor 投影、Agent Loop、MCP 或其他领域。
- 领域合同与唯一应用用例：在 `packages/core/src/skills/catalog-application.ts` 建立有限 `SkillCatalogQuery`、`SkillCatalogCommand`、`SkillCatalogView`、`SkillCatalogChangedFact` 与 `SkillCatalogApplication`，合同只表达 Skill id/state、catalog projection/revision 与已提交事实，不暴露 Store、路径、WAL、RPC 或 Server 类型。这组新增合同与实现只经 `@zhixing/core/skills/catalog` 公开：`packages/core/package.json` 与 `tsup.config.ts` 各有唯一 export/build entry，`skills/index.ts` 和 core 根 barrel 均不再转导它。`SkillCatalogApplicationService` 是 list/set-state/archive 的唯一应用入口：管理 list 恒经 `includeDisabled:true` 返回 main/work、pinned/disabled、usage 与 catalog revision；命令先取得当前 entry/revision，拒绝空 patch/初始 not-found，再以 expected revision 提交既有 `GlobalStatePort` Correctness 适配，Skill adapter 在同一次权威 transaction 结果中返回精确 catalog revision，应用只能在该 commit 成功返回后形成一个对应 fact。mutation 期间的 authority not-found/revision/idempotency failure 均保留为领域 `conflict`；其他提交失败原样传播且不产生 fact，也不靠提交后的竞态 read 猜测版本。
- 纯 binding 与旧路退场：`ServerContext`、Skill RPC 与 Anchor 组合根只从 `@zhixing/core/skills/catalog` 消费新增合同；三个认证 RPC 只做 wire 参数校验、应用调用、初始 not-found→`NOT_FOUND`、命令校验→`INVALID_PARAMS` 的既有显式映射，以及把 committed fact 投影为既有 `skill.changed { structuralVersion }` 通知。领域内部 concurrent conflict 不再被新增映射为 `BUSY`，而与迁移前 `SkillMutationConflictError` 一样由真实 dispatcher 投影为 `INTERNAL_ERROR / "Internal error" / data.message=<原始冲突消息>`；普通 commit failure 同样保持既有 INTERNAL_ERROR wire。`skill.list` 的 `{ skills, structuralVersion }`、三个 method 名与 registry、REPL `/skills`/动态 slash 刷新和 management facade 行为不变。Anchor 组合根直接构造唯一 `SkillCatalogApplicationService`；`@zhixing/server` 的 `SkillDirectory`、CLI `createSkillDirectory`、结构版本可变缓存、直接 skill GlobalState mutation 与同责兼容壳均已删除，没有双写或双事件。
- 直接证据：core `catalog-application.test.ts + global-state-adapter.test.ts` 2 文件 6/6，覆盖 include-disabled query、expected revision、精确 committed catalog revision、set-state、not-found/空 patch、authority conflict/commit failure 的零 fact及既有 CAS/materialization；Server `management-methods.test.ts` 1 文件 6/6（其中 Skill 4/4），新增真实 `HandlerRegistry → RpcDispatcher → toJsonRpcError → connection` 反例，逐项证明 list/setState/archive 成功，以及 NOT_FOUND、INVALID_PARAMS、concurrent conflict 与普通 commit failure 的迁移前 wire；成功恰一次广播，所有失败零广播。CLI facade/REPL 管理闭包 4 文件 22/22 输入未变且沿用已取得直接证据，纠正后另以 CLI typecheck/build 验证新 subpath 组合入口。当前完整直接闭包为 7 文件 34/34，精确计数不重复相加；公开 registry 由 canonical golden 继续证明无漂移。
- 结构、exports、构建与失效：S7 Skill 管理 owner inspector 现以八条反向 mutation 机械拒绝 Server/CLI 恢复 `SkillDirectory/createSkillDirectory`、直接构造 mutation、commit 前/重复传输 fact、core 根 barrel 回流、Server/CLI 绕过领域 subpath、第二 package export 与 conflict→BUSY wire 漂移；同时要求唯一 tsup entry。canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过。`pnpm runtime:package-exports` 直接加载新 subpath、确认 runtime/type surface 不泄入 core 根或第二正式 export；首次执行只因 Server 旧 dist 仍请求已退场 root export 失败，按验证手册重建 Server 后同一门禁通过，不属于产品失败。core/server/CLI canonical typecheck 通过；按依赖先后完成 core build、server build 与 `pnpm cli:build`；本次未修改 contracts schema，既有 `pnpm contracts:typecheck/contracts:lint` 证据未失效；纠正闭包 7 个适用文件的最窄 Biome 通过，最终 `git diff --check` 待交接前复取。未运行包全测、根级 lint/test/build、package check 或制品验收。若领域源码/subpath/export/build entry、GlobalState adapter conflict/commit 返回、ServerContext/binding、Anchor service 注入、公开 skill RPC/notification、CLI facade/REPL consumer、package-export gate 或 S7 owner/ordering/wire gate 任一变化，恢复本证据并只重验上述闭包；其余 Skill 链与 A1 证据不失效。
- 独立复核纠正：协调者指出新合同经 core 根 barrel 暴露与 conflict→BUSY 均违反本包边界；本轮没有把它们包装成后续债务，而是在原 A2-01 内建立唯一领域 subpath、删除根转导、恢复迁移前 wire，并新增 package-export 与 dispatcher 级可证伪证据。纠正不扩大到其他既有 Skill 根导出或 A7 全包 barrel 退场。
- 交接类型：已由协调者独立复核并纳入提交 `c0386d5f`；A2 与 Skill Catalog 登记行继续保持 `[ ]`。本包内无遗留；下一检查点为 A2-02 的 `save_skill` 创建/更新应用行为，不得提前进入 A3 或建设全局 Product API Catalog。

### A2-02：收归 `save_skill` 创建/更新应用行为

- 基线与边界：`HEAD c0386d5f85cbe999fb1addf4143dfa3e6ac212a6 + A2-02-skill-catalog-save-domain-v1`；进场索引为空，工作区只有协调者预登记的本文台账差异。本包即时沿 `save_skill → createAgentRuntime → createAssignmentSkillPorts → ArtifactStore/global query/assignment staged mutation` 与同 assignment overlay 取证并迁移；未改 `admit_skill/load_skill`、usage、legacy/materialization、GlobalState commit、Agent Loop、Kernel/Executor 投影、公开工具 schema/确认分类/返回文案或其他领域。
- 责任迁移与唯一入口：既有 `@zhixing/core/skills/catalog` 现在同时公开窄 `SkillCatalogSaveApplication` 与唯一 `SkillCatalogSaveApplicationService`。领域服务独立执行全字段 secret scrub/计数、trim 后非空与 `skillNameToId`、create/update 判定、standard frontmatter document、artifact-before-stage、expected revision、稳定 `${toolCallId}:save` 及结果投影；它从 path-free `SkillCatalogSaveCorrectnessPort` 读取当前 catalog 和按序 assignment overlay，并将 create/admit/update overlay 折叠为 read-own-writes 视图。disabled/linked 现有条目仍走 update、更新后重新启用并转 own；仅 builtin 同名而 catalog 无条目时仍 create/fork-to-own；同 assignment 连续保存按 overlay revision 继续 update。保存用例现在通过与 `createAssignmentMutationPort` 完全相同的 `{assignmentId, domain, operationId}` request identity 精确定位本 operation record：重放只折叠该 record 之前的其他 operation，draft/CAS/mode 相同则以耐久记录中的原 create/update payload 重驱并返回原 outcome；后续不同 operation 仍看到全部前序 overlay，继续形成 revision 单调的 update。该身份由 protocol digest 和耐久 ledger record 重启后重建，不使用进程内缓存、最后一条猜测或内容身份。
- Binding、Correctness 与旧路退场：tools-builtin 的 `createSaveSkillTool` 只持有领域应用接口、输入/default mode、确认边界与原产品文案；`BuiltinToolContext` 只注入 `skillCatalogSave`。orchestrator 的 adapter 只把 ALS 中的 global query、assignment overlay/stage、assignment issue time 与 `ArtifactStore` 映射到领域所需端口；生产 `createAgentRuntime` 注入唯一 `saveApplication`，不再持有 scrub、id/upsert、frontmatter、expected revision 或 staged suffix 规则。旧 core `save-pipeline.ts` 及专属正向测试、skills/root 转导、tools `SkillSaver` 合同与 orchestrator inline saver 均已删除；不存在双写、兼容壳或第二 package export/build entry。
- 失败与行为保护：原始缺字段仍由工具 binding 返回既有 `save_skill 需要非空...`；领域直调对脱敏/trim 后空描述、正文或空 id fail closed。ArtifactStore 失败时不 stage，stage/冲突失败保持原错误可观察；缺 durable tool operation id 仍在 artifact 形成后、stage 前以既有错误拒绝，不虚报入库。工具名/schema、无 boundary→确认、串行分类、main/work default、成功/更新/脱敏告知与“本轮成功完成后入库”文案均未漂移。
- 独立反证与纠正：协调复核证明初版仅覆盖“stage 已成功但读取 overlay 尚不可见”的重放；当真实 ledger 已返回本 operation record 时，旧折叠会把 create 改推为 update，并以同 requestId 提交异载荷冲突。本轮没有放宽 ledger 幂等或吞掉冲突，而是提取唯一的 `assignmentMutationRequestId` 协议算法供 mutation port 与 Skill Correctness adapter 共同使用；overlay adapter保留 durable `requestId/recordSeq`，领域以同 identity 的 recordSeq 为截点，且 exact draft 直接重驱原 mutation。name/description/body/mode 任一导致 payload 变化时仍进入同 requestId 异载荷冲突；不同 toolCallId 则读取前序记录并追加后继 update。
- 直接、结构与构建证据：core `catalog-application.test.ts` 1 文件 11/11，新增 overlay 可见且全局 catalog 已变化时仍返回原 create/payload 的反例，并保留不可见 overlay 稳定 payload；orchestrator `assignment-skill-port.test.ts` 1 文件 4/4，adapter mock 改用真实 request-id 算法；CLI `assignment-schedule-stager.test.ts` 1 文件 4/4，保持 mutation port 身份/拒绝边界；CLI `assignment-mesh-adapter.test.ts` 定向 1/1 以真实 Mesh dispatch、`createAssignmentMutationPort`、`ConversationAssignmentLedger` 与 `FileArtifactStore` 证明首次 stage 后 overlay 不可见重试、可见重试均成功且 ledger 仍仅 1 条，四类异载荷全部冲突，另一个 toolCallId 追加第 2 条 `expectedRevision:1` update。纠正直接闭包为 4 文件 20/20；未改 tools-builtin 2 文件 12/12 证据继续有效，合并后的 A2-02 直接闭包为 6 文件 32/32，不重复相加。
- 门禁、构建与失效：S7 Skill owner inspector 在原五类迁移突变外新增 durable request identity、recordSeq 截点、原 payload 重驱三类反向约束；最终 canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过。`pnpm runtime:package-exports` 重新加载 core protocol 并确认唯一共享 `assignmentMutationRequestId` 已进入既有 `@zhixing/core/protocol`、Skill 保存应用仍只在 catalog subpath 且 core 根无回流。core、orchestrator、CLI 最窄 TypeScript typecheck 均通过；按依赖完成 core build、orchestrator build 与最终 `pnpm cli:build`，tools-builtin 生产输入未变而复用本包初次有效 build；受影响路径最窄 Biome 与 `git diff --check` 通过。初次新增真实 ledger 测试因直接读取尚未物化的 dispatch artifact 失败，改走已有真实 Mesh executor adapter 传输完整 closure 后同一反例通过，该失败是测试装配错误而非产品缺陷。未运行包全测、根级 lint/test/build、package check 或制品验收。
- 状态、遗留与下一检查点：本包内无遗留；A2 与 Skill Catalog 登记行继续保持 `[ ]`，因为 admit/load/usage、legacy/materialization 与 Kernel/Executor 投影仍未完成整个 Skill 垂直域。若 `catalog-application.ts` 保存合同/原 mutation 重放、assignment request-id 算法、overlay `requestId/recordSeq` 投影、唯一 subpath/export/build entry、ArtifactStore→stage 顺序、tool binding/产品文案、operation suffix、旧路径负向集合或相关 S7/package-export 输入任一变化，恢复本证据并只重验上述闭包。交接类型为已由协调者独立复核并纳入提交 `6b400201`；下一检查点为 A2-03 的 `admit_skill` 两阶段安全接入生命周期，不得提前进入 A3。

### A2-03：收归 `admit_skill` 两阶段安全接入生命周期

- 基线与边界：`HEAD 6b4002012edfd45ac21d052a83892c9c9a2ecca8 + A2-03-skill-catalog-admission-domain-v1`；进场索引为空，工作区只有协调者预登记的本文台账差异。本包即时沿 `admit_skill → BuiltinToolContext → assignment Skill ports → OS-temp candidate/independent main LLM/ArtifactStore/assignment mutation ledger` 与全部清理分支取证并迁移；未改 `load_skill`、usage、legacy/materialization、Kernel/Executor 投影、Agent Loop、公开工具名/schema/权限边界或其他领域。
- 领域生命周期与唯一入口：既有唯一 `@zhixing/core/skills/catalog` subpath 现在公开窄 `SkillCatalogAdmissionApplication`、typed outcome、opaque candidate/Correctness port 与唯一 `SkillCatalogAdmissionApplicationService`。领域服务拥有首调/确认状态机、pending token、10 分钟 TTL、safe/needs-confirm/escalate 三态、独立 `assessSkill` fail-safe 裁决、frontmatter name/description/id、main/work mode、artifact digest 绑定、TOCTOU 比对、canonical 原文 artifact、稳定 `${toolCallId}:admit` staged mutation及清理决策。needs-confirm 只在有效等待期保留候选；safe/escalate、缺 name、首调异常、确认成功/失败、过期、篡改都会释放候选，进程遗留只由下一首调的 TTL sweep 清除。领域只持有 opaque candidate id/document/digest，不读取或写入任意 OS 路径，也没有新增耐久 token 产品事实。
- Binding、适配与旧路退场：tools-builtin 的 `createAdmitSkillTool` 只规范化 path/token/mode、调用领域应用并把 typed outcome 映射回迁移前完全相同的中文文案和 `isError`；工具 schema、`permissionArgumentKey=path`、filesystem/read + app-state/write 双边界和串行分类未漂移。orchestrator 的 `AssignmentSkillAdmissionCorrectnessPort` 只适配本地目录或单一 `SKILL.md` 获取、OS-temp candidate、独立 main 档 LLM、clock/random、ArtifactStore 与真实 assignment mutation；候选复制后以 `lstat` 递归拒绝 symlink/非常规文件，路径不越出受控根。生产 `createAgentRuntime` 注入唯一 admission application；旧 `SkillAdmissionPort/SkillAdmissionAssess/SkillAdmissionWorkspace`、工具 pending Map/firstCall/confirmAdmission、orchestrator 业务 admit 与同责兼容壳均已退出，不存在第二状态机、第二写入口或根 barrel/错误 subpath 回流。
- 耐久重放与资源终态：safe 或确认成功均先把原始 canonical `SKILL.md` 放入共享 CAS，再以 `${toolCallId}:admit` stage `skill-admit`；ArtifactStore/stage 失败、缺 operation id 和 ledger 异载荷冲突均不虚报成功，候选被释放，已写 CAS 仍保持不可变去重语义。同 toolCallId、同来源内容在 stage 已耐久但响应丢失后重新获取候选，会形成相同 content ref 与 mutation，由真实 `ConversationAssignmentLedger` exact replay 接受且不产生第二记录；同 toolCallId 改变 name/description/body/mode 继续由同 request identity 的异载荷冲突 fail closed，不同 toolCallId 则形成后继 admit。无/错/已消费 token 均返回既有“重新审查”终态；确认使用首调登记 mode，不能由重调参数改写。
- 直接证据：core `catalog-admission.test.ts + catalog-application.test.ts` 2 文件 17/17，覆盖三态、LLM 抛错/坏 JSON fail-safe、token/TTL/单次消费、mode、digest 篡改、缺输入/name/operation、artifact/stage failure、清理和重放/冲突；tools-builtin `admit-skill.test.ts + factories.test.ts` 2 文件 7/7，证明真实 factory 依赖与纯 binding 产品呈现；orchestrator `assignment-skill-port.test.ts` 1 文件 7/7，直接覆盖目录与单文件获取、真实 OS-temp artifact-bound confirmation、symlink 拒绝、过期 orphan sweep、全部适配及零临时候选残留。CLI `assignment-mesh-adapter.test.ts` 首次整文件 18 个既有用例通过而新增用例因测试分别加载 source/dist `AsyncLocalStorage` 产生装配失败；改为与生产 adapter 共享同一 source context 后，只重跑失效的新用例 1/1，通过真实 factory→领域应用→orchestrator adapter→`createAssignmentMutationPort`→`ConversationAssignmentLedger` 证明 exact replay、异载荷冲突和不同 operation 后继记录；该失败不属于产品缺陷，既有 18 项输入与结果未失效。
- 结构、exports 与构建证据：S7 Skill owner inspector 现在同时冻结 management/save/admission 三条有限应用边界，拒绝 retired admission port/workspace、工具内状态机、orchestrator 业务规则、根导入、错误装配、漏 digest 比对或第二 owner；为 admission 增加四条反向 mutation，并把 save 的 artifact-before-stage 检查限定到 save 段，避免 admission 的第二个合法 stage 使旧 mutation 失去识别力。canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过；`pnpm runtime:package-exports` 确认 admission 合同仅由 catalog subpath 加载且 core 根无泄漏。core/tools-builtin/orchestrator 最窄 typecheck 已通过，最终 CLI typecheck通过；按受影响依赖完成 core、tools-builtin、orchestrator build 与 `pnpm cli:build`，core build 仅保留既有 circular-chunk warning并成功生成 DTS。最窄 Biome 接受 10 个适用文件；最终 `git diff --check` 待交接前复取。未运行包全测、根级 lint/test/build、package check 或制品验收。
- 状态、失效与下一检查点：本包内无当前遗留；A2 与 Skill Catalog 登记行继续保持 `[ ]`，因为 load/usage、legacy/materialization 与 Kernel/Executor 投影尚未完成整个 Skill 垂直域。若 admission contract/service、`assessSkill` 三态、token/TTL/digest、候选适配/path 安全、artifact→stage 顺序、operation suffix、AssignmentMutationPort/ledger 幂等、工具文案/权限、生产注入、旧 owner 负向集合、catalog subpath/export 或相关 S7 输入任一变化，恢复 `A2-03-skill-catalog-admission-domain-v1` 并只重验上述闭包。交接类型为已由协调者独立复核并纳入提交 `79094479`；下一检查点为 A2-04 的 `load_skill` 读取与 usage 命中事实，不得提前进入 Kernel/Executor 投影或 A3。

### A2-04：收归 `load_skill` 读取与 usage 命中事实

- 基线与边界：`HEAD 79094479f6ae473acd091cd01adf96f871b17d01 + A2-04-skill-catalog-load-usage-domain-v1`；进场索引为空，工作区只有协调者预登记的本文台账差异。本包只沿 `createAgentRuntime → builtin factory → Skill Catalog load application → assignment Correctness adapter → global query/overlay/ArtifactStore/assignment ledger` 即时取证并迁移；未改 `renderAssignmentSkillIndex`、legacy/materialization、Kernel/Executor 不可变能力投影、Agent Loop、管理/save/admission、公开工具名/schema/权限边界或其他领域。
- 领域责任与行为保护：唯一 `@zhixing/core/skills/catalog` subpath 新增窄 `SkillCatalogLoadApplication`、path-free Correctness port 与唯一 `SkillCatalogLoadApplicationService`。领域应用拥有 id 读取、user-over-builtin、create/admit/update overlay 折叠、artifact 文档解析、返回投影及稳定 `${toolCallId}:usage`；user entry 即使 disabled 或与 builtin 同 id 仍按既有直接加载语义优先，overlay 保存/接入后可立即读到全文。Correctness 入口显式区分 `assignment` 与 `builtin-only`：前者必须有 active durable assignment 后才读取任何 id；后者只代表生产 runtime 没有 ArtifactStore，允许 builtin 零 usage，未知或 user Skill 继续以迁移前 `User skills require an active artifact-backed assignment` 失败。user artifact 先读取并经过 frontmatter parser，之后才以 assignment issued time stage `skill-usage { skillId, occurredAt, hitDelta: 1 }`，artifact/operation/stage 错误均不虚报成功。
- Binding、适配与旧路退场：tools-builtin 的 `createLoadSkillTool` 只校验 id、调用 typed application 并保留既有完整正文、中文成功/错误、app-state/write 自动放行与并行分类；`BuiltinToolContext` 和生产 runtime 只注入 `skillCatalogLoad`。artifact-backed orchestrator adapter 在读取 scope 的第一步要求真实 run context，缺失时对 builtin、user 和 unknown 均以迁移前 `Skill access requires an active durable assignment` fail closed，且不读取 artifact、不 stage usage；无 ArtifactStore 的 production fallback 则只返回 typed `builtin-only` scope，不在工具层按 id 特判。其余 adapter 只把 global `skill-get`、assignment create/admit/update overlay、ArtifactStore bytes、assignment issued time 与 mutation stage 投影到 path-free port，不再决定 builtin 优先级、Skill id/upsert 折叠、frontmatter 或 usage payload。`SkillTextLoader` 类型、`skillLoader` 装配、orchestrator `readSkillEntry/stageSkillMutation` 和 production fallback 旧入口全部退出；`SkillStore.loadText` 的 legacy/materialization 存储实现按后续边界保留，但没有生产 `load_skill` consumer 或同责兼容壳。
- 耐久重放：usage operation 恒为 `${toolCallId}:usage`，真实 `createAssignmentMutationPort` 继续用 assignment/domain/operation 生成 ledger request identity。同 toolCallId、同 Skill 在首次 stage 已耐久且 overlay 可见后的重试形成完全相同 mutation，由 `ConversationAssignmentLedger` exact replay 接受且仍只有一条 usage；同 identity 改载另一 user Skill 由异载荷冲突 fail closed，不同 toolCallId 追加独立后继 usage。该机制不依赖进程缓存、overlay 猜测、内容相等或第二状态机；usage overlay 本身不参与 Skill 内容折叠。
- 独立复核纠正与直接证据：独立复核指出 nullable assignment view 把“ArtifactStore 存在但缺 run context”和“production 无 ArtifactStore”错误折叠；本轮改为上述 discriminated scope，并保留唯一领域应用。core `catalog-application.test.ts` 15/15，机械锁定 builtin-only 的 builtin 零 usage及 unknown/user 旧错误；orchestrator `assignment-skill-port.test.ts` 7/7，新增真实 FileArtifactStore 反例证明缺 run context 时 builtin 与 user/unknown 均以 active durable assignment 错误拒绝且零 artifact/usage；CLI `assignment-mesh-adapter.test.ts` 定向 1/1（其余 19 项未运行）继续命中真实 factory→领域应用→adapter→`createAssignmentMutationPort`→`ConversationAssignmentLedger`，证明 user-over-builtin/overlay、artifact-before-usage、exact replay、异 Skill 同 identity 冲突、不同 operation 后继命中以及 active assignment 内 builtin 零 usage。纠正闭包实际重取 3 文件 23/23；未改的 tools-builtin `skill.test.ts` 10/10 继续有效，因此 A2-04 四文件证据仍为 33/33，但未重复运行或重复计数。CLI 定向测试首次因把 artifact-backed builtin 调用放在 run context 外而按修复后的 fail-closed 合同失败；将测试调用放回真实 assignment context 后通过，未放宽生产 adapter。
- 门禁、构建、状态与失效：S7 Skill owner inspector 同时冻结 management/load/save/admission 四条有限应用边界，拒绝 `SkillTextLoader/skillLoader`、根 barrel、工具 `loadText` 旁路、orchestrator 业务折叠、漏 domain service、usage suffix、artifact/parse 前提前 stage，以及 artifact-backed adapter 把缺 run context 降级为 builtin-only fallback；共以六条新增反向 mutation 证明。canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过。`pnpm runtime:package-exports` 证明 load service 只由 catalog subpath 导出且 core 根/其他 subpath 无泄漏；core、tools-builtin、orchestrator 与 CLI build、CLI typecheck 均通过，纠正涉及的 6 个适用文件最窄 Biome 通过，core build 仅有既有 circular-chunk warning。A2 与 Skill Catalog 登记行保持 `[ ]`；若 load contract/service、assignment/builtin-only scope、active durable assignment gate、builtin registry、overlay shape/折叠、artifact→parse→usage 顺序、operation suffix、AssignmentMutationPort/ledger identity、工具 binding/产品文案、runtime 注入、旧 loader 负向集合、catalog subpath/export 或相关 S7 输入任一变化，恢复本证据并只重验上述闭包。交接类型为已由协调者独立复核并纳入提交 `84b00ec8`；下一检查点为 A2-05 的 Kernel 不可变 Skill 能力投影，不得提前迁移 Executor cache 或进入 A3。

### A2-05：建立 Kernel 消费的不可变 Skill 能力投影

- 基线与边界：`HEAD 84b00ec85755bb8a14c2248827d586c4b417cbe3 + A2-05-skill-kernel-projection-v1`；进场索引为空，工作区只有协调者预登记的本文台账差异。本包只沿 `AgentRuntime initial/window boundary → assignment global skill-catalog query → Skill index prompt segment` 迁移当前投影责任；未改 A2-01～A2-04 的管理/save/admission/load/usage、legacy/materialization、`FileExecutionAssetCache`、ExecutionAssetSnapshot/Bundle、Mesh/Executor消费、Agent Loop、公开协议或其他领域。
- 领域投影与字节保护：唯一 `@zhixing/core/skills/catalog` subpath 新增 path-free `SkillCatalogKernelProjectionSource`、冻结 `{ catalogRevision, content }` 与唯一 `SkillCatalogKernelProjectionApplicationService`。领域服务独立拥有 main/work mode、disabled 用户项不显示但仍按全用户 id 遮蔽 builtin、authority 顺序、前 20 个可见用户、随后追加未被遮蔽 builtin 和 `renderSkillIndex` 最终字节规则；无 source 时同一服务生成 builtin-only revision `-1`，空用户 catalog 与该内容 byte-equal。输出不暴露 entry、Store、路径、ArtifactStore、GlobalState/Authority 或写端口，Kernel 不再解释 Skill 字段。
- Correctness、Kernel 与旧路退场：orchestrator 的 `createAssignmentSkillProjectionApplication` 只发送 `{ kind:"skill-catalog", includeDisabled:true }`，校验 result discriminant 后原样投影 revision/entries给领域服务；不做过滤、排序、shadow、top-N、builtin拼池或渲染。`createAgentRuntime` 的初始 builtin prompt 与真实 global query 窗口都调用同一领域应用；同 attention window 多 run 不重复读取且 prompt byte-equal，窗口换代才重取。实例只在 `catalogRevision` 递增且查询开始时的 `instanceEpoch` 仍为当前 epoch 时更新权威，因而旧 revision或前一窗口延迟结果不能覆盖后继窗口；窗口内局部换代继续沿既有 `myEpoch > instanceEpoch` 规则。旧 `renderAssignmentSkillIndex`、orchestrator 对 `renderSkillIndex/builtinIndexEntries` 的生产依赖、字段过滤与 top-N 常量均删除，没有第二 renderer、builtin 初始旁路或缓存掩盖 query 错误。
- 直接证据：core `catalog-application.test.ts` 19/19，新增四项直接锁定 frozen builtin-only、main/work/disabled shadow、authority order/top-20/builtin append、空 catalog byte equality与 revision；orchestrator `assignment-skill-port.test.ts` 8/8，证明 adapter 的 exact query与错误 result kind fail closed；`create-agent-runtime.test.ts` 定向 5/5，命中 builtin-only/own shadow、同窗口一次读取与 byte equality、旧 revision 拒绝、可控延迟的前一窗口结果不能覆盖后继窗口以及 workscene mode。没有把未运行的其余 66 项计入。core、orchestrator、CLI TypeScript typecheck通过；按依赖完成 core build、orchestrator build和`pnpm cli:build`，core build仅有既有 circular-chunk warning。六个适用 TypeScript文件最窄 Biome通过。
- 结构、exports、失效与下一检查点：S7 Skill owner inspector新增 immutable projection exact-set，并以反向 mutation 检出领域丢 disabled规则、orchestrator renderer回流、Kernel旧 epoch覆盖与 builtin拼池旁路；首次隔离执行因遗漏仓库规定的`--import=tsx/esm`而在TS源码的`.js`解析处失败，补回 canonical loader 后定向1/1通过，最终`pnpm s7:lint`为22/22且registry golden通过。`pnpm runtime:package-exports`确认新runtime/type合同只在既有catalog subpath可达，core根和其他subpath无泄漏；`git diff --check`通过。协调者独立复跑 core 19/19、orchestrator adapter 8/8、Runtime 六个直接反例 6/6、S7 22/22、registry golden 与 package exports，确认领域输出字节、Kernel 纯消费、main/work、disabled shadow、authority order/top-N、builtin-only、revision/epoch 并发换代和旧 renderer 归零均成立；本包已纳入提交 `ce02f215`。A2与Skill Catalog登记行继续保持`[ ]`；若投影合同/service、mode/disabled/shadow/top-N/render规则、raw query/discriminant、初始builtin或窗口revision/epoch更新、catalog subpath/export、旧renderer负向集合及相关S7任一变化，恢复本证据并只重验上述闭包。下一检查点为A2-06的Executor execution-asset投影，不得提前进入A3。

### A2-06：统一 Executor execution-asset 的不可变 Skill 投影

- 基线与边界：`HEAD ce02f215a8a484ea4d37503d2f11643ffc1a96cb + task-doc:coordinator-dispatch + A2-06-execution-asset-skill-projection-v1`；进场索引为空，工作区只有协调者预登记的本文台账差异。本包只沿 `Authority skill-catalog → signed ExecutionAssetSnapshot/Bundle → Mesh install → FileExecutionAssetCache/global query → SkillCatalogKernelProjectionApplicationService/load contentRef → AgentRuntime` 收口不可变 Skill 投影；未改 Rubric/Prompt 产品规则、snapshot/bundle schema v1、公开协议/导出、A2-01～A2-05 应用行为、legacy/materialization、Agent Loop 或其他领域。
- 真实责任变化：`createSignedExecutionAssetSnapshot` 不再把 Skill 按 id 重排，而是把 Authority 已通过 canonical catalog codec 的次序原样纳入 digest/signature；Rubric/Prompt index 继续按既有 id canonicalization。`publishFromAuthority` 对全部声明 Skill `contentRef` 先做 CAS 完整读校验，缺失或损坏即拒绝发布，不再按项过滤后以原 catalog revision 签出部分 catalog；Rubric/Prompt 无 producer/不可读时的既有 best-effort 语义原样保留。Executor cache 对“索引不存在”仍返回 builtin-only 的 revision 0 原始查询，但已存在而损坏的 signed index 必须报错；`skill-catalog` 与 skills asset-index 在返回前验证全部 Skill body，`skill-get` 对已声明但不可读正文报错而不是伪装 not-found，真正未知 id 仍为 null。cache 只履行 raw GlobalQuery 的显式 includeDisabled/mode/limit 约束，不拥有 Kernel 的筛选、排序、渲染或 revision 决策，也未引入第二 catalog owner。
- 发布、安装与单调裁决：既有 bundle codec 仍先验证 signed snapshot、artifact exact-set/base64/ref digest/bytes，全部 body 写入并复读通过后才由 `install` 原子发布 index。新增 Skill catalog transition 只接受更高 catalog revision，或同 catalog revision 下 Skill 数组（含内容、contentRef 与次序）全等；exact digest 重放幂等，同 revision 只改变 Rubric/Prompt 且 snapshot revision 递增仍允许，低 catalog revision 与同 revision Skill equivocation 均 fail closed。生产 `setup-delivery` 继续在 assignment 同步前经 Mesh `install-assets` 安装 bundle；Anchor 本机 raw GlobalState 与 Executor cache 都由 A2-05 的同一 `SkillCatalogKernelProjectionApplicationService` 解释 main/work、disabled shadow、authority order、top-N 与 builtin append，Kernel只收到冻结 `{catalogRevision,content}`。`load_skill` 仍沿 signed entry 的同一 `contentRef` 从本机 CAS 取得完整正文，未得到 Store、路径、Authority 或写端口。
- 直接证据：CLI `execution-asset-cache.test.ts` 3/3 以 21 个非 id 顺序 main 用户项、disabled builtin shadow 与 work 项证明 Authority 次序进入签名、本机 Authority source/安装后 Executor cache 在同 revision 的 main/work projection byte-equal、对应正文 byte-equal；删除已声明正文后 catalog 与对应 get 同时拒绝。另以真实 `installBundle` 证明 incomplete bundle 不发布 index、低 catalog revision 拒绝、同 revision 的 Skill 内容/次序改写拒绝，而同 revision 的独立 prompt 更新允许；损坏 signed index 和 Authority 声明缺 body 均 fail closed。`execution-snapshot-mesh.test.ts` 既有 wire/签名反例 2/2 同基线有效，合计 CLI 5/5。core `catalog-application.test.ts` 19/19 保持领域 main/work、disabled shadow、authority order/top-20/builtin append 规则；orchestrator 定向 7/7（raw adapter 1 项、Runtime 6 项）保持 builtin/用户遮蔽、同窗 byte-equal、旧 revision/延迟窗口拒绝与 workscene mode。首次 CLI 直接测试在 core 新源码尚未重建、进程实际加载旧 dist 时复现旧 id 排序失败；按验证手册先完成 core build 后，同一失效闭包通过，未把旧 dist 误作结果。
- 结构、构建、状态与失效：S7 Skill owner inspector 纳入 snapshot/cache producer，要求 signed Skill 次序原样、声明正文全有或全无、catalog rollback/equivocation 拒绝且 cache 不成为第二 renderer，并用四条反向 mutation 检出恢复 id 排序、逐项 partial filtering、删除 catalog 单调条件与 cache renderer 旁路；canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过。core build 与 `pnpm cli:build` 通过，core 仅有既有 circular-chunk warning；CLI TypeScript `tsc --noEmit` 通过；三个适用 TypeScript 源码/测试文件最窄 Biome通过。未改 package exports，故不重复 A2-05 已通过且输入未失效的 package-export 门；未运行根级全量或制品验收。协调者独立复跑 CLI execution-asset/cache 与 Mesh 5/5、core 领域投影 19/19、S7 与 registry golden，并反查 signed order、catalog 单调、bundle manifest-last、同 revision Prompt 独立更新及 present-corrupt/缺 body 失败链，未发现反证；本包已纳入提交 `6d946546`。A2 与 Skill Catalog 登记行保持 `[ ]`；若 ExecutionAssetSnapshot 的 Skill规范化/签名、catalog/body codec、publish/install 顺序、snapshot/catalog revision、bundle exact-set、cache query、Mesh install、assignment global query、领域投影/load contentRef、相关 S7 或 Rubric/Prompt 独立变化边界任一变化，恢复本证据并只重验上述闭包。下一检查点只处理 Skill legacy import/materialization 的最终责任与退场，不得提前进入 A3。

### A2-07：退役 Skill legacy import、文件物化与旧 writable Store，闭合 A2

- 证据与基线：`A2-07-skill-legacy-store-retirement-v1`；`HEAD 6d946546b98164695a4e8c697fe2a6a61b279352 + task-doc:coordinator-dispatch + 当前 A2-07 工作区`。进场索引为空，除协调者预登记台账外无工作区差异；本包只处理旧 home Skill compatibility reader、Authority 后 filesystem materialization、旧 writable Store/path/export/tests及动态 slash 的旧测试绑定，没有改 Rubric/Prompt、Memory/OWNWARD、A2-01～A2-06 产品合同或其他领域。
- 唯一事实与旧路退场：`setupAuthorityRuntime` 的 initial/installed 两个 Anchor generation 都不再构造或注入 `SkillStore/getSkillsRoot`；`AnchorSkillGlobalStateAdapter` 只拥有 Authority log、epoch、clock、durable projection和当前内存投影。`#importLegacyCatalog`、`skill-legacy:*`、`intent:skill-materialization`、pending/ack reducer、启动物化重驱与 commit 后 `own/linked/index/usage/archived` 回写全部删除。`<home>/skills` 若存在即为惰性数据：生产零枚举、零读取、零写入、零迁移、零删除；正式外部技能只经 `admit_skill` 的隔离 OS-temp staging进入 CAS/assignment链。
- Correctness 与正式行为：Skill durable projection reducer 升为当前无外部效果的版本，只消费 `intent:skill-authority`；`applyStagedMutation/refreshStagedMutations` 按 durable request identity、mutation digest及可选 target revision核对已经提交的 Authority entry并同步投影，exact replay幂等，异载荷/未提交/错误revision继续 fail closed，不再制造空 materializer或副作用 pending。旧 `store.ts/paths.ts`、四组只证明旧 Store的测试、过期 `SkillRecord/SkillState/SkillDraft` 类型与根导出已删除；`SKILL.md` 常量收回 admission中性边界。管理、save/admit/load/usage、main/work/disabled shadow、builtin、Kernel/Executor投影和 RPC/REPL行为未改；动态 slash 集成测试改为真实 `FileAuthorityCommitLog + FileArtifactStore + AnchorSkillGlobalStateAdapter + SkillCatalogApplicationService → registry/dispatcher`，不再把旧 Store当正式入口。
- 直接证据：core 定向 6 文件 46/46（global adapter、catalog application/admission、builtin/render/admission）通过，覆盖唯一 Authority stream、staged commit/exact replay/异载荷拒绝与正式领域行为；CLI dynamic slash/manager 3 文件 25/25通过，setup-delivery 的旧目录 sentinel 1/1证明真实启动对预置 `skills/own/legacy/SKILL.md` bytes和目录树零触碰，installed generation 复绑定向2/2通过。core与CLI TypeScript typecheck、core build和`pnpm cli:build`通过；core build仅有既有 circular-chunk warning。16个适用文件最窄Biome通过。首次隔离S7命令因漏加仓库要求的`--import=tsx/esm`而无法解析TS源码；补回canonical loader后定向1/1通过，该失败是命令装配错误而非产品反证。
- 结构、导出与残余：canonical `pnpm s7:lint` 一次运行即22/22并通过registry golden；门禁及反向mutation机械拒绝重新出现Store/path文件、Anchor注入、legacy/materialization stream、漏 committed-mutation核对或core根导出。`pnpm runtime:package-exports` 一次通过，确认runtime与declaration根面都不暴露`SkillStore/getSkillsRoot`，catalog subpath继续唯一。静态反扫确认core/CLI production source、core dist与构建入口没有`SkillStore/getSkillsRoot/skill-materialization/skill-materialized/skill-legacy/#importLegacyCatalog/SKILL_PENDING`或Skill home路径调用；没有访问真实home、秘密或外部系统，没有运行根级全量、package check或制品验收。
- A0恢复、状态与失效：A2-07曾精确作废`A0-06a-persistence-roots-v1`的P05、`A0-06b1-persistence-schema-v1`的S01～S04及`A0-10-initial-verification-map-v1`的旧Store/materialization证据；当前表已恢复为“P05仅有admission OS temp、旧home目录不属production exact-set、S01～S04退役且零兼容义务”，A0五类索引仍为`[x]`。若以后重新出现Skill home路径/Store/scanner/import/materializer/pending/ack、Anchor filesystem注入、根导出，或staged apply/refresh、正式动态slash/admission入口变化，立即恢复上述A0子证据、A2及其直接闭包。
- 交接与下一检查点：A2-01～A2-07现在共同证明Skill Catalog拥有管理、保存、接入、读取/usage、Authority事实、表面投影与Kernel/Executor不可变投影，旧写入口、第二事实、兼容reader和物化链均为零，故A2标为`[x]`、完成度为`3/8`。A5的Skill Catalog登记行仍为`[ ]`，因为本文明确要求A3完成其全部Product API binding后才可勾选；当前停在“等待协调者独立复核A2”，不得提前开始A3。

### A3-01：建立 Skill 管理 Product API Catalog / dispatcher 与 RPC 纯绑定

- 派发基线：`HEAD 1387fe3e9109b7966f309b9aa2e9334c5c88438a + task-doc:A3-01-dispatch`。A2-07 已由协调者独立复跑 core 46/46、CLI 49/49、S7 22/22、package exports 与 core/CLI typecheck，并以生产源码反扫确认旧 Store、home 路径、legacy import 和 materialization 链归零；提交 `1387fe3e` 后索引与工作区为空，A2 阶段退出门正式成立。
- 唯一架构结果：只从 Skill Catalog 已证明的管理 Query、Command 与 Fact Event 合同建立静态、传输无关且 fail-closed 的 Product API Catalog / dispatcher；Host 组合一个实例并把现有三个 Skill RPC 改为认证后的 wire 纯绑定。Catalog 不定义 Skill DTO、规则、状态、写入权或 RPC 名，ServerContext 不再持有 `SkillCatalogApplication`，本机直调不经过网络。
- 工作包边界：覆盖现有 `list / set-state / archive / skill-catalog-changed` exact-set、Product API 最小组合机制、Skill 领域贡献、Host 装配、ServerContext 与三个 RPC handler、直接测试和结构门禁。不得接入其他领域，不迁移 CLI client / Surface 订阅，不设计不存在的 Skill Progress Event，不新增 npm 包、动态插件注册、服务定位器、万能协议或第二业务入口。
- 完成与安全交接：领域继续唯一拥有业务合同与应用实现；Catalog 只组合不可变 descriptor 与调用，拒绝重复、unknown、kind mismatch、漏贡献和封存后变更；RPC 方法名、参数、结果、认证、错误和 `skill.changed { structuralVersion }` 行为完全等价；进程内与 RPC 绑定调用同一 dispatcher 且没有网络内绕。若约四小时不能闭合，停在可构建、现有三个 RPC 仍只有一个正式调用主链且无双注册/双入口的检查点，不得扩入下一责任链。
- 领域贡献与组合结果：新增唯一窄入口 `@zhixing/core/product-api`，只公开传输无关 descriptor/contribution/exact-set 与封存 `ProductApiDispatcher`，不经 core 根或 Skill 根 barrel 转导，也不含 Skill DTO、规则、状态、写权或 RPC 名。Skill 领域在既有 `@zhixing/core/skills/catalog` 内唯一贡献 `skill-catalog.query.list`、`skill-catalog.command.set-state`、`skill-catalog.command.archive` 和 Fact `skill-catalog-changed`；没有虚构 Progress Event。dispatcher 构造时一次取得完整 expected set 与全部 contribution，拒绝重复、遗漏、unknown、kind/descriptor/fact-set 不匹配，构造后无注册面且实例/descriptor/集合冻结；进程内测试直接调用同一实例，不开端口、不序列化。
- Host 与纯 binding：Anchor 唯一生产组合根在 `createServerContext` 边界恰一次构造 `ProductApiDispatcher(Skill exact-set, Skill contribution)`，贡献仍只委托既有唯一 `SkillCatalogApplicationService`。`ServerContext`/`CreateContextOptions` 只保留可选 `productApi`，不再导入或暴露 `SkillCatalogApplication`；缺贡献时三个现有 RPC 继续以迁移前 `INTERNAL_ERROR` 文案 fail closed。`skill.list/setState/archive` handler 只保留认证 registry、wire 参数校验、dispatcher 调用、既有 not-found/invalid-params 映射及 Fact 传输；concurrent conflict/普通提交失败仍走迁移前 INTERNAL_ERROR，所有失败零广播，成功命令各广播一次原 `skill.changed { structuralVersion }`。Executor-only 未获得虚假 Skill 贡献，CLI client/REPL/订阅留给下一 A3 责任链，A2 的 save/admit/load/usage、Authority/CAS 与 Kernel/Executor 投影未改。
- 直接与对抗证据：core `catalog-application.test.ts + product-api/catalog.test.ts` 为 2 文件 23/23，覆盖领域应用原合同、进程内 Query/Command/Fact、同一应用恰一次调用、duplicate/missing/unknown/kind/descriptor/fact mismatch 与封存变更；Server `management-methods.test.ts + context.test.ts` 为 2 文件 10/10，覆盖缺贡献、三个真实 RPC、参数、成功、not-found、conflict/commit failure wire、commit 后单次广播和 ServerContext；CLI `startup-server-owner.test.ts` 为 1 文件 5/5，直接反绑 Anchor 生产 root 的单 dispatcher/单 contribution/单 application 装配。合计 5 文件 38/38，不重复相加。
- 结构、导出与构建证据：canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过；结构门禁及反向 mutation 拒绝 `ServerContext.skillCatalog`/handler 直连回流、第二 dispatcher/注册点、领域规则进入 Catalog、根 barrel/第二 export、Fact 传输错序与原 conflict wire 漂移。`pnpm runtime:package-exports` 通过，确认 `./product-api` 是唯一正式入口且 runtime/declaration 不泄入 core 根或 Skill subpath。core/server/CLI typecheck、core/server build 与 `pnpm cli:build` 均通过；8 个适用 TypeScript 文件最窄 Biome 和 3 个脚本 `node --check` 通过。未运行根级 lint/test/build、package check 或制品验收。
- 状态、失效与交接：A3-01 在当前工作区形成可构建、可运行、单一 dispatcher 真相；没有双注册、双应用、兼容壳或未处置本包问题。若 Product API descriptor/catalog、Skill contribution/exact-set、Anchor composition、ServerContext、三个 Skill RPC/Fact、core export/build entry、S7 或 package-export gate任一变化，只恢复 A3-01 及上述直接闭包；A2 未变证据继续复用。A3 与 A5 Skill Catalog 行保持 `[ ]`，当前停在等待协调者独立复核 A3-01，不得由本对话进入下一责任链。

### A3-02：建立 Skill Product API client / Fact Event 订阅与 CLI Surface 纯绑定

- 派发基线：`HEAD b2f7398f47a2e130dae241bf08a869ee1f66f5c4 + task-doc:A3-02-dispatch`。A3-01 已由协调者独立复跑 core 23/23、server 10/10、CLI 5/5、S7 22/22、core/server/CLI typecheck、core build与 package exports，并以生产反扫确认只有 Anchor Host 一处 dispatcher 构造；提交 `b2f7398f` 后索引与工作区为空。
- 唯一架构结果：由 Skill 领域拥有管理 Query/Command/Fact 的稳定 client 合同，由唯一有限 RPC client binding 负责 `skill.list/setState/archive` 与 `skill.changed` 的 wire 编解码；`/skills` 管理器、动态 slash 和 REPL 只消费该 client 合同并负责呈现/刷新，不再知道 RPC 方法、通知名、wire DTO 或临时拼装业务端口。
- 工作包边界：只覆盖当前真实 Skill CLI 消费链 `RpcManagementFacade → repl.ts → SkillManagerController / SkillCommandSource / registry.refresh`、必要的领域 client 合同、有限 RPC binding、直接测试和结构门禁。保持三条 RPC 与一条通知的公开名称、参数、结果、错误、订阅/取消订阅、管理器交互、排序/选中、builtin-first 冲突和动态 slash 刷新行为完全等价。Channel 当前没有 Skill 表面，不得为它建立空入口；不得接入其他领域、设计通用远程协议、迁移 Server dispatcher、增加 Progress Event、重写 TUI 或进入 A4。
- 完成与安全交接：Skill wire 字符串和 wire 解码只存在于一个显式 RPC binding；领域 client 合同不导入 Server/RPC/CLI 类型，不持连接、Surface 状态或业务实现；Surface 不复制 Skill 状态机，只持 UI 选择和命令缓存，Fact 到达后以 Query 重取权威投影。未知/畸形 list 或 changed payload 必须 fail closed，不能用 `as never`、默认 revision 0 或宽松 index signature 掩盖协议漂移；断线重连沿既有 notification forwarding 行为，不新增第二订阅 owner。若约四小时不能闭合，停在可构建、旧 facade 路径仍完整或新 binding 已成为唯一正式入口的检查点，不得留下两套 client 主链或继续下一领域。
- 验证责任：以当前 manager controller/screen、skill command source、REPL/RPC facade 与 connection notification 直接测试为起点，补足同一 client 对管理器与动态 slash 的复用、三个请求的严格 wire 映射、Fact 订阅/退订/畸形拒绝、写后 Query 刷新、builtin 冲突和无 Channel 空入口；同步 S7/package export/build entry 中真正受影响的窄门禁。先运行最窄测试和受影响包 typecheck/build，只重验失效闭包，不重复 A3-01 server dispatcher、A2 领域全链、根级回归或制品验收。
- 状态规则：A3-02 全部闭合前 A3 与 A5 Skill Catalog 行保持 `[ ]`。完成后在本文记录生产端、全部消费端、旧路径删除、直接证据与精确失效条件，并停下等待协调者复核；不得自行开始其他领域或 Git 操作。
- 生产责任收口：`SkillCatalogClient` 由 `@zhixing/core/skills/catalog` 领域入口唯一定义，只暴露 Query/Command/Fact 及不可变管理投影，不依赖 Server/RPC/CLI。`SkillCatalogRpcClient` 与全部严格 codec 由唯一正式 `@zhixing/rpc/skill-catalog-client` subpath 拥有；它只依赖 Skill 领域合同和本文件定义的最小 request/notification transport port，不依赖 Server、CLI 或 WebSocket。binding 严格解码 list 响应、command acknowledgement 和 changed Fact，拒绝未知字段、缺字段、非 canonical 时间/digest、非安全 revision 与畸形数组；三个 request 和一个 notification 的公开 wire 保持不变，RPC 错误原样传播。
- Surface 与旧路退场：`CoreHostRpcLink` 只继续拥有 CLI 连接生命周期，以结构兼容的最薄 transport 实现直接交给 RPC binding；`repl.ts` 仅从 RPC 窄 subpath 构造一个 client 实例，并同时交给 `SkillManagerController` 与 `SkillCommandSource`。管理器写成功后重新 Query，Fact 到达后只驱动 registry refresh，动态 slash 在 refresh 时重取权威投影，不从 Fact 重建事实。builtin-first 冲突、管理投影全集、选中/重排和订阅/退订语义保持。CLI 旧 binding 文件及测试、`RpcManagementFacade.skill*`/`onSkillChanged`、`SkillManagerStore`、`listAll` 回调粘合、`as never` 与宽松 wire DTO 已从生产链删除；`packages/channels/` 反扫无 Skill Surface，未新增空 binding。
- 直接证据：初次 Surface 闭包的领域应用/动态 slash、管理器 query/command/写后重查与严格 binding 共 4 文件 34/34 通过，末次微调的 binding/dynamic-source 2 文件 16/16 复取通过。本次物理归位后，相同严格 codec/三 request/Fact 订阅退订与畸形拒绝的 RPC 责任边界测试 1 文件 9/9 通过；原 manager/dynamic-source 25 项 Surface 证据输入未变。RPC 与 CLI typecheck、RPC build 与 `pnpm cli:build`、`pnpm runtime:package-exports` 均通过。canonical `pnpm s7:lint` 最终 22/22 且 registry golden 通过；反向 mutation 新增识别 CLI binding 回流、RPC 根 barrel 泄漏/第二 export、Server/CLI 反向依赖和 wire exact-set 漂移。S7 归位后首次取证发现 RPC manifest 尚未纳入该检查器的 production record 输入，补齐机械输入后最终全绿，未放宽断言。13 个适用 TS/JSON 文件 Biome、3 个结构/导出脚本 `node --check` 与 `git diff --check` 通过。未运行根级 lint/test/build、package check 或制品验收。
- 协调反证与纠正：首次 A3-02 把完整 Product API RPC binding 放在 `@zhixing/cli`，虽然行为和 codec 成立，但违反 AE-001 的 `@zhixing/rpc = Product API RPC 绑定`、`@zhixing/cli = Surface/进程入口` 依赖方向，因而原物理 owner 结论作废。当前 binding 源码、直接测试、build entry 和 package export 已整体唯一归入 `@zhixing/rpc`；RPC 根导出不泄漏，CLI 无转导文件、镜像 DTO、wire 字符串或第二 binding，既有领域/Surface/公开 wire 行为没有重做或改变。
- 协调者独立验收：在纠正后的同一工作区重跑 RPC binding 9/9、CLI manager/dynamic Surface 25/25，依次完成 core、RPC、CLI 构建与 CLI typecheck；canonical `pnpm s7:lint` 为 22/22 且 registry golden 通过，`pnpm runtime:package-exports` 通过。生产反扫确认 Skill wire 只存在于 RPC binding 与服务端 provider，CLI 没有 wire、镜像 DTO、第二 binding 或根级转导；一个 client 实例同时供两个 Surface 使用，Fact 只触发 Query 重取。协调者接受 A3-02 终态。
- 失效与交接：若 Skill 领域 client Query/Command/Fact 合同、catalog subpath、RPC 窄 binding subpath/transport port/严格 codec、RPC root/export/build entry、CoreHost notification forwarding、REPL 单实例装配、manager/dynamic source、Skill wire exact-set、Channel 排除、S7 或 package-export gate 任一变化，只恢复 A3-02 及上述直接闭包；A3-01/A2 证据继续复用。当前无遗留旧 Skill client 主链或未处置本包问题，A3 与 A5 Skill Catalog 行均为 `[x]`，完成度为 `4/8`；下一检查点为 A4 首条 Kernel 合同责任链。

### A4-01：冻结唯一 Kernel Run Envelope 输入合同及生产调用入口

- 派发基线：`HEAD ceda8f65f767d6874a8c0377f840c82a24a0b048 + task-doc:A4-01-dispatch`。A3-02 已由协调者独立复跑 RPC binding 9/9、CLI Skill Surface 25/25、core/RPC/CLI build、CLI typecheck、S7 22/22 与 package exports，并以生产反扫确认 RPC binding 归属及 CLI 单 client 主链；提交后索引与工作区为空。
- 唯一架构结果：以现有 `AgentRuntime.run` 为唯一 Kernel 执行入口，把其输入收敛为由 `@zhixing/orchestrator/runtime` 拥有的唯一、不可变、显式分区 `Run Envelope` 合同；conversation、ephemeral scheduler 与 durable job 生产路径都只构造这份合同，不再依赖未命名的参数袋或各自复制输入语义。本包只固定输入边界，不重写 Agent Loop。
- 工作包边界：正向覆盖 `createAgentRuntime` 的执行入口，以及 `runtime-host/session-adapter`、`cli/serve/ephemeral-executor`、`cli/serve/agent-job-runtime` 三类直接生产调用；workscene/power 只作为 conversation 装配变体验证同一入口，不建立第二合同。Envelope 必须清楚区分模型输入、调用/运行身份、取消与预算、受控效果/Correctness 端口和事件观察，不让 Conversation、Advancement、Schedule 或 Assignment 规则进入 Kernel；现有必要字段只能作为调用方已经裁决后的值或窄端口进入，不能在 Kernel 重新解释产品状态。
- 明确不做：不在本包迁移 Kernel Event/Terminal、收窄 `AgentRuntime` 其他查询/管理成员、移动 Workscene/Schedule 装配、修改 Product API、领域 owner、公开 RPC/Event、持久协议或产品行为；不新建万能 Context/Capability、可选字段垃圾袋、并行 `runV2`、兼容别名或只包一层的空抽象。旧 `RunParams` 名称、重复类型和调用形态必须随唯一合同落地而退场。
- 完成与证据：类型与生产调用图证明只有一个正式 Envelope owner 和一个 `run` 入口；直接测试分别识别 conversation 的 turn/identity/observer、ephemeral 的单次/取消、durable job 的 assignment Correctness/effect 端口及 workscene/power 复用，不靠纯类型编译自证。保持消息、事件顺序、取消 first-wins、错误、终态、资源计量、schedule mutation、global query 和协议投影完全等价；补充只针对该输入边界的反向结构门禁。按验证手册只跑受影响最窄测试、typecheck/build 与相关 S7/package-export 闭包，不重复 A0～A3 或根级全量。
- 安全交接：若约四小时仍不能闭合，必须停在单一 `run` 入口、无双合同、可构建可运行的状态，记录未迁移的唯一调用链和有效证据；不得顺带进入 Event/Terminal、其他公共面或产品装配迁移。完成后更新本文并停止等待协调者复核，不执行 Git 写操作。

- 实际责任迁移：新增稳定证据 `A4-01-kernel-run-envelope-v1`。`KernelRunEnvelope` 只由 `@zhixing/orchestrator/runtime` 声明并从该窄 subpath 导出；顶层 required exact-set 固定为 `modelInput / identity / control / correctness / observation`，各分区及字段均 readonly，运行入口在首个异步边界前复制并冻结调用方拥有的容器。`AgentRuntime.run` 仍是唯一执行入口，旧 `RunParams` 声明、导出、正向注释和调用形态已归零，没有 `runV2`、兼容别名、镜像输入或第二入口。
- 输入与 owner 边界：`modelInput` 只承载已组装消息；`identity` 只承载 turn、conversation、source、advancement 与 turn context 的已裁决投影；`control` 承载 abort、watchdog 与模型调用资源计量；`correctness` 承载工具权威、受控效果、schedule/assignment/global query、稳定 assignment time 与子资源 reservation 窄端口；`observation` 承载 yield 与 protocol event observer。Conversation/Advancement/Schedule/Assignment 的规则、事实和状态机仍由调用方裁决，Kernel 只消费投影或端口。本轮同时补齐原调用方已提供而 adapter 类型/转发遗漏的 `resourceReservation` 与 durable-job `modelCallResourceMeter`，没有新增产品决定或第二 Correctness owner。
- 生产调用闭包：`runtime-host/session-adapter` 将 conversation 的 messages、turn/conversation identity、first-wins abort、observer 与全部 assignment Correctness 端口构造成同一 Envelope；`ephemeral-executor` 构造单次 turn、abort 与 observation；`agent-job-runtime` 构造 scheduler identity、durable assignment/effect/resource 端口和 observer。`RuntimeHost` 仍只有一个 `createAgentRuntime` 装配点，main/workscene power、ephemeral 与 job 都复用同一 assembly/run 合同；workscene/power 没有第二输入类型或执行主链。消息、事件、错误、终态、取消、资源计量、schedule mutation、global query 与协议投影实现未改变。
- 直接证据与归因：orchestrator Envelope/runtime 直接测试为 2 文件 72/72；CLI 四类 binding 合并证据为 4 文件 41/41，其中首次命令的 session adapter 11 项失败仅因 CLI 测试加载了迁移前的 `runtime-host/dist`，同一命令其余 3 文件与 session 既有 7 项共 30 项通过；按验证手册先完成 `@zhixing/runtime-host` 上游构建，再只重验失败文件为 18/18，故没有用重复全测掩盖失败。新增直接断言分别锁定 conversation 五分区/observer/Correctness、ephemeral 单次与 abort、durable job assignment/effect/resource 端口及不可变 capture；既有 `runtime-host.test.ts` 继续识别 main/workscene power/ephemeral/job 共用装配。
- 结构、导出与构建：S7 新 inspector 从 TypeScript AST 机械冻结唯一 owner、五分区及字段 exact-set、三个直接 production `.run` binding、单一 RuntimeHost assembly 与 `RunParams/runV2` 负向集合；反向 mutation 能识别 optional/字段袋、capture bypass、任一 binding 展平和第二 RuntimeHost factory。canonical `pnpm s7:lint` 初次只暴露 inspector 把 `import type KernelRunEnvelope` 误认声明的门禁实现错误；改为 AST 顶层 interface/type 声明判定后，仅重跑失效 canonical 闭包，23/23 mutation/coverage 与 registry golden 全部通过。`@zhixing/core`、`@zhixing/owner-kernel`、`@zhixing/orchestrator`、`@zhixing/runtime-host` 依赖顺序构建及受影响 typecheck 已通过，最终 `pnpm cli:build`、`pnpm runtime:package-exports`、19 个适用源码/门禁文件 Biome 均通过；未运行根级全量或制品验收。
- 失效与状态：若 `KernelRunEnvelope` owner/export、任一分区/字段、capture、`AgentRuntime.run` 签名、三个直接 binding、RuntimeHost assembly、相关 observation/Correctness 投影、S7 inspector/mutation 或 runtime package export 任一变化，恢复本证据并只重验上述闭包。Kernel Event/Terminal、其他查询/管理成员、Workscene/Schedule 产品装配与 RuntimeExecutionProfile 后续收口均未迁移，因此 A4 仍为 `[ ]`、完成度仍为 `4/8`、最终退出门保持 `[ ]`；下一检查点仍由协调者接受本证据后派发 Kernel Event/Terminal 的单一责任链，不进入 A5。
- 交接类型：完成，等待协调者独立复核；索引为空，未执行 Git 暂存、提交、历史改写或推送。
- 协调者独立验收：在同一未修改工作区重跑 orchestrator Envelope/runtime 2 文件 72/72、CLI conversation/ephemeral/durable-job/RuntimeHost 4 文件 41/41，orchestrator、runtime-host、CLI typecheck 与 canonical S7 23/23 + registry golden 均通过。生产反扫确认 `KernelRunEnvelope` 只有一个声明和窄导出，`AgentRuntime.run` 只有一个入口，三个直接调用方均构造五分区合同，`RuntimeHost` 只有一个 factory 装配点，旧 `RunParams`、`runV2` 和镜像类型归零。协调者接受 `A4-01-kernel-run-envelope-v1`；durable job 的 meter/reservation 变更经 HEAD 对照确认是调用方原已提供而 adapter 丢弃的 Correctness 端口补全，不是新增产品语义。A4 继续为 `[ ]`，下一责任链只处理 Kernel Event，不与 Terminal 合包。

### A4-02：建立唯一 Kernel Run Event 合同及两侧显式投影

- 派发基线：`HEAD 0ba58736dd7b463b04d1359bd2b7b13dee37e117 + task-doc:A4-02-dispatch`。A4-01 已由协调者独立复核并纳入提交；进场索引与工作区为空。
- 唯一架构结果：由 `@zhixing/orchestrator/runtime` 拥有有限、只读且可穷尽的 Kernel Run Event 合同。Agent Loop 内部事件必须在 Kernel 边界显式投影；Conversation、ephemeral 与 durable job 三类生产消费者必须再从 Kernel Event 显式投影到各自既有产品运行/传输合同，不能继续把 core 内部 `AgentYield` 当作 Kernel 公共合同直通。
- 生产闭包：覆盖当前全部真实运行事件变体、顺序、背压和失败传播；`RuntimeHost` 会话适配、ephemeral executor 与 agent job runtime 均消费同一 Kernel Event。现有 SessionRuntime、Owner/RPC/Server/Channel 观察到的事件 exact-set、payload、顺序和错误语义保持不变。既有 `SessionEventProjection/onProtocolEvent` 是独立带外协议投影，本包只在防止其与 Kernel Run Event 形成混合 owner 所需的最窄范围内处理，不扩展为 RPC 迁移。
- 旧路径与门禁：删除 Envelope/AgentRuntime 及三类直接消费者对 core `AgentYield` 的 Kernel 级直通、镜像别名、强制断言和第二事件入口；用穷尽投影和反向结构门禁拒绝漏变体、未知变体、直通回流、第二 owner 与根导出泄漏。不得用同构 type alias 或 `as` 掩盖边界。
- 保护与直接证据：直接证明全部现有事件逐项投影、未知/不完整输入 fail closed、异步回调背压与异常传播、事件顺序，以及 conversation/ephemeral/durable job 的正式行为不变；只运行受影响的 orchestrator、runtime-host、CLI/Owner/RPC 最窄测试、必要 typecheck/build、S7 与 package exports，不重复 A0～A3 或根级回归。
- 明确不做：不定义或迁移 Kernel Terminal，不收窄 `RunResult` 的产品/持久化字段，不迁移 Workscene、Schedule、Conversation 领域，不重做 Product API/RPC binding，不新增事件、产品能力或通用事件框架。
- 完成与止损：只有唯一 Kernel Event owner、Loop → Kernel → 三类产品消费面的两侧投影、全部旧直通退场和直接证据同时成立，工作区可构建且没有双事件真相，才算本包完成；完成后停止等待协调者复核，不进入下一包，不执行 Git 写操作。若约四小时仍未闭合、出现跨领域/RPC 公共协议扩面、两个以上独立未知或无法保持单一事件真相，停在可构建的安全检查点并报告唯一未闭合链。

执行记录（证据 ID：`A4-02-kernel-run-event-v1`）：

- 基线与事实：进场为 `HEAD 0ba58736dd7b463b04d1359bd2b7b13dee37e117 + task-doc:A4-02-dispatch`，索引为空。生产 `AgentYield` exact-set 机械确认为 `text_delta / thinking_block_start / thinking_delta / thinking_block_end / assistant_message / tool_start / tool_end / turn_complete` 八种；唯一 producer 是 `createAgentRuntime` 的 Agent Loop，三个直接 Kernel 消费面是 runtime-host conversation session adapter、CLI ephemeral executor 和 durable agent-job runtime。`SessionEventProjection/onProtocolEvent` 继续是独立带外协议投影，没有并入本合同。
- 责任迁移：新增只从 `@zhixing/orchestrator/runtime` 窄入口公开的 readonly `KernelRunEvent`；orchestrator 根 barrel 不导出该合同或 guard。Loop 边界逐变体校验、重建并冻结新事件，Envelope observation 改为 `onEvent`；三个产品 binding 各自再次校验并以穷尽 switch 重建既有 `AgentYield` 产品流，不共享对象、不用 alias/`as`/原对象直通。未知、缺字段、额外顶层字段和不合法嵌套载荷 fail closed；全部旧 production `observation.onYield` 直通归零。
- 行为保护：Loop 仍 `await` observation callback，因而事件顺序、异步背压与 callback 异常传播不变；conversation queue、ephemeral 文本聚合与回调、durable job yield stream 继续消费字节等价的既有事件。Owner/RPC/Server/Channel 的 downstream 合同、wire、`onProtocolEvent`、RunResult/Terminal、Workscene/Schedule 和任何产品事实均未改变。
- 直接证据：orchestrator `kernel-run-event / kernel-run-envelope / create-agent-runtime` 为 3 文件 74/74，通过八变体新对象/冻结、未知/不完整/扩字段拒绝、顺序/背压/异常传播及 Envelope capture；CLI/runtime-host 三个正式消费面为 3 文件 44/44，通过八变体逐项等价、fresh projection、畸形输入拒绝及既有 conversation/ephemeral/durable 行为。S7 新 owner inspector 与反向 mutation 冻结唯一声明、八变体字段 exact-set、Loop 投影、三消费面穷尽投影、带外协议隔离、窄 subpath 与根泄漏负向边界；canonical `pnpm s7:lint` 为 24/24 且 registry golden 通过。门禁编写中一次局部变量落点错误由最窄 mutation 测试直接发现并修正，不是产品失败。
- 类型、构建与导出：正确使用各 workspace 的 TypeScript 后，orchestrator、runtime-host、CLI 三包 typecheck 均通过；此前一次根级 `pnpm exec tsc` 解析到仓库旧 TypeScript 4.3.4 而无法读取 node24 tsconfig，属于非 canonical 调用归因，未用重复全测掩盖。随后依赖顺序完成 orchestrator build、runtime-host build 与 `pnpm cli:build`；`pnpm runtime:package-exports` 证明 runtime subpath 有 runtime guard/declaration、orchestrator 根无 Kernel Event 泄漏；最窄 Biome、三个门禁脚本 `node --check` 与 `git diff --check` 均通过。
- 失效、遗留与状态：若八变体/字段、Loop producer、Envelope observation、任一三类 projection、协议带外边界、runtime/root exports、S7 inspector/mutation 或对应测试发生变化，恢复本证据并只重验该闭包。Kernel Terminal、`RunResult` 收窄、Workscene/Schedule 装配及其他 AgentRuntime 公共成员均未迁移，因此 A4 与完成度继续为 `[ ]` / `4/8`；下一检查点只能在协调者接受本证据后单独处理 Kernel Terminal。交接类型为完成、等待协调者独立复核；索引为空，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：在同一未修改工作区反查 `KernelRunEvent` 声明、Loop producer、Envelope observation、三类生产消费面、root/runtime export 与旧 `observation.onYield` 残余；确认八变体 owner 唯一、两侧均为逐变体 fresh-value 投影、带外协议投影未混入、宽根未泄漏且旧直通为零。独立重跑 orchestrator/CLI 6 个直接文件 118/118、orchestrator/runtime-host/CLI 正式构建、runtime package exports、canonical S7 24/24 与 registry golden，全部通过；接受 `A4-02-kernel-run-event-v1`。A4 继续为 `[ ]`，下一责任链只处理 Kernel Terminal。

### A4-03：建立唯一 Kernel Terminal 与完成结果边界

- 派发基线：`HEAD 4a99495d48a08f193bdb8c4fc978aeedfa180779 + task-doc:A4-03-dispatch`。派发文本中的完整 hash `4a99495d13f47381941030416d35f0fcce0c6172` 不是当前仓库有效对象，短前缀 `4a99495d` 唯一解析到本记录的实际 HEAD；A4-02 已由协调者独立复核并纳入提交，进场索引为空且工作区只有协调者的本文台账差异。
- 唯一架构结果：由 `@zhixing/orchestrator/runtime` 拥有有限、只读、可穷尽的 Kernel Terminal 合同，并让 `AgentRuntime.run` 的完成结果明确区分 Kernel 运行终态与当前 Conversation/持久化消费所需的候选、证据、窗口变化、控制提议和诊断；Kernel 公共签名不再直接返回 core `RunResult/AgentResult` 混合合同。
- 生产闭包：覆盖 `completed / max_turns / aborted / error` 四种当前终态及其全部现有字段；Loop/internal completion 必须显式投影为 Kernel Terminal，runtime-host conversation、ephemeral、durable job 三类直接生产消费者再显式投影到各自既有产品合同或结果。`runRecord`、`windowCompact`、`newMessages`、`durationMs` 与 `pendingPostTurnControl` 必须保持现有含义和消费次序，但不能被 Terminal 冒充产品事实，也不能因拆分丢失、重复计算或变成第二权威。
- 行为保护：保持 completed message、usage、max-turns、abort reason/exit delay、结构化 AgentError、run record、compact、控制提议、诊断、Conversation commit 条件、ephemeral 输出/错误和 durable job summary/status 的现有行为；异常抛出与正常 `error` terminal 继续严格区分。Owner/RPC/Server/Channel 的公开结果和 wire 不得漂移。
- 旧路径与门禁：删除 `AgentRuntime.run`、Envelope 相邻类型和三个直接消费者对 core `RunResult/AgentResult` 的 Kernel 级直通、别名、强制断言、镜像 union 与第二 terminal owner；Kernel 合同仅从 runtime 窄入口公开，宽根不得新增泄漏。结构门禁必须识别漏终态/字段、直通回流、第二 owner、产品事实误并入 Terminal 和三消费面缺少投影。
- 保护与直接证据：逐终态证明 internal → Kernel → conversation/ephemeral/durable job 字段与语义等价，特别覆盖 error class/wire 保真、abort/max-turns、Conversation 仅 completed 提交、持久化失败不改运行终态、窗口/控制提议与诊断不丢失；只运行受影响的 orchestrator、runtime-host、CLI/owner/RPC 最窄测试、必要 typecheck/build、S7 与 package exports，不重复 Event/Envelope 或根级全量验证。
- 明确不做：不迁移 Conversation 领域或其 Authority/commit 状态机，不改变 core 下游公开 `RunResult` 合同，除非删除 Kernel 直通所需的最窄中性调整；不迁移 Workscene/Schedule 装配，不收窄其他 AgentRuntime 查询/管理成员，不建立通用 Outcome 框架，不进入 A5，不新增产品能力。
- 完成与止损：只有唯一四终态 owner、Kernel 完成结果的责任分区、三类产品结果投影、旧混合合同直通退场和直接证据同时成立，工作区可构建且没有双终态/双结果真相，才算本包完成。若约四小时仍未闭合、出现 Conversation 状态机或公开 RPC/wire 扩面、两个以上独立未知、连续两条实质路径失败或无法保持单一终态真相，停在可构建可运行的安全检查点并报告唯一未闭合链；完成后停止等待协调者复核，不执行 Git 写操作。

执行记录（证据 ID：`A4-03-kernel-run-terminal-v1`）：

- 责任迁移：新增 runtime 窄入口唯一拥有的 `KernelTerminal`，四分支 exact-set 为 `completed(message,usage) / max_turns(maxTurns,usage) / aborted(usage,abortReason?,exitDelayMs?) / error(error{name,message,type,recoverable},usage)`；Agent Loop 的 core `AgentResult` 只能经严格、逐分支、fresh-value 投影进入该合同，未知、缺字段或扩字段输入 fail closed。`AgentRuntime.run` 现在只返回浅冻结的 `KernelRunCompletion { terminal, artifacts }`，其中 artifacts exact-set 为 `runRecord / windowCompact? / newMessages / durationMs / pendingPostTurnControl?`，明确只是候选、证据、窗口变化、控制提议和诊断；`runRecord` 内既有运行记录不被提升为第二终态或产品权威。
- 三类产品投影与行为保护：runtime-host conversation adapter、CLI ephemeral executor、durable agent-job runtime 各自逐分支穷尽投影，不共享 core `AgentResult` alias、不用强制断言或原对象直通。Conversation 重新构造既有 core `RunResult`，`error` 分支恢复真实 `AgentError` class 及 `name/message/type/recoverable`，completed-only commit、持久化失败不改已提交运行终态和公开 wire 保持不变；ephemeral 的输出/取消/错误与 durable job 的 status/summary/usage 保持迁移前行为。正常 `error` terminal 与 `AgentRuntime.run` 自身 reject 仍是两条不同终态链。
- 旧路、导出与结构门禁：`AgentRuntime.run`、lifecycle after-run context 和三个直接消费者不再把 core `RunResult/AgentResult` 当作 Kernel 公共合同；新合同只由 `@zhixing/orchestrator/runtime` 窄入口导出，orchestrator 宽根无新增泄漏。S7 owner inspector 与反向 mutation 冻结四终态/五 artifacts、唯一声明与 Loop producer、三消费面逐分支投影、runtime-only export、禁止 `as`/直通、禁止把产品产物并入 Terminal，并机械拒绝恢复 completion 整图深拷贝或消费侧逐消息复制；恢复第二 owner、漏分支、旧混合合同或无依据复制会失败。
- 直接证据：Kernel terminal 单测 1 文件 3/3，覆盖四分支字段、终态必要快照深冻结、调用方后续修改不能破坏 completed message/usage、artifacts 零深拷贝与未知/不完整拒绝；`create-agent-runtime` 初次 71 项中 68 项通过，唯一 3 项失败是迁移后测试仍从错误分区读取 `forceCompact`，修正测试读取后只重跑该失效闭包 3/3，因此当前 71 项均有本轮有效证据且没有重复全文件。conversation/ephemeral/durable 三消费面为 3 文件 53/53，另有 conversation window 1 文件 3/3；Conversation 正常 durable commit 与 transcript projection failure 两项定向产品证据 2/2，证明完成结果投影未改变 commit/commit-failure 终态。canonical `pnpm s7:lint` 为 25/25 且 registry golden 通过。
- 类型、构建与静态边界：orchestrator、runtime-host、CLI 三包最窄 typecheck 均通过；依赖顺序的 orchestrator build、runtime-host build 与 `pnpm cli:build` 均通过；`pnpm runtime:package-exports` 通过并证明 runtime 窄入口存在、宽根无 Terminal/Completion 泄漏；本包路径的最窄 Biome 已通过。没有运行根级 lint/test/build、package check、制品验收或与本包无关的回归。
- 失效、遗留与状态：若四终态/字段、五 artifacts、Loop completion producer、`AgentRuntime.run` 返回合同、lifecycle after-run、任一三类产品投影、Conversation core `RunResult` 重构、runtime/root exports、S7 inspector/mutation 或上述直接测试变化，恢复本证据并只重验对应闭包。Envelope、Event、Conversation/Authority 状态机、Workscene/Schedule 装配和其他 AgentRuntime 查询/管理成员未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，下一责任链仍由协调者从 AgentRuntime 公共查询/管理面、RuntimeHost 产品特有装配或 Kernel Conformance 中选择，不进入 A5；未执行 Git 暂存、取消暂存、提交、历史改写或推送。

- 协调复核纠正（完成结果对象所有权）：独立复核作废初次实现“`createKernelRunCompletion` 对 terminal 与完整 artifacts 执行 `structuredClone + deepFreeze`、Conversation 再逐项深拷贝”的性能结论。生产所有权反查确认 `runRecord`、`newMessages`、`windowCompact` 与 `pendingPostTurnControl` 均由本次 Kernel run 新建并在 generator terminal 后停止写入；最终实现只在 core internal `AgentResult → KernelTerminal` 责任边界对 completed message/usage、abort reason 做一次 fresh clone + freeze，并把 error 复制为无 core class 的只读字段。completion 保留同一 terminal identity，只复制并冻结五引用外壳，绝不遍历或复制 artifacts 对象图。
- 纠正后的产品交付：Conversation 继续创建新的 core `AgentResult` 外壳并仅为旧 `RunResult.newMessages: Message[]` 的可变容器做一次引用级浅拷贝；`runRecord`、消息/工具结果/思考/图片正文、window compact 与控制提议均按单向所有权直接交付，`error` 分支只新建产品侧所需的真实 `AgentError`。ephemeral 不消费 artifacts，durable job 直接消费只读 usage；两者不再为未使用 artifacts 付出复制成本，也不存在同一 Conversation 结果的重复深拷贝。
- 纠正直接证据：更新后的 Kernel 单测 3/3 直接 spy 证明 completion 边界没有调用 `structuredClone`、terminal 与四类大 artifacts 保持正确 identity，同时源 `AgentResult` 的 message/usage 后续修改不能破坏已交付终态。`create-agent-runtime` 只重验对象图隔离和 lifecycle completion 2/2；runtime-host 上游重建后，Conversation 四终态与 artifacts 交付定向证据 6/6，durable job 13/13。第一次 Conversation 定向运行使用了未重建的 runtime-host dist，唯一 1 项 identity 断言失败，按验证手册归因为旧产物后先完成 orchestrator/runtime-host 构建再重取并通过，不作为产品失败。新增 S7 mutation 分别恢复 completion 深拷贝与 Conversation 逐消息 clone，均被门禁拒绝；canonical S7 25/25 与 golden、三包 typecheck、依赖顺序三包 build、runtime package exports 均通过。A4-03 其余架构与行为证据未失效，仍等待协调者独立复核。
- 协调者独立验收：反查完成结果的生产所有权、生命周期观察顺序和三个直接消费面，确认 Kernel 只对跨责任边界的有限终态做必要快照，五项 artifacts 由 run 创建后单向转交，Conversation 只浅拷贝旧合同要求的 `newMessages` 容器，不存在未消费大对象复制或第二结果真相。纠正后的同一工作区独立重跑 Kernel terminal 3/3、runtime 对象图与 lifecycle 2/2、Conversation 四终态及 artifacts 6/6，依次重建 orchestrator 与 runtime-host；canonical S7 25/25、registry golden、fresh dist 上的 runtime package exports 和 `git diff --check` 均通过。协调者接受 `A4-03-kernel-run-terminal-v1`；A4 继续为 `[ ]`，下一责任链只处理安全实现对象退出 AgentRuntime 公共面。

### A4-04：移除 AgentRuntime 返回对象的安全实现泄漏

- 派发基线：`HEAD a4e9ff87ec2d4a14f4f59ae2b4c282543491c806 + task-doc:A4-04-dispatch`；A4-03 已由协调者独立复核并纳入提交，派发前索引和工作区为空。
- 唯一架构结果：`AgentRuntime` 返回对象不再公开 `SecurityPipeline` 或 `IPermissionStore` 实现实例；Kernel 继续在内部唯一拥有安全决定、工具准入、权限读取、确认跟踪与子运行安全链，Host、Conversation 和其他产品调用方只能消费已经存在的有限查询、确认和效果端口。
- 生产闭包：从 `AgentRuntime` 合同、工厂返回对象、runtime/root declarations、RuntimeHost 适配和全部生产消费者双向删除两个实现对象出口；反查 `secureExecutor`、Task 子 Agent、独立 orchestration、`securitySnapshot`、`executionPermissionRules`、confirmation broker 与正常工具调用，确保它们仍由同一个内部 pipeline/store 驱动，不新增第二 pipeline、第二 store、服务定位器或绕过准入的执行路径。
- 保护边界：保持权限规则、builtin rules、trust/workscene context、确认、rate limit、阻断通知、durable permission snapshot、Task/编排子执行及公开 `/security` 行为完全等价；安全拒绝继续 fail closed。把原来直接读取实现对象的测试改为通过真实有限合同或实际受控效果验证，不得增加 test-only getter、类型断言、反射、根导出或兼容别名。
- 旧路径与门禁：删除返回对象上的 `securityPipeline`、`permissionStore` 字段及其公开声明；增加能识别任一字段、实现类型、getter/别名重新进入 `AgentRuntime` 返回面或外部生产消费者直接取得实例的结构门禁。内部闭包、`CreateAgentRuntimeOptions` 的装配输入和 core 安全实现不因本包机械改造；若生产事实证明某个装配输入本身构成 Host 泄漏，只登记为下一条独立责任链，不在本包扩面。
- 直接证据：最窄验证必须同时证明公共类型/产物没有两个实现对象、内部安全链仍能允许与拒绝工具、快照与 durable permission 投影等价、Task/编排不绕过；按依赖顺序构建 orchestrator、runtime-host 及实际受影响下游，运行 S7 和 package exports。不得重跑 Envelope/Event/Terminal 全文件、根级全量回归或与本包无关的安全矩阵。
- 明确不做：不改变权限、确认、沙箱或信任产品语义；不迁移 Trust Administration；不重构 SecurityPipeline/IPermissionStore 内部实现，不处理工作区解析、provider/model、生命周期、压缩、文本调用、其他 AgentRuntime 成员、RuntimeHost 产品特有装配或 Kernel Conformance，不进入 A5。
- 完成与止损：只有两个实现对象从 AgentRuntime 返回公共面和全部外部生产消费面归零、内部唯一安全链及全部直接行为证据成立、工作区可构建且没有临时访问桥，才算完成。预计超过四小时、需要改动安全产品语义或公开协议、出现两个以上独立未知、连续两条实质路径失败或影响扩至 Create options/Trust 领域时，停在原公共合同仍成立或新合同已完整接管的可构建检查点，记录唯一未闭合链并反馈；不执行 Git 写操作。

执行记录（证据 ID：`A4-04-agent-runtime-security-encapsulation-v1`）：

- 责任迁移与生产 exact-set：`AgentRuntime` 接口与 `createAgentRuntime` 返回对象上的 `securityPipeline`、`permissionStore` 两个实现出口已删除；fresh `dist/runtime/index.d.ts` 的同一接口不再出现字段、实现类型、getter 或别名。反向扫描 RuntimeHost、CLI、Server、Owner、RPC 与 Executor production source 没有外部属性读取；RuntimeHost conversation adapter 继续只透传 `securitySnapshot`、`executionPermissionRules`、`confirmationBroker` 与既有受控运行/效果合同。`CreateAgentRuntimeOptions.permissionStore` 作为本包明确排除的装配输入保留，当前 production caller 对它为零注入，因此未发现需要在本包扩面的第二 Host 消费链。
- 内部唯一安全链与行为保护：工厂仍只构造一个 `persistentStore` 和一个 `SecurityPipeline`；同一闭包实例继续注入正常工具 `createSecureExecuteTool`、Task 子 Agent 与 `createAgentNodeExecutorV1` 独立编排，并生成 `/security` 的只读 `securitySnapshot`、耐久 assignment 的 `executionPermissionRules`、confirmation broker、builtin rules、trust/workscene context、rate limit 与阻断通知。测试不再取得实现对象：scene/global 上下文只从有限安全快照观察，宿主秘密路径保护改为实际让模型请求一个 filesystem-read 工具，并直接证明效果函数零调用、`onSecurityBlocked` 收到 bypass-immune rule，安全拒绝保持 fail closed。
- 旧路与机械门禁：S7 新增 AgentRuntime security encapsulation inspector，解析唯一接口 owner 与真实工厂返回对象，拒绝实现字段、实现类型、getter/改名别名、外部 production 属性读取、第二 pipeline/store，以及正常工具、Task 或独立编排脱离同一内部链；五类反向 mutation 均被直接测试识别。runtime package-export gate 在 fresh 声明产物中截取 `AgentRuntime` 本体并拒绝两字段/两实现类型回流；没有 test-only getter、类型断言、反射、兼容字段、根导出或第二访问桥。
- 直接证据：orchestrator runtime 的 trust/context、有限快照与真实受控拒绝 5/5，Task 内部共享安全链 4/4，secure-executor allow/deny 与独立 orchestration 2 文件 35/35；fresh upstream dist 上的 RuntimeHost adapter 有限安全/confirmation 透传 2/2，durable conversation assignment 的 permission/profile 绑定与 readiness fail-closed 1/1，合计 5 个直接文件 47/47。canonical `pnpm s7:lint` 最终 26/26 且 registry golden 通过；首次 canonical 运行只暴露门禁把 Executor 私有派生 type alias 误判为第二公共接口 owner，收窄为真实 `interface AgentRuntime` owner 后同一结构反例完整通过，不是产品失败。
- 类型、构建与静态边界：orchestrator、runtime-host、CLI 三包 typecheck 均通过；按依赖顺序完成 orchestrator build、runtime-host build 与 `pnpm cli:build`；fresh dist 上 `pnpm runtime:package-exports` 与本包两个 TypeScript 文件的最窄 Biome 均通过。没有运行 Envelope/Event/Terminal 全文件、根级测试/构建、package check、制品验收或无关安全矩阵。
- 失效、遗留与状态：若 `AgentRuntime` 接口/工厂返回对象、runtime/root declaration、RuntimeHost adapter、`securitySnapshot`、`executionPermissionRules`、confirmation broker、正常工具/Task/独立编排安全注入、S7 inspector/mutation 或 package-export 声明门变化，恢复本证据并只重验对应闭包。权限/信任产品语义、core 安全实现、`CreateAgentRuntimeOptions`、工作区解析、RuntimeHost 产品装配和 Kernel Conformance 均未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：反查 `AgentRuntime` 唯一声明、工厂返回对象、fresh declaration 与全部外部生产属性访问，确认安全实现实例已退出公共面而内部同一 pipeline/store 仍唯一驱动主工具、Task、独立编排及有限安全投影。独立重跑 trust/context 与真实受控拒绝 5/5、Task/secure executor/agent-node executor 75/75、orchestrator build、canonical S7 26/26、registry golden、fresh runtime package exports 和 `git diff --check`，全部通过；接受 `A4-04-agent-runtime-security-encapsulation-v1`。A4 继续为 `[ ]`，下一责任链不进入 A5。

### A4-05：移除 AgentRuntime 返回面的工作区解析对象

- 派发基线：`HEAD 6d11fc90 + task-doc:A4-05-dispatch`；A4-04 已由协调者独立复核并纳入提交，派发前索引与工作区为空。
- 唯一架构结果：`AgentRuntime` 的正式接口与返回对象不再公开 `resolvedWorkspace`、`workspaceDirStatus` 或等价工作区解析/目录管理对象；Kernel 仍在内部按既有规则获得执行工作区并驱动 prompt、工具、安全与运行合同，宿主需要的默认工作目录由宿主装配边界直接持有，不能通过已创建的 Kernel 实例反向查询。
- 生产闭包：反查两个字段的唯一生产消费者 `command.ts` 中 post-adoption working directory 与 `hostInfo.workspace`，改由同一冻结配置和既有权威解析器形成一个宿主侧默认工作区投影并共同消费；不得复制工作区优先级、路径归一化或目录创建算法。显式 Workscene/Executor assignment 工作区继续由现有 environment/领域输入决定，不能误用宿主默认路径。`workspaceDirStatus` 当前没有生产消费者，退出返回面后不得保留空壳、别名或无意义转发；内部 `ensureWorkspaceDir` 的既有创建/重建副作用必须保持。
- 保护边界：默认配置工作区、cwd fallback、显式 runtime workspace、显式 null、Workscene 路径与无工作区五类语义不变；post-adoption review 与 server host-info 继续得到与本机主运行体相同的默认路径，文件工具、MCP、信任锚、prompt workspace source 和工作目录创建行为不变。不得把 `ResolvedWorkspace`、`WorkspaceDirStatus`、provider config 或目录管理能力重新包装成 AgentRuntime getter/callback/metadata bag，也不得新增第二 resolver、运行期服务定位或根导出。
- 门禁与证据：更新 AgentRuntime 公共面结构门禁和 fresh declaration/package-export 检查，使字段、类型、等价别名或外部生产读取回流均被拒绝；直接测试必须证明两个宿主消费者共用同一宿主投影，并覆盖默认路径、显式 null/Workscene 隔离及目录创建不回退。按依赖顺序只构建和验证 orchestrator、runtime-host、CLI 的失效闭包，运行 canonical S7 与 package exports；不重复安全矩阵、Envelope/Event/Terminal 测试或根级全量门禁。
- 明确不做：不改变工作区产品规则、配置格式、权限/信任、安全或文件工具语义；不处理 AgentRuntime 其他管理成员，不迁移 Workscene/Schedule 产品装配，不建立 Kernel Conformance，不进入 A5。
- 完成与止损：只有两个解析对象从 AgentRuntime source/declaration/return/external consumers 全部归零、宿主两处路径消费统一且行为等价、Kernel 内部工作区执行语义与目录副作用成立、门禁能识别回流并且工作区可构建时才算完成。预计超过四小时、需要改动公开配置/协议/产品语义、出现多个独立 workspace owner 或影响扩至 Workscene/Schedule 领域时，停在原合同仍成立或新合同完整接管的可构建检查点并反馈；不执行 Git 写操作。

执行记录（证据 ID：`A4-05-agent-runtime-workspace-encapsulation-v1`）：

- 基线与责任迁移：进场 HEAD 为 `6d11fc908cb896cafccf20201eab4132baee30c8`，索引为空且工作区只有协调者预登记的本文台账。`AgentRuntime` 唯一 interface、工厂返回对象和 fresh runtime declaration 已删除 `resolvedWorkspace` / `workspaceDirStatus`，相邻 `ResolvedWorkspace` / `WorkspaceDirStatus` 返回类型依赖及无消费者的目录状态变量一并退出；没有 getter、metadata、callback、别名或兼容壳。Kernel 内部仍按 `runtime override > global config > cwd/none` 调用既有 `resolveWorkspace`，显式 null 仍直接形成 `none`，随后无条件执行既有 `ensureWorkspaceDir`，prompt、文件/MCP 工具、安全信任锚和运行输入继续消费同一个内部 workspace。
- 宿主默认投影：Anchor 组合根在冻结的 `startup.config` 上只创建一次 `HostDefaultWorkspaceProjection`；该投影只调用 providers 的权威 `resolveWorkspace(config, { sessionType })`，不复制优先级、绝对路径验证、归一化或目录创建，并把同一解析结果分别投影为 post-adoption review working directory 与 `hostInfo.workspace`。两处旧 `ephemeralRuntime.resolvedWorkspace` 反向读取已删除；CI/无配置时仍保持 review 回退当前 cwd、host info 不发布 workspace。Workscene 的本机 binding 解析、显式 null 与 Executor assignment workspace 仍只经原 `RuntimeHost` 输入进入 Kernel，不消费宿主默认投影。
- 直接证据：orchestrator 工作区封装定向测试 1 文件 2/2，证明默认解析与目录建立仍在内部发生、显式 null 跳过 resolver 但继续完成安全的目录步骤，并且两个旧返回字段均不存在；CLI 宿主投影与 RuntimeHost 隔离测试 2 文件 6/6，证明配置路径同时供两个宿主消费者使用、非交互无 workspace 终态等价、main 显式 null 与 Workscene 路径互不回落。orchestrator、runtime-host、CLI 三包 typecheck 均通过；按依赖顺序完成三包 build（CLI 使用 canonical `pnpm cli:build`）。
- 结构、声明与纠正：新增独立 S7 inspector/mutation 冻结 AgentRuntime 唯一 owner、返回面零 workspace 字段/别名、外部 runtime 属性读取归零、宿主投影唯一权威 resolver 与两消费点共用。第一次 canonical 运行识别出 inspector 把 workbench 自有 `workspaceDirStatus` 和 Mesh environment 的 `resolveWorkspace` 误当 Kernel 泄漏；门禁随后收窄为“runtime receiver 属性读取”和“禁止裸 provider resolver 旁路”，没有放宽生产不变量。最终 canonical `pnpm s7:lint` 为 27/27 且 registry golden 通过；fresh orchestrator declaration 上的 `pnpm runtime:package-exports` 通过，并拒绝旧字段、类型与常见改名 metadata/info 回流。
- 失效、遗留与状态：若 `AgentRuntime` interface/return/declaration、内部 `resolveWorkspace/ensureWorkspaceDir`、Host 默认投影、post-adoption review、`ServerContext.hostInfo.workspace`、RuntimeHost 的 explicit null/Workscene/assignment 分叉、S7 inspector/mutation 或 package-export 声明门变化，恢复本证据并只重验对应闭包。工作区产品规则、配置格式、Workscene/Schedule 领域、权限/信任、安全、文件工具、其他 AgentRuntime 管理成员和 Kernel Conformance 均未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从 AgentRuntime 唯一声明、工厂返回、fresh declaration、Anchor 两个生产消费者和显式 Workscene/null 输入反查，确认工作区解析对象已退出 Kernel 公共面，宿主只在冻结配置上调用一次既有权威 resolver，且未复制目录算法或形成第二运行期解析链。独立重跑 Kernel 内部默认/null 工作区 2/2、宿主投影与 RuntimeHost 隔离 6/6、orchestrator/runtime-host/CLI 依赖顺序构建、canonical S7 27/27、registry golden、fresh runtime package exports 和 `git diff --check`，全部通过；接受 `A4-05-agent-runtime-workspace-encapsulation-v1`。A4 继续为 `[ ]`，下一责任链不进入 A5。

### A4-06：把 TurnContext providers 收口为不可变装配输入

- 派发基线：`HEAD fbb0edc9 + task-doc:A4-06-dispatch`；A4-05 已由协调者独立复核并纳入提交，派发前索引与工作区为空。
- 唯一架构结果：删除 `AgentRuntime.registerTurnContextProvider` 这一运行后宿主管理入口；全部 TurnContext providers 在 Kernel 实例对外可见前作为有限、不可变装配输入一次性注册。RuntimeHost 不再以 `onRuntimeCreated(runtime)` 回调取得完整 Kernel 对象做二次装配。
- 生产闭包：把现有 SchedulerProvider 与 TaskListProvider helper 从“拿 runtime 后调用两次 register”改为“根据既有依赖创建固定有序 provider 集合”；RuntimeHost 资产层持有 provider factory/投影，并在每次 conversation、Workscene 与 ephemeral 实例创建时交给 `createAgentRuntime`。Kernel 继续先注册内建 TimeProvider，再按既有顺序注册 scheduler、task-list，不能漏过任何原生产发放路径。ExecutorRuntimeSubstrate 当前没有这组 Anchor providers，不得因本包错误注入或改变其行为。
- 保护边界：每 turn 的 time/scheduler/task-list 内容、顺序、conversation ALS 绑定、scheduler lazy fallback、ephemeral 无 conversation 时自然省略、窗口与多 run 行为保持等价；provider 构造或注册失败必须在 runtime 发布前 fail fast，不能返回半装配实例。不得留下 post-create hook、setter、动态注册、服务定位器、可变 provider 数组、兼容别名或第二注册路径。
- 门禁与证据：从 AgentRuntime 接口/返回/declaration 删除注册方法，从 RuntimeHost options/生产调用删除 `onRuntimeCreated`，新增结构反例拒绝两者回流、provider 多/少/乱序、任一发放路径漏传或注册发生在 factory 返回后。直接测试覆盖 conversation、Workscene、ephemeral 三条 RuntimeHost 发放路径以及 scheduler/task-list 的真实注入与 ephemeral 降级；按依赖顺序只验证 orchestrator、runtime-host、CLI 的失效闭包、canonical S7 和 package exports。
- 明确不做：不改变 TurnContext 内容或新增 provider，不处理 conversation reset、attention-window、dispose、lifecycle diagnostics 等其他管理成员，不迁移 Schedule/Workscene 产品装配，不建立 Kernel Conformance，不进入 A5。
- 完成与止损：只有运行后注册入口与回调从 source/declaration/return/生产调用全部归零、三条 RuntimeHost 发放路径都在发布前获得同一有序 providers、行为和失败边界等价、门禁能识别回流且工作区可构建时才算完成。预计超过四小时、需要改变 provider 产品语义、出现非 Anchor 的独立动态注册需求或影响扩至 Schedule/Workscene 领域时，停在原合同仍成立或新合同完整接管的可构建检查点并反馈；不执行 Git 写操作。

执行记录（证据 ID：`A4-06-turn-context-provider-assembly-v1`）：

- 基线与责任迁移：进场 HEAD 为 `fbb0edc913f59ea73f33612ea57ee0cbf8775c16`，索引为空且工作区只有协调者预登记的本文台账。`CreateAgentRuntimeOptions` 新增唯一 `readonly turnContextProviders?: readonly TurnContextProvider[]` 装配输入；工厂在任何 runtime 资源装配前浅拷贝捕获、冻结序列并校验 provider shape/id，保留 Kernel 内建 `TimeProvider` 第一位，再按输入顺序一次性注册。`AgentRuntime` interface/返回对象已删除 `registerTurnContextProvider`，调用方后续修改原数组、重复 `time`/id 或非法 provider 均不能产生半装配 runtime。
- 宿主与生产组合：`RuntimeHostOptions` 的唯一资产入口改为 readonly provider factory；每次 conversation、Workscene、ephemeral（以及原本共用同一 `assemble` 的 durable job）发放都先取得完整投影，再调用唯一 `createAgentRuntime`，factory 抛错时 Kernel 工厂尚未开始。Anchor CLI helper 改为每次创建 fresh frozen exact-set `[SchedulerProvider, TaskListProvider]`，保持 scheduler lazy ref 和 task-list ALS closure；旧 `onRuntimeCreated(runtime)`、helper 内 runtime setter、第二注册路径和可变共享数组已归零。ExecutorRuntimeSubstrate 没有获得这组 Anchor providers。
- 行为与直接证据：orchestrator 定向文件 75/75 通过，其中新增证据真实运行 Agent Loop，证明 Time → scheduler → task-list 的请求字节顺序、调用方数组后续变异不影响已交付实例、旧 setter 不在返回面，并证明非法/重复输入在 workspace/Kernel 装配前 fail fast。CLI 两个定向文件合计 16/16，证明 provider exact-set/顺序、fresh frozen sequence、scheduler 动态读取、task-list conversation ALS 隔离/cache miss、ephemeral 无 conversation 自然省略，以及 RuntimeHost 三条指定发放路径在 factory 返回前取得独立 fixed sequence；factory 失败时 `createAgentRuntime` 调用次数为零。
- 结构、声明与构建：新增独立 S7 inspector 与六类反向 mutation，拒绝 AgentRuntime setter/post-create hook 回流、Create options 非 readonly、RuntimeHost 任一发放旁路、CLI provider 多/少/乱序、Anchor 漏装以及 Executor 误注入；canonical `pnpm s7:lint` 为 28/28 且 registry golden 通过。orchestrator、runtime-host、CLI 三包 typecheck 通过；按依赖顺序完成 orchestrator/runtime-host build 与 canonical `pnpm cli:build`。fresh declaration 的首次 package-export 检查准确暴露 checker 没有读取 `runtime-host.d.ts` 的盲点，补入该正式 subpath 后 `pnpm runtime:package-exports` 通过，并同时约束 AgentRuntime 零 setter、Create options readonly 输入和 RuntimeHost 零 post-create hook；最窄 Biome 与 `git diff --check` 通过。
- 失效、遗留与状态：若 `AgentRuntime`/`CreateAgentRuntimeOptions` source 或 declaration、provider capture/校验/Time-first 顺序、RuntimeHost 四条共用 assemble 路径、Anchor helper exact-set/顺序、command factory、Executor substrate、S7 mutation 或 package-export declaration reader任一变化，恢复本证据并只重验上述闭包。TurnContext 内容、conversation reset/attention-window/dispose/lifecycle diagnostics、Schedule/Workscene 产品装配、其他 AgentRuntime 管理成员与 Kernel Conformance 均未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：反查 `AgentRuntime`/Create options、provider capture 时序、RuntimeHost 四条共用 `assemble` 发放路径、Anchor provider exact-set 与 Executor 隔离，确认运行后 setter/post-create hook 已归零，Time → Scheduler → TaskList 在实例发布前冻结且失败 fail fast。独立重跑 immutable assembly 2/2、RuntimeHost/CLI providers 16/16、orchestrator/runtime-host/CLI 依赖顺序构建、canonical S7 28/28、registry golden、fresh runtime package exports 和 `git diff --check`，全部通过；接受 `A4-06-turn-context-provider-assembly-v1`。A4 继续为 `[ ]`，下一责任链不进入 A5。

### A4-07：把 Workscene 产品运行投影移出 RuntimeHost

- 派发基线：`HEAD 72a1e668 + task-doc:A4-07-dispatch`；A4-06 已由协调者独立复核并纳入提交，派发前索引与工作区为空。
- 唯一架构结果：`RuntimeHost` 不再直接认识 `WorksceneDto`、`powerProfile`、`WorksceneToolDirectory`、Workscene spec 或 `createWorksceneRuntime` 产品入口；现有 Workscene 产品组合责任者在 runtime 发放前一次性生成 profile、workspace、scene identity 与专用工具等完整不可变投影，`RuntimeHost` 只通过一个通用 conversation runtime 装配合同消费。
- 生产闭包：沿 `command.ts` 的 main/Workscene conversation factory、scene lookup 与 workspace 解析三条分支，把当前 `createWorksceneRuntime` 内的全部产品决定移到现有 Workscene 组合边界；main、scene 有 workspace、scene 无 workspace、scene 在首次读取后消失/更新的现有分支都必须保持。`BuiltinExtraToolsAssembly`、capability catalog 与 RuntimeHost 私有 `assemble` 中凡只为 Workscene 选择 profile/identity/spec/工具的判断均退出 Host 主体；允许相关实现暂留当前包内等待 A5 物理归位，但生产调用必须由产品责任者显式形成有限投影，不能以回调服务定位器、任意 metadata bag 或改名后的 Workscene DTO 继续注入 Host。
- 保护边界：保持 power role/profile、scene name/id prompt、显式 workspace/null、scene trust/permission/lifecycle identity、enter/exit/change/list/rename/set/clear 工具 exact-set、main 与 Workscene conversation 路由、capability catalog、MCP/Task/Schedule 基础工具及 Executor substrate 行为等价。投影构造或校验失败必须发生在 runtime 发布前；不得创建第二 profile/tool assembler、复制 Workscene 状态机或让 Kernel/Host 查询 Workscene owner。
- 旧路与门禁：删除 RuntimeHost 对 Workscene 合同/port/profile 的 import、字段、方法、分支和声明泄漏；增加能识别 `createWorksceneRuntime`/等价入口回流、Host 导入 Workscene 产品类型、Host 内按 scene 选择 profile/spec/tools、产品投影绕过唯一 conversation 发放合同以及 capability catalog 与真实投影漂移的结构反例。门禁必须绑定生产调用图和 fresh declaration，不能只搜文件名。
- 直接证据：最窄测试至少覆盖 main、带 workspace Workscene、无 workspace Workscene、scene 重读/消失、Workscene 工具 exact-set、capability catalog 和投影失败不发布 runtime；反查 Kernel 收到的 profile/workspace/identity/tools 与迁移前一致。按依赖顺序只验证实际受影响的 runtime-host、CLI 与必要上游，运行 canonical S7、package exports 与最窄格式检查；不得重复 A4-01～A4-06 全文件、根级回归或制品验收。
- 明确不做：不迁移 Workscene 事实 owner、Reducer、Authority、Product API、存储或物理包，不改变任何 Workscene 产品语义；不处理 Schedule/Task 的产品装配、job/ephemeral 模型覆盖、AgentRuntime 其他成员或 Kernel Conformance，不进入 A5。
- 完成与止损：只有 Workscene 产品知识从 RuntimeHost source/declaration/生产分支归零、唯一产品责任者形成完整冻结投影、所有原发放与 capability 路径行为等价、旧入口删除且门禁可识别回流时才算完成。预计超过四小时、需要改变 Workscene 产品合同/公开协议/事实权威、出现两个以上独立未知、影响扩到 Schedule/Task owner 或无法在通用 conversation 发放单一真相成立处安全交接时，立即停在可构建检查点记录唯一未闭合链并反馈；不执行 Git 写操作。

执行记录（证据 ID：`A4-07-workscene-runtime-projection-v1`）：

- 基线与责任迁移：进场 HEAD 为 `72a1e668a4e9490fc2ff4a1537b686f3b9130907`，索引为空且工作区只有协调者预登记的本文台账。新增有限、冻结的 `ConversationRuntimeProjection`，只携带已裁决的 workspace、primary role、profile、runtime identity 与 product tools；`RuntimeHost.createConversationRuntime` 只校验并消费该通用投影。`RuntimeHost` source/declaration 已删除 `WorksceneDto`、`powerProfile`、`WorksceneToolDirectory`、Workscene spec/directory、`createWorksceneRuntime` 与 capability catalog 产品入口，`BuiltinExtraToolsAssembly` 也不再按 Workscene 选择工具。
- 唯一产品组合与生产行为：Anchor 组合根只创建一个 `WorksceneRuntimeProjectionAssembly` 与一个 conversation runtime factory；前者唯一形成 main/power profile、scene identity、显式 workspace/null 和七项 Workscene 专用工具，后者沿 main、environment 已裁决 workspace、scene 无 workspace、scene workspace 二次解析/准备四条既有路径后才调用同一个 RuntimeHost 发放入口。首次读取后 scene 消失或 workspace 无法重解时不发布 runtime；scene 更新 workspace 时采用当前重读根而不复制解析规则。readiness 与 scheduler capability catalog 也从同一投影 owner 加上原 MCP/Task/Schedule 基础 assembly 机械取得，Executor substrate 未注入 Anchor Workscene 投影。
- 所有权与旧路退场：Kernel 工厂输入将 `KernelRuntimeIdentityContribution` 作为已裁决的有限身份消费，旧 `worksceneIdentity` 镜像字段和 RuntimeHost 内的 profile/spec/tool 分支归零；产品工具只能在投影构造边界产生，Host 不查询 Workscene owner、路径或产品 DTO。投影只捕获/冻结 profile 数组、工具和外层对象；身份由 Kernel 窄装配入口以 exact `sceneId`、私有 provenance 和冻结对象创建/校验，RuntimeHost 只调用 Kernel 断言并透传，不解释 identity schema。非法、伪造或可变输入在 `createAgentRuntime` 前 fail closed；没有 callback 服务定位、任意 metadata bag、第二 renderer 或第二 conversation 发放入口。
- 直接证据：CLI 定向闭包最终为 4 文件 26/26，其中 Workscene 产品投影 7/7 覆盖 main、带/不带 workspace scene、scene 初读消失/重读更新、工具 exact-set、capability catalog、prompt/identity 与构造失败零发放；RuntimeHost/base tools/Executor job 其余 19/19 覆盖通用投影、显式 null/undefined、provider 与 Executor 行为。orchestrator 的 runtime identity 定向用例 1/1 通过（同文件其余 74 项未运行）。canonical S7 首次准确暴露旧 inspector 仍要求 RuntimeHost 自持 power 分支的陈旧假设；修正为绑定唯一产品投影生产图后，定向反向 mutation 2/2、最终 `pnpm s7:lint` 29/29 且 registry golden 通过，无 golden 漂移。
- 构建、exports 与状态：orchestrator、runtime-host、CLI typecheck 通过；按依赖顺序完成 orchestrator/runtime-host build 与 `pnpm cli:build`；`pnpm runtime:package-exports` 证明通用 projection 只由正式窄 subpath 导出、runtime-host 根运行时/声明无该合同转导、fresh RuntimeHost declaration 零 `sceneId`/Workscene identity schema、旧 identity 退场，最窄 Biome 与 `git diff --check` 通过。若通用 projection 字段/冻结校验、Kernel identity constructor/assertion/provenance、RuntimeHost conversation 发放或根/subpath export、Workscene product assembly/factory、scene 二次读取与 workspace 准备、七项工具、capability catalog、Executor 隔离、S7 mutation 或 package-export declaration 任一变化，恢复本证据并只重验上述闭包。Workscene 事实 owner/Reducer/Authority/Product API/存储与物理包、Schedule/Task 产品装配、其他 AgentRuntime 成员和 Kernel Conformance 未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，不进入 A5，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

纠正记录（`A4-07-workscene-runtime-projection-v1`，root export / identity ownership）：

- 独立反证与恢复：协调者确认初次实现仍由 `packages/runtime-host/src/index.ts` 根转导 projection，并在 RuntimeHost validator 内直接读取 `runtimeIdentity.sceneId`；这两项初次“根旁路归零/Host 零身份解释”结论失效。本纠正删除根转导，保留 `./conversation-runtime-projection` 为唯一正式导出；新增 Kernel-owned `KernelRuntimeIdentityContribution`，只允许 runtime 窄入口 constructor 生成 exact、冻结且带私有 provenance 的单字段贡献。Workscene 产品组合与 Executor assignment 适配在边界创建贡献，RuntimeHost projection/validator 只调用 Kernel assertion、保留同一对象并透传，source/declaration 对 `sceneId` 和 Workscene identity schema 零认知。
- 直接与对抗证据：RuntimeHost projection 定向文件最终 6/6，新增伪造但同 shape 的冻结身份必须在 runtime 发布前拒绝，并证明合法贡献以同一对象零解释透传；Kernel identity 定向 2/2（同文件 76 项中 74 项未运行）证明合法身份继续驱动 work 技能、scene trust/permission/lifecycle，缺 provenance 输入在 workspace/provider/runtime 装配前 fail closed。CLI 首次两轮分别因 orchestrator、runtime-host 旧 dist 尚未按依赖构建而出现 2 项、1 项失败；依次完成上游构建后只重跑该文件即 6/6，未重复 Workscene 路由、七项工具或 capability catalog 既有闭包。
- 门禁、exports 与状态：S7 Workscene inspector 新增 runtime-host 根回流、Host/projection `sceneId`、Kernel exact/provenance/冻结、产品绕过 constructor 的反向 mutation；定向 1/1 通过，canonical coverage/mutation 链全部通过并进入 golden，随后 golden check 通过且无漂移。fresh `pnpm runtime:package-exports` 首次准确暴露门禁把根声明为既有 RuntimeHost 参数保留的非导出 type-only side-effect import误判成 public export；收窄为动态根成员与根 exported symbol 后通过，并继续要求窄 subpath 两函数、Kernel runtime-only constructor/assertion、Create options nominal contribution 及 RuntimeHost declaration 零 `sceneId`。orchestrator/runtime-host typecheck 与 build、CLI typecheck/`pnpm cli:build`、最窄 Biome 和 `git diff --check` 通过；本纠正未改变原 27 项 Workscene 路由/工具/capability 证据的输入。A4 仍为 `[ ]`，不进入 Schedule、A5 或其他责任链，当前等待协调者独立复核。
- 协调者独立验收：从 Workscene 组合根、唯一 conversation factory、通用 RuntimeHost 发放、Kernel identity constructor/assertion、fresh declarations 与根/窄 subpath 双向反查，确认 Workscene 的 profile/workspace/identity/七项工具及 capability 投影只在产品组合边界形成，Host 不查询产品 owner、不解释 `sceneId` 且不存在旧入口或根转导。独立重跑 Workscene/RuntimeHost/base tools/Executor job 4 文件 27/27、Kernel 身份正常与伪造拒绝 2/2、canonical S7 29/29、registry golden、orchestrator/runtime-host/CLI 依赖顺序构建、fresh package exports 和 `git diff --check`，全部通过；接受 `A4-07-workscene-runtime-projection-v1`。A4 继续为 `[ ]`，下一责任链不进入 A5。

### A4-08：把 Schedule、Task 与 MCP 产品工具装配移出 RuntimeHost

- 派发基线：`HEAD b624916d + task-doc:A4-08-dispatch`；A4-07 已由协调者独立复核并纳入提交，派发前索引与工作区为空。
- 唯一架构结果：Anchor 当前唯一产品组合边界在 runtime 发放前生成有限、冻结且通用的工具与 MCP 投影；`RuntimeHost` 不再导入、持有或调用 `BuiltinExtraToolsAssembly`、`SchedulerFacade`、`ExecutionSchedulerFacade`、Task 服务或 MCP hub，只验证并消费已经裁决的通用运行输入。Workscene 工具与 Schedule、Task、MCP 基础工具必须由同一产品装配事实组合，不得形成第二工具目录、callback 服务定位或可变工具袋。
- 生产闭包：把 `ExecutionSchedulerFacade` 的实例化、`BuiltinExtraToolsAssembly.assembleTools()` 与 MCP server catalog 读取迁到现有 Anchor 产品组合责任者；main、Workscene、ephemeral 与 durable job 均从该单一装配事实取得与迁移前全等的工具/MCP 输入。job 的 requested-tools unknown 拒绝、exact allowlist、模型覆盖与容量选择仍保持既有语义；capability catalog 必须由同一产品装配事实机械得到。Executor assignment runtime 继续使用既有独立 assignment-scoped MCP 投影，不得注入 Anchor 的 Schedule、Task 或 Workscene 工具。
- 保护边界与旧路退场：保持 schedule mutation 的 staged/replay/operation identity、TaskList conversation/ALS/cache、MCP catalog/call、惰性 scheduler readiness、main/workscene/ephemeral/job 工具 exact-set、确认、资源和异常终态不变。RuntimeHost 源码、声明和构造参数中的上述产品实现类型、getter 与装配调用全部归零；不得以泛化 service locator、provider callback bag、兼容字段或根导出保留旧路径。发布前投影构造失败必须 fail closed，不能发放半装配 runtime。
- 直接证据：用最窄直接测试覆盖四类发放的工具/MCP exact-set、schedule staged mutation、TaskList conversation 隔离、MCP catalog/call、job allowlist/unknown 拒绝与 capability catalog，并反查 RuntimeHost 和 fresh declaration 的产品类型/实现零认知、Executor 隔离及单一产品装配入口。只运行受影响包的直接测试、依赖顺序构建、canonical S7、package exports 与最窄格式检查；不得重复 A4-01～A4-07 全文件、根级回归或制品验收。
- 明确不做：不迁移 Schedule/Task 的事实 owner、Reducer、Authority、Product API 或存储，不重构 MCP 基础设施、job 协议或 Executor 拓扑，不进入 Kernel Conformance、A5 或 A6，不新增能力。
- 完成与止损：四类运行形态保持可构建、可运行和工具/MCP 行为全等，产品装配只有一个事实源，RuntimeHost 只剩通用运行装配且旧路径为零，受影响证据恢复后完成。若约四小时仍不能闭合，或发现需要迁移领域事实、改变 job/Executor 协议、形成第二 owner 或扩大到独立验证闭包，停在无双轨、可发布的安全检查点并反馈，不继续扩面。

执行记录（证据 ID：`A4-08-anchor-runtime-tool-projection-v1`）：

- 基线与责任迁移：进场 HEAD 为 `b624916dc4fa3ed2181dec38dcd085b65097b2e6`，索引为空且工作区只有协调者预登记的本文台账。新增有限、冻结、fail-closed 的 `RuntimeToolProjection { extraTools, executionMcpServers }`，仅由 runtime-host 正式窄 subpath 公开；`AnchorRuntimeProjectionAssembly` 在 runtime 发放前实例化唯一 `ExecutionSchedulerFacade`，调用现有 `BuiltinExtraToolsAssembly` 并读取 MCP catalog，再把 Schedule、Task、MCP 与 Workscene 专用工具组合成 main、Workscene、ephemeral、durable job 四类投影。`RuntimeHost` 只校验并透传该投影，source、constructor options 与 fresh declaration 中的 `BuiltinExtraToolsAssembly`、`SchedulerFacade`、`ExecutionSchedulerFacade`、Task service、MCP hub/getter/catalog/assembler 依赖均归零。
- 四类行为与唯一事实：main 与 Workscene conversation 继续由同一场景路由形成 profile/workspace/Kernel identity，基础 Schedule/Task/MCP 与七项 Workscene 工具在一个投影工厂内组合；ephemeral 在 MCP connect 后才取得同一基础投影；durable job 在产品边界先以 main profile 加同一工具目录执行 unknown 拒绝与 exact allowlist，再把冻结后的 profile/tools/MCP 交给 Host。模型覆盖和 scheduler/orchestration capacity 仍由既有通用 job runtime 装配处理。capability catalog 从同一个 main/scene 投影的 enabled tools、extra tools 与 MCP server projection 机械取得，没有第二目录或重复选择规则；Executor substrate 继续只读 assignment-scoped MCP catalog，不导入 Anchor projection，也未获得 Schedule、Task 或 Workscene 工具。
- 正确性与旧路退场：staged schedule adapter 从 runtime-host 物理归位到 Anchor CLI 产品组合边界，`runContextStorage.stageScheduleMutation` 的 operation identity、read-own-writes、direct fallback 与 replay 语义未改；TaskList service 的 conversation ALS/cache、MCP catalog/call、scheduler lazy getter、确认/资源/错误边界继续由原实现承担。旧 `RuntimeHost` 内 `assembleTools`、MCP catalog、job 工具选择与 `executionScheduler` 字段全部删除，旧 runtime-host scheduler adapter source/test 同步退场；投影拒绝可变、重复或非法 tool/server 项，产品构造或 allowlist 失败发生在 `createAgentRuntime` 前，不存在兼容字段、callback bag、根导出、第二 assembler 或半装配 runtime。
- 直接证据：CLI 最窄闭包 5 文件 32/32，其中 Anchor 四形态与 capability/unknown allowlist 8/8、通用 RuntimeHost 与 job model/capacity/非法投影拒绝 8/8、Schedule staged/direct 2/2、TaskList/MCP/lazy scheduler 7/7、Executor assignment MCP/unknown 隔离 7/7。测试直接覆盖 main/Workscene/ephemeral/job 工具与 MCP exact-set、显式 workspace/null、投影失败零发布、重复/扩展投影 fail-closed、schedule staged mutation、TaskList ALS、MCP mapping/call、job allowlist/unknown、模型覆盖、容量绑定与 Executor 零 Anchor 注入；上游 source 最后一次变化后首次下游运行准确表现为旧 runtime-host `dist` 的三项旧行为，按验证手册先重建上游后只重跑失效的 RuntimeHost 文件 8/8，其余四文件 24/24 继续有效。未重复 A4-01～A4-07 的 Envelope/Event/Terminal、安全、工作区或完整 Workscene 路由闭包。
- 结构、声明与构建：S7 扩展现有产品投影 inspector，绑定唯一 Anchor factory、四类 production issuance、同一 capability source、RuntimeHost 产品实现零认知、staged scheduler 产品归属、旧 source 退场与 Executor 隔离；反向 mutation 可识别 Host 重新取得 MCP/assembler、投影漏校验、ephemeral 绕过、capability 旁路和 Executor 注入。首次 canonical 运行准确暴露 inspector 仍要求旧 capability 调用文本，修正为当前单一投影事实后，最终 `pnpm s7:lint` 为 29/29 且 registry golden 通过。runtime-host final build、CLI typecheck 与 `pnpm cli:build` 通过；fresh declaration 上 `pnpm runtime:package-exports` 通过，证明通用 projection 只从窄 subpath 可达且 Host declaration 不泄漏产品实现；最窄 Biome 与 `git diff --check` 通过。
- 失效、遗留与状态：若 `RuntimeToolProjection` factory/assertion/窄 export、Anchor projection factory、Schedule/Task/MCP assembler、job allowlist、四类 command issuance、capability catalog、RuntimeHost source/declaration、Executor assignment MCP、S7 mutation 或 package-export declaration reader任一变化，恢复本证据并只重验对应闭包。Schedule/Task 事实 owner、Reducer、Authority、Product API、存储，MCP 基础设施、job/Executor 协议、Kernel Conformance、A5/A6 均未迁移；A4 与完成度继续为 `[ ]` / `4/8`。交接类型为完成、等待协调者独立复核；无本包内遗留，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从 Anchor 组合根、main/Workscene/ephemeral/durable-job 四类发放、RuntimeHost generic issuance、fresh declaration、capability catalog 与 Executor assignment runtime 双向反查，确认 Schedule、Task、MCP 与 Workscene 工具只由唯一产品投影装配，Host 不再导入或解释产品实现，job allowlist/model/capacity 与 Executor 隔离无漂移。独立重跑五个直接文件 32/32、canonical S7 29/29、runtime-host build、CLI typecheck/正式构建、fresh package exports 和 `git diff --check`，全部通过；接受 `A4-08-anchor-runtime-tool-projection-v1`。A4 继续为 `[ ]`，下一责任链只闭合 Kernel Conformance 与其余 AgentRuntime 公共面，不进入 A5。

### A4-09：闭合 Kernel Conformance 与 AgentRuntime 公共 API 预算

- 派发基线：`HEAD ff834cbe + task-doc:A4-09-dispatch`；A4-08 已由协调者独立复核并纳入提交，派发前索引与工作区为空。
- 唯一架构结果：建立一套直接消费 `KernelRunEnvelope / KernelRunEvent / KernelRunCompletion` 的可执行 Kernel Conformance，并由同一套合同证明 conversation、scheduled ephemeral、本机 durable job 与远端 Executor assignment 四种现有生产运行形态行为一致；同时把 `AgentRuntime` 正式公共面冻结为当前确有消费者的有限执行、查询和生命周期端口，使更换 Kernel 实现不需要产品领域、Surface、Host 或拓扑了解其内部对象。
- 公共面闭包：从 `AgentRuntime` 唯一声明和全部生产消费者反查每个成员的当前必要性、只读性与责任归属；保留现有能力所需的有限 run/auxiliary execution、用量/安全/运行查询、确认与生命周期端口，不为接口整齐改写产品行为。删除没有生产消费者、只允许实例发布后向 Kernel 注入宿主管理对象的入口及其无效状态链；不得把成员换成 metadata bag、服务定位器、宽 callback、第二 runtime 类型或根 barrel。查询只能返回有限不可变值，不能泄漏 Provider、Security、Permission、Workspace、Tool 或 lifecycle 实现实例。
- Conformance 闭包：复用已经冻结的 Envelope/Event/Terminal owner，不另建平行运行协议。共享测试合同必须对四种生产发放/适配路径逐一证明输入捕获、事件顺序与隔离、成功/失败/取消终态、同一完成结果语义、确认身份、实例隔离和 dispose/资源释放；远端形态必须经过现有 assignment/Executor 适配而非把本地工厂改名，产品投影/协议 wrapper 与 Kernel 合同各自有识别力。现有 `runtime-factory-conformance` 若属于上层 session/executor adapter，只能明确复用或保持分层，不得冒充 Kernel Conformance、复制一套近似合同或建设通用测试框架。
- 旧路、门禁与证据：S7 与 fresh declaration/package-export 门禁机械冻结 Kernel 公共成员 exact-set、runtime-only 窄导出、四类生产 binding、唯一 Envelope/Event/Terminal owner，并拒绝安全/权限/工作区实现回流、未声明成员、第二入口、产品或拓扑类型导入。最窄直接测试必须能分别推翻四种形态漏绑、事件/终态漂移、取消或清理缺失；只运行受影响的 orchestrator、runtime-host、CLI、Executor/test-utils 直接闭包、依赖顺序构建、canonical S7、package exports 与格式检查，不重复根级回归或 A4-01～A4-08 已未失效的场景测试。
- 明确不做：不迁移 Conversation/Schedule/Workscene/Task 领域事实或 Product API，不重构模型循环、安全/权限/确认、上下文压缩、编排、Executor 协议或远端拓扑，不新增运行形态、产品能力、benchmark 或通用诊断框架，不进入 A5/A6/A7。
- 完成与止损：四种生产形态在同一有限 Kernel 合同下通过有识别力的共享 Conformance，AgentRuntime 公共成员均有真实消费者和有限值边界，旧后装管理入口及平行合同归零，受影响证据恢复后完成。若预计或实际超过四小时、需要改变产品语义/公开协议、跨入两个独立公共责任，或无法让远端适配在不重构拓扑的前提下进入共享合同，停在可构建、原合同仍完整或新合同已唯一接管的安全检查点反馈，不继续扩面。
- 基线与即时裁决：进场为 `HEAD ff834cbe7b02f20a816e504c8adb38d60d306431 + task-doc:A4-09-dispatch`，索引为空。生产反查把 `AgentRuntime` 收敛为 16 项 exact-set：唯一执行 `run`；辅助执行 `forceCompact / callText / callTextWithUsage / runOrchestrationV1`；有限查询 `estimateConversationRequestBudget / estimateMessagesTokens / subAgentUsages / securitySnapshot / executionPermissionRules / executionProfile / calibrationFactor / drainLifecycleDiagnostics`；确认与生命周期 `confirmationBroker / dispose / onAttentionWindowChange`。这些成员分别由 conversation Owner/视角控制、Schedule/Advancement、Assignment setup-delivery、权限与 `/security` 投影、确认 Hub、attention-window 和 runtime 终止链直接消费；返回值均为既有有限值或窄端口，不暴露 Provider、SecurityPipeline、PermissionStore、workspace、Tool 或 lifecycle 实现实例。
- 公共 API 与旧链退场：`providerId / model` 没有生产消费者，只剩旧测试自证，已从接口、工厂返回和相应测试观察面删除，primary-role 行为改由真实 provider 调用证明。`registerConversationStateReset / resetConversationState` 只有测试注册、生产 registry 永远为空；现已连同 `Resettable`、聚合错误、Owner `SessionRuntime` 可选 no-op、clear 调用和 core 根转导一起退场，没有实例发布后的宿主管理注入或无效状态链。`AgentRuntime / createAgentRuntime / KernelRunEnvelope` 也从 `@zhixing/orchestrator` 宽根退出，只保留 `@zhixing/orchestrator/runtime` 窄正式入口；fresh declaration 与 runtime package-export gate 固定 16 项 exact-set，并继续拒绝 Event/Terminal/identity 的根泄漏。
- 共享 Conformance 与分层：新增一套直接引用现有 `KernelRunEnvelope / KernelRunEvent / KernelRunCompletion / KernelTerminal` 的参数化 Kernel Conformance，同一组四项行为分别驱动 conversation 的 `createOwnerRuntimeAdapter`、scheduled ephemeral 的 `runEphemeralTurn`、本机 durable job 的 `createAgentJobRuntimePort`，以及真实 `createExecutorRole → createInProcessAssignmentRuntimeFactory → createAssignmentRuntimeAdapter` 远端 assignment 路径；逐形态证明实例/确认 broker 隔离、Envelope identity/Correctness 捕获、Event 顺序、completed/reject/cancel-first-wins Terminal 和恰一次 dispose。既有 `@zhixing/test-utils/runtime-factory-conformance` 明确保留为上层 `SessionRuntime/RuntimeFactory` 投影合同；首次定向运行准确暴露其 fixture 仍伪造 A4 前的参数袋与 core `RunResult`，已改为由真实 Envelope 产生 Kernel Completion 后再验证 SessionRuntime 投影，未复制或冒充 Kernel Conformance。
- 生命周期与行为保护：四类 adapter 的产品投影、公开协议、错误/取消、确认 identity、Assignment/Executor 拓扑均未改变。生产反查发现 Anchor 的 shared scheduled ephemeral runtime 原先没有 dispose owner；现由既有 `AssemblyLifecycleContributions` 在取得后的第一个安全点登记唯一 `ephemeralRuntime.dispose`，同一幂等 handle 同时覆盖后续启动失败和正常 runtime-stage LIFO 关闭。canonical registry golden 只新增该真实 `anchor-host/runtime` cleanup descriptor 在四种拓扑投影及 shutdown 映射，没有其他 registry 漂移。
- 直接证据：Kernel Conformance 为 1 文件 16/16；CLI conformance/assembly/session adapter/window 为 4 文件 50/50；Executor 上层 runtime-factory conformance 为 1 文件 5/5；orchestrator primary-role 最窄闭包 3/3，Server clear 最窄闭包 1/1。canonical `pnpm s7:lint` 为 30/30 且更新后的 registry golden 通过，反向 mutation 可识别公共成员增删、根导出、任一四形态漏绑、远端改成本地 adapter、取消/清理证据缺失、无效 reset 链回流和 ephemeral owner 遗失；`pnpm runtime:package-exports` 在 fresh declaration 上通过。
- 构建、失效与状态：依赖顺序完成 core、tools-builtin、owner-kernel、orchestrator、runtime-host、executor、server 与 CLI 构建，CLI `tsc --noEmit`、最窄 Biome 和 `git diff --check` 通过；core 只出现既有 Rollup 循环 re-export 警告且 DTS 成功。若 AgentRuntime 16 项声明/返回值、runtime/root export、任一四形态 adapter、Envelope/Event/Terminal owner、SessionRuntime 上层投影、ephemeral lifecycle contribution、Conformance/S7 mutation 或 fresh declaration gate变化，恢复本证据并只重验对应闭包。A4 继续为 `[ ]`、完成度仍为 `4/8`；下一检查点只由协调者按 A4 退出门冷启动反查决定，未进入 A5/A6/A7，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从四类真实生产 adapter、AgentRuntime 唯一声明与全部消费者、`/clear` 和 TaskList 产品清理链、Anchor ephemeral 生命周期、窄导出及反向 mutation 双向复核，确认四种运行形态共用同一 Envelope/Event/Terminal 合同，16 项公共面均有当前消费者，退役 reset 链从未承载生产状态且删除不改变清空语义。独立重跑 CLI 四文件 55/55、orchestrator primary-role 3/3、Executor 5/5、Server clear 1/1、canonical S7 30/30、fresh package exports 与 `git diff --check`，全部通过；接受 `A4-09-kernel-conformance-agent-runtime-budget-v1`。A4 仍须按退出门冷启动反查 `@zhixing/runtime-host` 的产品领域依赖与公开残余，未据本包提前勾选。

### A4-10：清除 runtime-host 的产品领域实现与公开残余

- 派发基线：`HEAD f9d86268 + task-doc:A4-10-dispatch`；A4-09 已由协调者独立复核并提交，派发前索引与工作区为空。
- 唯一架构结果：`@zhixing/runtime-host` 只承载通用 Runtime Assembly、冻结运行投影和 Kernel/Conversation adapter，不再实现、导出或依赖 Workscene、Schedule、TaskList、MCP catalog 或 segment 产品装配；这些既有实现原样归回当前 Anchor 产品组合边界，A5 再按领域最终归位。
- 生产闭包：迁出 `builtin-extra-tools`、`segment-deps`、`workmode-tools` 与 `workscene-port` 的实现、构建入口、package exports、根转导和依赖；同步改绑 `command`、Workscene directory/runtime projection 及全部直接测试。生产消费端只改导入和唯一装配落点，不改变工具 exact-set、Profile、Schedule/TaskList/MCP/Workscene 行为、segment persistence/overlay、Conversation clear、Executor assignment 或四类 runtime 发放。
- 边界与旧路：不得复制文件后保留 runtime-host 兼容 subpath、root alias、deprecated wrapper、type-only 回流或双实现；runtime-host manifest 不再因这些产品实现依赖 `@zhixing/mcp`、`@zhixing/tools-builtin`，且其生产源码不得出现 Workscene/Schedule/TaskList/MCP 产品装配。CLI 中的暂居实现必须是当前唯一生产实现，不预建 A5 领域包或 Product API。
- 证据：补充可反向变异的 S7/package-export 门禁，证明四个旧 subpath、根转导、构建入口和错误依赖均归零，并能拒绝任一产品实现回流。运行迁移文件的直接测试、RuntimeHost/四形态 Conformance、受影响 CLI 组合根测试、runtime-host 与 CLI 的依赖顺序构建、fresh package exports、canonical S7、最窄格式与 `git diff --check`；不重复根级或未失效的 A4 回归。
- 明确不做：不改变领域事实、Reducer、Authority、Product API、公开协议、用户行为或物理拓扑，不重构产品实现内部逻辑，不进入 A5/A6/A7，不顺手移动其他 CLI 暂居实现。
- 完成与止损：只有 runtime-host 源码、root/subpath、build、manifest 和 fresh declaration 均只剩通用运行责任，全部生产消费者改绑唯一 Anchor 落点且行为证据通过，才算完成。若移动暴露真实跨领域语义变化、需要重设计产品合同、超过四小时仍不能保持单一实现或出现两个可独立失败的未知，停在可构建且无双实现的安全检查点反馈；不得扩成 A5 领域迁移。
- 基线与迁移结果：进场为 `HEAD f9d86268a3fe21133136ec38b015b3c37a3d2c0d + task-doc:A4-10-dispatch`，索引为空且只有协调台账差异。`builtin-extra-tools / segment-deps / workmode-tools / workscene-port` 四个既有实现原样移至 `packages/cli/src/serve/` 私有 Anchor 组合落点；`command`、Workscene directory/runtime projection 与三个迁移文件直接测试全部改用相对私有入口。没有新增 A5 领域包、公共 CLI subpath、兼容转导、第二实现或产品状态机。
- package 与依赖闭包：`@zhixing/runtime-host` 根转导、四个 package exports、四个 tsup entry、源码文件以及 `@zhixing/mcp / @zhixing/tools-builtin` 两条仅为产品装配存在的生产依赖已删除，lock importer 同步收敛；fresh build 只生成 `index / runtime-host / session-adapter / conversation-runtime-projection` 四个通用入口。包描述同步回到通用 AgentRuntime assembly 与 Kernel/Conversation adapter；现有 frozen `RuntimeToolProjection`、`segmentDeps` Kernel 输入和 assignment-scoped Executor 投影未被误删或改义。
- 生产行为与所有权：唯一 Anchor `createBuiltinExtraToolsAssembly` 仍组合 Schedule、TaskList 与 MCP catalog，唯一 `createAnchorRuntimeProjectionAssembly` 仍在发放前合并 main/Workscene/ephemeral/job 工具投影，segment persistence/overlay 与 TaskList reader 仍由同一 `runContextStorage` Correctness 输入驱动。工具/Profile exact-set、Workscene 七项工具、schedule replay identity、Conversation clear cache、job allowlist/model/capacity、Executor assignment 隔离和四类 runtime 发放均只发生导入归属变化，没有公开协议或产品结果变化。
- 结构与反向证据：S7 现在从 CLI 私有 owner 机械重取 extra-tool descriptor，同时要求四个私有实现存在、runtime-host 四个旧源码路径/根转导/tsup entry/产品实现 import 不存在；manifest mutation 拒绝旧 subpath 与 `mcp/tools-builtin` 依赖，consumer mutation 拒绝旧 runtime-host subpath 回流。fresh package-export gate 还直接检查 manifest、root/build、source 与清理后的 dist，任一旧 JS/DTS 文件、第二 export 或依赖恢复都会失败。
- 直接验证与状态：迁移文件、Workscene projection、RuntimeHost 与四形态 Kernel Conformance 共 6 文件 56/56；canonical `pnpm s7:lint` 为 30/30 且 registry golden 无漂移；`runtime-host` fresh build、CLI typecheck、`pnpm cli:build`、fresh `pnpm runtime:package-exports`、最窄 Biome 和 `git diff --check` 均通过。若四个实现/消费者、runtime-host source/manifest/exports/build/declarations、S7 mutation 或 package-export 输入变化，只恢复 A4-10 对应闭包；A4 与完成度继续为 `[ ]` / `4/8`，下一步只允许协调者独立复核与 A4 退出门冷启动反查，未进入 A5/A6/A7，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收与 A4 退出门：从 CLI/Anchor 生产组合根、四个迁移实现、全部直接消费者、RuntimeHost manifest/source/root/subpath/build/fresh declaration 和 Kernel 四形态反向核对，确认产品装配只有一个 Anchor 私有落点，RuntimeHost 只剩通用 assembly、冻结投影与 Kernel/Conversation adapter；没有兼容转导、第二实现、产品状态机或产品依赖残余。独立重跑六个直接文件 56/56、canonical S7 30/30、registry golden、fresh package exports 与 `git diff --check`，并用静态反向搜索确认旧生产 subpath 和管理入口归零。结合 A4-01～A4-09 当前未失效的 Envelope、Event、Terminal、封装、不可变装配、产品投影和 Conformance 证据，冷启动复核未发现未处置反证；接受 `A4-10-runtime-host-product-assembly-exit-v1`，关闭 A4，完成度更新为 `5/8`。

### A5-01：归位 Delivery uncertain-resolution 应用决定与 Product API binding

- 派发基线：`HEAD ca8608b6 + task-doc:A5-01-dispatch`；A4 已由协调者独立复核并提交，派发前索引与工作区为空。
- 唯一架构结果：`delivery.resolve` 只调用 Delivery 领域拥有的 uncertain-resolution 应用命令并经 Product API dispatch；Server/RPC 只保留认证、参数解析、稳定 principal 取得、调用和错误/结果映射，`ServerContext.runtimeControl.resolveDelivery` 与 CLI 同名闭包旧旁路彻底退场。
- 生产闭包：从当前 `RPC handler → ServerContext.runtimeControl → CLI closure → DeliveryStack.resolve → applyDeliveryResolutionControl → DeliveryAuthority` 正反追踪，在现有 Delivery 窄边界形成不可变 command descriptor、应用入口和 Product API contribution，并与已存在的 Skill contribution 组合成一个 sealed dispatcher。保持 control admission、authority coordinate/commit、epoch/open-fact 校验、三种 decision、幂等 replay、durable principal、post-commit status notice 和 wire 结果完全等价；Product API 不复制 Delivery reducer、authority 或通知事实。
- 旧路与门禁：删除 runtime-control 类型字段、Anchor closure、handler 直调及其测试自证；禁止兼容字段、别名、第二 dispatcher、第二 application 或 Product API 与旧回调双写。结构证据必须识别 `delivery.resolve` 绕过 Product API、旧字段回流、缺贡献/重复贡献、descriptor 漂移和 Server 取得 Delivery 实现对象。
- 明确不做：不迁移 enqueue/participant、outbox/receipt、sender/transport、accepted-work lifecycle、Channel、Schedule、Advancement 或整个 Delivery 领域；不改变 RPC 方法名、参数、认证、错误、通知与公开行为，不拆新包或预建通用领域框架。
- 直接证据：最窄覆盖真实应用的 applied/replayed/rejected、stale epoch/open-fact mismatch、observer failure 不回滚 durable fact；真实 RPC dispatcher 的合法调用、参数/身份拒绝、缺少贡献 fail closed、wire 等价；Anchor 唯一 Catalog composition、operation exact-set 和旧 runtimeControl 归零。只运行受影响的 core/owner-kernel/server/CLI 直接测试、必要依赖顺序构建、canonical S7、fresh package exports 与最窄格式检查，不重复 A4、根级回归或制品验收。
- 完成与止损：只有 uncertain-resolution command 的合同、应用决定、权威提交、Product API 和 RPC binding 形成一条唯一生产链，旧旁路归零且直接证据成立，才算完成。预计超过四小时、需要改变 Delivery/Conversation 产品语义、出现两个可独立失败的未知或无法保持单一写入口时，停在可构建、可运行、无双链的检查点反馈；不得顺带迁移其余 Delivery 责任。
- 实施结果：新增唯一窄入口 `@zhixing/core/delivery/application`，由 Delivery 拥有 readonly `DeliveryUncertainResolutionCommand`、三种 decision、耐久 outcome、单一 `DeliveryUncertainResolutionApplicationService`、`delivery.command.resolve-uncertain` descriptor 与无 Product API Fact 的 contribution；该入口有独立 package export/build entry，未进入 core 根或既有 `delivery` barrel。Product API 的 `supports(descriptor)` 只暴露封存 Catalog 的有限能力存在性，使未装配 Delivery 的 Host 保持原 `delivery resolution is not available` fail-closed wire，不暴露应用或实现对象。
- 领域决定、Correctness 与权威链：`DeliveryUncertainResolutionApplicationService` 在冻结 command 后唯一调用 `decideDeliveryResolution`，把三种 decision、epoch/open-fact fence 与 principal 归于 Delivery 应用决定；`@zhixing/owner-kernel/delivery` 的 `createDeliveryResolutionCorrectnessPort` 只提供当前 projection/transaction time/anchor epoch 的有限决定上下文、从 command 构造控制 envelope，并复用原 `ControlAdmissionJournal.applyAuthority → DeliveryAuthority.coordinate → delivery/control 同提交`，不再调用或复制领域决定。applied 后仍异步发送原 `DeliveryStatusNotice`，observer 同步/异步失败不回滚 durable fact。`setupDelivery` 只把该端口注入领域应用并暴露应用合同，不再暴露 `resolve` 写入口；request identity、principal、applied/replayed/rejected、commit LSN 与响应丢失语义未改。
- Product API 与 RPC binding：Anchor 仍只构造一个 `ProductApiDispatcher`，Skill contribution 恒定在场，Delivery stack 在场时把 Delivery exact-set/contribution 一次性并入同一 sealed Catalog；未装配时 Catalog 不宣称该能力。`delivery.resolve` handler 只做既有严格参数解析、认证 surface principal 与 durable principal 取得、descriptor 支持检查、command dispatch 和 result 返回；`ServerContext.runtimeControl.resolveDelivery`、`command.ts` 同名 closure、`DeliveryStack.resolve`、`applyDeliveryResolutionControl` 及对应旧测试主链全部归零，公开 RPC 名、参数、认证、错误和结果形状不变。
- 结构与反向证据：S7 与 fresh package-export gate 冻结 Delivery application 唯一窄 subpath、descriptor/contribution exact-set、一个 Host dispatcher、Skill + 条件 Delivery 组合、owner-kernel Correctness 端口和 handler dispatch；反向 mutation 可识别旧 runtimeControl 字段/handler 旁路、Delivery contribution 丢失或改接 Skill、application 根/barrel 泄漏、重复 export 与旧 apply/resolve owner 回流。Product API constructor 原有 duplicate/missing/unknown/kind/descriptor/fact fail-closed 继续覆盖缺失或重复 contribution。
- 直接验证与状态：当前基线的领域/Product API、耐久 Delivery control、真实 RPC dispatcher/wire 与 Anchor/Delivery 组合共 6 文件 81/81；其中 applied/replayed、stale epoch、open-fact mismatch、durable request binding、orphan source、observer failure、缺 contribution、参数/身份拒绝和公开 result 均有直接反例。core、owner-kernel、server 与 CLI 依赖顺序构建通过，CLI 包内 typecheck、canonical `pnpm s7:lint`、fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 通过。A5 与 Delivery 行继续为 `[ ]`；下一步只允许协调者独立复核本包，接受后沿同一 Delivery 责任链迁移义务产生、终态与 Channel effect，未进入其他领域或 A6/A7，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从唯一 RPC 方法、ServerContext、Anchor Catalog 组合、DeliveryStack 应用发放、Correctness 端口、ControlAdmission 与 DeliveryAuthority 同提交反向核对，确认领域决定、应用入口、耐久机制和 Surface binding 各自只有一个 owner；旧 `runtimeControl.resolveDelivery`、`DeliveryStack.resolve` 与 `applyDeliveryResolutionControl` 生产入口归零，未形成第二 dispatcher、第二决定或通知双写。独立重跑六个直接文件 81/81、canonical S7 30/30、registry golden、fresh package exports 与 `git diff --check`，静态反查确认旧符号仅剩负向门禁。接受 `A5-01-delivery-uncertain-resolution-product-api-v1`；Delivery 其余义务、终态、效果与生命周期尚未迁移，因此 Delivery 行和 A5 继续为 `[ ]`。

### A5-02：归位 Delivery 交付义务产生与唯一入队决定边界

- 派发基线：`HEAD b9e6a30d + task-doc:A5-02-dispatch`；A5-01 已由协调者独立复核并提交，派发前索引与工作区为空。
- 唯一架构结果：Delivery 领域拥有“结果需要被交付”的通用义务合同、入队规则与唯一决定入口；Conversation、Job、Scheduler 等 producer 只把各自已提交或同提交事实映射为 Delivery 义务并消费领域结果，不再直接调用或拥有 `DeliveryAuthority.prepareEnqueues` 及其唯一性、生命周期和状态修订规则。
- 生产闭包：正反追踪 `OwnerDeliveryParticipant` 的 Conversation final/staged/status/control-response、Job result/staged/status 与 Scheduler notice 全部生产入口，在现有 Delivery 窄边界形成可同步参加来源 Authority 同一事务的应用入口；保持来源事实 companion、staged revision/conflict、route/content 校验、生命周期 admission、幂等 key、commit time、max-attempts、enqueue record 与恢复投影完全等价。Correctness/Authority 只提供投影、fence、同提交写入和恢复机制，producer adapter 只拥有来源领域到通用义务的映射，二者均不得复制 Delivery 入队决定。
- 旧路与门禁：生产代码中 `DeliveryAuthority.prepareEnqueues` 只能由新的 Delivery 应用/Correctness 边界消费；`OwnerDeliveryParticipant` 不得持有或取得 DeliveryAuthority 实现对象，不得保留兼容回调、第二 prepare、双写或旁路。结构证据必须能识别 producer 直调 Authority、领域决定回流 owner-kernel、重复应用入口、根/barrel 泄漏及任一真实 producer 未改绑。
- 明确不做：不迁移 pipeline/outbox/receipt 的 attempt、retry、uncertain/terminal reducer，不改 Channel sender/transport，不迁移 accepted-work lifecycle、Schedule/Advancement 领域或 A6 边缘；不把 producer 的来源事实语义强塞进 Delivery，不改变公开协议、通知、内容、路由、优先级、错误或持久格式。
- 直接证据：覆盖全部现有 producer 的 accepted/rejected/duplicate/conflict、来源 companion、staged revision、无耐久 route、生命周期 admission 与恢复后唯一性；反向证明 producer adapter 无 Authority 实现依赖且旧直调归零。只运行 Delivery application/authority、owner-kernel participant 与受影响 Conversation/Job/Scheduler 直接测试、必要依赖顺序构建、canonical S7、fresh package exports、最窄格式及 `git diff --check`，不重复 A5-01、根级回归或制品验收。
- 完成与止损：只有全部现有 producer 经同一 Delivery 领域入队决定形成原子且行为等价的唯一生产链，旧直调归零、无双权威且直接证据成立，才算完成。预计超过四小时、需要改变来源领域或 Delivery 产品语义、出现两个可独立失败的未知、无法保持同事务 companion，或影响面扩到终态/Channel 时，停在可构建、可运行、单一真相成立的检查点反馈；不得顺带完成整个 Delivery。
- 执行基线与边界：`HEAD b9e6a30d4d0e2a34c4ef40f08c97b4dafd1ee9a6 + task-doc:A5-02-dispatch + A5-02-delivery-obligation-application-v1`；只改变义务产生与入队决定 owner，没有进入 attempt/retry/uncertain/terminal、outbox/receipt、Channel sender、accepted-work 或其他领域。
- 领域与 Correctness 归位：`@zhixing/core/delivery/application` 新增有限 `DeliveryObligation` 与唯一 `DeliveryObligationApplicationService`；领域在一次决定中拥有 route/content、幂等 key、生命周期 binding、当前投影冲突及默认 `maxAttempts`，并产生与既有格式相同的 enqueue records/items。`DeliveryObligationCorrectnessPort` 只提供串行 coordinate、当前 projection/fence 和同提交 append 所需的 Authority 机制；`DeliveryAuthority.prepareEnqueues` 不再内置领域规则，而只把投影与生命周期 binding 交给领域决定。
- Producer 与旧路清理：`OwnerDeliveryParticipant` 只持有领域应用，保留 Conversation final/staged/status/control-response、Job result/staged/status 与 Scheduler notice 八个来源映射、staged revision/conflict 和来源 companion 校验；它不再取得 `DeliveryAuthority`、不再拥有 retry policy，也不再直接调用 `prepareEnqueues`。两个 Anchor generation 均从 `@zhixing/owner-kernel/delivery` 窄入口创建同一 participant；生产 `.prepareEnqueues` 唯一直接 consumer 是该窄 Correctness adapter，owner-kernel 根不转导新工厂，没有兼容回调、第二 prepare 或双写。
- 行为与恢复保护：来源事实与 Delivery companion 仍由同一 Authority transaction 原子提交；无耐久 route 继续形成 staged conflict 而不拒绝来源提交，duplicate/conflict、生命周期 admission、commit time、staged revision、持久 record body、cold replay、recovery projection 与历史 `maxAttempts` 校验均保持。Scheduler missed-summary restart 去重、Conversation/Job commit 与 assignment ledger 响应丢失重放继续命中同一 Delivery unique index；公开协议、通知、文案、route、priority 与错误未改变。
- 直接证据：领域 application/Authority 2 文件 46/46，owner participant/Scheduler notice 2 文件 14/14，Conversation/Job 真实 assignment ledger 2 文件 442/442，Anchor Delivery 组合与恢复 1 文件 3/3，合计 7 文件 505/505。依赖顺序的 core、owner-kernel、executor 与 CLI 构建通过，CLI `tsc --noEmit`、canonical `pnpm s7:lint` 30/30 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 和 `git diff --check` 通过；S7 反向 mutation 可识别 participant 直调 Authority、root barrel 回流和 producer 漏改绑。
- 交接与失效：A5-02 完成，等待协调者独立复核；A5 与 Delivery 行继续为 `[ ]`。Delivery application/Authority enqueue 签名、participant 八类来源映射、Conversation/Job/Scheduler producer、Anchor participant 组合、owner-kernel delivery subpath、S7/exports 或相关持久/恢复合同变化时，本记录失效并只重验上述闭包。下一检查点由协调者沿 Delivery 的 attempt/retry/uncertain/terminal 与 Channel effect 剩余责任选择最窄工作包，不得把本包扩大为整个 Delivery 迁移。
- 协调者独立验收：从 Delivery application、Authority projection/fence、owner-kernel Correctness adapter、八条 producer 映射、两个 Anchor generation 与真实 Conversation/Job/Scheduler 消费端正反核对，确认领域拥有 retry/route/content/unique-index 入队决定，Correctness 只提供串行、投影、生命周期 binding 与同提交机制，participant 不再持有 Authority 或第二决定。独立完成 core/owner-kernel fresh build、Delivery application/participant/Scheduler/真实 CLI 组合 21/21、CLI typecheck、canonical S7 30/30、registry golden、fresh package exports 与 `git diff --check`；结合本包同基线 505/505 生产闭包证据，未发现旧直调、双写、持久格式或恢复语义漂移。接受 `A5-02-delivery-obligation-application-v1`；Delivery 的 attempt/retry/terminal 与 Channel effect 尚未迁移，因此 Delivery 行和 A5 继续为 `[ ]`。

### A5-03：归位 Delivery 尝试、重试与终态决定边界

- 派发基线：`HEAD c8220440 + task-doc:A5-03-dispatch`；A5-02 已由协调者独立复核并提交，派发前索引与工作区为空。
- 唯一架构结果：Delivery 领域应用拥有从 ready item 到 attempt claim、unknown/uncertain、永久 preflight failure、sent/retry/failed 终态的全部状态决定；Authority/Correctness 只提供串行投影事务、时间与 anchor fence，现有 pipeline 只驱动领域应用并执行 transport/content/event 效果，不再直接拥有或调用 Authority 业务决定。
- 生产闭包：正反追踪 `AuthorityDeliveryPipeline → DeliveryAuthority.claim/recordPreflightFailure/recordOutcome` 及 queue/recovery 的真实链，把 outcome policy、attempt authorization、response binding、receipt、automatic-attempt budget、backoff、unknown redrive/manual-resolution、status revision/notice 和冷启动重放规则归入唯一 Delivery 应用边界。保持 send 前耐久 claim、响应丢失、幂等 redrive、stale/mismatched outcome 拒绝、retry deadline、uncertain 人工裁决衔接和所有事件投影完全等价。
- 旧路与门禁：pipeline 不得持有或取得用于 claim/outcome 决定的 DeliveryAuthority 实现对象；`claim`、`recordPreflightFailure`、`recordOutcome` 的生产直接 consumer 只能是窄 Correctness adapter，Authority 不得继续内置相同状态分支。禁止兼容回调、第二 lifecycle application、双写、旧方法旁路或仅改名；结构证据必须能识别任一决定回流 Authority/pipeline、应用重复、根/barrel 泄漏和生产 consumer 漏改绑。
- 明确不做：不改变 transport resolve/readiness/send、content materialization、event bus、Channel sender/adapter、pipeline Host lifecycle、accepted-work transfer 或其他领域；不改变 outcome policy、重试次数/延迟、持久 record、公开 RPC/Event、用户文案和 uncertain-resolution A5-01 合同，不执行 A6 边缘收束。
- 直接证据：覆盖 queued/retry-wait claim、attempting restart 的 idempotent redrive 与 manual uncertain、缺 transport/policy skip、永久 content failure、sent receipt、retryable/non-retryable failure、attempt budget、binding/epoch/revision fence、response loss、cold replay、notice/event exact-set；反向证明 pipeline 无 Authority 决定直调且旧分支归零。只运行 Delivery application/authority/pipeline/queue/integration 的直接测试、必要依赖顺序构建、canonical S7、fresh package exports、最窄格式和 `git diff --check`，不重复 producer、根级回归或制品验收。
- 完成与止损：只有 attempt、retry、unknown 与 terminal 规则由一个 Delivery 应用入口闭合，Authority/pipeline 各自只剩机制和效果职责，旧决定路径归零且现有行为证据通过，才算完成。预计超过四小时、出现两个可独立失败的未知、必须改变产品/持久语义、无法保持 send 前 claim 或响应丢失终态，或影响面扩到 Channel/accepted-work 时，停在可构建、可运行、单一真相成立的安全检查点反馈；不得顺带完成整个 Delivery。
- 执行基线与边界：`HEAD c82204409138e5b71e8e049f9b4db4151518420f + task-doc:A5-03-dispatch + A5-03-delivery-lifecycle-application-v1`；只迁移 ready item 到 attempt/unknown/retry/terminal 的决定 owner，没有改变 producer、transport/content/event effect、Channel、accepted-work lifecycle、持久 record、公开 RPC/Event 或 A5-01 uncertain-resolution Product API。
- 领域决定与 Correctness：`@zhixing/core/delivery/application` 新增唯一 `DeliveryLifecycleApplicationService`，集中 queued/retry-wait admission、automatic/manual authorization、unknown redrive/manual uncertain、response binding、永久 preflight failure、receipt、attempt budget、指数 backoff、sent/retry/failed、status revision 与 notice 结果；共用的 authorization/failure/unknown policy 位于 Delivery 私有 `lifecycle-policy.ts`，Authority reducer只复用其规则校验持久记录，不再复制应用分支。`DeliveryAuthority` 的 `claim/recordPreflightFailure/recordOutcome` 业务入口和对应公共 barrel types 已删除，只保留 `transactDeliveryLifecycle` 串行机制，向窄 Correctness adapter 提供克隆投影、authority transaction time、anchor epoch、record 校验与单次耐久 append。
- Pipeline、投影与旧路清理：`@zhixing/owner-kernel/delivery` 的 `createOwnerDeliveryLifecycleBinding` 是唯一生产 Correctness 适配，分别发放领域 application 与只读 projection；`setupDelivery` 只在唯一 Anchor 组合根构造该 binding。`AuthorityDeliveryPipeline` 只调用 application 并执行 transport resolve/readiness/send、content materialization 和 event emit，queue/stats/accepted-work 只消费 projection port；pipeline 已不持有 `DeliveryAuthority`，retry delay 也从 effect-driver config 收回领域应用。`AuthorityDeliveryQueue` 改为只读 projection source，旧 Authority 直接决定、兼容 callback、第二 application、双写和根/barrel 回流均归零。
- 行为与恢复保护：send 前 `attempt-started` 仍先耐久提交；transport response loss 仍不伪造 outcome，冷启动由相同 attempt policy 决定 idempotent redrive 或 durable uncertain。缺 transport/outcome policy 继续 skip，永久 content failure 仍原子写 attempt+failed，retry deadline 仍取 Authority commit time，manual risk-ack 与 automatic budget 独立，stale attempt、错误 binding、late uncertain outcome 和 epoch/revision fence 均 fail closed；sent receipt、retry/failed/uncertain notice 与事件 exact-set、Host lifecycle 和恢复投影未改。
- 直接证据与门禁：Delivery application/Authority、pipeline、queue/outbox integration 共 5 文件 89/89，owner-kernel 真实 Correctness/control 1 文件 7/7，CLI 真实 setup 1 文件 3/3，合计 7 文件 99/99；覆盖 queued/retry claim、manual/idempotent unknown、preflight permanent failure、receipt、retryable/non-retryable、budget、binding/epoch/revision、response loss、cold replay、notice/event。core、owner-kernel、CLI 依赖顺序 build 与 CLI `tsc --noEmit` 通过；canonical `pnpm s7:lint` 为 30/30 且 registry golden 通过，fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 通过。S7/exports 反向 mutation 能识别 pipeline 恢复 Authority 决定、Authority 恢复 claim/outcome 分支、Correctness adapter 漏绑、私有 policy 漂移、application 根/barrel 泄漏和第二 export。
- 交接与失效：A5-03 实施完成，等待协调者独立复核；A5 与 Delivery 行继续为 `[ ]`。Delivery lifecycle application/policy、Authority transaction/reducer、owner-kernel binding、pipeline/queue/setup、transport outcome policy、status notice/event、S7/exports 或相关持久/恢复合同变化时，本记录失效并只重验上述闭包。协调者接受后再选择 Delivery accepted-work/lifecycle 与 Channel effect 的最窄后续责任链；本包未进入该边界、其他领域或 A6，也未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立复核纠正：状态迁移、响应丢失与恢复链的判别性验证成立，但领域应用仍直接导入 `AuthorityStorageError`，且统一承载 resolution/obligation/lifecycle 三类用例的源码和制品仍命名为 `resolution-application`；这两项分别违反 Domain 只依赖 Correctness port 的依赖方向和当前真实职责命名。A5-03 暂不接受，须在原包内移除实现错误依赖并把唯一应用入口收敛为职责一致的 `delivery/application` 源码与制品，保持公开 subpath、错误语义和全部现有行为不变后重验失效闭包。
- 纠正实施与错误边界：Delivery application 已删除对 `../authority` 与 `AuthorityStorageError` 的依赖，投影违反领域生命周期不变量时只产生有限 `DeliveryProjectionInvariantError`；唯一生产 Correctness adapter 在 `transactDeliveryLifecycle` 的决定回调边界把该信号映射为 `AuthorityStorageError("commit-log-corrupt", ..., { cause })`，投影校验和状态规则仍由领域 application 持有，Authority 仍只负责串行投影、record 校验与耐久 append。直接反例分别证明领域信号不泄漏 Correctness 实现，以及生产 adapter 保持原 `commit-log-corrupt` code/message/reason 语义并保留 cause；未改成普通 Error、未把规则移回 Authority、未形成第二状态机。
- 唯一 application 源码与制品：统一承载 uncertain resolution、obligation、lifecycle 的唯一实现和测试已从 resolution-only 名称收敛为 `packages/core/src/delivery/application.ts` / `application.test.ts`；公开 subpath 仍为 `@zhixing/core/delivery/application`，其 package export、tsup entry 与 fresh declaration/runtime target 唯一指向 `dist/delivery/application.{d.ts,js}`。旧 `resolution-application` source/test/build/dist target 与兼容转导均归零；fresh build 会清除旧制品，package-export gate 显式拒绝旧 dist 文件，S7 反向 mutation 可识别旧 source、旧 build/manifest target、根/barrel 第二入口、Domain→Authority 依赖和 Correctness 错误映射丢失。
- 纠正验证与状态：本次失效闭包为 core application/authority/pipeline 3 文件 82/82、owner-kernel 真实 Correctness/control 1 文件 8/8，合计 4 文件 90/90；core 与 owner-kernel fresh build 通过，fresh `pnpm runtime:package-exports` 通过，canonical `pnpm s7:lint` 为 30/30 且 registry golden 通过，最窄 Biome 与 `git diff --check` 通过。A5-03 纠正实施完成，等待协调者独立复核；A5 与 Delivery 行继续为 `[ ]`，producer、Channel、accepted-work 与后续 Delivery 责任均未进入，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从统一 application、Authority 事务机制、owner-kernel Correctness 翻译、pipeline/queue 与 CLI 组合根正反核对，确认领域投影不变量只以有限领域信号跨端口，原 `commit-log-corrupt` code/message/cause 在唯一生产适配器恢复；旧 `resolution-application` source/test/build/dist 与兼容入口均归零，公开 subpath 唯一指向职责一致的 `delivery/application`。独立完成 core fresh build与 82/82、owner-kernel fresh build与 8/8、CLI 真实组合 3/3及 typecheck、canonical S7 30/30、registry golden、fresh package exports、旧 dist 缺失检查和 `git diff --check`，全部通过；接受 `A5-03-delivery-lifecycle-application-v1`。Delivery accepted-work/lifecycle 与 Channel effect 尚未迁移，因此 Delivery 行和 A5 继续为 `[ ]`。

### A5-04：归位 Delivery accepted-work/lifecycle 应用责任

- 派发基线：`HEAD d181e926`；A5-03 已由协调者独立验收并提交，派发前索引与工作区为空。
- 唯一架构结果：Delivery 应用边界拥有生命周期 admission 的 install/restore/seal/release、冻结 source/delivery exact-set、后续 delivery binding、accepted-work 枚举及 immediate/drain/cancel 结算语义；Authority 只提供串行投影/状态机制，pipeline 只执行 quiesce、flush、等待和 transport 效果，Host 只调用一个有限 Delivery lifecycle 应用端口，不再组合领域规则。
- 生产闭包：正反追踪 `DeliveryStack`、host-stop/device-removal/startup recovery、`DeliveryAuthority.prepareEnqueues/#lifecycleAdmission/*LifecycleAdmission/lifecycleAcceptedWorkItems`、`AuthorityDeliveryPipeline.acceptedWorkItems/settleAcceptedWorkForLifecycle` 与全部 producer participant。迁移 conflicting operation/revision、source permit exact-set、初始与运行期 delivery 捕获、sealed/release、重启恢复、uncertain 阻断、deadline/timeout、quiesce/resume 和结算 read-back 的现有决定；保持外部 accepted-work artifact、公开协议、持久 delivery record 与 stop/removal 行为完全等价。
- 责任与旧路：优先复用现有 `@zhixing/core/delivery/application`，由 owner-kernel 的窄 Correctness adapter 提供 Authority 串行、当前投影和必要状态机制；不得在 CLI、pipeline、Authority、participant 或 Host-stop helper 中留下相同 admission/binding/membership/settlement 分支。可以保留不含产品判断的 pipeline effect port，但 Host 不得继续分别直调 Authority 和 pipeline 拼出同一生命周期用例。禁止第二 application、双 admission state、兼容回调、服务定位或仅改名。
- 明确不做：不改变 Delivery attempt/retry/terminal、uncertain Product API、producer obligation、Channel transport/sender/content/event effect、其他 accepted-work owner、通用 Host-stop/device-removal 协议、公开 RPC/Event、持久格式或用户体验；不进入 A6，也不顺带迁移其他领域。
- 直接证据：覆盖 install/restore 冲突与幂等、source/delivery revision exact-set、sealed 后绑定拒绝、release 后新 operation、冷启动 accepted-work 恢复、运行期新 delivery 捕获、immediate/drain/cancel、uncertain 阻断、timeout、response loss、quiesce/resume 与 stop/removal read-back；反向证明 CLI/Host 不再直调 Authority lifecycle 业务入口，Authority/pipeline 不再拥有领域分支且不存在第二状态。只运行 Delivery application/authority/pipeline、participant 与真实 CLI host-stop/device-removal 受影响闭包，必要依赖构建、canonical S7、fresh package exports、最窄格式与 `git diff --check`；不得重复根级或未失效 Delivery 全测。
- 完成与止损：只有 admission、binding、membership 与 settlement 由唯一 Delivery 应用责任闭合，Authority/pipeline/Host 各自只剩机制、效果和调用职责，全部旧直调归零且行为证据通过，才算完成。预计超过四小时、需要改变外部 accepted-work artifact/持久语义、出现两个可独立失败的未知、影响面扩到 Channel 或其他 owner，或无法保持单一 admission 真相时，停在可构建、可运行、可恢复且无双写的安全检查点反馈；不得继续扩面。
- 执行基线与唯一应用：`HEAD d181e92699e12e76fd9cf6eba6f2256e2b5deccc + task-doc:A5-04-dispatch + A5-04-delivery-accepted-work-lifecycle-application-v1`。A5-03 的同一个 `DeliveryLifecycleApplicationService` 现唯一拥有 admission install/restore/seal/release、source/delivery exact-set、运行期 binding、accepted-work membership 与 immediate/drain/cancel settlement；同 operation canonical replay（包括对象字段构造顺序不同）幂等，异 operation、source/revision 或 delivery/revision 冲突 fail closed。外部 HostStop accepted-work artifact 仍是启动恢复输入，恢复时经同一应用重建 admission；没有新增持久 record、第二 application 或进程缓存事实源。
- Correctness、Authority 与效果边界：`createOwnerDeliveryLifecycleBinding` 是唯一生产 Correctness 适配，向应用提供当前 Delivery 投影、串行 lifecycle transaction 与串行 admission state transaction。`DeliveryAuthority` 已删除 install/restore/seal/release、binding、accepted-work 枚举等业务入口和分支，只保留 `prepareEnqueues` 所需的当前 admission 投影及通用串行安装机制；producer obligation 在同一应用内按冻结 source exact-set 形成原 lifecycle binding。`AuthorityDeliveryPipeline` 已删除 accepted-work 筛选和 settlement policy，只实现 close、等待在途 flush、quiesced 单次 flush、resume 与既有 transport/content/event 效果；uncertain 人工裁决阻断、deadline、三种 strategy 与 membership read-back 均由领域应用决定。
- Host、恢复与拓扑闭包：`DeliveryStack.lifecycle` 是 Host 可见的唯一有限端口，startup restore、HostStop、永久设备移除、失败补偿和恢复后 resume 均只调用该端口；planned Authority transfer 仍只复用既有效果级 quiesce/resume，不被误写为 accepted-work 用例。Anchor generation rebind 会同时换代同一 Authority 对应的 lifecycle binding；Executor-only 不构造 Anchor Delivery authority，已删除会触发缺失 Anchor getter 的直调，并对外部 artifact 中意外出现 Delivery accepted-work fail closed。全部 producer participant 仍经 A5-02 obligation 应用绑定同一 admission，持久 Delivery record、公开协议、HostStop/device-removal artifact、attempt/retry/terminal、uncertain Product API 与 Channel effect 均未改变。
- 直接证据与门禁：最终当前源码上 core application/authority/pipeline 3 文件 84/84，owner-kernel participant/control 2 文件 18/18，CLI setup/真实 authority recovery/HostStop/device-removal/Executor terminal 5 文件 57/57，合计 10 文件 159/159；覆盖七类既有 producer 的冻结 exact-set、运行期捕获、重启恢复、sealed/release、canonical replay、响应丢失重驱、三种 settlement、uncertain、deadline、quiesce/resume 与 executor 隔离。core、owner-kernel 与 CLI 依赖顺序 build、CLI `tsc --noEmit`、canonical `pnpm s7:lint` 30/30 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 均通过；S7 反向 mutation 可识别 Authority/pipeline 决定回流、Host/Executor 直调 Authority、唯一 binding/端口丢失及 planned-transfer 与 lifecycle resume 混淆。
- 交接与失效：A5-04 实施完成，等待协调者独立复核；A5 与 Delivery 行继续为 `[ ]`，下一责任链仍只能单独收束 Channel effect 并裁决 Delivery 行。Delivery application 的 admission/binding/membership/settlement、Authority admission transaction、owner-kernel binding、pipeline effect port、DeliveryStack lifecycle port、startup/HostStop/device-removal/Executor 隔离、全部 producer lifecycle source、外部 accepted-work artifact、S7/exports 或相关持久/恢复合同任一变化时，本证据精确失效并只重验上述 10 文件与构建/门禁闭包。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从 Delivery application、Authority admission transaction、owner-kernel binding、pipeline effect port、动态 Authority 换代、DeliveryStack lifecycle 端口及 Anchor/Executor 两类生产根正反核对，确认 admission、binding、membership 和 settlement 只有一个领域决定 owner，Authority/pipeline/Host 已无旧业务直调或第二状态机；Executor-only 对意外 Anchor Delivery accepted-work 明确 fail closed。独立完成 core/owner-kernel/CLI 依赖顺序构建与 CLI typecheck，并重跑 core 84/84、owner-kernel 18/18、CLI 57/57；canonical S7 30/30、registry golden、fresh package exports 与 `git diff --check` 全部通过。接受 `A5-04-delivery-accepted-work-lifecycle-application-v1`；Channel effect 尚未迁移，因此 Delivery 行和 A5 继续为 `[ ]`。按用户要求，本包提交后暂停调度。

### A5-05：收束 Delivery → Channel 发送效果边界并裁决 Delivery

- 派发基线：`HEAD bf05dfa0 + task-doc:A5-05-dispatch`；A5-04 已由协调者独立复核并提交，派发前索引与工作区为空。
- 唯一架构结果：Delivery 领域拥有调用发送效果所需的窄端口、输入/输出合同与结果解释边界；Channel、Outbox 和具体 adapter 只报告就绪事实、执行发送并返回有限效果证据，不能定义投递义务、重试/未知/终态、产品事件或用户流程。完成后从 Delivery 合同、应用、权威适配、效果调用、Product API、生命周期和恢复正反追踪，裁决该领域行是否真实闭合。
- 生产闭包：即时正反核对 `AuthorityDeliveryPipeline → DeliveryTransportRegistry/channelAuthorityDeliveryTransport → OutboxSender/OutboxRegistry → ChannelRegistry/ChannelAdapter.send`、内容物化、receipt/idempotency、outcome policy/readiness、状态 notice/event、`setupDelivery` 与 Channel generation/lifecycle。需求方拥有稳定语义的端口；Host 只选择并装配具体实现，不能保留缺 adapter、重试或投递结果的业务分支。保留 per-target FIFO、turn slot/after-slot 顺序、send 前耐久 claim、响应丢失、重连窗口、receipt、人工裁决/idempotent redrive、状态事件和启动/关闭行为完全等价。
- 旧路与门禁：删除 pipeline 的 `sender` 兼容入口、领域 pipeline 内的 Channel 具体适配工厂、Host 中可由领域或效果适配器拥有的投递决定，以及任何第二 transport/send 合同；不得把旧结构仅改名或移动。Delivery 的生产调用只能依赖领域拥有的窄效果端口，Channel adapter 不得导入 Delivery application/Authority/状态机；结构证据必须识别旧 sender 回流、Channel/Host 生成业务决定、第二 registry/port、根 barrel 旁路和真实组合根漏改绑。
- 明确不做：不改变公开 RPC/Event、Delivery record/content schema、Channel 入站会话、确认卡片/交互、普通会话即时回复、具体飞书产品表现、Transport/Outbox 的既有顺序与超时语义；不迁移 Channel 整体生命周期、Provider/Storage/Executor/Mesh，不进入 A6 或其他 A5 领域，不顺手抽象万能 effect/capability。
- 直接证据：覆盖 adapter 缺失/未连接/连接竞态、成功 receipt、retryable/non-retryable 拒绝、发送抛错导致 unknown、outcome policy、idempotency meta、FIFO/slot/after-slot、内容物化永久/暂时失败、状态 event/notice、恢复 drain、generation rebind 与关闭清理；反向证明 Host/Channel 无投递业务决定、pipeline 无旧 sender/具体 Channel 工厂且唯一生产组合根只绑定一次。只运行 Delivery pipeline/transport/outbox、Channel adapter/registry 与真实 CLI setup 的受影响闭包、必要依赖构建、canonical S7、fresh package exports、最窄格式和 `git diff --check`，不重复 A5-01～A5-04、根级回归或制品验收。
- 完成与止损：只有 Delivery 的产品义务与终态全部由唯一领域应用闭合、发送效果合同由需求方拥有、Channel 只实现效果、Host 只装配、旧桥和第二入口归零，且现有行为直接证据成立，才可把 Delivery 行标为 `[x]`。预计超过四小时、发现两个独立责任链、必须改变公开/持久/产品语义、影响面扩到 Channel 入站或 A6 其他边缘，或无法在单一真相下删除旧 sender/transport 路径时，停在可构建、可运行、可发布的安全检查点反馈；不得借裁决继续迁移下一领域。
- 执行基线与领域决定：`HEAD bf05dfa07ae6855af0cdf188cdf10d3638cbf587 + task-doc:A5-05-dispatch + A5-05-delivery-channel-send-effect-v1`。`DeliveryEndpointTransport` 是 Delivery 需求方拥有的唯一发送效果端口；具体 effect 只返回连接就绪、脱敏后的有限发送结果及 `DeliveryResponseLossEvidence`。`decideDeliveryAttemptOutcomePolicy` 位于 Delivery application，将 `unverified/idempotent` 效果证据裁决为既有 `manual-resolution/idempotent-redrive`；pipeline 只驱动领域应用、物化内容、调用效果并把 message id/receipt 解释为领域 outcome，没有把 Channel 证据提升为第二终态决定。
- Channel、Outbox 与 Host 边界：`createChannelDeliveryEffect` 是 `@zhixing/core/delivery/channel-effect` 唯一窄实现，复用单一 `OutboxRegistry`，只实现 adapter presence/readiness、source、idempotency meta、agent turn-slot 与 scheduler after-slot 的发送效果；generation 切换竞态中 adapter 消失返回 retryable 的有限效果证据，adapter 失败细节仍按迁移前合同在专用效果边界脱敏。Outbox 只保持 per-target FIFO、slot 顺序和既有通用诊断，不再按 Authority origin/idempotency key 解释 Delivery 失败。`setupDelivery` 只构造并注册该 effect；旧 `DeliverySender`、`outbox-sender.ts`、pipeline sender 兼容入口、pipeline 内具体 Channel 工厂及 Host 的 adapter 缺失/发送业务分支均已删除，没有第二 registry、send 合同或根 barrel 旁路。
- 行为保护与直接证据：send 前耐久 claim、内容物化、receipt/idempotency、retryable/non-retryable、抛错 unknown、manual/idempotent redrive、响应丢失、重连竞态、FIFO/turn-slot/after-slot、状态 notice/event、恢复 drain、generation rebind 与关闭清理保持原合同。最终当前源码上 core pipeline/真实 Channel effect/Outbox 4 文件 75/75，CLI setup/Authority recovery 2 文件 27/27，合计 6 文件 102/102；其中协调者首次包内复核以 23/24 识别出 CLI 测试错误依赖已删除的 `Channel not found` 字面量，本次删除该无识别力邻域断言后，core effect 4/4 直接证明 readiness 后 adapter 消失返回 retryable 有限证据，CLI setup 24/24 只证明窄 subpath 的单次 effect 装配且 Host 不含 adapter/send/outcome 决定，CLI 两文件随后 27/27。生产源码未因纠正改变，既有 core build、CLI `tsc --noEmit` 与 `pnpm cli:build`、canonical `pnpm s7:lint` 30/30 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 均继续有效；最终 `git diff --check` 通过。S7/exports 反向 mutation 可识别旧 sender、Channel/Host 业务 policy、Outbox Authority 分支、具体 effect 根/索引转导、第二 build/export 入口和生产组合根漏改绑。
- Delivery 行裁决与失效：A5-01 已闭合 uncertain-resolution Product API，A5-02 已闭合义务/入队，A5-03 已闭合 attempt/unknown/terminal，A5-04 已闭合 accepted-work/lifecycle，本包再闭合唯一发送效果边界，因此 Delivery 当前全部产品责任、应用、Correctness、Product API、生命周期、恢复和效果消费已形成单一生产链，Delivery 行标为 `[x]`；A5 仍为 `[ ]`，未迁移下一领域。Delivery application/response-loss 证据、transport contract/registry、Channel effect、Outbox/ChannelAdapter result、pipeline outcome 解释、setup composition、core package export/build entry、S7/exports 或上述行为任一变化时，本证据精确失效并恢复 Delivery 行。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：正反核对 Delivery effect contract、领域 outcome policy、pipeline、Channel effect、Outbox、唯一 CLI 组合根、subpath/build/export 与 S7 负向门禁，确认 Delivery 独占义务、未知/重试/终态和效果证据解释，Channel/Outbox 只执行有限发送效果，Host 只装配，旧 sender、具体 Channel 工厂和 Outbox Authority 分支归零。首次复核识别并退回无效 `Channel not found` 字符串测试；纠正后独立完成 core 4 文件 75/75、CLI 2 文件 27/27、core/CLI typecheck、canonical S7 30/30 + registry golden、fresh package exports 与 `git diff --check`，全部通过。接受 `A5-05-delivery-channel-send-effect-v1` 并确认 Delivery 行 `[x]`；A5 继续为 `[ ]`。

### A5-06：归位 Workspace Administration 绑定 CRUD 应用责任

- 派发基线：`HEAD e494497f + task-doc:A5-06-dispatch`；Delivery 已闭合并提交，派发前索引与工作区为空。
- 唯一架构结果：Workspace Administration 领域应用拥有本机 workspace 的 list/create/authorize-for-control/rename/repath/remove 合同、名称解析、稳定视图、业务拒绝与请求身份；CLI/本机管理 Host 只绑定和呈现，Workspace binding Authority 与资源治理只提供 Correctness 机制。当前位于 `LocalWorkspaceFacade` 的这些产品决定迁入一个有限、非根级领域应用入口并删除旧业务 owner。
- 生产闭包：正反追踪 `workspace` CLI 命令、`runWorkspaceSceneCreateCommand`、`LocalWorkspaceClient/ManagementHost → LocalWorkspaceFacade → WorkspaceBindingAdminPort → WorkspaceBindingCatalog/Service`、本机 owner/transport 及 `setupAuthorityRuntime` 组合。保持 displayName 查找、not-found/name-conflict、revision conflict、create purpose、control authorization 返回的 deviceId/bindingRef、request nonce/abort、resource acquire/settle/release 和所有现有错误映射完全等价；Workscene 创建只消费明确的 workspace authorization 应用端口，不取得 binding store、raw recovery 或 Authority 实现。
- 旧路与门禁：CLI 不得继续定义 workspace 视图、业务错误、按名称选择或 CRUD 应用流程；领域应用不得导入 CLI、ExecutorResourceGovernor 具体实现、文件路径存储、Host/transport 或 Workscene。Correctness 适配只能提供有限 binding admin 与 control-resource 端口，不能复制领域规则；删除旧 facade/兼容导出、第二 CRUD service、根 barrel 旁路和直接 consumer 漏改绑。允许本包在现有目录形成边界，但新领域入口必须窄且可由结构证据唯一识别。
- 明确不做：不迁移 catalog status、reset/confirmation/recovery、durable operation outbox、管理 Host 的 prepare/commit/ack/retry/degraded 状态机、workspace owner/IPC 生命周期、binding reducer/持久格式、legacy activation、workspace probe/environment、Workscene 领域、公开命令或用户文案；不进入 A6，也不拆 npm 包或建设通用管理框架。
- 直接证据：覆盖 list/view、create settings/control、authorize-for-control、rename/repath/remove、名称 0/1/多匹配、revision/conflict/cancel、request replay/abort、resource admission/settle/release、CLI 管理 Host 与 create-scene 真实消费；反向证明 CLI/Host 无 CRUD 业务决定、领域无具体 Governor/Authority/Host 依赖且生产组合只实例化一个应用。只运行新应用、原 facade 对应行为、management-host/command/create-scene 的受影响闭包，必要依赖构建、canonical S7、fresh package exports、最窄格式和 `git diff --check`；不得提前运行 reset/recovery 全闭包、根级回归或制品验收。
- 完成与止损：只有六类 CRUD/授权用例、视图与拒绝语义由唯一 Workspace Administration 应用闭合，所有直接消费者改绑，旧 facade 业务 owner 和旁路归零，现有行为证据通过，才算完成。预计超过四小时、必须同时迁移 reset/outbox/owner 生命周期、发现两个独立事实 owner、需要改变公开/持久语义，或无法保持本机 raw path 与恢复 capability 不跨边界时，停在可构建、可运行、单一真相成立的安全检查点反馈；不得继续完成整个 Workspace Administration。
- 执行基线与唯一应用：`HEAD e494497f2d173e715ca8eac564ea0aabc02a8108 + task-doc:A5-06-dispatch + A5-06-workspace-administration-crud-application-v1`。新增唯一窄入口 `@zhixing/core/environment/workspace-administration`，由 `WorkspaceAdministrationApplicationService` 独占 list/view-by-name/create/authorize-for-control/rename/repath/remove、按 displayName 的 0/1/多匹配裁决、只读稳定 view、业务错误与 `outboxId/localSeq/operationId/inputDigest → environment-admin requestId`。领域只依赖有限 `WorkspaceBindingAdminPort` 与 `WorkspaceAdministrationControlPort`，不导入 CLI、Executor governor、Host/IPC、recovery、Workscene 或文件存储；core 根和宽 `environment` subpath 均不转导该合同。
- Correctness、Host 与恢复边界：CLI 的 `ExecutorWorkspaceAdministrationControl` 只实现一次 interactive control resource acquire、settle、release 与 abort 传递；`LocalWorkspaceManagementHost` 只把 durable outbox wire input 映射成领域 command/execution，并在唯一生产 factory 构造一次应用。status/reset/recovery 被原样隔离到 `LocalWorkspaceRecoveryService`，durable prepare/commit/ack/retry/degraded、owner/IPC、binding catalog/reducer/persistence 与资源错误分类均未迁移。三处生产组合根统一改传 `management` 机制依赖，未出现第二 CRUD service 或双写。
- Surface 与旧路清理：`LocalWorkspaceClient` 继续保持公开命令现有参数、结果和错误呈现，只消费领域 view；`workspace` CLI 与 REPL 统一经 `withLocalWorkspaceClient`，旧 `LocalWorkspaceFacade` 源码、类、业务错误、`withLocalWorkspaceFacade` 名称和兼容入口全部删除。durable authorize-for-control 结果与 recovered `controlWorkspace` 保持既有 strict `{deviceId,bindingRef}`；`create-scene` 先以该结果和稳定 consumption credential 创建/重放 Workscene，再经应用的 `viewByName` 读回 revision，最终输出不二次 list、不在 CLI 按名称选择，也不依赖 Workscene 可选展示元数据。Workscene wire、P10 result/digest、消费确认和恢复重放均不变。
- 行为与直接证据：领域应用 1 文件 4/4 覆盖 CRUD/授权、稳定 view、`viewByName` 的名称 0/1/多匹配、durable request replay、abort、revision conflict 与 cancel；CLI control/management-host/workspace-command 3 文件 23/23 覆盖资源 acquire→settle→release、业务失败、顺序重试、响应丢失、消费确认、旧两字段 completed control result 的恢复/confirm、第三字段拒绝、Workscene metadata 缺失后的领域 view，以及真实 Host/client delivery callback 内的 current-result 保留、view 失败不确认和其他 completed result 拒绝；真实 in-process/mesh `s7-environment-conformance` 1/1 覆盖 production factory、binding Authority、显式 view read-back 与 Workscene 消费。该 conformance 内早已失效的旧 `onYield/RunResult` 测试替身同步改用已接受的 Kernel Envelope/Event/Completion，只恢复测试对当前正式合同的识别力，未修改生产行为。合计本包直接闭包 5 文件 28/28。
- 构建、门禁与交接：core fresh build、CLI typecheck 与 `pnpm cli:build` 通过；canonical S7 最终 31/31 且 registry golden 通过，fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 通过。S7 反向 mutation 拒绝旧 facade/第二应用、领域规则回流 CLI、CLI 按名称选 binding、宽根转导或重复 export；package gate 锁定唯一窄 runtime/declaration 入口。A5-06 实施完成，等待协调者独立复核；Workspace Administration 行与 A5 继续为 `[ ]`，下一责任链仍只可处理其 operation delivery 或 reset/recovery。若领域 application/control port、Host durable mapping、三个组合根、Client/CLI/create-scene binding、core build/export、S7 或 binding admin/error/revision/abort 合同任一变化，本证据精确失效并只重验上述闭包；未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核反证与最终纠正：初次实现错误地从 `created.scene.workspace?.workspaceBindingRevision` 读取修订号；第一次纠正又把 revision 扩进 durable authorization，误改了 P10 已冻结的两字段 result/digest，二者均不成立。最终实现恢复且只接受 strict `{deviceId,bindingRef}`，旧 completed control operation 可由新代码 recovered delivery 并 confirm，第三字段仍 fail closed、没有兼容 reader；revision 改由领域 `viewByName` 复用唯一 0/1/多名称裁决。命令在 consumption confirm 前按“Workscene create/replay → viewByName”顺序完成，Workscene 无 metadata 仍成功；CLI/REPL wire 始终只含两字段。后续复核又发现 view 最初会把 delivery 正在消费的 `currentResult` 清空并误报 recovered；最终 client 先从既有 pending prefix 验证稳定 consumption credential，recover 后只从未认领集合排除该 exact operation，其他 completed 仍 fail closed。真实 Host/client 证明 view 成功后才 confirm 且 pending 归零，view 失败时 credential/pending 保留。S7 以反向 mutation 拒绝 scene metadata、CLI list/filter/name rule、领域 view 缺失、current result 清空、三字段 wire 或完整授权对象泄漏。纠正闭包的 core 4/4、CLI 21/21、production conformance 1/1、core/CLI build、CLI typecheck、canonical S7 31/31 + golden 与 fresh exports 均通过。两字段 durable shape/digest、恢复投影、current-result 差集、领域 view/name-resolution、两阶段命令顺序或 strict Workscene wire 任一变化时，本纠正子证据精确失效。
- 协调者独立验收：从三个生产组合根、durable Host/outbox、Workspace Administration 应用、Correctness control adapter、CLI/REPL 与 Workscene 创建消费链双向反查，确认 CRUD、名称 0/1/多裁决、稳定 view 与 request identity 只有一个应用 owner；Host 只映射耐久机制，CLI 不再解释名称或 workspace 事实，旧 facade 和宽导出均已退场。独立验证领域 4/4、CLI control/Host/command 23/23、真实 S7 environment conformance 1/1、core fresh build、CLI typecheck/正式构建、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome 与 `git diff --check` 均通过；接受 `A5-06-workspace-administration-crud-application-v1`。Workspace Administration 行与 A5 继续为 `[ ]`，下一包只处理 reset/recovery，不迁移 durable outbox/owner 或进入 Workscene。

### A5-07：归位 Workspace Administration reset/recovery 应用责任

- 派发基线：`HEAD fb1598f6 + task-doc:A5-07-dispatch`；A5-06 已由协调者独立复核并提交，进场索引与工作区应只有本文派发记录。
- 唯一架构结果：Workspace Administration 的唯一应用边界同时拥有 catalog health/status、degraded reset impact、世代一致性、展示—确认和 begin/complete reset 产品用例；CLI Host、client 与 durable outbox 只绑定请求、耐久操作身份、确认交付和 Correctness 效果，不再定义 reset 的产品规则、文案或业务错误。
- 生产闭包：把 `LocalWorkspaceRecoveryService`、`WORKSPACE_CATALOG_RESET_IMPACT`、status 投影、generation mismatch、确认内容和 reset request identity 归入 A5-06 已建立的 Workspace Administration 应用及其窄 subpath；三个生产组合根只构造一个应用实例。Host 的 reset prepare/commit/replay、client preview/confirm、workspace CLI/REPL status/reset 和真实 in-process/mesh 路径都消费该应用合同；`WorkspaceBindingRecoveryPort` 继续只提供 status、beginReset、completeReset Correctness 机制。
- 保护边界：保持 P10 reset operation 的 strict wire、prepare/commit/completed/ack、15 分钟确认有效期、稳定 operation identity/result digest、旧 completed replay、响应丢失与重启前滚、healthy/degraded 拒绝、generation/confirmation/reservation 冲突、receipt schema、资源准入、abort、公开命令、中文影响说明和用户两阶段体验全等。领域应用不得导入 CLI、outbox、owner/IPC、Executor、文件存储或 Workscene，也不得复制 catalog reducer、签名、持久化或恢复机制。
- 旧路与门禁：删除临时 `local-workspace-recovery.ts`、CLI-owned reset impact/business service 及 Host 内的 generation/impact 产品分支；允许 client/Host 保留 durable operation 编解码与两阶段交付绑定，唯一退场点是紧邻的 Workspace Administration operation-delivery 工作包。不得形成第二 reset application、兼容入口、宽根导出或以 callback/service bag 隐藏产品规则；结构门禁必须绑定唯一应用构造、三个组合根、真实 reset 调用与 fresh declaration。
- 直接证据：最窄测试覆盖 healthy/degraded status、impact、世代变化、错误确认、begin/complete、并发/响应丢失/重启 replay、abort、receipt 和 client preview/confirm/ack；反查现有 workspace binding catalog 的 corruption/reset Correctness 用例仍有识别力。只运行领域 reset 直接测试、现有 Host/client reset 失效闭包、真实 S7 environment 路径、必要依赖构建、canonical S7、fresh package exports、最窄格式与 `git diff --check`；不得重复 A5-06 CRUD 全闭包、根级回归或制品验收。
- 明确不做：不迁移 durable outbox/owner/IPC、operation delivery 通用协调、binding catalog reducer/Authority/存储或 Workspace 其他责任，不改变公开协议、持久格式、产品文案或恢复能力，不进入 Workscene/A6，不新增能力。
- 完成与止损：只有 reset/status 的全部产品决定和用例由唯一 Workspace Administration 应用闭合，所有直接消费者改绑，临时 recovery owner 与 Host 业务分支归零，现有恢复/耐久/用户行为证据通过，才算完成。预计超过四小时、必须同时迁移 outbox/owner、改变 P10 wire/receipt/公开体验、发现第二独立 owner，或无法在应用唯一且机制不复制的可发布状态安全交接时，立即停止并记录唯一未闭合链；不得继续完成整个 Workspace Administration。
- 执行基线与唯一应用：`HEAD fb1598f6e1836fa85a96dd73966c0b8c35bd1eac + task-doc:A5-07-dispatch + A5-07-workspace-administration-reset-recovery-application-v1`。A5-06 的同一个 `WorkspaceAdministrationApplicationService` 现唯一拥有 status/degraded impact、reset preview 的 generation/确认内容裁决、begin/complete reset 用例和 durable operation identity；领域只消费 `WorkspaceBindingRecoveryPort.status/beginReset/completeReset` 与既有 control port，不导入 CLI、outbox、owner/IPC、Executor、路径存储或 Workscene。三个生产组合根仍经 `createExecutorLocalWorkspaceHost → createLocalWorkspaceManagementHost` 只构造一个应用实例，没有第二 reset service、状态机或写入口。
- Durable delivery 与恢复边界：Host 只把 strict P10 reset input、outbox identity、prepared time 与 confirmation token 映射到领域 command/execution；client 仍只执行 preview/confirm/ack 和 strict codec。preview 在 prepare 时校验 canonical 中文 impact 与当前 generation；commit/restart 不在 durable receipt 前读取后继 generation，而把同 request 交给 recovery mechanism 的 reservation/receipt replay，因此已完成 reset 的 response-loss 重放不会被 successor generation 误拒。confirmation `issuedAt` 固定为 durable `preparedAt`、token 固定为 outbox committed record，Catalog 的确认窗口与 outbox 同为 15 分钟；同 preview 重放返回同 receipt 且不产生第二执行，异 confirmation/request、healthy catalog、generation/reservation conflict 与 abort 仍由既有机制 fail closed。P10 prepare/commit/completed/ack、operation/result digest、receipt schema、公开命令和两阶段中文体验未改变。
- 旧路、行为证据与门禁：删除 `local-workspace-recovery.ts`、`workspace-reset-impact.ts`、`LocalWorkspaceRecoveryService/BusinessError`、Host generation 分支和 Host 自建 reset nonce；领域窄 subpath 成为 status/reset impact/preview/reset 的唯一正式入口。当前源码上 core application + real catalog 2 文件 12/12，CLI Host/outbox/command 3 文件 27/27，真实 in-process/mesh S7 environment conformance 1/1；覆盖 healthy/degraded status、impact、generation/确认拒绝、begin/complete、15 分钟边界、stable confirmation/operation、响应丢失 replay、Catalog 重启恢复、receipt、client preview/confirm 与 ack。core fresh build、CLI `tsc --noEmit` 与 `pnpm cli:build`、canonical `pnpm s7:lint` 31/31 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 均通过。S7 反向 mutation 拒绝临时 recovery/impact 文件回流、第二应用、Host 自建 identity/generation 决定、任一 begin/complete 缺失、三个生产根漏经共享 factory、宽根或重复 export。
- 交接与失效：A5-07 实施完成，等待协调者独立复核；Workspace Administration 行与 A5 继续为 `[ ]`，下一责任链仍只能收束其 durable operation delivery 应用责任并裁决该行，不进入 Workscene。Workspace Administration status/preview/reset 合同、impact 文案、operation identity、outbox confirmation/preparedAt、Catalog confirmation TTL/reservation/receipt、Host/client strict codec、三个生产组合根、core subpath/build/export、S7 或上述直接行为任一变化时，本证据精确失效并只重验本包闭包。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从唯一 Workspace Administration 应用、三个生产根、Host prepare/commit/drain、client preview/confirm/ack、P10 outbox 与真实 Catalog reservation/receipt/recovery 双向反查，确认 status、impact、generation、确认与 reset 用例只有一个领域 owner，Host/client 只保留 durable binding，旧 recovery service、impact 文件和 Host 业务分支均已退场。独立验证 core application/Catalog 12/12、CLI Host/outbox/command 与真实 S7 environment 28/28、core fresh build、CLI typecheck/正式构建、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome 与 `git diff --check`，全部通过。额外裁决确认：迁移前用户表面的 preview/outbox 窗口本就是 15 分钟，旧 Catalog 的 5 分钟只从每次调用新生成的 `issuedAt` 计算，无法形成稳定崩溃重放摘要；当前以耐久 `preparedAt` 和 token 固定确认并统一 15 分钟，不扩大公开入口、wire 或用户操作，只恢复既有 15 分钟两阶段体验的响应丢失/重启语义。接受 `A5-07-workspace-administration-reset-recovery-application-v1`；Workspace Administration 行与 A5 继续为 `[ ]`，下一包只收束 durable operation delivery。

### A5-08：归位 Workspace Administration durable operation 合同与分派责任

- 派发基线：`HEAD d67364e5 + task-doc:A5-08-dispatch`；A5-07 已由协调者独立复核并提交，进场索引与工作区应只有本文派发记录。
- 唯一架构结果：Workspace Administration 的唯一应用边界拥有 durable management operation 的有限合同、strict 输入校验、目标投影和 create/settings、authorize/control、rename、repath、remove、reset 六类分派与结果语义；P10 outbox 只持久化领域 operation，Host 只把已提交 operation 与耐久 execution metadata 交给同一个应用入口，不再拥有业务 switch、字段解释或结果决定。
- 生产闭包：把 `LocalWorkspaceWriteOperation` 及其 strict validator、operation target 和 Host `#execute` 六分支归入 A5-06/A5-07 的 Workspace Administration 窄 subpath与同一 service；outbox writer/reader、Host request codec、client prepare/commit/recovery notice、CLI/REPL 和真实三个组合根统一消费该合同。operation identity 必须继续由 outboxId/localSeq/operationId/inputDigest 反绑领域 requestId，reset 继续携带 stable token/preparedAt，create-control 只返回 strict `{deviceId,bindingRef}`。
- 保护边界：保持 P10 operation discriminant、字段、canonical digest、prepared/committed/completed/abandoned 转换、result/resultDigest、业务错误、资源/取消、revision conflict、recovered notice target、旧记录读取、响应丢失和全部公开输出全等。领域不得导入 CLI、outbox、owner/IPC、Executor、文件存储或 Workscene；outbox 不得重新定义 operation union、产品字段默认值或业务分派。
- 临时边界与退场：本包保留 Host 的 outbox-ready/writable gate、prepare/commit、drain、retry/degraded、pending/ack 与 client delivery lifecycle，它们是紧邻 A5-09 的唯一退场点；不得在本包迁移 owner/IPC、耐久状态机或基础设施错误分类。删除 Host 业务 switch、CLI-owned operation contract/validator/target 与重复 result schema；不得以 adapter callback、第二 dispatcher、兼容 union 或宽根导出保留旧路。
- 直接证据：最窄测试覆盖六类 operation 的 strict 正反输入、应用分派、结果、错误、稳定 request identity、target projection，P10 旧记录/digest/replay 与 Host/client/CLI 真实调用全等；结构反例能识别 outbox/Host 重定义 operation、Host switch 回流、应用分支缺失、第二入口和宽导出。只运行领域 operation、outbox codec、Host/client/command 失效闭包、真实 S7 environment、必要依赖构建、canonical S7、fresh exports、最窄格式与 `git diff --check`；不重复 reset Catalog 深层恢复、A5-06 CRUD 全闭包、根级回归或制品验收。
- 明确不做：不迁移 durable delivery 的 admission/retry/degraded/pending/ack/生命周期，不改变 P10 持久格式、owner/IPC、公开命令或产品能力，不进入 Workscene/A6。
- 完成与止损：只有 operation 合同、输入/目标/结果语义与六类分派均归唯一应用，outbox/Host/client 只消费且旧定义和 switch 归零，当前行为证据成立，才算完成。预计超过四小时、必须同时迁移 delivery lifecycle、改变 P10 wire/digest/错误/公开输出、出现第二独立 owner，或无法保持可构建、可运行、单一 operation 真相时，停在安全检查点反馈；不得顺带裁决整个 Workspace Administration。
- 执行基线与唯一事实：`HEAD d67364e5c9efab174ca220af862d57397c5f7979 + task-doc:A5-08-dispatch + A5-08-workspace-administration-durable-operation-v1`。A5-06/A5-07 的同一个 `WorkspaceAdministrationApplicationService` 现唯一拥有五种 discriminant、create/settings 与 create/control 两条语义分支、strict operation/result/value codec、recovered target 投影及六类 durable 分派；operation identity、abort、`preparedAt` 与 reset confirmation token 只作为已经耐久裁决的 execution metadata 输入。领域继续只消费既有 workspace control/recovery ports，不导入 CLI、outbox、owner/IPC、Executor、文件存储或 Workscene。
- 生产接线与 P10 等价：`LocalWorkspaceOperationOutbox` 直接持久化、重读领域 operation，Host 在 committed operation 上只调用一次 `executeDurableOperation` 并传入 `outboxId/localSeq/operationId/inputDigest`、abort、`preparedAt` 与可选 confirmation token；同一应用再分派 create/settings、authorize/control、rename、repath、remove、reset。client/request codec、result codec、create-control exact `{deviceId,bindingRef}`、reset receipt、recovered notice target 均消费领域合同；P10 discriminant/字段、canonical input/result digest、prepared/committed/completed/abandoned、业务与基础设施错误分类、资源/取消、revision conflict、旧记录读取、response-loss replay、pending/ack 和公开输出未改变。
- 旧路与直接证据：删除 CLI `LocalWorkspaceWriteOperation`、`validateLocalWorkspaceWriteOperation`、Host `OperationResult`/六分支 `#execute`、command 本地 target switch 及同责 result/value/control codec；outbox/Host/command 不再定义业务字段或分派。当前源码上 core Workspace Administration 8/8，CLI Host/outbox/command 3 文件 27/27，真实 `local-workspace-operation-outbox` S7 environment witness 1/1；core fresh build、CLI `tsc --noEmit` 与正式 `pnpm cli:build`、canonical `pnpm s7:lint` 31/31 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 与 `git diff --check` 均通过。S7 反向 mutation 拒绝 outbox/Host 重新定义 operation、Host direct CRUD/switch、command target 旁路、应用分支或 durable metadata 缺失。
- 交接与失效：A5-08 实施完成，等待协调者独立复核；Workspace Administration 行与 A5 继续为 `[ ]`。下一责任链只能在 A5-09 收束 outbox-ready/writable、prepare/commit、drain、retry/degraded、pending/ack 与 client delivery lifecycle，不进入 Workscene。领域 durable operation/validator/target/result、应用六分派、outbox codec/digest/state、Host 单次调用与 metadata、client recovery/result、P10 旧记录兼容、三个生产组合根、core 窄 subpath/build/export 或上述直接/结构证据任一变化时，本证据精确失效并只重验本包闭包。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从领域应用、outbox writer/reader、Host committed drain、client prepare/commit/recovery/ack、CLI recovered notice 与三个生产组合根双向追踪，确认 durable operation 的 discriminant、strict 输入、target、result/value codec 和六类业务分派只有一个领域 owner；Host/outbox/command 中同责 union、validator、switch 与 result schema 已删除，剩余 retry/degraded/pending/ack 仅是登记给 A5-09 的 delivery lifecycle。独立验证 core Workspace Administration 8/8、CLI Host/outbox/command 27/27、真实 S7 environment 1/1、core fresh build、CLI typecheck/正式构建、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome 与 `git diff --check`，全部通过；P10 wire、canonical digest、旧记录、响应丢失重放、业务/基础设施错误、reset stable token/preparedAt 与公开输出未发现漂移。接受 `A5-08-workspace-administration-durable-operation-v1`；Workspace Administration 行与 A5 继续为 `[ ]`，下一包只收束 durable operation delivery 生命周期。

### A5-09a：归位 Workspace Administration durable operation 准入、执行与恢复生命周期决定

- 派发基线：`HEAD 8d8fceded07682b46f2737ec0f5893a24ed5f0b8 + task-doc:A5-09a-dispatch`；A5-08 已由协调者独立复核并提交，进场索引为空，工作区只应有本文派发记录。
- 唯一架构结果：A5-08 的同一个 Workspace Administration 应用边界拥有 durable operation 的 ready/recovering/degraded 产品状态、prepare/commit 准入、committed operation 执行、业务完成与 retry/degraded 决策；P10 outbox 只实现耐久 identity、状态转换、重放与原子存储机制，Host 只启动/关闭组件、承载 IPC 并把请求交给一个有限 lifecycle 应用端口，不再编排 operation 生命周期。
- 生产闭包：闭合三个 `createExecutorLocalWorkspaceHost` 生产根、`LocalWorkspaceManagementHost.start/close/#handle`、outbox initialize/prepare/commit/complete/oldestCommitted、committed drain、恢复调度、取消/资源/存储错误分类和 host-status 投影。领域拥有端口与生命周期策略，CLI/outbox 实现窄机制适配；领域不得导入 CLI、Executor 具体错误、IPC、路径、文件存储或 Workscene，Host 不得保留独立 ready gate、mutation tail、drain/recovery state machine、retry counter 或业务/基础设施完成裁决。
- 保护边界：保持 P10 文件、marker、operation 状态与 digest、prepared TTL、reset confirmation、串行与幂等、response-loss/restart replay、重试退避、degraded diagnostic code/message/localSeq、Host start/close/unpublish/abort、安全排空及公开 status/prepare/commit 行为全等；不得把 outbox 持久状态复制进领域，不能形成第二 lifecycle、双写、第二队列或 Host service locator。
- 临时边界与退场：本包保留 `pending/acknowledge`、client `readPendingDelivery/confirmDelivered`、recovered-result presentation 与 consumption credential 交付语义，作为紧邻 A5-09b 的唯一退场点；Host/IPC 可以暂时转发这两个机制请求，但不得继续决定其产品语义。A5-09a 完成后 Workspace Administration 行仍为 `[ ]`，不得进入 Workscene。
- 直接证据：最窄测试必须覆盖 start initialize、空队列 ready、committed 重启恢复、prepare/commit 幂等、一次执行、业务失败完成、资源/取消/存储 retry、不可恢复 degraded、并发 commit/drain、close 排空与 abort、P10 旧记录/digest/marker 全等，以及三个生产根只装配一个 lifecycle 应用。结构反例必须能识别 Host ready/mutation/drain/retry/degraded 状态机回流、应用绕过 outbox port、领域导入具体基础设施和第二入口。只运行本包失效闭包、必要上游构建、CLI typecheck/正式构建、canonical S7、fresh exports、最窄格式和 `git diff --check`；不重复 A5-06～A5-08 业务深测、pending/ack 全闭包、根级回归或制品验收。
- 明确不做：不改变公开协议、P10 wire/digest/状态、owner/IPC transport、pending/ack/client delivery 语义，不迁移 binding catalog Authority/存储，不进入 Workscene/A6，不新增能力或通用 durable workflow 框架。
- 完成与止损：只有准入、执行、恢复、retry/degraded 和产品状态均由唯一应用 lifecycle 决定，Host/outbox 只承担生命周期调用、IPC 与耐久机制且旧 Host state machine 归零，现有行为证据成立，才算完成。预计超过四小时、必须同时迁移 pending/ack、改变 P10/公开错误/退避/关闭语义、出现两个独立 lifecycle owner，或无法在可构建、可运行、无双写的安全点交接时，立即停止并记录唯一未闭合链；不得顺带裁决整个 Workspace Administration。
- 执行基线与责任变化：`HEAD 8d8fceded07682b46f2737ec0f5893a24ed5f0b8 + task-doc:A5-09a-dispatch + A5-09a-workspace-administration-durable-lifecycle-v1`。现有 Workspace Administration 窄领域入口新增有限 lifecycle application 与 P10 mechanism port；前者唯一拥有 `recovering/ready/degraded/draining/closed` 状态、prepare/commit 准入、committed 串行执行、业务失败完成、基础设施 retry/degraded、冷恢复与 close abort/safe-point，后者只提供 initialize/prepare/commit/complete/operation/oldestCommitted 耐久机制。具体 Executor、storage、capacity 与 Node error 仅在 CLI Correctness adapter 投影为有限 code/message/retryAfter，领域不导入 CLI、Executor、IPC、路径、文件存储或 Workscene。
- 生产链与旧路清理：三个 `createExecutorLocalWorkspaceHost` 根仍经同一个 factory 构造 A5-08 应用、一个 outbox 和一个 lifecycle application；`LocalWorkspaceManagementHost` 只启动/关闭 transport 与 lifecycle、承载 IPC 并转发有限调用。Host 原 ready gate、mutation tail、committed drain/recovery loop、retry/degraded 分类、attempt abort 与业务完成裁决全部删除；outbox 的 operation record/state 合同改用领域定义，仍唯一拥有 P10 原子日志、marker、digest、TTL 与状态转换，durable registry recovery owner 已改绑领域 lifecycle。初始化已成立而首次 committed 扫描可重试失败时由同一 drain 重驱，不会留下无恢复 owner 的永久 recovering。`pending/acknowledge` 与 client delivery 仅按已登记 A5-09b 临时桥原样保留。
- 直接证据与行为保护：领域 Workspace Administration 12/12（其中 lifecycle 4 项直接覆盖 prepare/commit/business completion、committed identity 冷恢复及首次扫描 retry、retry→成功/degraded、close abort 后保留 committed）；CLI Host/outbox 24/24 覆盖幂等与 response loss、旧记录、reset TTL/identity、串行 retry、业务完成、unknown degraded、close/restart；workspace command/bootstrap/owner 13/13；真实 durable registry 12/12（含 `local-workspace-operation-outbox` witness）。core fresh build、CLI typecheck 与 `pnpm cli:build`、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome 与 `git diff --check` 均通过；P10 wire/digest/marker、公开 status/error、退避、三生产根、pending/ack 与 client delivery 未发现漂移。
- 交接与精确失效：A5-09a 实施完成，等待协调者独立复核；Workspace Administration 行与 A5 继续为 `[ ]`，下一责任链固定为 A5-09b，不得进入 Workscene。领域 durable lifecycle 状态/端口/业务完成分类、CLI infrastructure observation、Host start/close/unpublish/request routing、outbox schema/atomic transition/recovery owner、三个生产根、P10 witness、core 窄导出/build、S7 或上述直接行为任一变化时，本证据精确失效并只重验本包闭包；pending/ack/client delivery 的变化只恢复 A5-09b。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从三个生产根、Host transport start/unpublish/close、领域 lifecycle、Correctness failure adapter 与 P10 outbox 双向反查，确认 ready/recovering/degraded、prepare/commit、committed drain、业务完成、retry/degraded、冷恢复与 close abort 只有一个领域应用 owner；Host 仅保留自身 started/closed 与 IPC 转发，outbox 仅保留 identity、原子状态转换、digest/marker、TTL 和重放机制。独立验证 core 12/12、CLI Host/outbox/command/bootstrap/owner 37/37、真实 S7 environment 1/1、durable registry 12/12、core fresh build、CLI typecheck/正式构建、canonical S7 31/31 + registry golden、fresh exports、最窄 Biome 与 `git diff --check` 全部通过。额外确认初始化成功但首次 committed 扫描遇可重试故障时，旧实现因 ready flag 已置位却只启动 initialize recovery 而可能永久停在 recovering；当前在同一 lifecycle 内直接重驱 committed drain，恢复既有 retry/重启语义，不改变 P10、公开入口、错误、退避或产品能力。接受 `A5-09a-workspace-administration-durable-lifecycle-v1`；Workspace Administration 行与 A5 继续为 `[ ]`，下一包只闭合 pending/ack 与 client result delivery。

### A5-09b：归位 Workspace Administration pending/ack 与结果交付生命周期

- 派发基线：`HEAD f46e15a0 + task-doc:A5-09b-dispatch`；A5-09a 已由协调者独立复核并提交，进场索引应为空，工作区只应有本文派发记录。
- 唯一架构结果：让 Workspace Administration 的领域应用唯一决定“哪些已完成操作属于同一可恢复交付前缀、当前调用结果是否被该前缀认领、哪些结果作为未认领恢复结果交给当前 Surface、何时形成有限 consumption credential、成功呈现后确认哪个连续前缀已交付”。P10 outbox 只提供 pending page 与 acknowledge 的耐久机制，Host 只做 IPC 编解码和有限调用转发，CLI/Workscene binding 只做调用、呈现与消费凭据，不再拥有前缀、认领、过滤、确认或恢复结果状态机。
- 必须迁移与删除：在现有 Workspace Administration 窄领域入口形成有限 delivery application/port，并由三个 `createExecutorLocalWorkspaceHost` 生产根装配同一实例；迁入并删除 CLI Host/client 中的 `readPendingDelivery`、`acknowledgeDelivery`、`consumptionCredentialOf`、credential matching、claimed/unclaimed filtering、recovered-result bookkeeping 和等价判断。Host 的 `pending/acknowledge` IPC 只可映射领域端口；outbox 的 contiguous prefix、outbox identity、result/input digest、ack marker、compaction 和旧记录兼容继续唯一归 P10 机制，不得搬进领域或 Surface。`workspace create-scene` 只消费有限 credential 并维持既有幂等 request identity，不进入 Workscene 领域迁移。
- 行为保护：原样保持 pending/ack wire、严格 codec、连续前缀与 digest/bytes 绑定、response loss、重启恢复、当前结果认领、其他已完成结果恢复呈现、失败结果消费、交付确认、重复确认、旧记录兼容、三生产根、全部公开文案/错误/终态以及 `create-scene` 的凭据消费和幂等语义；不得增加产品能力、通用 delivery 框架、网络协议或第二状态机。
- 最窄证据：先补领域 delivery application 的直接测试，覆盖当前结果成功/失败、无当前结果、claimed 与 unclaimed 分离、身份或 digest 不匹配拒绝、response-loss/restart 后恢复、确认 exact-prefix 与重复确认；再运行受影响的 CLI Host/outbox/client/command/REPL/create-scene 直接测试和对应 S7 environment/durable registry。源码稳定后按依赖顺序完成必要 core build、CLI typecheck/正式构建、canonical S7、fresh package exports、最窄 Biome 与 `git diff --check`；不得运行根级全测或重做 A5-06～A5-09a 未失效闭包。
- 完成与裁决：领域 application 是交付语义唯一 owner，Host/CLI/Workscene 无业务判断，P10 outbox 无产品语义，全部旧临时 binding 和平行判断归零；Workspace Administration 从身份、CRUD、reset/recovery、durable operation 到 delivery 的整条生产责任、全部消费端和直接证据闭合后，将登记表该行改为 `[x]`，并更新台账到下一真实 A5 责任链。若约四小时仍未完成、需要改变 P10 wire/marker/digest、公开错误或 Workscene 产品流程，或出现两个可独立迁移的生命周期，停在可构建、单一交付真相成立的安全检查点反馈，不得扩面。
- 固定边界：只处理 Workspace Administration 的 pending/ack 与结果交付；不进入 Conversation、Workscene、Schedule、Advancement、Trust、Device、Backup & Recovery、A6 或 A7，不修改 AE-001，不执行 Git 写操作。
- 执行基线与领域交付应用：`HEAD f46e15a0ff73a12828bbdafe6ad7a8ed68d157c9 + task-doc:A5-09b-dispatch + A5-09b-workspace-administration-result-delivery-v1`。唯一窄入口 `@zhixing/core/environment/workspace-administration` 新增 `WorkspaceAdministrationResultDeliveryApplicationService`，领域现拥有有限 `none/current/operation-input/operation` claim、未认领完成结果、当前 consumption credential、交付失败标记和 exact-prefix confirmation 决定。operation-input claim 直接从规范化 durable input 重建稳定 digest，current claim 绑定 outbox/localSeq/operationId/inputDigest/resultDigest；无进程缓存身份、Surface filter、第二状态机或通用 Delivery 框架。
- Host、Correctness 与旧路退场：`WorkspaceAdministrationDurableLifecycleApplication` 现是 Host 唯一有限入口，`pending/acknowledge` 只把严格 IPC 请求交给同一 lifecycle application 再访问 P10 mechanism；`LocalWorkspaceManagementHost` 不再持有 outbox delivery handle。每个 `createLocalWorkspaceClient` 只装配领域 result-delivery application 与严格 IPC mechanism adapter，CLI 保留 wire codec、调用和中文呈现，Workscene create-scene 只消费领域 credential 形成既有稳定 request identity。旧 `readPendingDelivery`、`acknowledgeDelivery`、`consumptionCredentialOf`、credential matcher、claimed/unclaimed filter、recovered bookkeeping、CLI error/credential owner、`delivery: outbox` 与 Host `#delivery` 均归零。P10 outbox 继续唯一拥有物理 localSeq 链、outbox identity、input/result digest、ack watermark、prefix digest 校验、marker/compaction 与旧记录机制；领域只裁决该机制返回的连续终态记录如何归属本次产品交付。
- 行为与恢复保护：pending/ack wire 与严格 codec 未改；领域应用只接受从 confirmation watermark 开始的连续 completed/abandoned 终态记录，abandoned 进入确认前缀但不形成用户恢复结果。当前成功或失败结果只有在同 outbox exact identity/digest 内才形成 credential；其他 completed 仍先以原错误名、code、中文文案交给 Surface，确认失败或 response loss 保留当前 pending/credential，同一确认可重驱，成功后才清空；重启可由 durable operation input/identity 重建 claim。list/status/reset-preview 的新读取会先放弃旧 current claim，view 则保留并重新核对 current，create-scene credential、失败 deliveryConfirmed、旧 two-field control authorization、reset TTL/identity 与三生产根行为未改。
- 直接证据、状态与失效：领域 Workspace Administration 16/16，其中新增 result-delivery 4 项直接覆盖 claimed/unclaimed、重启重建、空前缀与身份/digest mismatch、ack response loss/exact prefix/重复确认；CLI Host/outbox/client/command/bootstrap/owner、真实 S7 environment 与 durable registry 共 7 文件 50/50，覆盖成功/失败/current-view、其他恢复结果、旧记录、reset、三根和 P10 witness。core fresh build、CLI `tsc --noEmit` 与正式 build、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome 与 `git diff --check` 通过。S7 反向 mutation 会拒绝 Host 直连 outbox pending、领域 current claim 缺失、旧 helper/error owner 或第二 delivery handle 回流。A5-09b 实施完成，等待协调者独立复核；Workspace Administration 从身份、CRUD、reset/recovery、durable operation 到 result delivery 的完整责任与消费链当前闭合，故登记行改为 `[x]`，A5 仍为 `[ ]`。若领域 delivery claim/credential/error、Host lifecycle pending/ack、IPC codec、P10 pending/ack schema/digest/marker/compaction、client/Workscene consumption、三根、S7/exports 或上述直接行为任一变化，恢复 `A5-09b-workspace-administration-result-delivery-v1` 与 Workspace Administration 行并只重验本闭包；未进入 Workscene 或其他领域，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从领域 delivery application、Host lifecycle 转发、客户端调用/呈现、Workscene credential 消费和 P10 outbox 双向反查，确认结果认领、未认领恢复、有限凭据与 exact-prefix 确认只有一个领域 owner；Host 无 pending/ack 之外的交付判断，CLI 无旧 helper/错误 owner，outbox 无产品状态机。独立重跑领域 16/16、CLI Host/outbox/command/bootstrap/owner 37/37、core/CLI 正式构建、CLI typecheck、canonical S7 31/31 + registry golden、fresh package exports、最窄 Biome、旧 owner 残留反扫与 `git diff --check`，全部通过。接受 `A5-09b-workspace-administration-result-delivery-v1`，Workspace Administration 行保持 `[x]`；A5 继续为 `[ ]`。

### A5-10a：归位 Trust Administration 用户管理应用与 Product API binding

- 派发基线：`HEAD ab31fb9f + task-doc:A5-10a-dispatch`；Workspace Administration 已完整归位并提交，进场索引应为空，工作区只应有本文派发记录。
- 唯一架构结果：把现有 `/trust` 的“按当前对话语境列出用户可管理规则、撤销一条可见规则”形成 Trust Administration 拥有的 Query/Command、规则投影、语境解析、应用用例与 Product API contribution；`trust.list/revoke` RPC 和 REPL/typeahead 只做认证、codec、调用、错误映射与呈现。Security 只通过窄 repository adapter 提供既有规则存取机制，不再决定什么规则可管理、如何从 conversation/workspace/scene 得到管理语境或 not-found 产品语义。
- 必须迁移与删除：建立不依赖 CLI、Server、Provider、文件路径、`PermissionStore`、`SecurityPipeline` 或 Mesh 的领域窄入口；以当前 main/workspace/scene 语义和默认 workspace 投影解析 `conversationId?`，由应用过滤 builtin、验证撤销目标属于当前可见集合并在成功提交后形成有限事实。将两个 Product API operation 加入现有 sealed exact-set 与唯一组合根，RPC handler 改为调用同一 dispatcher；删除 Server `TrustDirectory` 平行合同、`ServerContext.trust` 服务定位和 handler 直接目录调用。CLI 只保留 PermissionStore repository adapter 与默认语境投影，`RpcManagementFacade`、`/trust`、typeahead 继续消费原 RPC。
- 临时桥与边界：`A5-TRUST-STORE-01` 只允许 repository adapter 把领域 context/rule 投影映射到当前 PermissionStore 的 list/revoke；A5-10a 不迁移确认链创建/沉淀、session/context/global 持久化机制、Security match/frozen snapshot、AI steward、builtin 执行规则或设备信任链。不得把 `PermissionRule`/`PermissionContextId` 直接提升为新的领域公共合同，也不得建立通用权限/策略框架；这些旧写入与执行交界必须由紧邻 A5-10b 退场或收窄。
- 行为保护：严格保持 `trust.list/revoke` 方法名、认证、参数与响应形状、invalid params/not-found 错误，main/workspace/scene/global 可见性、builtin/session 边界、撤销耐久性、活跃实例最终一致语义，以及 `/trust` 文案、typeahead、删除手势和所有模式可达性；不得改变权限审批、安全执行、规则冲突和设备信任产品行为。
- 最窄证据：新增领域应用/Product API 直接测试，覆盖 main/workspace/scene 语境、global 合并、builtin 排除、可见撤销、跨语境/不存在拒绝、提交后事实与 sealed exact-set；重跑 Server 两个 RPC 的认证/codec/error、CLI repository adapter、facade、`/trust`/typeahead 直接测试。源码稳定后按依赖顺序完成必要 core/server/CLI build 或 typecheck、canonical S7、fresh package exports、最窄 Biome 与 `git diff --check`；不运行根级全测、正式制品或无关 Security/Device/DR 回归。
- 完成与交接：领域应用是用户信任管理语义唯一 owner，Product API 是唯一应用入口，Server/CLI 无业务判断，旧 `TrustDirectory`/`ServerContext.trust`/直接调用归零，当前行为证据有效；Trust Administration 行继续为 `[ ]`，下一包只处理规则写入/沉淀与 Security 执行投影并删除 `A5-TRUST-STORE-01`。若约四小时仍未完成、需要改变公开 RPC/REPL、权限审批结果、PermissionStore 持久格式或设备信任链，停在可构建、单一管理入口成立的安全检查点反馈，不得扩面。
- 固定不做：Conversation、Workscene、Schedule、Advancement、Device、Backup & Recovery、A6、A7、AE-001 修改、新能力、设备 trust chain、通用授权平台或 Git 写操作。
- 执行基线与领域责任：`HEAD ab31fb9feecc5038a3143b86d99d0c8bd08d4ea4 + task-doc:A5-10a-dispatch + A5-10a-trust-administration-management-v1`。新增唯一窄入口 `@zhixing/core/trust-administration`，由 `TrustAdministrationApplicationService` 拥有 main/workspace/scene 管理语境、global 合并、builtin/session 排除、可见撤销、not-found 与提交后 `trust-administration-rule-revoked` 事实；公开规则投影只允许 context/global，临时 repository 投影才允许 Security 的四类 scope。领域不导入 CLI、Server、Provider、路径、`PermissionStore`、Security 实现或 Mesh，也未从 core 根导出。
- Product API、Host 与旧路退场：Trust 的 list Query、revoke Command 和 Fact Event contribution 与 Skill/Delivery contribution 进入同一个 sealed `ProductApiDispatcher`；唯一 Anchor 组合根构造一次领域应用并注入同一 dispatcher。`trust.list/revoke` handler 只保留认证、wire 参数、Product API 调用和 not-found 映射；Server `TrustDirectory`、`ServerContext.trust`、CLI `createTrustDirectory`、两个 `management-directories.ts` 及旧直调测试均删除。`RpcManagementFacade`、`/trust` 与 typeahead 改为消费领域只读投影，公开 RPC 名称、参数/响应、认证、invalid/not-found、中文呈现、删除手势和 active-instance 最终一致行为未变。
- 临时桥与行为保护：`A5-TRUST-STORE-01` 现只有 `trust-administration-adapter.ts` 一处，单向把领域 context/repository rule 映射到既有 `PermissionStore` list/revoke，并以原 `resolveWorkspace`、session type 与 workspace hash 形成默认语境；管理可见性、conversation scene 解析和撤销资格均不在 adapter。规则创建/确认沉淀、session/context/global 耐久机制、Security match/frozen snapshot、AI steward、builtin 与设备信任链未迁移，桥的唯一退场包仍为 A5-10b，故 Trust Administration 行与 A5 继续为 `[ ]`。
- 直接证据与验证：领域应用/Product API 4/4 覆盖 workspace/scene/global、builtin/session 排除、可见/跨语境撤销、repository race、提交后事实与 sealed exact-set；Server 真实 dispatcher/RPC 9/9 覆盖认证、strict params、list/revoke、not-found 与缺 contribution fail closed；CLI adapter、Host composition、`/trust`、typeahead、facade、candidate delete 共 6 文件 34/34。core/server/CLI `tsc --noEmit`、core/server/CLI 正式 build、canonical S7 32/32 + registry golden、fresh package exports 均通过；最窄 Biome 与 `git diff --check` 在交接检查通过。结构反例会拒绝领域反向依赖 Security、handler 绕过 Product API、`ServerContext.trust`/旧目录回流、adapter 重获过滤决定、Host 漏 contribution、core 根导出或第二窄入口。
- 交接与精确失效：A5-10a 实施完成，等待协调者独立复核；无技术阻塞，下一责任链固定为 A5-10b。Trust 管理 context/rule/Query/Command/Fact、repository bridge、Product API exact-set/Host composition、RPC codec/error、REPL/typeahead 消费、core 窄导出/build、S7 或上述直接行为任一变化时，精确恢复 `A5-10a-trust-administration-management-v1` 并只重验本闭包；规则写入/沉淀与 Security 执行投影变化归 A5-10b。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从 Trust Administration 应用、PermissionStore 窄适配、唯一 Host 组合、Product API dispatcher、两个 RPC binding、REPL/typeahead 与 package/结构门禁双向反查，确认管理语境、用户可见性、撤销资格、not-found 和提交后事实只有一个领域 owner；Server/CLI 无平行业务目录，Security 当前只提供登记退场点明确的存取机制。独立完成 core 4/4、Server 9/9、CLI 6 文件 34/34，依次重建 core、Server、CLI 并通过 CLI typecheck、canonical S7 32/32 + registry golden、fresh package exports、最窄 Biome、旧入口残留反扫与 `git diff --check`；公开 RPC、中文呈现、耐久撤销和活跃实例最终一致边界未发现漂移。接受 `A5-10a-trust-administration-management-v1`；Trust Administration 行与 A5 继续为 `[ ]`，下一包只处理规则写入/沉淀与 Security 执行投影并退场 `A5-TRUST-STORE-01`。

### A5-10b：归位用户信任规则写入、沉淀与 Security 执行投影

- 派发基线：`HEAD 614f598c + task-doc:A5-10b-dispatch`；A5-10a 已独立验收并提交，进场索引与工作区应只有本文派发记录。
- 唯一架构结果：让 Trust Administration 成为用户信任规则从贡献累计、显式准入、自动沉淀、规则身份/元数据、耐久写入到管理 Query/Command 的唯一应用 owner；Security Substrate 只消费不可变的匹配/执行快照并继续负责策略、安全判断、确认与效果前准入。`secure-executor` 只能把用户/安全助理已经形成的有限决定和当前执行语境交给 Trust 应用，不再创建规则、解释作用域或直接操作可变 `PermissionStore`/沉淀 tracker。
- 必须迁移与退场：把 `allow-once`/steward-safe 的贡献累计与阈值沉淀，以及 `allow-session/context/global` 的显式规则创建，收进 A5-10a 已建立的领域窄入口；领域拥有规则 ID、时间、contributors、contextPath、scope 与沉淀结果，存储适配器只映射当前耐久机制。把 pattern 建议/代表粒度与 tracker 归到唯一 Trust 责任，同时保留 Confirmation 对相同只读建议合同的消费；删除 Security 对 tracker 的所有权、`secure-executor` 的 `PermissionStore.createRule`/直接 `create`、退役 `listUserTrustRules` 与无消费者出口。将 `SecurityPipeline` 收窄为匹配/冻结快照所需的只读执行端口；`RuntimeSecuritySnapshot` 的用户规则与累计观察改从 Trust 应用投影，分布式 permission snapshot 继续来自同一底层规则事实。退场 `A5-TRUST-STORE-01` 的临时/平行语义；允许保留一个不含产品判断的最终 repository→现有存储机制适配器，但不得保留第二应用、双写、Security 写入口或临时桥标记。
- 行为保护：严格保持建议模式及顺序、low/medium 3 次、high 10 次、critical 永不自动沉淀、用户与 steward 平权累计、最高风险观察与完整贡献时间线、bypass-immune 不沉淀；沉淀阈值仍由当前这次审批的风险等级决定，累计最高风险只用于观察投影，不得改成跨次阈值。`allow-session` 只活在会话内，context/global 的落盘位置、冲突与撤销、workspace/scene/main 绑定、显式规则单条 user contributor、自动沉淀事件、显式确认不发该事件、活跃实例最终一致与 hot reload 复用均不得改变。保持权限审批、AI steward、策略优先级、builtin 规则、match/matchFrozen、工具效果前复验、`/security` 投影、Assignment/Executor 签名快照和设备信任链行为不变。
- 最窄证据：以领域直接测试证明显式三作用域、allow-once/steward 累计、各风险阈值、代表 pattern、bypass/critical、context/global/session 写入、提交后沉淀结果与观察投影；以 secure-executor 真实链证明不再直写且允许/拒绝/确认/审计行为等价；以 SecurityPipeline/Runtime 直接测试证明只读匹配、frozen snapshot、`/security` 状态与运行时换代不漂移，并重跑受影响的 permission snapshot/Assignment 最窄链。更新 S7/exports 结构证据，必须能拒绝 Security/secure-executor 重获用户规则写权、旧 tracker/list helper 回流、领域依赖 Security 实现、第二规则应用或桥标记残留。源码稳定后按上游到下游完成必要 build/typecheck、canonical S7、fresh package exports、最窄 Biome 与 `git diff --check`；不运行根级全测、正式制品、Device/DR 或无关领域回归。
- 完成与安全交接：所有用户信任规则生命周期只有一个 Trust 应用责任，Security 只有有限只读执行投影，A5-10a 管理链与现有安全/分布式行为同时成立，`A5-TRUST-STORE-01`、旧写入口和旧 helper 全部归零后，才可把 Trust Administration 行改为 `[x]`。若约四小时仍未完成、需要改变公开确认/RPC/REPL、持久格式、权限结果、快照签名或设备信任链，停在可构建、可运行且用户规则只有一个写入 owner 的安全检查点反馈，不得扩面或进入其他领域。
- 固定不做：Conversation、Workscene、Schedule、Advancement、Device、Backup & Recovery、A6、A7、AE-001 修改、新产品能力、通用授权/策略平台、PermissionStore 持久格式重写、设备 trust chain 或 Git 写操作。
- 执行基线与责任归位：`HEAD 614f598ca01338b132377ebb3cdde17a3c524672 + task-doc:A5-10b-dispatch + A5-10b-trust-administration-execution-v1`，进场索引为空且仅本文协调台账差异。新增 `TrustAdministrationExecutionApplicationService` 作为显式 `allow-session/context/global`、allow-once/steward contribution、建议/代表 pattern、风险阈值、最高风险、完整 contributors、规则 identity/time/context binding、沉淀结果、运行状态与签名 permission snapshot 的唯一应用 owner；A5-10a 管理 Query/Command 与该执行应用共享同一领域规则投影。唯一最终 repository adapter 只做 Trust context/rule 与现有 PermissionStore 机制的映射，不决定 scope、阈值、身份、元数据或沉淀结果，未改变 P04 物理格式。
- Security 与生产链收口：`SecurityPipeline` 只取得预绑定 context 的 `PermissionRuleExecutionSource.match/matchFrozen`，不再持有 PermissionStore、ConfirmationTracker、workspace/context getter 或用户规则写口；`secure-executor` 只提交用户/安全助理决定并仅对领域返回的 `rule-sedimented` 发审计事件，不创建规则、不解释 scope。主运行、Task child、orchestration child 共用同一 Trust 应用；`RuntimeSecuritySnapshot` 的用户规则/观察及 durable Assignment permission snapshot 均由该应用从同一底层事实投影。旧 `confirmation-tracker.ts`、`trust-rules.ts`、`listUserTrustRules`、Security 写入口与 `A5-TRUST-STORE-01` 临时语义全部退场；context/global 持久、session 内存、builtin 执行、match/frozen match、效果前复验和管理撤销仍沿既有机制。
- 行为与直接证据：领域直接证据覆盖建议顺序、三作用域、main/workspace/scene 绑定、显式 contributor、low/medium=3、high=10、critical/bypass 不沉淀、用户/steward 平权、最高风险、完整时间线、冻结观察/规则投影；真实文件 adapter 证明 context/global 跨重启而 session 不落盘及管理撤销。core 定向结果为既有 5 文件 99/99 加最终 adapter 1/1；orchestrator 为 secure-executor/permission E2E 34/34、四条 child propagation 107/107、create-agent-runtime 既有 72/72 加新增签名快照投影 1/1、拒绝回流 3/3。依赖顺序 fresh `@zhixing/core`、`@zhixing/orchestrator`、`@zhixing/server` 与 CLI build 均通过；canonical S7 32/32 与 registry golden、fresh `runtime:package-exports`、29 个变更源码/测试/脚本的最窄 Biome 及 `git diff --check` 通过。CLI 无独立 typecheck script，CLI build 的 DTS/ESM 边界即本包类型/构建证据。
- 交接与精确失效：A5-10b 实施完成，Trust Administration 行标为 `[x]`，A5 仍为 `[ ]` 并等待协调者独立复核；无技术阻塞、无打开桥。Trust execution/management contract、建议/阈值/贡献与 scope 规则、最终 repository adapter、PermissionStore schema/mechanism、Security 只读 source、secure-executor decision submission、Task/编排传播、runtime security/assignment snapshot、确认审计、S7 或窄导出任一变化时，精确恢复 `A5-10b-trust-administration-execution-v1` 与 Trust Administration 行；P04 物理格式变化还须恢复 A0 持久证据。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立复核反证：迁移前 `ConfirmationTracker.shouldSuggest(request, riskLevel)` 始终按当前审批的 `riskLevel` 选择阈值，`highestRisk` 只进入 `/security` 观察快照；本轮实现却按累计 `entry.highestRisk` 选择阈值，并用测试固化了“high 一次后后续 low/medium 仍等待 10 次”的新行为，构成架构迁移禁止的权限沉淀语义回退。另有可信本机 `confirmation.resolve` 当前允许 `argument: ""` 的显式 pattern，迁移前可创建该精确规则，新领域边界在耐久 resolve 后才以非空检查拒绝，形成公开确认路径漂移。A5-10b 与 Trust Administration 行已恢复为 `[ ]`；只纠正这两个既有行为并重验直接闭包，其他已核实结构不重做。
- 原边界纠正与证据恢复：`TrustAdministrationExecutionApplicationService` 的沉淀阈值恢复为当前 `approval.riskLevel`；累计 `highestRisk` 继续更新且只进入 `/security` observation，mixed `high(user) → low(steward) → low(user)` 因当前 low 的第三次贡献产生规则，同时完整时间线与最高风险 high 均保留。显式 `allow-session/context/global` 的 tool 仍须非空，argument 则按可信 `confirmation.resolve` 的既有合同接受任意 string（包括空字符串），领域与 repository 不再在耐久 resolve 后追加收紧；非 string 仍 fail closed。这两项仅恢复迁移前行为，不包装成新能力或宽松兼容承诺。
- 纠正直接证据与交接：core Trust execution 1 文件 9/9、Server 真实 `confirmation.resolve` 1 文件 26/26、orchestrator `secure-executor` 1 文件 33/33 均通过，分别直接锁定 mixed-risk 当前阈值/最高风险观察分离、三类可信 wire 空 argument 原样进入 broker、以及三作用域规则写入后实际执行成功。依赖顺序 fresh `@zhixing/core`、`@zhixing/orchestrator` 与 CLI build 通过；canonical S7 32/32 与 registry golden、4 个本轮纠正文件的最窄 Biome 及 `git diff --check` 通过。此前 A5-10b 其余 owner、只读执行投影、管理/快照/child propagation 与 package-export 证据输入未变并继续有效；`A5-10b-trust-administration-execution-v1` 与 Trust Administration 行恢复为 `[x]`，A5 仍为 `[ ]`，等待协调者独立复核。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从可信确认 wire、Trust execution 应用、最终 PermissionStore 适配、SecurityPipeline 只读 source、secure-executor、主/Task/编排 child 传播、runtime security/assignment snapshot 与 S7 负向门禁双向复核，确认两项反证已恢复迁移前行为，用户规则生命周期只有一个 Trust 应用 owner，Security 无可变写口，旧 tracker/helper 与临时桥归零。独立运行 core 4 文件 57/57、orchestrator 4 文件 113/113、Server confirmation 26/26、CLI 管理消费 3 文件 12/12，另以单文件纠正集 68/68 交叉取证；canonical S7 32/32、registry golden、fresh package exports、最窄 Biome 与 `git diff --check` 通过。接受 `A5-10b-trust-administration-execution-v1`，Trust Administration 保持 `[x]`，A5 继续 `[ ]`。

### A5-11a：归位用户调度定义的管理责任

- 派发基线：`HEAD ba1df018 + task-doc:A5-11a-dispatch`；A5-10b 已独立验收并提交，进场索引为空且工作区应只有本文派发记录。
- 唯一架构结果：让 Schedule Domain 成为用户调度定义 `list/create/update/delete` 的唯一应用 owner，统一拥有任务定义默认值与校验、稳定 operation identity、revision 并发前置、用户/系统任务边界、not-found/冲突结果和提交后的任务投影；RPC、Agent schedule 工具、本机与远端 facade 只做各自真实存在的 wire、身份、调用和呈现，不再解释或复制调度管理规则。
- 必须迁移与退场：建立有限的 Schedule Command/Query 与应用服务，并把其现有管理入口接入已证明的 Product API 模式；从 `schedule.*` RPC handler 移出 spec 默认、产品校验、not-found/system-task 错误判断，从 `LocalSchedulerFacade`、`ExecutionSchedulerFacade` 与 Agent schedule 工具反查所有直接管理消费端，使它们最终调用同一个领域用例或只作当前 execution/assignment context 的窄适配。现有 Scheduler/Anchor authority 只经一个不含产品决定的 repository/commit 端口提供耐久机制；不得保留第二管理应用、Surface 直写、facade 自有任务规则或第二 revision 状态机。
- 行为保护：公开 `schedule.list/create/update/delete` 方法、参数 exact-set、认证、稳定 requestId/surface principal、默认 enabled/priority、definition validation、taskRevision 乐观并发、系统任务不可删、not-found/invalid/conflict 错误、返回投影与操作重放必须 byte/语义等价。保持 Agent 工具的本机/assignment staged mutation、RPC facade、REPL/Channel 投影、内部维护任务过滤及 schedule run/abort/trigger/recovery/event/Delivery 行为不变；不得让管理迁移绕过 durable control principal 或把 execution/Authority/Mesh 术语暴露给领域和用户。
- 临时桥与边界：登记 `A5-SCHEDULE-RUNTIME-01`，只允许新领域应用经一个窄端口委托当前 Scheduler/Anchor 权威机制完成已经裁决的写入与读取；该桥不得决定默认值、校验、可删性、错误或用户投影，不得双写。唯一退场包为紧邻 A5-11b，负责 run/abort、定时触发、恢复、运行事件及 Kernel/Delivery 效果交界；本包不得提前迁移这些执行状态机，也不得勾选 Schedule 行。
- 最窄证据：领域直接测试覆盖 list/create/update/delete 的默认、校验、revision、重放、not-found、system-task 与 committed projection；Server 真实 handler/Product API 测试证明四个管理 RPC 只保留 binding 且公开行为等价；Agent schedule 工具、Local/RPC/Execution facade 的直接测试证明全部管理消费端共享唯一应用语义，assignment staged 写入不回退。更新 S7/Product API exact-set 与负向结构证据，使其能拒绝 handler/facade 重获产品规则、第二 Schedule 应用或 management write 绕过；源码稳定后只按失效闭包完成必要上游构建、最窄测试、canonical S7、fresh package exports、最窄 Biome 与 `git diff --check`，不运行根级全测、正式制品或无关领域回归。
- 完成与安全交接：只有用户调度定义管理的生产端、全部直接消费端、旧管理规则与入口、直接测试和 Product API/结构门禁同时闭合，且运行、恢复、投递和公开行为未改变时，才算 A5-11a 完成；Schedule 行继续 `[ ]`。约四小时仍未完成、发现管理与执行无法在可构建单一真相下分离、需要改变公开协议/持久 schema/运行终态或影响其他领域时，停在可构建、可运行且管理写入只有一个 owner 的检查点反馈，不得扩面。
- 固定不做：Conversation、Workscene、Advancement、Device、Backup & Recovery、A6、A7、AE-001 修改、新调度能力、cron/重试策略调整、持久 schema 重写、执行/恢复状态机迁移或 Git 写操作。
- 实施基线与唯一领域入口：进场为 `HEAD ba1df01851c62b25e7d682bae1fbeaab6baa52de + task-doc:A5-11a-dispatch`，索引为空。新增唯一窄入口 `@zhixing/core/scheduler/application`，由 `ScheduleManagementApplicationService` 拥有有限 `list/create/update/delete` Query/Command、默认 `enabled=true`/`priority=normal`、完整 definition 校验、稳定 operation identity、observed revision 必填/绑定、用户/系统边界、not-found/conflict 与提交后投影；真正的 revision CAS 与同 operation exact replay 仍由窄 Correctness commit 机制原子区分，因此 update/delete 响应丢失后不会被应用层的旧 revision 前查误挡，也没有第二 revision 状态机。Product API exact-set 为一项 Query、三项 Command、零 Fact Event，未从 core 根新增转导或第二应用入口。
- 生产链与 Correctness 边界：Anchor 的 `ScheduleManagementRepository` 只接受领域已裁决的完整 `TaskSpec`、operation identity、surface principal 与 expected revision，并经既有 GlobalState/Authority 机制串行提交；读取只返回未做用户/system过滤与排序的 raw task projection，repository 不再决定默认值、merge、可见性、可删性或产品错误。唯一 Host Product API dispatcher 同时组合 Schedule 与既有 contributions；四个管理 RPC 只保留认证、wire 参数/身份映射、dispatcher 调用及既有错误/返回投影。`LocalSchedulerFacade` 以同一应用服务管理定义，`ExecutionSchedulerFacade` 只把 assignment context 适配为 repository/staged mutation，RPC facade 只处理 wire；Agent schedule 工具移除 system filter 与 create 默认，继续只负责 schema、文案、operation id 和呈现。
- 旧路退场与行为保护：`SchedulerBackend` 与 Anchor product port 的公开管理 CRUD 入口归零，Server handler 的 spec 默认、`validateTaskDefinition`、not-found/system 判断及直接 scheduler 管理调用归零，工具/facade 不再复制默认、merge、revision 或 system-task 规则。公开四个 RPC、request/surface identity、Agent tool 文案、本机与 assignment staged read-own-writes、durable replay、内部 system task、run/abort/trigger/recovery/event/Delivery 语义和持久 record 均未改变；当前 `SchedulerBackend` 只保留 start/stop/run/get/abort 的执行桥，仍登记为 `A5-SCHEDULE-RUNTIME-01`，唯一退场点为紧邻 A5-11b。
- 直接证据：Schedule Domain 与 Local facade 2 文件 8/8，owner-kernel Authority/GlobalState/运行隔离 2 文件 15/15，Server 真实 dispatcher/binding 1 文件 11/11，Agent tool 1 文件 4/4，RPC/assignment facade 2 文件 8/8，合计 8 文件 46/46；覆盖默认/校验/replay/committed projection、revision/not-found/system、raw Correctness projection、Product API exact-set、durable principal、四个 wire binding、缺 contribution fail closed、工具呈现、assignment staged read-own-writes 与 system guard。受影响 core、owner-kernel、server、tools-builtin、CLI 依赖构建及 CLI typecheck 通过；最终 canonical S7 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 通过。
- 结构、失效与交接：S7 与 package-export 门禁冻结 Schedule 应用唯一窄 export/build entry、四操作 exact-set、Host 唯一 contribution、handler/facade/tool/Correctness 责任差集，并以反向 mutation 拒绝 Server 直写、facade 重获 system/default 规则、第二应用 export 或 core 根泄漏。若 Schedule application Query/Command/default/validation/revision/system/error、repository/GlobalState adapter、任一 management facade/tool/RPC binding、Host contribution、S7 或 package export 变化，本记录失效并只重验上述闭包。A5-11a 当前完成并等待协调者复核；A5 与 Schedule 行继续 `[ ]`，下一检查点仅为 A5-11b 的运行/触发/恢复/事件与 Kernel/Delivery 效果链，不进入其他领域。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者复核反证：迁移前真实生产组合根使用 `AnchorSchedulerProductPort`，其 update/delete 对系统任务均抛出 `Cannot modify system task: <taskId>`；旧 Server binding 只把 not-found 转为 `NOT_FOUND`，并未把该错误转为 `INVALID_PARAMS`。非法 update spec 同样沿通用内部错误路径；空字符串 update/delete id 则由 authority 判为 `Task not found`。当前 `mapScheduleManagementError` 把 system-task 和全部 invalid-command 统一转为 `INVALID_PARAMS`，且应用先拒绝空 id，造成三个可观察 RPC 错误语义回退。A5-11a 不予接受；纠正只允许恢复上述既有映射并增加可识别的应用与真实 binding 回归，不得改变已成立的领域 owner、Product API、Correctness 适配、公开方法/返回或扩大到 A5-11b。纠正后只重验 Schedule 应用、四个管理 RPC、相关 S7/Biome/diff 闭包。
- 原包纠正与证据恢复：领域应用继续保留 `not-found / system-task / invalid-command / conflict` 的内部分类，但 update/delete 的字符串 id（含空字符串）先进入权威查询，因此空 id 恢复为 `Task not found: `；系统任务仍产生 `Cannot modify system task: <taskId>`；update 的无效完整 definition 继续由既有 validator 形成原错误。Server binding 只按方法恢复迁移前 wire：create 的无效 spec 仍映射 `INVALID_PARAMS`，update 的 invalid-command 与 update/delete 的 system-task 均交给通用 RPC 转换形成 `INTERNAL_ERROR / Internal error` 并保留原错误 data，not-found 仍映射 `NOT_FOUND`。本纠正没有恢复旧管理 owner、第二写入口或扩大 `A5-SCHEDULE-RUNTIME-01`。
- 纠正直接证据与失效边界：Schedule Domain/Local 2 文件 8/8 直接锁定空 id not-found、系统任务精确分类与既有管理行为；Server 真实 Product API + `RpcDispatcher` 1 文件 12/12 直接锁定 system update/delete、非法 update、空 id update/delete 与非法 create 的精确 code/message/data。因跨包测试必须消费 fresh core 产物，仅重建 `@zhixing/core`，ESM/DTS 通过；随后 canonical S7 32/32 与 registry golden、纠正文件最窄 Biome 和 `git diff --check` 通过，并新增反向 mutation 拒绝 system-task/非法 update 回流 `INVALID_PARAMS` 或空 id 被应用层提前拒绝。初次 8 文件 46/46 中未受错误映射影响的证据继续有效，不能与本纠正计数重复相加。以后若 Schedule application 的 task-id/definition 接受域、领域错误分类、Server 分方法错误映射、通用 dispatcher conversion 或对应 S7 约束变化，只恢复本纠正闭包；A5-11a 现等待协调者独立复核，A5 与 Schedule 行仍为 `[ ]`，未进入 A5-11b。
- 协调者独立验收：从唯一 Schedule management application、Anchor repository/GlobalState 机制适配、Host Product API composition、四个 RPC、Local/RPC/Execution facade 和 Agent tool 双向反查，确认默认、校验、用户可见性、merge、revision、system guard 与产品错误只有一个领域 owner；Correctness 只提交完整已裁决 spec，Server/工具/facade 无第二规则或管理写入口，临时 `SchedulerBackend` 只剩 A5-11b 的执行职责。独立验证领域/Local 8/8、Owner/Correctness 15/15、真实 RPC dispatcher 12/12、Agent tool 4/4、RPC/Execution facade 8/8、canonical S7 32/32 + registry golden、最窄 Biome、旧管理入口反扫与 `git diff --check` 全部通过；三个公开错误反证均恢复。接受 `A5-11a-schedule-definition-management-v1`；Schedule 行与 A5 继续 `[ ]`，`A5-SCHEDULE-RUNTIME-01` 保持打开并必须由紧邻 A5-11b 退场。

### A5-11b1：归位 Schedule 手动运行与取消控制

- 派发基线：`HEAD d14ae976 + task-doc:A5-11b1-dispatch`；A5-11a 已独立验收并提交，进场索引与工作区除本文调度记录外应为空。
- 唯一架构结果：Schedule Domain 成为用户手动 `run` 与 `abortRun` 的唯一产品应用边界；稳定操作身份、用户任务可运行性、not-found/ghost abort、控制结果与公开错误投影只由该应用裁决。RPC、Agent/本机 facade 和其他表面只做身份、wire 与呈现 binding；现有 Anchor/Job Journal/Assignment/取消机制只能作为窄执行端口消费已裁决命令，不再被 ServerContext、Surface 或 facade 当作产品服务直接调用。
- 生产与消费闭包：沿 `schedule.run`、`schedule.abortRun`、`LocalSchedulerFacade.run`、RPC facade、Agent schedule 工具及 Host contribution 双向迁移；把两项 Command 纳入现有唯一 Schedule Product API exact-set 和同一应用，不新建第二 Schedule application。删除 Server `requireScheduler` 对手动控制的服务定位、`SchedulerBackend.runTask/abortRun` 的表面直达及相同产品判断；允许执行端口暂时委托当前 durable control、激活、等待结果和取消机制，但不得在领域复制 Job Journal、Assignment、Kernel 或 Delivery 状态机。
- 行为保护：公开 RPC 名称、参数严格性、稳定 `requestId` 与 durable principal、返回结构、任务不存在、系统任务、重复请求、未知 run、取消中/已终态、响应丢失重放、Agent 工具等待结果以及本机/RPC 等价行为必须与迁移前全等；不得改变定时触发、启动恢复、事件顺序/可见性、Delivery、Kernel、持久 schema 或生命周期。
- 最窄证据与结构门禁：新增应用级 run/abort 正常、拒绝、重放与 ghost/cancel 直接测试；通过真实 Product API dispatcher 覆盖两个 RPC 的精确 wire/error；覆盖 Local/RPC/工具实际消费链，并运行受影响包的最窄测试、必要上游构建、canonical S7、package exports、Biome 与 `git diff --check`。S7 必须反向拒绝 Server/facade 重新直调 Scheduler runtime、第二执行应用或两项 Command 漏出唯一 exact-set；不得重复根级回归或进入 A5-11b2 验收。
- 完成与安全交接：只有手动 run/abort 的产品责任、全部直接 binding、旧直达入口和直接证据同时闭合，且 `A5-SCHEDULE-RUNTIME-01` 已缩窄为纯定时/恢复/事件/效果机制时，A5-11b1 才完成；Schedule 行与 A5 继续 `[ ]`。约四小时仍未完成、出现多个独立执行状态机、需要改变公开/持久合同或影响其他领域时，停在可构建、可运行且手动控制只有一个 owner 的检查点反馈，不得扩入 A5-11b2。
- 实施基线与唯一应用入口：进场为 `HEAD d14ae976f7def0e1a5e7c286f9e6dc7c314317b5 + task-doc:A5-11b1-dispatch`，索引为空。现有 `@zhixing/core/scheduler/application` 在同一个 `ScheduleManagementApplicationService` 内新增 `run / abort-run` 两项有限 Command；Product API exact-set 由一项 Query、三项定义 Command 扩为一项 Query、五项 Command、零 Fact Event，没有第二 Schedule application、第二导出或新协议。领域先冻结稳定 operation/surface identity，手动 run 统一裁决 user/system 与 not-found，abort 将机制返回的 ghost/cancel/terminal 结果投影为既有 boolean；执行只经 `ScheduleManualExecutionPort` 委托现有 Job Journal/Assignment/取消与结果等待机制。
- 生产绑定与旧路退场：Anchor `AnchorSchedulerProductPort` 是 `ScheduleManualExecutionPort` 的唯一生产适配，保持同一 durable requestId、principal/ingress、Job Journal applyControl、结果等待与取消机制；Host 只把该窄端口交给唯一 Schedule application。`SchedulerBackend.runTask/abortRun` 与 Server `requireScheduler` 手动控制服务定位已删除；`schedule.run / schedule.abortRun` 只经 Host 的 sealed Product API dispatcher，Local facade 与 Agent 工具经同一应用，RPC facade 继续只处理既有 wire。全仓生产反扫后，`AnchorScheduler.runTask/abortRun` 的唯一直接消费者只剩上述 Correctness 适配；`ServerContext.scheduler` 暂只保留定时触发、恢复、运行事件、状态观察与效果激活职责，`A5-SCHEDULE-RUNTIME-01` 因而缩窄但继续打开至 A5-11b2。
- 行为与耐久保护：公开方法名、严格参数、稳定 requestId、durable surface principal、返回结构与 Agent 工具同步等待不变；空/未知 task 仍为 NOT_FOUND，system task run 继续走迁移前通用 INTERNAL_ERROR，ghost abort 仍成功返回 `{ aborted:false }`。同 operation 的手动 run 继续由真实 Job Journal durable control identity 重放，直接证据锁定响应丢失重驱不新增 occurrence、trigger 或 activation；取消中的/已终态结果、事件次序、定时触发、冷启动恢复、Delivery/Kernel 效果和持久格式未迁移且未改变。本包没有把宽松或当前机制行为包装为新兼容承诺。
- 直接证据与验证：Schedule Domain/Local facade 2 文件 9/9，owner-kernel Authority/Correctness 2 文件 15/15，Server 真实 Product API + `RpcDispatcher` 1 文件 13/13，Agent tool 1 文件 4/4，RPC/assignment facade 2 文件 8/8，合计 8 文件 49/49；覆盖 normal/not-found/system、稳定身份、ghost/cancel、同 operation durable replay、六操作 exact-set、缺 contribution fail closed与本机/RPC/工具消费链。按依赖顺序 fresh build `@zhixing/core`、`@zhixing/owner-kernel`、`@zhixing/server`、`@zhixing/tools-builtin` 与 `pnpm cli:build` 均通过；canonical S7 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、本包最窄 Biome 12 文件及 `git diff --check` 通过。
- 结构失效与交接：S7 以反向 mutation 拒绝两项 Command 漏出唯一 exact-set、Server/facade 恢复 runtime 直调、Host 重新把 run/abort 放入 `SchedulerBackend`、Correctness 适配缺失或出现第二 contribution；package-export 继续冻结唯一窄 Schedule application 入口。以后若 `ScheduleManagementApplication` 的 run/abort 合同、`ScheduleManualExecutionPort`、Anchor adapter/Job control identity、Host contribution、任一 Local/RPC/tool binding、两项公开 RPC/wire/error、S7 或 package-export 变化，本记录失效并只重验上述闭包。A5-11b1 当前完成并等待协调者独立复核；A5 与 Schedule 行继续 `[ ]`，下一检查点仅为 A5-11b2，不进入其他领域。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从现有唯一 Schedule application、Product API exact-set、Anchor execution adapter、Host contribution、两项 RPC、Local/RPC/Execution facade 与 Agent 工具双向反查，确认手动 run/abort 的产品准入、身份和结果投影只有一个应用 owner；Server/facade 不再直达 Scheduler runtime，底层仍只承担 Job Journal、Assignment、激活、等待与取消机制。独立重取 core 9/9、owner-kernel 15/15、真实 RPC dispatcher 13/13、Agent 工具 4/4、CLI facade 8/8，canonical S7 32/32 + registry golden 与 `git diff --check` 全部通过；system task、not-found、ghost abort、同 operation 重放和 durable principal 均保持迁移前行为。接受 `A5-11b1-schedule-manual-control-v1`；Schedule 行与 A5 继续 `[ ]`，桥只剩 A5-11b2 的定时、恢复、事件与效果职责。

### A5-11b2：闭合 Schedule 运行时、事件与效果边界

- 派发基线：`HEAD f6532e8b + task-doc:A5-11b2-dispatch`；A5-11a 与 A5-11b1 已独立验收并提交，进场索引与工作区除本文调度记录外应为空。
- 唯一架构结果：Schedule 形成完整垂直领域边界，唯一拥有定时触发、离线/失败策略、运行恢复、用户可见运行投影与事件语义；ApplicationHost 只持有其类型化生命周期贡献，Kernel/Assignment/Delivery/Journal/Resource 只实现 Schedule 需求方定义的窄效果与正确性端口。删除 `A5-SCHEDULE-RUNTIME-01`、`SchedulerBackend`、ServerContext scheduler 服务定位和所有等价旁路，完成后可据真实证据裁决 Schedule 行。
- 生产闭包：沿 `AnchorScheduler`、`AnchorSchedulerRuntime`、Host create/start/activate/recover/drain/stop、定时 tick/触发、queued/accepted/started/terminal、offline missed、failure backoff/auto-disable、system maintenance、Job Journal/Assignment 激活与取消、Scheduler user notices、Delivery/Channel、Server status/auth、RPC event bridge、Local/RPC facade 与 REPL 消费双向迁移。现有实现可在原目录成为领域实现，不得为了目录美观搬代码；但领域规则、状态与投影只能有一个 owner，Host 与 Surface 不得保留规则或运行期查找。
- 边界与退场：把当前 `activateUserJob/recoverUserJobs/cancelUserJob`、Kernel Run Envelope/Terminal、Assignment、资源治理、持久提交、用户通知与 Delivery 明确收成由 Schedule 拥有的窄端口；领域不得复制通用 Job Journal、Authority、资源、安全、确认、Kernel 或 Delivery 状态机。Server 的活跃运行数、已启用任务和 capability 投影必须经 Schedule Query/只读投影取得；公开四类 schedule event 的可见性、失败到 completed 的映射与内部任务过滤由 Schedule 领域投影统一决定。Local facade 不再依赖 raw EventBus 或 runtime `getTask` 判断可见性，Host 生命周期不再通过 ServerContext 暴露 scheduler。
- 行为保护：严格保持一次/周期调度、手动运行、离线 miss/coalesce、backoff、连续失败禁用、响应丢失重放、冷启动/换代/host-stop 恢复、取消、资源不足、远端 Assignment、结果等待、用户 notice 与 Delivery 的现有正常和异常终态；保持六个 RPC、四个公开事件、REPL/Agent 工具、Server status/capability、持久 schema、系统维护任务、单机/Anchor-only/Executor-only 拓扑和关闭零残留不变。不得把现有缺口包装为新合同，也不得新增调度能力。
- 最窄证据：领域直接测试覆盖 due trigger、manual/automatic 同一 occurrence 规则、offline miss、failure/backoff/disable、queued recovery、cancel/terminal、事件顺序与内部过滤；Host/owner 真实测试覆盖启动、恢复、换代、拒新、排空、停止及 Kernel/Assignment/Delivery 端口；Server 真实 dispatcher/event/status 测试和 Local/RPC/REPL 消费证明表面等价。更新结构门禁，使其能拒绝 `SchedulerBackend`/ServerContext scheduler 复活、Host/Surface raw EventBus 业务判断、第二 runtime owner、Schedule 直接实现通用机制或拓扑字段渗入领域。源码稳定后按验证手册串行运行必要上游构建、受影响最窄测试、canonical S7、fresh package exports、Biome 与 `git diff --check`；不提前运行根级全测或制品验收。
- 完成与安全交接：只有 Schedule 定义、手动控制、定时/恢复、事件/投影、Kernel/Effect、全部表面、Host 生命周期和旧路径删除在同一基线上闭合，`A5-SCHEDULE-RUNTIME-01` 与 `SchedulerBackend` 零残留，直接证据无反证时才勾选 Schedule 行；A5 仍待其他领域。约四小时仍未完成、出现两个以上可独立失败的未知、影响跨到其他领域或需要改变公开/持久合同，必须停在可构建、可运行且单一 owner 成立的安全检查点反馈；不得为了关闭 Schedule 扩入 Conversation、Advancement、Device、Backup & Recovery 或 A6。
- 实施基线与唯一领域边界：进场为 `HEAD f6532e8bcdbebf17770032edfab9ec53fc6a358a + task-doc:A5-11b2-dispatch`，索引为空。现有唯一 `@zhixing/core/scheduler/application` 新增有限 `ScheduleApplicationService`、运行状态 Query、只读运行事件与生命周期合同；领域纯策略集中裁决 due ordering、timer deadline、离线 user miss/system coalesce、稳定 occurrence identity、next-fire、连续失败、确定性 backoff、auto-disable 与 pending disable exact-set。`AnchorScheduler`、Job Journal、Assignment、资源/Kernel/Delivery 仍只实现定时器、串行日志、effect 和错误映射机制，没有复制通用状态机、持久格式或产品协议。
- 生命周期、恢复与效果责任：Host 只持有一个稳定 Schedule 应用实例，并以 `install/release` 单向接管当前 Anchor generation 的窄 mechanism port；start/activate、close admission、冻结 accepted-work、恢复、immediate/drain/cancel 结算、settled read-back、换代 recovery、manual surface resume 与 stop 全部只经该应用。accepted-work exact-set 在应用边界冻结，foreign generation/超出冻结集合 fail closed；响应丢失与冷启动仍由既有 Job Journal/Authority 机制按 durable identity 重放。Host 不取得公开 Scheduler 实现对象，`AnchorSchedulerRuntime` 的 mechanism 字段为私有，换代前先 stop/release 旧 generation，安装失败沿既有 Host 补偿链收口。
- 运行投影、事件与旧路退场：raw Correctness adapter 只提供任务/活跃运行快照和原始 signal；Schedule 应用唯一负责 internal task 过滤、active/enabled/turn-context 投影及 failed → `completed(status=error)` 折叠。Server auth/status 只经 sealed Product API Query，RPC event bridge 只订阅领域事件并做四个既有 wire notification 投影；Local/REPL 不再读取 raw EventBus 或 runtime task 判断，RPC client 对 list/ack/四事件 payload 执行 exact decode。`SchedulerBackend`、`ServerContext.scheduler`、Server lifecycle `scheduler.stop`、handler/facade 运行期查找和同责 fallback 均归零；canonical registry 只删除退场的 standalone `scheduler.stop` owner，`A5-SCHEDULE-RUNTIME-01` 当前关闭。
- 行为保护与直接证据：一次/周期调度、offline miss/coalesce、backoff/auto-disable、queued recovery、manual run/abort、内部维护、Assignment/Kernel/Delivery/notice、HostStop/device-removal/replacement、四个公开事件、六个 RPC、REPL/工具、status/capability、持久 schema 与错误映射均保持。Schedule Domain/Local 2 文件 14/14，Owner Journal/Authority/Correctness 4 文件 30/30，Server/Product API/event/lifecycle 4 文件 91/91，Agent tool 1 文件 4/4，CLI Local/RPC/assignment/Host structure 3 文件 14/14，合计 14 文件 153/153；覆盖领域策略、投影/事件、generation/accepted-work、冷启动与自动停用、真实 dispatcher/wire、无 scheduler context、表面严格解码及 Host activation owner。fresh build `@zhixing/core`、`@zhixing/owner-kernel`、`@zhixing/rpc`、`@zhixing/server` 与 `pnpm cli:build` 通过；canonical S7 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、本包 26 文件最窄 Biome 和 `git diff --check` 通过。额外探测 `distributed-runtime-structure.test.ts` 在到达本包 contract golden 前即命中进场基线已有的 `@zhixing/runtime-host` A4 依赖期望残留（仍期待已移除的 mcp/tools-builtin），未计入本包证据且本包未扩面修改该既有断言。
- 结构失效与交接：S7 及反向 mutation 冻结 Schedule domain policy、唯一 lifecycle/runtime application、Host generation install/release、Correctness mechanism 私有性、Product API status/capability、四事件 wire codec、Server/Surface 旧路归零，并拒绝把 offline/failure/auto-disable/filter/fold 决定移回 Authority/Host/Surface、恢复 raw EventBus 判断、第二 runtime owner 或 `SchedulerBackend`。以后若 Schedule runtime policy/application/lifecycle contract、Anchor mechanism/Job failure adapter、accepted-work/恢复/换代、Host composition、Server Product API/event binding、Local/RPC facade、四事件 wire、S7 或 package export 变化，本记录失效并只重验对应闭包。A5-11b2 当前完成并等待协调者独立复核；Schedule 行据直接证据标为 `[x]`，A5 继续 `[ ]`，下一责任链由协调者另行派发。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者复核反证与证据失效：初次完成记录遗漏 `packages/cli/src/commands/info-commands.ts` 的 `/tasks` 表面仍自行调用 `isInternal()` 过滤 `SchedulerFacade.list()`，因此“全部表面旧路归零”和 Schedule 行裁决在该残留闭合前均不成立；同时 lifecycle contract 虽接受 `immediate / drain / cancel`，初次直接测试只命中 `cancel`，尚未证明迁移前 Schedule 对三种 HostStop strategy 都使用同一耐久 pause-and-settle。该反证只失效 `/tasks` 可见性消费与三策略直接识别证据，不作废其余运行策略、Journal/Authority、Host/Server、wire、构建和 153 项已成立闭包。
- 纠正与证据恢复：`/tasks` 删除 `isInternal` 导入和二次过滤，直接按既有顺序展示 Schedule management Query 已裁决的任务集合；直接反例让 facade 返回带 `system` 标记但已裁决可见的条目，Surface 仍展示该条目，从而可识别同责过滤回流，空态、格式、排序来源与公开行为未变。领域 lifecycle 参数化 `immediate / drain / cancel` 三种输入，逐一证明均先核对相同 frozen accepted-work exact-set，再且只再调用一次既有 `pauseAndSettle`，foreign exact-set 仍在机制效果前拒绝，未新增策略差异。纠正最窄验证为 CLI `info-commands.test.ts` 11/11、core `application.test.ts` 13/13、canonical S7 32/32 与 registry golden、三份适用 TS 文件最窄 Biome 及 `git diff --check`；S7 新增反向 mutation 拒绝 `/tasks` 或其他已登记 Surface 恢复 `isInternal`/同责 filter，并拒绝三种 strategy 分叉或跳过统一 settlement。上游产物与其余输入未变，未重复构建、package exports 或先前 153 项。上述子证据现已恢复，A5-11b2 继续处于等待协调者独立复核，Schedule 行保持 `[x]`、A5 保持 `[ ]`，未进入下一领域。
- 协调者独立验收：从 Schedule 纯策略、唯一 runtime/lifecycle application、Anchor generation mechanism、Host 装配、Product API、Server status/auth、RPC event bridge、Local/RPC/REPL/Agent 工具及 Job Journal/Assignment 双向复核，确认定时、恢复、事件、可见性、效果和生命周期责任已形成唯一垂直边界，`SchedulerBackend`、`ServerContext.scheduler` 与表面同责判断归零；迁移前一次/周期、离线、失败禁用、重放、取消、换代、HostStop、公开 RPC/Event 和持久语义未改变。独立重取纠正失效的 CLI 11/11、core 13/13，canonical S7 进程树完整退出且 `git diff --check` 通过；其余 153 项、fresh builds、package exports 与 registry golden 的输入未变化，沿验证手册继续有效。接受 `A5-11b2-schedule-runtime-lifecycle-v1`，Schedule 行保持 `[x]`，A5 等待剩余领域。

### A5-12a：归位 Conversation 持久身份、目录与历史应用责任

- 派发基线：`HEAD 10f71c8b + task-doc:A5-12a-dispatch`；完整 Schedule 已独立验收并提交，派发前索引为空且工作区只有本文调度登记。
- 唯一架构结果：Conversation Domain 唯一拥有可恢复会话身份、名称、目录排序、创建、改名、存在性与历史倒读的产品合同、校验、错误和稳定投影；`session.list/new/history/rename` 及对应进程内消费只经同一 Product API 应用入口。持久 Repository/Transcript 只实现 Conversation 需求方定义的窄 Correctness/Storage 端口，Server、CLI 与 Surface 不再拥有上述产品决定或直接读取目录机制。
- 生产边界：从四个公开 RPC、REPL/Controller/历史尾部与 Host 组合根正向追踪全部直接消费端，再从 `ConversationDirectory`、Repository、Transcript 与全域 conversation id 路由反向核对。`session.list` 的 active/busy/observer/pending 与既有 advancement 摘要只作为只读外部投影输入，由 Conversation 应用决定最终列表形状；尚未迁移的 Manager/Advancement 只允许通过登记的最短只读端口供给，不得复制事实、回写或形成第二列表规则。场景会话的全域键与 user 列表隔离保持不变。
- 明确不做：不迁移 `resume/send/resolve/subscribe/unsubscribe/abort/clear/delete/compact/contextBudget/usage/security/taskList*`，不改变 turn admission、Run/Authority/Assignment、窗口与摘要、恢复、删除清理、Advancement、Workscene 或 Channel；不新增能力、协议方法、持久 schema、兼容入口或通用领域框架，不勾选 Conversation 行。
- 旧路与证据：删除 Server handler 对目录默认、校验、分页上限、改名 not-found、列表叠加和变更通知决定的所有权，只保留认证、wire decode/encode、调用、错误映射与 notification binding；删除 `ServerContext.conversationDirectory` 对这四项应用调用的服务定位旁路，Host 只组合唯一应用 contribution。直接证据必须覆盖空/非空列表、纯内存会话不入列表、active/busy/observer/pending、Advancement 摘要、创建壳、场景全域键、改名/缺失、history 默认/上限/坏游标/不存在容错/跨 shard 分页、通知与本机/RPC 等价；S7 反向拒绝 handler/Surface 恢复目录直调或同责规则。只运行受影响闭包、必要上游构建、canonical S7、package exports、最窄格式与 `git diff --check`，不跑根级全量或制品验收。
- 完成与安全交接：只有上述四项产品责任、全部生产 binding、旧目录服务定位旁路、直接回归和结构门禁同时闭合且公开行为逐项等价，A5-12a 才完成；Conversation 行与 A5 保持 `[ ]`。预计超过四小时、发现列表投影不能在不迁移另一领域写权的情况下闭合、出现多个独立未知或需要改变协议/持久语义时，停在可构建、可运行且持久目录只有一个产品 owner 的安全检查点反馈，不得扩入 Conversation 其余生命周期或其他领域。
- 实施基线与唯一领域应用：进场为 `HEAD 10f71c8b39f06e0c3a79f07dadc7629512c9e4 + task-doc:A5-12a-dispatch`，索引为空。新增唯一窄入口 `@zhixing/core/conversation/application`，由 `ConversationDirectoryApplicationService` 拥有目录列表、历史倒读、创建与改名四项有限 Query/Command，以及提交后 `conversation-renamed` Fact；领域合同只暴露稳定 identity、目录/历史投影、外部运行/Advancement 只读投影和需求方 Storage port，不暴露 Repository、Transcript、路径、Authority、RPC 或 Surface 类型。四项 operation exact-set、默认 history limit `20`、上限 `200`、名称与 cursor 校验、not-found 和稳定目录排序均在此唯一应用边界冻结；core 根与 conversation 宽入口均未新增转导。
- 生产绑定与旧路退场：Anchor Host 只构造一份 Conversation 应用并将其 contribution 合入既有唯一 sealed Product API dispatcher；Repository/Transcript adapter 只实现目录记录、创建、改名和历史倒读，Manager 与 Advancement 只提供 active/busy/observer/pending 和摘要的只读投影。`session.list / session.new / session.history / session.rename` 只经 Product API descriptor，Server handler 仅保留 wire 校验、领域错误映射和改名成功后的既有 `session.renamed` 广播；无显式 id 的 `session.send` 壳创建也复用同一 create 应用决定。Executor-local binding 复用同一领域应用服务并只保留 topology/consent/wire 投影，没有第二 Product API dispatcher。`ServerContext.conversationDirectory` 已缩窄为尚未迁移的 exists/ensure/transcript/touch/clear/remove 生命周期桥，四项应用方法与 Server 业务 DTO 归零；CLI facade/controller 的历史类型改从领域窄入口消费，旧 handler/owner 目录直调与同责排序、默认、校验和列表叠加归零。
- 行为、Correctness 与邻接边界：目录仍只列耐久记录，纯内存 runtime 不自行成为条目；先按耐久记录 `lastActiveAt` 降序确定目录次序，再以 active/busy/observer/pending 和 Advancement 摘要覆盖既有条目的展示字段而不重排，保持 user/workscene 全域 identity 隔离。创建壳、改名 trim/not-found、跨 shard history、空历史与缺记录容错、RPC code/message/data、改名通知时机和本机/远端合并行为均保持。local-only 改名继续通过稳定 staged operation identity 提交，同责产品判断由领域应用作出。`listForAdvancement/readRunsReverse` 仅作为尚未迁移 Advancement 恢复读取的显式只读短桥，共用同一 Repository/Transcript storage primitive，不取得 Conversation 写权；resume/send 其余生命周期、clear/delete、turn admission、Run/Authority/Assignment、恢复与 Advancement/Workscene/Channel 均未迁移。
- 直接证据与验证：core Conversation 应用 1 文件 4/4，CLI Repository adapter 与 local-only Product binding 2 文件 15/15，Server 真实 session RPC/Product API 1 文件 76/76，distributed runtime golden 1 文件 3/3；覆盖四项 exact-set、目录叠加与排序、create/rename/history、改名 Fact、local-only 同应用消费、场景 identity、wire/error/notification 和缺 contribution fail closed。按依赖顺序 fresh build `@zhixing/core`、`@zhixing/server` 与 `pnpm cli:build` 均通过，CLI typecheck 通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、本包 15 文件最窄 Biome 均通过。验证没有运行根级回归或制品验收。
- 结构失效与交接：S7/package-export 及反向 mutation 冻结唯一 Conversation application/export/exact-set、Host contribution、Server 四 handler 的 Product API 消费、Server directory 四项应用方法退场、local-only 同应用消费，以及 core 根/宽入口零泄漏；能拒绝 handler/Surface 恢复目录直调、第二排序/校验、owner 直接 create/transcript、第二 contribution 或错误 barrel 回流。以后若四项领域合同、Storage/runtime/Advancement 只读 port、Host composition、Server/local binding、RPC wire/error/notification、Repository/Transcript adapter、S7 或 package export 任一变化，本记录失效并只重验上述闭包。A5-12a 当前完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点由协调者从未迁移的 Conversation 生命周期中另行裁决。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-12a 协调复核纠正：耐久目录顺序与跨域依赖

- 公开行为反证：迁移前 `ConversationRepository.list()` 按耐久 meta 的 `lastActiveAt` 排序，Server 只把活跃 runtime 的 `lastActiveAt` 覆盖进返回字段而不重新排序；当前应用在覆盖后再次排序，使一个耐久上较旧但本轮活跃的对话提前，属于未授权列表顺序变化。现有应用测试把该变化写成新预期，不能作为等价证据。
- 架构反证：`packages/core/src/conversation/application.ts` 直接导入 `../advancement/types.js` 的 `AdvancementReviewDecision` 与 `RubricContractDraftSnapshot`。Conversation 可以拥有自己所需的只读投影端口，但不得依赖 Advancement 内部类型；AE-001 要求跨领域调用显式且同步依赖无环，A0 固定的是 `Conversation + Skill → Advancement`，不是反向依赖。
- 最窄纠正边界：Conversation 应用应先按耐久记录时间保持迁移前目录顺序，再叠加 runtime/Advancement 只读字段；增加“耐久顺序与 runtime 时间冲突”反例。把 Advancement 投影改为 Conversation 自有的稳定只读结构，由组合根适配，不导入 Advancement 源码类型；S7 增加可识别的反向依赖与顺序 mutation。不得改变四项 Product API、RPC/wire/error/notification、本机合并排序、持久 schema或扩入其他 Conversation 生命周期。只重验 core 应用、Server list 真实 binding、受影响类型/构建、S7、package exports、Biome 与 diff；其余已通过证据输入未变时不得重复。
- 纠正实现与责任边界：`queryList()` 现在先复制并按耐久 `ConversationDirectoryRecord.lastActiveAt` 排序，再逐项叠加 runtime/Advancement 只读字段；叠加后的活跃时间仍按迁移前值进入 payload，但不再参与目录重排。直接反例锁定“耐久 newer 仍在前，即使 older 的 runtime 时间更新”；local-only 的 `mergeConversationDirectoryViews()` 未改，跨设备最终排序语义保持。Conversation 自己定义查询结果需要的 readonly rubric draft/review 投影，`application.ts` 对 Advancement 源码零导入；Anchor 组合根继续把 Advancement 当前快照逐字段映射到该需求方结构，没有复制其写权、规则或状态机。
- 纠正证据与失效恢复：core Conversation 应用 1 文件 4/4，Server 真实 `session.list` binding 1 文件 3/3（同文件其余 73 项未运行）；fresh core/server build、CLI typecheck 与 `pnpm cli:build`、canonical S7 32/32 与 registry golden、fresh package exports、两份适用 TS 文件最窄 Biome 通过。S7 新增反向 mutation，Conversation 应用重新导入 `../advancement/types.js` 必然失败，并继续冻结耐久排序入口；core 反例直接识别 overlay 后重排回流。其余已成立证据输入未变，按要求未重复。上述两项失效证据现已恢复，A5-12a 等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，未进入其他生命周期，也未执行任何 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：从 Conversation 应用合同、耐久 Storage/Transcript adapter、runtime/Advancement 只读投影、Host Product API contribution、四个 Server RPC、local-only 同应用消费、CLI facade/controller 与 S7/package export 双向复核，确认目录事实、四项产品决定和列表投影只有一个 Conversation owner，旧目录服务定位的同责方法归零，公开 wire/error/notification、持久 schema、Anchor/Executor-local 行为均未漂移。独立运行 core 应用 4/4、CLI adapter/local binding 15/15、Server session/golden 79/79；纠正后再重取 core 4/4、Server list 3/3 与 canonical S7 32/32 + registry golden，`git diff --check` 通过。耐久顺序反例与 Conversation → Advancement 反向依赖门禁均成立，接受 `A5-12a-conversation-directory-application-v1`；Conversation 行和 A5 继续 `[ ]`。

### A5-12b：归位 Conversation clear 生命周期应用责任

- 派发基线：`HEAD 52dd209f + task-doc:A5-12b-dispatch`；A5-12a 已独立验收并提交，派发前索引为空且工作区只有本文调度登记。
- 唯一架构结果：Conversation Domain 唯一拥有“清空既有对话”的 Command、准入、稳定 operation identity、busy/not-found/cleared 终态、提交后 Fact 与用户投影；Anchor RPC、Executor-local 第一方入口和耐久恢复重放只调用同一应用语义。Conversation 需求方端口明确区分领域决定与 owner 串行、Authority/Journal、Transcript clear、窗口重置和恢复 claim 等 Correctness 机制。
- 生产闭包：正向覆盖 `session.clear`、local-only router、Product API contribution 与通知 binding，反向覆盖 `ConversationManager.clear` 的活动/非活动互斥、durable `writeSession/projectSession`、`ConversationProtocolRuntime.projectLifecycle/recovery`、目录 `appendClear + clearViewLayerState + task-list cache` 及全域 workscene id 路由。耐久在线与重启重放必须命中同一应用/投影责任，响应丢失不得重复产生领域事实或通知。
- 旧路与桥：删除 Server handler 与 local router 对 clear 的业务编排、错误决定和 manager/directory 直调；从 `ServerContext.conversationDirectory` 删除 `clear`，把 `A5-CONVERSATION-LIFECYCLE-01` 缩窄为 exists/ensure/transcript/touch/remove。Host 只装配 Conversation 应用所需窄机制端口；不得把 clear 决定移入 Protocol、Manager、Directory、Server 或 Surface，也不得形成第二 application/dispatcher。
- 行为保护：保持 requestId 接受域、durable/legacy 分支、BUSY/NOT_FOUND code/message、active busy 串行、inactive 与激活 FIFO、transcript clear 边界、meta view/task-list/segment 清理、窗口重置、runtime change hook、changed/diagnostic 通知时机、workscene 全域键、响应丢失重放与 local-only consent/wire 逐项不变。
- 明确不做：不迁移 delete、resume/subscribe、send/abort/resolve、compact/context/usage/security/taskList、turn admission/run、Advancement/Workscene/Channel；不改变持久 schema、公开 RPC/Event 或能力，不勾选 Conversation 行。
- 证据与安全点：直接测试至少覆盖 active/inactive/busy/not-found、legacy/durable、同 request 重放、projection 失败后恢复、窗口/盘/缓存一致性、通知恰一次、workscene 路由和 local-only 等价；S7 反向拒绝 handler/router 直调、第二 clear owner、ServerContext clear 回流或事实在投影前发布。只运行受影响测试、必要上游构建、canonical S7、package exports、Biome 与 `git diff --check`。约四小时未完成、出现第二独立未知、需要改变公开/持久合同或扩入 delete 时，停在可构建、可运行且 clear 只有一个产品 owner 的安全检查点反馈。
- 实施基线与唯一应用责任：进场为 `HEAD 52dd209fa79fd2dade6a686cfef536b502236c82 + task-doc:A5-12b-dispatch`，索引为空。Conversation 窄入口新增 `CONVERSATION_CLEAR_COMMAND`、`CONVERSATION_CLEARED_FACT_EVENT`、`ConversationClearCommitPort` 与 `projectConversationClear`；`ConversationDirectoryApplicationService.clear()` 唯一拥有 conversation/operation identity 校验、durable identity admission、active-turn/pending-lifecycle busy、not-found/cleared 终态和提交后 Fact。Host 只装配 owner 串行、Authority/Journal commit、Transcript/view 清理、runtime window reset 与 Fact transport 所需窄机制；这些机制不再决定 clear 产品语义。
- 三条生产绑定与恢复：Anchor `session.clear` 只经唯一 sealed Product API dispatcher 调用领域命令；legacy 分支使用同一 projector 执行 storage-before-runtime reset，durable 分支以 authenticated surface principal 与稳定 requestId 写入 `window-op clear`，再由 `ConversationProtocolRuntime` 的 lifecycle claim/ack/recovery 重放同一 projector。Executor-local router 复用同一应用，真实 `SessionStatePort.mutate` 与 protocol projection 保持相同 request identity；投影 Fact 订阅才传输一次 `session.changed { change: "cleared" }`，相同 request 的应用响应重放不产生第二 durable record 或第二通知。active runtime 仍由 `ConversationManager.clear` 串行，inactive 当前 owner 仍完成耐久投影；目录 adapter 继续执行 `appendClear`、view/task-list cache 清理与全域 workscene id 路由，持久 schema、协议和公开 wire 未改变。
- 旧路、桥与行为保护：Server handler 已删除 manager/directory clear 编排、durable write/project 和直接 changed 广播，只保留 wire 校验、surface principal、Product API 调用、既有 error/diagnostic 投影；local router 删除直接 window mutation 与提前通知。`ServerContext.conversationDirectory` 与 Server `ConversationDirectory` 的 `clear` 应用入口归零，CLI storage adapter 缩窄为需求方 `clearStoredView`，`A5-CONVERSATION-LIFECYCLE-01` 现只剩 exists/ensure/transcript/touch/remove。legacy 无 requestId 自动生成、durable 缺/坏 requestId、BUSY/NOT_FOUND code/message、active/inactive FIFO、runtime change hook、transcript/meta/task-list/segment 清理、local consent 和 workscene 全域键保持迁移前语义；delete 及其他 Conversation 生命周期未进入本包。
- 直接证据与验证：core Conversation 应用 1 文件 7/7，覆盖稳定 identity、commit 后 Fact、busy/not-found 与 storage-before-runtime 投影；Server 真实 session RPC/Product API 1 文件 76/76，覆盖 legacy/durable clear、wire/error、窗口清理、diagnostic 与 changed；CLI directory/local router 2 文件 16/16，覆盖本机同应用消费、真实 wire 与 response-loss exact replay 的单通知；durable lifecycle claim/ack 真实协议反例 1/1（同文件其余 24 项按输入未变跳过）。fresh `@zhixing/core`、`@zhixing/server` 与 `pnpm cli:build`、CLI package-filtered typecheck均通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh package exports 和本包最窄 Biome 均通过。一次扩大探索中，未纳入本包有效证据的既有 fixture 暴露 storage-maintenance backpressure（13/14）和缺失 baseline lifecycle contribution（0/2）；二者不命中本包 clear 断言，随后只重取上述真实 protocol 投影闭包，未以失败组冒充通过。
- 结构失效与交接：S7 与反向 mutation 冻结 Conversation clear command/fact/projector exact-set、Host composition、Anchor/Executor-local 应用消费、Server directory clear 退场、storage/runtime 投影次序、通知只由投影 Fact 产生以及旧 handler/router 直调归零；能拒绝第二 clear owner、local direct mutation、ServerContext clear 回流、projection 前广播或 production binding 漏接。以后若 clear command/fact、stable operation identity、busy/not-found mapping、Manager/Authority/Journal projection、directory clear storage primitive、恢复 claim/ack、notification binding、Server/local consumer、S7 或 package export任一变化，本记录失效并只重验该闭包。A5-12b 当前实施完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，`A5-CONVERSATION-LIFECYCLE-01` 尚未关闭。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-12b 协调复核纠正：legacy clear 不得预创建身份

- 公开行为反证：初次实现的 Anchor legacy commit 在 `clearStoredView` 前调用 `conversationDirectory.ensure(id)`；不存在的 conversation 会因此先获得 meta/transcript 身份，再被清空并返回成功，而迁移前 `ConversationManager.clear → directory.clear(false)` 的终态是公开 `NOT_FOUND`、零身份和零 changed 通知。durable admission 已在 Authority 写入前验证身份，提交后的 projector 按恢复合同物化视图，不受该反证影响。
- 最窄纠正与单一 owner：新增 Anchor 内部 `createAnchorConversationClearCommitPort` 作为唯一生产 Correctness binding，`command.ts` 只把该 port 装入既有 Conversation application；legacy projector 直接以 `clearStoredView` 的 false 判定 not-found，完全不调用 ensure，durable write/project、surface principal、稳定 requestId 与恢复顺序原样保留。该 binding 不新增命令、Fact、dispatcher或产品决定，clear 仍只有 `ConversationDirectoryApplicationService.clear()` 一个产品 owner。
- 直接反例与恢复证据：CLI 真实 `ConversationDirectory + ConversationManager + Conversation application + Anchor binding` 1 文件 9/9，新增反例证明未知 id 返回领域 not-found，repo 身份、transcript 与 Fact transport 均为空；真实 Server `session.clear` 定向 3/3（同文件其余 74 项按输入未变跳过），锁定既有 `NOT_FOUND` code/message、空目录/正文和零 `session.changed`；core clear 7/7、Executor-local clear/replay 8/8继续通过。canonical S7 32/32 与 registry golden通过，并新增 mutation 使 legacy binding 恢复 ensure 必失败；CLI package-filtered typecheck 与 fresh `pnpm cli:build` 通过。该反证所失效的 A5-12b legacy 行为证据现已恢复，Conversation 行、A5 与最终退出门继续 `[ ]`；未进入其他生命周期，也未执行任何 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-12c：归位 Conversation delete 生命周期应用责任

- 派发与实施基线：进场为 `HEAD 685dffb05586406eaf2ef2964343677aa8ea17ae + task-doc:A5-12c-dispatch`，A5-12b 已由协调者独立验收并提交，索引为空。Conversation 窄入口新增唯一 `delete` Command、`conversation-deleted` Fact、`ConversationDeleteCommitPort` 与 `projectConversationDelete`；`ConversationDirectoryApplicationService.delete()` 唯一拥有 conversation/operation identity 校验、active turn 或 pending lifecycle busy、not-found/deleted 终态与提交后 Fact。Correctness 投影端口只表达 runtime/storage 删除、既有依赖级联与 durable/legacy 失败策略，不暴露 Repository、Transcript、Authority、RPC 或 Surface 类型。
- 三条生产链与恢复：Anchor `session.delete` 只经 sealed Product API dispatcher 调用同一领域应用；durable 分支以 authenticated surface principal 和稳定 requestId 写入既有 lifecycle authority，再由 `ConversationProtocolRuntime` 的 claim/ack/recovery 调用同一 projector，storage 已删除的恢复重驱不会重复 Fact，Advancement 级联失败继续保留 durable 待恢复状态。legacy 分支保持既有自动 operation identity 与 best-effort Advancement cleanup。Executor-local router 复用同一应用和稳定 `SessionStatePort.mutate` identity，实际 protocol delete 成功后只从领域 Fact 投影一次 `session.changed { change: "deleted" }`；响应丢失重放不新增第二 record、第二删除或第二通知。
- 行为与级联保护：active runtime 与 inactive storage 删除仍由 `ConversationManager`/目录机制串行，删除本体先于 Advancement 取消与 data purge；Workscene 删除继续经需求方窄 storage port 使用同一物理 primitive，未被误升为第二 Conversation 应用 owner。BUSY/NOT_FOUND、durable 缺失或非法 requestId、legacy Advancement 失败诊断、active/pending 互斥、全域 workscene identity、runtime observer、response-loss replay、重启恢复、公开 RPC result/error 与 changed notification 均保持迁移前语义；delete 不改变 clear、resume/observer、turn admission/run、Advancement 或 Workscene 的领域责任。
- 旧路与桥收敛：Server handler 已删除对 `ConversationManager.delete`、directory remove、durable lifecycle write/project、Advancement 级联和直接 broadcast 的业务编排，只保留 wire 校验、surface principal、Product API 调用及既有错误映射；local router 删除 direct lifecycle mutation 与提前通知。Server `ConversationDirectory.remove` 和 CLI `createConversationDirectory().remove` 应用入口归零，物理 primitive 改为需求方命名的 `deleteStoredConversation`；`A5-CONVERSATION-LIFECYCLE-01` 因而缩窄为 exists/ensure/transcript/touch。S7 与反向 mutation 能拒绝 Server/local 旧直调、第二 delete owner、缺少 Anchor/Executor-local binding、投影前通知、恢复 direct remove 或 Server directory remove 回流。
- 直接证据与验证：core Conversation application 1 文件 9/9；CLI Anchor directory binding、Executor-local RPC 与 Workscene storage consumer 3 文件 23/23，另以真实 local owner durable replay 定向 1/1 证明同 operation 只投影一次 Fact；Server 真实 `session.delete` Product API/wire/并发/通知闭包定向 1 文件 7/7。按依赖顺序 fresh `@zhixing/core`、`@zhixing/server` 与 `pnpm cli:build` 均通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、本包最窄 Biome 与 `git diff --check` 通过。未运行根级回归或制品验收。
- 失效与交接：以后若 delete command/fact、stable operation identity、busy/not-found mapping、Manager/storage primitive、Authority lifecycle write/project/recovery、Advancement 级联、notification binding、Server/local/Workscene consumer、S7 或 package export 任一变化，本记录失效并只重验该闭包。A5-12c 当前完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，`A5-CONVERSATION-LIFECYCLE-01` 尚未关闭，下一检查点由协调者从 resume/observer 或 turn admission/run 中裁决。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-12c 协调复核纠正：legacy Fact 与 Workscene 删除投影桥

- legacy Fact 反证与修复：初次 binding 在 `ConversationManager.delete()` 取得 per-id 锁前快照 `has(conversationId)`，只按该快照或 storage remove 结果转发 `onDeleted`；当 `getOrCreate` 已持锁创建 runtime、delete 排在锁后且没有存储身份时，Manager 锁内会成功删除，但锁外快照仍为 false，迁移后错误抑制 deleted Fact。修复后 legacy 完全服从 Manager 的锁内成功回调；只有 `deletionAlreadyCommitted` 的 durable projector 才以“入口 runtime 快照或本次实际删到 storage”抑制物理删除已完成后的恢复重播。真实竞态反例以 deferred runtime factory 固定“快照无 runtime → 锁内刚激活 runtime”窗口，证明 runtime-only legacy 删除成功、dispose 一次且 Fact 恰一次；既有 durable dependent cleanup 首次失败、重驱成功且 Fact 仍恰一次。
- 独立桥裁决：初次记录把 Workscene 的 `WorksceneSessionOwner → deleteStoredConversation` 描述为普通物理 consumer，却没有给出可退场桥身份，因而“旧路径归零”只对 Server/Anchor/Executor-local delete 应用旁路成立，不能覆盖 Workscene cascade。现新增独立 `A5-CONVERSATION-WORKSCENE-DELETE-01`：Anchor 组合根是 `ConversationWorksceneDeleteProjectionBridge` 唯一 producer，`WorksceneSessionOwner.removeScene` 是唯一 consumer；桥只在 Workscene Authority 的 session delete fact 后执行 Conversation storage projection，不取得 Conversation delete command、准入、busy/not-found/deleted 终态、Fact 或通知权。`A5-CONVERSATION-LIFECYCLE-01` 仍只描述 `ServerContext` 的 exists/ensure/transcript/touch，二者不混写；本桥必须由 Workscene A5 迁移包退场。
- 代码与结构闭包：Workscene directory/owner 不再取得裸 `deleteStoredConversation` 能力，而只依赖具名冻结桥；物理 primitive 仍由 Conversation adapter 实现，没有第二 storage owner。S7 在 production exact-set 上冻结唯一 factory consumer 为 Anchor `command.ts`、唯一 projection consumer 为 `workscene-session-owner.ts`，并拒绝第二 bridge consumer、Workscene direct storage delete、ServerContext/remove 回流或 legacy Fact 条件回退；桥标识及退场责任是后续 Workscene 完成门禁的显式输入。
- 纠正证据与状态：CLI Conversation directory/binding 1 文件 12/12（含 runtime-only legacy 锁竞态与 durable recovery 去重），Workscene directory/owner 2 文件 11/11，Executor-local delete 代表集 1 文件 9/9；core Conversation application 9/9、Server delete Product API/wire/并发/通知定向 7/7。canonical S7 32/32 与 registry golden、fresh `pnpm cli:build`、最窄 Biome 和 `git diff --check` 均通过；core/server 输入未变，未重复其构建。Conversation 行、A5 与最终退出门继续 `[ ]`，两个桥均保持显式打开；未进入 Workscene、resume/observer 或 turn 主链，也未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-12d：归位 Conversation resume 应用责任

- 实施基线与唯一应用：进场为 `HEAD a04c4f2e8201c24336ea0741c5c80d1ee1ef83f8 + task-doc:A5-12d-dispatch`，索引为空。Conversation 窄入口新增唯一 `resume` Command、`ConversationResumePort` 与稳定结果投影；`ConversationDirectoryApplicationService.resume()` 唯一拥有 identity restore/not-found、依赖恢复、runtime/adoption/Advancement 投影的应用顺序。领域端口只表达 Conversation 所需的耐久身份、依赖恢复与 adoption review 稳定投影，不暴露 Directory、Manager、Advancement 实现、RPC、Server 或 Surface 类型，也未新增 resume Fact 或第二 dispatcher。
- 两种拓扑与绑定边界：Anchor `session.resume` 在认证和 wire 校验后先建立 observer，再经唯一 sealed Product API dispatcher 调用 Conversation 应用；应用依次恢复耐久 identity、依赖 lifecycle、runtime、adoption review 与 Advancement 摘要，Server 最后只投影既有 wire 结果和错误。Executor-local router 同样先建立 observer，再调用同一领域应用；local Correctness binding 只从 local owner/meta 恢复稳定 identity，依赖恢复为空操作，不复制应用规则。observer/subscribe、historical final replay、unsubscribe 与 disconnect cleanup 继续属于 Server/Surface binding，不被误写为领域事实；缺失 identity 时只回滚本次 provisional observer。
- 行为与旧路收敛：保持输入/global conversation id、not-found code/message、active/busy/runtime projection、Advancement 恢复与摘要、surface principal adoption review、observer-before-recovery、已订阅 historical final、unsubscribe/disconnect 及 Anchor/Executor-local 公开结果等价。Server handler 已删除 directory `touch`、Advancement recovery、active/busy 读取、adoption review 与结果拼装；local router 已删除 owner list/meta 的 resume 业务判断。`ServerContext.conversationAdoptionReview` 与 `ServerContext.conversationDirectory.touch`、Server `ConversationDirectory.touch` 均归零；Host 只在 Conversation resume Correctness binding 中持有具体 directory touch/recovery/review 机制，`A5-CONVERSATION-LIFECYCLE-01` 因而缩窄为 exists/ensure/transcript。clear/delete、turn admission/run、observer 协议、Advancement 与 Workscene 领域责任均未迁移。
- 直接证据与验证：core Conversation application 1 文件 10/10，覆盖 resume 顺序、稳定投影与 not-found；CLI Anchor resume binding 与 Executor-local RPC 2 文件 11/11，覆盖 surface/host adoption、同应用结果与本机 missing；Server 真实 dispatcher resume 定向 1 文件 7/7，覆盖 observer-before-recovery、existing/missing、active/busy 与 wire，另以 session send/subscribe 代表集 1 文件 2/2 锁定 historical final 与 disconnect observer cleanup。fresh `@zhixing/core`、`@zhixing/server` 与 `pnpm cli:build` 均通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports` 和本包最窄 Biome 均通过。未运行根级回归或制品验收。
- 结构失效与交接：S7 与反向 mutation 冻结 resume command/port/application、Anchor 与 Executor-local production binding、observer-before-command、Server directory/adoption hook 退场及 handler/router 旧业务判断归零；能拒绝 Server/local 恢复 direct touch/owner 判断、observer 晚于恢复、第二 resume owner、Host binding 漏接或旧 adoption hook 回流。以后若 resume command/result、identity/recovery/adoption/runtime/Advancement 投影次序、observer rollback/subscribe binding、Server/local wire/error、Host composition、S7 或 package export 任一变化，本记录失效并只重验该闭包。A5-12d 当前完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点为 turn admission/run，不把 observer/subscribe 扩入领域应用。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-12d 协调复核纠正：missing resume 只回滚本次 observer

- 反证与修复：初次实现虽然把 observer 建立放在恢复前，却在 resume identity not-found 时无条件 `removeObserver`；同一 connection 先前已订阅，或 quiescing 使本次 add 返回 false 但既有名册身份仍在时，会误删非本次建立的 observer并可能错误启动 grace。Anchor binding 现在先从 manager 只读名册记录 `alreadyObserved`，再记录本次 `observerAdded`；not-found 只有在“此前不存在且本次 add 成功”时移除。fresh missing 仍零泄漏，already-subscribed missing 保留原 observer，existing identity 仍严格 observer-before-recovery；quiescing、grace 和其他错误路径未改变。Executor-local 原有 `alreadySubscribed` 纪律保持，并新增 fresh missing 后注入 Conversation Fact 不再通知该连接的直接断言。
- 纠正证据与失效恢复：Server `session.resume` 定向 1 文件 5/5（同文件其余 74 项未运行），直接覆盖 fresh missing 回滚、already-subscribed missing 保留和 existing observer-before-recovery；CLI local resume 定向 1 文件 1/1（其余 9 项未运行），覆盖 fresh missing 零 observer 泄漏。canonical S7 coverage/mutation 32/32 与 registry golden 通过；反向 mutation 把条件回滚恢复为 unconditional 时，Conversation owner 与 adoption 两条结构检查均失败。协调者独立复核后在纠正基线上重新取得同一定向结果与 S7 结果，并因 Server handler 构建输入已经变化补跑 fresh `@zhixing/server` build 与 `pnpm cli:build`，二者均通过；core 与公共导出输入未变，未重复 core build 或 package exports。初次“只回滚本次 provisional observer”的未成立声明现已由对抗证据恢复；A5-12d 继续等待协调者独立复核，Conversation 行与 A5 保持 `[ ]`，未进入 turn admission/run，也未执行任何 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-12e：归位 Conversation abort/resolve 运行控制应用责任

- 实施基线与唯一应用：进场为 `HEAD 879d0f41067cbe96c714c0a54a738565e1f7d024 + task-doc:A5-12e-dispatch`，索引为空。Conversation 窄入口在 A5-12d 的同一个 `ConversationDirectoryApplicationService` 上新增 `abort`、`resolve-uncertain` 两个 Command、有限 `ConversationRunControlPort` 与稳定结果投影；Conversation Product API exact-set 由 7 个 operation 扩为 9 个、Fact Event 仍为既有 3 个。领域应用唯一拥有 stable cancellation operation/run identity 的必需性与校验、durable/legacy 取消成功或 not-found 终态、权威取消后 Advancement proxy 结算/恢复顺序，以及 uncertain resolution 的 identity/epoch/open-fact/decision 校验；端口只表达 Owner/Authority principal、取消/resolve 提交和跨领域投影机制，不暴露 Manager、Authority、Advancement、RPC、Server 或本地 owner 实现。
- Anchor 与 Executor-local 绑定：Anchor 唯一 `createAnchorConversationRunControlPort` 把已认证 surface caller 映射为 durable principal；durable 形态保持 requestId + authoritative runId、耐久取消后才作用本机 runtime、同 request replay 与异载荷冲突，legacy 形态继续生成本次 operation identity 并调用既有内存 abort。取消 disposition 只在 binding 聚合为匹配数、in-flight/pending 效果与可选 Advancement ingress；领域应用再按既有顺序结算当前 active/outstanding proxy，结算异常不回滚已权威取消并 fire-and-forget 启动既有恢复。Executor-local router 继续要求 `continueLocally` 和 stable requestId，但不虚构 authoritative runId；同一应用经 local owner 的 current-authority fence 调用 durable cancellation/resolve mechanism，空 disposition 仍保持迁移前成功语义。两种表面均只解析 wire、构造 caller、调用应用并映射错误/结果。
- 行为保护与旧路收敛：保持 `conversationId/sessionId` alias、Anchor durable 缺 requestId/runId 与非法 identity 的原错误、legacy no-work 的 NOT_FOUND、local consent/中文错误、user-cancel reason/time、resolve strict exact-key wire、三种 decision、owner epoch/open-fact digest、authenticated principal、resolve result、response-loss/replay、取消 first-wins 与公开 RPC/Event 全等。Server `session.abort` 已删除对 `ConversationManager.abort/cancelDurableRuns`、durable principal、Advancement load/settle/recovery 的业务编排，`session.resolve` 已删除 manager 直调；local router 已删除 `owner.cancelTurns/resolveDurableUncertain` 直调，local owner 只保留 `cancelConversationRuns/resolveConversationUncertain` Correctness mechanism。未进入 send/admission/run、Advancement 内部状态机、Workscene、Channel 或 A6，两个既有 bridge 均未改变。
- 直接证据与验证：core Conversation application 1 文件 14/14，覆盖两 Command、identity/epoch/digest/decision fail-closed、空取消、依赖结算及失败后恢复；Server handler 1 文件 9/9，覆盖 wire identity/error、同 dispatcher 调用、Advancement 匹配/恢复和 resolve principal；真实 Server loopback abort/resolve 定向 1 文件 7/7（同文件其余 72 项未运行）；CLI Anchor binding 与 Executor-local router 2 文件 14/14，覆盖 durable/legacy principal、resolve、local empty-success 与唯一应用调用；local owner lifecycle 只重验受接口变化影响的 write-fence 1/1。首次把完整 local-owner lifecycle 与本包两文件合跑时为 27/28，唯一失败是既有 storage maintenance `backpressured:ioOperations`，与 run-control 调用图无关；随后只重验失效的 write-fence 闭包通过，未重复其余长时测试。fresh `@zhixing/core`、`@zhixing/server` 与 `pnpm cli:build`、canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 均通过；未运行根级回归或制品验收。
- 结构失效与交接：S7 与反向 mutation 冻结两个领域 Command/port/application、9-operation exact-set、Anchor 唯一 binding、Executor-local 同应用调用、Server/local 旧直调归零，并拒绝漏 command、恢复 Manager/owner 直调、第二 run-control owner 或 Host 漏接。以后 abort/resolve command/result、durable/legacy/local identity 规则、principal、disposition/Advancement 后置顺序、empty/no-work 终态、strict resolve wire、Owner/Authority mechanism、Product API composition、Server/local binding、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-12e 当前完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只进入 send/admission/run，不扩入 Workscene 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-12f：归位 Conversation 普通 Agent turn admission/start 应用责任

- 实施基线与唯一领域应用：进场为 `HEAD 928cc04d75f0defe20f608ba9226e92be935d457 + task-doc:A5-12f-dispatch + A5-12f-conversation-agent-turn-admission-v1`，索引为空。A5-12d/e 的同一 `ConversationDirectoryApplicationService` 新增相邻且同属一个应用的 `prepare-agent-turn-identity` 与 `admit-agent-turn` Command、有限 `ConversationAgentTurnAdmissionPort/IdentityPort/ExecutionPort`、不可伪造的 `ConversationPreparedAgentTurnIdentity` 与稳定 admission result；Conversation Product API exact-set 由 9 个 operation 扩为 11 个，Fact Event 仍为既有 3 个。领域应用唯一拥有 durable stable turnId 必需性、legacy 单次生成、普通 Agent 非空输入、现存/新建/预分配 conversation identity、`immediate/queued/replayed/not-found/full/lifecycle-busy` 分类、durable admission 后投影和 immediate start-once 顺序；admission 只消费同一应用产生的 prepared identity，不再执行第二份 identity policy，不暴露 Manager、Journal、Authority、Kernel runtime、RPC、Server 或本地 Owner 实现。
- Correctness、Anchor 与 Executor-local 单链：新增唯一窄入口 `@zhixing/owner-kernel/conversation-agent-turn-admission`，只把领域 admission request 映射到既有 `ConversationManager.admitTurn → admitDurableTurn` 串行/容量/耐久机制，并把 Manager 返回的 task 转成一次性 start handle；它不决定产品错误或公开响应。Anchor 唯一组合根把同一应用及该 adapter 加入既有 sealed Product API；`session.send` 在取得 Manager、执行 Advancement/Perspectives 预处理、创建/ensure conversation、登记 observer 或通知前，先经 `prepare-agent-turn-identity` 取得一份 prepared identity，此后普通 Agent、Advancement 与 Perspectives 分支都只消费其 `turnId`，普通 admission 再把同一 prepared identity 交给 `admit-agent-turn`。Executor-local 唯一应用同样先 prepare 后 admit；local owner 只提供 current-authority fence 与 execution effect，router 不再调用 owner admission。两拓扑共享身份、durable-before-run、queue/replay/start-once 决定，同时保留 Anchor Product API transport 与 Executor-local in-process binding 的真实差异。
- 行为保护与旧路收敛：保持显式 durable turnId、legacy 随机 ID、现存/首轮/预分配会话、稳定 `{conversationId,surfacePrincipal,turnId}` admission identity、同载荷响应丢失重放、异载荷冲突、容量 5/配置上限、公开 `SessionSendResult`、`runId`、immediate/queued/replayed 投影、observer/lifecycle diagnostics、运行事件/终态/提交/异常顺序及本机 `continueLocally` 语义。Server 普通 Agent helper 已删除 `manager.admitTurn/admitDurableTurn`、exists/create 与 start 决定；local owner port 已删除 `runTurn/admitTurn` 第二应用入口，local router 只调用领域应用。Perspectives、Advancement 的领域预处理/状态机、Channel inbound、observer 管理、abort/resolve、Kernel run 与其他 accepted-work owner未迁移；Advancement 确认/取消后的原始普通 Agent turn 只复用本命令，不改变其领域状态机。
- 直接证据与验证：core Conversation application 1 文件 21/21，覆盖 stable/legacy identity、伪造 prepared identity 在 mechanism 前拒绝、现存/创建/预分配身份、non-empty、三种成功投影、not-found/full/lifecycle-busy、onAdmitted-before-start 与 Product API；Server 薄 binding 1 文件 13/13，新增 durable 缺 turnId 在普通 Agent、Advancement 草案和 Perspectives admission 前统一返回原 `INVALID_PARAMS` 且零推进/会话/通知效果，并直接证明 legacy 普通与 Advancement 预处理各只生成一次 identity；真实 Server loopback 本轮重验 Perspectives/Advancement 5/5，初次普通 Agent 定向 8/8 继续有效；Executor-local router 11/11，真实 Anchor+Executor/Executor-only owner conformance 3/3。local owner lifecycle 初次 14/15 的唯一共享 storage maintenance 背压项已在 fresh 进程定向 1/1 通过。fresh core/Server 与 CLI build、canonical S7 coverage/mutation 32/32 + registry golden、fresh `runtime:package-exports`、最窄 Biome 与 `git diff --check` 通过；owner-kernel 机制输入未变并复用初次 fresh build。额外 CLI `tsc --noEmit` 的接受基线 A5-12e 诊断未因本纠正扩面。
- 协调反证与恢复：初版只在 `admit-agent-turn` 内依据 `legacy-generated` 拒绝 durable 缺 ID，导致 Advancement/Perspectives 可能先于普通 admission 产生效果，故初次“所有 `session.send` 在效果前守住 stable identity”结论失效。本纠正把规则提升为同一 Conversation 应用的前置 prepared identity Command；Server 删除 `generateTurnId/validateTurnId` 与任何 `manager.usesDurableTurnProtocol()` 判断，所有分支共享一份领域结果，普通 admission 只接受该不可伪造结果。S7 三条反向 mutation 分别把校验下沉回普通 admission、恢复 Server Manager 判断或漏掉前置领域 gate，均会失败；未迁入 Advancement/Perspectives 状态机。
- 协调复核夹具纠正：协调者独立复取时，`session-send-methods.test.ts` 13/13 已通过，但完整 `session-rpc.test.ts` 的“in-flight turn 期间 task-list update 返回 `BUSY` 且零写”用例仍直接构造缺少 Conversation Product API 的 `ServerContext`，使新的正式 `session.send` 入口在建立在途 turn 前 fail closed；这是测试夹具未随生产必需依赖迁移，不是 handler 应回退的产品反证。该用例现复用同文件既有 `createConversationProductApi({ directory, conversations })` 与内存 directory，继续通过真实 `session.send` 建立在途 turn，并保持 `session.taskListUpdate` 为 `BUSY`、`updates` 为空的原识别力；未增加 handler fallback、Manager 旁路或第二应用入口。最窄复取为隔离反例 1/1、Server 薄 binding 13/13、完整 `session-rpc.test.ts` 79/79；生产、声明、构建、S7 与 package-export 输入未变，前述 fresh build、canonical S7 与 exports 证据继续有效。
- 结构失效与交接：S7 与反向 mutation 冻结领域两 Command/三个窄 port/11-operation exact-set、prepared identity provenance、owner-kernel 唯一非根 subpath/build entry、Anchor 前置 Product API gate、Executor-local 同应用 prepare/admit、Server/local 旧 admission 与 identity policy 入口归零，并拒绝 Manager 直调/判断、Owner `runTurn/admitTurn`、identity gate 下沉或漏接、root barrel/第二 export、漏组合或 durable adapter 漂移。以后普通 Agent Command/result、turn/conversation identity、principal/turnOrigin/environment、admission status/queue/start 顺序、Manager/Journal mechanism、execution projection、Anchor/local binding、owner-kernel export/build、S7 或公开 wire任一变化，本记录精确失效并只重验该闭包。A5-12f 纠正完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只按 Conversation 剩余未归位责任拆包，不进入 Perspectives、Advancement 内部状态机、Channel、Workscene、A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-12g：归位 Conversation task-list 查询与更新应用责任

- 实施基线与唯一领域应用：进场为 `HEAD 0851629c5ab84e2a18a62414fada354da0de299d + task-doc:A5-12g-dispatch + A5-12g-conversation-task-list-application-v1`，索引为空。既有 `ConversationDirectoryApplicationService` 新增有限 `ConversationTaskListPort`、task-list Query/Command/result/view/Fact 合同，唯一拥有 add/done 输入不变量、索引与 id-prefix 选择、already-completed/disabled no-write、not-found/busy、稳定 operation identity、权威更新后的 committed snapshot 与变更 Fact。Conversation Product API exact-set 由 11 个 operation 扩为 13 个，Fact Event 由 3 个扩为 4 个；task-list Command 显式声明 Fact subset，因为合法 no-write 结果保持零 Fact，而 Product API 其余 Command 继续默认要求 exact fact-set，未放宽未知或重复 Fact。
- Correctness、Anchor 与 Executor-local 单链：Anchor 唯一组合根用 `ConversationManager.runMaintenanceExisting`、当前 `TaskListService` 与 UUID 机制实现 path-free adapter，并把同一应用贡献给 sealed Product API；Server 的 `session.taskList/session.taskListUpdate` 只保留 wire 校验、领域错误映射和成功 Fact 的 `session.changed` 传输。Executor-local owner 只提供 lifecycle command gate、当前 Authority、`SessionStatePort.readTaskList/mutate` 与稳定 SHA-256 task id/operation mechanism，local router 只调用同一领域应用。由 LLM/tool 已权威提交的 task-list 更新仍在既有 access surface 接受 committed state 后，复用领域唯一 `createConversationTaskListChangedFact` 形成同一通知投影，不建立第二更新决定或全局订阅。
- 行为保护与旧路收敛：保持查询快照、add/done、在途 turn busy、未知 conversation/task、已完成任务 no-write、提交后 revision/snapshot、持久 mutation/replay、RPC 方法/载荷、`session.changed` 和单机/Executor-local 存储语义；写失败或 busy/not-found 不发布 Fact。删除 `ServerContext.taskListSnapshot/taskListUpdate`、Anchor `command.ts` 的回调闭包与 TaskListService 全局变更订阅、Executor-local router 对 session state 的业务直调，以及 CLI `task-list-actions` 旧应用 owner 与测试旁路；Owner/Manager/TaskListService/SessionState 只保留串行、权威读写和恢复机制。未迁移 Advancement 内部状态机、Perspectives、Channel、Workscene 或其他 Conversation 生命周期。
- 直接证据与验证：core Conversation application 与 Product API catalog 2 文件 30/30，覆盖查询、成功提交/committed Fact、invalid/missing/already-completed 零写零 Fact、busy/not-found、stable operation 与 subset/default-exact Fact 规则；Server 完整 `session-rpc.test.ts` 1 文件 79/79；CLI Anchor adapter、Executor-local RPC 与既有 task command 3 文件 21/21，真实 local-owner task-list authority 边界定向 1/1。一次把完整 local-owner lifecycle 与上述三文件合跑为 36/37，唯一失败是未受本包影响的既有恢复用例触发 storage maintenance `backpressured:ioOperations`；随后只重取本包新增的 real-owner 用例通过，未以该无关失败冒充本包回退。fresh core/server build、`pnpm cli:build`、canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、17 个受影响文件最窄 Biome 与 `git diff --check` 均通过；package-filtered CLI typecheck 仅保留进场基线中 A5-12 其他未归位文件的既有诊断，本包新增/修改 task-list 文件无新增诊断。
- 结构失效与交接：S7 与反向 mutation 冻结领域 Query/Command/port/Fact、13-operation/4-fact exact-set、合法 no-write 的显式 subset policy、Anchor 唯一 adapter、Executor-local 同应用消费、ServerContext 两回调和旧 `task-list-actions` 退场、local router 业务直调归零及 access surface 只从领域 Fact helper 投影；能拒绝第二 task-list owner、恢复 direct mutate、Host 漏接、宽松 Product API Fact 或旧文件复活。以后 task-list action/result/view/Fact、operation/task identity、Manager/Authority/SessionState mechanism、Anchor/local binding、RPC wire/error/notification、Product API Fact policy、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-12g 实施完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只按 Conversation 剩余未归位责任拆包，不进入 Advancement 内部状态机、Perspectives、Channel、Workscene 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调反证与恢复：初版 `ConversationTaskListUpdateResult` 直接携带可选 `fact`，使 Product API `dispatch.result`、Anchor JSON-RPC 和 Executor-local result 都可能新增内部字段；`toMatchObject` 未识别该公开 wire 回退。纠正后领域应用返回只在进程内使用的 `{ result, fact? }` outcome，正式 `ConversationTaskListUpdateResult` 严格只有 `ok/message/taskList`；Product API contribution 从同一次 outcome 分离 `result` 与 `facts`，Anchor 只从 `dispatch.facts`、Executor-local 只从 `outcome.fact` 投影恰一次 `session.changed`，两者都原样返回三字段 `result`，合法 no-write 与失败仍为零 Fact/零通知。core application/catalog 2 文件 30/30、真实 Anchor 完整 `session-rpc` 79/79、Executor-local router 与 Anchor adapter 2 文件 15/15通过；两拓扑测试均以实际对象 exact key-set 拒绝任何 `fact` 泄漏，并覆盖成功单次通知、no-write/失败零通知。fresh core/server/CLI build、canonical S7 32/32 与 registry golden、fresh package exports、6 个适用 TypeScript 文件最窄 Biome 通过；S7 新增反向 mutation，公开 result 恢复 Fact、Server 从 `dispatch.result.fact` 取值或 local 返回内部 outcome 均必失败。该反证所失效的 wire 与所有权证据现已恢复；A5-12g 仍停在同一等待协调者复核的安全交接点，Conversation 行与 A5 保持 `[ ]`，未进入其他责任链。

### A5-12h：归位 Conversation 手动上下文压缩应用责任

- 实施基线与唯一应用责任：进场为 `HEAD 1c039ec53393527ea5fceff5dc45c63cd9132093 + task-doc:A5-12h-dispatch`，索引为空。`ConversationDirectoryApplicationService` 新增有限 `ConversationCompactPort`、`ConversationCompactResult` 与 `conversation-window.command.compact`，Conversation Product API exact-set 从 13 个 operation 扩为 14 个、Fact Event 保持 4 个；领域应用唯一裁决非空 identity、busy/not-found/unsupported/local-unavailable 终态及 `modified = runtimeModified && windowApplied`、tokens 与 emergency-floor 的公开投影，compact 不产生领域 Fact。`ConversationManager.compactExisting` 继续只负责既有串行互斥、inactive-existing 激活、运行体 `forceCompact`、窗口折叠、派生 snapshot 与 attention-window hook 机制，不获得 Product API 或 wire 责任。
- 生产绑定与行为保护：Anchor 唯一 `createAnchorConversationCompactPort` 把 Manager 的机制状态和窗口产物投影给领域应用，`command.ts` 在唯一 Conversation application 组合点注入；Server `session.compact` 只校验 wire identity、dispatch Product API、映射既有 NOT_FOUND/BUSY/INTERNAL_ERROR 并在成功后转发现有 lifecycle diagnostics，不再直接调用 Manager 或解释窗口产物。Executor-local 应用显式贡献 unavailable mechanism，local router 仍经同一应用返回迁移前中文 BUSY，未把 context-budget/usage/security 或 Anchor runtime 能力引入本机拓扑。公开成功结果严格保持 `modified/tokensBefore/tokensAfter/emergencyFloor`，无 `windowCompact` 时即使运行体报告 modified 也返回 `modified:false`；存在性、在途互斥、运行体不支持、窗口折叠/snapshot/diagnostics、inactive 激活、公开方法名和错误文案均保持既有行为。
- 直接证据与验证：core Conversation application 1 文件 27/27，覆盖 Product API 零 Fact、结果 exact shape、no-window、四类机制终态与非法 identity；Anchor/Executor-local adapter 2 文件 16/16，覆盖 Manager 参数/产物映射、无窗口/status 透传及 local-only exact BUSY/零副作用。Server 完整 `session-rpc.test.ts` 81/81，真实 dispatcher 证明成功 exact wire + lifecycle warning、in-flight BUSY、NOT_FOUND 零激活和 unsupported INTERNAL_ERROR；Manager compact 定向闭包 14/14，保持窗口折叠、inactive/deletion race 与运行体支持机制。fresh core/server build 与 `pnpm cli:build` 通过；CLI package 无独立 `typecheck` script，未虚报该门禁。canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、10 个适用文件最窄 Biome 和 `git diff --check` 通过。
- 旧路、失效规则与交接：Server 对 `compactExisting` 的业务直调、结果判断和窗口字段拼装归零，local hardcoded compact 分支归零；S7 与反向 mutation 冻结 14-operation/4-fact exact-set、领域 port/command/result 决定、Anchor adapter 唯一接线、Server Product API dispatch、local 同应用 unavailable 终态，并拒绝 Host 漏接、Server Manager 旁路或本机硬编码回流。以后 compact command/result/error、Manager compact mechanism、窗口产物/diagnostics、Anchor/local binding、RPC wire、Product API exact-set、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-12h 实施完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只按 Conversation 剩余未归位查询/管理责任拆包，不进入 Advancement、Perspectives、Channel、Workscene 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-12h 交付物上独立复核并提交为 `53ba829d`；该记录转为已接受基线，A5-12i 只使上下文预算与用量查询闭包失效，不重开 compact 责任。

### A5-12i：归位 Conversation 上下文预算与用量只读应用责任

- 实施基线与唯一应用责任：进场为 `HEAD 53ba829d8405d5757eb7072228cc3fecd05e4953 + task-doc:A5-12i-dispatch`，索引为空。既有 `ConversationDirectoryApplicationService` 新增有限、只读的 `ConversationUsageProjectionPort`、`context-budget/usage` Query 与冻结结果投影；Conversation Product API exact-set 由 14 个 operation 扩为 16 个、Fact Event 仍为 4 个，两 Query 均零 Fact。领域应用唯一拥有非空 conversation identity、not-found/unsupported/local-unavailable 终态以及 `budget/turnCount/calibrationFactor/subUsages` 的稳定投影；context-budget 与 usage 是两个明确机制调用，前者绝不读取子 Agent 用量，后者才包含当前 attention-window 的完整 `subUsages`。`ConversationManager` 与 runtime 继续只负责 existing/inactive 激活、当前窗口 messages、预算估算、校准因子和子 Agent trailer 解析机制，不获得 Product API、RPC 或产品错误责任。
- 生产绑定与行为保护：Anchor 唯一 `createAnchorConversationUsageProjectionPort` 把 Manager 的两个现有机制状态投影给同一领域应用，`command.ts` 在唯一 Conversation application 组合点注入；Server `session.contextBudget/session.usage` 只保留 wire identity、Product API Query dispatch、迁移前 NOT_FOUND/INTERNAL_ERROR 映射、exact result 返回及成功后的 lifecycle diagnostics 传输，不再直接调用 Manager 或拼装用量字段。Executor-local 应用显式贡献两项 unavailable mechanism，local router 仍经同一应用返回迁移前中文 BUSY 且零 Owner/存储副作用；`session.security` 保持原独立路径，未纳入或改变。公开方法名、result key-set、预算值、turnCount、calibrationFactor、完整 sub-usage 可选字段、inactive-existing 激活、ghost 零激活、unsupported 文案和 diagnostics 均保持既有行为。
- 直接证据与验证：core Conversation application 1 文件 29/29，覆盖两 Query、零 Fact、context/usage 分流、完整 immutable exact shape、not-found/unsupported/unavailable 与非法 identity；Anchor adapter 与 Executor-local router 2 文件 17/17，覆盖两个 Manager mechanism/exists check、非成功终态、local-only exact BUSY 和零副作用。Server 完整 `session-rpc.test.ts` 83/83，真实 dispatcher 证明 context 不触发 sub-agent usage、usage 保留全部字段、两种 unsupported exact wire、ghost 零激活、两个 inactive identity 各激活一次并转发 lifecycle diagnostics。fresh core/server build 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、10 个适用文件最窄 Biome 均通过，未运行根级回归或制品验收。
- 旧路、失效规则与交接：Server 对 `inspectContextBudgetExisting/inspectUsageExisting` 的业务直调和字段拼装归零，local hardcoded context/usage BUSY 分支归零；S7 与反向 mutation 冻结 16-operation/4-fact exact-set、领域 port/Query/result 决定、Anchor 唯一 adapter/组合、Server Product API dispatch、local 同应用 unavailable 终态，并拒绝 Host 漏接、Server Manager 旁路或本机硬编码回流。以后 context-budget/usage Query/result/error、Manager/runtime mechanism、sub-usage 边界、inactive 激活/diagnostics、Anchor/local binding、RPC wire、Product API exact-set、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-12i 实施完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只按 Conversation 剩余未归位查询/管理责任拆包，不进入 Advancement、Perspectives、Channel、Workscene、security 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-12i 交付物上独立复核并提交为 `a9545507`；该记录转为已接受基线，A5-12j 只使会话安全状态只读投影闭包失效，不重开 context-budget/usage 责任。

### A5-12j：归位 Conversation 会话安全状态只读投影

- 实施基线与唯一应用责任：进场为 `HEAD a9545507b323dbd0fb45c2e33a1920f8c928c957 + task-doc:A5-12j-dispatch`，索引为空。既有 `ConversationDirectoryApplicationService` 新增有限、只读的 `ConversationSecurityProjectionPort`、`security` Query 与领域自有 `ConversationSecurityResult`；Conversation Product API exact-set 由 16 个 operation 扩为 17 个、Fact Event 仍为 4 个，本 Query 零 Fact。领域结果不依赖或别名 `RuntimeSecuritySnapshot`，而是逐字段新建并深度冻结 `contextId/workspacePath/permissionRules/builtinRules/rateLimits/confirmations`，包括嵌套 context、contributors、match spec 和数组；后续机制对象变更不能污染已交付结果。SecurityPipeline、permission/rate-limit/confirmation 与 `ConversationManager.inspectSecurityExisting` 继续只是 Correctness/Owner 机制，Conversation 应用唯一拥有 identity、not-found/unsupported/local-unavailable 终态和稳定产品投影。
- 生产绑定与行为保护：Anchor 唯一 `createAnchorConversationSecurityProjectionPort` 只把 Manager 的 existing/inactive 机制状态交给领域应用，`command.ts` 在唯一 Conversation application 组合点注入；Server `session.security` 只保留 wire identity、Product API Query dispatch、迁移前 NOT_FOUND/INTERNAL_ERROR 映射、成功后的 lifecycle diagnostics 传输和 exact result 返回，不再直接调用 Manager 或解释安全字段。`@zhixing/rpc` 的 `SessionSecurityResult` 改为领域结果类型，不再把 Owner `RuntimeSecuritySnapshot` 提升为公开 wire 合同；CLI `/security` 直接消费 readonly 领域结果且渲染字段、排序与文案不变。Executor-local 应用显式贡献 unavailable mechanism，local router 经同一应用保持原中文 BUSY 与零 Owner/存储副作用。Perspectives 的独立 runtime security snapshot、Trust/Security 决策、权限/确认行为和运行时安全链均未迁移或改变。
- 直接证据与验证：core Conversation application 1 文件 31/31，覆盖 Product API 零 Fact、全部字段与嵌套 union 的 fresh/deep-frozen 投影、源对象变更隔离、not-found/unsupported/unavailable 和非法 identity；Anchor adapter 与 Executor-local router 2 文件 17/17，覆盖 Manager existing check、状态透传、local-only exact BUSY 与零副作用。Server 完整 `session-rpc.test.ts` 83/83，以真实 dispatcher 证明完整 security wire、unsupported exact INTERNAL_ERROR、ghost 零激活和 security inactive identity 激活后 lifecycle diagnostics；CLI `/security` 表面 5/5 保持现有呈现消费。fresh core→rpc→server→CLI 依赖顺序构建全部通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、12 个适用文件最窄 Biome 和 `git diff --check` 通过。CLI package 仍无独立 `typecheck` script；额外非 canonical 的 `tsc --noEmit` 已确认本包 readonly DTO 适配错误归零，剩余报错均位于既有 delete/resume/run-control binding，未据此扩大本包或虚报 typecheck 通过。
- 旧路、失效规则与交接：Server 对 `inspectSecurityExisting` 的业务直调、Owner snapshot wire alias 与 local hardcoded security BUSY 分支归零；S7 和反向 mutation 冻结 17-operation/4-fact exact-set、领域 port/Query/result、Anchor adapter/唯一组合、Server Product API dispatch、local 同应用 unavailable 终态和 RPC 领域 DTO，并拒绝 Host 漏接、Server Manager 旁路、本机硬编码或 `RuntimeSecuritySnapshot` wire 回流。以后 security Query/result/error、Manager/runtime security mechanism、nested snapshot shape、inactive 激活/diagnostics、Anchor/local binding、RPC/CLI 表面、Product API exact-set、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-12j 实施完成并等待协调者独立复核；Conversation 行与 A5 继续 `[ ]`，下一检查点只按 Conversation 剩余未归位查询/管理责任拆包，不进入 Advancement、Perspectives、Channel、Workscene 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-12j 交付物上独立复核并提交为 `079fed26`；该记录转为已接受基线，A5-13a 只使 Workscene 五项管理应用责任闭包失效，不重开 Conversation 会话安全查询。

### A5-13a：归位 Workscene 五项管理应用责任

- 实施基线与唯一领域责任：进场为 `HEAD 079fed2645441753ad48525f1f77bdd3a1a222d9 + task-doc:A5-13a-dispatch`，索引为空。新增唯一窄入口 `@zhixing/core/workscene/application`，由 `WorksceneManagementApplicationService` 拥有 `list/create/rename/set-workspace/delete` 的有限 Query/Command、名称规范化、scene/request/workspace identity 校验、not-found/input/busy 分类和稳定 `sceneId/revision/name/workspace/lastActiveAt/workspaceWarning` 投影；Workspace Administration 只经 path-free metadata read port 提供 `workspaceBindingRevision/deviceName/workspaceName`，Authority/global state、workspace probe、per-scene serialization、revision fence、response-loss replay、session quiesce、删除 guard/cleanup 与 recovery 仍是 Correctness 机制。五个 Product API operation 组成一个无 Fact Event 的 exact-set；当前生产不存在需要补造的 Workscene 管理 Fact。
- 生产组合、RPC 纯绑定与旧路清理：Anchor 组合根只创建一次 `createAnchorWorksceneManagementPorts`、领域应用和 Product API contribution，并与既有领域 contribution 进入同一个 sealed dispatcher。`workscene.list/create/rename/setWorkdir/delete` handler 只保留认证后的 strict wire 参数解析、dispatcher 调用和既有 `INVALID_PARAMS/NOT_FOUND/BUSY/INTERNAL_ERROR` 映射，不再取得管理实现、规范化名称、查询 workspace catalog 或拼装产品结果；`@zhixing/rpc` 的 `WorksceneSummary` 改为领域稳定投影类型。Server `WorksceneDirectory` 删除 list/create/rename/setWorkdir/remove/get/recover/recordActivity 管理合同，只保留 enter/exit 与其结果元数据所需的最短桥；CLI `WorksceneToolDirectory` 同步删除未被工具消费的五项管理入口，避免工具装配重新取得第二写路径。`createWorksceneDirectory` 继续作为 Anchor Correctness mechanism 实现原 revision/幂等/探测/清理事实，业务名称规范化从该机制退出；assignment staged 的既有 Workscene 管理工具、确认和提交语义未改变。
- 行为保护与初次直接证据：core 领域应用 1 文件 4/4，覆盖 authority 顺序、workspace enrichment/freeze、名称一次规范化、request/workspace identity、not-found/input/busy/未知机制错误和 1 Query + 4 Command + 0 Fact exact-set；Server 真实 dispatcher/wire 1 文件 10/10，覆盖 CRUD/list、workspace bind/unbind、strict 参数、INVALID_PARAMS/NOT_FOUND/BUSY、enter/exit/Advancement 桥及管理 handler 零 Directory 直调；CLI Correctness/组合 3 文件 14/14，覆盖 adapter 机械映射、Authority-backed revision/replay/workspace probe/quiesce/delete cleanup、Host 单 dispatcher contribution 与旧接口归零，合计 5 文件 28/28。fresh core→rpc→server→CLI 依赖顺序构建、canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、15 个适用文件最窄 Biome 和 `git diff --check` 均通过。初次记录把未成功闭合的额外 package typecheck 误写成“本包新增 Workscene 类型错误归零”；协调者随后在提交 `e498fe8f` 上直接复现四处 Workscene 诊断，该句与 A5-13a 验收证据当即失效，不能归入既有 Conversation 基线。
- 协调反证与恢复：`createWorksceneRenameCurrentTool` 已删除从未消费的 `Pick<WorksceneToolDirectory, "rename">` 参数，唯一生产投影与直接测试均只传当前 scene identity，未把管理写入口加回工具端口。`AnchorWorksceneDirectory` 改为 `WorksceneDirectory & WorksceneToolDirectory & 管理机制` 的显式交叉组合：Server 的 optional catalog 仍只属于未迁移 enter 结果 metadata，工具与管理 adapter 在 Anchor 具体对象上取得同一个 required catalog，不使用非空断言或第二投影。当前 package typecheck 已确认 Workscene/A5-13a 文件与调用链诊断归零，只剩 HEAD 已登记的 Conversation delete/resume/run-control 诊断。原 A5-13a 五文件 28/28 全量重取通过，另加受影响 staged workmode 工具 1 文件 7/7，当前纠正直接闭包合计 6 文件 35/35；`pnpm cli:build` 通过。S7 新增两个反向 mutation，恢复旧 rename 管理参数或 optional/required 双接口继承均会失败；canonical coverage/mutation 32/32 与 registry golden 通过，4 个适用 TypeScript 文件的最窄 Biome、`git diff --check` 与空索引边界也已复核。enter/exit 桥、管理 Product API、Authority/Correctness 语义和 A5-13b 边界均未改变。
- 结构失效与交接：S7 与反向 mutation 冻结 Workscene 领域应用/Correctness port、五 operation/零 Fact、core 窄 subpath/build/export、Anchor 唯一 adapter/application/contribution、Server 五 handler Product API dispatch、Server enter/exit 桥及工具端管理入口退场；恢复 Server Directory 直调、工具 management port、第二应用/dispatcher、根导出、漏 operation 或结果拼装都会使门禁失败。以后管理 Query/Command/result/error、name/request/workspace 规则、Authority revision/replay/probe/delete cleanup、workspace metadata、Product API exact-set/Host composition、五 RPC wire、S7 或 package export 任一变化，本记录精确失效并只重验该闭包。A5-13a 实施完成并等待协调者独立复核；Workscene 行与 A5 继续 `[ ]`，下一检查点固定为迁移 `workscene.enter/exit` 并退役 `A5-WORKSCENE-ENTRY-01`，不得进入 Advancement、Conversation transcript/删除桥、Channel、Device、Backup 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-13b：归位 Workscene enter/exit 应用责任

- 实施基线与唯一应用：进场为 `HEAD 9009d931af7667775c3b48cc0b93e8bd33badd06 + task-doc:A5-13b-dispatch`，索引为空。A5-13a 的唯一 `@zhixing/core/workscene/application` 从管理专用入口收敛为 `WorksceneApplicationService`，在原 1 Query + 4 Command 上增加 `workscene-entry.command.enter/exit`，形成 1 Query + 6 Command + 0 Fact 的有限 exact-set；领域应用拥有 scene/observer/request/conversation identity 校验、scene-conversation 归属、not-found/input/busy 终态和稳定 scene/workspace 元数据投影。`WorksceneSessionOwner`、Authority/global state 与 conversation manager 继续只作为 Correctness mechanism，唯一负责 observer claim、scene current conversation、请求 replay、activity fact、per-scene serialization 与恢复，不形成第二应用决定。
- 生产链、旧桥退场与跨领域边界：Anchor 只创建一次 `createAnchorWorksceneApplicationPorts`，把管理、workspace read 与 entry Correctness ports 注入同一个应用和 sealed Product API dispatcher。`workscene.enter/exit` RPC 只保留认证连接到稳定 observer identity、strict wire 参数解析、Product API dispatch、既有错误映射和结果传输；enter 提交成功后的 Advancement `recoverConversation/loadAdvancementState` 仍是独立跨领域响应投影，失败只记录并软降级，不回滚已经成立的 Workscene observer/entry 事实。`ServerContext.workscenes`、`CreateContextOptions.workscenes`、Server `WorksceneDirectory`、`packages/server/src/runtime/workscene-directory.ts`、runtime 转导和旧 management adapter 全部删除；CLI 的 `AnchorWorksceneDirectory` 只作为一个具体 Correctness 组合类型存在，Server/RPC 不再取得该实现对象。公开方法名、请求/结果、全域 conversation id、workspace metadata、Advancement 状态、response-loss replay、并发 observer 和 activity 行为不变。
- 直接证据与验证：core 应用 1 文件 5/5，覆盖 enter/exit identity、scene-conversation 绑定、workspace 投影、not-found/input 与 1 Query + 6 Command + 0 Fact exact-set；Server 真实 dispatcher/wire 1 文件 10/10，覆盖七项 operation 支持门、enter/exit 参数与 observer/request 转发、NOT_FOUND/INVALID_PARAMS/BUSY、Advancement 成功投影和恢复失败软降级；CLI Correctness/组合 3 文件 14/14，覆盖同 scene 并发 enter 的单一 conversation、observer guard、activity、请求重放、adapter 映射、Host 单 dispatcher 和 `ServerContext.workscenes` 退场，合计 5 文件 29/29。fresh core→rpc→server→CLI 依赖顺序构建、canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports` 均通过。core package typecheck 通过；server/CLI 额外 `tsc --noEmit` 未出现本包诊断，只保留已登记的 Conversation A5 后续责任链诊断，故不把 package typecheck 虚报为全绿。
- 失效规则与交接：S7 与反向 mutation 冻结 1 Query + 6 Command + 0 Fact、同一领域应用/adapter/contribution、enter/exit handler Product API dispatch、Server context/runtime bridge 零残留、工具端 management 零回流、core 窄 subpath/build/export和单一 Host composition；恢复 Server Directory/context 直连、漏 entry operation、第二 adapter/application/dispatcher、根导出、RPC 结果重组或 scene-conversation 归属旁路都会使本证据失效。以后 Workscene Query/Command/result/error、entry observer/request/replay/activity、workspace 投影、Product API exact-set、Advancement 响应投影、RPC wire、Host composition、S7 或 package export 任一变化，只恢复对应闭包。`A5-WORKSCENE-ENTRY-01` 已退役；Workscene 行与 A5 继续 `[ ]`，本包停在等待协调者复核，不进入 Advancement、Conversation transcript/删除桥、Channel、Device、Backup 或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-13b 交付物上独立复核并提交为 `4189ec7d`；该记录转为已接受基线，A5-13c 只使 Workscene 会话运行投影闭包失效，不重开七项 RPC、Server bridge 退场或 Advancement 响应投影。

### A5-13c：归位 Workscene 会话运行投影产品裁决

- 实施基线与唯一应用：进场为 `HEAD 4189ec7dd658f88242459121676e3bdd49305e01 + task-doc:A5-13c-dispatch`，索引为空且工作区只有协调者预登记的本文台账。既有唯一 `@zhixing/core/workscene/application` 增加 path-free `WorksceneRuntimeProjectionReadPort`、有限 `WorksceneConversationRuntimeQuery` 与冻结的 `main | scene { sceneId, name, workspace binding }` 投影；同一个 `WorksceneApplicationService` 唯一裁决 conversation scope、当前 scene identity、workspace binding、not-found 与 invalid-input。Product API exact-set 仍严格为 1 Query + 6 Command + 0 Fact，runtime query 不是新增公开 Product Surface operation；Authority-backed directory 只作为同一 adapter 的 Correctness read mechanism，不拥有路由、profile、workspace 选择或运行结果。
- 生产组合与基础设施 binding：Anchor 在 `worksceneDirectory` 后只创建一次 ports 与应用实例，该实例同时供七项 Product API contribution、conversation runtime factory 和 guidance workspace 查询消费。runtime factory 已删除 `parseConversationId`、`WorksceneDto` 与 `getScene` 直读：首次领域投影冻结 scene identity/name；调用方显式 environment workspace 时保持该裁决且不二读；无 binding 时发放 null workspace；存在 binding 时以同一领域 query 二读当前 Authority，仅用最新 binding 交给 Anchor adapter 做 device/executor 归属、`resolveWorkspace`、`probePath` 与按需建目录，首次 scene name/identity 仍保持。首次 scene 消失继续报原“不存在”终态；二读消失、binding 消失或无法解析继续在 runtime 发布前报原“工作区无法在当前 executor 解析”；remote binding、非目录/非法 probe 与准备失败也不发布 runtime。guidance 通过同一应用读取一次当前 binding，缺 scene/无 workspace 仍退化为原 global guidance；`command.ts` 的 `worksceneDirectory.get` 产品旁路归零。
- 行为保护、旧路与直接证据：main、带/不带 caller workspace 的 scene、二读 binding 更新/消失、power profile/prompt、Kernel scene identity、七项 Workscene 工具和 capability catalog 均继续由既有 Anchor projection 机械映射；RuntimeHost/Kernel 合同、A5-13b 七项 RPC、Advancement 响应投影、Workscene Authority/持久格式和删除桥未改变。core 领域应用 1 文件 7/7 覆盖 main/scene、冻结 finite projection、当前 binding、not-found/invalid 与重复读取；CLI 3 文件 14/14 覆盖 main/scene 路由、显式/null/二读 workspace、消失/更新、零提前发放、adapter runtime port、Host 单应用/双消费和 directory 旁路归零；Server dispatcher/wire 1 文件 10/10 证明新增必需 runtime port 未改变七项 RPC，合计 5 文件 31/31。
- 构建、门禁、失效与交接：fresh core build 与 `pnpm cli:build` 通过；额外 CLI `tsc --noEmit` 未出现 Workscene/A5-13c 诊断，只保留接受基线中的 Conversation delete/resume/run-control 诊断，未虚报为全绿。canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、最窄 Biome 和 `git diff --check` 通过；反向 mutation 能拒绝 runtime factory 恢复 directory/getScene/parse 旁路、command 直读、adapter 漏 runtime port、第二应用或 Product API exact-set 漂移。以后领域 runtime query/projection、directory read adapter、同一应用组合、首次/二读 binding 时序、Anchor device/path/probe binding、profile/identity/tool/capability 映射、S7 或 package export 任一变化，本证据精确失效并只重验上述闭包。`A5-CONVERSATION-WORKSCENE-DELETE-01` 仍保持打开且未触碰，Workscene 行与 A5 继续 `[ ]`；A5-13c 当前完成并等待协调者独立复核，不进入删除桥、Advancement、Channel、Device、Backup、A6 或其他责任链。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-13c 交付物上独立复核并提交为 `88aaa560`；该记录转为已接受基线，A5-13d 只使 Workscene 删除级联物理投影清理闭包失效，不重开七项 RPC、会话运行投影或 Server bridge 退场。

### A5-13d：退役 Workscene 删除级联临时桥并归位物理投影清理需求

- 实施基线与唯一责任：进场为 `HEAD 88aaa5608f0459642a489ab7875453e9b1124669`，索引为空；协调者派发时仓库未带预登记差异，执行者只在本文补记本包台账。唯一 `@zhixing/core/workscene/application` 增加 path-free、只含 `removeCommittedProjection({ sceneId, conversationId })` 的 `WorksceneConversationStorageProjectionCleanupPort`；它表达 Workscene Authority 的 session-delete fact 已提交后，对 Conversation 物理存储投影的清理需求，不取得普通 Conversation delete command、准入、busy/not-found/deleted 终态、Product API Fact、通知、Manager、路径或存储实现。Anchor 只创建一次 `createAnchorWorksceneConversationStorageProjectionCleanup`，机械验证 conversation 为同一 Workscene scope 后调用既有 `deleteStoredConversation` 物理原语；没有第二 application、第二 admission state 或可写兼容旁路。
- 顺序、恢复与旧桥退场：`WorksceneSessionOwner.removeScene` 对每个 conversation 继续先以稳定 `workscene-delete:${sceneId}:${conversationId}` identity 提交/重放 `deleteWorksceneSession` Authority fact，成功后才调用领域需求 port；全部 conversation 均完成后才调用 scene storage cleanup。物理投影清理失败不会提前清 scene，既有 committed storage-maintenance obligation 保持 pending，启动/维护重驱重新进入同一顺序；Authority request replay 与幂等物理删除使响应丢失和重复 action 不产生第二事实或第二投影。`ConversationWorksceneDeleteProjectionBridge`、`createConversationWorksceneDeleteProjectionBridge` 及其 command/directory/session-owner 生产引用全部删除；普通 Conversation delete 仍走原 application/Correctness 链，Workscene 删除没有获得其产品权限。
- 直接证据与验证：Workscene session owner/Anchor adapter/directory/storage 4 文件 21/21，覆盖两 conversation 的 `authority → projection → authority → projection → scene` 顺序、projection 失败后同 identity 重驱、跨 scene fail closed、端口 finite/frozen、同 scope 映射、scene storage 末位与幂等物理删除；core committed maintenance/recovery 1 文件 6/6，证明 cleanup 失败保留 obligation 并可恢复重驱；Conversation directory 1 文件 12/12，证明普通与 Workscene 物理路径边界未漂移；S7 生产装配与 startup owner 2 文件 6/6，证明单一 adapter/consumer、同一 Host 注入和旧 bridge 零残留，合计 8 文件 45/45。fresh core build、`pnpm cli:build`、canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports` 通过；CLI `tsc --noEmit` 未出现 A5-13d/Workscene 新诊断，只保留已接受的 Conversation 后续责任链诊断，未虚报 package typecheck 全绿。
- 有限反查、状态与精确失效：从唯一 Workscene application/Product API contribution、Anchor application/cleanup adapter、Authority-backed session owner、runtime projection、七项工具/capability 与 RuntimeHost 隔离反向复核，A5-13a/b/c 的管理、enter/exit、运行投影证据继续有效；本包删除最后一个已登记 Workscene 临时桥后，未发现独立 Workscene 写入口、第二事实 owner、Product API/RPC 旁路或产品装配残余，故 Workscene 行标为 `[x]`，A5 仍为 `[ ]`。以后该 cleanup port/adapter、session delete Authority fact 或稳定 request identity、storage-maintenance obligation/recovery、Conversation physical projection primitive、逐 conversation/scene cleanup 次序、Host composition、S7 或 package export 任一变化，恢复 `A5-13d-workscene-delete-projection-cleanup-v1` 并只重验上述闭包；管理、entry 或 runtime projection 的独立变化仍按 A5-13a/b/c 精确恢复。本包完成并等待协调者独立复核，不进入普通 Conversation delete、Advancement、Channel、Device、Backup、Executor/Mesh、A6 或 A7；未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调验收：协调者已在同一 A5-13d 交付物上独立复核并提交为 `8e99fa55`；该记录转为已接受基线，Workscene 行保持 `[x]`。A5-14a 只使 Conversation identity/shell 的 Server bridge 闭包失效，不重开 Workscene 管理、entry、runtime projection 或删除投影清理。

### A5-14a：退役 Conversation identity/shell Server bridge

- 实施基线与唯一应用责任：进场为 `HEAD 8e99fa556304f260d72af61111b77645d9538db7`，索引与工作区为空；协调者派发文本预期有本文预登记差异，但仓库事实未携带该差异，本轮按真实基线补齐台账。既有唯一 `ConversationDirectoryApplicationService` 在同一个 `@zhixing/core/conversation/application` 窄入口增加 `conversation-identity.query.exists` 与 `conversation-identity.command.ensure-shell`，Product API exact-set 现为 7 Query + 12 Command + 4 Fact。领域应用唯一拥有 bounded conversation identity 校验、耐久身份存在判断与固定全域 ID 的幂等 shell 建立；两项 operation 经既有 `ConversationAgentTurnIdentityPort` 取得 Correctness mechanism，不产生 Fact Event、不读取路径/Store/Server 类型，也没有第二 application、dispatcher 或 identity state。
- 生产消费、行为保护与旧桥退场：`session.send` 的预分配 ID、Advancement prepare/maintenance、Perspectives admission 与 inactive `session.subscribe` 四类 Server 消费现在只经同一 Product API dispatcher 查询或命令；handler 仅保留 wire 参数、既有错误映射和消费时序。Anchor adapter 的 `exists` 继续以 meta 或 transcript 任一存在为 true，不激活 runtime、不 touch；`ensure-shell` 继续按 user/workscene 全域 ID 路由并以同一 `repo.ensure → transcript.init` 建立 meta+transcript shell，重复调用幂等。identity descriptor 未装配时，原 optional bridge 的 ensure helper no-op、existing-check unavailable 与 inactive subscribe false 退化保持；需要其他 Conversation Product API 的入口仍按既有 `INTERNAL_ERROR` fail closed，已装配 application 但 identity mechanism 缺失也不静默放行。`ServerContext.conversationDirectory`、`CreateContextOptions.conversationDirectory`、Server `ConversationDirectory`、`packages/server/src/runtime/conversation-directory.ts`、runtime index/root 转导、handler 的 `requireDirectory` 与直接 exists/ensure 全部删除；fresh Server declaration 不再泄漏该合同。CLI 的 `AnchorConversationDirectoryMechanism` 只为现有 application/Correctness 与尚待归位的本机 lifecycle 直接消费者保留，未迁移 transcript、delete/clear/resume/run control。
- 直接、结构与构建证据：core Conversation application 1 文件 32/32，新增 user/workscene exists、同一 shell 重复 ensure、冻结结果与非法 identity fail-closed；Server 真实 RPC/绑定 2 文件 96/96，覆盖四类生产消费、inactive persisted/ghost subscribe、Advancement/Perspectives/普通 send、busy/not-found 与既有通知；distributed runtime golden 1 文件 3/3。fresh core、server、CLI 依赖顺序 build 通过，fresh `pnpm runtime:package-exports` 通过，Server `dist/index.d.ts` 对旧字段/接口零泄漏；canonical `pnpm s7:lint` 为 coverage/mutation 32/32 且 registry golden 通过。受影响 structural test 已删除退役的 `server/runtime` zone；其运行继续暴露接受基线中与本包无关的旧 A4 runtime-host manifest 期望（仍期待已退役的 `@zhixing/mcp/@zhixing/tools-builtin` 依赖），本包未越界改写该历史断言，也未把它冒充当前失败。
- 精确失效、遗留与交接：S7 与反向 mutation 冻结两个领域 operation、同一 contribution/dispatcher、四类 Server 消费、Server context/runtime/root export 零残留，并能拒绝旧 runtime 文件、context 字段、RPC directory 直连或漏 operation 回流。以后 identity query/ensure command、`agentTurnIdentity` mechanism、meta-or-transcript existence、meta+transcript shell 次序、四类 handler 消费、Product API exact-set/Host composition、Server declaration、S7 或 package export 任一变化，恢复 `A5-14a-conversation-identity-shell-product-api-v1` 并只重验上述闭包。`A5-CONVERSATION-LIFECYCLE-01` 尚未关闭：CLI `AssemblyContext/access-surfaces` 仍直接消费 exists/ensure/ensureTranscript，须由紧邻工作包归位；Conversation 行与 A5 保持 `[ ]`。本包完成并等待协调者独立复核，不进入该本机 lifecycle、transcript、普通 delete/clear/resume/run control、Advancement、Channel、Device、Backup、Executor/Mesh、A6 或 A7；未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-14b：退役 Conversation 本机 identity/shell lifecycle 旁路

- 派发基线与唯一结果：`HEAD 99c2ffa41613d2b15c6ec7ae59f38e524f7b34ae + task-doc:coordinator-dispatch`，索引为空；只关闭 `A5-CONVERSATION-LIFECYCLE-01`。CLI 本机组合必须让 Conversation 应用边界拥有 identity 存在判断与持久 shell/transcript 初始化策略，`AssemblyContext` 和 `access-surfaces` 不再认识或直接消费 `AnchorConversationDirectoryMechanism`，也不得把该 mechanism、路径、Repository 或 Transcript 暴露给 Surface、Protocol、Manager 或其他领域。
- 生产闭包与行为保护：迁移当前四类直接消费——Advancement review maintenance 的 conversation existence、Protocol clear 投影前的 shell 保证、Protocol delete 投影的 storage existence、ConversationManager 激活时 user/workscene storage 初始化。保持既有 `protocol.ensureSession` 先行、user scope 建立 meta+transcript、workscene scope 只初始化 transcript 且不创建第二 scene meta、exists 以 meta 或 transcript 任一存在为 true、重复调用幂等，以及 clear/delete/recovery 的顺序、错误、Fact/通知与异常终态；跨领域只消费 Conversation 拥有的有限应用端口。可以调整组合时序和现有应用合同，但不得新增运行期服务定位、可变 lazy ref、第二 application/dispatcher、第二事实源或永久桥。
- 旧路与边界：删除 `AssemblyContext.conversationDirectory` 和 `access-surfaces` 中全部 `exists/ensure/ensureTranscript` 机制直调；若 `AnchorConversationDirectoryMechanism` 已无生产类型消费者，删除该兼容接口并让 `createConversationDirectory` 只暴露真实需求方端口。组合根仍可把同一物理 adapter 注入 Conversation application/Correctness 的窄机制端口，不能把普通 clear/delete/resume/run-control、transcript schema、Advancement 状态机、Channel、Workscene、Device、Backup、Executor/Mesh、A6 或 A7 扩入本包；`command.ts` 中仅用于现有唯一 Conversation application/Correctness adapter 和独立 Advancement storage reader 的 directory 使用不按名字误删。
- 最窄证据与完成条件：直接覆盖 user/workscene 首次与重复激活、meta-only/transcript-only existence、clear/delete projection、Advancement maintenance、异常传播和装配顺序；结构门禁必须证明 Surface/AssemblyContext 机制直调及兼容接口零残留，并能反向识别其恢复。按验证手册只运行受影响 core/CLI 直接测试、必要 typecheck/build、S7 与 package export；共享 identity operation、adapter 或 Host composition 若变化，精确重取 A5-14a 受影响证据。只有生产端与全部四类消费端改绑、旧桥归零、行为等价且代码处于可构建可运行单一真相时，才登记 A5-14b 完成并关闭桥；Conversation 行只有在有限反向审计和 package typecheck 也无其他迁移残余时才能标为 `[x]`，否则保留 `[ ]` 并登记唯一下一责任链。预计超过四小时、出现独立跨域问题或无法同时保持 user/workscene 语义时，在上述单一真相检查点止损反馈，不得扩面。
- 实施基线与唯一应用端口：进场为 `HEAD 99c2ffa41613d2b15c6ec7ae59f38e524f7b34ae + task-doc:coordinator-dispatch`，索引为空。Conversation 窄入口新增冻结的 `ConversationIdentityLifecycleApplication`，由 `createConversationIdentityLifecycleApplication` 唯一拥有 identity 存在、identity 创建、普通持久 shell 与 activation storage 初始化策略；它只依赖 `ConversationIdentityLifecycleMechanism`，不暴露 Repository、Transcript、路径、Surface、Protocol 或 Manager。Anchor 组合根只创建一份 lifecycle application，并把同一实例同时交给 A5-14a 的 `agentTurnIdentity` adapter 与四个本机协作者；没有第二 dispatcher、运行期 locator、可变 lazy ref 或第二事实源。
- 四类消费与行为保护：Advancement review maintenance 和 durable delete projection 的存在判断只调用 `identityExists`；durable clear 投影前的 shell 保证只调用 `ensureShell`；ConversationManager activation 继续严格先 `protocol.ensureSession`，再调用 `initializeRuntimeStorage`。领域应用按 scope 决定 user 使用既有 `repo.ensure → transcript.init`、Workscene 只 `transcript.init` 且不创建第二 scene meta；meta-only 或 transcript-only 任一事实仍使 identity 存在，重复初始化幂等，mechanism 异常原样传播。clear/delete/recovery 的 Authority、投影、Fact/通知与异常次序未改变，跨领域消费者只见有限 Conversation 应用端口。
- 旧路、结构与有限反查：`AssemblyContext.conversationDirectory`、`access-surfaces` 的全部 `.exists/.ensure/.ensureTranscript` 直调及 `AnchorConversationDirectoryMechanism` 兼容接口归零；`createConversationDirectory` 只按 `ConversationDirectoryStorage`、identity lifecycle mechanism 的需求方法及既有 clear/delete/resume/Advancement storage ports 暴露能力。S7 冻结 lifecycle application owner、Host 单实例、四个 Surface 消费、user/workscene 分支、core 根/Conversation 宽入口零转导，并以反向 mutation 拒绝 scope 反转、Surface mechanism 回流、AssemblyContext 旧字段或兼容接口复活。有限反向检索只剩 `command.ts` 中唯一 Conversation application/Correctness adapter 和独立 Advancement storage reader 对物理 directory 的合法消费；`A5-CONVERSATION-LIFECYCLE-01` 因而关闭。
- 直接证据与验证：core Conversation application 1 文件 35/35，覆盖冻结端口、user/workscene scope、首次/重复初始化、非法 identity 与 mechanism 异常传播；CLI directory/surface 2 文件 16/16，覆盖 meta-only/transcript-only existence、真实 user shell、Workscene transcript-only/零 scene meta、历史恢复和 `protocol.ensureSession` 先于 lifecycle 初始化。fresh core build 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 32/32 与 registry golden、fresh `pnpm runtime:package-exports`、本包最窄 Biome 通过。CLI package-filtered `tsc --noEmit` 诚实返回 18 条既有 Conversation adapter 诊断：delete dependent cleanup 的返回类型，以及 resume/run-control adapter 的隐式参数类型；本包未越界修改这些普通 delete/resume/run-control 合同，也未把 typecheck 冒充通过。
- 状态、失效与下一检查点：Conversation 行保持 `[ ]`，唯一下一链收敛为 A5-14c 的 application/Correctness adapter package type closure；它不得恢复本 bridge 或改变领域/公开行为。以后 identity lifecycle application/mechanism、scope 初始化策略、meta-or-transcript existence、`protocol.ensureSession` 顺序、四个本机 consumer、A5-14a agent-turn identity adapter、Host composition、S7 或 package export 任一变化，恢复本记录并只重验对应闭包；其他 Conversation application 责任仍按既有 A5-12 证据精确失效。A5-14b 完成并等待协调者独立复核，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-14c：闭合 Conversation adapter 类型边界并完成领域终验

- 派发基线与唯一结果：`HEAD add620a6cd305e02c59a2554695217d867893b74 + task-doc:coordinator-dispatch`，索引为空；只消除当前 CLI package typecheck 精确报告的 18 条 Conversation adapter 诊断，并对 A5-12a～A5-14b 已迁移责任做一次有限反向闭包审计。不得借类型修复重开领域合同、改变公开或持久语义、恢复 lifecycle bridge或扩入其他领域。
- 实现边界：delete projection/commit 的 dependent cleanup 必须显式适配现有 `Promise<void>` 端口，等待既有 Advancement 调用完成但丢弃其返回值，不改变调用次数、顺序或错误传播；resume 与 run-control binding 必须从现有 Conversation application port 类型推导或声明完整输入类型，消除隐式 `any`，不得复制 DTO、放宽类型或引入断言绕过。修复必须同时覆盖 Anchor 两个 delete 组合点和两个 binding 的真实消费者。
- 领域终验：CLI package typecheck 归零后，从 Conversation Product API exact-set、application/Correctness ports、Anchor/Executor-local binding、Server/RPC/CLI surface、Host composition、identity/clear/delete/resume/run-control/turn/task-list/compact/usage/security、历史/恢复和物理 storage projection 正反核对，主动寻找第二 application/dispatcher、旧 Manager/directory 业务直调、Surface 决定、宽导出、打开桥和等价改名残余。只处理能够反绑 AE-001 与当前生产事实的 Conversation 迁移残余；无反证时关闭 Conversation 行，不重复 A5-12～A5-14 已有效的全部测试。
- 证据、边界与交接：运行三个 adapter 的最窄直接测试、CLI package typecheck、必要 CLI build；只有相关 S7 输入变化才重跑对应 S7，其他有效证据复用。必须记录 18 条诊断归零、有限反向审计范围与结果、精确失效条件。不得进入 Advancement 状态机、Channel、Device、Backup、Executor/Mesh、A6 或 A7。完成条件是类型闭包真实成立、产品行为零变化、Conversation 无未处置迁移残余并可将其登记行标为 `[x]`；若审计发现独立真实残余，保持 `[ ]` 并停在可构建可运行检查点登记唯一下一链。
- 实施与行为边界：Anchor 两个 delete 组合点把 Advancement dependent cleanup 收窄为显式 `async` 适配，等待既有 `cancelOpenConversationSession` 完成后丢弃其 session 返回值，调用次数、顺序、失败传播与后续 data removal 均未改变。resume binding 的 identity/adoption 输入和 run-control binding 的 cancel/uncertain 输入均以 `Parameters<现有 Conversation port>`（含 `NonNullable` 可选成员）直接推导，消除重复 DTO、隐式 `any` 与断言旁路；领域合同、Product API descriptor、公开 wire 和持久行为零变化。
- 直接证据与类型闭包：CLI directory/resume/run-control 三个定向文件 18/18 通过；package-filtered `tsc --noEmit -p tsconfig.json` 零诊断，进场 18 条 Conversation adapter 诊断全部归零；`pnpm cli:build` 与四个受影响 TypeScript 文件的最窄 Biome 通过。`command.ts/access-surfaces.ts` 属于 S7 生产扫描输入，故精确复取一次 canonical `pnpm s7:lint`，coverage/mutation 32/32 与 registry golden 通过；导出与 build entry 未变，复用 A5-14b 已接受的 fresh package-export 证据，未重复无失效闭包。
- 有限领域终验与退出裁决：从 7 Query + 12 Command + 4 Fact 的 Conversation Product API exact-set、唯一 `ConversationDirectoryApplicationService`、application/Correctness ports、Anchor 单 dispatcher、Executor-local 同应用直调、Server/RPC/CLI/Channel binding、identity/clear/delete/resume/run-control/agent-turn/task-list/compact/usage/security、历史/恢复与物理 storage projection 双向回扫；生产构造只剩 Anchor 与 Executor-local 两个拓扑适配点，Server directory/runtime bridge、Surface 业务决定、旧 lifecycle mechanism 直调、第二 dispatcher/application、core 根或 Conversation 宽入口转导及等价改名残余均为零。现存 Manager/Protocol/Directory 使用均属于已接受的串行 owner、observer/运行、Correctness/recovery 或独立 Advancement storage read 机制，不形成第二产品决定或写入口。Conversation 全部已登记责任因此闭合并将登记行标为 `[x]`；A5 仍保持 `[ ]`，下一领域为 Advancement。
- 精确失效与交接：以后 delete related cleanup 端口/两个组合点、resume/run-control port 或 adapter、CLI package typecheck、Conversation operation/fact exact-set、领域应用/Correctness port、Anchor/Executor-local 绑定、Server/RPC/CLI/Channel 消费、Host dispatcher、Manager/Protocol/storage 投影边界、S7 或 package export 任一变化，只恢复对应 A5-12～A5-14 证据与 Conversation 行并重验受影响闭包；其他领域变化不误伤本记录。本包完成并等待协调者独立复核，未进入 Advancement、Channel、Device、Backup、Executor/Mesh、A6 或 A7，也未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-15a：迁移 Advancement detail 查询垂直切片

- 派发基线与唯一结果：`HEAD 4d840174e97a3430973eeb866b5aa8281a87b87c + task-doc:coordinator-dispatch`，索引为空。只把 `session.advancementDetail` 的“选择最新推进会话并形成可展开详情”产品决定迁入唯一 Advancement application，并通过 Product API Query 供 Server binding 消费；这是 Advancement 的首个垂直切片，不预建整个领域合同或第二 controller。
- 生产责任链：Advancement application 拥有 `loadLatestSession` 需求端口、会话不存在时的 `null` 终态、最新 review 选择、rubric title 与 `buildClosureFacts` 投影；Correctness/owner-services 只提供当前 `AdvancementController.loadLatestSession` 机制适配。定义一个冻结且 path-free 的 detail Query/Result 与有限 Product API exact-set/contribution；Anchor Host 在已有 Advancement controller 存在时只装配一次并并入现有 sealed dispatcher。Server handler 只保留 wire 参数校验、operation availability 和 DTO 映射，不得再引用 `buildClosureFacts` 或直接读取 controller。
- 行为保护与退场：严格保持无 Advancement 组合时 `detail:null`、从未有会话时 `detail:null`、open 优先而无 open 返回最新终态、rubric title 优先 confirmed 后 pending、`facts` 与 `lastReview` 字段及错误传播不变。删除该 handler 的 controller/storage/closure 业务直调；不得迁移 revise/cancel/confirm、send/admission、review/evidence/recovery、Conversation 的 active summary projection，或改变 RPC method、wire、事件、持久格式和用户文案。
- 证据与完成条件：用 Advancement application 直接测试证明 open/terminal/null、title/facts/last-review 投影；用真实 `session.advancementDetail` RPC 测试证明装配存在/缺失、wire exact shape 和旧 controller 直调退场；更新现有 S7/包导出结构证据以冻结唯一 application、Host contribution、Server Product API 消费和无根级宽导出。只运行受影响 core/owner-services/server/CLI 直接测试、必要构建、S7 与 package export。只有生产端、Host、dispatcher、Server 消费与旧路删除同链闭合且行为等价时本包完成；Advancement 行保持 `[ ]`，下一链由协调者另行裁决。约四小时仍未闭合或发现必须同时迁移另一个产品决定时，停在可构建可运行检查点反馈，不得扩面。
- 实施与责任迁移：新增只从 `@zhixing/core/advancement/application` 可达的 path-free `AdvancementDetailReadPort`、只读 Query/Result、`AdvancementApplicationService` 与单 Query/零 Fact Product API contribution。应用层现唯一负责缺会话 `null`、confirmed-before-pending 标题、最后一条 review 与 `buildClosureFacts`；Anchor 把现有 controller 的 `loadLatestSession` 收窄为机制适配，并将可选 contribution 合入既有唯一 sealed dispatcher。`session.advancementDetail` 只验证 conversation identity、检查 operation availability、调用 Query 并显式映射 wire DTO；该 slice 对 controller、store 与 closure helper 的业务直调已归零。
- 行为保护与退场结果：真实 controller 继续唯一实现 open 优先、否则最新终态的持久投影读取；未装配 Advancement contribution 与从未有会话均保持 `detail:null`，现有 rubric title、facts、lastReview、exit、错误传播和公开 RPC 名/shape 不变。新增应用没有进入 core 根或 Advancement 宽入口，也未迁移 revise/cancel/confirm、send/admission、review/evidence/recovery 或 Conversation active summary；无第二 application、dispatcher、状态或写入口。
- 直接、结构与构建证据：领域 application 1 文件 4/4，覆盖 null/非法 identity、open/terminal、confirmed-before-pending、最新 review、closure facts 与冻结结果；真实 Server RPC 定向 1 文件 3/3，覆盖 open/无会话、无 contribution `null`、最新终态与原 wire 投影；Anchor 组合根结构测试 1 文件 5/5，证明 application/contribution 先于唯一 dispatcher 且只装配一次。新增 S7 inspector 与四条反向 mutation 冻结唯一 Query owner、Host contribution、Server Product API 消费、宽入口/直调退场；canonical `pnpm s7:lint` 为 33/33 且 registry golden 通过，fresh `pnpm runtime:package-exports` 通过。依赖顺序 core、owner-services、server、CLI build 与 CLI `tsc --noEmit` 通过，最窄 Biome 通过。
- 精确失效与交接：若 Advancement detail Query/Result/应用规则、controller `loadLatestSession` 机制、Host contribution/dispatcher exact-set、Server handler/wire projection、core 窄 subpath/build entry、S7/package-export 或上述直接测试任一变化，只恢复 A5-15a 并重验本闭包；Advancement 其他状态机变化不误伤本证据，除非改变本 Query 的输入或结果。A5-15a 当前完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`，下一责任链仍由协调者从 rubric revise/cancel/confirm 或 send/admission 中选择。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调者独立验收：在未修改工作区反查 Advancement application、controller 机制、Anchor 唯一 dispatcher、Server handler 与公开 wire，确认 detail 选择和投影只有一个领域应用 owner，Server 对 controller/closure 的旧业务直调为零且无宽入口泄漏。独立重跑 application 4/4、真实 RPC 3/3、Anchor 组合根 5/5、CLI typecheck、core fresh build、canonical S7 33/33 与 registry golden、fresh package exports 和 `git diff --check`，全部通过；接受 `A5-15a-advancement-detail-v1` 并提交为 `c96fea2a`。Advancement 行与 A5 继续为 `[ ]`。

### A5-15b：迁移 Advancement Rubric 修订垂直切片

- 派发基线与唯一结果：`HEAD c96fea2a + task-doc:coordinator-dispatch`，索引为空。只把 `session.advancementRevise` 的“在待确认状态下按用户反馈生成并持久化新准则草案”产品决定迁入现有唯一 `AdvancementApplicationService`，以一个 Product API Command 及其 Fact Event 供 Server binding 消费；不得新建第二 application/controller/dispatcher。
- 生产责任链：领域应用拥有非空身份与反馈的防御性约束、待确认会话与 pending draft 前置、调用准则修订机制、持久化新 draft、修订版本/结果投影及 `contract_draft` 事实。现有 contract builder、store 和 Conversation maintenance 继续作为机制/Correctness 端口由 Anchor 适配；Host 只在既有 Advancement contribution 上增加一次 Command/Fact 并并入同一 sealed dispatcher。Server 只保留 wire 参数校验、认证、Product API 调用、既有 RPC error 映射和 Fact→`advancement:contract_draft`/DTO 映射，不再直接调用 controller、builder、store 或执行业务 maintenance。
- 行为保护与明确不做：严格保持 feedback trim/空值拒绝、conversation/session identity、只能修订 awaiting-confirmation 且必须有 pending draft、连续修订 draft/version、无 main run、busy/not-found/error、事件 seq/payload、RPC response shape 与公开方法不变。不得迁移 confirm、cancel/直接执行、send/admission、review/evidence/recovery、detail Query、active summary、持久格式或用户文案；不得把 Conversation maintenance 复制进领域或把 RPC 错误类型反向带入 core。
- 最窄证据与完成条件：领域 application/机制适配直接测试覆盖正常与连续修订、空反馈、错误状态/缺 draft、异常传播、不可变结果和单 Command/Fact exact-set；真实 `session.advancementRevise` RPC 测试覆盖 trim、busy/not-found、两次修订、事件 exact shape、无 main run 及旧 controller/maintenance 直调退场。更新 S7 与 package-export 结构证据，能反向识别第二 owner、Server 业务直调、宽导出和漏装配；只运行受影响 core/owner-services/server/CLI 的直接测试、必要依赖构建、typecheck、S7 与 package export。只有应用决定、Host/dispatcher、Server binding、全部直接消费、旧路删除和行为等价同链闭合时本包完成；Advancement 行仍保持 `[ ]`。
- 安全交接点：约四小时仍未闭合、发现必须同时改变 confirm/cancel 或持久协议、连续两条实质路径失败，或影响面越出本 Command/Fact 直接闭包时，停在可构建、公开 RPC 仍只有一条生产主链且无双写/双 owner 的检查点反馈；不得顺带扩入下一状态转换，不得执行任何 Git 写操作。
- 执行结果（`A5-15b-advancement-rubric-revision-v1`）：现有 `AdvancementApplicationService` 新增唯一 `advancement.command.revise-rubric` Command 与 `advancement-contract-draft-revised` Fact；应用在 Conversation maintenance 串行门内防御性校验 identity/trim 后反馈、读取并验证 awaiting-confirmation/pending draft、调用修订机制并持久提交，且只从 persist 返回的 `updated.pendingRubricDraft / updated.id / updated.rubricDraftVersion` 权威投影形成冻结 Result/Fact；提交后缺少 pending draft 时 fail closed，不回退到提交前候选或二次读取。`AdvancementController` 的旧复合 `reviseRubricDraft` 与 `AdvancementRevisedDraft` 已删除，只保留 load/build/persist 三个 path-free 机制动作；Anchor 用现有 `ConversationManager.runMaintenanceExisting` 与 conversation directory existence 适配同一 application contribution，并继续并入唯一 sealed dispatcher。
- RPC 与行为保护：`session.advancementRevise` 不再取得 `AdvancementController`/`ConversationManager` 或调用 `runAdvancementMaintenance`，只执行既有 wire 校验、Product API Command、应用错误映射及 Fact→`advancement:contract_draft`/response 投影。trim/空反馈拒绝、conversation/session identity、连续 draft/version、待确认与 pending 前置、NOT_FOUND/BUSY/既有内部错误、公开 response、事件 `runId/seq/payload/meta` 和不启动 main run 均保持；confirm/cancel/send/admission/review/evidence/recovery/detail/active summary 与持久格式未迁移。生产扫描中旧 Controller 复合入口、Server 业务 maintenance/直调及第二 contribution/dispatcher 均为零。
- 直接与结构证据：领域 application 1 文件 8/8，覆盖 detail 既有投影、正常/连续修订、trim、空反馈、busy/not-found、错误状态/缺 draft、机制异常、提交前候选与提交后规范化投影分歧、提交后 draft 缺失 fail closed、冻结共享草案及 Query+Command+Fact exact-set；真实 Server RPC 1 文件 2/2，经实际 Controller/builder/store/Conversation maintenance 覆盖 trim、两次 revision、事件 exact shape、无 main run、NOT_FOUND 与 BUSY wire；Anchor 唯一组合根 1 文件 5/5，含同一 controller 机制和 maintenance 注入，总计 3 文件 15/15。core、owner-services、server 与 CLI 依赖顺序 build、CLI `tsc --noEmit` 通过；按验证手册以 PowerShell 数组向 Biome 传入本包全部 11 个代码/脚本文件，11/11 通过且 package-export 缩进漂移已归零；canonical S7 33/33 且 registry golden 通过，fresh `pnpm runtime:package-exports` 通过。
- 精确失效与交接：若 Advancement revision Command/Result/Fact、awaiting/pending/trim 决定、Controller 三机制动作、Conversation maintenance/existence 适配、Host contribution/dispatcher exact-set、Server error/event/wire 投影、core 窄 subpath/build entry、S7/package-export 或上述直接测试任一变化，只恢复 A5-15b 并重验本闭包；confirm/cancel 等其他 Advancement 状态转换变化不误伤本证据，除非改变该 Command 的输入、maintenance 或待确认 session/draft 投影。A5-15b 当前完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：独立复核指出初版 Result/Fact 在 persist 返回后仍读取提交前 `revisedDraft`，以及 package-export 检查脚本新增块缩进漂移；本轮以提交后权威 session 投影替代候选读取，并新增 S7 反向 mutation 拒绝 `revisedDraft` 回流。上述 8/8 application、2/2 RPC、5/5 Host、构建/typecheck、11/11 Biome、S7/golden、fresh exports 与 `git diff --check` 均在纠正后的同一工作区重取；初版未获接受，本条保留反证与恢复过程供后续复核。
- 协调者独立验收：纠正后的同一工作区再次反查领域状态决定、persist 后权威投影、Controller 三项机制、Host maintenance 适配、Server Command/Fact 纯绑定及旧复合入口，确认只有一个修订 owner、一个持久写入口且无提交前候选回流。独立重跑 application 8/8、真实 RPC 2/2、Anchor 组合根 5/5、changed-file Biome、canonical S7 33/33 与 registry golden、package export 和 `git diff --check`，全部通过；接受 `A5-15b-advancement-rubric-revision-v1` 并提交为 `53b66c0a`。Advancement 行与 A5 继续为 `[ ]`。

### A5-15c：迁移 Advancement Rubric 取消与原任务交接垂直切片

- 派发基线与唯一结果：`HEAD 53b66c0a + task-doc:coordinator-dispatch`，索引为空。只把 `session.advancementCancel` 的“取消待确认准则，并按用户选择结束或直接执行原始任务”完整产品决定迁入现有唯一 `AdvancementApplicationService`，形成一个 Product API Command、取消 Fact 与可选 Conversation 原任务执行端口；不得新建第二 application/controller/dispatcher。
- 生产责任链：领域应用在 Conversation maintenance 串行门内拥有 identity/executeOriginal 约束、awaiting-confirmation 前置、pending draft/original turn 选择、取消原因与消息、终态持久提交、取消结果/Fact 以及 execute-original 分支的先取消后交接顺序。现有 review-attempt 终止、资源清理、Advancement store 和 Conversation agent-turn 执行继续作为机制/Correctness 或跨领域应用端口由 Anchor 适配；`AdvancementController.cancelRubric` 复合产品入口必须退场。Server 只保留认证、wire 校验、表面身份准备、Product API 调用、既有错误映射及 Fact/DTO/进展呈现，不得再直接决定取消分支、读取 session 或调用 controller/Conversation manager 执行业务流程。
- 行为保护与明确不做：严格保持仅 awaiting-confirmation 可取消；pending draft 存在且 `executeOriginal:true` 时复用原 `turnId`/原输入并返回 `direct-execution`，否则返回 `cancelled`；取消事件必须继续在原任务交接尝试前可观察，event seq/runId/payload、busy/not-found/error、runStatus、公开 response、review-attempt 终止与资源清理均不变。不得迁移 confirm、rubric publication、settleOriginalTaskAdmission、send/admission 总链、review/evidence/recovery、detail/revision 或 active summary；不得把 RPC/connection 类型带入领域合同。
- 最窄证据与完成条件：领域 application/机制/跨域端口直接测试覆盖 cancelled、direct-original、无 draft 降级、错误状态、busy/not-found、持久后权威终态、取消 Fact 先于交接、交接异常和不可变结果；真实 `session.advancementCancel` RPC 覆盖普通取消、直接执行、事件 exact shape/order、原 turn identity、无重复 main run、错误与旧 handler/controller 直调退场。更新 S7/package-export 证据并反向识别第二 owner、Surface 分支、事件次序漂移、宽导出和漏装配；只运行本包失效的直接测试、必要依赖构建/typecheck、S7 与 package export。只有取消决定、终态、事件、跨域交接、全部消费端和旧路删除在一条生产链闭合时完成；Advancement 行仍保持 `[ ]`。
- 安全交接点：若事件先于交接的既有语义无法在 Product API/Host 边界无重复地保持、约四小时仍未闭合、必须改变 Conversation 领域合同或持久协议，或影响面越出 cancel/execute-original 闭包，停在现有 endpoint 仍只有一条可运行主链且无双写/双 owner 的检查点反馈；不得以推迟事件、吞掉交接错误或保留永久 Surface 编排换取绿灯，不得执行任何 Git 写操作。
- 实施与唯一应用链：进场为 `HEAD 53b66c0a + task-doc:coordinator-dispatch`，索引为空。现有 `AdvancementApplicationService` 新增唯一 cancellation Command、取消 Fact、持久取消机制端口和 Conversation 原任务执行端口；应用在同一 Conversation maintenance 内完成 identity、awaiting 状态、pending draft、取消原因/消息、持久终态与 execute-original 决定，退出 maintenance 后仍由同一应用严格先投影取消 Fact、再按决定调用 Anchor 跨领域执行端口，避免在 maintenance 锁内嵌套 Conversation admission。Anchor 适配只把既有 `ConversationDirectoryApplication` 的稳定 turn identity、admission 与运行效果接入；没有第二应用、dispatcher、状态或写入口。
- 旧路与行为保护：`AdvancementController.cancelRubric` 复合产品入口及其 Result 已删除，只保留 session 读取和既有 `cancelSession` 持久/终止/清理机制；Server handler 不再读取 Advancement session、运行业务 maintenance、决定 cancel/direct 分支或直调 controller/Conversation admission，只保留 wire/身份/效果投影、Product API 调用、错误映射与取消 Fact/结果呈现。仅 awaiting-confirmation 可取消、无 pending draft 时降级 cancelled、原 draft 存在时复用原 `turnId`/原输入、取消 Fact 在原任务交接前可观察、事件 seq/payload、busy/not-found/internal error、runStatus、review-attempt 终止与资源清理均保持原行为；confirm/publication/settle-original/send 总链和其他 Advancement 责任未迁移。
- 直接、结构与构建证据：领域 application 1 文件 11/11，覆盖持久后权威终态、cancel/direct/no-draft、busy/not-found/identity、Fact/交接失败及 Product API exact-set；Anchor 跨域适配与唯一组合根 2 文件 7/7，证明只经 Conversation 应用、原 identity/输入与 replay 映射；真实 `session.advancementCancel` RPC 定向 1 文件 6/6，覆盖普通取消、直接执行以及 direct-original 的 queue-full、lifecycle-busy、conversation-not-found、turn-identity-invalid 四类既有 wire 错误，合计 4 文件 24/24。core、owner-services、server 与 CLI 依赖顺序 build 通过；canonical S7 33/33 且 registry golden 通过，fresh `pnpm runtime:package-exports` 通过；Biome 对本包 10 个适用代码文件检查通过，`git diff --check` 通过。
- 精确失效与交接：若 cancellation Command/Result/Fact、awaiting/pending/identity/取消消息决定、Controller 持久取消机制、Fact-before-handoff 顺序、Anchor Conversation 应用适配、Host contribution/dispatcher exact-set、Server wire/error/事件投影、S7/package-export 或上述直接测试任一变化，只恢复 A5-15c 并重验本闭包；其他 Advancement 状态转换不误伤本证据，除非改变共享 maintenance、待确认 session/draft 或原任务 admission 合同。A5-15c 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`，下一责任链仍为 rubric confirm 与准则沉淀/原任务准入结清。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：独立复核指出初版 direct-original 交接已经改经 Anchor `ConversationDirectoryApplication`，但 `mapAdvancementRubricCancellationError` 未复用普通 send 的 Conversation admission→RPC 映射，导致 queue-full、lifecycle-busy、conversation-not-found 和 turn identity 错误可能退化为通用 INTERNAL。Server 现以唯一 `sessionAgentTurnAdmissionRpcError` 同时服务普通 send 与 Advancement 交接，保持原 code/message，RPC 四个对抗分支均直接证明取消 Fact 已先可观察且取消终态已耐久；S7 新增反向 mutation 拒绝 cancellation mapper 绕开共享映射。纠正后 application 11/11、Anchor 7/7、RPC 6/6、Server build、changed-file Biome、canonical S7 33/33 与 golden、`git diff --check` 均在同一工作区重取；初版 2/2 RPC 证据不足，本条保留反证与恢复过程。
- 协调者独立验收：在纠正后的同一工作区反查取消应用、持久机制、Fact-before-handoff、Anchor Conversation 适配、Server 纯 binding 与共享 admission 错误映射，确认只有一个取消决定、一个耐久写入口，取消事实在交接失败前已可观察，且旧 Controller/Server 业务分支归零。独立重跑 application 11/11、Anchor adapter/组合根 7/7、真实取消 RPC 6/6 与 `git diff --check`，并核对执行者本轮 Server build、changed-file Biome、canonical S7 33/33/golden 和 package export 证据；接受 `A5-15c-advancement-rubric-cancellation-v1` 并提交为 `da865f85`。Advancement 行与 A5 继续为 `[ ]`。

### A5-15d：迁移 Advancement Rubric 确认、原任务准入与准则沉淀垂直切片

- 派发基线与唯一结果：`HEAD da865f85 + task-doc:coordinator-dispatch`，索引为空。只把 `session.advancementConfirm` 的完整产品事务迁入现有唯一 `AdvancementApplicationService`：确认用户所见 Rubric、提交 confirmed 事实、发布确认 Fact、经 Conversation 应用准入原任务、耐久结清 admission，并按选择沉淀或降级准则；形成一个确认 Command 及其有限 Result/Fact，不新建第二 application/controller/dispatcher。
- 生产责任链：领域应用拥有 conversation/session/draft/persistence/surface identity 约束、awaiting/pending/stale-draft 前置、draft→confirmed contract 转换、原任务 input digest 与 admission intent、confirmed 持久提交、确认 Fact、Fact-before-admission 顺序、Conversation 准入结果、runId 结清以及 publication outcome/message。Controller、store、contract builder、rubric publication 与 Conversation admission 只作为机制/Correctness 或跨领域应用端口由 Anchor 适配；`AdvancementController.confirmRubric`、`settleOriginalTaskAdmission` 两个复合产品入口必须退场。
- 行为保护与故障终态：严格保持“确认所见版本”、原 turn/input/surface principal/turn origin、确认事件 seq/payload、稳定 admission intent、response-loss replay、direct/queued runStatus、准入成功后的 runId 结清和公开 response 不变。确认提交前发现原对话不存在时，只 best-effort 取消悬空 session 并返回原 RPC 错误，不投影尚未发生的确认或取消事实；确认提交后遇到原对话不存在或耐久 admission identity 冲突时，必须取消已确认的 Advancement session、发布原因明确的取消事件并返回原 RPC 错误。queue-full/lifecycle-busy 等既有非终止错误不得被误取消；准入已成功后的 settlement 写入异常不得覆盖成功响应，由 recovery 按 pending intent 重驱结清。generated draft 的可选准则沉淀继续不阻塞原任务：saved/deferred/failed/异常分别保持既有用户消息与降级，library draft 或未选择 persistence 不发布。
- 明确不做：不得迁移 `session.send` 的 Advancement admission/prepare 总链、active user turn、review/evidence/recovery、closure/active summary、rubric library 内部存储架构或其他领域；不得改变 RPC/wire、持久事件 schema、准则内容、用户文案或设备/拓扑语义。可复用 A5-15c 已证明的原任务执行端口，但若 confirm 所需 durable admission intent/settlement 无法在不污染取消合同的情况下表达，应增加确认专用的最窄跨领域端口，不得把 Surface 编排或 Controller 业务入口带回。
- 最窄证据与完成条件：领域 application/机制直接测试覆盖正常确认、stale/缺 draft/错误状态、提交后权威投影、Fact-before-admission、immediate/queued/replayed、准入结清、NOT_FOUND/耐久冲突取消、queue/lifecycle busy 不误取消，以及 publication saved/deferred/failed/throw/no-op；真实 `session.advancementConfirm` RPC 覆盖 wire exact shape、事件顺序、原 identity、错误 code/message、取消补偿与用户消息。更新 S7/package-export 结构证据，反向拒绝 Controller/Server 旧业务链、第二 application、未结清 intent、Surface 分支和宽导出；只运行本包失效的直接测试、依赖顺序构建/typecheck、changed-file Biome、canonical S7，package export 仅在导出输入变化时重跑。
- 安全交接点：本包是一个公开 Command 的原子产品闭包，但包含确认、准入、结清和 publication 四个不可拆开的终态阶段；预计超过四小时、发现必须改变 Conversation 领域合同或持久协议、需要进入 recovery/send 总链，或不能保持 confirmed Fact 先于原任务执行时，停在 endpoint 仍只有一条可构建可运行主链且无双写/双 owner 的检查点反馈。不得用吞错、先返回后结清、永久桥或保留旧 confirm 编排换取绿灯，不得执行任何 Git 写操作。
- 实施与唯一应用链：进场为 `HEAD da865f85 + task-doc:coordinator-dispatch`，索引为空。现有 `AdvancementApplicationService` 新增唯一 confirmation Command、confirmed Fact、确认机制端口、确认专用 Conversation admission 端口和可选 publication 端口；同一应用完成用户所见 draft/identity 校验、稳定 admission intent 形成、confirmed 耐久提交、Fact 投影、Conversation 准入、可用 `runId` 的耐久 settlement 以及 publication outcome/message。确认 Fact 在任何原任务 admission 前可观察；immediate admission 没有 `runId` 时保持迁移前返回语义且不虚构 settlement。Host 只把既有 Controller 机制、Rubric library 和 `ConversationDirectoryApplication` 接到有限端口，没有第二 application、dispatcher、状态或写入口。
- 旧路与故障终态：`AdvancementController.confirmRubric`、`settleOriginalTaskAdmission` 两个复合产品入口及其旧结果类型已删除，Controller 只保留确认/结清持久机制与 publication 效果；Server handler 不再读取 Advancement session、运行 maintenance、构造持久 intent、决定 admission/cancellation/publication 分支或直调 Conversation，只保留 wire/身份、Product API 调用、共享 admission 错误映射、Fact/DTO 与用户消息投影。Conversation not-found 或 durable identity conflict 会由领域应用 best-effort 取消已确认 session、发布带 `original-task-admission-failed` 原因的 cancellation Fact，再保留原 RPC 错误；queue-full/lifecycle-busy 不误取消。saved/deferred/failed/throw/no-op 的既有 publication 文案与非阻塞降级、原 turn/input/principal/origin、response-loss replay、immediate/queued/replayed 和公开 response 均保持。
- 直接、结构与构建证据：领域 application 1 文件 19/19，覆盖 confirmed 持久事实、stale/缺失 committed confirmation、Fact-before-admission、pre-confirmation NOT_FOUND 无虚假事实、settlement 失败恢复、publication 与 Product API exact-set；Anchor admission adapter 1 文件 6/6，覆盖原 identity、重放及四类 admission 错误分类；真实 `session.advancementConfirm` RPC 定向 1 文件 8/8，覆盖确认/取消事件、wire、queue/lifecycle 不误取消及原错误；Server controller/recovery 2 文件 47/47 与 CLI startup 1 文件 5/5 保护机制改名、恢复和唯一组合根，合计 6 文件 85/85。CLI `tsc --noEmit`、core/owner-services/server/CLI 依赖构建闭包通过；canonical S7 33/33 且 registry golden 通过，fresh `pnpm runtime:package-exports` 通过；Biome 对 15 个适用变更文件检查通过，`git diff --check` 通过。
- 精确失效与交接：若 confirmation Command/Result/Fact、awaiting/pending/stale-draft/identity/digest 约束、Controller 确认或 settlement 机制、Fact-before-admission、terminal admission compensation、Anchor Conversation 应用适配、publication outcome/message、Host contribution/dispatcher exact-set、Server wire/error/事件投影、S7/package-export 或上述直接测试任一变化，只恢复 A5-15d 并重验本闭包；send/admission、review/evidence/recovery 与 active summary 的后续变化不误伤本证据，除非改变共享 maintenance、session/draft、Conversation admission 或 Product API exact-set。A5-15d 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：独立复核以迁移前生产链推翻了初版两个错误终态。其一，确认尚未提交时 Conversation maintenance 返回 NOT_FOUND，迁移前只 best-effort 耐久取消悬空 session 并返回原错误，不投影 cancellation 事件；应用现以独立 pre-confirmation 补偿完成同一取消且严格零 confirmed/cancelled Fact，只有 confirmed 已提交后的 conversation-not-found/idempotency-conflict 才保持 confirmed→cancelled 两事件。其二，原任务 admission 已返回 `runId` 后 settlement 属于 best-effort 恢复机制；写入异常现保留 confirmed/pending 权威并返回同一成功 `runId/runStatus`，不覆盖已经开始的任务，既有 recovery 仍按耐久 intent 重驱并结清；settlement 成功返回的投影仍执行严格 admitted 校验，admission、confirmed commit 或 Fact 错误均不吞掉。新增 application 两条反例后该文件 19/19，通过 recovery 定向代表 1/1 证明 pending 重启结清；canonical S7 33/33 与 registry golden、项目依赖顺序 `pnpm build`、changed-file Biome 均通过。初版 application 17/17 未覆盖这两项公开行为，不能单独作为完成证据；本条恢复 A5-15d 对应失效闭包。
- 协调者独立验收：反查迁移前后确认事务、Controller 机制、Anchor Conversation admission、Server 纯 binding、恢复重驱与 S7 反向门禁，确认只有一个确认决定、一个持久写入口且两处纠正保持原公开终态。独立重跑领域 application 19/19、Anchor admission/组合根 11/11、真实确认 RPC 8/8、pending admission recovery 1/1 与 `git diff --check`，并核对执行者同基线 build、canonical S7 33/33/golden、fresh package exports 和 Biome 证据；接受 `A5-15d-advancement-rubric-confirmation-v1` 并提交为 `d083e64c`。Advancement 行与 A5 继续为 `[ ]`。

### A5-15e：迁移 awaiting-rubric 自然语言控制垂直切片

- 派发基线与唯一结果：`HEAD d083e64c + task-doc:coordinator-dispatch`，索引为空。只把 `session.send` 命中 `awaiting-rubric-confirmation` 会话后的三种现有产品决定迁入唯一 `AdvancementApplicationService`：保持等待、取消待确认任务、降级为直接执行原始任务；形成一个有限 Command/Result/Fact 闭包，Server 只保留 wire、observer、事件和 DTO 投影。
- 生产责任链：领域应用在 Conversation existing-maintenance 内加载并校验当前 awaiting session，经现有 admission strategy 机制按 `hasOpenAdvancementSession` 分类用户输入；`keep-awaiting-confirmation` 返回持久草案投影且不写不发 Fact，`cancel-pending-task` 耐久取消后发布 `user-cancelled` Fact，`downgrade-to-direct` 耐久取消并先发布取消 Fact、再经现有 Anchor Conversation 应用端口交接原始 turn/input。无 awaiting session 必须返回明确 not-applicable，由 `session.send` 继续非 awaiting 状态链，不得由 Server 猜测会话状态。
- 行为保护与旧路退场：严格保持显式 `engage` 在无 awaiting 时绕过推进、awaiting 时仍处理待确认合同；保持原 admission 输入/近期上下文降级、草案全文与 identity、observer 时机、事件 seq/payload/reason、自然语言取消 response exact shape、降级直执的原 turn 和既有 Conversation admission 错误/队列/运行语义。删除 `hasAwaitingAdvancementConfirmation`、Server 对三种 prepare result 的业务分支，以及 Controller `prepareUserTurn` 中 awaiting 分支和对应旧结果类型；不得保留同责 fallback。`A5-ADVANCEMENT-PREPARE-01` 只能让非 awaiting 的新任务与 active 状态继续走旧 Controller，不能回流本分支。
- 明确不做：不得迁移新任务的 start-advancement/draft 创建、active user-turn/takeover/rubric regeneration、review/evidence/recovery、active summary、Rubric 显式 revise/confirm/cancel 或其他领域；不得改变 RPC/wire、持久 schema、admission strategy、用户文案、Conversation 合同、设备或拓扑语义。
- 最窄证据与完成条件：领域直接测试覆盖三种 action、无 awaiting/not-found/busy/identity、keep 零写零 Fact、两种取消的 committed 投影与 Fact-before-handoff、handoff 失败不伪造成功；真实 `session.send` RPC 覆盖 engage、observer、三种 exact response/event、原任务 identity 及既有错误映射。更新 S7 与反向 mutation，证明 Server 只经 Product API、Controller awaiting 决策与旧 helper 归零、non-awaiting bridge 不可吞并本分支；只运行受影响的最窄直接测试、必要依赖构建/typecheck、changed-file Biome、canonical S7，export 输入变化时才运行 fresh package exports。
- 安全交接点：预计超过四小时、需要修改 Conversation 持久协议或 admission strategy、出现两个以上独立未知、无法保持 Fact-before-handoff，或为删除旧分支被迫进入新任务/active 链时，停在 awaiting 只有一个可构建可运行 owner 且 non-awaiting bridge 明确的检查点反馈；不得扩成整个 `session.send` 或整个 Advancement 迁移，不得执行任何 Git 写操作。
- 实施与唯一应用链：进场为 `HEAD d083e64c + task-doc:coordinator-dispatch`，索引为空。现有 `AdvancementApplicationService` 新增唯一 `advancement.command.control-awaiting-rubric` Command、有限 Result、awaiting admission 机制端口及既有 cancellation Fact 的条件贡献；领域应用先进入 Conversation existing-maintenance，随后只在该互斥 operation 内唯一读取并校验权威 awaiting session，以 path-free admission 决定区分 keep/cancel/direct。keep 返回冻结的权威 draft 且零写零 Fact；cancel 耐久提交 `user-cancelled` 后发布既有取消 Fact；direct 耐久取消后先使 Fact 可观察，再经 A5-15c 已成立的原任务应用端口交接原 turn/input。maintenance 内无 awaiting 才明确返回 not-applicable，Server 随后继续非 awaiting 单向桥。
- 旧路、公开行为与故障终态：`hasAwaitingAdvancementConfirmation`、Server 三种旧 prepare 业务分支、Controller awaiting 决定/写入分支及对应结果类型已删除；Controller 只保留 path-free admission mechanism，若竞态使旧 `prepareUserTurn` 在 awaiting 状态被调用则 fail closed，不能把本分支吞回 `A5-ADVANCEMENT-PREPARE-01`。真实 `session.send` 只经 Product API 调用并投影 observer、Fact、DTO 与既有 admission 错误：自然语言取消仍返回原 cancelled exact shape，direct 不新增状态字段，显式 engage 与非 awaiting 链保持迁移前顺序；not-found/busy/identity mismatch、Fact 或 handoff 失败均不虚报成功，direct 的 committed cancellation/Fact 不因后续 Conversation handoff 失败回滚。
- 直接、结构与构建证据：领域 application 1 文件 24/24，覆盖三种 action、权威 draft、maintenance 内唯一状态读取、并发后可见 awaiting 不被旧快照短路、keep 零写零 Fact、两种取消、Fact-before-handoff、not-applicable、identity/not-found/busy 与 handoff failure；Controller 与 Server method 2 文件 34/34，覆盖 path-free 决定、旧 prepare fail-closed、Product API 投影及非 awaiting bridge；真实 `session.send` RPC 定向 1 文件 4/4，覆盖 awaiting keep/cancel/direct/engage 的 response、observer 与事件；CLI 唯一组合根 1 文件 5/5，合计 5 文件 67/67。core、owner-services、server 与 CLI 依赖构建闭包通过；canonical S7 33/33 且 registry golden 通过，纠正后 Advancement S7 定向门禁 1/1 通过且扫描输入/结论未失效，未重复 canonical；changed-file Biome 检查通过，`git diff --check` 通过。package export/build entry未变化，按失效闭包未重复 fresh package exports。
- 精确失效与交接：若 awaiting control Command/Result、conditional Fact、admission mechanism、Conversation maintenance 重读、三种 action、keep 零写、Fact-before-handoff、原 turn/input、observer/response/error 投影、Host contribution/dispatcher exact-set、旧 helper/Controller awaiting 分支退场、non-awaiting fail-closed bridge、S7 或上述直接测试任一变化，只恢复 `A5-15e-advancement-awaiting-rubric-control-v1` 并重验本闭包；新任务 draft、active user-turn/takeover/regeneration 的后续变化不误伤本证据，除非改变共享 admission strategy、Conversation maintenance/应用端口或 Product API exact-set。A5-15e 当前实施完成并等待协调者独立复核；`A5-ADVANCEMENT-PREPARE-01`、Advancement 行与 A5 继续为 `[ ]`。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：独立复核推翻了初版 maintenance 外预读的线性化假设。旧实现先读取一次 Advancement detail 并可能直接返回 not-applicable；若该旧快照之后并发建立 awaiting session，Server 会误入 non-awaiting bridge，而 Controller 已按本包边界拒绝 awaiting，造成迁移前在 maintenance 内可正确处理的输入失败。现已删除 maintenance 外判断；该 Command 对已有 conversation 必先取得 existing-maintenance，再在互斥 operation 内执行首次且唯一状态读取、identity/awaiting/not-applicable 裁决。新增反例让 maintenance 外读取返回 active、maintenance 内读取返回 awaiting，证明结果仍为 keep-awaiting、调用顺序严格为 maintenance→read→decision 且只读一次；领域 application 恢复为 24/24，core fresh build、changed-file Biome 与 Advancement S7 定向门禁 1/1 通过。初版 23/23 没有覆盖该并发窗口，不能单独作为完成证据；本条恢复 A5-15e 对应失效闭包。
- 协调者独立验收：纠正后的同一工作区再次反查 awaiting 三种决定、Conversation maintenance 线性化、取消 Fact 与原任务交接顺序、Controller 机制边界、Server 纯 binding、唯一组合根及 S7 反向门禁，确认只有一个状态决定与持久写入口，且非 awaiting 链仍由明确单向桥承接。独立重跑领域 application 24/24、Controller 21/21、Server send method 13/13、真实 awaiting RPC 9/9、CLI 组合根 5/5 与 `git diff --check`，并核对执行者同基线构建、canonical S7 33/33/golden、changed-file Biome 证据；接受 `A5-15e-advancement-awaiting-rubric-control-v1` 并提交为 `9543363f`。`A5-ADVANCEMENT-PREPARE-01`、Advancement 行与 A5 继续为 `[ ]`；按用户要求在此安全检查点暂停调度。

### A5-15f：迁移新任务准入与 Rubric 草案创建垂直切片

- 派发基线与唯一结果：`HEAD 9543363f + task-doc:coordinator-dispatch`，索引为空。只迁移 `session.send` 在无 active/awaiting Advancement session 时的现有新任务链：无开放会话准入、直接执行分流、Rubric 草案生成失败、Conversation 壳按需建立、awaiting session 耐久创建及 committed draft 的产品投影；形成唯一有限的 Advancement Command/Result/Event 闭包，Server 不再编排这条业务链。
- 生产责任链：现有 `AdvancementApplicationService` 必须在同一 Conversation 串行边界内拥有“无开放 session”前置、近期上下文降级与 admission timing、`run-direct`/`start-advancement` 决定、draft build、创建前 Conversation shell 顺序、Advancement session 创建、提交后 identity/draft 校验及有限结果。Controller、builder、store、Conversation ensure-shell 与 maintenance 只作为 path-free 机制或跨领域应用端口由 Anchor 适配；既有会话用 existing-maintenance，新会话沿预分配 conversation/turn identity 使用当前 new-maintenance 语义，不得把锁与壳创建重新交给 Server 决策。
- 行为与故障保护：保持显式 `engage` 在无 awaiting 时完全绕过 Advancement；保持已有 conversation not-found、maintenance busy 时直接执行 fallback、新 conversation 的稳定预分配 id、仅 `start-advancement` 且 draft 成功后才建立 Conversation 壳、草案失败不创建 Advancement session、recent-context 失败按无上下文降级、原 user input/turn identity、draft/session identity、observer 时机、`advancement:contract_draft` 与 `advancement:contract_failed` 的 seq/payload/response exact shape。只有耐久创建成功的 awaiting draft 可以声明 committed Fact；非耐久 draft-build failure 只能作为有限 Result/瞬时产品投影，不得伪装成已提交事实。
- 旧路退场与桥收窄：删除 Server `prepareAdvancementUserTurn` 对新任务的 maintenance、`beforeCreateSession` 与结果编排，删除 Controller `prepareUserTurn` 中 no-open admission/build/create 分支及相应复合结果；Server 只保留 wire、预分配公开 identity、Product API 调用、observer、错误、Event 与 DTO 投影。`AdvancementController.prepareUserTurn` 只允许暂时承接 active 分支并在 no-open/awaiting 状态 fail closed；`A5-ADVANCEMENT-PREPARE-01` 必须缩窄为 active user-turn/takeover/regeneration 一条桥，不能保留新任务 fallback、第二 admission 或第二 session 创建入口。
- 明确不做：不得迁移 active user-turn、proxy interrupt/settlement、takeover、rubric regeneration、review/evidence/recovery、closure/active summary、显式 Rubric revise/confirm/cancel、普通 Conversation `sendUserTurn` 或其他领域；不得改变 admission strategy、Rubric 内容、RPC/wire、持久 schema、用户文案、设备或拓扑语义。
- 最窄证据与完成条件：领域直接测试覆盖 run-direct/start、existing/new maintenance、busy/not-found、近期上下文降级、draft failure、shell-before-session、壳/持久写失败、提交后 identity/draft、零虚假 Fact；真实 `session.send` RPC 覆盖 engage bypass、existing/new id、busy fallback、草案成功/失败的 exact response/event/observer 与普通 direct send。更新 S7 及反向 mutation，证明新任务只经 Product API、Server helper/Controller no-open 分支归零、new shell 与 session 创建顺序唯一、旧桥只剩 active；只运行失效闭包的直接测试、必要依赖构建/typecheck、changed-file Biome、canonical S7，export/build entry 变化时才运行 fresh package exports，不重复其他 Advancement 或根级回归。
- 安全交接点：预计超过四小时、出现两个以上独立未知、需要改变 Conversation 持久协议/admission strategy/公开结果，无法在单一 maintenance 中保持 shell-before-session 与 busy fallback，或删除新任务分支被迫进入 active/review/recovery 时，停在新任务只有一个可构建可运行 owner、active 桥仍明确的检查点反馈；不得把整个 `prepareUserTurn` 或 Advancement 剩余责任合并进本包，不得执行任何 Git 写操作。
- 实施与唯一应用链：进场为 `HEAD 9543363f + task-doc:coordinator-dispatch`，索引为空。现有 `AdvancementApplicationService` 新增唯一 `advancement.command.prepare-new-task` Command、有限 Result 与 `advancement-contract-draft-created` committed Fact；应用按 existing/new 分别进入 Conversation existing/new maintenance，在互斥 operation 内首次读取 open session，open 时只返回 not-applicable 交由已成立的 awaiting 应用链，no-open 时唯一执行近期上下文 admission、run-direct/start 分流、draft build、new-scope shell 与 awaiting session 持久创建。只有 persist 返回的 session 经 status/conversation/session-id/original-input/full-draft 全等校验后才形成 Result/Fact；draft failure 只形成瞬时 contract-failed，shell/persist 失败均不发 Fact。
- 生产绑定与旧路退场：Anchor 只把 Controller 的 path-free admission/build/persist 机制、Conversation maintenance 与 `ensureShell` 应用端口注入同一 sealed dispatcher；Server 为普通 send 预分配稳定 conversation/turn identity，经 Product API 调用后只投影 observer、Fact/Event、错误与 DTO。owner busy 仍直接落回普通 send；新任务应用观察到 open 后先交 A5-15e 处理 awaiting，若 awaiting 也不适用则只再调用一次现存 active bridge，命中 active 时复用原 active-user-turn/takeover/regeneration 投影，未命中则按普通 send 收口，绝不重跑新任务 admission/session 创建；显式 engage 在无 awaiting 时继续绕过新任务应用。旧 `prepareAdvancementUserTurn` helper、Server 的 no-open maintenance/build/create 编排、Controller 的 no-open run-direct/build/create 分支及对应复合结果已删除；Controller 对 no-open/awaiting 明确拒绝，`A5-ADVANCEMENT-PREPARE-01` 只剩 active 分支。
- 直接证据：领域 application 28/28 覆盖 existing/new、run-direct/start、busy/not-found、open/not-applicable、draft/shell/persist/commit 校验与零虚假 Fact；Controller 21/21 锁定近期上下文降级、admission timing 与旧 no-open 入口 fail closed；Server send method 13/13、真实 `session.send` 定向闭包 6/6 覆盖 new/existing awaiting、engage awaiting、draft failure、existing not-found 与 busy direct fallback；Anchor 组合根 5/5 证明同一 application contribution 的 new maintenance/mechanism/shell 端口。core、owner-services、server、CLI 依赖顺序 build、CLI typecheck、changed-file Biome 与 canonical S7 33/33、registry golden 全部通过；本包未改 package export/build entry，故按派发边界未重复 fresh package exports。首次 canonical S7 的生产覆盖通过但一条新增 mutation 未命中真实缩进；补强为对该 Command 的 Product API 调用精确约束后，定向 mutation 1/1 与最终 canonical 全部通过，未接受 golden 漂移。
- 精确失效与交接：若 prepare-new-task Command/Result/Fact、open/no-open 裁决、existing/new maintenance、admission action、shell-before-session、committed session 全等校验、busy/not-found/open 终态、Server observer/Event/DTO、Host dispatcher exact-set、Controller no-open 退场、active-only bridge、S7 或上述直接测试任一变化，只恢复 A5-15f 并重验本闭包；active user-turn/takeover/regeneration、review/evidence/recovery 的后续变化不误伤本证据，除非改变共享 Conversation maintenance、admission strategy 或 Product API exact-set。A5-15f 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`。本包未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：独立复核推翻了初版“new-task 与 awaiting 均 not-applicable 即 INTERNAL”的新终态。初次 active helper 预读与 new-task maintenance 之间可以合法建立 active session；迁移前后段 maintenance 会由 active owner 处理，而初版新增 `Advancement state changed before new-task dispatch` 错误造成产品回退。现已删除该 INTERNAL：两项 Product API 均不适用时，对同一 conversation/turn/input 只作一次有界 active bridge 重读，命中后复用同一 active 投影函数，未命中直接进入既有普通 send；不循环、不第二次调用 prepare-new-task，也不把 active 状态机搬回 Server。真实 RPC 反例通过 `loadActiveSession` 首次返回 null、new-task maintenance 从真实 store 观察 active、awaiting 不适用、第二次 active bridge 返回 continuation，且 new-task admission 与 `createSession` 均为零调用；Server method 13/13、该 RPC 1/1、changed-file Biome（2 files）、Advancement S7 定向门禁 1/1 与受影响 Server build 通过。领域应用、其他依赖构建、canonical S7/golden 与 package-export 输入均未改变，复用本包既有有效证据。
- 协调者独立验收：纠正后的同一工作区反查 new-task Command、existing/new maintenance、shell-before-session、committed draft Fact、Controller 机制、Server 纯 binding、active-only 桥及 S7 反向门禁，确认新任务只有一个 admission 与持久创建入口，合法 active 竞态只做一次有界回交。独立重跑领域 application 28/28、Controller 21/21、Server send method 13/13、真实 active 竞态 RPC 1/1 与 `git diff --check`，并核对执行者同基线构建/typecheck、canonical S7 33/33/golden、组合根及 changed-file Biome 证据；接受 `A5-15f-advancement-new-task-preparation-v1` 并提交为 `943de5cc`。`A5-ADVANCEMENT-PREPARE-01` 只剩 active 状态，Advancement 行与 A5 继续为 `[ ]`。

### A5-15g：迁移 active user-turn、接管与 Rubric 再生垂直切片

- 派发基线与唯一结果：`HEAD 943de5cc + task-doc:coordinator-dispatch`，索引为空。只把 `session.send` 命中 active Advancement session 后的现有完整互斥状态链迁入唯一 `AdvancementApplicationService`：继续同一目标、用户接管退出、Rubric 修订再生及受控失败；同时闭合代理中断/结算、普通 Conversation turn 交接、退出/草案事实与失败重接顺序，并永久关闭 `A5-ADVANCEMENT-PREPARE-01`。
- 生产责任链：领域应用必须在 Conversation existing-maintenance 与当前 active session 身份约束下拥有 active admission、近期上下文降级与 timing、continue/take-over/revise 决定、用户接管或缺 confirmed Rubric 的退出、Rubric prefill/revise、成功后 superseded 退出与新 awaiting session 创建、closure 形成及提交后权威投影。Controller/store/builder/closure synthesizer 只保留 path-free 机制；代理 busy 判断、中断、outstanding settle、普通 turn admission 与 regeneration failure 后的恢复触发只通过 Anchor 的有限跨领域/正确性端口执行，业务顺序仍由 Advancement 应用拥有。
- 行为与时序保护：保持 active 输入在 admission 前优先中断仅属于 Advancement 的 pending/in-flight proxy，普通 turn 不被中断；continue-active 只在确实中断时结算原 outstanding proxy，再以同一 conversation/turn/input 进入普通 Conversation send，并返回既有 `advancementContinuation.interruptedProxy`。take-over 使用既有 `user-took-over` exit/文案/时间与 closure，先使 committed exit Fact/Event 可观察，再把当前输入交给普通 send；缺 confirmed Rubric 仍以 `system-error` 退出后继续当前输入。revise 仍用 confirmed Rubric 反投影、当前输入文本作反馈、旧 original task 作新 session 原任务；draft 失败不修改旧 active session，先投影 `contract_failed`，再 best-effort 重接被中断代理且不覆盖失败 response；成功时先耐久 superseded/closure，再创建并严格校验新 awaiting session，按原 seq/payload/observer/response 投影 exited 与 contract_draft。不得虚构 interruption、Fact、closure、session 或恢复成功。
- 竞态与旧路退场：初次 active 检查和 A5-15f 的一次有界竞态回交必须调用同一 active Product API Command，不再读取 Controller 或复制投影；Command 在线性化点发现非 active 时只返回 not-applicable，由既有 awaiting/new/direct 链继续，不得误中断代理。删除 Server `prepareActiveAdvancementUserTurn`、`interruptAdvancementProxy`、active 业务投影/恢复编排和 Controller `prepareUserTurn`、私有 `regenerateRubricContract` 复合入口及其旧结果类型；Server 只保留 wire、Product API、observer/Event/DTO/error 投影，不得留下第二 active 状态机、第二 admission 或同责 fallback。
- 明确不做：不得迁移 reviewer、evidence coordinator、after-turn review、proxy 内容生成/入队、recovery maintenance 内部算法、closure/active summary 查询、预算保险丝、显式 Rubric revise/confirm/cancel、Conversation 普通 send 内部、其他领域或 A6；本包只复用其现有有限机制并保护行为，不改变 RPC/wire、持久 schema、Rubric 内容、用户文案、设备或拓扑语义。
- 最窄证据与完成条件：领域直接测试覆盖 active/not-applicable、三类 admission、近期上下文失败、proxy 无/有 outstanding 与 abort/settle、takeover/system-error、revise success/failure、退出与新 session 提交校验、Fact/Event-before-handoff、handoff/settle/recovery 失败终态；真实 `session.send` RPC 覆盖 continue 的两种 interruption、takeover、missing rubric、regeneration、contract failure/recovery、初次与竞态 active 路由及 exact response/event/observer。更新 S7 与反向 mutation，证明两处 active 检查只经同一 Product API、Server/Controller 旧入口归零、桥关闭且 active 事实/效果顺序唯一；只运行失效闭包、必要依赖构建/typecheck、changed-file Biome、canonical S7，export/build entry 变化时才运行 fresh package exports，不重复其他 Advancement 或根级回归。
- 安全交接点：预计超过四小时、出现两个以上独立未知、必须改变公开/持久合同、无法保持 interrupt→admission→commit/settle/handoff 的既有顺序，或被迫进入 review/evidence/recovery 内部迁移时，停在 active 只有一个可构建可运行 owner、旧桥状态明确的检查点反馈；不得扩成 Advancement 余项或 A6，不得执行任何 Git 写操作。
- 实施结果与唯一主链：新增唯一 Product API Command `advancement.command.prepare-active-user-turn` 与条件 Fact `advancement-session-exited`，由 `AdvancementApplicationService.prepareActiveUserTurn` 统一拥有 active identity 复核、proxy interrupt、continue/take-over/revise 决定、退出或新草案耐久提交、代理结算/恢复和普通 Conversation handoff 顺序。初次 active 检查与 A5-15f 的一次有界竞态回交都调用同一 Command；Command 在 existing-maintenance 内重读同一 active identity 后才允许中断代理并提交决定，maintenance 释放后才投影 Fact/Event并进入普通 turn，避免对同一 Conversation owner 递归准入。Controller 只保留 active session 读取、准入决定、退出/再生/closure 等 path-free 机制；Anchor 只注入有限 proxy interrupt/settle/recovery 与 Conversation handoff 端口；Server 只保留 wire、observer、Product API 调用、Fact/Event/DTO/error 投影。
- 行为、故障与竞态终态：非 active 或 identity 已变化只返回 not-applicable，零 proxy interrupt；普通 busy turn 继续沿既有 active 分类和普通 send 收口，不把普通 turn 当代理中断。continue 仅在真实 proxy interruption 且存在 outstanding identity 时结算，并准确返回 `interruptedProxy`；take-over 与缺 confirmed Rubric 分别耐久写入 `user-took-over` / `system-error` exit 和 closure，committed exit Fact/Event 先于 handoff，后续 handoff 失败不回滚事实。Rubric 再生失败先发布原 `contract_failed`，再 best-effort 恢复被中断代理且恢复失败不覆盖主失败；成功先提交 superseded/closure，再创建并全等校验新 awaiting session，按既有顺序投影 exited 与 draft。settle 或持久步骤失败阻断虚假 handoff/成功，响应丢失仍由稳定 identity 与现有机制重驱。
- 旧路退场与桥关闭：删除 Server `prepareActiveAdvancementUserTurn`、`interruptAdvancementProxy` 及 active 状态决定/恢复编排，删除 Controller `prepareUserTurn`、私有 `regenerateRubricContract` 和旧复合结果类型；生产 `session.send` 的两处 active 路由均只经 sealed dispatcher 的同一领域 Command。S7 exact-set、source ownership、调用次序和反向 mutation 可拒绝 Server/Controller 业务入口恢复、绕过线性化 identity 重读、漏 await interrupt、第二 active 检查主链或组合根端口缺失，故 `A5-ADVANCEMENT-PREPARE-01` 在当前工作区归零。
- 直接验证：领域 application 37/37；Server Controller + send method 34/34（21+13）；真实 `session.send` active RPC 定向 7/7，覆盖 regeneration、failure/recovery、takeover、missing rubric、真实 proxy interruption、无 outstanding continuation 与有界竞态；CLI 组合根 5/5。fresh core、owner-services、server build，CLI typecheck 与 `pnpm cli:build` 均通过；changed-file Biome 9 files、canonical S7 coverage/mutation 33/33 与 registry golden、`git diff --check` 通过。反查 Server RPC structure golden 后删除随旧 `AdvancementPrepareResult` 一并退场的 `AdvancementPrepareOwnerResult`；其单项测试在读取 golden 前即命中接受基线已有的 runtime-host dependency exact-set 陈旧断言（仍期待 A4-10 已删除的 `@zhixing/mcp` / `@zhixing/tools-builtin`），故只登记该独立基线失败，不越界改写 A4 门禁或虚报通过。package export/build entry 未变化，按派发边界未重复 package-export 门禁；A5-15a～A5-15f 和其他 Advancement 闭包未重验。
- 精确失效与交接：若 active Command/Result/Fact、active identity 线性化重读、admission 分类、proxy interrupt/settle/recovery、exit/closure/regeneration 持久顺序、Fact/Event-before-handoff、Conversation handoff、初次/有界竞态 Product API 路由、Host contribution/dispatcher exact-set、Controller 机制边界、Server wire/observer/DTO/error 投影、S7 或上述直接测试任一变化，只恢复 `A5-15g-advancement-active-user-turn-v1` 并重验本闭包；review/evidence/recovery、closure/active summary 与运行事件后续迁移不误伤本证据，除非改变上述共享端口或 Product API exact-set。A5-15g 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`，不进入下一责任链或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

- 协调者独立验收：反查 active Command 的线性化身份重读、Advancement-only proxy 中断、admission、退出/再生耐久顺序、Fact/Event-before-handoff、失败恢复、Controller 机制边界、Server 两处同一 Product API 路由、Host 组合根与 S7 反向门禁，未发现第二 active 状态机或旧桥残留。独立重跑领域 application 37/37、Controller 21/21、Server send binding 13/13、真实 active `session.send` RPC 7/7、canonical S7 33/33 与 registry golden，并通过 `git diff --check`；接受 `A5-15g-advancement-active-user-turn-v1`。`A5-ADVANCEMENT-PREPARE-01` 已关闭，Advancement 行与 A5 仍待后续责任链。

### A5-15h：迁移 Advancement 对话退场与孤儿清理生命周期

- 派发基线与唯一结果：`HEAD 6a856ba0 + task-doc:coordinator-dispatch`，索引与工作区除本派发记录外为空。只迁移 Advancement 随 Conversation 删除而取消 open session、在 Conversation 删除提交后移除该对话全部 Advancement 控制数据，以及启动维护清理已无 Conversation owner 的孤儿控制目录这一个数据生命周期责任；使决定、顺序和结果由唯一 Advancement 应用边界拥有，Controller/store 只保留 path-free 读取、取消、删除与有限枚举机制。
- 生产与消费链：从 Anchor `ConversationApplication.delete` 的 `cancelDependentLifecycle → Conversation authority/storage delete → removeDependentData` 顺序和 local access-surface 的同一删除投影双向追踪，二者必须调用同一 Advancement 应用用例，不能直调 Controller。系统维护 `advancement.runSweep` 也只调用同一应用边界的孤儿清理用例；alive probe 仍是 Host 注入的物理端口，领域应用只决定“Conversation 不存在的 Advancement 控制数据必须退场”。无 open session 时取消幂等空成功；存在 awaiting/active 时保持既有 `user-cancelled` reason/message、终态提交和错误传播；Conversation 删除提交前取消失败仍阻断主删除，主删除提交后控制数据删除失败仍按现有投影合同传播，不伪造清理成功。
- 旧路与范围：删除 `AdvancementController.cancelOpenConversationSession`、`removeConversationData`、`sweepOrphanData` 这些应用级复合入口及 Anchor/local/system-handler 对 Controller 的直调；不得留下 fallback、双写、第二 sweep 或 Server/Surface 业务决定。可以保留 recovery 专用的精确 session cancellation、proxy settlement、store 机制和现有持久格式，本包不迁移 `cancelOpenSession`、after-turn review/evidence、recovery maintenance、proxy rebuild/dispatch、detail/closure 查询、Rubric 流程、Device/Backup/A6，也不改变 Conversation 删除协议、RPC/wire、用户文案或数据格式。
- 直接证据与完成条件：领域直接测试覆盖 no-open、awaiting、active、取消失败、删除失败、幂等删除、alive/dead/混合孤儿、probe/枚举失败及扫描计数/告警；Anchor 与 local 删除真实消费链证明相同应用语义和原 `cancel → 主删除 → remove` 顺序；system maintenance 证明只经应用用例；S7 与反向 mutation 拒绝 Controller 旧入口或三处直调恢复、顺序漂移、第二清理 owner 和组合根缺失。只运行受影响测试、必要依赖构建/typecheck、changed-file Biome、canonical S7；Product API exact-set 或 package export/build entry 未变化时不重复无关门禁，不跑根级回归或制品验收。
- 安全交接点：预计超过四小时、需要改变 Conversation 删除提交语义或 Advancement 持久格式、出现两个以上独立未知、影响进入 recovery/review 主链，或无法保持可构建可运行且单一 owner 时立即停在明确检查点反馈；不得扩成 Advancement 全量清理或 A6，不得执行任何 Git 写操作。

- 实施基线与唯一应用责任：进场为 `HEAD 6a856ba0eb1b609ac98f3ac8c795ca7a730348b6 + task-doc:coordinator-dispatch`，索引为空。唯一 `@zhixing/core/advancement/application` 新增冻结的 `AdvancementConversationLifecycleApplication`；同一 `AdvancementApplicationService` 唯一拥有 open session 退场、对话控制数据移除和孤儿候选清理决定。它只消费 path-free cancellation/remove/enumeration mechanism 与 Host 注入的有限 Conversation alive probe，不取得 Store、路径、Conversation Authority、RPC 或 Surface 类型；Controller 只保留精确 session 读取/取消、数据删除和候选枚举机制，filesystem store 也只枚举 opaque 单段 candidate 并删除指定 candidate，不再判断 orphan。
- 顺序、幂等与 A5-12c 精确纠正：durable Anchor delete 仍先验证 authenticated surface caller，再调用同一 Advancement 应用取消 open session，随后写入/投影 Conversation delete；local projector 与恢复重驱同样只经该应用，no-open 重放为空成功，存在 awaiting/active session 时继续提交固定 `user-cancelled` 与原中文 message。Conversation 主删除只在取消成功后发生，删除 Fact/本体成功后才移除 Advancement 数据；取消失败阻断主删除，durable post-delete removal 失败保持投影待重驱，legacy post-delete removal 仍按既有 best-effort 诊断。故 A5-12c 初次记录中“删除本体先于 Advancement 取消”的单句结论在当前基线上失效并由本包纠正；其 Command/Fact、Authority replay、wire/notification 与其他责任结论不受影响，公开协议和用户行为未改变。
- 三类消费、清理终态与旧路退场：Anchor commit、local access-surface 和 `__advancement-gc` system maintenance 共享同一个 late-bound、fail-closed、有限 lifecycle application 端口；maintenance 只注入物理存活判断，应用对 alive 候选跳过、dead 候选删除，enumeration 失败收敛为空报告，单候选 probe/delete 失败聚合 warning 并继续其余候选。`AdvancementController.cancelOpenConversationSession/removeConversationData/sweepOrphanData`、Store `sweepOrphanDirs`、Host/Surface/system-handler 的 Controller 业务直调和 cancel 失败 best-effort 分支全部归零；recovery 专用 `cancelOpenSession`、review/evidence/proxy 机制、持久格式及其他领域未迁移。
- 直接证据与验证：core Advancement application/Conversation delete/store 3 文件 93/93；Server Controller/session store/system maintenance 3 文件 34/34；真实 `session.delete` RPC 定向 4/4；CLI Conversation directory/delete 1 文件 14/14，合计 8 文件 145/145，覆盖 no-open、awaiting/active、取消阻断、post-delete removal、alive/dead/枚举/probe/delete 失败、Anchor/local 顺序、RPC 错误与重驱。fresh core、owner-services、server 与 `pnpm cli:build` 均通过，CLI `tsc --noEmit` 通过；changed-file Biome、canonical S7 coverage/mutation 33/33 与 registry golden 通过。package export/build entry 未变化，按派发边界未重复 package-export 门禁，也未运行根级回归或制品验收。
- 结构失效与交接：S7 与反向 mutation 冻结唯一 lifecycle application/Host proxy、Controller/Store 机制边界、`cancel → Conversation delete → remove` 顺序、Anchor/local/system 三类生产消费，并拒绝 Controller 旧复合入口、Store orphan 决策、未 await cancel、第二 sweep 或 Surface 直调回流。以后 lifecycle application/mechanism、固定 cancellation 终态、Conversation delete projector/Authority replay、alive probe、candidate exact-set、三类 Host 消费、S7 或上述直接测试任一变化，只恢复 `A5-15h-advancement-conversation-retirement-v1` 并重验本闭包；review/evidence/recovery、proxy、detail/closure 与 Rubric 流程仍按各自证据独立失效。A5-15h 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`，不进入下一责任链或 A6。本轮未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-15h 协调复核纠正：恢复已接受删除顺序并消除 lifecycle lazy ref

- 反证与撤回：协调者独立复核确认，上方初次实施记录把 writer 取得 Advancement lifecycle 应用的时序误写成了 Conversation 删除事务本身的顺序，并由此未经授权改成 `cancel → Conversation delete → remove`；同时用可变 late-bound `advancementLifecycleApplication`/require proxy 在 Host consumer 已创建后补接完整 Advancement 应用，留下半装配和第二 owner 风险。撤回“本包精确纠正 A5-12c 顺序”“取消失败阻断主删除”“同一完整 AdvancementApplicationService 同时拥有管理与 lifecycle 用例”以及对应验证声明；这些不再代表当前实现。
- 当前唯一责任与固定装配：新增独立、有限的 `AdvancementConversationLifecycleApplicationService`，只拥有 `cancelConversationLifecycle / removeConversationData / sweepOrphanData` 三项跨领域 lifecycle 用例，消费 Controller 的 path-free 机制和 Host 注入的 Conversation alive probe；完整 `AdvancementApplicationService` 不再实现或注入这些 lifecycle 端口。Anchor 在任何 delete/access-surface/system-maintenance consumer 发布前一次构造该固定实例，三类 consumer 共用它；生产中不存在 `let advancementLifecycleApplication`、`requireAdvancementLifecycleApplication`、后置赋值、proxy、第二 lifecycle application 或 Controller fallback。
- 删除与失败顺序恢复：恢复 A5-12c 已接受的 `Conversation runtime/storage delete → cancelDependentLifecycle → removeDependentData`。durable Authority 写入/投影先提交 Conversation 删除，再由 committed projection 严格传播 cancel/remove 失败并保留依赖重驱；local access-surface 走同一严格投影。legacy Anchor 在主删除和 Fact 后依次 best-effort 执行 cancel、remove，任一步失败均记录诊断且不阻断另一项；因此 cancel 失败不回滚或伪装未发生的 Conversation 删除。no-open cancel 仍幂等空成功，open session 仍以固定 `user-cancelled` reason/message 提交；孤儿枚举、alive probe 和逐 candidate 删除决定不变。
- 结构失效与证据：S7 冻结 lifecycle 独立应用恰一实例、完整 Advancement 应用不重叠、固定构造先于三类 consumer、Conversation delete/cancel/remove 顺序和 durable/local 严格与 legacy best-effort 语义；反向 mutation 可识别 cancel 前移、完整应用重新吸收 lifecycle、late-bound ref/proxy、Controller/Store 复合入口、Surface 直调或第二 sweep 回流。纠正后直接证据为 core application/store/Conversation delete 3 文件 93/93、Server Controller/session store/system maintenance 3 文件 34/34、真实 `session.delete` RPC 定向 4/4、CLI Conversation directory/delete 14/14，合计 8 文件 145/145；fresh core、owner-services、server build、CLI `tsc --noEmit` 与 `pnpm cli:build` 均通过，changed-file Biome 与 canonical S7 coverage/mutation 33/33、registry golden、`git diff --check` 通过。A5-12c 继续有效，只有本 A5-15h lifecycle application/mechanism、删除投影、候选 exact-set、三类消费或上述门禁变化时才恢复本证据。Advancement 行与 A5 继续为 `[ ]`，不进入下一责任链或 A6。

- 协调者独立验收：按接受基线重新追踪 Conversation delete 的生产顺序、durable/local 严格依赖重驱、legacy best-effort、Advancement lifecycle 三项用例、Controller/Store 机制边界、Anchor/local/system 三类消费者和固定装配，确认纠正版没有功能语义漂移、可变 lazy ref、第二清理 owner 或旧复合入口。独立重跑 core application/store/Conversation delete 93/93、Server Controller/session store/system maintenance 34/34、真实 `session.delete` 4/4、CLI directory/delete 14/14、canonical S7 33/33 与 registry golden，并通过 `git diff --check`；接受 `A5-15h-advancement-conversation-retirement-v1`。Advancement 行与 A5 仍待 after-turn review/evidence/recovery 等后续责任链。

### A5-15i：归位 Advancement accepted-turn 补审、验收与结果投影应用链

- 派发基线与唯一架构结果：以 `HEAD 60b7388b` 加本节调度记录为基线。把非 ephemeral committed turn 触发后的“同会话串行 → 补齐当轮之前欠账 → 验收当轮 → 发布 review 事实投影/安排 proxy turn”收归唯一、有限、路径无关的 Advancement 应用用例；Anchor 与 Executor-local 两类 Host 只绑定该应用。应用拥有触发准入、catch-up 连续性结果判断、调用次序和 review result 的产品投影；Conversation committed hook、恢复设施、Controller、Server/CLI adapter 只提供明确端口或机制，不再各自编排这条业务链。
- 生产链与必须保持：覆盖 `createAdvancementReviewMaintenance` 的两处真实组合、`TurnCommittedInfo` 的 ephemeral/identity/run record 输入、每 conversation promise chain、`beforeRunIndex` 补审、catch-up 抛错/非连续终态停止当轮、`afterTurnCommitted` 调用、review deferred/reviewed/proxy/completed/exited 事件顺序与 payload、proxy schedule 及 conversation existence；同时覆盖 recovery 对同一 review result 投影语义的复用。保持迁移前 fire-and-forget、不同会话可独立推进、同会话严格有序、上一轮失败不毒化后续、catch-up 失败不误审当轮、响应丢失与重放、事件 seq、代理消息身份和两种拓扑行为不变。
- 旧路与有限过渡：删除 CLI `advancement-review-maintenance` 对业务顺序、连续性状态集合和结果投影的拥有；结果事件与 proxy 决定必须由同一 Advancement 应用合同产生，不能在 CLI、Server、recovery helper 或 Controller 留第二份。允许应用经登记桥 `A5-ADVANCEMENT-REVIEW-01` 调用现有 Controller 的有限 review mechanism，因为 review attempt/evidence/持久提交状态机由后续紧邻工作包迁移；桥只能承载 path-free 输入/结果，不得含 Surface、ConversationManager、RPC、路径、可变 lazy ref、第二 flight map 或 fallback。若一次性删除旧 `review-dispatch` 会迫使本包迁入 recovery 扫描/attempt/evidence 内部状态机，则只把其调用改绑同一应用拥有的结果投影用例，不扩大本包。
- 明确不做：不迁移 review attempt generation/root lease、evidence 收集与验证、reviewer 调用、持久 review/proxy 提交、recovery 扫描/重驱算法、proxy scheduler mechanism、Rubric/closure/active-session、Conversation、Device、Backup、A6 或公开能力；不新增事件、状态、错误、重试、配置、诊断框架或 root/full 验证。
- 直接证据与完成条件：先双向核对两类 Host committed hook、recovery 对 review result 的全部消费者及现有直接测试；以 Advancement 应用测试证明准入、串行、catch-up 连续/失败、review/dispatch 次序和所有 result variant，以 Anchor/local 真实组合测试证明只接同一应用，以 recovery 直接测试证明共享投影没有语义漂移，以 S7/反向 mutation 证明 CLI 旧编排、重复 dispatch、Controller 第二触发和 Host 漏接不能回流。受影响包按依赖顺序构建，运行最窄直接测试、changed-file lint、canonical S7 与 `git diff --check`；不重复根级回归、制品或未失效门禁。完成时生产只有一个 accepted-turn 应用调度/投影 owner、两类 Host 行为等价、桥 exact-set 与紧邻退场点可核验、工作区可构建可运行，并在本文登记证据后停在安全交接点；执行者不得执行 Git 操作。

- 实施责任与固定装配：唯一 `@zhixing/core/advancement/application` 新增 `AdvancementAcceptedTurnApplicationService` 与同模块唯一 `AdvancementReviewResultProjectionApplicationService`。前者拥有 non-ephemeral 准入、每 conversation 串行链、`beforeRunIndex` catch-up 连续性判断、当轮 review 与结果投影次序；后者唯一拥有 deferred/reviewed/proxy/completed/exited 事件的 seq/payload、proxy 调度决定及 recovery 的 `emitProxyEnqueued/scheduleProxy` 抑制语义。Anchor 与 Executor-local 均按 `result projector → recovery → accepted-turn application → ConversationManager` 固定构造，committed hook 只调用应用端口；旧 `advancementRecoveryRef`、`reviewCommitted` 后置赋值和应用 lazy proxy 归零，应用发布前已完成全部机制绑定。
- 过渡桥、恢复复用与旧路退场：`A5-ADVANCEMENT-REVIEW-01` 现唯一落在 `@zhixing/owner-services/advancement/review-application-bridge` 窄 subpath，只把 path-free accepted-turn input/result 委托给现有 `AdvancementController.afterTurnCommitted`，不含 Surface、ConversationManager、RPC、路径、可变 ref、flight map 或 fallback；其 proxy 端只封装既有 `ProxyMessageScheduler` 效果。Controller 的 attempt/evidence/reviewer/持久 review 状态机与内部同 run flight 继续留给紧邻后续包。recovery 扫描/重驱仍保留原算法，但已强制注入并调用同一个 application-owned result projector；CLI `advancement-review-maintenance`、owner-services `review-dispatch`、旧 package/build export、两处 Host 业务编排和 recovery 重复结果决定全部删除，公开事件、proxy identity、conversation-existence 门禁、响应丢失重放与持久格式不变。
- 直接证据与验证：core Advancement application 56/56，覆盖 ephemeral 跳过、catch-up-before-current、连续/失败、同会话串行、跨会话独立、失败不毒化后继、六类 result variant、事件顺序/payload/proxy identity 和 recovery 抑制；Server recovery 与真实 session RPC 2 文件 121/121，证明冷恢复复用同一投影且既有公开行为不漂移。Anchor/Executor-local 组合闭包首轮 3 文件 22/23，唯一失败为测试资源治理的 `backpressured:ioOperations`；只重跑该 lifecycle 单例后 1/1 通过，其余 22 项首轮已通过。fresh core、owner-services、server 与 `pnpm cli:build` 均通过，CLI `tsc --noEmit`、changed-file Biome、fresh package exports、canonical S7 coverage/mutation 33/33 与 registry golden 通过；canonical S7 初次因新 owner-services manifest 未加入检查输入而 fail closed，补齐真实 manifest/build 输入后完整重跑通过。未运行根级回归或制品验收。
- 结构失效与交接：S7 与反向 mutation 冻结 accepted-turn/result-projector 各一 owner、catch-up 连续集合、review/result 次序、两类 Host 固定消费、recovery 共用投影、桥窄导出和旧文件/根转导归零；可识别跳过 catch-up、Host 恢复 Controller 直调、recovery 重建 dispatch、旧 maintenance/dispatch 文件或第二导出回流。以后 committed-turn input、catch-up status、review result union/event/proxy 投影、recovery 接口、两类 Host hook、bridge exact-set/export、S7 或上述直接测试任一变化，只恢复 `A5-15i-advancement-accepted-turn-review-v1` 并重验本闭包；A5-15a～A5-15h 不随之失效。A5-15i 当前实施完成并等待协调者独立复核；Advancement 行与 A5 继续为 `[ ]`，下一包须收束 review attempt/evidence/recovery 内部状态与写权并退场 `A5-ADVANCEMENT-REVIEW-01`，本轮不进入该责任链或 A6，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

#### A5-15i 协调复核纠正：固定装配不得改名为 manager getter

- 协调反证：实现删除了旧 `advancementRecoveryRef.current`，却为新 accepted-turn 链扩大 Server adapter 为 `ConversationManager | (() => ConversationManager)`，并在 Anchor 与 Executor-local 生产组合中新增 `manager: () => manager`、`createAdvancementOriginalTaskAdmissionPort(() => manager, ...)`。这些闭包在 consumer/recovery/application 构造时捕获尚未赋值的可变 `let manager`，只是把 late-bound ref 从显式 `.current` 改成 getter；与本包“应用发布前依赖固定”“桥不得含可变 lazy ref”及 AE-001 静态可追踪装配要求直接冲突。现有测试和 S7 未识别该反例，因此当前固定装配与结构门禁结论撤回。
- 纠正边界：保留已迁移的 accepted-turn、result projector、recovery 共享投影和窄 Controller bridge，只修复新引入的装配循环。恢复 Server adapter 对已构造 `ConversationManager` 的直接依赖，生产中不得保留本包新增的 manager getter、可变 holder、require/proxy、use-before-bind fallback 或按事件重建应用。可采用显式且只能绑定一次、在任何入口开放前 fail-fast 完成并拒绝重绑的 committed-turn listener，或其他真正无环且依赖已构造的固定装配；不得借纠正改变业务顺序、公开合同或扩大到 review/evidence 内部迁移。S7/反向 mutation 与直接组合测试必须新增可识别证据，证明两类 Host 无 late-bound manager/recovery/application getter、未绑定不可运行且 pre-open 绑定后不能重绑。纠正后重跑 A5-15i 全部失效闭包并更新本记录，再停回协调复核；不得执行 Git 操作。

- 纠正实施：`ConversationManager` 新增唯一 bind-once committed-turn listener seam，构造期 listener 与显式绑定互斥，未绑定断言 fail closed、重绑拒绝；既有 fire-and-forget 与“维护失败不污染已提交 turn”语义未变。Anchor 与 Executor-local 均先构造 protocol 与 manager，以实体 manager 一次性完成 protocol 绑定并验证，再构造 proxy/original-task adapters、result projector、recovery 与 accepted-turn application；随后一次性绑定 auxiliary recovery 与 committed-turn listener，验证 listener 已绑定后才把 manager/owner 发布给生产 ingress。Server adapter 恢复只接收实体 `ConversationManager`，review proxy bridge 也恢复为已构造端口的直接依赖；两类生产组合中的 `manager: () => manager`、widened union/`resolveManager`、proxy getter、recovery getter和 use-before-bind fallback 归零。Anchor 仍按 `turnMaintenance → accepted-turn application` 顺序 fire-and-forget，Executor-local 仍只调用 accepted-turn application；recovery、review、事件与持久协议均未改变。
- 纠正直接证据：Server `conversation-manager` 122/122，新增反例直接证明未绑定断言、一次绑定、重绑拒绝及真实 committed turn 调用；Anchor `conversation-surface` 3/3，证明 bind→verify 后才返回已装配 manager；Executor-local 的同一组合测试定向重取 1/1，证明 owner 发布前完成 bind→verify。首次组合运行中 `local-conversation-owner-lifecycle` 的其余 16 项有 15 项通过，唯一既有末项在 Artifact lifecycle maintenance 收到 `backpressured:ioOperations`，与本轮 listener 装配无调用链关系，未用该失败冒充本轮绿灯；本轮只对新增组合反例作精确重取。Core accepted-turn application 56/56、Server recovery 27/27继续通过，既有 session RPC 94项输入未变，初次 A5-15i 的 121/121 recovery/RPC 证据继续有效。
- 纠正结构与构建证据：S7 现要求 Server adapters 只能接收 direct manager、两类 Host 都在生产发布前执行 bind/verify、auxiliary recovery 以一次性 seam 绑定、review bridge 不接受 getter；反向 mutation 可恢复 widened union、移除 listener bind或把 proxy port改回 getter，均被同一 inspector拒绝。当前 `pnpm s7:lint` 为 33/33且registry golden通过；owner-kernel、owner-services、server与CLI依赖顺序构建通过，CLI `tsc --noEmit` 通过，9个适用变更文件的最窄Biome检查无修复。fresh package-export输入未变化，复用初次 A5-15i 已通过证据。`A5-15i-advancement-accepted-turn-review-v1` 的 fixed-assembly 子证据据此恢复，当前等待协调者独立复核；Advancement 行与 A5 继续为`[ ]`，未进入后续 review/evidence 状态机责任链，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

- 协调者独立验收：重新追踪 accepted-turn 应用、共享 result projector、recovery 消费、窄 review bridge、两类 Host 构造与生产发布顺序，确认纠正版只以 bind-once/pre-open seam 闭合 protocol/manager 周期，生产无新 manager/recovery/application getter、可变 holder、fallback 或第二调度/投影 owner。独立重跑 core application 56/56、Server Manager+recovery 149/149、Anchor/Executor-local 组合 20/20、canonical S7 33/33 与 registry golden，并通过 `git diff --check`；接受 `A5-15i-advancement-accepted-turn-review-v1`，提交为 `d0e27b53`。

### A5-15j：归位 Advancement review attempt 代际与根资源生命周期

- 派发基线与唯一架构结果：以 `HEAD d0e27b53` 加本节调度记录为基线。把 accepted run 的 review attempt lineage、generation、phase 转移、耐久 winner 仲裁以及 immediate-root reservation/lease/replay/cleanup 生命周期收归唯一、路径无关的 Advancement 应用状态机；Controller 只通过有限端口请求“准备可调用 attempt、提交 invoking/terminal、调和并清理终态”，不得继续拥有 attempt 代际或根资源业务决定。Store/Authority 只提交和恢复事件，Resource Governor 只实现通用 reservation/inspection/release 机制，Evidence 只提供本包需要的冻结 root target，不获得 attempt owner。
- 必须保持的生产语义：完整覆盖无耐久 `runRecordRef` 的 system exit、同 accepted run 已 review 幂等、重启发现 invoking 即 deferred 且禁止 provider 重放、legacy evidence generation 与当前 attempt generation 单调合并、lineage/root/request identity、root target 漂移导致 expired、reservation replay terminal、业务 owner 在 acquire 前后推进、started→invoking→deferred/expired/consumed 合法转换、同代 terminal winner、terminal root cleanup/retry、session 消失/非 active、cancel/exit 后清理及 load/recovery 的 terminal reconciliation。任何 acquire/transition/cleanup 失败都保持当前 fail-closed、响应丢失与重驱语义，不复活已终结代、不重复 reviewer/evidence 外调、不泄漏或错结算资源。
- 旧路与过渡桥：从 `AdvancementController` 删除 attempt identity/generation/root contract、phase 合法性、terminal arbitration、root acquire/replay/cleanup/reconcile 的私有状态机与直接 resource/store 编排；对应规则、类型使用与测试只归唯一 Advancement 应用。`A5-ADVANCEMENT-REVIEW-01` 本包后只能暂载尚未迁移的 eligibility、evidence 收集、reviewer 调用与 review/proxy outcome 持久提交，并通过本次 attempt 应用端口消费可调用上下文；不得复制 attempt 决策、创建第二 flight map 或保留 Controller fallback。下一紧邻 A5-15k 必须迁移剩余链并删除该桥。
- 明确不做：不迁移 evidence request/settlement/验签/设备目标内部算法，不迁移 reviewer Provider 调用、Rubric 判定、预算保险丝、review/proxy/closure outcome 持久提交、result projector、recovery 扫描、proxy scheduler、其他 Advancement 管理用例、其他领域或 A6；不改变公开事件/RPC、持久 event schema、用户文案、预算、重试或产品能力。
- 直接证据与完成条件：以独立应用状态机测试覆盖每条 phase、同代竞态、generation、target drift、acquire 结果不明、owner 前后漂移、terminal winner、cleanup retry/reconcile 和重启 invoking；复用 Controller/recovery/evidence 直接测试证明 reviewer/evidence 调用次数、结果、错误和持久记录不变；更新 S7/反向 mutation，拒绝 Controller attempt helper/direct resource/store 回流、第二 lineage/generation/root owner、桥重新吸收 attempt 决策或非法 phase。按依赖顺序构建，只运行失效闭包、changed-file lint、canonical S7 与 `git diff --check`，不得运行根级回归、制品或未失效 A5-15i Host 组合闭包。完成时 attempt/root lifecycle 只有一个应用 owner，Controller bridge exact-set 已缩窄且无 fallback，工作区可构建可运行，本文登记证据后停在安全交接点；执行者不得执行 Git 操作。

- 实施责任与生产组合：`@zhixing/core/advancement/application` 新增唯一 `AdvancementReviewAttemptApplicationService`，以有限 state/root/mechanism ports 独占同 run flight、稳定 lineage/request/root identity、legacy/current generation 合并、started/invoking/consumed/deferred/expired 转换、同代 terminal winner、target drift、owner acquire 前后复核、reservation replay、terminal cleanup/reconcile 与 session cancellation。`@zhixing/owner-services/advancement/review-attempt-correctness` 是唯一窄 Correctness binding，只把 `AdvancementSessionStore` 的提交/恢复原语与 `ResourceReservationPort` 的 inspect/acquire/settle/release 机制适配给应用；该 binding 不从 owner-services 根导出。Anchor 唯一 `createServeAdvancementController` 在生产发布前构造并注入同一应用，Controller 不再持有 Resource Governor 或 attempt identity/generation/root helper。
- 过渡桥与行为保护：Controller 现只保留 `prepareEligibility / commitMissingDurableRun / resolveRootTarget / prepareEvidence / invokeReviewer / commitConsumed` 六项 A5-15k 待迁机制；Evidence 完整 target 仍由 bridge 在同次调用内保留，领域应用只取得冻结的 executor/owner-epoch root target。重启 invoking 先耐久转 deferred 且绝不重放旧代 provider，legacy evidence generation 与当前 attempt 单调取下一代；target drift、acquire 响应丢失/terminal、owner 前后推进、cancel/exit、terminal winner、cleanup 失败与 load/recovery reconcile 均保持原 fail-closed 与有界重驱。无 `runRecordRef` system exit、already-reviewed、review/proxy/closure 持久提交、Evidence/reviewer 调用、公开事件/RPC、预算和用户文案未改变；旧 Controller flight map、attempt transition/root acquire/cleanup/reconcile 和 direct Store/Resource 编排归零。
- 直接证据与结构门禁：新增独立领域状态机 7/7，覆盖三段 phase、generation/legacy merge、invoking 禁止重放、target drift、acquire response loss、owner 漂移、terminal winner、cleanup retry/reconcile 与 started/invoking cancellation；真实 governor/持久 store/reviewer 闭包 7/7 覆盖崩溃、响应丢失、队列、竞态和资源终态；Server Controller+recovery 2 文件 48/48、owner-services Evidence 8/8 证明既有 bridge、恢复与取证行为不漂移，合计 5 文件 70/70。S7/反向 mutation 现冻结唯一应用 class/flight、phase/generation/root 决定、窄 Correctness export/build、生产注入和 Controller 直管归零，并能拒绝应用缺失、Controller 恢复 Resource 访问、Correctness 吸收 phase 决定、根转导或生产漏装配。
- 构建、失效与交接：core typecheck/fresh build、owner-services typecheck/fresh build、CLI typecheck 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 33/33 与 registry golden、fresh `pnpm runtime:package-exports`、10 个适用变更文件的最窄 Biome 检查和 `git diff --check` 通过，未运行根级回归、制品或 A5-15i Host 组合闭包。以后若 review attempt input/state/root/mechanism port、identity/generation/phase/terminal winner、root target/acquire/replay/cleanup、Store transition/恢复、Resource adapter、Controller 六项 remainder、生产组合、S7/package export 或上述直接测试任一变化，只恢复 `A5-15j-advancement-review-attempt-lifecycle-v1` 并重验本闭包；A5-15a～A5-15i 不随之失效。A5-15j 当前完成并等待协调者独立复核；`A5-ADVANCEMENT-REVIEW-01` 仍待 A5-15k 迁移剩余 evidence/reviewer/outcome 链后完全退场，Advancement 行与 A5 继续为 `[ ]`，本轮未进入 A6，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

- 协调者独立验收：重新沿生产装配、accepted-turn 调用、attempt 持久转换、Resource Governor 适配与 recovery/load 调和追踪 A5-15j，确认生产只有一个 review-attempt 应用实例，Controller 已归零 lineage/generation/phase、flight、root contract、acquire/replay/cleanup/reconcile 决策；取消会话时的 attempt 终态化与根回收仍由同一应用状态机保证顺序，不构成第二取消 owner。独立重跑 core attempt 状态机 7/7、真实 CLI governor/store/reviewer 7/7、Server Controller+recovery 48/48、owner-services Evidence 8/8，合计 70/70；canonical S7 33/33 与 registry golden、`git diff --check` 通过。接受 `A5-15j-advancement-review-attempt-lifecycle-v1`，提交为 `bab398e2`。

### A5-15k：归位 Advancement review eligibility、outcome 与 proxy 持久状态链

- 派发基线与唯一架构结果：以 `HEAD bab398e2` 加本节调度记录为基线。把 accepted proxy run 合法性与结算、Rubric/reviewer/审前预算 eligibility、缺少耐久 run 的 system exit，以及 passed/failed/exit、审后预算、dead-end、proxy message 选择与 review/proxy/closure 持久提交收归唯一 Advancement 应用状态链。应用拥有这些产品决定及其写入顺序；Store/Authority 只提交领域已决定的事件，Controller 不再解释 review 结果或选择推进终态。
- 必须保持的生产语义：代理 run 的 session/proxy identity 校验、known/outstanding proxy 幂等结算；缺 Rubric、缺 reviewer、缺 `runRecordRef` 与审前预算触发的现有 system-exit 原因和文案；passed→completed、explicit exit、failed 后审后预算退出、failure handling 选择、dead-end 退出、proxy id/content、review/window 同步、evidence request 与 consumed attempt 原子提交；closure report、返回 kind、持久事件顺序、响应丢失/重放、公开 RPC/event 和用户可见结果均不得漂移。禁止先写 review 再由第二 owner 补终态或 proxy，禁止 Controller 与应用同时判断 eligibility/outcome。
- 旧路与桥边界：从 `AdvancementController` 删除 `settleAcceptedProxyRun`、`systemExitReview`、`persistReviewOutcome`、`persistProxyOutcome` 及等价 eligibility/outcome 分支；相关预算、Rubric、failure-handling、proxy 构造和终态规则只能存在于 Advancement 应用。`A5-ADVANCEMENT-REVIEW-01` 完成本包后只允许保留 `resolveRootTarget / prepareEvidence / invokeReviewer` 三项外部机制，不得持有产品状态决定、Store 写入口、结果投影或 fallback；紧邻 A5-15l 迁移这三项适配并删除整座桥。
- 明确不做：不迁移 Evidence target/collect 内部算法、Reviewer Provider 调用及其资源计量，不改变 attempt lineage/generation/phase/root lifecycle，不迁移 recovery scan、proxy scheduler/delivery、closure report 的数据采集实现、其他 Advancement 管理用例、其他领域或 A6；不新增能力、协议、持久 schema、预算、重试、用户文案或兼容入口。
- 直接证据与完成条件：新增/扩展应用直接测试，逐项覆盖 accepted proxy 正常/已结算/未知/错配、缺 Rubric/reviewer/durable run、审前与审后预算边界、passed/exit/failed、dead-end、failure handling、proxy 原子写入、closure 结果、evidence/window/attempt 透传及持久失败；复用真实 Controller/Store/recovery 直接测试证明调用次数、事件序列、返回结果和恢复不变。更新 S7 与反向 mutation，拒绝 Controller outcome helper、eligibility 分支、review/proxy Store 写入回流，冻结桥三项 exact-set 和唯一应用 owner。按验证手册只运行 core/owner-services/Server/CLI 的失效闭包、changed-file lint、canonical S7、必要上游构建与 `git diff --check`，不得运行根级回归、制品、A5-15j 资源闭包或未失效领域测试。完成时领域状态决定与持久写入只有一个应用 owner，桥只剩三项外部机制，工作区可构建可运行并登记证据后停在安全交接点；执行者不得执行 Git 操作。

- 实施责任与生产链：唯一 `AdvancementReviewAttemptApplicationService` 现拥有 accepted proxy 的 session/proxy identity 校验、known/already-settled 幂等与 outstanding settlement，Rubric/reviewer/durable-run/审前预算 eligibility，以及 passed/explicit-exit/审后预算/dead-end/proxy 的完整 outcome 决定。应用以有限 `AdvancementReviewPersistenceDecision` 一次性交给 Correctness port 提交 review、window/evidence settlement、consumed attempt 与 terminal/proxy/closure；`AdvancementSessionStore` 只执行已决定的原子事件事务。缺失 proxy 的恢复重建也调用同一领域 failure-handling/proxy 规则，不在 Controller 形成第二选择器。
- 旧路与桥收口：`AdvancementController` 的 `settleAcceptedProxyRun`、`systemExitReview`、`persistReviewOutcome`、`persistProxyOutcome`、eligibility/budget/failure-handling/proxy 规则与 review/proxy Store 直写全部归零；owner-services 原 `proxy-content` 源码、导出和 build entry 同步退役。`A5-ADVANCEMENT-REVIEW-01` 只剩 `resolveRootTarget / prepareEvidence / invokeReviewer` 三项外部机制，Controller 只保留 Evidence target/collect 与 reviewer Provider 调用，不拥有产品状态决定、持久写入口或 fallback。审查结果、closure report、proxy id/content、结构版本、公开 RPC/Event 和用户文案保持既有值与顺序。
- 直接证据：Core 应用 13/13 直接覆盖 accepted proxy 正常/已结算/未知/错配、缺 Rubric/reviewer/durable run、审前/审后预算、passed/exit/dead-end/proxy、review/window/evidence/attempt 同事务透传、原子持久失败与 missing-proxy 同规则重建；CLI 真实 governor/store/metered reviewer 7/7；Server Controller+recovery 2 文件 48/48，合计 4 文件 68/68。直接反例证明已知且已结算 proxy 不重复结算，未知/错配 system-exit，提交失败不产生假 terminal/root release，review/window/evidence/attempt 与 proxy/closure 仍由同一事务提交。
- 构建、门禁、失效与交接：core、owner-services 与 CLI typecheck 通过，core/owner-services fresh build 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 33/33 与 registry golden、fresh `pnpm runtime:package-exports`、适用变更文件最窄 Biome 和 `git diff --check` 通过。S7 冻结三项 mechanism exact-set、唯一 state/persistence owner、Correctness Store 映射、Controller 旧 helper/直写归零及 owner-services 旧 proxy 源码/导出/build entry 归零，并能拒绝桥增项、Controller outcome helper 与 Correctness terminal 映射缺失。以后若 proxy settlement、eligibility/budget、review outcome/failure handling/proxy/closure 规则、持久 decision/Store 映射、missing-proxy 重建、三项 mechanism、生产装配、S7/package export 或上述直接测试任一变化，只恢复 `A5-15k-advancement-review-outcome-application-v1` 并重验本闭包；A5-15a～A5-15j 不随之失效。A5-15k 当前完成并等待协调者独立复核；下一责任链只允许 A5-15l 迁移三项 Evidence/Reviewer 适配并删除 `A5-ADVANCEMENT-REVIEW-01`，Advancement 行与 A5 继续为 `[ ]`，未进入 A6，未执行 Git 暂存、取消暂存、提交、历史改写或推送。
- 协调复核纠正：删除 `#settleAcceptedProxyRun` 中首个 `!outstandingProxyMessageId` 分支已完整返回后紧邻复制的不可达同条件分支；保留唯一的 known/already-settled 幂等返回、unknown proxy system-exit 与 outstanding mismatch 裁决。对本包迁入 core 的 `#prepareEligibility / #settleAcceptedProxyRun / #systemExitReview / #persistReviewOutcome` 作一次同类局部反扫，未发现其他连续不可达重复。Core 应用 13/13 与 `git diff --check` 重新通过；既有 68/68、构建、S7、package-export 证据未因纯删除不可达逻辑失效。`A5-15k-advancement-review-outcome-application-v1` 据此恢复，仍等待协调者独立复核。

- 协调者独立验收：重新沿 accepted proxy、eligibility、review outcome、Correctness Store 映射、closure 与 missing-proxy recovery 追踪 A5-15k，确认 review/window/evidence/attempt 与 terminal/proxy 仍由一个事务提交，Controller 已归零状态决定、outcome helper 与 review/proxy 写入口，三项外部 mechanism exact-set 之外没有 fallback；协调复核发现并退回删除一段不可达重复 proxy 分支，纠正后局部反扫无同类残余。独立重跑 core 应用 13/13，并核验既有 CLI/Server 55 项、构建、S7 33/33、registry golden、package-export 与本次纯删重复逻辑的失效关系；`git diff --check` 通过。接受 `A5-15k-advancement-review-outcome-application-v1`，提交为 `e7271f5a`。

### A5-15l：归位 Evidence/Reviewer 外部适配并删除 A5-ADVANCEMENT-REVIEW-01

- 派发基线与唯一架构结果：以 `HEAD e7271f5a` 加本节调度记录为基线。把 `resolveRootTarget / prepareEvidence / invokeReviewer` 作为固定装配的三项外部机制端口注入唯一 `AdvancementReviewAttemptApplication`，使 accepted-turn、recovery catch-up 与测试都直接调用同一个应用对象；Controller 不再拥有 after-turn review 入口、Evidence/Reviewer 装配或逐次创建 mechanism，`A5-ADVANCEMENT-REVIEW-01` 源码、导出、build entry 和生产消费全部删除。
- 必须保持的生产语义：carried outcome target 优先、executor/owner-epoch 冻结、无 Evidence/runId 时跳过取证、Evidence collect 的 lineage/generation/run/root/abort 输入、requestId 与 canonical evidence 透传、deferred 的 aborted/infrastructure 分类和现有文案、Reviewer 输入/资源 lease/abort、异常转 deferred、accepted run identity 校验及调用次数均不漂移。两类 Host 必须在开放入口前固定构造且只共享一个 review 应用；recovery 对 invoking 禁止重放、catch-up 连续集合、结果投影与 proxy schedule 次序继续成立，不出现 getter、late bind、Controller fallback 或第二应用实例。
- 旧路与适配边界：删除 `AdvancementController.afterTurnCommitted`、`reviewAttemptMechanism`、`reviewAttempts/evidence/reviewer` review 专属字段和构造参数，以及 `createAdvancementAcceptedTurnReviewMechanism` 与 `review-application-bridge` 子路径。外部机制适配只把 `AdvancementEvidenceCoordinator`、Reviewer port 和错误分类翻译成 core 的三项有限合同，不拥有 eligibility、attempt、root、outcome、持久化或结果投影。旧桥文件中独立的 proxy schedule 效果适配迁回既有 `proxy-scheduler` 机制边界并保持行为，不借本包迁移 proxy delivery。
- 明确不做：不改 Evidence 内部 request/settlement/验签/设备选择算法，不改 Reviewer Provider、Prompt、工具、计量与预算，不改 attempt/outcome 状态机、recovery 扫描算法、result projector、proxy scheduler 行为、其他 Advancement 管理用例、其他领域或 A6；不新增能力、协议、持久 schema、错误、重试或用户文案。
- 直接证据与完成条件：用三项机制适配直接测试覆盖 target 来源、无 target、collect 成功/deferred/abort/异常、Reviewer 成功/deferred/异常与 accepted-run mismatch；复用真实 governor/store/metered reviewer、accepted-turn 两类 Host 和 recovery 最窄组合，证明固定同一应用、调用次数、响应丢失与重启语义不变。更新 S7/反向 mutation 和 package-export 门禁，拒绝 Controller after-turn/mechanism/字段回流、桥文件/子路径/build entry、Host getter/第二应用、recovery Controller 直调或机制端口增项。按验证手册只运行失效闭包、必要依赖构建、changed-file lint、canonical S7、fresh package-export 与 `git diff --check`，不得运行根级回归、制品、A5-15k outcome 全闭包或未失效领域测试。完成时桥登记为零，所有生产消费者固定调用唯一 review 应用，Controller 仅保留尚属其余管理机制的职责，工作区可构建可运行并登记证据后停在安全交接点；执行者不得执行 Git 操作。

- 实施责任与生产链：`AdvancementReviewAttemptApplicationService` 构造时固定接收只含 `resolveRootTarget / prepareEvidence / invokeReviewer` 的 readonly mechanism，调用期不再接受可替换机制；accepted-turn 与 recovery 都直接消费同一个 `AdvancementReviewAttemptApplication`。owner-services 新的 `review-external-mechanism` 仅以 request 对象 identity 绑定本次完整 Evidence target，并翻译 collect/reviewer 输入与 aborted/infrastructure 分类，不读写 review state、attempt、outcome 或持久事实；并发 request 不共享可覆盖 target。Anchor 与 local-owner 两个生产组合点均在公开入口前一次构造 `{ controller, reviews }`，同一 `reviews` 同时交给 accepted-turn、recovery 和 cancellation，未引入 getter、late bind、fallback 或第二 application。
- 旧路与机制归位：`AdvancementController.afterTurnCommitted`、逐调用 `reviewAttemptMechanism`、Controller 的 `reviewAttempts/evidence/reviewer` 字段与构造参数、`createAdvancementAcceptedTurnReviewMechanism`、`review-application-bridge` 源码/subpath/build entry 和全部生产消费归零；Controller 仅保留仍属管理面的读取与管理用例。旧桥中的 proxy schedule 效果适配原样迁至既有 `proxy-scheduler` 边界，未获得 review state、结果投影或 delivery 责任。carried target 优先、冻结 executor/owner epoch、无 Evidence/runId 跳过、collect/reviewer 输入与错误分类、accepted-run identity、invoking recovery 禁止重放、catch-up/projection/proxy schedule 顺序均保持既有合同。
- 直接证据：Core application/review-attempt 2 文件 69/69，Server Controller/recovery 2 文件 48/48，新外部机制适配 2/2，CLI 真实 governor/store/metered reviewer 7/7，合计 6 个本包直接文件 126/126；Anchor access-surface 3/3 与 local-owner surface 5/5 进一步命中两类 Host 接线。直接反例覆盖并发 target 隔离、Evidence/reviewer 失败分类、accepted-run mismatch、invoking 不重放、固定应用调用和旧 Controller 路径归零。额外扩大执行的 local-owner lifecycle 中 16/17 通过，唯一既有用例 `fails close when settlement is unprovable...` 两次均在 artifact lifecycle maintenance 被 `backpressured:ioOperations` 拒绝，与本包 review 机制/组合输入无关；该用例不计作本包通过证据，也未据此放宽断言或修改实现。
- 构建、门禁、失效与交接：core 与 owner-services fresh build、CLI typecheck 和 `pnpm cli:build` 通过；canonical `pnpm s7:lint` 的 coverage/mutation 33/33 与 registry golden、fresh `pnpm runtime:package-exports`、适用变更文件最窄 Biome 通过。S7 冻结固定三项 mechanism、adapter 无 state/outcome 写权、两个生产组合共享一个 review 应用、accepted-turn/recovery 直调、Controller 旧入口归零，以及旧桥 source/export/build 归零，并以反向 mutation 拒绝调用期 mechanism、第二 Host application、Controller fallback、桥复活和 adapter 状态写入。以后若三项 mechanism/adapter、review 应用构造、两类 Host 组合、accepted-turn/recovery/cancellation 消费、Controller review surface、proxy schedule 归属、owner-services export/build、S7/package export 或上述直接测试任一变化，只恢复 `A5-15l-advancement-review-external-mechanism-v1` 并重验本闭包；A5-15a～A5-15k 不随之失效。A5-15l 当前完成并等待协调者独立复核，Advancement 行与 A5 继续为 `[ ]`，未进入 A6，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

- 协调者独立验收：重新追踪 review 应用唯一构造、三项外部机制、两类 Host、accepted-turn、recovery、取消链、proxy schedule 适配和 owner-services 出口，确认 `A5-ADVANCEMENT-REVIEW-01` 的源码、子路径、build entry、Controller after-turn 与生产消费全部归零；应用在构造期固定 mechanism，Anchor/local 各自只有一个实例并在入口开放前交给全部消费者，无 getter、late bind、fallback 或第二实例。独立重跑 core application/review-attempt 69/69、Server Controller/recovery/external-mechanism 50/50、CLI 真实 governor/store/reviewer 7/7，合计 126/126；`git diff --check` 通过，并核验既有 S7 33/33、registry golden、fresh package-export 与本轮输入一致。接受 `A5-15l-advancement-review-external-mechanism-v1`，提交为 `29fca498`。

### A5-15m：Advancement 全领域 owner、入口、桥与写权零残留收口

- 派发基线与唯一架构结果：以 `HEAD 29fca498` 加本节调度记录为基线。复用 A5-15a～A5-15l 已接受证据，从当前生产入口重新枚举 Advancement 的 detail/query、Rubric revise/cancel/confirm/publication、原任务准入、active user turn、conversation retirement、accepted-turn review、attempt/evidence/reviewer/outcome、recovery、result projection、proxy scheduling 与持久 Store，证明每项产品决定只有一个 core Advancement 应用 owner，Controller/Correctness/外部 adapter 只保留有限机制，全部旧桥、第二入口和第二写权为零；成立后勾选 A5 登记表的 Advancement 行。
- 核查方法与修复边界：从 CLI/RPC/Conversation committed-turn/recovery/system maintenance 四类生产入口正向追踪到应用，再从 `AdvancementSessionStore` 全部写方法、持久 event、公开导出、Host 构造和 package build 反向追踪消费者。逐个核对 A5-15a～A5-15l 的应用 owner、机制 port、Product API/accepted-turn 入口与退场点，确认没有 Surface/Server/CLI/Controller 直接复刻规则、绕 dispatcher 写入、旧文件/导出/build entry、兼容别名、fallback、双状态机或未登记桥。若发现真实残余，只修复所属同一责任链并精确恢复相应证据；影响超过一个独立子领域或不能在四小时内安全闭合时停止反馈，不得无界扩面。
- 必须保护：所有现有 Advancement RPC/Event、用户文案、Rubric、原任务与 active turn 流程、accepted review、Evidence、资源计量、proxy identity/delivery、closure、conversation deletion、恢复/重放、持久 schema、错误和终态保持不变。不得因收口把 Controller 机械清空；仍作为有限 persistence/provider/adapter 机制的成员必须有真实应用消费者和唯一职责，不能为追求目录纯度误删。
- 明确不做：不重新设计或重跑任何已接受 A5 子迁移，不进入 Device、Backup、A6，不改 Evidence/Reviewer/Proxy/Store 算法，不新增包、通用框架、能力或协议；不运行根级回归、制品、全量 S7 重复取证或全部 A5 历史测试。只有代码/门禁实际变化才运行对应最窄失效闭包；纯裁决使用生产调用图、现有直接证据索引、最窄静态门禁和 `git diff --check`。
- 完成条件：形成紧凑的“产品入口 → 唯一应用 owner → 有限机制/写入 → 直接证据”exact-set，并由可执行结构门禁冻结；Advancement 所有生产写入口都有且只有一个应用决定者，A5-15a～A5-15l 无未处置反证，桥登记为零，Controller 与 adapter 不拥有产品状态机，公开/持久行为无变化。满足后把 A5 登记表 Advancement 行更新为 `[x]`，登记失效规则与下一检查点 Device Administration，停在干净、可构建、可运行的安全交接点；执行者不得执行 Git 操作。

- 实施基线与反证修复：以 `HEAD 29fca498` 加协调者调度记录为基线，形成稳定证据 `A5-15m-advancement-domain-closure-v1`。正向复核四类正式入口、反向扫描 `AdvancementSessionStore` 全部读写方法、event、Host 构造、ServerContext、公开导出与 build 输入后发现两项真实残余：Server/CLI 对 active Advancement 状态仍各自读取 Controller 并复刻投影；Conversation run-control 与 recovery 在 proxy 已被 durable claim 关闭时仍直接经 Controller/Store 结算。现以 `AdvancementReviewAttemptApplicationService.queryActiveState / settleProxyRun` 成为唯一 projection/settlement 决定，`AdvancementApplicationService` 只把同一 active-state Query 纳入既有 sealed Product API；Server `session.list/resume/send` 和 Conversation 目录只消费有限投影，recovery/run-control 只调用同一个 review 应用。`ServerContext.advancement`、Controller 公共 `settleProxyMessage` 与两处旧编排归零，Controller 内只保留 active-user-turn interruption 所需的有限 persistence mechanism。

  | production family / 入口 | 唯一应用 owner | 有限机制或唯一写入 | 直接证据 |
  |---|---|---|---|
  | active-state | `AdvancementReviewAttemptApplicationService`；Product API 仅转发同一 Query | `AdvancementReviewAttemptStatePort.loadActiveSession` | review application + session RPC |
  | detail | `AdvancementApplicationService` | `AdvancementDetailReadPort.loadLatestSession` | application + session RPC |
  | Rubric lifecycle / publication | `AdvancementApplicationService` | Controller rubric read/build/persist mechanism + Fact publisher | application + session RPC |
  | original-task admission | `AdvancementApplicationService` | original-task execution/confirmed-admission ports | application + session RPC |
  | active user turn | `AdvancementApplicationService` | Controller transition mechanism + Conversation runtime effect | application + session RPC |
  | conversation retirement | `AdvancementConversationLifecycleApplicationService` | Controller data mechanism + Conversation lifecycle projector | application + Conversation delete/system maintenance |
  | accepted committed turn | `AdvancementAcceptedTurnApplicationService` | catch-up + review + result projection ports | application + Anchor/local-owner binding |
  | review attempt / outcome / proxy settlement | `AdvancementReviewAttemptApplicationService` | review Correctness state/root ports；三项 Evidence/Reviewer mechanism | review application + real review recovery |
  | result projection | `AdvancementReviewResultProjectionApplicationService` | observer/proxy scheduling effects | application + recovery |
  | recovery | `AdvancementRecoveryMaintenance` | 上述 lifecycle/review/result 三个应用；不直接决定 Store transition | recovery maintenance |
  | proxy scheduling | `AdvancementProxyScheduler` | Conversation proxy-turn effect port | accepted-turn/recovery 既有证据 |
  | evidence | `AdvancementEvidenceCoordinator` | 签名请求/验签与 `AdvancementSessionStore` evidence primitives | A5-15a/A5-15l 既有 Evidence 证据 |
  | persistence correctness | `SessionAdvancementStore` | 唯一生产 `SessionStatePort`/Authority event commit 与折叠读取 | session-store/A0 P06 既有证据 |

- 写权、导出与行为裁决：生产 Store 写调用只剩三个有限机制 owner：Controller 提供 Rubric/active/lifecycle persistence primitives，`AdvancementEvidenceCoordinator` 提交 evidence primitive，`review-attempt-correctness` 提交 attempt/outcome/proxy primitive；领域状态分支、顺序与终态均在上表应用中，CLI/Server/recovery/system maintenance 没有 Store 写入。无生产构造的旧文件 `AdvancementStore` 不属于 production exact-set，其正式 `@zhixing/core` 根/`./advancement` 转导已删除，fresh 声明与运行时导出均不再包含该类；源码内仅作为内部测试夹具，S7 明确拒绝任一 production import/new/export 或 build/public 入口回流。A0-06a 的“无第二持久根”结论因此不变。全部现有 Advancement RPC/Event、中文文案、Rubric、原任务/active turn、accepted review、Evidence、资源计量、proxy identity/delivery、closure、conversation deletion、恢复/replay、错误、终态与 Authority schema 均未改变。

- 验证、失效与交接：Core application/review 2 文件 71/71；Server recovery/session/workscene 3 文件 131/131 与 Controller 21/21；CLI 真实 review recovery 7/7，合计 7 个直接文件 230/230。core、owner-services、server fresh build、CLI typecheck 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 33/33 + registry golden、fresh `pnpm runtime:package-exports`、changed-source Biome 与 `git diff --check` 通过。S7 冻结上述 13-family exact-set、active Query/settlement、两个生产消费面、recovery、ServerContext、Store write allowlist 和旧 Store 正式入口归零，并以反向 mutation 拒绝任一回流。以后若任一应用/机制 owner、Product API exact-set、Controller/Correctness/外部 adapter、Host 构造、Store primitive/event/schema、active projection、proxy settlement/recovery、公开 export/build 或上述 S7/direct evidence变化，只恢复本证据及实际受影响的 A5-15 子证据；无关 Device/Backup/A6 变化不误伤本闭包。Advancement 行已据当前实施结果更新为 `[x]`，A5 继续 `[ ]`，下一检查点为 Device Administration；本包未进入后续领域，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-16a：归位 Device Administration 用户可见读取投影与纯 binding

- 实施基线与责任 exact-set：以 `HEAD d73fc944` 加协调者调度记录为基线，形成待复核证据 `A5-16a-device-administration-read-projection-v1`。即时正向追踪 `device.list`、`device.status`、`dutyMigration.targets` 三个认证 RPC，反向核对 `MeshRuntimeAssembly.removableDevices/deviceRemovalStatus/plannedAnchorTargets`、Host 组合、ServerContext 与 CLI management facade 后，确认本包读取 exact-set 恰为“设备关系候选、指定设备移除公开状态、值班接班候选”三个 Query；它们不产生 Fact Event，也不拥有 remove/continue/prepare/commit/cancel 写入、Trust/DeviceLifecycle/Assignment/Mesh 状态机或外部效果。
- 唯一应用与机制边界：新增唯一窄入口 `@zhixing/core/device-administration/application`，由 `DeviceAdministrationApplicationService` 定义三个有限 readonly Query/Result、不可变用户投影和 `DEVICE_ADMINISTRATION_PRODUCT_API_EXACT_SET`；三个 read port 只读取既有关系、移除状态和候选 readiness 机制，不暴露 Store、Authority、路径、Mesh/connection 或写端口。Anchor 唯一组合根只构造一次该应用与 contribution，并与既有领域 contribution 一并封入同一个 `ProductApiDispatcher`；应用缺失时三个 RPC 继续以迁移前“设备管理当前不可用/值班设备迁移当前不可用”fail closed。
- Surface 与旧路退场：`device.list/device.status/dutyMigration.targets` handler 现只保留认证后的 exact 参数校验、Product API Query 调用及既有错误/结果投影；`ServerContext.dutyMigration.targets`、`ServerContext.deviceLifecycle.list/status` 与 CLI Host 三个直接 read closure 已删除。CLI `RpcManagementFacade` 继续只拥有原 wire 请求、严格解码和现有公开类型，设备列表、移除状态 `null`、候选 `ready/code`、中文错误和命令行为均未改变。五条写接口按实时台账桥 ID 保留，下一检查点固定为 `A5-16b device.remove/device.continue`，本包未迁移或重解释其写决定。
- 直接证据与构建：Core Device Administration application/Product API 3/3、Server management binding 62/62、CLI management facade + device removal journey 2 文件 13/13，合计 4 文件 78/78；覆盖三 Query exact-set/零 Fact、不可变投影、缺状态为 `null`、无 contribution 拒绝、原 wire DTO、设备列表/状态消费与现有写方法不漂移。Core fresh build、Server fresh build、CLI typecheck 与 `pnpm cli:build` 通过；fresh `pnpm runtime:package-exports` 通过。canonical S7 coverage/mutation 34/34 与 registry golden 通过，新增反向 mutation 可识别 Query identity/kind 漂移、第二应用构造、RPC 恢复 ServerContext 直读、read owner 回流和根/重复导出；最窄 Biome 无修复。
- 失效与交接：以后若三个 Query/Result、read port、应用/descriptor owner、Host contribution/exact-set、ServerContext、RPC handler、CLI wire codec、`MeshRuntimeAssembly` 三个读取机制、core 窄 export/build、S7/package-export 或上述直接测试任一变化，只恢复本证据并重验实际失效闭包；五条写桥、DeviceLifecycle/Trust/Assignment/Mesh 内部与后续 Backup/A6 变化不自动误伤本读取证据。Device Administration 行和 A5 继续为 `[ ]`；当前停在可构建、可运行、单一读取真相成立且写链未改的检查点等待协调者独立复核，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-16b：归位 Device Administration 设备移除写决定与纯 binding

- 实施基线与责任 exact-set：以已接受 `HEAD 85d8eea9` 加协调者调度记录为基线，形成待复核证据 `A5-16b-device-removal-application-v1`。即时正向追踪 `device.remove/device.continue`，反向核对 `CurrentIssuerDeviceRemovalAuthority`、DeviceLifecycle journal/receipt、target Mesh client、accepted-work、current-duty identity、Host composition、Server/RPC 与 CLI management facade，确认本包产品 exact-set 恰为“开始移除”和“继续 transfer/destroy/lost/cancel”两个 Command、既有开始/公开移除结果及零 Fact Event；journal、签名 receipt/abort、accepted-work settlement、target physical cleanup 与 Mesh transport 继续是有限 Correctness/Infrastructure 机制。
- 应用决定与机制边界：唯一 `DeviceAdministrationApplicationService` 现拥有 current-duty guard、active/同名/自移除选择、稳定 request/operation identity、operation/target/name 全等、connected/offline、cancel/lost/transfer/destroy 分支，以及 cancelled replay、waiting-for-device、removed/unknown 和既有中文/凭据行动结果。泛型 `Accepted/Abort` token 对领域保持不透明；Authority port 只执行 accept-for-device、operation projection、durable abort/commit-lost，Mesh effect port 只执行 reachability 与同一 receipt/abort/decision 的目标效果，领域未复制签名、journal、恢复或 accepted-work 状态机。`MeshRuntimeAssembly.beginDeviceRemoval/continueDeviceRemoval` 产品入口已删除，只保留可逐项反查的 context/Authority/target effect 原语；同 request/operation 的 journal 幂等、响应丢失重放、accepted-work drift、sealed/release 和物理清理顺序继续由既有机制唯一负责。
- Product API、Surface 与旧路退场：Device Administration sealed exact-set 由 3 Query 扩为 3 Query + 2 Command；Host 仍只构造一个应用与一个 contribution。`device.remove/device.continue` handler 只保留认证后的 exact wire 解码、dispatcher Command、既有 `runDeviceLifecycleOperation` 错误映射和结果传输；`ServerContext.deviceLifecycle.remove/continue`、CLI 同名 closure 以及两条桥 `A5-DEVICE-REMOVE-01/A5-DEVICE-CONTINUE-01` 归零。公开 RPC 名、字段、operationId 只对 exact cancel 可选、中文错误/行动、管理 facade、持久 schema 与单机/分布式拓扑均未改变；三条 duty migration 写桥按下一责任链保留，本包未进入 uninstall、Trust Administration、Backup/A6 或 duty migration 写决定。
- 直接证据与构建：Core Device Administration application/Product API 8/8 与 Server management binding 64/64，合计 2 个直接文件 72/72；覆盖 begin connected/offline、current-duty/unknown/self/同名拒绝、transfer/lost、online/offline cancel、exact cancelled replay、inactive/mismatch/offline、opaque token 单次传递、Command exact-set/零 Fact、RPC exact decode、缺 contribution fail closed 与既有 wire error。A5-16a 的三个读取 Query 证据在同两文件中继续成立；未修改的 DeviceLifecycle/Mesh 既有 restart/response-loss/accepted-work drift 机制证据按已接受基线复用，未以本轮重复全测冒充新结果。Core、Server fresh build 与最终 `pnpm cli:build` 通过；canonical S7 coverage/mutation 34/34 + registry golden、fresh `pnpm runtime:package-exports`、changed-source Biome 与 `git diff --check` 通过。
- 失效与交接：S7 现冻结 3 Query + 2 Command exact-set、唯一 Host 应用构造、opaque Authority/effect ports、两个 RPC 只走 dispatcher、ServerContext 写桥归零、Mesh 产品方法归零和 core 窄 export/build，并以反向 mutation 拒绝 Command/Query kind 或 identity 漂移、第二应用、RPC/ServerContext 直连、Mesh 恢复产品决定及根/重复导出。以后若两个 Command/Result、选择/分支规则、Authority/effect port、Host contribution/exact-set、ServerContext、RPC handler/error mapping、CLI wire、DeviceLifecycle/Mesh mechanism、core export/build、S7/package-export 或上述直接测试任一变化，只恢复本证据及真实失效的 A5-16a 交界；duty migration、Backup/A6 的无关变化不误伤。Device Administration 行和 A5 继续 `[ ]`；下一检查点固定为 duty migration 写链，当前停在可构建、可运行、可恢复且无双写/双 admission 的检查点等待协调者独立复核，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

### A5-16c：归位 Device Administration 值班迁移写决定与纯 binding

- 实施基线与责任 exact-set：以已接受 `HEAD 5fdb7cd6` 加协调者调度记录为基线，形成待复核证据 `A5-16c-duty-migration-application-v1`。即时沿 `dutyMigration.prepare/commit/cancel → ServerContext/CLI closure → MeshRuntimeAssembly → PlannedAnchorTransferOwner/Journal/Checkpoint/Transport` 正向追踪，并从 Product API、当前值班/换代/设备移除 gate、target membership、prepare/freeze/commit/abort 与既有恢复入口反向核对；本包产品增量恰为 prepare、commit、cancel 三个 Command 与 `ready/completed/cancelled` 三个 readonly result，Device Administration 完整 exact-set 为 3 Query + 5 Command、零 Fact Event。
- 应用决定与机制边界：唯一 `DeviceAdministrationApplicationService` 现形成稳定 request/transfer/target identity，拥有 current-duty、post-install generation、device-removal admission、active paired duty-capable target 选择与三阶段结果；prepare/commit 继续在设备移除期间拒绝，cancel 保留既有可达边界。窄 `dutyMigrationContext` 只投影本机/当前值班身份、换代 readiness、移除在途与成员资格，窄 mechanism port 原样交付稳定 identity；`PlannedAnchorTransferOwner`、运行期串行器、Authority log、签名、checkpoint、CAS/artifact、Mesh transport 与 journal 继续唯一拥有 durable claim、freeze、response-loss/restart replay、异载荷冲突、commit/abort 边界和 physical cleanup，未复制第二 admission state、journal 或恢复链。
- Product API、Surface 与旧路退场：既有 Host 仍只构造一个 Device Administration 应用和一个 sealed contribution；三个 RPC handler 只保留认证后的 exact wire 解码、dispatcher Command、既有 `runDutyMigrationOperation` 错误/行动文案映射与结果传输。`ServerContext.dutyMigration`、CreateContext 字段、CLI 返回阶段 closure 及三条 `A5-DUTY-MIGRATION-*-01` 桥归零；公开 RPC 名、请求字段、中文错误、目标 query、CLI journey、持久格式、当前设备卸载和单机/双拓扑均未改变。本包没有进入 uninstall、Backup、Trust、Assignment、Channel 或 A6。
- 直接证据与构建：Core Device Administration application/Product API 11/11 与 Server management binding 65/65，合计 2 个直接文件 76/76；覆盖 3 Query + 5 Command exact-set、稳定 identity 同载荷重驱、prepare target/current-duty/generation/removal admission、cancel 的既有 removal overlap、三阶段结果、严格 wire、缺 contribution fail closed、目标/unknown/busy/响应不明/提交后取消的原 wire error。未修改的 PlannedAnchorTransfer journal/checkpoint/signature/response-loss/restart 机制证据按已接受基线复用；本轮通过同一 identity 直传与生产结构门证明没有新的重放 owner。Core、Server fresh build、CLI `tsc --noEmit` 与 `pnpm cli:build` 通过；canonical S7 coverage/mutation 34/34 + registry golden、fresh `pnpm runtime:package-exports`、changed-source Biome 与 `git diff --check` 通过。
- 失效与交接：S7 现冻结 3 Query + 5 Command/8 binding exact-set、唯一 Host 应用/contribution、duty migration context/mechanism ports、三个 RPC 只走 dispatcher、ServerContext 与 CLI 阶段闭包归零、core 窄 export/build，并以反向 mutation 拒绝 operation 漏项/第二应用、RPC 或 ServerContext 直连、阶段决定回流 CLI、context/mechanism 端口丢失及根/重复导出。以后若三 Command/Result、准入/目标规则、稳定 identity、context/mechanism port、Host composition、ServerContext、RPC handler/error mapping、planned transfer owner/journal/checkpoint/transport、S7/package-export 或上述直接测试任一变化，只恢复本证据及真实相交的 A5-16a/A5-16b 结论；uninstall、Backup/A6 的无关变化不自动误伤。Device Administration 行和 A5 继续 `[ ]`；下一检查点固定为有限零残留审计与当前设备卸载责任裁决，当前停在可构建、可运行、可恢复且无双写/双 admission 的检查点等待协调者独立复核，未执行 Git 暂存、取消暂存、提交、历史改写或推送。

## 十、用户提示词

### 收敛 AE-001 架构迁移任务

```text
目标：持续审查并收敛 `docs/tasks/ae-001-companion-intelligence-architecture-migration.md`，直到它成为一份真实、完整、无歧义且可直接接入知行持续协调自主开发体系的任务契约；协调者仅依据 AE-001、本文、当前仓库事实和有效证据持续派发工作包，执行完本文全部任务后，就能客观证明知行生产实现已经完整迁移到 AE-001 目标架构，当前全部正式能力和首版产品基线没有回退，旧责任路径没有残留。

目标架构已经完成独立审查并获得用户认可，本任务不重新设计架构。`research/design/architecture/evolutions/AE-001-companion-intelligence.md` 的“一、原始构想”是最高产品依据，正文是唯一目标架构权威；本文只能把它转化为可执行、可调度、可验证、可结束的迁移契约，不得弱化、替换或增加另一套架构。只有生产事实证明 AE-001 内部存在无法唯一裁决且会改变产品定位或目标架构的真实冲突时，才可把唯一决策点交给用户；不能因为实现困难、当前代码不整齐或已有结论不同而重开架构设计。

以下五项要求不可修改、不可弱化：①完整落实 AE-001 的核心命题、职责、状态归属、依赖、控制、生命周期、组合、表面、内核、正确性底座和设备拓扑边界，不能只改名、搬文件或增加接口；②当前全部正式能力、公开行为、协议语义、持久化数据、安全边界、异常终态、单机/分布式行为和首版产品体验不得回退，用户不得承担新的架构概念与维护负担；③迁移后必须删除旧业务入口、第二组合根、第二应用行为、双写、双权威、双状态机、永久兼容桥、服务定位器旁路、根导出旁路和等价改名残余；④本任务只迁移架构，不新增能力，不恢复 Memory、不接入 OWNWARD，不引入未来对象、插件平台、自修改、微服务或没有当前事实依据的通用框架，也不把 distributed-runtime 或任何技术提升为产品中心；⑤本文必须足以让协调者通过实时台账和可追溯证据独立核对执行者反馈、把阶段门拆成默认 2～4 小时且只有一个架构结果的工作包、在约四小时或异常扩面时安全止损、选择下一条最关键问题链、维护真实状态、只重验失效闭包，并在全部完成条件真正成立时客观停止。

AE-001 和上述五项要求决定“必须达到什么”；生产源码、包依赖、组合根、调用与消费链、状态写入、测试、构建配置、公开文档、Git 历史和制品只用于核实现状、保护基线、迁移依赖与任务可执行性，不能反向降低目标。既有设计文档、本文当前阶段划分、文件名、包名、测试绿灯和执行者自报均不能自证正确。必须从真实生产入口正向追踪，再从写入口、消费者、恢复、公开出口和制品反向核对；本文现有内容同样待复核。

只允许修改 `docs/tasks/ae-001-companion-intelligence-architecture-migration.md`。不得修改 AE-001、代码、测试、其他文档、工作台提示词或 Git 状态，不得实施迁移、运行正式构建/全量回归/制品验收，也不得把任务状态、A0～A7、A5 登记行或最终退出门提前标记为完成。允许使用只读源码、Git 和最窄静态查询核实任务真实性；不得为了收敛文档建设诊断工具、门禁实现或迁移脚手架。

每次开始、恢复或上下文压缩后，重新完整读取本文与 AE-001，检查当前仓库和 Git 状态，并从当前事实继续。持续执行以下循环：

1. 审查目标映射是否完整。把 AE-001 的核心命题、三条变化轴、领域所有权、Product API、Intelligence Kernel、Correctness Substrate、ApplicationHost、可组合边缘、设备/拓扑分离、十四条不变量和迁移检查点逐项反查到本文的 A 项、约束、验证或退出门。每条目标必须有唯一、可执行的任务落点；不得遗漏，也不得在多个 A 项形成重叠所有权。
2. 重建当前到目标的真实迁移闭包。沿全部生产进程角色、CLI/Channel/Server 入口、组合根、ServerContext、AgentRuntime、RuntimeHost、领域用例、Owner/Journal/Recovery、工具与效果链、Executor/Mesh、包依赖、导出和制品，确认本文覆盖当前 producer、consumer、写入口、恢复、生命周期和公开出口，并能把每项能力唯一归入 Domain、Product API binding/Surface、Kernel、Correctness、Host 或 Infrastructure。任务应以责任链为单位，不能把文件清单、目录整理或包拆分冒充迁移，也不能根据 AE-001 的示例枚举漏掉当前真实产品责任或为了穷举虚构新领域。
3. 审查保护基线。确认任务能够冻结并持续保护模型—工具循环、上下文、子 Agent/编排、多视角、任务列表、对话与历史、工作场景、调度、推进、投递、信任、技能、设备、权限/确认/资源、安全、持久化/恢复、恢复备份/灾难恢复、配置/Provider/MCP、维护/卸载、单机/分布式、公开协议、构建与首版旅程；每个迁移交界都要有可识别回归，公开命令/RPC/Event、工具/Profile、配置 schema、持久记录族、包导出和制品入口要有 exact-set 防漂移证据。内部行为等价调整不得悄悄改变默认值、错误、终态、数据兼容或用户体验。
4. 审查 A0～A7 的依赖和颗粒度。A0 只建立足以裁决 A1/A2 和后续迁移顺序的最小基线：生产角色/生命周期、公开与持久 exact-set、代表性产品回归、owner/Kernel/Host/拓扑/依赖决策图和首批验证映射；不得预先穷举所有产品旅程、错误、状态机和消费者，详细事实在实际改变对应责任的工作包中即时双向核实。A1 先闭合 Host 生命周期而不搬业务；A2 用一个真实领域证明垂直模型；A3 只从已证明合同建立 Product API 和纯 binding；A4 只封闭 Kernel 合同并把产品投影交回当前唯一责任者，不提前复制领域状态机、权威或 Product API；A5 复用 A2～A4 产物逐项归位全部现有领域与管理责任，并把业务语义移出 Correctness 共享机制而不在领域重造通用机制；A6 收束边缘和拓扑；A7 删除桥并终验。A0～A7 和 A5 各行都是阶段验收单元而非默认工作包，必须能够沿一个 owner/写权、生命周期或可独立证伪的不变量继续拆分；不得存在循环前置、长期不可构建中间态、先建空框架、同一责任重复迁移或完成一个 A 项仍不能安全进入下一项的情况。
5. 审查自主调度有效性。每个工作包必须写清当前检查点、唯一架构结果、生产端与全部消费端、保护边界、旧路径处置、明确不做、最窄证据、完成条件和安全交接点，默认具有 2～4 小时真实有效工作量。预计超过四小时、包含多个独立 owner/写权/生命周期、存在多个可分别失败的未知、具有不同验证闭包、跨 A 项或 A5 领域、或无法说明四小时内安全交接点时，必须在派发前继续拆分；短包只能合并同一责任链，不能为凑时长扩面。执行中约四小时仍未完成、连续两条实质路径失败、影响面显著扩大、出现意外跨域依赖或任务假设被反证时，必须停在可构建、可运行、单一真相成立的检查点反馈协调者，不得继续钻研到一天；已启动且不可安全中断的单次确定性验证可运行到结束，但不得顺带继续下一事项。必要的跨包迁移只能用有明确紧邻退场包的最短单向适配，任何阶段不得双写、双权威或留下不可发布半链。协调者必须独立复核结果，普通技术问题交回协调者调整工作包而非上抛用户，执行者始终没有 Git 写权限。
6. 审查状态与证据规则。文档必须用实时台账明确当前接受基线、A 项、活跃/下一责任链、打开的单向桥、已失效证据和阻塞，并以只增工作包记录保留历史；协调者应能在对话中断、上下文压缩、工作区变化或证据失效后从同一检查点恢复，而不必从长日志猜测当前结论。`[x]` 只能表示当前基线上的完整责任链已经成立。源码、测试、架构文档、构建配置、制品或验收输入变化时，必须能精确恢复 A0 基线索引、对应 A 项、A5 行和退出门并作废旧证据；目标架构变化时必须重新执行受影响的 A0 映射。状态不得依赖执行者自报、历史绿灯或对话记忆。
7. 审查验证质量与效率。每个任务使用足以证明其架构合同的最窄直接证据，最终同一基线才执行架构门禁、根级回归和 Windows x64 正式制品验收；严格复用 `research/design/workbench/verification-runbook.md` 的串行、一次取证、上游先构建和失效闭包规则。不得通过 mock、旧 dist、workspace 源码、无识别力 snapshot、放宽断言、重复全测或纯观测 benchmark 获得完成结论，也不得为了显得完整建设当前任务不需要的新验证框架。
8. 审查最终退出门与对抗复核是否真正能发现问题。退出门必须分别证明目标架构落实、产品行为等价、唯一 owner/权威、Host 生命周期、Kernel 封闭、正确性链、表面一致、拓扑透明、变化影响面、零迁移残留、文档/制品一致和全部门禁成立。A7 对抗必须从不相信现有实现与绿灯的立场重新攻击真实生产路径，并只在隔离临时副本或门禁负向 fixture 中以反向变异证明新增 Surface、Provider、设备拓扑或领域不能跨轴穿透，不得污染冻结交付物；发现问题后能恢复状态、修复并在新冻结基线上重做完整对抗，不能把复述清单或“测试全绿”当作复核。
9. 极致删减。只保留会改变范围判断、工作包生成、实施顺序、状态恢复、验证选择或完成裁决的信息；删除重复架构说明、作者心理、无法反绑事实的风险设想、过早文件设计和不产生不同动作的文字。删减不得损失五项硬要求、真实迁移闭包和持续协调所需状态。

准备结束时，对同一份未修改任务文档执行六次相互独立的冷启动复审，不得复用前一轮结论：

- **目标忠实性**：仅按本文执行，是否会完整实现 AE-001，而不是得到简化版、改名版、技术中心版或另一套架构；是否混入能力增强和未来记忆决策。
- **生产真实性与可执行性**：任务是否覆盖全部生产进程角色、真实组合根、调用/消费链、事实 owner、生命周期、公开出口与制品；A0 最小决策基线能否直接裁决 A1/A2 和后续顺序，各迁移包是否会即时闭合自身影响链；A0～A7 是否能从当前代码渐进到达且没有循环依赖、空框架、重复迁移或不可发布中间态。
- **产品基线保护**：每条迁移责任链是否保护全部直接交界，尤其是多视角、编排/任务列表、恢复备份/灾难恢复、配置/MCP、维护/卸载及公开 exact-set，是否可能出现编译和测试通过但正式能力、协议、数据、安全、错误、终态、单机/分布式或用户体验已经变化。
- **架构闭包与零残留**：领域、Product API、Kernel、Correctness、Host、Surface、Infrastructure 和 Topology 是否各有唯一责任与可验证边界，是否仍可能存在第二主链、双权威、反向依赖、旁路或永久桥。
- **调度、状态与证据有效性**：外部协调者是否能只靠本文的实时台账、A0 索引和历史记录，把阶段门稳定拆成 2～4 小时、单一架构结果且可安全交接的工作包，独立验收、提交完整工作包、处理止损与阻塞、恢复失效结论并选择下一检查点；是否仍可能出现整阶段派发、为凑时长扩面、执行者无界探索一天、临时桥无退场包、当前状态只能从长日志猜测或依赖作者与执行者对话记忆的情况。
- **退出与对抗有效性**：完成全部勾选是否真的等价于整个迁移预期成立；最终门禁和六面对抗是否有能力主动推翻虚假完成，发现问题后是否必然重新进入修复与完整复核循环。

任何一次复审发现真实问题，都直接修复本文，并从头重新执行全部六次复审。不得为了显得严格而加入没有 AE-001、产品基线或生产事实依据的问题，也不得用“未来可能需要”扩张任务。

只有 AE-001 的全部目标与不变量都有唯一、准确、可执行的任务落点；A0 最小迁移决策基线与实时台账足以从任意中断恢复并直接开始迁移，详细产品事实由实际影响它的工作包即时闭合；A0～A7 与 A5 登记表的依赖、工作包、状态和证据规则足以支撑持续协调自主开发，并能防止整阶段派发、无界探索和单包工作一天；每项迁移都能闭合新责任、全部消费端、旧路径删除和直接验证；最终退出门能够证明当前能力无回退、目标架构真实落地且没有残余；六次冷启动复审均无未处置反证时，回复“伴身智能架构迁移任务文档收敛完成”，简要说明最终判断和实际修改，立即停止；不得开始实施迁移、编写协调调度提示词或执行 Git 操作。
```
