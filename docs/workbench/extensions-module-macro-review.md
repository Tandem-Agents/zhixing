# extensions 模块宏观审查 · 终版报告（final）

线程：20260922-extensions-macro-review-5d2c｜冻结对象：HEAD `809d566f65a86571a6eb704412c704cc86bad247`（工作树 clean）
方法：三角色独立盲审（需求忠实性/边界正确性/实用复杂度）→ 交叉批判互相复核 → 分歧 3:0 消解 → 单点合成。每条发现均经至少一人亲核证据，关键主张三人独立收敛。

## 一、总体结论（无模糊表述）

1. **架构与实现质量成立**：三单元真实复用同一底座（同一 Authority 操作流、同一制品/受管运行、同一 Conversation 接续），无平行状态机、无第二权威/调度器/维护智能体，共用层零渠道字段泄漏（grep 实证）。架构 3.1–3.6 全部落入生产路径。
2. **"三单元全部完成"的准确含义**：协议层与受管流程已验证（真实 Authority 提交/重放、真实子进程与 IPC、隔离、有限重试）；**真实平台连通（飞书账号端到端）与模型自主查找/修复质量未验证**——三单元均用夹具，任务书自认。不得表述为"实机可用已证"。
3. **发现计数：blocker 0｜major 4｜minor 6**。其中代码级缺陷 2（M1 运行能力、M2 信息边界）、权威文档缺陷 1（M3）、验证边界 1（M4）。

## 二、major 发现（4）

### M1 [major·代码·运行能力] rollback readiness 不回填分发 seed 制品
- 位置：`packages/cli/src/runtime/extensions/channel-readiness.ts:27-28`（rollback 分支）vs `:35-40`（当前实例分支，有 `try/catch + artifacts.import` 兜底）
- 证据（ken 发现，donald+linus 双重复核）：活跃 update/repair 操作的 `previous` 若为分发 seed 且本机 artifact 副本缺失，`artifacts.resolve` 直抛无回填；linus 补可达路径：`disaster-recovery-command.ts:739`、`setup-delivery.ts:1114`
- 影响：值班迁移/灾备恢复时，只要存在未完成换版且本机副本缺失，readiness 失败——违反准绳 §3.5"回退复用已保存制品"与接管承诺
- 建议：rollback 分支复用当前实例的 seed 回填逻辑；非 seed previous 继续强制 candidates+artifacts 可用
- 验证：ken（发现）＋donald（亲核）＋linus（复核＋可达性）

### M2 [major·代码·信息边界] 公共快照暴露原始请求全文
- 位置：`packages/core/src/extensions/application.ts:56-58`（publicSnapshot 仅剥 verification/continuation/returnAddress，保留 `source.request` ≤16000 字符）、`:295`（list 经 publicSnapshot 返回）；`packages/server/src/rpc/methods/extensions.ts:12-15`（仅 requiresAuth、无 loopback）
- 证据（ken 发现，donald 端到端亲核，linus 补宽）：①任一已认证 RPC 会话可读全部扩展操作的原始请求全文；②linus 补宽：`extensionManage` 同样返回 publicSnapshot，且经对话内 `extension` 工具（`extension-tools.ts:55-57`）可把**其他会话的请求原文读进模型上下文**——绕过原 Conversation 边界
- 建议：publicSnapshot 移除或摘要 `source.request`（如哈希＋短摘要），原文仅留 Authority 供接续；source-export 走单独受控 action
- 验证：ken（发现）＋donald（端到端）＋linus（泄漏面扩宽）

### M3 [major·权威文档] 核心架构概述未同步扩展域
- 位置：`research/design/architecture/overview.md:17-39`（责任图无扩展）、`:95-106`（领域表无 Extension Management）、`:112-132`（包表仍把 channel-feishu 描述为产品内适配器）；该文件最后更新 `c2242cff` 早于 extensions 落地链（`312bcce6` 起）
- 定级过程：donald/linus 初判 major，ken 初判 minor，交叉批判后 **ken 改投 major（3:0，无残余异议）**——理由：AGENTS.md:56 强制每次模块设计先读概述，入口失真系统性误导后续开发判断（§3.9 影响对象含未来开发）；显式标注：**无运行时影响，属交付/权威文档缺陷**
- 建议：同步概述的责任图、领域表、包索引与 `packages/channels/feishu` 现状（扩展制品形态，非内置适配器）
- 验证：三方独立发现＋定级经辩论收敛

### M4 [major·验证边界] 真实平台与模型自主质量未端到端验证
- 位置：任务书 `:117,128,141` 三处自认夹具；`packages/channels/feishu/src/adapter.test.ts:20`（mock SDK）、`packages/cli/src/runtime/extensions/packaged-artifact.test.ts:27`（明示无凭据无网络）
- 影响：产品体验 1/4/5/6 的"可用"当前只到协议层；对外陈述必须收窄为"合同与受管流程完成"
- 建议：把"一次真实飞书账号 E2E（接入→本人双向验证→断线恢复→主动更新→失败回退→报障修复）"列为**唯一待用户决策的验收下一步**；其余证据边界逐类声明（linus 建议，三方采納）
- 验证：三方独立收敛（donald F2"分散未收敛"并入本条，避免双记）

## 三、minor 发现（6）

| # | 发现 | 位置 | 处置建议 | 验证 |
|---|---|---|---|---|
| m1 | 旧 `ChannelRegistry` 仍为 core 公开导出，生产零消费者（仅测试） | `core/src/channels/index.ts:45`、`registry.ts:30-184` | 移入 test-support 或删除；防第二通道生命周期被误用 | ken+linus 双独立发现 |
| m2 | 平台包仍公开发布无消费者的库入口（`FeishuAdapter`）；devDep 本身合法（构建期制品种子），残留仅在库导出面 | `packages/channels/feishu/src/index.ts:1-2`、`cli/package.json:40` | 移除库导出或标 internal；终版叙事界定"保留的是迁移制品种子" | linus 发现，ken 亲核，donald 复核收窄表述 |
| m3 | `secretRevision` 只写不读，四修订维度中一维无消费者 | `contracts.ts:20-23`（定义）、`channel-configuration.ts:105`（唯一写） | 注明预留用途或删除 | linus 发现，ken 亲核 |
| m4 | RPC `refresh` 能力检查错引 `supports(extensionSetEnabled)` | `server/src/rpc/methods/extensions.ts:17-28` | 按方法分别 supports | ken 发现，donald 亲核 |
| m5 | 需求"无法可靠接入时提供可行替代"只有提示词软承诺，无系统落点 | `builtin-skills.ts:104`、`extension-continuation.ts:33` | 终版如实归类"模型行为软承诺，非系统保证" | linus 发现，ken 亲核 |
| m6 | 复杂度账单：竞态治理集中三大函数（application.commit ~50 行事务、onboarding.reconcile 嵌套、setupChannels ~300 行/5 状态标志）；实例 12 字段/操作 11 字段 | `application.ts:318-370`、`onboarding.ts:69-139`、`channels.ts:130-425` | 已知账单，本轮不改；后续改动前先补拆分检查点 | linus 发现（donald 独立观察到 application.ts:91-108 密度，收敛） |

## 四、六维度终版结论

| 维度 | 结论 |
|---|---|
| 1 需求覆盖 | 主链承诺全部有实现落点；唯一例外＝m5"替代方式"为软承诺（如实归类，非虚账） |
| 2 产品六项 | 六项入口+执行+恢复+交付链全部成立；断链风险仅 M1（恢复链 readiness） |
| 3 架构 3.1–3.7 | 3.1–3.6 落入生产；3.7 运行时清退成立，残留＝M3（文档）+m1/m2（导出面） |
| 4 单元接缝 | 同一底座真实复用，无平行机制；遗留＝m6 复杂度账单 |
| 5 共同要求 | 全部满足（零渠道字段、无第二权威、秘密边界、简洁中文提示词实证）；既有能力不退化按单元证据采信，本轮未重跑 |
| 6 收尾状态 | 双主链已消除；未净点＝m1/m2；"完成"的准确含义见 M4 |

## 五、delta 审计与贡献归因

- **ken-thompson**（unique：M1、M2、m4）：三个代码级发现全部亲核成立，本轮审查的技术含金量主要来源；M1 为唯一运行能力缺陷。
- **linus-torvalds**（unique：m2、m3、m5、m6；补宽 M2 泄漏面；补 M1 可达路径）：复杂度账单与需求诚实性审计不可替代；"三单元完成的真实含义"底线表述被终版全文采纳。
- **donald-knuth**（发起人＋合成；独立发现 M3、M4 的收敛形态；端到端复核 M2、亲核 M1）：需求-产品-架构映射核实、发现合并去重、定级裁决（M3 3:0）。
- 交叉批判修正记录：donald"唯一代码级缺陷"用词被 linus 校正（M2 同为代码级）；donald F2 并入 M4（linus 建议）；m2 表述收窄（donald 复核）。
- **残余异议：无**（M3 定级 3:0 收敛；其余定级全程一致）。

## 六、处置建议（按优先级）

1. **M1、M2**：代码修复，走后续修复流程（本轮审查只读未动代码；修复需用户授权后另行开工，修复后按独立复核闭环）
2. **M3**：文档同步（改 overview，不动代码）
3. **M4**：真实飞书账号 E2E——唯一待用户决策项（涉及真实账号与费用）
4. **m1–m6**：随下次触碰相关文件时顺带处置；m5 需求侧在下一版需求文档中明确归类

---

## 七、被审方（实现方）处置回应与裁决记录（2026-09-22，append-only）

实现方对本报告部分认可并提出修正；发起人（donald-knuth）逐条以证据裁决如下，均经亲核：

- **M1**：双方一致，明确功能缺陷，优先修复。
- **M2 定性修正：接受。** 亲核 `docs/modules/conversation/collaboration.md:15`——产品合同明确不为通信另建对话间授权体系，原报告"绕过会话权限边界"套用了产品选择不建的边界；修正为"**管理投影过度暴露＋无关上下文自动混入**"。保留：合同允许的是用户主导的跨对话访问，原文随状态查询自动混入仍违背该意图，**严重度维持 major**。修复方案从"哈希＋短摘要"改为"公共投影完全不含请求原文，原文仅留 Authority 供接续，需要时走显式导出"（摘要仍可能带出不必要内容——实现方正确指出）。
- **M3/M4**：认可事实；接受"代码缺陷／文档缺口／验证边界分别定级呈现"的表述方式。真实验收按实现方建议**拆为两条**：①平台链路可用性（预制适配 E2E 可证）；②在线模型自主准备、诊断、修包能力（须单独验收）——原报告 M4 建议合并了两者，此为实质改进。
- **m1、m4**：双方一致，随收尾修复。
- **m2、m3 重分类：接受。** m2 归"待确认清理项"（公开合同无价值须先确认，仓库内无消费者不自动成立）；m3 措辞修正——`contracts.ts:112` 校验器确有读取，实义为"无业务决策消费者"，非"只写不读"，归待确认清理项。
- **m5、m6 撤销缺陷定性：接受。** m5 改归体验兑现验证项（并入验证边界，检验失败时是否兑现"如实说明＋提供替代"体验）；m6 保留为观察账单，不构成缺陷——并采纳纪律：**不为通过审查而增加无价值机制**。
- **总体结论修订为实现方版本**："核心架构方向与三单元复用主链成立；当前确认存在恢复缺陷、公共投影过度暴露和文档遗漏。已有工程验证具备价值，但完整产品可用性的验收证据仍不充分。"实现方同时撤回其此前"工程实现和受控流程验收已完成"的表态（M1 新反证重开相关收尾项），符合有证据重开纪律。

**修复范围（用户授权后开工）：优先 M1、M2；收尾 M3＋m1＋m4；m2/m3 待确认；两条真实验收待用户决策。**

## 八、授权修复与本机实测记录（2026-09-22，append-only）

用户已授权彻底修复，并要求先核实本机曾成功使用的安装与账号。实际检查确认：`zz` 链接当前仓库产物，原飞书配置及受保护的账号材料仍在，无需新建测试环境或重新提供凭据。修复及当前证据以[任务书第五节](../tasks/autonomous-channel-integration.md#五宏观审查修复与当前验收2026-09-22)为准。

- M1／M2 已修复并补充回归；M3 已同步 overview；m1／m2 的死代码及无消费者库入口已清理，m3 删除无业务用途的字段，m4 能力检查已更正。m5 通过技能和接续明确失败说明及查证后的替代建议，仍按体验验证项管理；m6 不作无依据拆分。
- 本机实测还发现原审查未覆盖的启动生命周期、共享日志流误归属、分发候选误拒、突发容量与单步预算不匹配、SDK 连接信号格式不匹配等回归，已修复并取得直接回归证据。原第五节“M1 为唯一运行能力缺陷”仅适用于当时审查证据，不能用于描述此次实测后的整体状态。
- 17 包全量构建、定向回归、类型及相关边界检查通过；新飞书制品已复用原账号完成真实连接，用户已在原飞书私聊完成双向收发与本人确认，Authority 操作为 `ready`，RPC 通道为 `connected`。实测暴露的成功回执缺失也已补齐并通过 6 项直接回归。**M4 尚未全部完成**：在线模型实际请求被服务端 HTTP 401 拒绝，模型自主质量仍未验收。未把提示词、mock、进程存活或存在本地凭据作为该项证明。
- 本节是实现方修复记录，不冒充原三方的新一轮独立复核。未进行暂存、提交或推送；保留原审查及第七节的裁决历史。

**同日在线补验更新**：用户授权从本机 OpenCode 复用 DeepSeek 凭据，401 已解除。5 次限量真实模型请求完成源码生成、失败反馈后的修正及模拟协议变更的诊断补丁；代码经真实扩展管理、受管进程、双向确认、重启与失败回退核验。该样例由驱动封装／构建候选和提交操作，不能描述为模型独立完成真实平台接入／修复。初稿缺字段、一次不完整源码及诊断中不准确的表述均保留在任务书的证据边界中；系统以执行结果裁决，不采信自述。

正常对话补验中的取消另暴露子迭代器／控制心跳未清理导致 Host 崩溃，随后重启还暴露零等待锁回收误报 busy；两处已修复并取得直接回归。详细验证及额度记录见任务书第五节。**工程修复可提交；M4 已有真实平台与有限在线样例证据，完整无人辅助的真实平台自主交付质量仍不得声称已证明。**

---

## 九、修复验收与模块关闭判定（2026-09-22，ken-thompson＋donald-knuth 独立复核收敛）

双审查方按 §5.2 对暂存修复（58 文件，+712/−736，基线 `809d566f`，index tree `48ad8e3f`）独立落盘 findings（线程 `collab/threads/20260922-extensions-fix-review-e6a8/`），交换后判定收敛，无分歧：

**模块可以结束（工程与文档层面），附三项放行条件：**

1. **提交拆分**：协作协议升级（§3.13 终局六问，暂存区原有、非修复产物）从模块修复提交中拆出，单独立项评审提交；若用户批准随行，须显式记录"协议变更已知情纳入"，不得静默带过。
2. **S7 入口检查与分布式 golden 基线漂移**登记为独立后续工程项（任务书已如实记录，未掩盖）。
3. **关闭表述以任务书 §五为准**：平台链路＝真实飞书账号证据闭环；在线模型自主能力＝受限样例＋如实质量边界，不得声称"完整无人辅助自主交付已证"。

七项处置（M1/M2/M3/m1/m2/m3/m4）全部核实成立；越界代码改动定性为实机测试发现的真实回归修复（prepared 拒新、延迟意图流归属、正则误判→AST、突发桶背压、LoggerProxy、零等待锁、取消泄漏），各配直接回归，未见行为退化。

---

## 十、后续独立复核重开与同机制收尾（2026-09-23）

第九节是前一快照的历史判定。后续可达反例重开了配置验证轮次、初次受阻修复、并发死亡锁回收，以及正常对话执行的确认／流生命周期；不能用前一判定代替新修复验证。

本次修复：身份或公开配置变化建立独立验证轮次；首装失败可在原实例修正、复用完整投影且不伪造回滚；trial 交接窗口保留原已准入控制请求；配置 publication 精确匹配修订和编辑意图；死亡回收锁采用原子 guard，旧观察不删除新 owner。正常执行透传耐久签发时间，实际 broker 接回唯一 ConfirmationHub，远端耐久确认以消费者身份判定；登记期间取消恰好提交一次终态。回调流在有限容量和语义边界反压，提前退出负责取消并等待生产者，销毁保留已排队终态。Grep 可选字段在 DTO 生产端省略，避免真实工具结果被严格耐久协议拒绝。

同机制复核覆盖配置编辑／更新／修复／取消／停用、嵌套回收竞争、本机及远端 conversation/job/child 确认、正常与异常流关闭，以及三类 presentation 递归成员。新增发现均修复并有直接回归；没有放宽协议、权限或模型预算。详细变更、测试及本轮用量以[任务书第五节](../tasks/autonomous-channel-integration.md#五宏观审查修复与当前验收2026-09-22)为准。

工程关闭与质量边界分开：先前真实飞书双向收发与本人确认的证据有效；本轮正常对话在线样例暴露并推动修复实际缺陷，但没有完成独立候选交付，不计为完整自主接入成功。现有有限生成／修包证据不能升级为无人辅助真实平台自主交付已证。未进行 Git 写操作；协作协议改动、旧 S7 入口和 golden 漂移继续保持前述独立边界。

最终工程验证：本次 CLI 类型检查、17 包构建、安装包仓库外两项回归、秘密边界／运行包导出／npm 结构检查均通过；最终 dist 已恢复本机服务，飞书 connected、原实例与操作保留已验证状态，活跃运行数为 0。已知工程问题可关闭，可以继续下一个需求；该结论不扩张上述模型质量证据边界。

最后一次无讨论史冷读额外重开了“唤醒后、出队前销毁清队列”的竞态；已用条件等待循环修复，保持真实生产者终态。两项确定性探针旧构建失败、修后完整适配器 37 项通过，独立核对后关闭；本次最终 CLI 类型检查与 17 包构建再次通过。此前冷读未通过记录保留，最终未关闭实质发现为 0。

## 十一、提交后配置保存复核收尾（2026-09-23）

`15b91240` 后独立复核重新发现凭据完整快照覆盖并发更新；同机制查齐后，一并修复首次引导的旧配置覆盖、失败编辑污染发布、MCP 配置与凭据分段保存中断后的错配，以及无关凭据轮换误阻断 MCP。配置与秘密由原 owner 统一核对基线、耐久接纳和前滚恢复，没有新建业务权威。

最后冷读另外发现发布捕获旧凭据、标记／公开配置缺少持久屏障两项 P1，均在修复后独立复验关闭。最终 Provider 83 项、Channel 31 项、CLI 入口 48 项通过，CLI 类型、17 包构建及发布边界检查通过；具体机制、故障注入和边界以[任务书收尾记录](../tasks/autonomous-channel-integration.md#提交后复核配置保存与来源配对收尾2026-09-23)为准。

本轮已知工程问题与同机制发现可关闭；没有新的真实平台／在线模型验收，没有将模拟持久恢复当作物理断电验证。构建产物已更新，生产服务未重启；原暂存区未变，没有执行提交或推送。此前各节为对应快照的历史结论，不代替本次证据。
