# 知行开发与审查双人协作

类型：自主执行与独立审查协作文档。

本文只维护双方交接、问题处置和验证证据。当前协作是[外部 APP 自主接入](../tasks/autonomous-channel-integration.md)单元 3 的独立审查、修复与收尾；需求、产品、架构与执行范围以该任务文档为准。历次完成记录只作历史证据。

## 双方与沟通

| 职责 | 对话名称 | 对话 ID |
|---|---|---|
| 执行、问题核实与修复、最终验证、维护本文和任务状态 | zhixing 项目调度者 | `01a02f45-1a72-7690-90e8-9e8def854d08` |
| 当前模块的独立需求、架构与实现审查 | zhixing项目架构师 | `01a06ff2-bf10-7f83-b982-f462c4594ed9` |

双方 host 均为 `local`，共享工作区 `E:\Dev\longxia\zhixing`。通过应用对话消息工具互发交接、任务等待工具接收结果。投递异常先重新取得列表并核对名称、ID、host 与项目；仍异常则报告已核实事实，与用户确认处理办法，不自行改用 CLI、另建对话或绕行，也不把 `notLoaded` 当成对话不存在或用户必须打开的依据。同一对话不得并发投递。消息写清单元、轮次、所审输入、请求和结论。恢复时先读本文与任务当前台账，再读对方最新交接，不依赖聊天记忆。

执行者独占源码、测试和本文写入；审查者只读源码与文档，通过消息返回结论，不同时修改交付物。双方均不操作暂存区、提交历史或远程；已有暂存变更原样保留。审查期间执行者暂停代码修改与构建，避免读到变化中的输入；必要定向测试由双方明确约定一方执行，禁止同时跑同一验证闭包。

## 背景与边界

用户希望发布后由知行按需准备、接入和维护外部能力，减少主产品的平台绑定，但不把维护责任转给用户。本次任务已经形成共用扩展管理与运行底座、Channel 首个应用的设计；核心继续拥有事实、权限、资源、恢复和终态，扩展只实现有限的类型协议，不走“万物皆插件”。

单元 1、2 已交付并提交共用底座、Channel 迁移及发布后自主接入。本轮审查单元 3：共用换版、失败回退、自主修复、控制与回执交接、取消／停用、恢复及必要通知，并按产品六项体验核对整体交付。用户主动更新或报障、有限恢复耗尽后，都应沿同一受控链交付经过验证的连接；不把重启或进程存活当作恢复。只交付 Channel 类型，不新增其他能力家族、插件市场或维护智能体；既有收发、确认、取消、身份、会话、任务与恢复体验不得退化。

先读取：①任务全文，区分整体目标与单元 3 义务；②[核心架构](../../research/design/architecture/overview.md)及 [AE-001](../../research/design/architecture/evolutions/AE-001-companion-intelligence.md)的领域所有权、薄内核、封闭核心和 Host 边界；③[验证耗时复盘](../postmortems/2026-08-06-final-validation-overrun.md)。沿源码和真实调用链审查，不能以测试数量、命名或执行者自述证明正确性。

## 协作顺序与结束条件

1. 执行者先交代背景与当前交付；审查者按上述顺序理解本次需求、设计和单元边界，主动返回“背景已理解”、目标/边界及需澄清事项。此步不启动正式审查或测试。
2. 背景确认后，执行者明确发出审查请求。审查者覆盖用户请求／健康耗尽 → 技能与有限 Run → 原版本资料与候选 → 权威切换 → 类型交接与验证 → 生效／回退 → 结果交付；核对原 Delivery 尝试、未决确认、同账号单消费者、配置修订、取消／停用、关闭、重启和值班迁移。先读代码，必要轻量验证由执行者补证；不运行包全测或重型组合验证。
3. 审查者一次性按根因返回问题清单：位置、生产依据、影响、修正方向和验收条件；无问题直接通过，不为凑轮次硬找问题。已知未覆盖范围与证据缺口必须明示。结果须主动投递给执行者并确认成功；诊断或中断结束后，执行者须明确恢复未完成工作，不将“对话回合结束”当成“审查完成”。
4. 执行者核实并集中修复真实问题，覆盖同根消费者，完成必要直接验证后交回受影响范围。审查者复核变化和直接交界，未受影响结论复用；存在问题继续闭环，代码及必要审查证据无问题后明确给出通过结论。
5. 审查通过后才执行最终验证。先列精确命令、真实失效输入、可复用证据、耗时与截止；同输入已通过的构建和测试不重复，禁止以整包全测代替影响面识别。重型验证串行，构建与依赖其产物的检查不得并发。
6. 最终验证发现问题由执行者定位、修复并复验受影响闭包；若改变已审查的生产逻辑或提示含义，回交该变化及直接交界复核，不重审未受影响范围。不得借此扩单元；若确需改变架构/产品范围，报告用户裁决。
7. 独立审查通过、范围内真实问题关闭且必要验证通过，才登记单元收尾。必须区分真实管理／进程／权威链验证与平台网络夹具；不虚报真实账号连通，不因没有外部账号而放弃可完成的源码与合同核实。审查收尾不自行提交、推送、发布或启动后续单元。

验证纪律参考[开发工作台](../../research/design/workbench/unit-development-workbench.md)、[审查工作台](../../research/design/workbench/unit-review-workbench.md)、[验证手册](../../research/design/workbench/verification-runbook.md)和[验证耗时复盘](../postmortems/2026-08-06-final-validation-overrun.md)。本次双方分工与最终验证失败处置按以上用户约定执行，不套用额外角色或重复审批流程。

## 当前工作：共用换版、自主修复与整体交付（2026-09-20）

- 状态：单元 3 独立复核及最终验收通过，六根因全部关闭，协作收尾完成；不启动新任务，不进行 Git 写操作。
- 输入：HEAD `01327918` 之后的全部单元 3 变更，含暂存与未暂存差异；现有 27 文件均已由外部暂存，双方不改变索引。
- 交付标准：主动更新与自动／用户报障修复复用唯一管理事实、固定候选和已有有预算 Run；保留可修复源码。按实例交接，准备时旧版可用，新版实际验证后才接正常业务；失败／取消恢复原绑定，停用及迟到结果栅栏耐久有效，不重启整个知行。
- 正确性标准：不连带升级其他实例、不并行消费同一账号、不倒退历史或消费水位；已发效果按原尝试结算，未知效果不盲重放，未发义务仍归 Delivery。确认／取消不等待原业务 Run；重启和值班接管具备原绑定、制品及本机材料，不搬运秘密。当前渠道维护自身不自锁，结果可从其他既有入口取得；无可达入口保留待告知结果。
- 架构与提示标准：共用管理／运行不依赖 Channel 字段，类型绑定承担兼容、配置和实际验证；核心封闭、唯一权威、统一安全链，不新造队列、调度器或维护智能体。修复不换账号、扩权、增功能或清历史。新增及修改提示为简洁中文，不含作者心理活动或交接噪音。
- 主要入口：`core/src/extensions/{application,onboarding,runtime,process,contracts}.ts`、`core/src/channels/extension.ts`、内置技能；`cli/src/runtime/extensions/`、`serve/{channels,extension-tools,extension-continuation,extension-management-mesh,access-surfaces,command}.ts`；`server/src/channels/inbound-router.ts`、现有 Delivery／确认消费者及[安装指引](../modules/extensions/authoring.md)。
- 已有证据：开发期定向回归涵盖真实 Authority、独立进程、换版／回退／修包、取消／停用与重启、凭据续接、故障合并、远端分块、通知与控制入口。最终 17 包构建、CLI 类型、秘密边界及新产物迁移／关闭两例通过；构建原始输出在 `tmp/extension-unit3/build-final.log`，其他命令结果在执行任务上一轮工具记录。夹具驱动候选与平台行为，不声称真实账号或在线模型修复质量已验证。
- 验证安排：先独立源码审查；需要补证时只执行能裁决真实疑点的窄检查，明确执行方。复用未失效输入的证据；修复复核通过后按实际变更确定最终检查，不预先重复全量构建或全包测试。

### 本轮记录

| 阶段 | 结果 |
|---|---|
| 背景交接 | 原伙伴已回读需求、六项体验、架构及单元 3，确认唯一权威、类型边界、换版／修复和完整交付标准，无澄清项；正式派发 R1 |
| R1 定向补证 | 六条真实路径已复现：HTTP 交接期返回 503、取消未到消费者；维护 ready 的真实 Conversation 入站丢失 APP 回复目标；候选通过验证后改变群聊归组；接管只检查可执行制品而漏当前源码归档；新报障被已受阻修复操作吞掉；回退事实提交与进程退役间向未验证候选放行业务。正式源码未动，隔离配置只加入夹具和时序屏障；原始命令／退出码／日志为 `tmp/communication-unit2-review/extension-unit3-r1-{probes,confirmed,rollback-confirmed}.json/.log`，保护断言失败是复现证据，不算通过。首版 HTTP 等待切换完成、首版来源读取错层及单例启动等待过短已纠正，不作缺陷依据 |
| R1 完整结论 | 不通过，UNIT3-R1-01～06 六根因（3 项 P1、3 项 P2）；已覆盖六项体验、架构 3.1—3.7、全部 25 个包变更文件及直接交界，无其他已确认根因。源码／HEAD／索引首尾一致，解除冻结集中修复 |
| R2 修复 | 类型桥绑定原代际传输，HTTP 控制不依赖已退役的普通调用入口；原请求来源与回复目标不删除，其他入口通知仅补充。群聊归组进入类型声明、换版兼容检查和启动一致性检查；接管同时核对当前及回退版本源码归档。明确新请求可替代已受阻诊断，自动故障仍合并、原请求重放幂等。普通发送与新 challenge 同时核对启用、准入和实际代际，已发回执仍原尝试排空 |
| R2 直接验证 | Core 生命周期／接续 31 项、CLI 接入／工具 30 项、真实 Conversation 来源 2 项，共 63 项通过；覆盖 HTTP 交接、取消／停用提交窗口、声明及运行时归组漂移、ready／verifying 归档、明确重试和原 APP 准备／完成回复目标。Core 类型及差异检查通过。原始命令／结果见 `tmp/communication-unit2-review/extension-unit3-r2-{core,channel,origin,core-types-fixed}.json/.log`（30.071／126.822／21.035／12.348 秒）；错误的根级 TypeScript 入口未运行检查，改用包内入口后通过。无重型构建、无真实账号或在线模型 |

R2 输入基线：27 个 `packages/` 文件，按 R1 算法 SHA-256 `5393f3257fc75f0b9cb75438ee949dce27e3ee95b188e01cc5e84562c223d18c`；HEAD 与索引仍为 R1 值。本轮修复涉及 13 个包文件和安装指引，协作记录不计入源码指纹。

R2 独立结论：通过。伙伴逐项确认六根因关闭，检查直接 Conversation／Delivery／确认、类型归组及启动、回退和自动诊断消费者，无新增可达失败；完整核实 63 项原始结果与 Core 类型证据。源码、HEAD 和索引首尾一致。源码复核通过不代替新构建和安装产物验收；解除冻结，仅执行以下终验。

最终验证计划（独立复核通过后执行）：Core 合同和进程输入已变，执行一次 `pnpm build`（命令局部 Node 堆 8 GB，近期约 192 秒，进程树截止 600 秒），随后 CLI `tsc -p packages/cli/tsconfig.json --noEmit`（约 12 秒，截止 90 秒）、秘密边界检查（约 1 秒，截止 30 秒）。新产物只补仓库外安装资料、HTTP 交接控制及既有迁移／最终关闭四例，Vitest 单 worker、显式 20 秒单例上限、约 35 秒、截止 90 秒，不使用源码替换配置；原 63 项及未失效跨模块证据复用。预计约 4 分钟，命令和结果仍用 `run-check.mjs` 留存；超时按失效原因裁决，不盲重跑。

最终验证结果：17 包构建通过（180.576 秒）；新 DTS 的 CLI 类型检查通过（20.187 秒）；秘密边界通过（1.410 秒）；无源码替换配置的安装资料／HTTP 交接／原渠道迁移／最终关闭四例通过（33.706 秒，45 项跳过不计入）。原始命令、退出码和日志为 `tmp/communication-unit2-review/extension-unit3-final-{build,types,secrets,artifacts}.json/.log`，全部退出 0、未超时；无终验代码修复。Core 公开 DTS 已包含 `bindTransport`；安装包指引与源文件 SHA-256 同为 `c3e5239e8424e6deca4f9929defd0d010b3d578177718fc72a017fe12fed29a5`，schema 同为 `bcaa74991b3cea4b63bdc073c03437748d540a6453bee993b814588c3629db37`。27 文件指纹、HEAD 与索引均与 R2 一致，命令局部构建环境已恢复。伙伴已独立核实全部终验原始材料、安装产物与相同指纹，明确最终验收通过；范围内问题全部关闭，单元 3 及本轮协作完成。

开发期原始工具输出已从本地 `rollout-2026-09-19T21-42-38-01a02f45-1a72-7690-90e8-9e8def854d08_01a0b9e7-9a5d-7550-b014-059c1c99b2a6.jsonl` 交审查方核实：Core 行 4883／5023、CLI 行 4859／4898／5032／5047、Server 行 4975、最终构建／秘密边界／fresh dist 行 5141／5148／5157。任务读取 API 不返回这些原始输出，不以摘要替代；会话原文不复制进仓库。

R1 输入基线：25 个 `packages/` 变更文件，按排序路径、NUL、原始字节、NUL 计算 SHA-256 为 `ae701035642b70b99560766729a996a519c46164903d8055af096d70b82c1527`；HEAD `01327918c3269269294ccf1173a132e6357defa1`，`git ls-files --stage` 摘要 `534c862221ee3249a06cd0a9351a3aad8daf45321d3da9a2aeb20a80a67d6299`。另审任务文档和安装指引；协作记录不计入源码指纹。

---

## 已完成交接：发布后自主准备、接入与接续（2026-09-20）

- 状态：单元 2 独立审查及最终验收通过，三根因全部关闭，收尾完成；本轮协作停止，不启动单元 3，不进行 Git 写操作。
- 输入：HEAD `312bcce6` 之后的全部单元 2 工作区变更，含暂存、未暂存及新增文件；不只审未暂存差异。双方保留索引，不进行 Git 写操作。
- 交付标准：仅凭正式安装产物可获取现成适配或自主准备适配，源码、固定依赖及构建说明随候选保留；安装／试运行前有耐久操作与必要确认。凭据仅在目标设备安全入口填写，模型不可读写生效资料或宣告已验证。本人身份、回复目标、真实双向收发及确认通过后才开放业务。
- 接续标准：原请求、操作身份、目标实例、候选与结果去向耐久保存；等待不占 Run，录入／确认／执行结束及重启后继续同一请求。重复不重复安装，取消／停用和迟到结果有栅栏；原绑定、其他实例及已有任务不受破坏。主／工作、本地／远端均走相同管理语义，不引入主对话限制。
- 架构与提示标准：共用生命周期仍只有一个 Authority owner，Channel 业务证据只经类型绑定消费；Host 静态装配、Kernel 无新业务分支，不另造队列或调度器。新增或修改提示使用简洁中文，只包含模型需要的身份、目标、约束和行动信息，不混入作者心理活动、交接过程或无关规则。
- 主要入口：`core/src/extensions/{application,onboarding,candidate}.ts`、内置技能；`cli/src/serve/extension-{tools,continuation,management-mesh}.ts`、`channels.ts` 及主／远端组合根；`runtime/extensions/channel-verification.ts`、配置编辑器与配置命令、`server/src/rpc/methods/extensions.ts`；CLI 安装资料构建及[编写指引](../modules/extensions/authoring.md)。
- 已有证据：开发期 Core 接入／技能 13 项、CLI 相关去重 70 项、包结构 4 项及 CLI／Server 类型、秘密边界、17 包构建通过。原始日志可从执行任务工具记录及系统临时目录 `zhixing-unit2-{regression,acceptance,build-final,cli-final}.log` 核实。平台与网络为夹具；真实 Authority、子进程及仓库外安装资料不等同于真实账号或在线模型验证。
- 验证安排：先源码审查和必要直接补证；已通过且输入未变的证据复用。审查修复后，按实际失效闭包安排定向测试、类型检查与必要构建，不预先跑全包测试，不启动单元 3。

### 本轮记录

| 阶段 | 结果 |
|---|---|
| 背景交接 | 原伙伴已回读需求／产品／架构、单元 2 义务及验证经验，主动确认完整接入、耐久接续、真实验证、唯一责任与禁止扩面，无澄清项；正式派发 R1 |
| R1 定向补证 | 账号 A→B：真实 Authority／SecretStore／独立进程两例确认旧 challenge 或已完成 admission 可跨账号开放业务，`extension-unit2-r1-identity.log/.json`，退出 1、15.338 秒。版本字段：真实目录投影→配置校验确认新候选字段覆盖旧实例，`extension-unit2-r1-catalog.log/.json`，退出 1、5.425 秒。准备失败：真实 ConversationProtocolRuntime／Authority／管理操作确认 Run 已 failed、成功提交钩子未触发，操作仍 preparing，手工访问后才 blocked，`extension-unit2-r1-terminal-confirmed.log/.json`，退出 1、15.961 秒。均为保护断言复现，非通过证据；隔离配置位于 `tmp/communication-unit2-review/`，正式输入未改 |
| R1 完整结论 | 不通过，共三根因：01 验证证据／准入未绑定账号与配置身份；02 不同实例版本串用声明，影响字段、校验及敏感展示；03 准备失败／取消和试运行重试耗尽未自动收敛操作并回告原请求。正常成功提交顺序已排除，无需修改。44 个包文件首尾一致，已有检查原始证据已由伙伴核实；协作文档被外部暂存，双方未写索引。源码解冻，集中修复三根因 |
| R2 修复 | 配置身份变更原子撤销旧准入和证明，原操作重入验证，迟到入站再验代际与准入，验证发送幂等键随新证明换代；同账号正常重启保留证明。字段、输入和展示遮罩、校验与入口标签均按实例精确声明。准备接续改由既有 Run 终态通知触发，主／工作、本地／远端装配统一；试运行耗尽既有有限重试时原子标记 blocked 并回告，不新增轮询或重置重试预算。明确重新启用取消后的实例需重新验证，不复用旧确认 |
| R2 直接证据 | Core 接入 8 项、运行代际 13 项、CLI 实际 Authority／独立进程与目录投影 9 项、真实 Conversation 准备失败／取消 2 项通过；最后收窄幂等键后账号变更 2 项复验通过（不重复计数）。CLI／Core 类型检查与差异检查通过。日志 `tmp/communication-unit2-review/extension-unit2-r2-{core,runtime,onboarding,terminal-fixed,identity-final,types-fixed,core-types}.log/.json`；32 项去重。CLI 扩展窄导出指向本轮源码，其余未改包复用构建产物；无真实外部账号和在线模型。首轮终态夹具漏必需成功钩子，补空钩子后通过；不存在的根级工具路径改为包内入口，不计作产品失败或通过证据 |

R2 输入基线：47 个 `packages/` 变更文件，SHA-256 `9e6c59d146cb60086a9a53f73e21bfdf69632b4ad2bab0112e1d87f615cdaf15`（与 R1 同算法）；HEAD 不变，`git ls-files --stage` 摘要 `c3924e40fbb2a3b1c10a542233dd8419d137c76e9ca813ef34c1b237908b8df1`。

R2 独立结论：通过。伙伴核对 19 个修复文件及直接交界，三根因全部关闭；确认新启用意图必须重验、Run 终态通知在事实提交后触发、远端经既有 Anchor 管理路由接续、耗尽重试不因查询复活。已读取全部上述原始日志，47 文件／HEAD／索引首尾一致，无范围内待核疑点或阻止最终构建的证据缺口；解除冻结进入终验，未宣称实际账号联网或在线模型验证。

最终验证计划（独立通过后执行）：`pnpm build`，本轮 Core 输入变化使旧构建失效，命令局部 Node 堆 8 GB，近期约 192 秒、硬截止 600 秒；随后 `node scripts/check-secret-boundaries.mjs`（约 1 秒、截止 30 秒）、CLI fresh dist 类型检查（约 13 秒、截止 90 秒）、`installed-kit.test.ts` 与账号切换两例（包内 Vitest 单 worker，不使用源码替换配置，约 20 秒、截止 75 秒）。预计约 4 分钟。未改动的包结构、服务器类型、其余直接回归复用；不启动组合 baseline 或全包测试。日志和退出码仍由 `run-check.mjs` 留存，超时只终止该命令子树，先归因再裁决，不盲重跑。

最终验证结果：17 包 `pnpm build` 通过（199.216 秒）；秘密边界通过（0.548 秒）；基于新构建产物的 CLI 类型检查通过（10.395 秒）；无源码替换配置的仓库外安装资料 1 项和账号切换 2 项通过（19.868 秒，另外 7 项跳过）。精确命令、退出码和时长见 `tmp/communication-unit2-review/extension-unit2-final-{build,secrets,types,artifact}.json`，同名 `.log` 保存原始输出；均退出 0、未超时。构建堆设置已恢复，无后续源码修复；47 文件指纹、HEAD 和索引与 R2 一致，差异检查通过。伙伴已独立读取上述日志、复取相同指纹并确认最终验收通过，无范围内未关闭事项；单元 2 完成，本轮协作结束。

R1 输入基线：44 个 `packages/` 变更文件，按排序路径、NUL、原始字节、NUL 计算 SHA-256 为 `c5b0d507e622b815d8cd99602a645221f4f35795deff8622f6b3533f416495d3`；HEAD `312bcce644d57eb494ef8b1a7718140eecba91c1`，`git ls-files --stage` 摘要 `733de476dc8a1bd1354dc44441914e18c4517edce903278ba88553c07d61a7d3`。另审任务状态、编写指引和文档入口；协作台账不计入源码指纹。

---

## 已完成交接：共用扩展底座与 Channel 迁移（2026-09-20）

- 状态：2026-09-20，独立审查及最终验收通过，单元 1 收尾完成，协作停止；不启动单元 2、3，不执行 Git 写操作。
- 输入：当前全部单元 1 代码、测试、声明、构建配置、锁文件与直接架构门禁变更；包含新增文件。`AGENTS.md` 是既有用户规则变更，不属于本次代码修复；所有已有暂存状态保留。
- 核心标准：管理事实只有一个 owner；共用管理／运行层无 Channel 业务或平台分支；外部进程不取得核心对象或任意注册权。入站耐久接纳后才 ACK，出站尝试／幂等／回执与未知效果仍由 Delivery 裁决，确认与取消不自锁。配置部分保存不破坏旧绑定，单实例启停不影响其他实例，停用和所有权变化能拦截迟到结果；正式安装形态可运行固定飞书制品，无双生产链。
- 生产入口：`packages/core/src/extensions/`、`core/src/channels/extension*.ts`、`cli/src/runtime/extensions/`、`cli/src/serve/channels.ts` 及 access-surfaces／command／setup-delivery；`server/src/rpc/methods/extensions.ts`、入站路由、Delivery effect／outbox；飞书外置入口与两端构建配置。
- 已有证据：开发期全量构建、CLI 类型检查、核心 41／CLI 41／飞书 29／服务器入站 74／管理 RPC 3 项定向检查通过；另有两个边界守卫、四项包结构、秘密边界、包导出及独立制品加载证据。复用未失效输入，不把平台夹具当在线飞书验证。总 S7 因既存旧文档路径缺失未通过，其影响须核实，不冒称全门禁通过。
- 分工：审查方只读，主动回传完整问题及复核结果；执行方独占修复、验证和文档状态。新增事项须对应本单元合同或当前可达失败，不扩建通用框架，不提前做单元 2、3。

### 本轮记录

| 阶段 | 结果 |
|---|---|
| 背景交接 | 原伙伴已完整回读需求／产品／架构与单元划分，确认单元 1 范围、禁止扩面、唯一权威与证据边界，无澄清项；正式派发 R1 |
| R1 定向补证 | 项目外隔离脚本、真实 Authority 与独立子进程复现：快速停用/启用、suspend/resume 均在旧进程停止前启动新进程；管理 API 停用后，仅更新凭据再应用配置会重新启用，配置界面仍显示“已启用”。命令 `node --import=tsx/esm …/zhixing-unit1-review-978b064aa31348dfa608bb486e06bac9/probe.mts`，chunk `6916fd`，退出 0、8.90 秒；未连接平台、未改正式输入、未重跑整包测试。完整 R1 尚未结束 |
| R1 完整结论 | 暂不通过：UNIT1-R1-01 代际停止屏障／迟到激活；02 配置与管理启停双源及保存后恢复交接；03 拒新提前物理关闭、截断在途回执。第二组真实进程补证 chunk `438e0d`、退出 0、4.87 秒，确认在途成功回执变未知，以及保存后重启仍用旧投影；无其他已确认根因。原 188 项及构建原始结果已由伙伴核实 |
| 集中修复 | 保留每实例停止屏障、激活后重验连接意图；配置界面消费管理快照，字段编辑与显式启停分开，完整来源发布材料仅留本机 SecretStore，按独立启停修订防迟到覆盖，Authority 仍为唯一生效事实。保存中断保留旧配对并显示待应用，完整发布可重启接续；无发布材料的变更不自动混配。拒新暂停激活、保留既有尝试与控制回路，计划迁移／卸载／移除先结算后断开 |
| R2 直接证据 | Core 真实进程 10 项通过（session `11508`，16.98 秒）；CLI 受影响直接闭包 84 项去重通过，其中配置旧夹具漏 `credentials` 已补齐后与 Channel 15 项复验通过（`82012`，26.06 秒）。涵盖真实配置命令→发布→双源保存→RPC 失败→重启消费；700ms 在途回执保留，新直接发送被拒，终止关闭的未知效果不冒充成功。CLI 连源码类型检查通过（`94573`）；两项 Channel／值班边界守卫通过（chunk `7ad30e`，3.14 秒）。CLI 测试仅把 extension 窄导出指向本轮源码，未用旧 dist 冒充新实现；最终构建留待独立复核后 |
| R2 隔离补证 | 真实 Authority／运行组件／独立进程：并发 begin 的 CAS 失败留下 `enabled=true, phase=starting, slots=0`；pause 时仍在准备的代际随后启动。`probe3.mts`，chunk `fc89ff`、退出 0、4.03 秒。真实配置命令→发布→双源保存→apply：清空必填凭据并显式停用后仍 `enabled=true, connected`，错误为必填字段缺失；隔离测试 `tmp/communication-unit2-review/unit1-disable-probe.test.ts`，chunk `d7b783`、退出 1、9.45 秒。三项均为缺陷复现，不算通过；正式输入保持冻结 |
| R2 完整结论 | 三根因尚有直接交界遗漏：最新启动决定未收敛、暂停放过未激活代际；无效候选挡住显式停用，RPC 丢失后原样保存不重试；Host／设备移除在 Delivery 结算前回读 Channel 已关闭，物理状态又与配置问题混用。未发现三根因外的新确认问题，解冻集中修复 |
| R3 修复 | CAS 冲突由仍有效的最新决定重读，暂停退休未激活 slot，运行观察提交后复验 abort；显式停用独立于候选有效性，保留旧完整配对及待处理提示。配置入口识别本机未消费发布，原样保存重试原意图修订，修改字段也不丢未消费意图。Host／当前设备移除／普通移除均先结算 Delivery，再物理关闭及原有回读；物理状态与配置问题分别投影，凭据轮换按实例身份且无待应用问题才验证，不借旧连接证明新凭据 |
| R3 证据 | Core 13、Channel 23、HostStop 15、配置命令 16、凭据轮换 6 项去重通过；CLI 连源码类型检查和两个边界守卫通过。Channel 的 3 个收尾用例从 `command.ts` AST 提取原生产捕获／settle／readBack／拒新绑定执行，实际 Authority、独立进程及 700ms 回执；其他空闲 owner 与 Delivery 结算端口为夹具，不冒充整个服务端到端。首轮发现配置问题未刷新状态投影，已修复并通过；HostStop 测试夹具误用另一制品目录，修正后该单例通过，未改生产存储合同。并行类型检查期间一次 5 秒测试预算超时，串行 15 秒单例预算下通过；没有放宽生产超时 |
| R3 独立结论 | 仅剩 02 同根 P1：无效候选停用复用旧绑定后，遇 CAS 冲突会错误回收仍被权威引用的旧投影。隔离真实 Authority／加密 SecretStore 复现，日志 `channel-unit1-r3-fallback-probe`，退出 1、11.211 秒。其余三根因与直接消费者均已关闭，无其他待核疑点；伙伴已读取上述 73 项有效结果及类型／守卫原始日志 |
| R4 修复与证据 | CAS 冲突只回收投影修订不同于当前绑定的新候选，不删除借用的已提交投影。正式两例经真实 ProductAPI 插入并发 stop，分别确认旧绑定仍可读取、无效新来源不破坏旧秘密，以及新建失败候选确实被回收；2 项通过、23 项跳过不重跑（17.454 秒、退出 0），日志 `tmp/communication-unit2-review/channel-unit1-r4-cleanup.log/.json`。未失效结论与证据复用 |
| R4 独立结论 | 通过。伙伴核实清理条件与投影所有权一致、新旧候选两条正式 ProductAPI 回归有效，73 个文件／HEAD／索引首尾一致；三根因全部关闭，无范围内剩余已知 P0/P1 或待核疑点。批准进入既定最终构建和窄检查，不把代码审查通过冒充产物验收完成 |

R4 输入：仍为 73 个非文档变更文件，SHA-256 `f4075e09bc23bbb4424fd86bc2b2f48f2804970213b7420d0962d5a23547a36e`，仅 `channels.ts` 的清理条件及其直接测试较 R3 改变；HEAD／索引不变，差异检查通过。

### 最终验证（2026-09-20）

- `pnpm build` 17 包通过，191.658 秒、退出 0、未超时。构建后已恢复命令局部 Node 堆设置。
- 静态秘密边界首轮发现 Channel 配置使用完整凭据类型；改为现有 `ChannelCredentialProjection`，配置命令仅传渠道字段，未修改安全守卫或白名单。随后 CLI 重建通过（2.458 秒），上游输入未变，不重复全量构建。
- fresh dist CLI 类型检查通过（15.217 秒）；独立复制的飞书制品协议加载 1 项、包结构 4 项、原秘密边界检查通过；真实配置命令保存／恢复单例在 fresh dist 上通过（13.875 秒）。复用其余已核实直接回归，不把跳过项计为新通过。
- 原始命令、退出码和日志：`tmp/communication-unit2-review/channel-unit1-final-{build,cli-build,types,artifact,package-structure,secrets-fixed,config-projection}.json/.log`。独立加载不触及真实账号或平台网络，生产回调绑定仍为隔离真实组件证据。
- 最终 73 文件指纹 `8e26b27c280191fd26217368220bf71cc16417804a947c21326b869297d5e1df`；HEAD 与外部暂存后的 index 不变，差异检查通过。伙伴已独立读取终验原始日志，并以内存逆转比对确认 R4 后仅两处凭据投影收窄；最终验收通过，范围内已报问题全部关闭。
- 构建保留四项既有 DTS 重导出警告（`AdvancementSnapshot`、`TranscriptRunRecord`、`SkillModeDto`、`SkillUsageRecord`），对应源文件相对 HEAD 未改；不宣称零警告，也不扩面修理。总 S7 的既存旧文档缺失边界不变。本轮不改暂存区、提交或远程，单元 2、3 尚未开始。

R3 输入：73 个非文档变更文件，SHA-256 `2df00e69b6d2b7e644a318c2bd8cb036b64f47705d97c6db5291e0545f9b9584`；HEAD 与外部暂存后的索引保持不变。日志及命令／退出码在 `tmp/communication-unit2-review/channel-unit1-r3-* .log/.json`（文件名不含空格）：core、cli、final-channels、host-binding、config、status-consumer、final-types、guards；有效成功证据按上述逐项组合，不把失败运行说成整轮通过。复核仅三根因的实际变化及直接消费者，其余结论复用。

R2 输入：70 个非文档变更文件，SHA-256 `2ed2ae4a0aeea53c8e045fe77a4335330b7488c0a9d549c21c082cf55dd53c3a`。HEAD 仍为 R1；索引为外部暂存后的 `fba0a1da9ae057eccd483f6cb8caa079d5a3d0d6957ba73e7dd32113ddeaf567`，本轮未写 Git。仅回交三项根因及直接交界，其余 R1 结论复用。

最终验证计划（独立复核通过后执行）：上游 core 变化使旧构建失效，串行运行一次 `pnpm build`，命令局部 8GB Node 堆，预计 4–6 分钟、进程树硬截止 10 分钟并保留日志／退出码；随后以 fresh dist 做 CLI 类型检查和独立制品加载单例、包结构／秘密边界检查。复用本轮定向回归和仍有效的原入站、平台适配、RPC 等证据，不运行整包全测或总 S7。若有真实失败，仅修复与复验失效闭包。

R1 冻结输入：64 个代码／测试／构建／锁文件及直接门禁变更（不含 `AGENTS.md` 和过程文档），按排序路径、NUL、原始字节（删除项为 `DELETED`）、NUL 计算 SHA-256 为 `d152563da57851c4f6f6e6d9cf06c38dc2f832b4d8893b76225f6ba7fb035119`。HEAD `f2465d92128258dab30233acf58ce48e9b2edee4`，`git ls-files --stage` 输出 SHA-256 为 `b8521e2b10bda767eeeada32d0fb2a58036d322651b09c016811d3f411b31aef`；现有实现已由外部暂存，本轮双方保留，不进行 Git 写操作。

---

## 已完成交接：跨平台交付修复（2026-09-19）

- 状态：R2 独立复核通过，两项问题关闭，本机审查修复与收尾完成。真实异平台验收继续保留，不宣告跨平台总任务完成。
- 输入：当前工作区跨平台改动（含未跟踪新文件），不含既有暂存 README、技能和 AGENTS 改动；保留所有索引状态。
- 重点：五目标单源声明与真实支持边界；原生产物完整性、打包允许集合及发布门禁；Unix 目录句柄与重复枚举；macOS 卸载当前 job 后的注册/重启/维护/卸载；现有 Windows 兼容、密钥与工具的直接交界。CI 与文档必须如实区分待验收和已支持证据。
- 已有证据：56 项定向检查、17 包构建、Windows 16 个公开 tarball 安装消费通过，详见下方本轮记录。未改变输入时复用；仅失效闭包补测，必要最终构建放在独立复核之后。
- 分工：审查方只读返回问题与关闭条件；执行方核实、修复、验证及维护两份文档。真实其他平台验收不在本机虚报完成。

### 本轮记录

| 阶段 | 结果 |
|---|---|
| 背景交接 | 原审查方已读任务、架构和测试经验，主动确认范围与分工，无澄清项；正式派发 R1 |
| R1 结论 | 暂不通过，2 项 P2：CP-R1-01 服务测试使用当前宿主且缺少 launchctl 回读，阻断新 macOS CI；CP-R1-02 首发清单仍以 Windows-only/不建 CI 为当前范围。未发现新生产代码 P0/P1；真实异平台验收继续待办 |
| 修复 | 服务安装/启动/停用用例显式覆盖 win32/darwin/linux，分别模拟注册、加载、启用及运行状态，macOS bootstrap 的 RunAtLoad 不再套用 Windows 安装后未运行断言；不跳过平台、不改生产语义。首发当前清单同步五目标与手动验证 CI，历史 P01～P08 不变 |
| 直接验证 | `pnpm --filter @zhixing/cli exec vitest run src/serve/managed-service.test.ts --maxWorkers=1 --minWorkers=1`：33/33 通过，9.87 秒，session 85731，退出 0。差异检查通过。本轮只改测试和文档，原 17 包构建、16 包安装消费及其余 25 项直接证据仍有效，不重复重测 |
| R2／本机收尾 | 独立复核通过，CP-R1-01/02 全部关闭，无新增问题；审查方已核实 33 项原始输出及未失效证据，58 项去重直接测试、已有 17 包构建与 16 包消费成立。本次交接/任务/首发记录的 20 个本地链接有效。只更新收尾状态，不重复构建或装包；后续真实平台验收归首次发布清单。临时任务文档按用户随后要求删除，现行引用已更新 |

R2 冻结输入：27 个代码/测试/交付文档/锁文件（含新增工作流和原生产物模块/测试，不含两份过程文档及无关暂存内容），按排序路径、NUL、原始字节、NUL 计算 SHA-256 为 `18d51aa3ec2169d5dde21fdb927842799157da68564fab708e21c2cd6f37714d`。HEAD `70b0cd3b42ed3152e1b1e398cfabdc15d86b2ab6`、索引摘要 `ff04348b6a25201b31d085644e86c8d435fd0d784aa9fcb98dffea52078391e8` 首尾未变；双方未进行 Git 写操作。

证据核对：审查方已从执行任务的 2026-09-19 原始工具记录核实开发期测试、构建与装包输出（工具返回行 557、573、705、690、771；构建 session 76813，均对应退出 0）。记录定位为本地 `rollout-2026-09-19T17-12-08-01a02f45-1a72-7690-90e8-9e8def854d08_01a0b8ef-f3f7-7c32-a831-64bb81813e2e.jsonl`；不将整份会话复制入仓库。原 56 项去重证据中的服务 31 项由本轮 33 项替代，总计 58 项；跳过及重复项不计。

---

## 已完成交接：对话间通信整体审查

- 状态：2026-09-16，整体 R3 独立复核、最终版本检查与 17 包构建均通过，模块整体审查及收尾完成；范围内无未解决问题，未扩张需求、未操作 Git。
- 输入：`3c145e7166931439af18392f96adbb0cbb8b1bb5..f4ec0b19`，包含单元 1 `a15cfcef` 与单元 2 `f4ec0b19`。开始时工作区干净；本轮不进行 Git 写操作。
- 输入校验：整体 64 个变更 TS 文件按排序路径、NUL、文件原始字节、NUL 的 SHA-256 为 `aef9b1488cd69390be0bf6742b8799fe7ca481365bd6f0bb4d69a3c4eb90c716`；HEAD 为 `f4ec0b1960e1b3cdaadc462a9ad6fa9c290d99d4`，源码无未提交变更，索引为空。
- 目标：证明独立对话的发现、读取、发送、观察和按需回信在真实装配中闭合，空闲/忙时输入、逐条来源、停止与恢复均兑现封版合同；不增加固定协作流程、等待前提、共享额度或额外授权体系。
- 审查路径：技能与模型工具 → Conversation 应用及真实来源 → 当前权威路由 → Owner／Journal 接纳、去重与消费 → 本地／远端 Kernel Turn 边界 → 正式记录、事件、读取与 CLI 介入。额外核对初始化/恢复、关闭竞态及用户输入、工作场景、委托、Job、确认的直接交界。
- 验证策略：先独立静态审查；疑点由执行者做约定的直接验证。同输入的已通过测试、类型检查和 17 包构建复用；仅修复导致失效的闭包才重验，独立复核通过后执行必要最终构建。
- 完成：背景确认 → 整体 R1 → 真实问题修复及 R2／R3 复核 → 最终证据确认 → 收尾。

### 整体审查记录

- 背景确认：审查方已回读需求、产品与架构、方向及测试经验，确认本轮覆盖整个模块，无澄清项；已正式派发整体 R1，双方不运行重型验证。
- 执行方核对：技能与 main/work 工具装配、真实来源、接收方 Journal、两种 Turn 边界、正式记录及 Provider 投影已逐链核对；已核对历史恢复、停止未消费、重试、分页、UI 混合来源与漏流、用户停止入口、本地／远端装配及 Job 能力隔离，未确认新增缺陷。独立结论尚待返回，不据此提前宣告通过。
- 证据复用：已读取 `tmp/communication-unit2-review/` 中 `r3-terminal-regressions`（75 项）、`r3-production-types`、`final-version`、`final-build` 的原始结果；另 42 项未失效直接回归及单元 1 正常/终止/恢复证据见下方记录。当前源码未改，不重复构建或全测；测试包含确定性模型与同机 Mesh，不代表在线模型或实体多设备验证。
- R1 定向补证：审查方发现重连时自动通信缺少权威补显。执行方按约定以真实 LocalOwner → RPC 及真实 Controller 隔离验证，2 项均复现：本机订阅成功却不补已提交 Final；已见通信运行开始、断线期间失败后重连，不查询无本地 waiter 的 Run 状态。证据 `tmp/communication-unit2-review/overall-reconnect-confirmed.json/.log`，28.802 秒、退出 1；运行只执行一次，问题在呈现恢复而非消息丢失。首版探针误用 RPC 返回 envelope 的断言已纠正，旧日志不作缺陷依据。正式源码仍冻结，待完整 R1 结论后集中修复。
- R1 完整结论：暂不通过，仅上述 `OVERALL-R1-01`／P2；独立审查已覆盖整个模块及直接交界，未发现第二根因，未受影响结论复用。两条路径包括本机 Final 重放，以及各部署下无 waiter 自动运行的异常终态补读；不能只核对断线前已观察到的运行。
- 集中修复：本机订阅先登记 observer 再按 `afterCommitRevision` 从既有 Owner `finalHistory` 重放 Final／publish 结果；Controller 重连从当前已观察运行和权威未入历史通信输入定位 Run，经已有 `statusHistory` 分页补读。沿用来源去重和终态收束，当前对话切换不串出，本地 waiter 路径不变；没有新执行／恢复机制。
- 直接验证：Controller／Presenter／Local RPC 96 项通过（16.034 秒）；真实本机 Owner → binding → RPC 重订阅 → Controller／Presenter 成功补来源和最终结果，重复重连不重复显示、运行仅一次，1 项通过（30.026 秒，4 项跳过不计入）。失败／取消／过期、离线前已见与未见运行、分页、迟到通知和切换隔离有正式回归。日志前缀 `overall-reconnect-ui`、`overall-reconnect-local`；新增游标 null 拒绝的检查另作定向复验。
- 工作区：修复期间发现协作文档已有部分内容进入暂存区，HEAD 未变；双方未执行 Git 写操作，不推断来源，保留现有索引。
- R2 输入与验证：本轮 5 个变更 TS 文件路径／字节 SHA-256 为 `2c573a2fade5d2c97c80fe486d119d5720bd294865259e46764d553b86e55229`；HEAD 不变，现有索引摘要 `c1194b739a286ae738104c39f28638a70f13c16feb7223fb47311a2fd39eb1a6`。上述 97 项直接回归通过；游标 null 拒绝的单例补验通过（15.547 秒，不重复计数），生产闭包类型检查通过（29.399 秒），暂存／未暂存差异检查通过。证据另见 `overall-subscribe-cursor`、`overall-production-types`。复核该根因及直接交界，未失效的整体 R1 结论复用。

- R2 结论与补证：暂不通过，只有同根两处直接交界。真实 facade 经 Executor 及 CurrentAnchor 路由把本机状态查询发到 Anchor；Controller 三处补读把非空 `next` 当成 hasMore，但真实 Owner 到尾页仍返回原水位，导致反复查询。两项精确夹具均复现（33.899 秒，退出 1，`overall-r2-route-confirmed`）；新增整体成功链原先直接绑定 Owner 状态端口，未证明真实 facade 路由，不据此关闭异常补读。无第三处根因，其他独立结论复用。
- R2 修正范围：状态补读作为有限 `session.statusHistory` 投影复用现有 session 当前 Owner 路由及 Owner 查询；facade 按对话分组，三处循环仅续读有水位推进的 subject。保持原 session 状态组播和接管栅栏，不改执行、恢复与授权机制；正式回归改走真实 facade／registry／router。
- R3 直接证据：Controller／facade／Local RPC／canonical 状态查询共 108 项通过；首轮 107 项通过、1 项对展示文案的断言误用了错误类型名，改正测试断言后单独通过，未因此改生产逻辑。真实 Owner → registry → Executor/Local 路由 → facade → Controller 的离线成功／失败 2 项通过，同时覆盖真实保留水位尾页、重复重连与重连后实时失败通知（与修正断言单例合跑 53.819 秒）。注册表、入口归属及新方法真实空请求行为 2 项通过（11.448 秒）；生产闭包类型检查通过（25.380 秒）。证据前缀 `overall-r3-direct`、`overall-r3-route-ui`、`overall-r3-registry`、`overall-r3-types`，跳过项不计；复用仍有效的 Presenter 等证据。
- 注册表证据边界：原生成器因既有 `s7-entry-coverage.mjs` 引用已迁走的 `research/design/drafts/transcript-persistence-and-attention-window-architecture.md` 而退出（HEAD 中同样引用且无该文件）；未运行全量 S7 或扩为迁移整改。只更新新 RPC 的两份快照和既有 conversation-read 归属，以上两项直接生产注册表／实际 handler 校验通过，不声称整个生成器通过。
- R3 输入：12 个 TS 指纹 `a3e669c73a0c9a6f6d52d0606154abfa81d484f1c9dca5859d30abbb58ec98d4`；连同两个 JSON 快照及覆盖映射共 15 文件指纹 `9b90869130bc4e2548d5a1aac908c8a6a04d0bbb9bdbce88f972c649c72127e0`。HEAD 未变；现有索引摘要变为 `d141d7221ee92b88c6a8f817edadfe9718d294244fea43abbb322c050807c7c1`，双方未进行 Git 写操作，保留外部暂存结果；暂存／未暂存差异检查通过。
- R3 结论：独立通过，两处剩余 P2 关闭，修复及直接交界无新问题；其余整体审查结论复用。审查方核实 112 项去重直接回归及生产类型证据，确认 Owner 选择／冻结栅栏、三消费端尾页退出和既有实时组播均保留。所审 15 文件与 HEAD 首尾一致；审查时索引已变为 `296438834ebb80c634766b9897075d2fc705c58c82d1a4f613dec7ff7bc704b8` 且首尾一致，双方保留、不操作 Git。允许执行登记终验，不重复同输入测试。

整体终验计划：独立复核通过后执行版本检查及 17 包串行构建（上游 server 已改，先前仅 CLI 构建的前提失效；预计 4–6 分钟，10 分钟进程树硬截止），使用既有 `run-check.mjs` 留日志／退出码。复用未失效直接测试，不跑包全测或调用外部模型。随后核对源码、HEAD、索引和文档状态；失败只处理实际失效闭包。

### 整体收尾结果（2026-09-16）

- R3 独立复核通过；修复自动通信重连后的 Final／终态补显、状态查询 Owner 路由和尾页水位消费，覆盖本地 waiter 与无 waiter 路径，未改变产品范围和执行语义。
- 112 项去重直接回归及生产闭包类型检查通过，未受影响的既有证据复用。模型为确定性夹具，远端为同机协议／传输夹具，不宣称在线模型或实体多设备验证；注册表生成器的既有旧路径阻碍按上述记录保留，未冒称全量 S7 通过。
- 最终版本检查通过（1.972 秒）；17 包串行构建通过（371.389 秒、未超时），17 份 ESM／16 份 DTS 全部成功，CLI 按配置不产 DTS，运行产物已更新。终验无代码修复、未重复测试，原始命令／退出码／日志为 `tmp/communication-unit2-review/overall-final-version.json/.log` 和 `overall-final-build.json/.log`。
- 最终 12 TS、全部 15 个代码／快照／映射文件、HEAD 与 R3 一致；索引仍为 R3 实读的 `296438834ebb80c634766b9897075d2fc705c58c82d1a4f613dec7ff7bc704b8`，暂存／未暂存差异检查通过。双方未进行 Git 写操作，保留已有暂存结果；所有验证命令已退出，协作收尾，不启动新需求。

---

## 已完成交接：对话间通信单元 2

- 状态：2026-09-16，R4 独立复核及最终验证均通过，单元 2 与完整通信模块协作收尾完成；范围内审查问题全部关闭。未操作 Git，未开展新需求。
- 初始输入：HEAD `a15cfcef390abb9e41263c6da4af20038495a3f5`（单元 1 已提交）之后的单元 2 交付。审查开始时 39 个变更 TS 文件，按排序路径、NUL、文件字节、NUL 的 SHA-256 为 `c4f866e3db76919e14d00f8b90bf539eb9c36fad0289c8da5c11b3f62aeb2b9f`；暂存 diff 原始字节 SHA-256 为 `ea435c878f84057c75a3c69c9dda798618701607bbfd3b9ccd8503f39e90e76e`。本轮开始时交付已在暂存区，双方不修改索引；修复后的输入见下方各轮记录。
- 入口：内置技能索引／`load_skill` → `conversation-tools.ts` → Conversation 通信应用 → 本地 Owner 或 Mesh 路由／现有执行链；远端 Worker 经 Owner 输入端口接收下一 Turn 来信；正式事件与读取结果进入 RPC、CLI 控制器、历史和工具卡。

### 本轮审查重点

1. **可发现且可用**：main/work 实际模型窗口能发现并加载简洁中文技能，通信工具真实装配；不污染无关运行或提示，不夹带设计者心理活动、固定协作剧本及未实现承诺。
2. **完整链与所有权**：发现／读取不启动目标，发送可靠接纳且来源由真实运行绑定；A 结束不阻断 B 收信或回信，回执不自动唤醒；无重复通信执行引擎、Host 业务状态、主从关系或共享额度。
3. **本地／远端一致**：已注册的 Owner、Anchor、Executor 路由和失联／部分目录结果真实；忙时来信经当前权威进入同一 Run 的下一 Turn，遵守 assignment fencing、关闭、停止和恢复，不以空数组或成功掩盖失败。
4. **用户可辨且不受打扰**：来源、去向、接纳／消费／停止／运行状态准确；历史、分页、未进入历史的来信以及漏流后最终输出不丢来源。通信不切换用户当前对话，用户仍能按原入口介入和停止。
5. **整体兼容**：检查单元 1 与新工具、技能、Mesh、RPC、运行事件、UI 的全部直接交界；保留既有用户输入、工作场景、委托、Job、确认、记录／恢复语义。对真实可达问题给出根因、位置、影响、修正方向和关闭条件；不以未来可能有用扩面。

### 可复用开发证据

- 158 项定向用例通过：工具／技能 6、Mesh 3、历史 13、展示器 9、控制器 50、本地 RPC 16、场景投影 14、工具卡 44；另有 Runtime Turn 输入、真实本地通信整链及真实远端 Worker 接收各 1 项。
- 本地整链覆盖技能加载、发现／读取、A 向 B 连续发信、A 结束、B 同 Run 追加接收并回信、A 后续 Run 接收；远端链覆盖 Owner 耐久接纳、Mesh 输入端口、边界重试及正式记录／消费关联。模型为确定性夹具，远端为同机真实协议／Worker 夹具，不是在线模型或实体多设备验证。
- 当前生产源码类型检查及 17 包构建通过。构建日志为 `tmp/communication-unit2/build.log`；测试与类型检查原始结果在执行方开发回合，未另存完整机读报告。源码映射配置 `tmp/communication-unit2/vitest.config.mts`、`production-tsconfig.json` 仍在，必要补证可复用。
- 输入不变则复用，不因开始协作重跑全部检查。若需补证，双方先明确谁执行、精确用例与时间上限；修复只重验失效的直接证据。审查通过后才登记并执行必要最终构建，不运行整包、组合 baseline 或无关全量验证。

### 本轮协作记录

| 阶段 | 结果与下一步 |
|---|---|
| 初始化 | 已从最新任务列表核实原伙伴名称、ID、host 与工作区；已将协作内容、目标及要求更新到单元 2 和整体通信模块，准备背景确认 |
| 背景确认／R1 | 审查方已主动确认需求起点、产品／架构、单元 2 与整体模块边界、证据限制，无澄清项。派发 R1：核验输入后完成生产链审查，一次性返回根因清单或通过；必要补证先约定，执行方保持源码、测试及构建冻结 |
| R1 隔离补证 | 双方约定由执行方独占运行 `tmp/communication-unit2-review/communication-r1-probe.test.ts`：普通通信 Run 无本地 waiter 时取消调用为 0；final-only 仅补首条 C 来信、漏掉 A 的追加来源；仅规范 assignmentStream 的远端变体连首条来源也不显示。前两例 10.524 秒，远端规范字段复跑 12.108 秒，均为缺陷复现而非验收通过。原始命令、退出码与日志在该目录的 `r1-probe`、`r1-remote-canonical`；首版远端夹具多余字段已校准，旧日志不作为规范协议依据。正式源码、测试、索引未变，等待完整 R1 结论 |
| R1 结论 | 不通过：01 P1 普通通信 Run 无本地 waiter 时停止入口失效；02 P1 本机 Owner 自动通信无接收端通知；03 P2 远端规范流及漏流补绘缺少逐条来源；04 P1 已接管的 local 地址从其他 Executor 仍发往原设备；05 P2 Anchor 本机恢复早于通信端口绑定。源码解冻，按根因覆盖停止键盘分支、Owner/RPC 订阅、流与历史展示、当前权威路由及启动生命周期；不新增协作流程或恢复机制。审查首尾 HEAD／39 TS／索引指纹一致，其余未失效结论复用 |
| R1 集中修复 | 01 从当前对话权威未入历史输入取得 Run，main/work、无 waiter／漏流情况下仍经原取消合同停止，三个空闲键盘入口一致；02 本机 Owner 转发同一耐久 StreamFrame、状态及已核实 Final，经现有 RPC 订阅投影，不另建执行链；03 规范流与正式历史均逐条投影来源，展示器按消息 ID 去重，覆盖用户发起 Run 的追加来信及无末尾文本；04 非本机 local 地址先到当前 Anchor 查接管归属，未接管才转原设备，本机冻结栅栏不变；05 分离本机 Owner 创建与启动，通信端口与 Mesh 服务绑定后才恢复，原生命周期参数和失败清理保留 |
| 修复验证 | UI／路由／RPC／创建装配 6 文件 97 项通过（24.112 秒）；真实本机 Owner → RPC 成功／失败／取消 3 项通过（37.908 秒），未走 sender 的 delta／complete 旁路；本机生命周期启动 2 项、服务器启动装配 7 项通过；真实权威排队恢复 1 项通过（23.865 秒）。恢复例首轮漏清夹具内的准入调度占位，停在 queued；改用既有 deferScheduling 模拟待恢复交接后通过，没有修改生产恢复策略。去重合计 110 项，不包含跳过项；模型为脚本夹具，路由为实际生产函数与传输端口夹具，不宣称实体三设备或真实模型验证。命令／退出码／日志在 tmp/communication-unit2-review/r1-* |
| R2 输入 | 46 个变更 TS 文件（含两份新增直接测试），路径／字节 SHA-256：`48b9be731be7dc2723f5240785d629f7bf61aaab91191f920a7880b2ecec29ab`；HEAD 与索引指纹仍为本轮基线。生产闭包及三份新增／调整测试严格类型检查通过（19.693 秒），差异检查通过；未运行重型全测或最终构建。复核上述五项及其直接交界，其余未失效整体结论复用 |
| R2 结论与补证 | 暂不通过，仅剩 1 项 P2：无 waiter 且终态先于流时，Controller 丢弃 failed／cancelled 状态，迟到文本仍作为活动输出。其他五根因关闭。双方约定执行方隔离运行真实 Controller＋Presenter：2 例均复现，无帧时无来源／终态，迟到流仍输出；证据 r2-terminal-probe，9.885 秒、退出 1，不算验收通过。只是界面错误续显，未声称实际 Run 被重启 |
| R2 修复／R3 交接 | 当前对话的权威状态按 revision 缓存，失败／取消／过期从既有 inputsOutsideHistory 补来源，读取暂时失败保留状态并重取；已知终态阻断迟到输出，来源／终态只展示一次，本地 waiter 已处理的失败不重复进入旁观展示。不新增运行状态机。仅 Controller 及其测试改变；73 项 UI 回归通过（9.418 秒），覆盖无帧、重复、旧 revision、读取失败、迟到流、其他对话及 waiter 保护；未失效的 42 项证据复用，本轮去重 115 项。生产闭包及新增测试类型检查通过（23.138 秒），差异检查通过。46 TS 指纹 `2f4f1df2a2e8a7383c72f7d61c40c1765ce35dd01bd3cab327e27ee99db6ec7c`；HEAD／索引未变，交 R3 只复核该根因及直接交界 |
| R3 结论与补证 | 普通通信 Run 终态关闭；剩余 P2：用户 waiter／工作场景续接按原规则收束后，没有独立补绘同 Run 的追加通信来源。真实 Controller＋Presenter 隔离两例均仅来源断言失败（9.451 秒、退出 1，r3-mixed-terminal-probe），终态本身正确且没有重复。执行方解除冻结，修正同根两个消费者，不改变任务身份或执行语义 |
| R3 修复 | 来源补绘不再受 Run 起因或已展示终态限制；同 Owner 输入按 Run 筛选、消息 ID 去重，读取瞬断独立重试。本地 waiter／续接仍立即按原规则收束，已知终态继续阻断迟到文本；没有通信输入不冒充通信 Run。新增两个代表性正式用例，75 项 Controller／Presenter 回归通过（9.982 秒）；未失效 42 项复用，本轮去重 117 项。源码指纹 `6fd943f8dbd2954b9bed253a9de6771008fb2ae3bc9bb84960ac68885d0dfca9`，HEAD／索引不变 |
| R4 交接 | 生产闭包与新增测试严格类型检查通过（28.708 秒、r3-production-types），差异检查通过。仅 Controller／直接测试变化，复核来源独立补绘及直接终态交界，复用已关闭 Owner／路由／装配与整体设计结论。源码／测试重新冻结，独立通过前不构建 |
| R4 独立结论 | 通过。混合来源终态补显关闭；核实来源读取与终态展示解耦、同 Owner 按 Run 筛选、逐条来源及消息 ID 去重、瞬断重试、重复／旧状态和迟到流保护，无新范围内问题。117 项有效直接证据与严格类型检查已核，复用已关闭整体结论；审查方未写文件或运行测试／构建，首尾 HEAD／46 TS／索引指纹一致。允许执行下述终验，尚不把代码审查通过记为整体收尾完成 |

本轮终验计划（仅独立复核通过后启动）：`pnpm release:version:check`（60 秒截止），随后 `pnpm -r --workspace-concurrency=1 run build`（17 包串行，预估 4–6 分钟，600 秒硬截止）。CLI 生产修复使旧构建失效；原先直接回归及类型证据复用，不重跑包全测、组合 baseline 或真实外部模型。命令经 `tmp/communication-unit2-review/run-check.mjs` 留日志、退出码与耗时，超时仅终止所登记子进程树；构建子进程临时使用 6 GiB 堆。通过后核对 HEAD／索引／源码指纹并更新本单元完成状态；失败只修复实际失效输入。

### 最终结果（2026-09-16）

- 版本检查通过（1.268 秒）；17 包串行构建通过（285.424 秒、退出 0、未超时），包含 17 份 ESM 和 16 份 DTS 成功结果，CLI 按既有配置不产 DTS。实际运行产物已更新，终验无代码修复；core 仍有既有的 SkillModeDto／SkillUsageRecord 两条类型重导出警告，没有新增构建错误。
- 复用独立审查已核实的 117 项有效定向回归及生产闭包类型检查；没有重复整包全测或调用外部模型。确定性运行、同机协议与传输端口夹具的证据边界不变，不冒充在线模型、实体多设备或完整进程重启验证。
- 最终 46 TS 指纹仍为 `6fd943f8dbd2954b9bed253a9de6771008fb2ae3bc9bb84960ac68885d0dfca9`；HEAD 与索引指纹均为本轮基线，差异检查通过。双方未操作 Git；本轮修复保留在工作区，原暂存区未改动。
- 精确命令、耗时与退出码、原始输出保存在 `tmp/communication-unit2-review/final-version.json/.log`、`final-build.json/.log`；各轮隔离复现与正式回归证据同目录保留。任务已登记单元 2 完成，协作停止，不启动其他需求。

---

## 已完成交接：对话间通信单元 1

- 状态：2026-09-16，本单元协作收尾完成。R2 独立复核通过，R1 两项关闭；必要最终构建及输入核对通过，无范围内未解决问题。未开始单元 2、未执行 Git 写操作。
- 输入：HEAD `3c145e7166931439af18392f96adbb0cbb8b1bb5` 后的全部暂存／未暂存交付及未跟踪源码。当前 25 个变更 TS 文件，按排序路径、NUL、文件字节、NUL 的 SHA-256 为 `16ef0f98584a90c3393884e7df0ac6380477c4cb398305a6cd4a3335896a8eb7`；完整暂存 diff 原始字节 SHA-256 为 `c0a1b3e169e459ed46cef88dd8ca7f60fc73842c1418957c6f3989a200d00c66`。索引由用户预先暂存，双方不改动。
- 实现入口：`core/conversation/communication-application.ts` → 既有 Directory／准入 → Owner `conversation-assignment.ts` 的追加输入事实与端口 → 本地 `conversation-protocol-runtime.ts`／Session adapter → `create-agent-runtime.ts`／`agent-loop.ts` → 正式 RunRecord、权威消费状态及历史读取。

### 审查重点和通过条件

1. **产品与架构**：能力归 Conversation，事实归接收方权威，Kernel 只有通用输入；复用正常生产链，不新增协作引擎、源端状态副本或 Host 业务旁路。发现／读取不启动对方；发送不擅自创建目标或切换界面。
2. **接纳及运行**：稳定消息 ID、不同内容复用 ID 的冲突、忙时并发接纳、完整 Turn 边界、纯文本收尾竞态及连续多条消息均正确；接纳不冒充消费，消费不冒充完成。不中断当前工具、不重置计量，不另启并行 Run。
3. **记录及恢复**：消息原文与逐条来源贯穿接纳、消费、正式记录、当前 Run 可读投影及历史；不借临时上下文注入。投影释放／重建、重试、异常／停止／限额及既有恢复决策下无静默丢失、错误归属、重复消费或越过旧终态。
4. **兼容与边界**：旧消息、普通用户输入、已有委托／推进／确认／记录／Provider 消费保持原语义；检查新协议及提交约束的全部直接消费者。仅新增／修改与本次有关的内容；若涉及提示，使用简洁中文，不夹带设计者心理活动或固定协作剧本。
5. **判据**：以可达调用链和反例确认问题，完整报出根因、影响、位置和关闭条件；无真实问题直接通过。明确尚未验证的条件，不把单元 2 的已登记工作错报为本单元缺陷，也不把单元 1 缺口推给单元 2。

### 已有证据及终验约束

- 轻量回归：Agent Loop、Conversation 应用、Owner 记录合同、Kernel envelope 共 168 项通过；新增 Runtime 2 项、adapter 及协议兼容／来源校验组通过。原始输出在执行方上一回合，临时源码映射配置已清理，不伪称已有独立机读证据文件。
- 真实应用／Owner／本地协议／Kernel loop 集成单例通过（该文件其余 43 项未运行）：覆盖空闲、忙时混合来源、重试冲突、收尾后新 Run、历史、投影重建及未启动来信重启恢复。Runtime 组另行验证正式记录及工具决策上下文；模型为确定性夹具，未进行真实模型或跨设备通信验证。
- 源码类型检查、差异检查、版本一致性及 17 包串行构建已通过；core 构建保留两条既有 Skill 类型重导出警告。无修复则复用，不重复构建。修复后只补失效的直接证据；正式复核通过后再做必要最终构建，先登记命令／范围／预计耗时与截止，禁止整包全测或组合 baseline。

### 当前协作记录

| 阶段 | 结果与下一步 |
|---|---|
| 初始化 | 已从最新任务列表核实原伙伴的名称、ID、host 和工作区；更新本文件到通信单元 1，准备背景交接。执行方冻结源码、测试和构建，仅维护协作状态 |
| 背景确认／R1 | 审查方已主动确认产品洞察、单元边界、Owner／Kernel／Host 职责及既有功能保护，无澄清项。现在派发 R1：对上述 25 TS 交付及直接生产交界做完整独立审查；执行方保持源码、测试和构建冻结 |
| R1 补证 | 双方约定在隔离副本验证：maxTurns 工具结束及纯文本最后一轮成功接纳来信后，均出现 consumed=false、常规读取不可见；投影重建和原操作重试不改变现象。两个反例分别 20.174 秒、19.214 秒；证据在 `tmp/communication-review/` 的 `pending-probe-final`、`puretext-probe` 日志、JSON 及命令记录。这是缺陷复现，不是功能验收，也不是进程重启验证；正式代码和索引未变 |
| R1 结论 | 不通过：P1，关闭／终态时未消费消息缺少明确处置及常规可读入口；P2，历史倒序页误用首条续页，造成重复。其余已审范围无单列问题；源码解冻，修复停止处置／读取、分页及直接消费者，不重开终态、不重置额度、不扩展单元 2 |
| 集中修复 | Owner 从既有耐久关闭／终态及消费事实投影 pending／consumed／stopped；常规读取返回 `inputsOutsideHistory`，保留终止后不在正式历史中的原文与来源，并遵守 clear／delete 边界。原操作重试保持原回执，不自动执行；分页取倒序页末。未新增日志种类、调度器或 Kernel 业务规则 |
| 直接验证 | 先运行通信整链保护例、分页和四种终止变体；整链与分页通过，四个新变体的停止／读取／恢复／重试断言均通过，但最后清空步骤误传夹具 principal 的多余字段。仅修正测试参数，定向重跑四个变体；不重复成功整链或全包测试。原始输出及命令记录保存在 `tmp/communication-review/r1-regressions*` |
| R2 交接 | 26 个变更 TS 文件指纹 `cf11774a9e460e7cafa4b4c8029c2daa578dfea4281514a9b0952b33ac34b902`，索引指纹与 R1 相同。四种终止回归 4/4 通过（42.684 秒），包含真实取消、错误、额度、纯文本收尾、原文／来源、投影重建、原操作重试、清空边界及只执行一次；正常整链和分页的 2 项成功证据复用。生产调用链源码类型检查通过（18.041 秒），diff 检查通过。未重建、未扩展单元 2 |
| R2 结论 | 独立通过，两根因关闭，无修复及直接交界中的新问题。确认权威派生停止状态、历史之外输入的完整消费链、clear／delete 边界、原回执重试与独立计量，以及倒序分页合同。逐项读取真实回归与命令证据，未将首轮含夹具失败的报告说成全绿。首尾代码／索引／HEAD 一致；未重复测试或构建，允许执行登记终验 |

类型证据边界：额外纳入两个既有测试文件做严格类型检查时有 12 条诊断；以只读编译宿主加载修复前索引源码复核，诊断完全相同，均不在新增用例中，不将其冒称全测试类型通过或扩为无关整改。正式生产源码检查显式装载 Node 类型后通过；首轮临时配置缺少 Node 类型及两次工具路径错误不属于生产失败。具体命令、退出码与原始输出均保存在 `tmp/communication-review/`。

### 本单元最终验证计划

仅在 R2 通过后串行执行：生产代码若未再修改，复用上述 6 项直接行为证据及源码类型检查，不跑整包、S7/A7 或真实模型。

| 顺序 | 精确命令／范围 | 必要性与截止 |
|---|---|---|
| 1 | `pnpm release:version:check`；`pnpm -r --workspace-concurrency=1 run build` | core／Owner／CLI 接口变化，按项目要求重建全部依赖产物；与 `pnpm build` 相同构建集合但串行。仅子进程使用 `NODE_OPTIONS=--max-old-space-size=6144`。预计 4–6 分钟，截止 10 分钟；日志、命令结果和超时进程树由 `tmp/communication-review/run-check.mjs` 保留 |
| 2 | `git -c core.safecrlf=false diff --check`，核对 HEAD、代码／索引指纹、引用和单元状态 | 确认最终交付输入与审查一致，保留原暂存区；预计 2 秒，截止 10 秒 |

完成后清理本轮临时测试源码／配置和运行助手，保留审查与验证日志／JSON；不操作 Git，不开始单元 2。

### 本单元最终结果（2026-09-16）

- 版本检查通过（0.708 秒）；17 包串行构建全部通过（263.402 秒），CLI 实际运行产物已更新。只有此前已有的两条 Skill 类型重导出警告，没有新增构建错误。
- 复用 R2 已核对的 6 项直接行为证据和生产源码类型检查；最终阶段没有代码修复，未重复测试或新增审查轮次。
- 代码仍为 R2 的 `cf11774a9e460e7cafa4b4c8029c2daa578dfea4281514a9b0952b33ac34b902`，HEAD 仍为 `3c145e7166931439af18392f96adbb0cbb8b1bb5`，diff 检查通过。终验时索引摘要变为 `72ba8a0589ef5ba540a17c72bfe7dc6189ccbe3b82c8e417b83bda4db2b1d483`，但所审源码字节未变；双方未做 Git 写操作，不推断来源，也不恢复或改动现有索引。
- 本轮临时测试副本、类型／测试配置及运行助手已清理；原始复现、回归、类型对照、版本和构建日志／JSON 保留在 `tmp/communication-review/`。所有本轮命令已退出。
- 单元 1 完成；模型工具、内置技能、界面与远端接入仍归单元 2，未将本单元完成冒称完整通信产品已交付。

---

## 已完成交接：通用自主性整体审查与收尾

- 状态：2026-09-16，整体 R3 独立审查和最终验证均通过，所有范围内问题已关闭，模块整体收尾完成并停止；未扩张需求、未修改封版设计、未操作 Git。
- 输入：`a78dbf2ef250f49fd8f19d7005b361189546a977..7f8dae86ca3cf9164647438b5e72fd6be9afff72` 的完整模块变更及其当前生产交界，包含 `cf92822b`、`097c34a0`、`7f8dae86` 三次提交。本轮开始时工作区干净；只更新协作状态，不操作 Git。
- 分工：审查者独立核对整体实现；执行者核对需求到生产链的覆盖、处理补证与修复。正式审查期间冻结源码／测试，不启动重型测试或构建。

### 整体审查目标

| 封版义务 | 整体核对重点 |
|---|---|
| 稳定身份与通用自主行为 | 主对话、场景、本地／远端、后台、两条子任务链的最终模型输入一致且适用；专用调用不被人格污染。提示引导求解而非固定流程，不夹带设计者心理活动、虚构经历或结果。 |
| 寻找办法而非转交技术负担 | 工具、技能、程序、服务的发现与组合真实可用；能直接使用就不强制注册。权限拒绝不绕过，只有必要选择与凭据输入交用户，不以能力缺口直接结束原任务。 |
| 原目标连续推进和交付 | 沿实际用户入口核对身份／约定 → 探索与委派 → 场景切换或能力接入 → 生效后的上下文与工具 → 原目标返回／验收／交付；本地、远端及隔离承接的可达边界不得只存在于提示。 |
| 安全、隐私与耐久性 | 实际权限与模型可见能力一致；私人上下文、秘密、设备配置不跨界。取消、用户改意、重连／重启、未知副作用、晚到或重复结果不丢责任、不复活旧任务、不虚报完成。 |
| 架构归属与现有体验 | 产品拥有身份、管理和续接决定，Host 装配，Kernel 执行通用合同；Conversation／Advancement／Schedule／Delivery 各守事实所有权。现有管理、确认与交付入口可用，无重复权威、遗留双路径或新任务账本。 |

### 证据与结束边界

复用下方三个单元仍适用的独立审查及定向验证，但重新核对跨单元完整调用链。最新单元 3 终验已对当前源码完成版本检查和 17 包构建（215 秒）；若本轮无代码修改，不重复构建。若修复使证据失效，先登记受影响用例及预算，审查通过后再补必要最终验证。

已知全 S7 旧文档清单缺口、完整 behavior golden 两项既有错误码差异、两条类型重导出警告，以及未使用真实外部 MCP 服务／凭据的验证限制继续明示，不冒充通过。整体结论须区分：已落实的产品与架构义务、实际执行边界、仍未验证的外部条件；不新增封版需求之外的发布门禁。

### 整体协作记录

整体终验计划（仅独立复核通过后执行）：`pnpm release:version:check`（60 秒截止），随后 `pnpm -r --workspace-concurrency=1 run build`（17 包串行，参考当前机器上轮 215 秒，10 分钟硬截止）。core 提交意图与 CLI 装配改变使旧构建失效；仅构建子进程临时设 6 GiB 堆。运行器 `tmp/autonomy-review/final-check.mjs` 保存逐命令日志／机读结果，超时终止本次明确 PID 的进程树。定向用例、类型、静态检查已有有效证据，不再跑整包或组合 baseline；失败只处置实际失效输入。

| 阶段 | 结果与下一步 |
|---|---|
| 初始化 | 已重新核实原审查伙伴的名称、ID、host 和工作区；重读需求起点、封版设计、三个单元完成记录及测试效率经验，向伙伴交接整体目标 |
| 背景确认 | 审查方已主动确认完整需求、产品／架构和整体边界，无澄清项；同意复用有效证据，按完整目标链独立审查，不扩展需求 |
| R1 输入与补证 | 生产输入摘要 `795c5f88d428f9e9e7f2962abeaf3e075a17792a310bff6b33aae7a9a4b9b299`（相对整体基线的 packages/scripts 原始 diff）。双方只读发现 Job 签发清单与增量 MCP 后实际装配不一致，约定两个隔离用例、90 秒总截止、不构建。真实产品投影／Authority／Agent Job 桥接的 2 项反例断言通过，9.24 秒：新任务仍签发启动清单，旧已签发任务也会获得后来工具。完整 worker 透传由源码核实，模型／MCP 为夹具；首次命令的探针清理错误已纠正，不计作生产失败。证据暂存 `tmp/autonomy-review/`，等待独立整体结论 |
| R1 结论与修复 | 独立审查暂不通过，2 项 P1：① Job 签发应读取当前真实 Job 投影，已签发的 tools／mcpServers 必须贯穿 worker、桥接及本地／远端装配；② 远端主对话在空场景目录时缺少到 Anchor 的实际受控接入承接入口。修复复用既有隔离承接及提交／取消事实，不赋予远端配置权、不复制秘密、不新增任务账本。源码解冻，仅执行者集中修复，完成直接验证后交 R2；其余结论及未改变的证据复用 |
| R2 交接 | 20 个变更 TS 文件（含新测试）的排序路径及文件字节 SHA-256：`31d4fef891be1a92b844017d544d03fd88ced15926005f9e6e786eeb4ad2c9b3`。① Scheduler 每次签发读取真实 Job 投影，Authority 不签发角色外显式工具，worker 传冻结集合，本地／远端工厂过滤并由桥接核对实际 runtime；② 远端 `mcp_delegate` 只提交公开方案与原任务，成功提交后复用隔离 Anchor 承接，接入仍逐次确认，结果／凭据／取消沿既有事实回原对话。无新配置权、秘密同步或任务账本 |
| R2 直接证据 | 9 文件共 115 项去重用例已在对应最终行为输入通过：首批 7 文件 94 项（18.42 秒）；新增消费者组 29 项中 28 项通过，1 项误把原对话已保留的目标要求重复写入返回正文，改为断言原目标交接及结果链后单项通过（8.30 秒）；Job 生命周期补显式角色边界后 3 项通过（5.94 秒）。真实 Authority／产品投影／Job 桥接、远端主对话工具装配与隔离续接均覆盖；Kernel／模型和 MCP 为夹具，未启动外部服务。11 个变更生产根及源码依赖类型检查 0 错误，20 TS 静态检查、diff 检查通过。尚未最终构建，复用未改变的单元确认、隔离、路由及交付证据 |
| R2 结论与补漏 | Job 清单根因关闭；远端主对话隔离承接入口成立，但返回原主对话未带回原 Run 显式工作区，后续签发可能回到默认工作目录，仍属原目标续接的 P1。真实远端 Authority／产品续接／CLI 准入适配器的隔离反例证实遗漏；随后将 owner 权威准入中的环境引用投影到续接来源，仅同一任务接入续跑及返回原任务时传递，重新准入核验。隔离承接、新进入场景及主动换工作区不继承旧引用；不读取模型文本推断工作区，不新增权威或权限 |
| R3 直接证据 | 受影响 core 39 项及真实远端 Authority／准入适配器 1 项通过（10.12 秒）；真实 ConversationProtocolRuntime／owner 的原对话→隔离承接→原对话 1 项通过（35.14 秒，单例 27.61 秒），核实权威投影释放后重载、承接无工作区、返回绑定及三次签发一致、重复恢复不重跑。后者首次 15 秒等待不足，第二次三段执行完成但签发 spy 挂在方法被捕获之后，修正观测时点后通过；失败不计入通过数。13 个变更生产根及源码依赖类型检查 0 错误，24 TS 静态及差异检查通过。R2 未受影响证据复用，未运行整包测试或最终构建 |
| R3 输入 | 24 个变更 TS 文件（包含两份新增测试），按排序路径、NUL、文件原始字节、NUL 逐文件 SHA-256：`286d0cd4d6354f11a4dfeb2e427f3c1e429ac46d950351f7489cf5ea30398b2c`。本轮相对 R2 仅 workspace 传递及其三个测试文件、MCP 说明与本文更新；请复核这条根因及直接交界，复用未失效整体结论。模型／外部 MCP 仍为夹具，不宣称真实外部服务端到端通过 |
| R3 独立结论 | 通过。审查方独立读取生产链、正式测试及机读证据，确认工作区引用来自原 Run 权威准入、重载可恢复，经现有准入重新核验；隔离承接、新场景及主动换工作区不继承旧引用。取消、验收、交付和 Job 冻结边界未退化，复用未失效整体结论，无待修复的范围内 P0/P1。首尾 24 TS 指纹一致，审查方未修改文件、运行测试或操作 Git；允许进入登记终验。全轮对应最终输入的定向证据去重为 118 项通过，真实协议文件仅筛选 1 项通过、42 项跳过，不冒充全文件通过 |
| 最终验证与整体结项 | 版本一致性检查通过（0.842 秒）；17 包串行构建一次通过（310.391 秒，退出码 0，未超时），仅构建子进程使用 6 GiB 堆，无持久配置或终验代码修复。保留既有两条类型重导出警告及上述证据边界，不冒充全 S7、完整 behavior golden 或真实外部 MCP 端到端通过。7 份本轮临时脚本／探针配置已清理，正式测试保留，临时机读证据与日志保留在忽略目录。任务底部登记整体完成，停止，不操作 Git、不启动新需求 |

---

## 已完成交接：通用自主性单元 3

- 基线：`097c34a02468af11c774304d341e08e6eaf90c7c`（单元 2 提交）。审查 `git diff HEAD` 的全部交付变更及未跟踪源码，不能只看未暂存 diff；本轮开始时交付变更已暂存，双方不操作索引。
- R1 输入：`git -c core.safecrlf=false diff HEAD --binary -- packages scripts` 原始字节 SHA-256 为 `d5de019cb0e978edb23c6581ee022dc11114c5d177f83d2c2fc70e1ec7398d48`；完整 `git -c core.safecrlf=false diff --cached --binary` 原始字节 SHA-256 为 `77d11e7208bc1478ba95cbe23d573e39242b46046fe99f49808b341b4cdd17d1`。当前无未跟踪文件；指纹只确认输入，不证明正确性。
- 状态：2026-09-16 R3 独立复核及最终检查通过，全部范围内审查问题关闭，单元 3 已完成。临时验证文件已清理；未执行 Git 写操作。
- 交付链：core `mcp-management/` 产品应用、有限端口、候选与来源解析 → CLI `mcp-tools.ts` 模型绑定／配置面板 → 原 Run 确认与成功提交 → `workscene/continuation.ts` → `mcp-connection-adapter.ts`、Configuration／Secret 提供者和 MCP Host 增量生命周期 → owner-kernel 下一轮投影更新 → 原目标续接与既有验收／交付。
- 当前设备行为：Anchor 本地可自动新增连接；远端只发现和使用已有能力，新增方案沿任务交接回主对话。子任务与后台不借用主对话配置权，秘密不跨设备复制。此项是待核验实现事实，不是用来豁免封版需求：审查须核实回交是否真实可达、边界是否满足原目标，不能接受仅有提示却缺少承接路径。

### 单元 3 审查重点与通过条件

1. **需求闭环**：从陌生目标或能力不足出发，核实发现、学习、直接组合已有能力或受控接入，再到原任务续接与交付；不把找到服务、写入配置、结束 Run 当完成，不强制为本可直接使用的程序注册工具。
2. **产品所有权**：界面与模型入口共享发现、解析、探测及提交／生效语义；Host 只装配和执行生命周期。核对全部消费者与已迁出 CLI 的旧路径，无第二套产品决定或仅改目录的假抽取。
3. **安全与秘密**：实际命令／URL 在副作用前明确确认，拒绝不绕过；仅安全面板输入凭据，模型参数、持久交接、结果与错误不携带秘密。验证设备归属、配置绑定、孤儿凭据、凭据代际、并发更新及子任务权限；外部说明只是资料，不是指令或授权。
4. **生效与恢复**：接入在源 Run 成功提交后执行，不等待自身运行排空、不修改已签发冻结工具集。新增能力、权限和模型可见说明在后续运行一致；探测、保存、启动与实际可用分开。覆盖取消／改意／清空／删除、探测与提交故障、旧配置重放、重复恢复、Host 换代、多个任务并发及管理入口回归，不丢原目标、不误报、不盲目重复副作用。
5. **角色和提示**：覆盖主对话、工作场景、本地／远端、子任务与后台的实际可达路径；继续保留单元 1/2 的隐私隔离、独立验收和交付责任。新增／修改提示使用中文、简洁、有效，不夹带设计者心理活动、过程自述、竞品比较、虚构结果或无效口号；无关英文不动。
6. **交付判据**：全部当前义务有生产承接，根因与同根消费者完整覆盖，安全、功能、耐久性和现有用户管理体验不退化。真实问题一次性列明位置、依据、影响与关闭条件；无问题直接通过，证据缺口明确指出，不为未来扩展造机制或硬找问题。

### 单元 3 已有证据与终验边界

开发阶段已通过 MCP 产品应用、模型入口、Configuration／Secret 并发与隔离、Workscene 续接、冻结投影、会话运行及管理面定向测试；源码类型、56 个变更 TS 静态检查、3 条 MCP 结构规则和差异检查通过。测试使用工作区源码别名，不消费旧 `dist`；临时配置已清除。已知 S7 全入口引用不存在的旧文档，不能宣称全入口通过。这些仅供按输入复用，不代替独立需求／实现审查。

审查阶段先读真实调用链；需验证疑点时约定精确用例，由执行者运行并回传，双方不并发跑测试。通过后才登记最终命令与预算，预计执行版本一致性检查和 17 包串行构建（参考上轮约 6 分钟、10 分钟截止，构建进程临时使用 6 GiB 堆）；修复只复验受影响闭包，不跑全包、组合 baseline 或无关门禁。所有范围内问题关闭且终验成功后，任务状态才改为完成。

### 单元 3 协作记录

终验计划（仅独立复核通过后执行）：`pnpm release:version:check`（60 秒截止），随后 `pnpm -r --workspace-concurrency=1 run build`（17 包串行，预计 4–7 分钟，10 分钟硬截止）。core 产品出口及上游合同已改变，旧产物失效；只给该构建子进程设置 `NODE_OPTIONS=--max-old-space-size=6144`，持续保留命令输出，到期终止本次进程树。复用已有效的定向证据，不启动包全测、组合 baseline 或已知缺口的全 S7；失败只修复、复验受影响闭包。

| 阶段 | 结果与下一步 |
|---|---|
| 初始化 | 已重新取得任务列表并核实“zhixing项目架构师”的 ID、host 和项目；保持既有双方分工，更新本文到单元 3，准备背景交接 |
| 背景确认 | 审查方已完整理解需求、封版设计、调研与 AE-001 边界，主动回送目标与分工，无背景疑问；远端回交和能力实际生效明确列为待核验项，不预设通过 |
| R1 | 正式审查上述冻结交付及直接生产交界；执行者暂停源码／测试修改与构建，审查方只读，需探针时明确请求执行者补证 |
| R1 补证与结论 | 未通过，共四项 P1 根因：设备能力投影未贯通签发／远端执行；独立接入探测在提交前中断会被重放；待接入查询错误中继到 Anchor、候选却由远端本机编辑器保存；无委派来源的远端场景不能按提示回交。5 个隔离用例复现，分别约 7.26s、16.05s、8.24s、19.04s（签发对照含一次错误文字断言调整）；不是功能通过。59 个变更文件及直接消费者已盘点，正式代码与索引指纹未变；无真实设备／服务／秘密操作。新增 mcp.pending 的注册基线也须同步，不与 S7 既有清单缺口混同 |
| R2 修复中 | 集中修复上述根因和直接消费者；保留封版设计、设备隔离、逐次确认、真实生效与原目标耐久续接，不靠删提示或放宽校验掩盖问题。只补受影响正式回归，源码与测试重新冻结后交回独立复核；最终构建仍未开始 |
| R2 交接 | 代码指纹 `ef8f103e2111d2ccc429686cf635bb88c9d086e89eaaed7065979ac20f2d8f20`，索引指纹与 R1 相同。① Conversation 在签发前投影目标设备能力，缺失的可移植工具仍拒绝，远端按签发工具／MCP 集创建运行，补齐任务查询／停止能力目录；② 自动接入删除独立预探测，先保存精确绑定、正常 Host 连接验证并启用，未知提交先读回核对；③ mcp.pending 只走设备本地，远端无本地接入任务返回空，确认与凭据说明明确执行设备，补自身 RPC／覆盖映射和基线；④ 无来源场景复用耐久交接创建稳定身份的隔离 Anchor 主会话，禁入私人约定／技能目录／默认工作目录，不转派远端，完成后恢复原场景及 Advancement 身份。两项架构方向已先获审查方认可；无新增任务账本或跨设备配置 |
| R2 直接验证 | 产品接入 25、续接 29、worker 16 项通过（10.64s，含提交前中断、启用后准入未知的重启恢复）；投影 12、场景工具 11、远端装配 2、真实路由 1、真实 Authority 设备能力签发 1 项通过。模型入口 6、新 MCP 行为和注册／覆盖基线 2 项通过。生产源码类型、62 个变更 TS 静态检查及 diff 检查通过。完整 behavior golden 另有既有 workscene.stopTask/tasks 空请求 -32602/-32603 差异，涉及生产文件相对 HEAD 未变，未更新旧预期；新增 MCP 项已单独匹配。S7 全入口旧文档清单缺口仍不算通过；无实际设备、安装、服务或秘密操作 |
| R2 结论 | 前三项根因关闭；隔离承接方向成立，但仍有两项 P1：真实承接会话的确认／凭据等待对原接入面不可见；Anchor 无本机执行器时先创建了永远不可调度的承接任务。审查方认可沿当前权威委托投影交互可见性，以及准入前返回有限阻塞的修正方向，不新建通知／任务账本，不给远端配置权限 |
| R3 修复 | Workscene 按有效父链、真实活动子 Run 和持久接入身份解析交互来源；确认推送、重连查询及应答共享该投影，保留真实会话所有权，resolved 只清理已签发请求。面板明确执行设备和原对话，设备本地待接入查询补充实际凭据状态。隔离会话准入前核对 Anchor 本机执行器，缺失时复用原结果回执耐久返回阻塞及 Advancement 身份，恢复不重复创建／反馈 |
| R3 直接证据 | 续接 31、产品接入 26、确认构造 41、确认 RPC 27、桥接 9、MCP 面板 26 项通过（合计 160，10.05s）；生产源码类型、69 个变更 TS 静态与 diff 检查通过。两项真实协议回归覆盖无 helper 订阅时原面确认／重连／异身份拒绝／应答后返回，以及无本机执行器时原目标有限续接；最后运行的进程输出在中断后不可再取，保留的 Vitest 文件结果为 failed=false、37.31s，时间晚于最后源码／用例修改，且无残留进程。未重跑包全测或构建；不将模拟基础设施的验证宣称为真实外部服务接入 |
| R3 输入 | 代码原始字节摘要 `652d6cf939d1734bf4e2c2af454276bd00a14681daf13853f558fa69fa568266`。恢复时现有索引摘要为 `d5de71ff559b47ec5cfc4be765503674d55bf6a065fefbff35c7019846799991`，与 R2 不同；执行者未进行 Git 写操作，不推断变化来源，不调整现有索引。封版设计与 HEAD 相同。复用未受影响的 R1／R2 结论，只复核本轮修复与直接交界 |
| R3 结论 | 通过，R2 两项残留及 R1-04 全部关闭，无范围内新问题。审查方核对有效委托、真实 child／接入身份、确认推送／查询／应答／清理、取消与恢复、Anchor 原场景 `/mcp` 入口、保存换代后续接和无本机执行器时稳定返回；首尾代码与索引摘要一致。两项协议证据仅按筛选用例及保留文件结果采信，不声称全文件或真实外部服务验证；确认用例实际拒绝请求。允许进入已登记终验，未将审查通过提前记为单元完成 |
| 最终验证与结项（2026-09-16） | `pnpm release:version:check` 通过；17 包串行构建一次通过，退出码 0，215 秒，未触及 600 秒截止。仅构建子进程使用 6 GiB 堆，未改持久配置；无终验代码修复，不重复已有效测试。core 的 `SkillModeDto`／`SkillUsageRecord` 两条既有类型重导出警告仍在，对应定义和重导出文件相对 HEAD 未变。清理 `tmp/unit3-review/` 下 7 个本轮探针／验证配置，正式回归保留；上述全入口与真实外部服务验证限制不变。封版设计未动，任务单元 3 标记完成，停止而不自动提交 |

---

## 已完成交接：通用自主性单元 2

- 基线：`cf92822bcc3d52b2d9921c69e113921eb64e8b37`（单元 1 提交）。审查全部暂存与未暂存变更及未跟踪源码，不只看某一层 diff；现有索引保持原样。
- R1 输入：`git -c core.safecrlf=false diff HEAD --binary -- packages scripts` 的原始字节 SHA-256 为 `5202eaa756d0a8afa412d7670815cc820af538cc5b19a3f99419760ed0f5fc60`；未跟踪 `packages/core/src/workscene/__tests__/continuation.test.ts` 文件字节 SHA-256 为 `0a405dc50578d3b4397f7583bd17fc6958ed3766b8270cb8087ad7e2549d75a1`；完整 `git -c core.safecrlf=false diff --cached --binary` 原始字节 SHA-256 为 `9a8beba384407a811e50611082e01c74d9eb0720208f5f2a41e78533e61dcc04`。指纹只识别输入，不代替内容判断。
- 状态：2026-09-16 R3 独立复核及终验通过，全部审查问题关闭，单元 2 已完成。临时文件已清理；未启动单元 3，未执行 Git 写操作。
- 交付入口：core `workscene/continuation.ts`、`workscene/application.ts`；owner-kernel `conversation-assignment.ts` 的准入／提交／生命周期投影；runtime-host `session-adapter.ts` 和 protocol 的提交／来源合同；CLI `workscene-continuation-adapter.ts`、`conversation-protocol-runtime.ts`、`access-surfaces.ts` 的恢复装配；`workmode-tools.ts` 与远端 `executor-role-runtime.ts`；Advancement application/recovery、Delivery participant；CLI controller/presenter 与 RPC final/stream 消费。

### 单元 2 审查重点与通过条件

1. **完整推进责任**：以需求和封版设计核实任务切换、委派、恢复与返回后是否仍围绕原目标推进和交付；不得仅因某次 Run 结束、模型自报或发出交接就认定任务完成。只导航或已完成任务不得自动启动旧工作。
2. **事实与恢复**：交接随源 Run 成功提交后才消费；稳定身份复用 Conversation 准入去重，不另建目标账本或通知链。沿完整生产链检查失败／不确定提交、重复请求、晚到结果、跨设备执行、清空／删除、原会话与目标会话的新意图、取消及恢复，不丢任务、不复活旧任务、不盲目重放副作用；不能把普通询问误当用户取消或改意。
3. **职责与安全**：产品应用拥有交接、返回及冲突决定；Host 只装配、接入面只发起与呈现，Journal 持有权威事实与事务边界。检查权限确认、获准交接材料、场景隔离及实际工具／提示一致性；不得搬出一段代码却留下 CLI 独占或第二套决定。
4. **全部消费者**：核对 Task 两条委派链、已接纳 Schedule、Advancement 原任务／proxy／独立验收和 Delivery 自动／显式交付；交接中间态、已取消工作及外国任务不得误消费验收，后台结果必须有真实回收与交付路径。覆盖本地／远端、实时／历史／重启、最终帧与流式帧乱序及大内容引用。
5. **提示专审**：新增／修改提示中文、简洁、有效，保持封版身份及已有价值语义；无关英文不动。逐段核实最终模型输入，不夹带设计者心理活动、过程自述、虚构结果、竞品比较或无效口号；交接说明是任务材料，不是新权限或用户新指令。
6. **交付判据**：覆盖根因与同根消费者、无范围内已知残留，既有功能、安全、隐私、耐久性和独立验收不退化；不为未来扩展造框架，不把单元 3 作为本单元缺陷。无真实问题直接通过，有问题一次性给出位置、证据、影响和验收条件。

### 单元 2 可复用证据与终验边界

| 已有证据 | 范围及限制 |
|---|---|
| core 72 项 | Workscene continuation 16、workscene application 12、Advancement review 17、commit 2、stream 12、Schedule application 13 |
| CLI 125 项 | 工具 9、controller 42、adapter 3、session adapter 26、guidance 9、presenter 7、runtime projection 9、真实协议交接／取消／改意／清空／删除 6、远端装配 9、broadcast lifecycle 5 |
| owner-kernel 18、orchestrator 44、server 6 | Delivery 14、Schedule notices 4；Task 40 与用量 4；Advancement 已拥有耐久 proxy 时不重复调度 6 |
| 源码类型、变更文件静态和差异检查 | 通过；测试按源码别名执行，未依赖旧 dist。开发临时配置已清除，未运行最终构建；265 项只是对应路径的工程证据，不是需求完整性自证 |

先独立读代码，不重复上述未失效测试。疑点需要运行证据时先协商精确用例和执行者，不先跑全包或重型验证。审查通过后由执行者运行版本一致性检查及 17 包串行构建（上游 core 合同变化使旧产物失效；仅构建进程临时设置 6 GiB 堆，不改项目配置），补修复影响的必要定向验证；不运行整包全测或组合 baseline。具体命令、成本与截止在审查结论后登记，S7 全入口既有失效文档路径不冒充已通过。

### 单元 2 协作记录

独立审查通过后的终验计划：先 `pnpm release:version:check`（预计数秒，60 秒截止），再在当前构建进程临时设 `NODE_OPTIONS=--max-old-space-size=6144` 后执行 `pnpm -r --workspace-concurrency=1 run build`，结束恢复原进程变量。core 合同及多个上游包已变更，17 包旧产物全部失效；串行构建预计 4–7 分钟、10 分钟硬截止，保留持续输出与已完成包结果，到期先定位／停止本次构建进程树，不无结果换参数重跑。复用有效定向测试，不运行全包测试、组合 baseline 或已知被旧文档清单阻塞的 S7 全入口。总预算约 11 分钟；若终验确有失败，只修复并复验真实受影响范围。

| 阶段 | 结果与下一步 |
|---|---|
| 背景交接 | 审查方主动回送“背景已理解”，已读需求、封版设计及 AE-001 对应边界，无背景缺口；明确既有普通恢复不重造、本轮关注跨场景衔接，普通询问不能等同改意，确认只读分工和最终构建阶段 |
| R1 | 正式请求完整审查上述冻结输入与直接生产交界；一次性回送真实根因清单或通过结论。执行方不改源码／测试、不跑构建，审查者不启动重型验证 |
| R1 直接补证 | 按审查方请求，执行方用根目录隔离副本复现四项现象，冻结输入未改、未构建：普通“现在进度如何？”导致源 current=false 且 queued 子任务取消；seq1 文本→Final→seq2 文本只呈现前半、未查完整历史；只有 Final 不显示结果；嵌套工作区变更失败只向场景回执，丢失原 main 返回路径和 Advancement 身份。前三项同批 35.29 秒，第四项 2.84 秒；这些是错误复现证据，不计作功能通过。结果已回传，等待完整根因清单再集中修复 |
| R1 中断补证 | 审查方沿 REPL 确认源 turn 收尾后卸载中断、自动运行只有呈现订阅；执行方在自动 continuation 首帧与 Final 之间调用真实 controller.abort，取消 RPC 调用数为 0（单例 104ms，总 17.24 秒）。已回传，尚未修复 |
| R1 结论 | 不通过：R1-01/P1，所有后续用户输入误作撤销；R1-02/P1，嵌套失败丢失根任务返回关系／验收身份；R1-03/P1，自动任务缺少正常中断入口；R1-04/P2，最终内容依赖瞬时流而丢失。其余成功提交、确认与隔离、本地／远端、Task／Schedule、独立验收与 Delivery 及提示专审结论按未失效范围复用 |
| R1 输入核对 | HEAD 未变，所审代码内容未变，但索引和文件归类发生变化：原未跟踪测试现已纳入索引，字节摘要不变；排除它后的 tracked diff 仍为 R1 原摘要。当前含该测试的代码摘要为 `7860ad8b37a7ab2a06130ed052322ce4d9a2eccd2af475f0534d40958bae9769`，索引为 `7f282346df941651ddfee703b631de8b95662ea7d3e62a421ea1f15c71c1a867`。双方未操作 Git、不推断变化来源，保留当前暂存状态 |
| R2 修复 | R1-01：取消“后续普通输入即撤销”的规则，Workscene 从原 Run 血缘投影待交付委托，UI 与模型共用精确停止用例。模型只用 Assignment 签发的任务引用，成功提交后消费独立停止集合，先停旧委托再交接新工作；沿已有 cancel 控制回执保留停止事实，不改已 committed 的根 Run、不建任务账本。R1-02：嵌套换工作区的失败回原任务，并承接其验收身份。R1-03：当前委托可在无本地 waiter／无流帧／重连时停止，Typeahead 与 legacy 空闲输入均接入，多个不明目标不批量误取消。R1-04：Final 回源读取完整历史，保存来源元数据，补齐未展示正文且只完成一次，支持乱序、引用和 Final-only |
| R2 直接证据 | 135 项不重复的受影响用例通过：core continuation 20、controller 45、工具 11、收集器 10、Anchor 投影 9、Executor 装配 9、Workscene RPC 11、RPC facade 4、输入 Esc 1、远端 worker 3、session adapter 1、注册表 1、真实协议 10。含普通追问／补充、停止重载、同轮旧停新接及重放、原始 Run 不改写、嵌套失败返回／验收、Final 乱序／部分文本／仅 Final。新增入口的固定断言和注册快照已同步；初次夹具断言失败后仅复验对应失败项。生产源码类型检查、52 个变更 TS 文件 Biome、差异检查通过；三份新源码／测试作格式化，未运行最终构建或全量测试 |
| R2 输入 | `git -c core.safecrlf=false diff HEAD --binary -- packages scripts` 原始字节 SHA-256：`374ae810c7f70d603c1250ae676c69095f5e5691762608109e3db7af34b4dfc8`；索引仍为 `7f282346df941651ddfee703b631de8b95662ea7d3e62a421ea1f15c71c1a867`。根目录 `.unit2-r1-*` 为复现与源码别名测试临时文件、`.unit2-r2-tsconfig.json` 为生产源码类型配置，均不作为交付物，终验结束前删除。S7 全入口仍受既有文档清单缺失影响，不伪称通过；只同步本次新增入口的映射与快照 |
| R2 结论与补证 | 未通过，完整清单仅两项：P1，多次“主对话 → 场景 → 主对话 → 新场景”后查询签发最早根 Run，但停止校验在较近同 Conversation 的 Run 提前结束；P2，中途引用／缺段后的流式后缀破坏已展示前缀，Final 补全重复正文。双方约定的隔离探针分别复现合法引用被拒（30ms）和“甲丙甲乙丙”（75ms），不计作功能通过；R2 首尾代码／索引指纹一致。其他修复及 R1 未失效结论复用 |
| R3 修复范围 | 停止关联校验按 Conversation + Run 精确走完整血缘，其他结果返回／验收仍取原来最近的对应 Conversation Run；引用、序号缺口或流换代后停止展示不连续后缀，保留已显示的连续前缀，等耐久 Final 补齐。只改两处生产逻辑及三个直接测试文件，不改提示、冻结设计或其他领域 |
| R3 交接 | 代码摘要 `ec8d3b0538e280179fe784893fe88be25f3722246c0dc3f03d08ff7b42f32bc4`，索引不变。core 22 + controller 49 项通过（13.83 秒），新增精确根停止的直接／提交提议两种入口、重建重试及无关根保护，中途引用／缺帧／晚订阅／流换代四种正文恢复。另一个真实协议用例覆盖四段往返、Assignment 签发根引用、提交停止、权威重载与不复活（单例 58.19 秒，总 99.40 秒）。源码类型、五个修复 TS 静态及差异检查通过；原 R2 其他证据复用。请求审查方只复核本次五文件及直接交界，不重审未变化领域 |
| R3 结论 | 通过，两项 R2 残留均关闭，无新增待修复项。精确停止跨过较近同 Conversation Run，原返回／验收语义、循环防护和隔离不变；连续前缀保护覆盖引用、缺段和流换代，Final 权威补全和完成去重保持。首尾代码／索引指纹一致，审查方只读并复用工程证据；允许进入上列终验计划 |
| 最终验证与结项（2026-09-16） | `pnpm release:version:check` 通过；既定 17 包串行构建一次通过、退出码 0，约 6 分钟，在预算内。仅构建进程临时使用 6 GiB 堆，结束恢复原变量；无终验代码修复，复用已通过的直接证据，不重跑测试。core 类型声明生成有 `SkillModeDto`／`SkillUsageRecord` 两条循环重导出警告，相关三个源文件相对 HEAD 无变化且为纯类型引用，未扩展整改；S7 旧文档清单缺口仍明示未通过。清理 6 个根目录临时探针／验证配置，正式回归用例保留；封版设计和单元 3 未变，代码与索引仍为 R3 输入，单元 2 标记完成并停止 |

---

## 已完成交接：通用自主性单元 1

- 基线：`a78dbf2ef250f49fd8f19d7005b361189546a977`。2026-09-15 开始本轮协作时，已有 25 项暂存变更，无未跟踪文件；双方不操作索引。审查输入是 `git diff HEAD -- packages scripts` 加上实际未跟踪源码，不能只看未暂存 diff；文档按当前工作区读取。
- 初始代码指纹：上述 tracked diff 按输出行以 LF 连接（不补末尾换行）的 UTF-8 SHA-256 为 `80cfec4daea8679cd91e97263ede24791cc3fb76c2870aa6898c7f84f4959a46`；完整 `git diff --cached --binary` 同算法为 `6b9c5ee9ac20cc002f891994656b8669cd874f61f58b2e0bbcbbc6e8352e2fa7`。指纹只用于确认同一输入，不代替内容审查。
- 状态：2026-09-15 单元 1 已完成独立审查与终验收尾，无范围内遗留。本轮没有需要修复的代码或提示；有效验证已复用，未重复测试/构建。停止，不启动单元 2/3，不操作 Git。
- 交付入口：`packages/cli/src/serve/zhixing-agent-profile.ts`；Anchor 的 `workscene-runtime-projection.ts` 与 `workscene-agent-guidance.ts`；Executor 的 `executor-role-runtime.ts`；Kernel 的 profile、system-prompt、Task、agent-node-executor、subagent/factory 透传链；工具自身的 web-fetch 指引。两条子任务链只传显式共同价值，不传完整父身份或私人约定。补齐 Executor 的窗口级 guidance，不增加记忆系统或能力接入机制。

### 审查重点与通过条件

1. **需求与产品**：独立判断当前代码是否落实单元 1 的全部义务，是否支持主动寻找办法、基于事实换方法和核实结果，且讨论仍可止于讨论。身份、价值和性格连贯，底线约束目标及手段；不以提示承诺尚未存在的能力或持续责任。
2. **提示词专审**：逐段看最终模型输入及其来源，不只匹配关键词。身份稿承接封版原文；新增或修改的提示用中文，无关英文不动。严查设计者心理活动、创作解释、迁移/审查自述、形象比喻、竞品比较、虚构记忆/当下思考/成功和无效口号；分清对模型的“你”与模型对用户的“我”。检查职责是否必要、简洁、无重复堆规和互相冲突，保留有价值的原义，不为精简删掉核心价值，也不把每个设计句子机械塞进提示。
3. **架构与完整消费者**：产品定义身份，Kernel 通用消费；主对话/场景/后台、本地/远端同源。子任务只获共同价值和委托职责，摘要/验收等专用调用不套主助手。沿真实装配、provider 请求、clear/compact/resume 核实稳定身份、适用约定/技能与即时事实三层，不靠历史摘要维持人格，不跨场景泄漏个人材料。
4. **事实和安全**：实际工具、工作区和失败反馈与提示一致；用户纠正和获准经验复用沿已有 guidance、Conversation、Skill Catalog，不暗改身份、权限或内部配置。现有安全、确认、取消、只读子工具、专用调用及模型槽位不退化。必要代码缺口不能只用提示掩盖，但不提前实现单元 2/3。
5. **结论**：一次性返回真实问题的文件/行、生产或封版依据、影响、根因范围与验收条件；无问题直接通过，证据缺口明示。测试只证明对应工程路径，不宣称已证明长期人格体验，不新增效果评分或评测项目。

### 可复用验证

| 已有证据 | 范围及限制 |
|---|---|
| orchestrator 215 项 | profile、system-prompt、create-agent-runtime、agent-node-executor、subagent/factory、Task；含主子隔离、窗口 clear/resume/compact 和专用文本调用 |
| tools-builtin 45 项 | web-fetch、skill、admit-skill 的既有执行与受控保存 |
| CLI 53 项 | zhixing-agent-profile、workscene-agent-guidance/runtime-projection、executor-role-job-runtime、guidance lifecycle/read、RuntimeHost |
| CLI 类型检查、17 包构建、diff 检查 | 通过；core 声明构建先遇默认堆内存不足，临时 6 GiB 堆、串行完成剩余包，没有持久配置变化 |
| S7 Anchor 工具/MCP 投影定向规则 | 通过；S7 全入口因既有失效文档路径中断，未记通过，未修无关迁移清单 |

本轮先审查，不重跑上述同输入证据。发现疑点先返回具体反例；执行者核实修复后补直接证据，审查者按变化复核。通过后仅补失效构建或真实未覆盖交界，没有新增验证价值就不重跑。

### 本轮交接记录

| 阶段 | 结果与下一步 |
|---|---|
| 背景交接 | 审查方已主动回送“背景已理解”，确认通用自主性本质、封版价值/身份、单元 1 与 2/3 边界、三层输入、提示专审及双方分工，无阻碍正式审查的背景缺口；本阶段未审实现或运行测试 |
| R1 | 以初始代码指纹为输入开始完整独立审查，执行方冻结源码、测试及构建；审查方只读核实真实链路并回送根因清单或通过结论 |
| R1 结论 | 通过，无待修复项。21 个变更文件及直接生产交界已核对：封版身份与简洁职责、各运行入口及实际 Provider 请求、clear/resume/compact、两条子任务链、专用调用、guidance/技能/即时事实、真实能力和批准/失败反馈。未发现设计者心理活动、创作解释、竞品叙事或虚构经历/成功；perspectives 的显式上下文不夹带私人 guidance 前缀。首尾代码、索引、HEAD 均未变，无未跟踪文件。审查方未运行测试或构建，复用有效工程证据，不声称证明长期产品效果 |
| 终验安排 | 没有修复、失效输入或实际未覆盖交界，不重复 313 项测试、类型检查及 17 包构建。只执行 `git diff --check`、代码/索引/HEAD 指纹核对，确认封版设计、单元 2/3 和本轮文档引用；预计数秒，无重型验证 |
| 终验结果 | 通过。最终代码和完整索引指纹均与 R1 一致，HEAD 不变、无未跟踪文件；封版设计和单元 2/3 逐字未变，本轮引用均存在，diff 检查通过。没有使既有测试/构建失效的输入变化，未重跑；S7 全入口的既有文档路径限制仍保留，不冒充全项目通过。任务单元恢复“完成”，交付闭环 |

---

## 已完成交接：NOTICE-C01

- 基线：`87a21773ed5e4dfc2b3fa486be844387eaddaff7`。开始协作时 21 项开发变更已在暂存区，任务文档另有未暂存交接更新，无未跟踪文件；双方不操作索引，修复和记录只写工作区。
- 状态：2026-09-14 NOTICE-C01 已完成。R2 独立复核及必要最终验证通过，R1 两项关闭，无范围内未解决问题；四单元已完成 4/4，停止。A7 及整体退出门仍待单独验收，不自动启动，不操作 Git。
- 最终输入：`git -c core.safecrlf=false diff HEAD -- packages scripts`，包含暂存与未暂存代码；输出按行以 LF 连接（无末尾额外换行），UTF-8 SHA-256 为 `92be754c92ace301d0340bf350f3d1d6a6ee2028ee9933eff62dcab41e548357`，与 R2 一致。完整暂存区 `git diff --cached --binary` 同算法指纹始终为 `a3ee4b8836873cb68bfaba09df4abb2d49ea69276bab2b603b437b3af9788b1f`。修复只在工作区，索引和 HEAD 未动。
- 实现交接：Conversation 的 `publish-results.ts`、`notifications.ts` 拥有公共发布结果反馈、会话状态通知及空取消回执；Schedule 的 `user-notices.ts` 拥有能力缺口开闭、任务发布反馈、离线摘要及任务状态通知。Journal 在原事务快照中调用领域决定，Delivery participant 的提交准备与重放校验复用同一决定。CLI、实时及历史结果消费同一领域来源，旧 owner-kernel 文案模块及出口退出。

### 单元 4 的任务、目标与验收要求

1. **任务与目标**：核实产品通知的生成、更新、关闭、文案、操作建议及结果投影真正归属 Conversation/Schedule，机制层只负责事务、身份、持久化、重放和交付；不能只搬文字、留下第二套产品选择，也不能新增通知框架或把业务目录挪到另一个共享层。
2. **完整范围**：覆盖 ConversationJournal、JobJournal、SchedulerUserNoticeJournal、SchedulerAuthority 和 OwnerDeliveryParticipant 的生产调用、prepare/assert、实时与历史、CLI/Channel 消费、正常启动及权威换代装配；反查其他 Journal 和投递生产者中的同根遗留。已有 Schedule 失败策略、领域权威适配、技术错误与协议合法性检查保留其合法责任，不做无关 Assignment 重构。
3. **功能与数据保护**：保持公开分类、原文、建议、结果细节和投递目标，保留原记录格式/摘要、稳定身份、去重、水位、回源和 Delivery 义务。依赖快照的决定仍在同一事务内执行，触发事实和通知原子提交；不得异步补写、双写、复制状态机或削弱重放伴随记录校验。非通道会话、系统任务、无来源与无需通知状态不产生相应义务。
4. **审查与证据**：以基线实现和真实生产链核对冲突/成功、能力缺口开闭、离线摘要、取消/失败/过期/不确定状态、空取消回执及重试恢复；检查领域依赖方向、事务调用时点、全部同根消费者和旧出口退场。开发记录不是正确性自证，不能仅凭搬移或测试数量通过。一次返回完整根因清单或明确通过；只读且先审查，不先跑重型测试。
5. **验证与结束**：复用下列未失效证据；独立通过后才补实际未覆盖交界或修复影响的最终验证，不跑整包/完整 S7/A7、不重复同输入构建。审查与必要验证通过、无本单元未解决问题后标记单元 4 完成并停止；四单元总验收 A7 单独保留，不自动提交。

### NOTICE-C01 开发证据与协作记录

| 已有证据 | 结果与复用边界 |
|---|---|
| Core Conversation notifications/publish-results、Schedule user-notices | 3 文件 7/7；纯领域决定、公共原文、开闭/去重及离线成员身份 |
| owner-kernel delivery-participant、scheduler-user-notices | 2 文件 16/16；prepare/assert、伴随记录拒绝、无通知分支、持久去重与实时/历史水位 |
| Executor Job/Conversation ledger 定向用例 | 3/3；能力诊断不外泄、无效 staged delivery 冲突、durable publish summary/detail |
| CLI presenter、会话批取消与空回执重放 | 5/5；实际呈现与幂等消费 |
| Core S7 invalid-reset-genesis 单例 | 1/1；修正基线夹具缺失 lease/abort 的调用，损坏恢复断言保留，不运行完整 S7 |
| 17 包构建、Core/owner-kernel/CLI 类型检查、出口检查、变更 TS 的 Biome 与 diff 检查 | 通过；Core 声明初次堆上限失败后仅补失败步骤并串行完成剩余包，未持久改变构建配置；只有输入或依赖变化才使相应证据失效 |

| 轮次 | 请求/结论 | 处置与下一步 |
|---|---|---|
| 背景交接 | 原审查任务已主动回送“背景已理解”，确认权威边界、完整生产/消费链、保护合同及只读分工，无阻碍正式审查的缺口；指出旧调度台账状态差异，执行方已同步校准 | 背景交接完成，正式派发 R1 |
| R1 | 以代码指纹 `288dc26ee99f85fcbf40676791ba289c9a9ce42ed2174f507c0dd2f016860397` 的当前工作区开始完整独立审查 | 执行方冻结代码与构建，审查方只读核实并主动回送根因清单或通过结论；暂不执行最终验证 |
| R1 结论 | 不通过，共两项基线遗留：P1，批取消按整个事务准备状态通知，Journal 却按单条状态校验全部义务，多 channel queued 或 running+channel queued 会在提交前失败；P2，Job ControlAdmission 的 queued 取消/手动替换漏接缺口关闭与 notice companion stream，终态后通知仍 open。其他完整范围无确认问题，首尾输入不变；独立证据为真实生产链和必要内存反例，不冒充持久集成测试 | 执行方核实并集中修复两个 Journal 的事务级 exact-set 校验、Job 两种事务入口共用的领域关闭适配及提交后通知；补真实持久控制、重放恢复、缺失/多余拒绝及无来源分支，完成直接证据后派发 R2 |
| R2 交接 | 代码指纹 `92be754c92ace301d0340bf350f3d1d6a6ee2028ee9933eff62dcab41e548357`；完整索引指纹不变。Conversation/Job 的 replay 改为从整个事务取状态输入，保留 exact-set；Job 两种事务复用唯一关闭适配，ControlAdmission 同步声明通知流，提交后只发布已耐久事实，不引入失败策略重构 | owner-kernel 构建通过（声明 12.66 秒）。直接回归合计 11 项：两个真实 Conversation 批次（两个 channel queued、running+channel queued，均混合第一方 queued）、Job cancel/replace × 有/无来源四项、原空批次重放/正常 assigned 关闭两项、participant 完整集合及缺失/多余/无通知三项；首次新增夹具的队列序号、取消 source 和选取提交条件已修正，仅复验失败项，最终全通过。5 个修复文件 Biome 与 diff 检查通过。执行方再次冻结代码和构建，仅请求受影响范围复核 |
| R2 结论 | 独立通过，P1/P2 均关闭。完整事务输入与 exact-set 未降级；Job 普通/控制事务共用关闭决定，控制结果与终态/gap/notice 同事务提交，随后发布耐久事实。已核对新增持久测试及保护反例，R1 未失效结论复用；首尾代码/索引指纹一致 | 审查方未重复跑测试或构建。执行方进入下列最终验证，失败自行定位修复并复验，不再增加正式审查轮次 |

### NOTICE-C01 最终验证计划

复用修复后 owner-kernel 构建及 11 项直接回归；其他 16 包构建和未失效的纯领域/公开呈现证据保持有效。修复只改变 owner-kernel 内部事务接线及重放输入，没有公开类型变化；CLI 产物仍以包导入消费 owner-kernel，未内联旧实现，因此不重复全量或 CLI 构建。以下严格串行执行，不跑完整 S7/A7 或整包测试。

| 顺序 | 精确命令/范围 | 新增证据、预算与截止 |
|---|---|---|
| 1 | Executor：`node node_modules/vitest/vitest.mjs run src/__tests__/assignment-ledger.test.ts src/__tests__/job-assignment.test.ts --testNamePattern ' state: real production, full/guard acceptance, adversarial-vector rejection$' --maxWorkers=1 --reporter=verbose` | 两个 Journal 的普通已提交状态、full/guard 重放及损坏记录拒绝，补批取消以外直接交界；预计 20 秒，截止 90 秒，仅两项 |
| 2 | owner-kernel：`node node_modules/vitest/vitest.mjs run src/__tests__/scheduler-authority.test.ts --testNamePattern 'redrives a missed summary only after a durable miss hint' --maxWorkers=1 --reporter=verbose` | 调度通知失败后的耐久提示重试/停止重试；预计 5 秒，截止 30 秒，仅一项 |
| 3 | CLI：`node node_modules/vitest/vitest.mjs run src/__tests__/setup-authority-delivery.test.ts --testNamePattern 'publishes a revisioned resolved notice\|rebuilds queued delivery authority' --maxWorkers=1 --reporter=verbose` | 真实投递装配、live/history 消费及共享日志重建，补纯领域和 Journal 测试外的直接交界；预计 15 秒，截止 60 秒，仅两项 |
| 4 | 根目录 `node scripts/check-runtime-package-exports.mjs`；`git -c core.safecrlf=false diff --check`；核对 HEAD/代码/索引指纹 | 检查更新后的 owner-kernel 制品可加载与旧出口退场，确认最终冻结输入及索引未变；预计 5 秒，截止 30 秒 |

各测试命令输出保留；若失败只重验失败项或修复造成的失效输入，不能重跑整集取日志。完整应用进程重启/物理 Channel/Mesh 端到端不在本轮证据内，未变装配链复用独立审查结论。

### NOTICE-C01 最终验证结果（2026-09-14）

| 验证 | 结果 |
|---|---|
| Executor 两个 Journal 的 state 生产/full+guard/损坏拒绝 | 2/2 通过，14.90 秒；446 项无关用例跳过 |
| AnchorScheduler missed summary 耐久提示重试 | 1/1 通过，2.47 秒；11 项无关用例跳过 |
| CLI 真实投递控制通知与共享日志重建 | 2/2 通过，10.75 秒；live/history、queued 义务及停止/恢复链保持 |
| runtime package exports、diff 与冻结输入 | 通过；代码与 R2 相同，完整索引及 HEAD 与协作开始时相同，无未跟踪文件 |

最终阶段未再修改生产代码或测试，未运行整包、完整 S7/A7、重复构建或 Git 写操作。所有本轮验证命令均已退出；修复与协作记录留在工作区，外部既有暂存内容原样保留。四个执行单元均已独立通过并完成必要终验，不表示 A7/整体退出门已关闭。

## 已完成交接：KERNEL-C01

- 基线：`16051748ca1a5ca4bc333444ba734783a0b277bd`。本轮开始时第三单元 18 个变更文件已在暂存区，无未跟踪文件；双方只读暂存区，修复与记录更新留在工作区。
- 状态：2026-09-14 KERNEL-C01 已完成。R2 独立复核和必要最终验证全部通过，R1 唯一 P2 已关闭，无范围内未解决问题；四单元完成 3/4（75%），停止，等待下一单元授权。修复留在工作区，原暂存区、HEAD 未动。
- 审查输入：`git -c core.safecrlf=false diff HEAD -- packages scripts`，同时包含暂存与未暂存代码。输出 CRLF 规范化为 LF 后按 UTF-8 计算 SHA-256：`deb4c6e18edceba375b8aa65c64be69a83a3fe747e1bb442c5405f3140b2d507`。暂存区完整 `git diff --cached --binary` 同算法指纹为 `ea8ea6d0bacb8a7ea18256600ffa965bd96a9ce6f738b6338249daf66e6a42fb`；记录更新不改变代码审查输入。
- 实现交接：产品侧 `workscene-agent-guidance.ts` 接管场景 `powerProfile` 与进入指引原文，Anchor、Executor 和能力目录消费同一来源；实际进入工具携带 `systemPromptGuidance`。Kernel 的中性 `tool-guidance` 段在原位置逐字渲染工具声明，删除旧 Workscene 判断、模板和 Profile 出口。RuntimeHost、Skill 窗口投影、模型选择、工具调用与权限实现未改动。

### 单元 3 的任务、目标与验收要求

1. **任务与目标**：沿全部 Profile/提示词生成、传递及消费链核实 Workscene 产品策略归属；产品责任者决定行为，Kernel 只消费已决定的输入。不能仅搬文件或把产品判断移到 RuntimeHost，也不得新增万能模板、插件或提示词框架。通用角色、工具循环和环境投影保留其合法责任。
2. **完整范围**：覆盖 Anchor main/scene/job/ephemeral、Executor 场景 Runtime、能力目录、工具裁剪及子 Agent 段子集；闭合旧 `WORKING_MODE_TEXT`、`buildWorkingMode`、`powerProfile/WorksceneProfileInput`、默认段与旧出口。反查同根生产者、消费者和别名/遗留路径，不局限改动行；NOTICE-C01 的通知职责不混入本单元。
3. **功能保护**：模型可见原文、提示词顺序与缓存分界、窗口内稳定性/换代、工具及 main/power 模型选择、无工作区隔离、权限确认、显式身份和子 Agent 专注约束不回退。指引随实际能力贡献，缺少工具不得产生虚假指引。新有限字段必须有真实消费必要性，不能以架构整理新增功能或默改既有体验。
4. **审查与证据**：以基线源码和真实调用链核实行为等价，不能以指纹或测试自证全部正确。核查元数据穿过真实工具装配/筛选和窗口边界后的行为、产品 Profile 全部调用者及公开出口；发现问题一次按根因报全，给出依据、影响和验收条件。执行者修复后只复核受影响范围，未失效结论复用；无问题直接通过。
5. **验证与结束**：审查方先读代码，只执行用于核实疑点的必要轻量检查；执行方暂停代码修改和构建。独立通过后执行有新增证据价值的最终验证，复用同输入已通过的 90 项直接测试、构建、类型与边界检查，不跑整包或完整 S7/A7。双方确认无范围内遗留且必要验证通过后，才标记单元 3 完成并停止；不得自动提交或开始单元 4。

### KERNEL-C01 协作记录

| 轮次 | 请求/结论 | 处置与下一步 |
|---|---|---|
| 背景交接 | 审查方已确认权威设计、全部生产入口与保护边界，主动回送背景理解；本阶段未审查源码或运行测试 | 交接完成，立即派发 R1 |
| R1 | 审查输入代码指纹 `deb4c6e18edceba375b8aa65c64be69a83a3fe747e1bb442c5405f3140b2d507`，进行完整独立审查 | 执行方冻结代码与构建，等待一次性根因清单或明确通过 |
| R1 结论 | 不通过，唯一 P2：Executor 的 `ws:*` 场景 Profile 包含未装配的退出/改名/工作区管理指引。基线已有，但属于本单元明确的“缺少工具不注入虚假指引”义务；本机与 Mesh assignment 正式入口均可达。其余完整范围无确认问题；审查方独立比较 9 组 Profile、45 组提示词等价，真实 Executor 类的有/无工作区探针确认缺口 | 产品输入增加必需的 `hasSceneControlTools`，按现有装配事实投影：Anchor scene 为 true，Executor scene 和只读能力目录为 false。共享基础身份/专注/隔离不变，只有具备场景控制工具的入口追加原控制原文；未新增工具或改路由、模型、安全、窗口机制 |
| R2 交接 | 代码指纹 `9b9ca0be9a46cbd6996233069386425971927e7654570d2185e7f4aac81de505`；真实 Executor 装配两分支、普通 main/job 不变、Anchor 原文与完整前缀、工具/投影共 3 文件 27 项通过（19.67 秒） | 复核修复及其直接交界；R1 未失效结论复用。暂存区完整指纹保持不变；尚未重建 CLI 或执行最终验证 |
| R2 结论 | 独立复核通过，R1-01 关闭；6 个代码/测试修复文件及直接交界已核对，首尾代码与暂存区指纹一致。确认三个生产调用、两个实际控制能力分支、Anchor 原文和 Executor 有效语义均正确，无新增问题或范围内遗留 | 未重复运行测试，R1 未失效结论复用；执行方按下列计划完成必要最终验证，不追加审查或启动下一单元 |

### KERNEL-C01 开发证据与最终验证安排

| 已有证据 | 结果与复用边界 |
|---|---|
| orchestrator 的 `default-profiles.test.ts`、`system-prompt.test.ts` | 2 文件 72 项通过；角色、身份、段顺序、指引声明及子 Agent 子集 |
| CLI 的 `workscene-agent-guidance.test.ts`、`workscene-runtime-projection.test.ts` | 2 文件 18 项通过；真实产品工具/投影、工作区限制、作业筛选及基线提示词逐字等价 |
| S7 `Anchor tool and MCP projection` 定向组 | 通过；生产边界及新增反例，无完整 S7 运行 |
| `pnpm build`、CLI `tsc -p tsconfig.json --noEmit`、diff 空白检查 | 通过；构建已完成，未因用户中断重复执行 |

审查前不启动最终验证。R1/后续复核通过后，按实际修复及未覆盖的直接交界列出精确命令、失效输入、复用证据、耗时与截止，再串行执行；生产代码未变时不重建、不重复已通过测试。最终阶段发现问题由执行者自行修复并复验受影响闭包，按双方约定不追加形式化审查轮次。

R2 通过后按以下计划执行增量，结果见下表。复用 Kernel 的 72 项直接测试、修复后的 CLI 27 项和未变边界对应的 S7 证据，不运行这些集合的重复版本。

| 顺序 | 精确命令/范围 | 必要性、预算与截止 |
|---|---|---|
| 1 | 根目录 `pnpm cli:build`，随后 `pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` | R1 修复仅改 CLI，需要更新产物并核对新增必需投影字段的全部调用者；上游构建有效。预计合计 40 秒，截止 3 分钟，串行 |
| 2 | CLI `node node_modules/vitest/vitest.mjs run src/runtime/__tests__/workmode-tools.test.ts src/runtime/__tests__/runtime-host.test.ts --maxWorkers=1 --reporter=verbose` | 补工具真实调用/确认/overlay 与 Host 发放透传交界；不重复产品投影纯构建测试。预计 15 秒，截止 1 分钟 |
| 3 | orchestrator `node node_modules/vitest/vitest.mjs run src/runtime/__tests__/create-agent-runtime.test.ts src/subagent/__tests__/factory.test.ts -t 'primaryRole 槽位\|只消费产品已裁决\|产品提示只在窗口边界投影\|sub-agent profile.enabledTools\|backgroundMessages' --maxWorkers=1 --reporter=verbose` | 仅补真实 Kernel 运行中的模型选择、窗口投影/稳定性与子运行过滤/专注交界；不跑两个整文件。预计 30 秒，截止 2 分钟 |
| 4 | `git -c core.safecrlf=false diff --check`，核对 HEAD/代码及暂存区指纹 | 确认最终输入及既有暂存内容未被修改；预计 2 秒，截止 10 秒 |

### KERNEL-C01 最终验证结果（2026-09-14）

| 验证 | 结果 |
|---|---|
| 根目录 `pnpm cli:build` | 通过，7.36 秒；更新 R1 修复后的 CLI 产物，上游构建复用 |
| CLI `tsc -p tsconfig.json --noEmit` | 通过；新增必需输入的全部生产调用者类型闭合 |
| CLI 工具调用与 RuntimeHost 两文件 | 15/15 通过，Vitest 6.12 秒；确认/overlay、场景读取及 Host 发放透传保持 |
| Kernel/子 Agent 两文件定向用例 | 9/9 通过，86 项无关用例跳过，Vitest 6.27 秒；main/power/fallback、窗口 clear/resume/compact 与多 run 稳定性、子工具过滤和背景/任务隔离保持 |
| diff 空白与输入核对 | 通过；代码指纹仍为 R2 的 `9b9ca0be9a46cbd6996233069386425971927e7654570d2185e7f4aac81de505`，完整暂存区指纹仍为 `ea8ea6d0bacb8a7ea18256600ffa965bd96a9ce6f738b6338249daf66e6a42fb`，HEAD 仍为 `16051748`，无未跟踪文件 |

最终阶段没有新增代码修复，未运行整包、完整 S7/A7、组合门禁或重复上游构建。所有本轮测试/构建命令均已退出；单元 3 的独立审查与最终验证闭合，不代表四单元总验收已完成，不启动 NOTICE-C01、不操作 Git。

## 已完成交接：HOST-C02

- 基线：`139a3baf3bb5b9b33a44d8ac1b2a360641b6324d`，开始时工作区干净。
- 状态：2026-09-14 HOST-C02 已完成。R2 独立复核及必要最终验证通过，R1 唯一 P1 根因及 Channel/Advancement 两组生产失败路径均已关闭；无范围内未解决问题。暂存区、HEAD 未动，停止在单元 2 验收点。
- 范围：14 个构造/恢复单元、command 静态依赖图及直接消费者；删除统一 setup 与共享可写产物容器。保留既有生命周期贡献、可选接入选择和确有必要的开放前一次性窄绑定，不改产品功能及其他纠偏单元。
- 实现：`command.ts` 直接连接 14 个具名工厂的只读输入与返回产物，不再读写共享产物总线、遍历 setup 数组或按名称切片。profile 仅选择 Mesh、Channel、Delivery、确认桥；核心与恢复义务常驻。Channel 的可用/缺席产物及完整本机执行依赖显式传递，保留现有资源 owner、一次性窄绑定和关闭顺序，依赖就绪后才执行恢复与开放接入。
- 直接消费者：原有 S7 检查与反例测试改查实际工厂、返回边界和组合根；同步清除其中已被 C01 固定 home/稳定引用替代的旧文本匹配，未改变对应生产行为或新增验证平台。
- 审查输入：本基线之后的 `git diff HEAD -- packages scripts`（同时包含暂存与未暂存变更），LF 规范化后 UTF-8 原文 SHA-256 为 `f191a340aa1d69698db5b2dc759846af2d125de70bdf3b302ee5b42c902eb961`；无未跟踪文件。原 17 个文件的暂存快照未改变。审查前后核对输入，不用单独的 unstaged diff 漏掉已暂存代码。

### 单元 2 的任务、目标与验收要求

1. **任务**：从常驻生产入口追踪全部 14 个工厂、主干直接创建组件及其消费者，核实创建、连接、恢复、激活和关闭，覆盖 Anchor-only/带 Executor、单机/Mesh、可选接入及无通道分支。不局限于改动行或旧问题清单。
2. **目标**：依赖由明确输入和返回产物静态表达，组合根唯一且可读；旧容器、统一 setup、名称查找/切片及同类旁路全部退出，不得改名成为多个上下文、getter、注册表或新增通用 DI 框架。核心义务不能被 profile 裁剪。
3. **功能与生命周期**：保持全部既有公开行为、配置/协议/持久化/安全合同与 C01 的身份、数据根和稳定引用；当前 owner/权限/连接/代际仍向原责任者实时查询。保持唯一 ApplicationHost/RuntimeHost、资源归属、失败补偿、正常关闭交接、拒新→排空→逆序关闭与幂等性。一次性绑定仅限真实构造环，必须窄合同、开放前就绪、不可重绑，不能等待业务到来才完成依赖。
4. **证据与结束**：以源码和真实调用链核实，不凭名称、行数、测试自证或个人偏好制造问题；发现问题按根因一次报全，附生产依据、全部受影响消费者及修正验收条件。执行者集中修复后只复核受影响范围，复用未失效证据。先审查后最终验证，不跑整包、全量 S7/A7 或重复已通过闭包；审查通过、必要验证通过、范围内无未解决问题后单元 2 才验收完成并停止。

### HOST-C02 协作记录

| 轮次 | 请求/结论 | 处置与下一步 |
|---|---|---|
| 背景交接 | 2026-09-14 用户重启应用后消息入口恢复；重新核对目标对话并正式投递，对方在应用中回送“背景已理解”，确认 14 个单元、全部消费者、功能/生命周期与 C01 保护及分工 | 正式背景交接完成；此前未经确认的绕行不作为交接依据，相关进程已结束；后续异常按“双方与沟通”规则报告处理 |
| R1 | 完整审查不通过：1 个 P1 根因，Channel 初始/stop/迁移恢复提前连接，以及 Advancement 正常/stop 恢复在广播安装前发事件；其余 14 工厂、消费者、角色/缺席分支与清理责任审查完成，无其他确认问题。输入指纹 `3bb123ba99ce4759a8be55e6c9a75cb21d563f1112c5ded5cba44c3f9c6d19cc` | 集中修复同一 Host 就绪边界；Job owner 构造期资源泄漏疑点已被排除；current-removal 取消属于运行期恢复，不冒充冷启动已证实路径 |
| R2 | 独立复核通过，首尾指纹均为 `f191a340aa1d69698db5b2dc759846af2d125de70bdf3b302ee5b42c902eb961`；确认 Product API、真实广播与确认消费链先就绪再恢复，物理 Channel 最后激活。有限连接生命周期并非隐藏构造依赖，旧 binding 等待者已退出 | 无剩余问题；退役终态、动态 owner、Mesh 会话面后移、HTTP 回调 Map、连接中关闭及回滚责任均复核通过。直接测试 4 文件 27 项、Channel S7 已通过；最新组合根改动进入下面的最终验证，不重审未受影响范围 |

### HOST-C02 开发验证（2026-09-14）

| 验证 | 结果与复用边界 |
|---|---|
| 根目录 `pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` | 通过；覆盖静态生产调用图的输入/返回类型 |
| CLI 直接测试 | `node node_modules/vitest/vitest.mjs run <文件> --maxWorkers=1 --reporter=verbose`，8 文件 33 项通过：`serve/__tests__/` 下 access-surface、asset-maintenance-surface、local-conversation-owner-surface、executor-job-owner-surface、channels、conversation-surface，以及 `serve/lossless-data-plane-composition.test.ts`、`serve/startup-server-owner.test.ts`。首轮旧数组断言失败，修正后只重跑受影响文件；会话真实存储装载 4 项通过 |
| 原有 S7 定向结构/反例测试 | `node --import=tsx/esm --test --test-name-pattern <相关测试名> scripts/s7-entry-coverage.test.mjs`，18 组最终通过：本机 owner、执行调度、Workscene 探测及静态装配、Assignment 数据面/资源/制品、Evidence、会话接管、备份、计划迁移、Managed Host、设备生命周期/管理、Channel、配置投影、Advancement 和 Skill。失效文本反例改为实际路径，仅复验受影响组；未运行完整 S7 |
| 最后受影响结构检查 | 本机执行依赖封口、稳定 evidence 清理引用及 imports 收尾后，Managed Host、Assignment、Channel、Skill 的生产检查均通过 |
| 根目录 `pnpm cli:build` | 通过；仅 CLI 变更，不重复上游全量构建 |
| `git diff --check` | 通过 |

以上只记录开发阶段证据，最终结果见下文。未运行整包全测、A7 或重型组合验证，未启动单元 3/4。

### HOST-C02 最终验证计划

R2 通过后依次串行执行；当前输入下已通过的 binding、Channel 行为和真实 Advancement 恢复测试复用。只重验随后改动的组合根、当前 owner 激活边界及直接结构消费者，不运行整包、全量 S7/A7。到期未结束先检查具体进程与输出，不以换参数重跑代替定位。

| 顺序 | 命令/精确范围 | 失效输入、预算与截止 |
|---|---|---|
| 1 | 根目录 `pnpm --filter @zhixing/cli exec tsc -p tsconfig.json --noEmit` | 最后追加的当前 owner 激活检查与 Mesh 会话入口时点；预计 30 秒，截止 2 分钟 |
| 2 | CLI `node node_modules/vitest/vitest.mjs run src/serve/startup-server-owner.test.ts src/serve/__tests__/access-surface.test.ts --maxWorkers=1 --reporter=verbose` | 组合根与开放顺序断言受变更影响；预计 15 秒，截止 1 分钟 |
| 3 | 根目录 `node --import=tsx/esm --test --test-name-pattern '^(planned duty migration stays|managed host stays|device lifecycle stays|Device Administration reads|Advancement whole-domain exact-set)' scripts/s7-entry-coverage.test.mjs` | 启动恢复时点影响的五组已有生产约束及反例；Channel 同输入检查已通过，不重复；预计 15 秒，截止 1 分钟 |
| 4 | 根目录 `pnpm cli:build`；随后 `git -c core.safecrlf=false diff --check` | 更新 CLI 产物；上游未改动，不全量构建。预计 1 分钟，截止 3 分钟 |

代码审查若要求新增修复，只调整实际失效的验证范围。最终测试暴露问题按用户约定由执行者修复、复验，不增加额外审查轮次。

### HOST-C02 最终验证结果（2026-09-14）

| 验证 | 结果 |
|---|---|
| CLI 类型检查 | 通过；最后追加的 owner 激活检查和 Mesh 会话入口后移均包含在内 |
| 启动 owner 与静态装配图 2 文件 | 10/10 通过；Vitest 耗时 1.47 秒 |
| 上述 5 组 S7 生产约束/反例 | Device lifecycle、Device Administration、Advancement 首次通过；计划迁移检查仍匹配内联 owner 旧写法，Managed Host 反例仍依赖广播安装后的相邻注释，均已校准。补充 owner 查询和激活时清除连接意图的负向变异，仅重跑这 2 组，全部通过，耗时 6.53 秒 |
| 根目录 `pnpm cli:build` | 通过，命令耗时 6.55 秒；上游未变，不全量构建 |
| `git -c core.safecrlf=false diff --check` | 通过 |

复用修复阶段 4 文件 27 项中的有效行为证据（绑定、Channel、真实 Advancement 恢复）及 Channel S7 约束/反例；其中启动 owner 断言已按最终输入重跑。最终阶段没有生产修改，仅修正上述两处结构检查并复验；按用户约定不追加审查轮次。

最终代码 diff（同上算法，`packages/scripts`）SHA-256：`1eae977f5e9fe2659ca5e5d0d279184ffc491f41e3af9b41eeee4e1d2ad91cd4`。HEAD 保持 `139a3baf3bb5b9b33a44d8ac1b2a360641b6324d`；暂存代码指纹仍为原始 `3bb123ba99ce4759a8be55e6c9a75cb21d563f1112c5ded5cba44c3f9c6d19cc`。无未结束的测试/构建进程，无范围内未解决问题；四单元完成 2/4（50%），停止，等待下一单元授权。

## 已完成交接：HOST-C01

- 基线：`3f31c9b4a95e52bf1b5da1804e1740e103a8167b`；审查对象为 `git diff HEAD -- packages` 对应的当前工作区，必须同时看暂存和未暂存变更。既有发布、文档迁移变更不混入代码审查。
- 背景交接输入：59 个代码/测试文件；diff 文本按行以 LF 连接、UTF-8 编码的 SHA-256 为 `d2e393af38a22f2f6323e9caeaf0421ec57995ab9419236b141b7f347ba94d51`。审查开始前及结束时核对，发生变化只使受影响结论失效。
- 状态：2026-09-14 HOST-C01 已完成。R3 独立审查通过，R1 两项及 R2 遗漏全部关闭；最终构建、类型检查及定向回归全部通过，无范围内未解决问题。其余三个单元未开始。
- 实现交接：长期回调改为直接持有稳定依赖；当前 owner/连接/代际继续查询原责任者；身份投影沿主/子 Runtime、Profile 和确认链显式传入；home 沿入口、Host/角色 bootstrap、存储、窗口指引及日志链显式传递。未启动其余三个单元。
- 重点复核：状态聚合与第一方终态的绑定时点；启动/恢复/公开入口顺序；Mesh owner 变化；资源 governor 换代；无通道的合法分支；Executor-only 独立入口；同根存储、懒建场景、只读降级、指引动态重读；多 Runtime 身份隔离及子运行传递。此清单是风险提示，不限制完整范围检查。
- 复用证据：core 4 文件 158 项；orchestrator 25 文件 394 项（新增 Runtime 身份隔离单例随后单独通过）；CLI 24 文件 201 项（其中失败断言修正后 3 文件 13 项通过）；另 7 文件 82 项集成回归通过。只复用未受后续修改影响的结论；当前构建、类型检查和受影响闭包以文末最终验证结果为准。
- 补验证据：协议迟绑定状态订阅/防重绑断言、最新根绑定与配置消费者均已通过定向验证；无待补项。审查者未重复执行测试。
- 效率纠正：前一轮在独立审查前过早运行了较重的回归。现已停止扩大验证；先审查、集中修复，再复用有效证据完成最终验证。

### 交接与问题处置记录

| 轮次 | 请求/结论 | 处置与下一步 |
|---|---|---|
| HOST-C01 / 背景交接 | 对方已确认三个闭包、功能与生命周期保护及后续单元边界；未启动测试或修改文件 | 背景交接完成 |
| HOST-C01 / R1 | 不通过；固定 home 未覆盖秘密保护、发现/状态/日志/清理、REPL 重连及配置换代；无通道 Mesh 被致命判空 | 两项已核实；集中闭合全部消费者与合法缺席分支，再交受影响范围复核。审查者未修改文件或运行测试 |
| HOST-C01 / R2 | 原两项主要修正通过；剩 REPL 新建场景未给本地 workspace helper 传固定 home | 已补交互入口，helper 的 home 改必填，独立 workspace 命令也显式传入；三处生产调用者闭合 |
| HOST-C01 / R3 | 独立审查通过；完整 tracked diff 指纹 `4d25f47a7a11b520229a113f411205c3c2ea1beb3cb14ab7d973407fdd5f86e3`，两份未跟踪测试未变；三处 workspace 调用者显式根闭合 | 实际 REPL 组合回调定向 1/1 通过（14.77 秒，主体 318ms）可复用；进入最终验证，不追加审查轮次 |

R2 历史输入：当时 `git diff HEAD -- packages` 的同算法 SHA-256 为 `62d7ab0ed0102b86621f7c9b6aef10599865e5a1d8fd29b90813da7fee43899c`，另含未跟踪新增 `runtime/__tests__/core-host-home-binding.test.ts` 与 `config-command-home.test.ts`；共 81 个 TypeScript 文件。当时只读语法转译检查与 diff 空白检查通过，新增行为测试留至最终验证，现已通过。R1 未受影响的身份、状态订阅及运行期边界结论复用。

修复：Anchor/Executor 显式绑定秘密保护、发现锁、token、state/ready 与日志；REPL 及独立入口固定连接根，贯通懒启动、重连、远端 surface、配置换代及停止清理；公开配置文件独立固定位置并继续重读内容，SecretStore 不再由配置目录反推。Managed preflight 与本地 workspace 共用入口根。无通道 Mesh 的移除、迁移、拒新/排空/恢复共用既有有限空入站生命周期，必需责任者断言保留。未新增产品能力或改变其他单元。

最终验证范围：上游发生 server 路径参数及 providers 配置路径 API 变更，复核通过后先构建，再运行受影响的精确测试与 CLI 类型检查；未运行整包测试或重复之前的重型 7 文件回归。

### HOST-C01 最终验证计划

依次串行；命令输出与退出码保留在本对话，失败立即保留具体反例，不把超时当成功，不换参数重跑无结果范围。时间为执行预算而非测量结果；到期仍无可定位结果则停下检查运行方式。

| 顺序 | 命令/精确范围 | 失效原因与预算 |
|---|---|---|
| 1 | 根目录 `pnpm build` | server/providers API 新增参数，必须先更新上游声明及 CLI 产物；预计 4～6 分钟，截止 10 分钟 |
| 2 | providers：`node node_modules/vitest/vitest.mjs run src/__tests__/config-loader.test.ts --maxWorkers=1 --reporter=verbose` | 显式配置路径读写与既有语义；预计 15 秒，截止 1 分钟 |
| 3 | CLI：`pnpm --filter @zhixing/cli exec tsc --noEmit` | 所有生产调用者的必需输入及新上游声明；预计 30 秒，截止 2 分钟 |
| 4 | CLI 定向文件：runtime 下 core-host-home-binding、core-host-connection、config-command-home、config-command、surface-core-host-link、repl-local-view；security 的 secret-boundary；serve 的 daemon、stop、topology-command、startup-server-owner；根 __tests__ 的 startup-secret-store。均用 `node node_modules/vitest/vitest.mjs run <精确文件列表> --maxWorkers=1 --reporter=verbose` | 根绑定、重连/启动/停止、配置编辑与合法缺席的直接闭包；预计 2～3 分钟，截止 6 分钟 |
| 5 | CLI：`node node_modules/vitest/vitest.mjs run src/serve/__tests__/conversation-protocol-runtime.test.ts -t 'admits queued cancellation' --maxWorkers=1 --reporter=verbose`；`git diff --check` | 补协议迟订阅/单次终态绑定的未执行断言；预计 20 秒，截止 1 分钟 |

复用之前 core 身份/确认/存储、orchestrator 主子 Runtime、CLI 存储/指引/换代以及 7 文件集成证据；本轮 REPL workspace 定向 1/1 不重复。无需包全测、组合 baseline、制品打包或四单元总验收。

### HOST-C01 最终验证结果（2026-09-14）

| 验证 | 结果 |
|---|---|
| 根目录 `pnpm build` | 17 包全部通过，约 146 秒 |
| providers 配置路径定向测试 | 29/29 通过，Vitest 耗时 1.08 秒 |
| CLI `tsc --noEmit` | 首次发现文本调用包装函数把可选参数推断为必填；显式声明 `GovernedTextCall` 后复验通过 |
| 修正后的 `pnpm cli:build` | 通过；上游未再改动，不重复全量构建 |
| 上述 CLI 12 文件及新增 `serve/__tests__/governed-control-llm.test.ts` | 共 13 文件、98/98 通过，Vitest 耗时 28.13 秒 |
| 协议迟绑定 `admits queued cancellation` | 1/1 通过，29 项无关用例跳过；耗时 10.31 秒 |
| 实际 REPL 组合回调 | 复用 R3 前已通过的定向 1/1；不重复运行 |
| `git diff --check` | 通过 |

最终阶段只修复上述调用签名，并补充执行真实组合回调的 governor 换代回归，确认每次调用仍读取当前 governor。按用户约定自行修复、复验，不增加正式审查轮次。没有功能扩展，没有未结束的构建或测试进程。

最终 tracked 代码 diff 同算法 SHA-256：`6bf4713809a1b02620b8e28a968e6cde79176b11a7ce283fc7471a6929664238`；上述两份未跟踪测试相对 R3 未变。HEAD 保持基线，暂存区指纹保持 `d2e393af38a22f2f6323e9caeaf0421ec57995ab9419236b141b7f347ba94d51`。所有修复留在工作区，未操作 Git，未开始 HOST-C02。

完成通知：最终结果回传时，任务列表仍能找到审查方 ID，但消息工具两次返回 `thread not found`，故未送达；结果已在本文落盘供恢复时读取。这不影响此前已取得的 R3 通过结论，不触发重审或重跑测试。
