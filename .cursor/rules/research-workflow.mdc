---
description: 认知研究工作流 —— 研究类问题的产出必须遵循 _private → 审阅 → insights 的流转路径
globs: research/**
alwaysApply: false
---

# 研究工作流规则

## 核心原则

**研究产出禁止直接写入 `insights/`。** 必须遵循以下流转路径：

```
_private/（草稿）→ 用户审阅确认 → insights/（正式）
```

## 执行步骤

1. **记录原始问题**：在 `research/_private/questions/` 下创建草稿文件，文件名格式 `q{序号}-{短标题}.md`
2. **执行研究**：分析源码、查阅资料，将研究发现**写入同一个草稿文件**
3. **向用户呈现**：在对话中向用户展示研究结论，等待审阅
4. **用户确认后**：将草稿内容脱敏、通用化，提炼到 `research/insights/` 对应位置
5. **同步更新**：更新 `source-analysis/` 等关联文档

## 禁止行为

- ❌ 研究完直接写 `insights/`，跳过 `_private/` 和用户审阅
- ❌ 没有用户明确确认就将内容提炼到 `insights/`
- ❌ 在 `insights/` 中保留原始问题的个人化表述

## 判断标准

- 用户说"研究一下 X"、"X 是怎么实现的" → 产出先进 `_private/`
- 用户说"确认了，提炼吧" / "可以公开了" → 才写入 `insights/`
- `source-analysis/` 是事实性源码分析，可以随研究同步更新，不需要等审阅
